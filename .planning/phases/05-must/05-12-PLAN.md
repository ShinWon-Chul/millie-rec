---
phase: 05-must
plan: 12
type: tdd
wave: 4
depends_on: ["05-09"]
files_modified:
  - src/millie_rec/serving/dashboard_agg.py
  - src/millie_rec/serving/dashboard_api.py
  - tests/serving/test_dashboard.py
autonomous: true
requirements: [SERV-13]
lane: Serving (dashboard 최소형 Worker — Should, 시간 부족 시 wave 4 통째로 버린다)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/cascade.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/demo_api.py
  - src/millie_rec/serving/privacy_api.py
  - src/millie_rec/serving/persona.py
  - src/millie_rec/serving/bench.py
  - src/millie_rec/serving/__init__.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - artifacts/serving/**
  - results/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_dashboard_api.py
  - tests/serving/test_smoke.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "GET /api/dashboard 는 recommendations·events 테이블만 집계해 DashboardOut(kpi 6개 전부 n 병기 · latency p50/p95/p99 + by_stage[p50,p95] · quality 3키 · events_recent ≤50 · impressions_log · by_variant · by_hour · ab_table 셀×신규/기존 · mde_note 고정 문장) 을 준다(D-13 최소형, SERV-13 Should 부분)"
    - "빈 DB 에서도 200(kpi value 0.0·n 0, latency 0.0) 이고 파일·사용자 입력을 읽지 않는다; 백분위는 stdlib 정렬 인덱스"
    - "기존 GET /api/showcase(05-07) 와 build_router 시그니처는 불변이고 test_dashboard_api.py 가 그대로 통과한다"
  artifacts:
    - path: "src/millie_rec/serving/dashboard_agg.py"
      provides: "집계 전담(Advisor 확정 17 B6 신설, 아키 §9-3 목록 외 — 05-09 PROGRESS 1줄): SQL 상수 · percentile(bench 와 같은 규칙 중복) · build_dashboard(db, *, now=None) -> DashboardOut · KPI_KEYS · EVENTS_RECENT_N=50 · MDE_NOTE · P95_NOTE='서버 실측·참고용' · ERROR_NOTE='5xx 없음 — cascade 가 항상 200'"
      exports: ["build_dashboard", "percentile", "KPI_KEYS", "EVENTS_RECENT_N", "MDE_NOTE"]
      max_lines: 150
    - path: "src/millie_rec/serving/dashboard_api.py"
      provides: "GET /api/dashboard 라우트 1개 추가(build_dashboard(db) 호출만) — showcase 라우트·상수·build_router 시그니처 불변"
      contains: "def dashboard"
      max_lines: 150
    - path: "tests/serving/test_dashboard.py"
      provides: "DashboardOut 파싱 · kpi 6키 n 병기 · latency 손계산 · by_stage 키 · quality · events_recent 상한 · by_variant · by_hour · ab_table · 빈 DB 200 · 무흔적"
      min_lines: 70
  key_links:
    - from: "src/millie_rec/serving/dashboard_api.py"
      to: "src/millie_rec/serving/dashboard_agg.py"
      via: "from millie_rec.serving.dashboard_agg import build_dashboard (B6)"
      pattern: "from millie_rec.serving.dashboard_agg import"
    - from: "src/millie_rec/serving/dashboard_agg.py"
      to: "recommendations(latency_total_ms · latency_breakdown JSON · fallback_level · model_version · cell) · events(event_type · quality_flag · ts · candidate_set_id · selected)"
      via: "db.query(SQL_*) ? 바인딩 없음(집계만, 사용자 입력 0)"
      pattern: "FROM recommendations"
---

<objective>
Should 꼬리 3/3(D-13 최소형 — 버리는 순서상 별점보다 먼저 버림): **`GET /api/dashboard`** 라우트를 `dashboard_api.py`(05-07 showcase 옆) 에 1개 추가하고, **KPI·latency·quality·by_hour 집계 SQL·`percentile` 은 신규 `serving/dashboard_agg.py`(Advisor 확정 17 B6)** 에 둔다 — `dashboard_api.py` 는 라우트·상수만. 원천은 `recommendations`(D-12 전 응답 로그) + `events`(품질 게이트 플래그). Phase 6 DEMO-08 관제 대시보드 화면(`#/dashboard`, ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2) 의 데이터. `GET /metrics`·24h 집계는 **설계만**(SERV-14, 이 플랜 밖).

Purpose: **SERV-13** Should 부분(백엔드 서빙 01 §11), 축④ "관측 존재" 증거(`../.claude/rules/serving.md` 숫자 규칙 — 대시보드 숫자는 PDF 숫자가 아니다, `p95_latency_ms` 에 `note: "서버 실측·참고용"`).
Output: `dashboard_agg.py` 신규(≤150) + `dashboard_api.py` 라우트 1개 + 테스트 1 신규. 커밋 0. wave 4 병렬 겹침 없음(05-10 = state·nearline·compose·cascade, 05-11 = ratings_api·api).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-07-SUMMARY.md
@.planning/phases/05-must/05-09-SUMMARY.md
@src/millie_rec/serving/dashboard_api.py
@src/millie_rec/serving/schemas_should.py
@src/millie_rec/serving/schema.sql
@src/millie_rec/serving/db.py
@tests/serving/test_dashboard_api.py
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@../.claude/rules/serving.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**실행 명령:** `uv run pytest <대상> --no-header` 만, RED = `AssertionError`. 기존 `test_dashboard_api.py` 무수정(회귀). **star 의존:** `dashboard_agg.py` 는 `contracts`·`schemas_should`·`db`(타입) 만, `dashboard_api.py` 가 `dashboard_agg` 를 import(같은 슬라이스, B6). **간결함:** 두 파일 각 ≤150 — SQL 은 dashboard_agg 상단 상수, 백분위는 05-07 bench 와 같은 정렬 인덱스 규칙을 **3줄 중복 정의**(bench 는 CLI 모듈이라 import 하지 않는다). **서빙(serving.md):** "`dashboard_api.py`: `GET /api/dashboard`" · "관제 대시보드·Grafana 캡처는 '관측 존재'의 증거로만." 백엔드 01 §11 JSON 예시가 형태 정본(`by_stage` 값 `[p50, p95]`, 모든 KPI `n` 병기).
**완료 기준(자기 경로만):** ruff 클린(dashboard_agg.py·dashboard_api.py·test_dashboard.py) · `uv run pytest tests/serving/test_dashboard.py tests/serving/test_dashboard_api.py tests/test_architecture.py --no-header` failed 0.
</project_rules_inline>

<interfaces>
From src/millie_rec/serving/schemas_should.py (verbatim): `KpiValue(value: float, n: int | None, note: str | None)` · `AbRow(cell, segment, n, primary?, reader_open?, completion?, rating_mean?, first_completion?, p95_ms?, fallback_rate?)` · `LatencyBlock(p50, p95, p99, by_stage: dict[str, list[float]] = {})` · `DashboardOut(generated_at, window="all", kpi: dict[str, KpiValue], ab_table=[], mde_note?, latency: LatencyBlock, quality: dict[str, float] = {}, events_recent=[], impressions_log=[], by_variant: dict[str, int] = {}, by_hour: list[dict[str, int]] = [])`.
From schema.sql: `recommendations(recommendation_id, user_key, snapshot_id, model_version, cell, forced, fallback_level, latency_total_ms REAL, latency_breakdown TEXT JSON, weights, rows, ts)` · `events(…, event_type, ts, …, candidate_set_id, selected, quality_flag)` · `users(user_key, is_new, …)` · `candidate_sets(candidate_set_id, survey_variant, …)`.
From 05-07(실제 코드): `build_router(*, db, eval_table_path=…, latency_path=…) -> APIRouter`(`db` 인자 — 이 플랜이 처음 사용) · `Database.query(sql, params=()) -> list[sqlite3.Row]`.
**추가 시그니처(dashboard_agg.py — B6):**
```python
EVENTS_RECENT_N = 50
KPI_KEYS = ("qualified_reading_start_rate", "first_completion_rate_new", "fallback_rate", "p95_latency_ms", "error_rate", "active_user_keys")
MDE_NOTE = "데모 표본으로 검정하지 않음 — read-start +1%p 검출에 필요한 셀당 n 은 실서비스 트래픽으로 산정한다"
P95_NOTE, ERROR_NOTE = "서버 실측·참고용", "5xx 없음 — cascade 가 항상 200"   # revision C9
def percentile(sorted_ms: Sequence[float], p: float) -> float          # bench.py 와 같은 규칙, 중복 정의
def _parse(ts: str) -> datetime                                         # by_hour·freshness 용(C9), 실패 행 skip
def build_dashboard(db: Database, *, now: Callable[[], datetime] | None = None) -> DashboardOut
# dashboard_api.py 추가: from millie_rec.serving.dashboard_agg import build_dashboard
@router.get("/dashboard", response_model=DashboardOut) def dashboard() -> DashboardOut: return build_dashboard(db)
```
</interfaces>
</context>

<feature>
  <name>GET /api/dashboard 최소형(D-13) — recommendations·events 집계 → DashboardOut</name>
  <files>src/millie_rec/serving/dashboard_api.py, tests/serving/test_dashboard.py</files>
  <behavior>
    시드(테스트 헬퍼가 직접 INSERT): `users` 3명(`u1` is_new 1 cell A, `u2` is_new 1 cell B, `u3` is_new 0 cell A) · `recommendations` 6행(latency_total_ms `[10, 20, 30, 40, 50, 60]`, fallback_level `[0,0,0,1,0,3]`, model_version `hybrid_v1×3·hybrid_div_v1×2·fallback_v1`, cell `A,A,B,B,A,NULL`, latency_breakdown JSON `{"feature": 1, "pipeline": 8, "compose": 1, "total": 10}` 등, ts 시각 2개(21시 4건·22시 2건)) · `events`: reader_open 3(u1·u2·u3 각 1) · qualified_read 1(u1) · completion 1(u1) · impression 4(cand_1, selected 1·NULL·NULL·1) · preference_book_selected 2 · quality_flag `ts_future` 1건.
    - `# ── 계약 ──` 200 → `DashboardOut.model_validate`; `set(kpi) == set(KPI_KEYS)`; 모든 `kpi[k].n is not None`(`active_user_keys` 포함 — n = 유저 수); `kpi["p95_latency_ms"].note == "서버 실측·참고용"`; `window == "all"`; `generated_at` ISO
    - `# ── 정확성 ──` `latency.p50 == 30.0 or 40.0`(정렬 인덱스 규칙 — 손계산 `[10..60]` p50 = ceil(0.5·6)−1 = 2 → 30.0), `p95 == 60.0`, `p99 == 60.0`; `by_stage` 키 ⊇ `{"feature", "pipeline", "compose"}`(breakdown JSON 키 합집합 − total), 각 `[p50, p95]` 2원소
    - `kpi["fallback_rate"].value == pytest.approx(2/6)`(level ≥1 비율), `n == 6`; `kpi["error_rate"] == KpiValue(value=0.0, n=6, note="5xx 없음 — cascade 가 항상 200")`(revision C9 — 총 응답 수 n 병기); `kpi["active_user_keys"].value == 3.0`(users 수); `kpi["qualified_reading_start_rate"].value == pytest.approx(1/3)`(reader_open 유저 중 qualified_read 유저), `n == 3`; `kpi["first_completion_rate_new"].value == pytest.approx(1/2)`(is_new 유저 2명 중 completion 1명), `n == 2`; `kpi["p95_latency_ms"].value == 60.0`, `n == 6`
    - `quality == {"impression_receipt_rate": 0.5, "flagged_events": 1.0, "feature_freshness_s": <float ≥ 0>}`(impression 4건 중 selected 채워진 2 = 0.5; flagged 1; freshness = now − 마지막 events.ts 초)
    - `len(events_recent) <= 50`, 최신순, 각 항목 키 ⊇ `{"ts", "event_type", "user_key", "book_id", "recommendation_id", "model_version", "preference_snapshot_id"}`; 60건 INSERT 후 정확히 50
    - `impressions_log` 항목 키 == `{"candidate_set_id", "book_id", "position", "selected", "survey_variant"}`(candidate_sets 조인, 없으면 `"v1"` 기본)
    - `by_variant == {"hybrid_v1": 3, "hybrid_div_v1": 2, "fallback_v1": 1}`; `by_hour == [{"hour": 21, "n": 4}, {"hour": 22, "n": 2}]`(**revision C9: `_parse(ts).hour` UTC, 파싱 실패 행 건너뜀** — `ts="bad"` 행 1개를 넣어도 200 이고 by_hour 합 == 6)
    - `ab_table`: 행 `(cell A, new)`·`(A, existing)`·`(B, new)` 등 존재하는 조합만, `n` = 그 셀·세그먼트의 users 수, `fallback_rate`·`p95_ms` 는 그 셀의 recommendations 로, `primary`(reader_open 유저 비율)·`completion`·`first_completion` 채움, 나머지 None 허용; `mde_note == MDE_NOTE`
    - `# ── 안전성 ──` 빈 DB → 200, `kpi` 6키 전부 `value == 0.0`, `latency.p50 == p95 == p99 == 0.0`, `by_stage == {}`, 목록 전부 `[]`; 손상 `latency_breakdown` JSON(`"{bad"`) 행은 by_stage 계산에서 건너뜀(200); `showcase` 라우트 회귀(`test_dashboard_api.py`) 통과; 소스 grep: `Query(`·`request.`·`open(`·`Path(` 가 dashboard 함수 안에 없다(파일·사용자 입력 0 — showcase 의 상수 경로는 제외); 응답 텍스트 `"Traceback"` 없음
  </behavior>
  <implementation>Task 2 action.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): dashboard_agg 스텁(빈 DashboardOut) + dashboard_api 라우트 1줄 + test_dashboard.py 10건 → AssertionError</name>
  <files>src/millie_rec/serving/dashboard_agg.py, src/millie_rec/serving/dashboard_api.py, tests/serving/test_dashboard.py</files>
  <read_first>src/millie_rec/serving/dashboard_api.py 전문(`build_router`·`db` 인자 — 05-07 결과) · schemas_should.py L14-52 · schema.sql · db.py `query` · 백엔드 01 §11 JSON · .planning/phases/05-must/05-CONTEXT.md D-13 dashboard 최소형 문장 · tests/serving/test_dashboard_api.py 조립 헬퍼</read_first>
  <action>**`dashboard_agg.py` 스텁(B6)**: docstring `"""GET /api/dashboard 집계(백엔드 서빙 01 §11, D-13 최소형) — recommendations·events 만 읽는다. Advisor 확정 17 B6 로 dashboard_api 에서 분리."""`, 상수(`KPI_KEYS`·`EVENTS_RECENT_N`·`MDE_NOTE`·`P95_NOTE`·`ERROR_NOTE`), `percentile` → `return 0.0`, `_parse` 진짜, `build_dashboard(db, *, now=None)` → `DashboardOut(generated_at="", kpi={}, latency=LatencyBlock(p50=0, p95=0, p99=0))`. **`dashboard_api.py`**: `from millie_rec.serving.dashboard_agg import build_dashboard` + `@router.get("/dashboard", response_model=DashboardOut) def dashboard(): return build_dashboard(db)` — showcase 라우트·상수·`build_router` 시그니처 무변경. 테스트 10건: `test_dashboard_parses_kpi_six_keys_all_with_n_and_note` · `test_latency_percentiles_and_by_stage_hand_computed` · `test_kpi_rates_hand_computed` · `test_quality_block_three_keys` · `test_events_recent_capped_50_latest_first_keys` · `test_impressions_log_joined_with_survey_variant` · `test_by_variant_and_by_hour` · `test_ab_table_cells_by_segment_and_mde_note` · `test_empty_db_is_200_zero_values` · `test_corrupt_breakdown_skipped_and_no_file_or_query_access_grep`. RED 확인 + `test_dashboard_api.py` 회귀 통과.</action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_dashboard.py --no-header 2>&1 | tail -25; uv run pytest tests/serving/test_dashboard_api.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>`grep -c "def test_" tests/serving/test_dashboard.py` → 10 · RED `AssertionError` 만 · 회귀 failed 0 · `grep -c "def dashboard" src/millie_rec/serving/dashboard_api.py` == 1 · `grep -c "def build_dashboard" src/millie_rec/serving/dashboard_agg.py` == 1 · `grep -c "from millie_rec.serving.dashboard_agg import" src/millie_rec/serving/dashboard_api.py` == 1</acceptance_criteria>
  <done>RED 확인, 회귀 0. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): dashboard_agg SQL 집계 + KPI·latency·quality·목록·ab_table 조립 → 10 passed</name>
  <files>src/millie_rec/serving/dashboard_agg.py</files>
  <read_first>Task 1 스텁·테스트</read_first>
  <action>
    SQL 상수(사용자 입력 없음 — 파라미터도 없음):
    ```python
    SQL_RECS = "SELECT latency_total_ms, latency_breakdown, fallback_level, model_version, cell, ts, user_key FROM recommendations"
    SQL_USERS = "SELECT user_key, cell, is_new FROM users"
    SQL_EV_USERS = "SELECT user_key, event_type FROM events WHERE user_key IS NOT NULL GROUP BY user_key, event_type"
    SQL_EV_RECENT = ("SELECT ts, event_type, user_key, book_id, recommendation_id, model_version, preference_snapshot_id "
                     "FROM events ORDER BY ts DESC, rowid DESC LIMIT ?")
    SQL_IMPRESSIONS = ("SELECT e.candidate_set_id, e.book_id, e.position, e.selected, COALESCE(c.survey_variant, 'v1') AS survey_variant "
                       "FROM events e LEFT JOIN candidate_sets c ON c.candidate_set_id = e.candidate_set_id WHERE e.event_type = 'impression' ORDER BY e.ts DESC LIMIT ?")
    SQL_FLAGGED = "SELECT COUNT(*) FROM events WHERE quality_flag IS NOT NULL"
    SQL_LAST_TS = "SELECT MAX(ts) FROM events"
    ```
    조립(`build_dashboard`): recs = `db.query(SQL_RECS)`; `ms = sorted(r["latency_total_ms"] for r in recs if r["latency_total_ms"] is not None)`; `stages: dict[str, list[float]]` ← `json.loads(breakdown)` 성공 행의 `total` 제외 키(실패 행 skip) → `by_stage = {k: [percentile(sorted(v), 50), percentile(sorted(v), 95)]}`; `by_variant = Counter(model_version)`; `by_hour`: `Counter(_parse(r["ts"]).hour for r in recs if _ok(r["ts"]))`(**C9 — `_parse` 실패 행 skip**) → `[{"hour": h, "n": c} …]` 시 정렬; users·ev_users 로 `opened = {uk for reader_open}`, `qualified`, `completed`; `kpi = {"qualified_reading_start_rate": KpiValue(value=len(qualified & opened)/len(opened) if opened else 0.0, n=len(opened)), "first_completion_rate_new": (new ∩ completed)/new, n=len(new), "fallback_rate": sum(level ≥ 1)/len(recs), n=len(recs), "p95_latency_ms": KpiValue(value=percentile(ms, 95), n=len(ms), note=P95_NOTE), "error_rate": KpiValue(value=0.0, n=len(recs), note=ERROR_NOTE)(C9), "active_user_keys": KpiValue(value=float(len(users)), n=len(users))}`; `quality = {"impression_receipt_rate": selected 채워진/impression 수(0 이면 0.0), "flagged_events": float(flagged), "feature_freshness_s": (now − parse(last_ts)).total_seconds() if last_ts else 0.0}`; `ab_table`: `for (cell, seg), uks in groupby(users by (cell, "new" if is_new else "existing"))`: `AbRow(cell=cell, segment=seg, n=len(uks), primary=len(uks & opened)/len(uks), completion=len(uks & completed)/len(uks), first_completion=(seg=="new") and …, p95_ms=percentile(sorted(ms of recs where cell == cell), 95) or None, fallback_rate=…)`; `mde_note=MDE_NOTE`; `events_recent`·`impressions_log` 는 `dict(r)` 목록. `percentile` 3줄 중복 정의(bench 와 동일 규칙 — 주석). 전부 `dashboard_agg.py` 에(B6). GREEN: `uv run pytest tests/serving/test_dashboard.py tests/serving/test_dashboard_api.py tests/test_architecture.py --no-header` failed 0. `dashboard_agg.py` 가 150 초과면 **보고**(추가 분리는 Advisor).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_dashboard.py tests/serving/test_dashboard_api.py tests/test_architecture.py --no-header && wc -l src/millie_rec/serving/dashboard_agg.py src/millie_rec/serving/dashboard_api.py</automated>
  </verify>
  <acceptance_criteria>failed 0 · `wc -l` 두 파일 각 ≤150(초과 시 SUMMARY 에 보고) · `grep -c "FROM recommendations" src/millie_rec/serving/dashboard_agg.py` ≥ 1 · `grep -c "FROM recommendations" src/millie_rec/serving/dashboard_api.py` == 0(B6) · `grep -c '"서버 실측·참고용"' src/millie_rec/serving/dashboard_agg.py` == 1 · `grep -c '"5xx 없음 — cascade 가 항상 200"' …` == 1(C9) · `grep -c "execute(f" src/millie_rec/serving/dashboard_agg.py` == 0 · `grep -n '^EVENTS_RECENT_N = 50' …` 1행</acceptance_criteria>
  <done>10 passed + showcase 회귀 0. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · SUMMARY</name>
  <files>src/millie_rec/serving/dashboard_agg.py, src/millie_rec/serving/dashboard_api.py</files>
  <read_first>Task 2 결과</read_first>
  <action>`uv run ruff format src/millie_rec/serving/dashboard_agg.py src/millie_rec/serving/dashboard_api.py tests/serving/test_dashboard.py && uv run ruff check <같은 경로>` 클린. SUMMARY: RED/GREEN·KPI 6개 정의 표(분자/분모)·`TDD Gate Compliance`·"Phase 6 DEMO-08 이 읽는 필드"·변경 파일·"커밋하지 않는다".</action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/dashboard_agg.py src/millie_rec/serving/dashboard_api.py tests/serving/test_dashboard.py && uv run ruff check src/millie_rec/serving/dashboard_agg.py src/millie_rec/serving/dashboard_api.py tests/serving/test_dashboard.py && uv run pytest tests/serving/test_dashboard.py tests/serving/test_dashboard_api.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>ruff 클린 · failed 0 · `git status --short` 이 플랜 3파일 외 변경 없음</acceptance_criteria>
  <done>ruff 클린 · SUMMARY. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| 클라이언트 → `GET /api/dashboard` | 비인증 읽기, 쿼리 없음 |
| dashboard_agg → SQLite 집계 | 파라미터 없는 SELECT(LIMIT 상수만) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-12-01 | Information Disclosure | `events_recent` 에 user_key 노출 | accept | 가명 UUID 만(백엔드 01 §11 예시에 포함된 필드). 개인정보 없음 |
| T-05-12-02 | Denial of Service | 전 테이블 집계 비용 | accept | 데모 규모(bench 550행). 상한 `LIMIT 50`. 인덱스 `idx_recommendations_user_ts`(05-03) |
| T-05-12-03 | Tampering | SQL 인젭션 | mitigate | 파라미터가 없다(상수 LIMIT). `execute(f` 0 |
| T-05-12-04 | Information Disclosure | 예외 문자열 | mitigate | 손상 JSON skip, 빈 DB 0 값, 500 경로 없음 |
</threat_model>

<verification>`uv run pytest tests/serving/test_dashboard.py tests/serving/test_dashboard_api.py tests/test_architecture.py --no-header` failed 0 · ruff 클린 · `wc -l` 두 파일 ≤150 또는 보고 · 커밋 0</verification>
<success_criteria>**SERV-13 Should 부분**: `GET /api/dashboard` 가 `DashboardOut` 으로 파싱되고 KPI 6개 `n` 병기·latency 링버퍼(p50/p95/p99·by_stage)·quality 3키·목록·ab_table·고정 `mde_note` 가 손계산 테스트로 단정, showcase 회귀 0</success_criteria>
<output>`.planning/phases/05-must/05-12-SUMMARY.md` — RED/GREEN·KPI 정의 표·DEMO-08 필드·변경 파일·"커밋하지 않는다".</output>
