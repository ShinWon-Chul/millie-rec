---
phase: 05-must
plan: 07
type: tdd
wave: 2
depends_on: ["05-03", "05-04"]
files_modified:
  - src/millie_rec/serving/dashboard_api.py
  - src/millie_rec/serving/bench.py
  - tests/serving/test_dashboard_api.py
  - tests/serving/test_bench.py
autonomous: true
requirements: [SERV-13, SERV-10]
lane: Serving (dashboard_api showcase · bench Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/cascade.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/demo_api.py
  - src/millie_rec/serving/privacy_api.py
  - src/millie_rec/serving/persona.py
  - src/millie_rec/serving/__init__.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - src/millie_rec/evaluation/**
  - artifacts/serving/**
  - results/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_smoke.py
  - tests/serving/test_recommend_level0.py
  - tests/serving/test_api_weights.py
  - tests/app/**
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "GET /api/showcase 는 artifacts/serving/eval_table.json(키 rows·meta, 지표 값이 문자열 '0.063', 지표 키 recall@20) 을 schemas_should.EvalTable(split_mode·source·rows[EvalRow(variant, recall_at_20 float, …, p95_ms)]) 로 변환해 ShowcaseOut 으로 응답하고 data_notice 는 serving.md 2트랙 문장 그대로다(SERV-13 Must, Advisor 확정 11)"
    - "eval_table.json 이 없어도 /api/showcase 는 200(rows 빈 목록·split_mode 'unknown'(Advisor 확정 14)) 이고 500·예외 문자열이 없다; 파일 경로는 상수 2개만(사용자 입력으로 경로 구성 없음)"
    - "bench.py 는 python -m millie_rec.serving.bench 로 인자 없이 돌고 POST /api/preferences 로 user_key 50개를 먼저 만든 뒤 warmup 50 → 500 요청(user_key 라운드로빈, model 미지정, k=40) 의 클라이언트 측 전체 응답 시간을 재어 results/latency.json 에 p50/p95/p99·fallback_level 분포·variant 분포·카탈로그 규모·created_at·git_sha 를 쓴다 — 표준 라이브러리 urllib 만, httpx 없음(D-11, SERV-10)"
    - "percentile·summarize 는 HTTP 와 분리된 순수 함수로 손계산 테스트가 가능하고, 결과 파일에 user_key 원문은 기록되지 않는다"
  artifacts:
    - path: "src/millie_rec/serving/dashboard_api.py"
      provides: "build_router(*, db, eval_table_path=EVAL_TABLE_PATH, latency_path=LATENCY_PATH) → APIRouter(GET /api/showcase) · load_eval_table · 상수 PHILOSOPHY·METRIC_MAPPING·MEMORABLE_5·ROADMAP·DATA_NOTICE"
      exports: ["build_router", "load_eval_table", "DATA_NOTICE", "EVAL_TABLE_PATH", "LATENCY_PATH"]
      max_lines: 150
    - path: "src/millie_rec/serving/bench.py"
      provides: "percentile · summarize · make_users · run · main(argv) · 상수 N_USERS=50 WARMUP=50 N_REQUESTS=500 K=40 OUT_PATH"
      exports: ["percentile", "summarize", "main"]
      max_lines: 150
    - path: "tests/serving/test_dashboard_api.py"
      provides: "ShowcaseOut 파싱 · 문자열→float 변환 · split_mode meta · p95_ms 병기/None · data_notice verbatim · 파일 부재 200 · 손상 200"
      min_lines: 60
    - path: "tests/serving/test_bench.py"
      provides: "percentile 손계산 · summarize 키·분포 · user_key 미기록 · main 이 네트워크 없이 인자 파싱(--help/dry)"
      min_lines: 40
  key_links:
    - from: "src/millie_rec/serving/dashboard_api.py"
      to: "artifacts/serving/eval_table.json"
      via: "load_eval_table(path, latency_path): json.loads → rows[*]['recall@20'] → float → EvalRow(recall_at_20=…)"
      pattern: "recall@20"
    - from: "src/millie_rec/serving/dashboard_api.py"
      to: "src/millie_rec/serving/schemas_should.py"
      via: "ShowcaseOut·EvalTable·EvalRow·MetricMapping·Memorable 그대로"
      pattern: "from millie_rec.serving.schemas_should import"
    - from: "src/millie_rec/serving/bench.py"
      to: "contracts.DIR_RESULTS / 'latency.json'"
      via: "OUT_PATH = DIR_RESULTS / 'latency.json' — PDF 의 유일한 지연 숫자"
      pattern: "latency.json"
---

<objective>
Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md "Phase 5: 서빙 Must 완성") 의 **쇼케이스 비교표(Must)와 bench** 를 만든다: `serving/dashboard_api.py` 에 `GET /api/showcase`(SERV-13 의 Must 부분 — `GET /api/dashboard` 최소형은 wave 4 Plan 05-12), `serving/bench.py`(D-11, SERV-10). 라우터는 `build_router(*, db, eval_table_path, latency_path)` 팩토리, `create_app` 포함은 05-06 이 선택 import 로 이미 넣었다(wave 2 병렬 — 이 플랜이 끝나면 자동으로 붙는다).

Purpose: ROADMAP Success Criteria 4 "`results/latency.json` 에 p50/p95/p99 가 기록되고 p95 < 200ms 이며, `GET /api/showcase` 의 비교표가 `eval_table.json` 에서 `split_mode`·2트랙 `data_notice` 와 함께 응답" (**SERV-10·SERV-13**, 수용 기준 '로컬 p95 < 200ms'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)). PDF 축④ — 비교표는 Goodbooks(Track A) 숫자만, 데모 latency 는 `results/latency.json` 만(`../.claude/rules/serving.md` 숫자 규칙). 실측(`make bench`) 은 05-09 Advisor.
Output: `src` 2 신규 + 테스트 2 신규. 커밋 0. **`results/latency.json` 은 이 플랜이 만들지 않는다**(05-09 실측이 만든다 — `results/**` 는 must_not_touch).

**형제 플랜(wave 2 병렬):** 05-06 이 `api.py`·`cascade.py` 를, 05-08 이 `app/**` 를 고친다. 이 플랜은 `create_app` 을 import 하지 않는다 — 테스트는 `FastAPI()` + `include_router`. bench 는 HTTP 클라이언트라 서버 코드를 import 하지 않는다(`contracts` 만).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/schemas_should.py
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/db.py
@src/millie_rec/app/cli.py
@src/millie_rec/app/export.py
@src/millie_rec/evaluation/report.py
@scripts/collect_millie.py
@artifacts/serving/eval_table.json
@Makefile
@tests/app/test_export.py
@tests/test_architecture.py
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@../.claude/rules/serving.md
@../.claude/rules/data.md
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/references.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 오직 `uv run pytest <대상> --no-header`. 맨 `pytest` 금지(Python 3.9). RED 는 `AssertionError`/`E  assert` 만. 스텁 라우터는 라우트를 등록하고 틀린 값. `pytest.skip` 금지. **네트워크 절대 호출 안 함** — bench 테스트는 순수 함수(`percentile`·`summarize`) 와 `main(["--help"])`/인자 파싱만, 실제 HTTP 는 05-09 Advisor 가 `make bench` 로. 파일은 `tmp_path`.

**star 의존(`../.claude/rules/architecture.md`):** `dashboard_api.py` 는 `millie_rec.contracts`·`millie_rec.serving.schemas_should`·`millie_rec.serving.db`(타입) 만. `bench.py` 는 `millie_rec.contracts`(`SEED`·`DIR_RESULTS`·`DIR_ARTIFACTS`·`FALLBACK_*`) 만 — `millie_rec.evaluation.report.git_sha` 는 import 금지 → **6줄 중복 정의**(PATTERNS §7 Analog B, 중복 < 결합). `results/` 는 evaluation 소유 + "serving 의 `latency.json`" 만 허용(`architecture.md` 산출물 소유권 표).

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄. 고정 문장은 상단 대문자 상수. `argparse` 1개, 서브커맨드 없음(Makefile `bench` 가 인자 없이 부른다). 의존성 추가 0 — `httpx` 는 dev 전용이라 **`urllib.request`**(PATTERNS §7 Analog C). 백분위는 정렬 인덱스(stdlib).

**서빙·숫자 규칙(`../.claude/rules/serving.md`):** "`/api/showcase.data_notice`·쇼케이스 고지는 2트랙 문장: **"평가 = Goodbooks-10k(CC BY-SA 4.0) · 데모 카탈로그 = 밀리 공개 도서 페이지(수치·메타·표지 URL만, 텍스트 미노출, 요청 시 삭제) · 데모 이웃 = 콘텐츠 유사도 · 개인정보 무수집 · 서버 latency는 참고값"**" · "서버 `latency_ms`는 PDF 숫자가 아니다. p95는 `bench.py`(로컬 uvicorn 1 worker, warmup 50, 요청 500) → `results/latency.json`만." · `GET /api/showcase`(비교표는 Must) 는 `dashboard_api.py`.

**데이터 표기(`../.claude/rules/data.md`):** "비교표 숫자는 Track A(Goodbooks-10k)에서만 … 두 트랙의 숫자를 섞지 않는다." `p95_ms` 는 로컬 bench(데모 서버) 숫자라 **비교표 행에 병기하되 `source.p95 = "results/latency.json"` 으로 출처를 분리**한다(백엔드 01 §13 형태).

**완료 기준(자기 경로만):** `uv run ruff format --check src/millie_rec/serving/dashboard_api.py src/millie_rec/serving/bench.py tests/serving/test_dashboard_api.py tests/serving/test_bench.py && uv run ruff check <같은 경로>` 클린 · `uv run pytest tests/serving/test_dashboard_api.py tests/serving/test_bench.py tests/test_architecture.py --no-header` failed 0 · `uv run python -m millie_rec.serving.bench --help` 종료 코드 0. `make smoke`·`make bench` 는 wave 2 종료 후 Advisor(05-09).
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. schemas_should.py 는 변경 금지. "이 플랜이 만드는 공개 시그니처" 는 05-06(선택 import)·05-09(make bench)·05-12 가 소비. -->

From src/millie_rec/serving/schemas_should.py (verbatim):
```python
class EvalRow(_Strict): variant: Variant; recall_at_20: float; ndcg_at_10: float; ild_at_10: float; p95_ms: float | None = None
class EvalTable(_Strict): split_mode: str; source: dict[str, str]; rows: list[EvalRow]
class MetricMapping(_Strict): stage: str; metric: str
class ShowcaseBook(_Strict): book_id: int; title: str | None = None; image_url: str | None = None; reason: str | None = None; badge: BadgeOut | None = None
class PersonalCase(_Strict): seeds: list[ShowcaseBook]; recommendations: list[ShowcaseBook]
class Memorable(_Strict): claim: str; how_to_verify: str; route: str
class ShowcaseOut(_Strict):
    philosophy: str; eval_table: EvalTable; metric_mapping: list[MetricMapping]; personal_case: PersonalCase | None = None
    memorable_5: list[Memorable] = []; roadmap: list[str] = []; data_notice: str
```

`artifacts/serving/eval_table.json` 실형태(2026-09-06 실측 — Advisor 확정 11):
```json
{"rows": [{"variant": "pop", "recall@20": "0.063", "ndcg@10": "0.054", "ild@10": "0.764", "n_users": "2000", "split_mode": "holdout", "model_version": "pop_v1"}, …4행],
 "meta": {"dataset": "goodbooks-10k", "split_mode": "holdout", "n_users": 2000, "k_recall": 20, "k_rank": 10, "seed": 42, "git_sha": "8e5172b", "created_at": "2026-09-05T15:14:05+00:00"}}
```

From src/millie_rec/contracts.py: `SEED = 42` · `DIR_RESULTS = ROOT / "results"` · `DIR_ARTIFACTS = ROOT / "artifacts"` · `FALLBACK_PERSONALIZED, FALLBACK_CACHE, FALLBACK_SEGMENT_POP, FALLBACK_GLOBAL_POP = 0, 1, 2, 3` · `VARIANTS`.

From src/millie_rec/evaluation/report.py L53-66 (아날로그 — bench 에 중복 정의):
```python
def git_sha() -> str | None:
    try:
        out = subprocess.run(["git", "rev-parse", "--short", "HEAD"], capture_output=True, text=True, check=False, cwd=ROOT, timeout=5)
    except (OSError, subprocess.SubprocessError):
        return None
    return out.stdout.strip() or None if out.returncode == 0 else None
```

From scripts/collect_millie.py L66-69 (urllib 아날로그): `urllib.request.Request(url, headers=…)` + `urlopen(req, timeout=30)`.

From src/millie_rec/app/cli.py L113-135 (argparse 아날로그): `ap = argparse.ArgumentParser(prog=…, description=…)` · `args = ap.parse_args(argv)` · `logging.basicConfig(...)` · `if __name__ == "__main__": main()`.

**이 플랜이 만드는 공개 시그니처:**
```python
# serving/dashboard_api.py
EVAL_TABLE_PATH = DIR_ARTIFACTS / "serving" / "eval_table.json"     # 고정 경로 1
LATENCY_PATH = DIR_RESULTS / "latency.json"                           # 고정 경로 2
SPLIT_MODE_UNKNOWN = "unknown"   # Advisor 확정 14
EVAL_KEY_MAP = {"recall@20": "recall_at_20", "ndcg@10": "ndcg_at_10", "ild@10": "ild_at_10"}
PHILOSOPHY = "사용자가 취향 설정에서 직접 알려준 선호를 cold-start의 강한 prior로 사용하되, 읽기 행동이 쌓일수록 그 비중을 줄여 가는 시간 가변 가중치로 4단계 파이프라인(후보 → 순위 → 재순위 → 페이지 구성)을 조립한다. 목표는 클릭이 아니라 유효 독서 시작(QRS)이다."
METRIC_MAPPING = (("Candidate Retrieval", "Recall@20"), ("Ranking", "NDCG@10"), ("Re-ranking", "ILD@10"))
MEMORABLE_5 = ( (claim, how_to_verify, route) × 5 )   # 아래 action 값
ROADMAP = ("Reviewer-affinity", "Interleaving", "선호 교정 루프", "텍스트 난이도", "Two-Tower/ANN", "Kafka/K8s", "피크 autoscaling")
DATA_NOTICE = "평가 = Goodbooks-10k(CC BY-SA 4.0) · 데모 카탈로그 = 밀리 공개 도서 페이지(수치·메타·표지 URL만, 텍스트 미노출, 요청 시 삭제) · 데모 이웃 = 콘텐츠 유사도 · 개인정보 무수집 · 서버 latency는 참고값"
def load_eval_table(path: Path = EVAL_TABLE_PATH, latency_path: Path = LATENCY_PATH) -> EvalTable
def build_router(*, db: Database, eval_table_path: Path = EVAL_TABLE_PATH, latency_path: Path = LATENCY_PATH) -> APIRouter

# serving/bench.py
BASE_URL = "http://localhost:8000"; N_USERS, WARMUP, N_REQUESTS, K = 50, 50, 500, 40   # D-11. HTTP 는 urllib.request 만·git_sha 는 subprocess 중복 정의(Advisor 확정 15)
SERVE_HINT = "make serve 를 먼저 실행하세요"                                    # revision C8
OUT_PATH = DIR_RESULTS / "latency.json"; TIMEOUT_S = 10.0; SEEDS_PER_USER = 5
def percentile(sorted_ms: Sequence[float], p: float) -> float
def summarize(records: Sequence[tuple[float, int, str]], *, n_users: int, warmup: int, k: int, base_url: str,
              n_books: int | None, created_at: str, git_sha: str | None, seed: int = SEED) -> dict   # revision C8: seed 키 포함
def make_users(base: str, n: int, rng: random.Random) -> list[str]                    # POST /api/preferences × n → user_key 목록
def run(base: str, *, n_users: int, warmup: int, n_requests: int, k: int, out: Path, seed: int, n_books: int | None = None) -> dict   # revision C8: n_books 는 --n-books 인자(기본 None)
def main(argv: list[str] | None = None) -> None
```
</interfaces>
</context>

<feature>
  <name>GET /api/showcase(비교표 Must, D-13·Advisor 확정 11) + bench.py(D-11) — 실측은 05-09</name>
  <files>src/millie_rec/serving/dashboard_api.py, src/millie_rec/serving/bench.py, tests/serving/test_dashboard_api.py, tests/serving/test_bench.py</files>
  <behavior>
    **dashboard_api.py** (테스트: `tmp_path/"eval_table.json"` 에 위 실형태 JSON 을 **그대로** 써 넣고, `app = FastAPI(); app.include_router(build_router(db=Database(tmp_path/"t.db"), eval_table_path=…, latency_path=…))`)
    - `# ── 계약 ──` `GET /api/showcase` 200 → `ShowcaseOut.model_validate(r.json())`; `eval_table.split_mode == "holdout"`(meta 에서); `eval_table.source == {"metrics": "results/latest.csv", "p95": "results/latency.json"}`; `len(rows) == 4`, `[r.variant for r in rows] == ["pop", "cf", "hybrid", "hybrid_div"]`(파일 순서); `rows[0].recall_at_20 == 0.063 and isinstance(…, float)`, `rows[0].ndcg_at_10 == 0.054`, `rows[0].ild_at_10 == 0.764`, `rows[3].ild_at_10 == 0.684`(문자열 → float, Advisor 확정 11)
    - `p95_ms`: `latency.json` 없음 → 4행 전부 `p95_ms is None`; `tmp_path/"latency.json"` 에 `{"p95": 41.2, "p50": 22.1, "p99": 88.0}` 있으면 4행 전부 `p95_ms == 41.2`(D-11 bench 는 셀 혼합이라 variant 별 값이 없다 — 전체 p95 병기, 출처는 `source.p95`)
    - `philosophy` 는 `PHILOSOPHY` 상수와 동일 문자열이고 `"QRS"` 를 포함; `metric_mapping == [{"stage": "Candidate Retrieval", "metric": "Recall@20"}, {"stage": "Ranking", "metric": "NDCG@10"}, {"stage": "Re-ranking", "metric": "ILD@10"}]`; `len(memorable_5) == 5` 이고 각 `route` 가 `"#/"` 로 시작; `roadmap == [7개]`; **`data_notice == "평가 = Goodbooks-10k(CC BY-SA 4.0) · 데모 카탈로그 = 밀리 공개 도서 페이지(수치·메타·표지 URL만, 텍스트 미노출, 요청 시 삭제) · 데모 이웃 = 콘텐츠 유사도 · 개인정보 무수집 · 서버 latency는 참고값"`**(verbatim); `personal_case is None`(Should 미구현)
    - `# ── 정확성 ──` `load_eval_table(path, latency_path)` 직접 호출이 라우트와 같은 `EvalTable` 을 준다; `meta.split_mode` 가 `"temporal"` 이면 그대로 전달(거짓 temporal 을 만들지 않는다 — 파일 값 그대로)
    - `# ── 안전성 ──` `eval_table.json` 부재 → 200, `rows == []`, `split_mode == "unknown"`, `data_notice` 그대로; 손상 JSON(`"{not json"`) → 200 `rows == []`(로그 후 강하, `caplog` 에 ERROR 1건); 행에 `recall@20` 키가 없는 경우 그 행 건너뜀(나머지 행 유지); 응답 텍스트에 `"Traceback"` 없음; 라우터 소스에 `Query(`·`request.` 가 없다(경로를 사용자 입력으로 구성하지 않음 — 소스 grep)

    **bench.py** (네트워크 0)
    - `# ── 계약 ──` `percentile([1.0, 2.0, 3.0, 4.0], 50) == 2.0`, `percentile([...4개], 95) == 4.0`, `percentile([...4개], 99) == 4.0`, `percentile([5.0], 50) == 5.0`; `percentile([10, 20, …, 100](10개), 90) == 90.0`(`ceil(p/100*n)-1` 인덱스 — PATTERNS No Analog 스니펫)
    - `summarize(records=[(10.0, 0, "hybrid_div_v1"), (20.0, 0, "hybrid_v1"), (30.0, 1, "hybrid_div_v1"), (40.0, 3, "fallback_v1")], n_users=2, warmup=0, k=40, base_url="http://x", n_books=20, created_at="2026-09-07T00:00:00Z", git_sha="abc1234")` → 키 집합 == `{"p50", "p95", "p99", "n", "warmup", "users", "k", "seed", "fallback_levels", "variants", "catalog", "created_at", "git_sha", "base_url"}`(revision C8 `seed`); `n == 4`, `p50 == 20.0`, `p95 == 40.0`, `fallback_levels == {"0": 2, "1": 1, "2": 0, "3": 1}`(키 문자열 0..3 전부, 없는 단계 0), `variants == {"hybrid_div_v1": 2, "hybrid_v1": 1, "fallback_v1": 1}`, `catalog == {"n_books": 20}`, `users == 2`
    - `# ── 정확성 ──` `json.dumps(summarize(...))` 문자열에 user_key 형태(uuid4 hex) 가 없다 — records 에 user_key 가 들어갈 자리가 없음(튜플 3원소: ms·fallback_level·model_version); `summarize(records=[], …)` → `n == 0`, `p50 == p95 == p99 == 0.0`(빈 표본 안전)
    - `main(["--help"])` 는 `SystemExit(0)`; `main(["--n", "abc"])` 는 `SystemExit(2)`; 기본값: `argparse` 파서를 `build_parser()` 로 노출해 `build_parser().parse_args([])` 가 `base == "http://localhost:8000"`, `users == 50`, `warmup == 50`, `n == 500`, `k == 40`, `out == DIR_RESULTS / "latency.json"`, `seed == 42`, `n_books is None`; `parse_args(["--n-books", "9447"]).n_books == 9447`(revision C8)
    - **서버 없음 안내(revision C8):** `run("http://127.0.0.1:1", …)` 처럼 첫 `GET /health` 가 `URLError` 면 `SystemExit(1)` 이고 stdout 에 `"make serve 를 먼저 실행하세요"` — 테스트는 `pytest.raises(SystemExit)` + `capsys`(닫힌 포트라 외부 네트워크 호출 없음)
    - `# ── 안전성 ──` `run()` 은 테스트하지 않는다(네트워크). 대신 소스 grep: `"import httpx" not in src`, `"urllib.request" in src`, `"user_key" not in json.dumps(summarize(...))`, `"localhost" in BASE_URL`
  </behavior>
  <implementation>Task 2 action 그대로.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): dashboard_api·bench 스텁 + 테스트 2파일 → AssertionError</name>
  <files>src/millie_rec/serving/dashboard_api.py, src/millie_rec/serving/bench.py, tests/serving/test_dashboard_api.py, tests/serving/test_bench.py</files>
  <read_first>
    - src/millie_rec/serving/schemas_should.py L55-101(§13 모델 7개)
    - artifacts/serving/eval_table.json 전문(실형태 — 테스트 fixture 문자열로 복사)
    - src/millie_rec/app/export.py L19-41(`write_eval_table` — 생산자, 읽기만) · src/millie_rec/evaluation/report.py L53-66(`git_sha` 중복 정의 원본) · scripts/collect_millie.py L60-70(urllib) · src/millie_rec/app/cli.py L113-135(argparse)
    - Makefile `bench:` 타깃(인자 없이 `python -m millie_rec.serving.bench`)
    - .planning/phases/05-must/05-CONTEXT.md D-11 전문, D-13("dashboard 최소형" 은 05-12 — 이 플랜은 showcase 만), Claude's Discretion "`GET /api/showcase`"
    - .planning/phases/05-must/05-PATTERNS.md §6(dashboard Analog A·B — 키·타입 불일치 경고, 상수 문장 정본), §7(bench Analog A·B·C·테스트 가능성), "No Analog" `percentile` 스니펫
    - ../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md §13(JSON 예시 — `philosophy` 시작 문장·`metric_mapping`·`roadmap` 7개·`data_notice`)
    - ../.claude/rules/serving.md "숫자 규칙"·2트랙 고지 문장 · ../.claude/rules/data.md "두 트랙" 고정 문장
    - tests/app/test_export.py L20-55(파일 페이로드 테스트 관례)
  </read_first>
  <action>
    **1-a. `dashboard_api.py` 스텁** — docstring `"""GET /api/showcase(Must, 백엔드 서빙 01 §13) — eval_table.json → ShowcaseOut. GET /api/dashboard 최소형은 wave 4(05-12)."""`. 상수 전부 실제 값(`<interfaces>` + MEMORABLE_5 5개 — 아래 2-a 값). `load_eval_table` → `EvalTable(split_mode="x", source={}, rows=[])`(틀린 값). `build_router` → `APIRouter(prefix="/api")` + `GET /showcase` → `ShowcaseOut(philosophy="", eval_table=load_eval_table(...), metric_mapping=[], data_notice="")`(틀린 값).
    **1-b. `bench.py` 스텁** — docstring `"""로컬 bench(D-11): user_key 50 → warmup 50 → 500 요청 라운드로빈 → results/latency.json. 표준 urllib 만, PDF 의 유일한 지연 숫자."""`. 상수 전부 실제 값. `percentile` → `return 0.0`, `summarize` → `return {}`, `build_parser()` → 실제 argparse(기본값 정확 — 파싱 테스트는 스텁에서 통과해도 된다), `make_users`·`run` → `raise NotImplementedError`(테스트가 부르지 않음), `main(argv)` → `args = build_parser().parse_args(argv); run(...)`, `git_sha()` 중복 정의, `if __name__ == "__main__": main()`.
    **1-c. `tests/serving/test_dashboard_api.py`** (8건): `EVAL_JSON = """{…실형태 4행…}"""` 상수, `_client(tmp_path, *, eval_json=EVAL_JSON, latency=None)`.
    - `# ── 계약 ──` `test_showcase_parses_and_converts_string_metrics_to_float` · `test_showcase_p95_none_without_latency_json_and_filled_with` · `test_showcase_constants_philosophy_mapping_memorable_roadmap_notice_verbatim`
    - `# ── 정확성 ──` `test_load_eval_table_direct_matches_route_and_passes_split_mode_through`
    - `# ── 안전성 ──` `test_missing_eval_table_is_200_with_empty_rows_unknown` · `test_corrupt_eval_table_is_200_and_logged(caplog)` · `test_row_without_metric_keys_is_skipped` · `test_no_query_params_or_request_path_building_source_grep`
    **1-d. `tests/serving/test_bench.py`** (7건, revision C8 `test_run_exits_1_with_hint_when_server_down` 추가): `test_percentile_index_rule_hand_computed` · `test_summarize_keys_distributions_and_percentiles` · `test_summarize_empty_records_is_safe_zero` · `test_summarize_output_has_no_user_key` · `test_parser_defaults_match_d11_and_help_exits_zero` · `test_bench_uses_urllib_not_httpx_source_grep`
    **1-e. RED 확인:** `uv run pytest tests/serving/test_dashboard_api.py tests/serving/test_bench.py --no-header 2>&1 | tail -40` → 14건 중 파서·grep 2~3건 제외 failed, 사유 `AssertionError`/`E  assert`. `uv run pytest tests/test_architecture.py --no-header` → `3 passed`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_dashboard_api.py tests/serving/test_bench.py --no-header 2>&1 | tail -30; uv run pytest tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/serving/test_dashboard_api.py tests/serving/test_bench.py` → 8, 7
    - RED 출력에 `AssertionError`/`E  assert` 있고 `ImportError`·`AttributeError`·`TypeError` 없음
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(data\|app\|retrieval\|ranking\|reranking\|evaluation\|serving\.api\|serving\.cascade\)" src/millie_rec/serving/dashboard_api.py src/millie_rec/serving/bench.py` → 출력 없음
    - `grep -c "import httpx" src/millie_rec/serving/bench.py` == 0
  </acceptance_criteria>
  <done>신규 14건 대다수 AssertionError RED, 아키텍처 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): showcase 변환·상수 + bench 순수 함수·run·main → 14 passed, --help 0</name>
  <files>src/millie_rec/serving/dashboard_api.py, src/millie_rec/serving/bench.py</files>
  <read_first>
    - Task 1 스텁·테스트 · .planning/phases/05-must/05-PATTERNS.md §6·§7 스니펫
  </read_first>
  <action>
    **2-a. `dashboard_api.py`**
    ```python
    MEMORABLE_5 = (
        ("취향 설정은 시작점이자 갱신되는 상태다 — 재설정하면 새 앵커 행이 뜨고 이전 기록은 남는다", "취향 재설정 후 메인의 anchor_ 행 seed 가 바뀌고 GET /api/users/{key}/state 의 snapshots 가 2개", "#/refresh"),
        ("설문 7단계는 전부 다른 종류의 신호다 — 카테고리·기준·시드가 각각 행·배지·앵커를 만든다", "S3 기준을 바꾸면 카드 배지 종류가 바뀐다", "#/onboarding"),
        ("클릭이 아니라 유효 독서 시작(QRS)이 목표다 — 이어 읽기·완독 이벤트가 상태를 바꾼다", "뷰어에서 읽기 후 user_state_weights 의 beta 가 0 에서 올라간다", "#/reader/:id"),
        ("Top-K 가 아니라 페이지 구성까지가 메인 추천이다 — 5행 dedup·fresh_picks 는 내 카테고리 밖", "GET /api/recommend 의 rows 5개와 dedup_removed", "#/home"),
        ("정확도와 실서비스 제약을 같은 수준에서 — 예산 초과·예외는 fallback 0→3, 항상 200", "인스펙터 fallback_level 과 results/latency.json p95", "#/dashboard"),
    )
    def _rows(raw: dict, p95: float | None) -> list[EvalRow]:
        out = []
        for row in raw.get("rows", []):
            try:
                kw = {dst: float(row[src]) for src, dst in EVAL_KEY_MAP.items()}    # "0.063" → 0.063
            except (KeyError, TypeError, ValueError):
                log.warning("eval_table row skipped: %s", row.get("variant")); continue
            out.append(EvalRow(variant=row["variant"], p95_ms=p95, **kw))
        return out
    def load_eval_table(path=EVAL_TABLE_PATH, latency_path=LATENCY_PATH) -> EvalTable:
        """artifacts/serving/eval_table.json 그대로(02-CONTEXT D-09 형태) → EvalTable. 키 매핑+float 캐스팅은 EVAL_KEY_MAP(Advisor 확정 14). 부재·손상은 log.exception 후 빈 rows + split_mode='unknown'(500 없음)."""
        source = {"metrics": "results/latest.csv", "p95": "results/latency.json"}
        p95 = _read_p95(latency_path)
        try:
            raw = json.loads(path.read_text(encoding="utf-8")) if path.exists() else {}
        except (OSError, ValueError):
            log.exception("eval_table unreadable at %s", path); raw = {}
        split = (raw.get("meta") or {}).get("split_mode") or SPLIT_MODE_UNKNOWN
        return EvalTable(split_mode=split, source=source, rows=_rows(raw, p95))
    ```
    `_read_p95(path)`: 존재하면 `json.loads(...).get("p95")` → `float | None`, 예외는 None. `build_router(*, db, eval_table_path=…, latency_path=…)`: `GET /showcase` → `ShowcaseOut(philosophy=PHILOSOPHY, eval_table=load_eval_table(eval_table_path, latency_path), metric_mapping=[MetricMapping(stage=s, metric=m) for s, m in METRIC_MAPPING], personal_case=None, memorable_5=[Memorable(claim=c, how_to_verify=h, route=r) for c, h, r in MEMORABLE_5], roadmap=list(ROADMAP), data_notice=DATA_NOTICE)`. `db` 인자는 받아두기만(05-12 dashboard 가 쓴다). 요청마다 파일을 읽는다(작은 json, 재기동 없이 `make bench` 후 p95 반영 — 의도).
    **2-b. `bench.py`**
    ```python
    def percentile(sorted_ms, p):
        if not sorted_ms: return 0.0
        idx = min(len(sorted_ms) - 1, max(0, math.ceil(p / 100 * len(sorted_ms)) - 1))
        return float(sorted_ms[idx])
    def summarize(records, *, n_users, warmup, k, base_url, n_books, created_at, git_sha):
        ms = sorted(r[0] for r in records)
        levels = {str(lv): 0 for lv in range(FALLBACK_GLOBAL_POP + 1)}
        for _, lv, _v in records: levels[str(lv)] = levels.get(str(lv), 0) + 1
        variants = Counter(v for _, _, v in records)
        return {"p50": percentile(ms, 50), "p95": percentile(ms, 95), "p99": percentile(ms, 99), "n": len(records), "warmup": warmup,
                "users": n_users, "k": k, "fallback_levels": levels, "variants": dict(variants), "catalog": {"n_books": n_books}, "seed": seed,
                "created_at": created_at, "git_sha": git_sha, "base_url": base_url}
    def _get(url) -> dict:  urllib.request.Request(url, headers={"User-Agent": "millie-rec-bench/0.1"}); urlopen(timeout=TIMEOUT_S) → json.loads
    def _post(url, body) -> dict: Request(url, data=json.dumps(body).encode(), headers={"Content-Type": "application/json", …}, method="POST")
    def make_users(base, n, rng):
        meta = _get(f"{base}/api/meta/onboarding")
        cats = [c["name"] for c in meta["categories"] if c["supported"]][:3] or [c["name"] for c in meta["categories"]][:3]
        cand = _get(f"{base}/api/candidates/onboarding?categories={urllib.parse.quote(','.join(cats))}&n=60")
        pool = [i["book_id"] for i in cand["items"]]
        keys = []
        for _ in range(n):
            key = uuid.uuid4().hex
            seeds = rng.sample(pool, min(SEEDS_PER_USER, len(pool)))
            _post(f"{base}/api/preferences", {"user_key": key, "categories": cats, "criterion": "bestseller", "seeds": seeds, "candidate_set_id": cand["candidate_set_id"]})
            keys.append(key)
        return keys
    def _measure(base, key, k) -> tuple[float, int, str]:
        t0 = perf_counter(); d = _get(f"{base}/api/recommend?user_key={key}&k={k}"); return ((perf_counter() - t0) * 1000, int(d["fallback_level"]), str(d["model_version"]))
    def run(base, *, n_users, warmup, n_requests, k, out, seed, n_books=None):
        try: _get(f"{base}/health")                                   # revision C8: 서버 선행 확인
        except (urllib.error.URLError, OSError): print(SERVE_HINT); raise SystemExit(1)
        rng = random.Random(seed); keys = make_users(base, n_users, rng)
        for i in range(warmup): _measure(base, keys[i % len(keys)], k)
        records = [_measure(base, keys[i % len(keys)], k) for i in range(n_requests)]
        payload = summarize(records, n_users=n_users, warmup=warmup, k=k, base_url=base, n_books=n_books, seed=seed,
                            created_at=datetime.now(UTC).isoformat(timespec="seconds").replace("+00:00", "Z"), git_sha=git_sha())   # n_books 는 --n-books 인자(C8, 파일 읽기 없음)
        out.parent.mkdir(parents=True, exist_ok=True); out.write_text(json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8")
        log.info("bench p50=%.1f p95=%.1f p99=%.1f n=%d → %s", payload["p50"], payload["p95"], payload["p99"], payload["n"], out)
        return payload
    def build_parser(): ArgumentParser(prog="millie_rec.serving.bench", description="로컬 p95 bench → results/latency.json (D-11)"); --base(BASE_URL) --users(50) --warmup(50) --n(500) --k(40) --out(Path, OUT_PATH) --seed(SEED) --n-books(int, None)
    def main(argv=None): args = build_parser().parse_args(argv); logging.basicConfig(level=logging.INFO, format="%(levelname)s %(name)s: %(message)s"); payload = run(args.base, n_users=args.users, warmup=args.warmup, n_requests=args.n, k=args.k, out=args.out, seed=args.seed, n_books=args.n_books); print(json.dumps({k_: payload[k_] for k_ in ("p50", "p95", "p99", "n", "fallback_levels")}, ensure_ascii=False))
    ```
    (`git_sha()` 6줄 중복 정의 + 주석 "evaluation/report.py 와 동일 — serving 은 evaluation 을 import 못 한다".) `run` 의 HTTP 예외(`URLError`·`HTTPError`) 는 잡지 않고 그대로 올린다 — bench 는 CLI, 실패는 사람이 본다(05-09).
    **2-c. GREEN 확인:** `uv run pytest tests/serving/test_dashboard_api.py tests/serving/test_bench.py tests/test_architecture.py --no-header` → `18 passed`. `uv run python -m millie_rec.serving.bench --help; echo $?` → `0`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_dashboard_api.py tests/serving/test_bench.py tests/test_architecture.py --no-header && uv run python -m millie_rec.serving.bench --help >/dev/null && echo HELP_OK && wc -l src/millie_rec/serving/dashboard_api.py src/millie_rec/serving/bench.py</automated>
  </verify>
  <acceptance_criteria>
    - 요약 줄 `18 passed`, failed 0 · `HELP_OK` 출력 · `wc -l` 두 파일 ≤150
    - `grep -c '"recall@20": "recall_at_20"' src/millie_rec/serving/dashboard_api.py` == 1 · `grep -c "평가 = Goodbooks-10k(CC BY-SA 4.0)" src/millie_rec/serving/dashboard_api.py` == 1
    - `grep -c "urllib.request" src/millie_rec/serving/bench.py` ≥ 1 · `grep -c "httpx" src/millie_rec/serving/bench.py` == 0 · `grep -n '^N_USERS, WARMUP, N_REQUESTS, K = 50, 50, 500, 40' src/millie_rec/serving/bench.py` 1행
    - `grep -c 'DIR_RESULTS / "latency.json"' src/millie_rec/serving/bench.py src/millie_rec/serving/dashboard_api.py` → 각 1
    - `ls results/latency.json` → 없음(이 플랜은 실측하지 않는다)
  </acceptance_criteria>
  <done>17 passed · --help 0 · 파일 ≤150 · latency.json 미생성. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · 상수 문장 대조 · 05-09·05-12 인계 · SUMMARY</name>
  <files>src/millie_rec/serving/dashboard_api.py, src/millie_rec/serving/bench.py</files>
  <read_first>
    - Task 2 결과 2파일 · ../.claude/rules/serving.md 2트랙 고지 문장(글자 대조)
  </read_first>
  <action>
    1. `uv run ruff format src/millie_rec/serving/dashboard_api.py src/millie_rec/serving/bench.py tests/serving/test_dashboard_api.py tests/serving/test_bench.py && uv run ruff check <같은 경로>` 클린. 다른 serving 파일 포맷 금지(05-06 편집 중).
    2. `DATA_NOTICE` 를 `../.claude/rules/serving.md` 의 문장과 `grep -F` 로 글자 대조(1건). `ROADMAP` 7개를 백엔드 01 §13 과 대조.
    3. 재실행 → `18 passed`. SUMMARY 에: `build_router`·`load_eval_table` 시그니처(05-06 선택 import 가 `dashboard_router(db=db)` 로 부른다 — 시그니처 일치 확인 1줄) · bench 실행 절차(05-09 Advisor 용): `make serve` 터미널 1 → `make bench` 터미널 2 → `cat results/latency.json | jq .p95` · `latency.json` 키 목록 · "05-12 는 이 파일에 `GET /api/dashboard` 를 추가(db 인자 사용)".
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/dashboard_api.py src/millie_rec/serving/bench.py tests/serving/test_dashboard_api.py tests/serving/test_bench.py && uv run ruff check src/millie_rec/serving/dashboard_api.py src/millie_rec/serving/bench.py tests/serving/test_dashboard_api.py tests/serving/test_bench.py && uv run pytest tests/serving/test_dashboard_api.py tests/serving/test_bench.py tests/test_architecture.py --no-header && grep -c "평가 = Goodbooks-10k(CC BY-SA 4.0) · 데모 카탈로그 = 밀리 공개 도서 페이지(수치·메타·표지 URL만, 텍스트 미노출, 요청 시 삭제) · 데모 이웃 = 콘텐츠 유사도 · 개인정보 무수집 · 서버 latency는 참고값" ../.claude/rules/serving.md src/millie_rec/serving/dashboard_api.py</automated>
  </verify>
  <acceptance_criteria>
    - ruff 클린 · `18 passed` · grep 결과 두 파일 각 1
    - `git status --short` 에 이 플랜 `files_modified` 4개 외 이 Worker 변경 없음
  </acceptance_criteria>
  <done>ruff 클린 · 17 passed · 문장 대조 · SUMMARY. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → `GET /api/showcase` | 비인증 읽기. 쿼리 파라미터 없음 |
| `dashboard_api` → 로컬 파일 2개 | `artifacts/serving/eval_table.json`·`results/latency.json` 고정 경로 상수 |
| `bench.py`(클라이언트) → 로컬 서버 HTTP | `localhost` 기본, `--base` 로 바꿀 수 있음(운영자 CLI) |
| `bench.py` → `results/latency.json` | PDF 숫자의 유일한 출처 파일 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Elevation of Privilege | 파일 경로 조작(디렉터리 탈출) | mitigate | 경로는 모듈 상수 2개(`EVAL_TABLE_PATH`·`LATENCY_PATH`) 또는 `create_app` 주입 — 요청에서 경로를 만들지 않는다(테스트 소스 grep: `Query(`·`request.` 0건) |
| T-05-07-02 | Denial of Service | 손상·거대 JSON 파일 | mitigate | 예외는 `log.exception` 후 빈 표 200. 파일은 4행 KB 단위(생산자 `app/export.py`) |
| T-05-07-03 | Information Disclosure | 예외 문자열 | mitigate | 파일 부재·손상·키 누락 전부 잡아 200(테스트 3건) |
| T-05-07-04 | Information Disclosure | bench 결과에 user_key | mitigate | `records` 튜플은 (ms, level, model_version) — user_key 가 들어갈 자리가 없다(테스트 `…has_no_user_key`). 50개 user_key 는 프로세스 메모리에만 |
| T-05-07-05 | Tampering | bench 가 서버 상태를 오염(50 user·50 스냅샷·550 추천 로그) | accept | 로컬 DB(`data/local/millie.db`) 만. Railway 볼륨에 bench 를 돌리지 않는다(05-09 절차·serving.md "로컬 uvicorn 1 worker") |
| T-05-07-06 | Spoofing | `--base` 로 외부 URL 지정 | accept | 운영자 CLI 인자. 기본 `localhost`. 응답을 파일에 쓰기만 하고 실행하지 않는다 |
| T-05-07-07 | Repudiation | 숫자 재현 불가 | mitigate | `created_at`·`git_sha`·`seed`·`n`·`warmup`·`users`·`k`·`base_url` 전부 페이로드에(D-11) |
</threat_model>

<verification>
- `uv run pytest tests/serving/test_dashboard_api.py tests/serving/test_bench.py tests/test_architecture.py --no-header` → `18 passed`
- `uv run python -m millie_rec.serving.bench --help` 종료 0 · ruff 클린 · `wc -l` ≤150
- `DATA_NOTICE` 가 `../.claude/rules/serving.md` 문장과 글자 동일(`grep -c` 1)
- (05-09 Advisor) `make serve` + `make bench` → `results/latency.json` p95 < 200 · `curl localhost:8000/api/showcase | jq .eval_table.rows[0]`
- 커밋 0
</verification>

<success_criteria>
- **SERV-13(Must 부분)** `GET /api/showcase` 가 `ShowcaseOut` 으로 파싱되고 비교표 4행이 `eval_table.json` 문자열 값을 float 로 변환해 `split_mode`·`data_notice` 와 함께 응답(Advisor 확정 11) · **SERV-10** bench 가 D-11 모집단(user_key 50·warmup 50·500·k 40·model 미지정) 을 구현하고 `results/latency.json` 페이로드 키가 D-11 목록과 일치, 순수 함수 손계산 테스트 존재
- 의존성 0(urllib), 경로 상수 2개, 500 없음, 커밋 0, `results/` 미변경
</success_criteria>

<output>
완료 후 `.planning/phases/05-must/05-07-SUMMARY.md`: RED/GREEN 요약 줄 + AssertionError 발췌 · showcase 변환 규칙(문자열→float, split_mode ← meta, p95 병기 규칙) · bench 절차 3줄(05-09 용) · latency.json 키 목록 · `## TDD Gate Compliance` · 인계 시그니처 · 변경 파일 목록 · "커밋하지 않는다" 한 줄.
</output>
