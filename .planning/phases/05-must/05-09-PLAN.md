---
phase: 05-must
plan: 09
type: execute
wave: 3
depends_on: ["05-06", "05-07", "05-08"]
files_modified:
  - results/latency.json
  - report/draft.md
  - report/figures/p2_docs.png
  - ../PROGRESS.md
  - ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md
autonomous: false
requirements: [SERV-10, SERV-14, SERV-01, SERV-02, SERV-03, SERV-04, SERV-05, SERV-06, SERV-07, SERV-08, SERV-09, SERV-13]
lane: 조립(Advisor 직접 — 전역 게이트·실측·문서·Codex 리뷰, 04-04 관례)
no_commit: true   # 커밋하지 않는다 — 사용자 승인 후 Phase 1~5 일괄(Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/**          # 이 플랜은 코드를 고치지 않는다 — 게이트 실패는 소유 플랜에 수정 브리프
  - src/millie_rec/contracts.py
  - artifacts/serving/**
  - demo/**
  - tests/**
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "전역 게이트가 통과한다: uv run ruff format --check . && uv run ruff check . 클린, uv run pytest --no-header 가 327 을 넘는 passed 와 failed 0, make smoke PASS(ROADMAP Success Criteria 5)"
    - "make serve 로 띄운 서버에 make bench 가 user_key 50 → warmup 50 → 500 요청을 보내 results/latency.json 을 만들고 p95 < 200 이다 — PDF 의 유일한 지연 숫자(D-11, SERV-10, Success Criteria 4)"
    - "report/draft.md §4-3 실서비스 표의 응답 지연 행 증거 칸이 latency.json 의 p95 숫자 1개로 채워지고(손으로 쓴 숫자 없음), 관측 행은 '설계만 — 관측(/metrics·24h 집계, prometheus-client 미추가)' 로 표기된다(SERV-14 설계만, D-13)"
    - "/docs 캡처(report/figures/p2_docs.png) 가 Must 엔드포인트 9개 + /api/showcase 를 보여준다(Success Criteria 5 '/docs 캡처 확보')"
    - "Codex 필수 리뷰(privacy_api.py·db.py: /codex:review --scope working-tree + /codex:adversarial-review) 가 실행되고 지적이 필수 fix / 토론 / 무시 3분류로 ≤10줄 보고되며, 사용자 승인분만 수정 브리프로 반영된다"
    - "PROGRESS.md 결정 로그 2줄(cascade.py 신설 · D-04 seeds 경로 예외)과 개발일지 D77(6요소) 이 기록된다"
  artifacts:
    - path: "results/latency.json"
      provides: "p50 p95 p99 n warmup users k fallback_levels variants catalog created_at git_sha base_url"
      contains: "\"p95\""
    - path: "report/draft.md"
      provides: "§4-3 응답 지연 행 p95 실측 1줄 + 관측 행 '설계만' 표기 + §5-3 p95 실측 마커 해소"
      contains: "results/latency.json"
    - path: "report/figures/p2_docs.png"
      provides: "/docs 캡처(Swagger UI, 엔드포인트 10개)"
    - path: "../PROGRESS.md"
      provides: "결정 로그 2줄 + 결과 스냅샷 p95"
      contains: "cascade.py"
    - path: "../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md"
      provides: "### D77. 6요소 항목"
      contains: "### D77."
  key_links:
    - from: "results/latency.json"
      to: "report/draft.md §4-3"
      via: "p95 숫자 복사(손으로 쓰지 않음 — jq .p95 출력을 붙인다)"
      pattern: "p95"
    - from: "make bench"
      to: "make serve(8000)"
      via: "터미널 2개 — serve 가 떠 있어야 bench 가 돈다(Makefile 주석)"
      pattern: "bench"
---

<objective>
Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md "Phase 5: 서빙 Must 완성") 의 **wave 2 게이트 + Advisor 실측 + 문서 + Codex 리뷰**. 코드는 고치지 않는다 — 게이트 실패는 소유 플랜(`files_modified`) 에 수정 브리프로 되돌린다. 이 플랜의 산출물은 `results/latency.json`(PDF 숫자), `report/draft.md` 문장, `/docs` 캡처, PROGRESS·개발일지 기록, Codex 3분류 보고다.

Purpose: ROADMAP Success Criteria 4("`results/latency.json` 에 p50/p95/p99, p95 < 200ms", **SERV-10**) · 5("`uv run pytest -q`·`make smoke` PASS · `/docs` 캡처 확보") 의 실측 증거. **SERV-14** 는 D-13 에 따라 이 페이즈에서 **설계만** — PDF 초안에 "설계만 — 관측" 1줄 + `prometheus-client` 미추가 확인(코드 0). PDF 축④ "p95 200ms 예산이 계층 배치를 결정" 의 유일한 숫자 출처(`../.claude/rules/serving.md` 숫자 규칙). `../.claude/rules/codex-review.md` 필수 항목(`privacy_api.py`·`db.py`) 실행. 이 플랜은 Advisor(오케스트레이터) 세션이 직접 실행한다(`autonomous: false` — bench·캡처·Codex 결과를 사람이 본다).
Output: 파일 5개(코드 0). 커밋 0(사용자 승인 후 Phase 1~5 일괄).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-06-SUMMARY.md
@.planning/phases/05-must/05-07-SUMMARY.md
@.planning/phases/05-must/05-08-SUMMARY.md
@Makefile
@report/draft.md
@../PROGRESS.md
@../.assets/개발일지/README.md
@../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md
@../.claude/rules/serving.md
@../.claude/rules/local-run.md
@../.claude/rules/codex-review.md
@../.claude/rules/references.md
@../.claude/docs/docs-policy.md

<wave_gate>
## Wave 2 게이트 — 이 플랜 Task 1 이 곧 게이트다(Advisor 1회)

| 명령 | 기대 |
|---|---|
| `ls .planning/phases/05-must/05-0{6,7,8}-SUMMARY.md` | 3파일 존재 |
| `cd millie-rec && codegraph sync .` (서버 연결 시) | 인덱스 갱신 |
| `uv run ruff format --check . && uv run ruff check .` | 클린 |
| `uv run pytest --no-header` | failed 0, passed = 기준선 327 + wave 1 신규 + 05-06 신규 + 05-07 신규 + 05-08 신규(각 SUMMARY 요약 줄 합산 — 하드코딩하지 않는다, revision C10) |
| `uv run pytest tests/serving/test_smoke.py tests/app --no-header` | failed 0 — **revision A5 로 05-06·05-08 에서 이관**(test_smoke.py L175 가 `millie_rec.app.server` 를 import 하므로 wave 2 전체가 끝난 뒤에만 의미 있다) |
| `make smoke` | `smoke: PASS`(A5 이관 — wave 2 세 플랜 완료 후 첫 실행) |
| `wc -l src/millie_rec/serving/*.py src/millie_rec/app/*.py \| sort -n \| tail -4` | `schemas.py` 외 전부 ≤150 |
| `uv run python -c "import millie_rec.serving.dashboard_api, millie_rec.serving.cascade; print('ok')"` | `ok`(05-06 의 선택 import 가 실제로 붙었다) |
| `grep -rn "prometheus\|sentry" src/ pyproject.toml` | 출력 없음(SERV-14 설계만, 의존성 0) |
| `git status --short \| grep -v "^??" \| grep -c "src/millie_rec/contracts.py\|serving/schemas"` | `0`(freeze 무변경) |

게이트 실패 시: 실패 파일의 소유 플랜에 수정 브리프(원인·기대·실제 3줄) 재위임 → 그 플랜 acceptance 재확인 → 게이트 재실행. 이 플랜은 코드를 직접 고치지 않는다(5줄 이하 마무리만 예외 — `.claude/skills/slice-brief` 7).
</wave_gate>

<project_rules_inline>
**로컬 기동(`../.claude/rules/local-run.md`):** "평가·빌드도 로컬 1명령 … 클라우드 자원 전제 금지." bench 는 `make serve`(8000, 1 worker) 위에서만 — Railway 에 bench 를 돌리지 않는다. "배포는 로컬 통과 후에만" — Phase 7 은 이 게이트 뒤.

**숫자 규칙(`../.claude/rules/serving.md`·`../CLAUDE.md` §3-5):** "PDF 숫자는 `results/latest.csv`·`results/latency.json`(로컬 bench)만. 서버·mock 의 `latency_ms` 는 참고값." draft 에 숫자를 손으로 쓰지 않는다 — `jq` 출력을 붙인다.

**Codex(`../.claude/rules/codex-review.md`):** `privacy_api.py`(동의 철회·열람) · `db.py` 쓰기 헬퍼 → **필수** `/codex:review --scope working-tree` + `/codex:adversarial-review`. 큰 범위는 `--background`. "Advisor 가 지적을 필수 fix / 토론 / 무시 3분류로 ≤10줄 보고. 무시 사유 1줄씩. 사용자가 승인한 항목만 반영 — 최소 변경 원칙. 반영은 수정 브리프로 Worker 에게(5줄 이하면 Advisor 직접)." `contracts.py` 무변경이면 나머지는 선택 — Day 3 모델 freeze 뒤라 `/codex:adversarial-review` 를 cascade(예산·예외 가정) 에도 권장(선택).

**문서(`../.claude/docs/docs-policy.md`·`../.assets/개발일지/README.md`):** 개발일지 항목은 6요소 고정·12줄 이내·번호 전역 연속(마지막 D76 → **D77**). PROGRESS 결정 로그는 `| 날짜 | 결정 | 근거 | 반영 |` 표 형식(기존 행 관례). draft 문장은 humanize 문체(AI 티 제거 — `humanize-korean` 스킬 있으면 사용).

**참조 표기(`../.claude/rules/references.md`):** 모든 기록에 이름 + 경로 병기. `D-04` 만 쓰지 않고 `결정 D-04(.planning/phases/05-must/05-CONTEXT.md, dedup·비개인화 행)` 처럼.

**커밋 없음(Advisor 확정 10):** `git commit` 을 하지 않는다. 사용자 승인 후 Phase 1~5 일괄.
</project_rules_inline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 2 전역 게이트(위 표 9행) → 통과 시에만 다음</name>
  <files></files>
  <read_first>
    - .planning/phases/05-must/05-06-SUMMARY.md · 05-07-SUMMARY.md · 05-08-SUMMARY.md("동시 진행 중 실패 목록" 절 — 남은 실패가 있는지)
    - Makefile `smoke`·`serve`·`bench` 타깃
  </read_first>
  <action>
    `<wave_gate>` 표의 9행을 순서대로 실행하고 출력 요약 줄을 그대로 기록한다. `pytest` 요약 줄은 `N passed, M warnings in …` 형태를 SUMMARY 에 verbatim. 실패 시 소유 플랜으로 수정 브리프 → 재실행. 전부 통과하기 전에는 Task 2 로 가지 않는다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check . && uv run ruff check . && uv run pytest --no-header && make smoke && grep -rn "prometheus\|sentry" src/ pyproject.toml; test $? -eq 1 && echo NO_OBSERVABILITY_DEPS</automated>
  </verify>
  <acceptance_criteria>
    - ruff 두 명령 클린 · `uv run pytest --no-header` 요약 줄 failed 0, passed ≥ 450
    - `smoke: PASS` · `NO_OBSERVABILITY_DEPS` 출력
    - `wc -l` 결과 `schemas.py` 외 ≤150(SUMMARY 에 상위 4개 기록)
    - `git status --short` 에 `src/millie_rec/contracts.py`·`serving/schemas*.py` 없음
  </acceptance_criteria>
  <done>게이트 9행 통과, 요약 줄 기록. 커밋 없음.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: make serve → make bench → results/latency.json p95 < 200 확인 + /docs 캡처</name>
  <files>results/latency.json, report/figures/p2_docs.png</files>
  <read_first>
    - .planning/phases/05-must/05-07-SUMMARY.md "bench 절차 3줄"·latency.json 키 목록
    - .planning/phases/05-must/05-CONTEXT.md D-11(모집단 정의 — "Must 서빙 p95")·D-18(완료 형태)
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §3-2(볼륨 영구성 확인 절차 — 참고, 이 플랜은 로컬만)
  </read_first>
  <action>
    Advisor 가 순서대로 실행한다(사람 확인 전 자동 부분): (1) 터미널 1 `make serve`(8000) — 로그에 카탈로그 로드 확인. (2) 터미널 2 `uv run python -m millie_rec.serving.bench --n-books 9447`(C8) → 종료 시 stdout 의 `{"p50", "p95", "p99", "n", "fallback_levels"}` 한 줄 기록. (3) `jq '{p50, p95, p99, n, users, warmup, k, fallback_levels, variants, catalog, git_sha}' results/latency.json`. (4) `KEY=$(sqlite3 data/local/millie.db "SELECT user_key FROM users LIMIT 1")` 후 `curl -s "localhost:8000/api/recommend?user_key=$KEY" | jq '[.rows[].row_id, .fallback_level, .model_version, .cell]'` → 5행·0·셀 variant. (5) `curl -s localhost:8000/api/showcase | jq '.eval_table.split_mode, .eval_table.rows[0]'` · `curl -s localhost:8000/health | jq '{model_version, artifacts_loaded_at, nearline_last_run, db_row_count}'`. (6) `/docs` 캡처를 `report/figures/p2_docs.png` 로 저장(`mkdir -p report/figures`; Playwright 있으면 `uv run --with playwright python -c "from playwright.sync_api import sync_playwright as sp; p=sp().start(); b=p.chromium.launch(); pg=b.new_page(viewport={'width':1280,'height':1400}); pg.goto('http://localhost:8000/docs'); pg.wait_for_selector('.opblock'); pg.screenshot(path='report/figures/p2_docs.png', full_page=True); b.close(); p.stop()"`, 없으면 브라우저 수동 캡처). p95 ≥ 200 이면 서버를 그대로 두고 `make bench` 1회 재실행 후 두 값 모두 기록. 그 뒤 아래 `<how-to-verify>` 를 사용자에게 제시하고 멈춘다.
  </action>
  <what-built>
    Advisor 가 자동으로 실행한 것(사람 확인 전): 터미널 1 `make serve`(8000, 아티팩트 로드 — `artifacts/serving/books_kr.json` 9,447권) → 터미널 2 `uv run python -m millie_rec.serving.bench --n-books 9447`(revision C8 — `make bench` 는 인자 없이 `n_books: null`; 카탈로그 규모 기록을 위해 직접 호출. user_key 50 생성 → warmup 50 → 500 요청 → `results/latency.json`) → `jq '{p50, p95, p99, n, fallback_levels, variants, catalog}' results/latency.json` → `open http://localhost:8000/docs` 스크린샷을 `report/figures/p2_docs.png` 로 저장(macOS `screencapture -x -R<좌표>` 또는 브라우저 저장; Playwright 가 있으면 `uv run --with playwright python -c "..."` 로 자동 캡처 허용 — `../.claude/rules/data.md` playwright dev 전용 규칙과 동일).
    자동 검증 완료 항목: `results/latency.json` 존재·키 13개·`n == 500`·`users == 50`·`warmup == 50`·`k == 40`; `curl -s "localhost:8000/api/recommend?user_key=<bench 가 만든 키 1개>" | jq '[.rows[].row_id, .fallback_level, .model_version]'`(5행·level 0·셀 variant) ; `curl -s localhost:8000/api/showcase | jq '.eval_table.rows[0], .eval_table.split_mode'` ; `curl -s localhost:8000/health | jq '{model_version, artifacts_loaded_at, nearline_last_run}'`.
  </what-built>
  <how-to-verify>
    1. `cat results/latency.json | jq .p95` — **< 200** 인지 본다. 200 이상이면: 첫 실행은 cold(아티팩트 로드·JIT 없음)일 수 있으니 서버를 그대로 두고 `make bench` 를 1회 더 → 두 번째 값을 채택(SUMMARY 에 두 값 모두 기록). 그래도 ≥200 이면 사용자와 논의(`k` 는 40 고정, 모집단 변경 금지 — D-11 이 PDF p95 의 정의).
    2. `jq .fallback_levels results/latency.json` — `"0"` 이 대다수(≥ 450/500)인지. level 1·2 가 많으면 예산 초과가 잦다는 뜻 — 원인(`latency_breakdown` 의 어느 구간) 을 `jq '.latency_breakdown' <(curl -s "localhost:8000/api/recommend?user_key=<키>")` 로 본다.
    3. `jq .variants results/latency.json` — `hybrid_v1`·`hybrid_div_v1` 둘 다 있다(셀 A/B 배정 증거).
    4. 브라우저 `http://localhost:8000/docs` — 엔드포인트 목록에 `/health`·`/api/recommend`·`/api/meta/onboarding`·`/api/candidates/onboarding`·`/api/preferences`·`/api/events`·`/api/users/{user_key}/state`·`/api/users/{user_key}/data`·`/api/users/{user_key}/personalization`·`/api/showcase` 10개가 보이는지, 캡처 `report/figures/p2_docs.png` 가 그 화면인지.
    5. `http://localhost:8000/?source=api` — Phase 6 전이라 화면은 v1 이지만 콘솔 500 이 없는지만(선택).
  </how-to-verify>
  <resume-signal>"approved" 또는 p95 값과 문제(예: "p95 231, level 2 가 120건")</resume-signal>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && jq -e '.p95 < 200 and .n == 500 and .users == 50 and .warmup == 50 and .k == 40' results/latency.json && test -s report/figures/p2_docs.png && echo BENCH_AND_CAPTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `jq -e '.p95 < 200 and .n == 500 and .users == 50 and .warmup == 50 and .k == 40 and .seed == 42 and .catalog.n_books == 9447' results/latency.json` 종료 0
    - `jq '.fallback_levels["0"]' results/latency.json` ≥ 450
    - `jq '.variants | keys' results/latency.json` 에 `hybrid_v1`·`hybrid_div_v1` 둘 다
    - `test -s report/figures/p2_docs.png` 성립
    - 사용자 응답 "approved"(또는 문제 보고 후 재실행·재확인)
  </acceptance_criteria>
  <done>latency.json p95 < 200(사용자 확인) · /docs 캡처 파일 존재. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 3: draft.md P4 실서비스 문장 1줄(숫자는 latency.json 에서만) + 관측 '설계만' 표기 + Codex 필수 리뷰 → 3분류 보고 + PROGRESS·개발일지 기록</name>
  <files>report/draft.md, ../PROGRESS.md, ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md</files>
  <read_first>
    - report/draft.md §3-1(L141 "계층 배치의 기준은 지연 예산이다"), §4-3 표(L218-228 — `[Phase 5]` 마커 3개: 응답 지연·개인정보·모니터링), §5-2(L236 "데모의 완독 수는 이력 권수로 대신하며"), §5-3(L239 "p95 실측")
    - ../PROGRESS.md "결정 로그" 표 마지막 행 형식 · "결과 스냅샷" 절
    - ../.assets/개발일지/2026-09-06_*.md D76 항목(형식) · .planning/phases/05-must/05-08-SUMMARY.md 의 D77 초안
    - ../.claude/rules/codex-review.md "표준 흐름" 1~6 · "언제" 표
  </read_first>
  <action>
    **3-a. draft.md(문장은 humanize 문체, 숫자는 `jq` 출력 복사):**
    - §4-3 표 "응답 지연" 행 증거 칸: `` Phase 1 테스트 · p95 실측 `[Phase 5]` `` → `` Phase 1 테스트 · 로컬 bench p95 **{jq .p95}ms**(user_key 50·warmup 50·500요청·k 40, `results/latency.json`) `` — 숫자 1개.
    - §4-3 "개인정보" 행 증거 칸 `[Phase 5]` → `` `GET/DELETE /api/users/{key}/*` · 철회 후 level 3(테스트 `test_delete_personalization_then_level3_until_reconsent`) ``.
    - §4-3 "모니터링" 행 증거 칸 `[Phase 5]` → **"설계만 — 관측(`/metrics` Bearer·24h `book_stats` 집계는 설계만, `prometheus-client` 미추가; 대시보드 최소형은 `GET /api/dashboard`)"**(SERV-14, D-13).
    - §5-2 "데모의 완독 수는 이력 권수로 대신하며(실서비스는 완독 이벤트)" → "데모 서버는 완독 이벤트 수(`n_completed`) 를 쓰고, Track A 평가만 이력 권수로 대신한다" (D-07 이행 — 각주 갱신).
    - §5-3 "p95 실측" 마커 → "p95 실측 {값}ms(`results/latency.json`)".
    - `grep -c "\[Phase 5\]" report/draft.md` → 0 이 목표(남은 마커는 Phase 6·7 몫이면 그대로).
    **3-b. Codex 필수 리뷰(코드 수정 없이 실행·보고만):** `/codex:review --scope working-tree` → `/codex:adversarial-review`(privacy_api·db·cascade 예산/예외 가정 도전). 결과를 **필수 fix / 토론 / 무시** 3분류 ≤10줄로 SUMMARY 와 사용자 보고에. 필수 fix 는 사용자 승인 후 소유 플랜(05-05·05-03·05-06) 수정 브리프 — 이 플랜 안에서 코드를 고치지 않는다(5줄 이하면 Advisor 직접 허용).
    **3-c. PROGRESS.md 결정 로그(표 형식 그대로) — 아래 2행 + Advisor 확정 17(150줄 분할) 신설 파일 6개 각 1행 + 중복 허용 1행:**
    - 신설 파일(아키텍처 01 §9-3 목록 외, 확정 17): `serving/badges.py`(05-02 배지 로직) · `serving/onboarding_api.py`(05-04 meta·candidates) · `serving/rec_log.py`(05-06 추천 로그·rows 축약·breakdown 병합) · `app/staged.py`(05-08 StagedPipeline 이동) · `serving/ratings_api.py`(05-11) · `serving/dashboard_agg.py`(05-12) — 각 `| 09-06 | **§9-3 목록 외 파일 신설: <파일>** — <이유 1줄> | Advisor 확정 17(150줄) | <경로> |`
    - `| 09-06 | **`catalog_categories`(compose.py) ↔ `catalog_categories_meta`(onboarding_api.py) 중복 2곳 허용** — wave 1 독립성, `_shared` 는 3곳부터(architecture.md) | revision C11 | 두 파일 주석 |`
    - `| 09-06 | **Phase 5 서빙 파일 분할 예외: `serving/cascade.py` 신설**(아키텍처 01 §9-3 목록 외) — `api.py` 150줄 유지, user_key 해석·cascade 0→3·breakdown·추천 로그 담당 | 05-CONTEXT Claude's Discretion "api.py 150줄 유지 방법" · Advisor 확정 3(.planning/phases/05-must/05-06-PLAN.md) | `src/millie_rec/serving/cascade.py`, 아키 §9-3 표에 1행 추가 예정 |`
    - `| 09-06 | **결정 D-04(05-CONTEXT.md, dedup·비개인화 행) 의 예외: `seeds` 쿼리 cold-start(CLI·smoke·bench 준비) 는 Phase 2 1행 `trending` 형태 유지** — Must 5행은 `user_key` 경로, 2행은 `catalog` 있는 익명·level 2·3 | 기존 테스트 단정 무수정 원칙(python-tdd.md) · PATTERNS '권장 (i)' · Advisor 확정 7 | `serving/cascade.py::_cold_start`, `compose.build_response(rows=None)` |`
    - "결과 스냅샷" 절에 `p95 {값}ms (results/latency.json, 2026-09-06)` 1줄.
    **3-d. 개발일지 D77(6요소, 12줄 이내)** — 05-08 SUMMARY 초안을 다듬어 2026-09-06 파일 "의사결정" 절 끝에 추가. 제목·6요소는 05-08 초안 그대로, "결과" 에 p95 값과 `results/latency.json` 경로.
    **3-e. `.planning/STATE.md`·ROADMAP Phase 5 체크박스 갱신은 `/gsd-verify-work 5` 이후 — 이 플랜은 SUMMARY 만.**
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && jq -e '.p95 < 200 and .n == 500 and .users == 50' results/latency.json && grep -c "results/latency.json" report/draft.md && grep -c "설계만 — 관측" report/draft.md && grep -c "cascade.py" ../PROGRESS.md && grep -c "^### D77\." "../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md" && grep -c "\[Phase 5\]" report/draft.md; true</automated>
  </verify>
  <acceptance_criteria>
    - `jq -e '.p95 < 200 and .n == 500 and .users == 50' results/latency.json` 종료 0
    - `grep -c "results/latency.json" report/draft.md` ≥ 2 · `grep -c "설계만 — 관측" report/draft.md` == 1 · `grep -c "\[Phase 5\]" report/draft.md` == 0
    - `grep -c "cascade.py" ../PROGRESS.md` ≥ 1 · `grep -c "seeds" ../PROGRESS.md` ≥ 1(D-04 예외 행)
    - `grep -c "^### D77\." ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md` == 1 이고 그 항목에 `**문제:**`·`**동기:**`·`**선택지:**`·`**근거:**`·`**장애:**`·`**결과:**` 6개 전부
    - `ls report/figures/p2_docs.png` 존재
    - SUMMARY 에 Codex 3분류 표(필수 fix / 토론 / 무시) 와 `git log --oneline | head -1` 불변 기록
    - `git diff --stat -- src/` → 출력 없음(이 플랜은 코드 무변경; Codex 필수 fix 를 사용자가 승인해 반영했다면 그 파일과 브리프 번호를 SUMMARY 에 명시)
  </acceptance_criteria>
  <done>draft 4곳 갱신·Codex 3분류 보고·PROGRESS 2행·D77·캡처. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench 클라이언트 → 로컬 서버(8000) | 같은 머신. 50 user·550 요청이 `data/local/millie.db` 에 쓰인다 |
| Codex 리뷰 도구 → 작업 트리 소스 | 외부 모델에 코드가 전송된다(밀리 데이터·개인정보 없음 — 소스만) |
| draft.md → PDF | 숫자는 `results/` 파일에서만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Repudiation | PDF p95 숫자 재현 불가 | mitigate | `latency.json` 에 `created_at·git_sha·seed·n·warmup·users·k·base_url·catalog.n_books`(D-11). draft 에 조건을 병기 |
| T-05-09-02 | Tampering | 로컬 DB 에 bench 데이터 잔존 | accept | `data/local/millie.db` 는 gitignore·로컬. Railway 볼륨에는 bench 를 돌리지 않는다(local-run.md) |
| T-05-09-03 | Information Disclosure | Codex 로 소스 전송 | accept | `artifacts/`·`data/`·`.assets/` 는 리뷰 범위 밖(`--scope working-tree` 는 diff 만). 밀리 텍스트·과제 PDF 미포함 확인 |
| T-05-09-04 | Denial of Service | bench 중 서버 과부하 | accept | 1 worker·순차 요청(동시성 1) — bench 는 부하 테스트가 아니라 지연 측정 |
</threat_model>

<verification>
- Wave 2 게이트 9행 통과(Task 1)
- `results/latency.json` p95 < 200, n 500, users 50(Task 2 사람 확인 + Task 3 jq)
- `report/draft.md` `[Phase 5]` 마커 0, p95 숫자 1개(파일 출처), "설계만 — 관측" 1줄
- `report/figures/p2_docs.png` 존재
- Codex 필수 리뷰 3분류 보고 · PROGRESS 2행 · D77 · 커밋 0
</verification>

<success_criteria>
- **SERV-10** p95 < 200 실측 파일 · **SERV-14** "설계만" 표기 + 의존성 0 확인 · Success Criteria 5(전역 pytest·smoke·`/docs` 캡처) · Success Criteria 4 전반(latency.json) · 나머지 SERV Must 11개는 게이트(전역 pytest) 로 회귀 확인
- Codex 필수 리뷰 완료(`../.claude/rules/codex-review.md` "필수에 해당하면 안내가 아니라 실행 후 3분류 보고까지가 완료 조건")
- 커밋 0 — 다음은 `/gsd-verify-work 5` 또는 wave 4(Should) 착수
</success_criteria>

<output>
완료 후 `.planning/phases/05-must/05-09-SUMMARY.md`: 게이트 9행 출력 요약 · `latency.json` 핵심 값(p50/p95/p99·fallback_levels·variants) · bench 1회/2회 값 · draft 변경 4곳 diff 발췌 · Codex 3분류 표 · PROGRESS 2행·D77 인용 · 캡처 경로 · "커밋하지 않는다 — 사용자 승인 후 Phase 1~5 일괄" · **wave 4(Should) 착수 여부 판단 1줄**(Day 4 시간 잔여 기준 — "시간 부족 시 통째로 버린다", Advisor 확정 1).
</output>
