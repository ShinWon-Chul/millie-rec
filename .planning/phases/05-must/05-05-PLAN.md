---
phase: 05-must
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/serving/privacy_api.py
  - tests/serving/test_privacy_api.py
autonomous: true
requirements: [SERV-07]
lane: Serving (privacy_api Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/demo_api.py
  - src/millie_rec/serving/persona.py
  - src/millie_rec/serving/__init__.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - artifacts/serving/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_smoke.py
  - tests/serving/test_recommend_level0.py
  - tests/serving/test_api_weights.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "GET /api/users/{user_key}/state 는 users 행이 있으면 UserStateOut(consent·cell·is_new·library 3분류 added/reading/completed·snapshots(최신 1개 active)·user_state_weights) 를, 없으면 404 를 준다(백엔드 01 §8, SERV-07)"
    - "GET /api/users/{user_key}/data 는 users 행 + preference_snapshots·events·ratings·recommendations 전 행을 dict 목록으로 돌려주고 note 는 '가명 user_key 외 개인정보 없음' 이다(열람권, §9)"
    - "DELETE /api/users/{user_key}/personalization 은 preference_snapshots·events·ratings·recommendations 행을 지우고 users.consent=0 으로 남기며 deleted 에 테이블별 rowcount 를 주고, 주입된 state.forget(user_key)·cache.invalidate(user_key) 를 부른다 — 멱등(두 번째 호출은 deleted 전부 0·200)(§10, D-08)"
    - "user_key 경로 파라미터는 ^[A-Za-z0-9_-]{1,64}$ 만 허용하고 그 외는 422 — SQL 은 전부 ? 바인딩, 응답 어디에도 예외 문자열이 없다"
  artifacts:
    - path: "src/millie_rec/serving/privacy_api.py"
      provides: "build_router(*, db, catalog=None, weights=None, state=None, cache=None) → APIRouter(GET state · GET data · DELETE personalization) · USER_KEY_RE · library 3분류 SQL"
      exports: ["build_router", "USER_KEY_RE", "DELETE_TABLES"]
      max_lines: 150
    - path: "tests/serving/test_privacy_api.py"
      provides: "state 스키마·library 3분류·active 스냅샷·weights 주입 · data 열람 4테이블·note · DELETE 카운트·consent=0·멱등·forget/invalidate 호출 · 404 · 422 형식 · 인젭션 · 무흔적"
      min_lines: 100
  key_links:
    - from: "src/millie_rec/serving/privacy_api.py"
      to: "src/millie_rec/serving/schemas.py"
      via: "UserStateOut·LibraryBook·SnapshotSummary·UserDataOut·PersonalizationDeleted 그대로"
      pattern: "from millie_rec.serving.schemas import"
    - from: "src/millie_rec/serving/privacy_api.py"
      to: "state.forget / cache.invalidate (duck-typed 주입)"
      via: "DELETE 후 if state is not None: state.forget(user_key); if cache is not None: cache.invalidate(user_key)"
      pattern: "\\.forget\\("
    - from: "src/millie_rec/serving/privacy_api.py"
      to: "SQLite users/preference_snapshots/events/ratings/recommendations"
      via: "db.connect() + ? 바인딩 SELECT/DELETE/UPDATE"
      pattern: "DELETE FROM .* WHERE user_key = \\?"
---

<objective>
Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md "Phase 5: 서빙 Must 완성") 의 **열람·삭제·철회 API** 를 TDD 한 사이클로 만든다: `serving/privacy_api.py` — `GET /api/users/{user_key}/state` · `GET /api/users/{user_key}/data` · `DELETE /api/users/{user_key}/personalization`. 라우터는 `build_router(*, db, catalog, weights, state, cache)` 팩토리로 의존을 받고 `create_app` 배선은 Plan 05-06(Advisor 확정 2). "DELETE 이후 `/api/recommend` 가 항상 level 3" 의 **끝단 검증은 05-06 `test_cascade.py`** 가 한다 — 이 플랜은 `users.consent=0`·행 삭제·콜백 호출까지를 단정한다.

Purpose: ROADMAP Success Criteria 2 후반 "`DELETE …/personalization` 이후는 항상 level 3" 의 삭제·철회 측(**SERV-07**, 수용 기준 '동의 철회 → 비개인화 fallback'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)). PDF 축④ 실서비스 "개인정보 — 가명 키, 최소 수집, 동의 철회 시 개인 피처 차단, 열람·삭제권"(main §7, 아키텍처 01 §3-8). Codex **필수 리뷰** 대상(`../.claude/rules/codex-review.md` — 05-09 에서 실행).
Output: `src` 1 신규 + 테스트 1 신규. 커밋 0.

**형제 플랜 불변:** `db.py` 는 `connect()` 만(05-03 헬퍼 미의존, `with con:` 커밋). `state`·`cache` 는 **`object | None` duck-typed** — `StateStore`·`Level1Cache` 를 import 하지 않는다(05-01·05-03 이 같은 시각에 만드는 중). `weights` 는 `Callable[[UserState], dict[str, float]] | None`(create_app 과 같은 타입, `ranking` import 없음).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/api.py
@src/millie_rec/serving/db.py
@src/millie_rec/serving/schema.sql
@tests/serving/test_smoke.py
@tests/test_architecture.py
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@../.claude/rules/serving.md
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/codex-review.md
@../.claude/rules/references.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 오직 `uv run pytest <대상> --no-header`. 맨 `pytest` 금지(Python 3.9). RED 는 `AssertionError`/`E  assert` 만 — 스텁 라우터는 3 라우트를 등록하고 틀린 값을 준다. `pytest.skip` 금지. 기존 테스트 단정 수정 금지. SQLite `tmp_path`, 시간 `now` 주입, 네트워크 0. 3종 구분 주석.

**star 의존(`../.claude/rules/architecture.md`):** `privacy_api.py` 는 `millie_rec.contracts`·`millie_rec.serving.schemas`·`millie_rec.serving.db`(타입) 만 import. `Catalog` Protocol 로 카탈로그(제목·표지 조인) 를 받는다.

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄. SQL 은 상단 상수. 핸들러 본문 ≤15줄. 클래스 없음.

**서빙(`../.claude/rules/serving.md`):** "HTTP 핸들러는 전부 동기 `def`." · "`privacy_api.py`: `GET /api/users/{key}/state` · `GET /api/users/{key}/data`(열람) · `DELETE /api/users/{key}/personalization`(삭제·철회 → consent=false → level 3)." · "`consent=false`는 항상 3, 가중치 0." · 오류(백엔드 01 §0): 미존재 `404 {"detail": "…"}`, 검증 실패 `422`.

**Codex(`../.claude/rules/codex-review.md`):** `privacy_api.py` 는 **필수** — `/codex:review --scope working-tree` + `/codex:adversarial-review` 를 05-09(Advisor) 가 실행. Worker 는 f-string SQL 0건·예외 문자열 미노출·경로 파라미터 형식 검증을 코드로 보장한다.

**완료 기준(자기 경로만):** `uv run ruff format --check src/millie_rec/serving/privacy_api.py tests/serving/test_privacy_api.py && uv run ruff check <같은 경로>` 클린 · `uv run pytest tests/serving/test_privacy_api.py tests/test_architecture.py --no-header` failed 0. `make smoke` 는 wave 1 종료 후 Advisor 1회.
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. schemas.py 는 변경 금지. "이 플랜이 만드는 공개 시그니처" 는 05-06 이 verbatim 소비. -->

From src/millie_rec/serving/schemas.py (verbatim):
```python
class LibraryBook(_Strict): book_id: int; title: str | None = None; image_url: str | None = None
class SnapshotSummary(_Strict): snapshot_id: str; created_at: str; categories: list[str] = []; criterion: str | None = None; active: bool = False
class UserStateOut(_Strict):
    user_key: str; consent: bool; cell: Cell; is_new: bool
    library: dict[str, list[LibraryBook]]  # added | reading | completed
    snapshots: list[SnapshotSummary] = []; user_state_weights: dict[str, float] = {}; nearline_lag_s: float | None = None
class UserDataOut(_Strict):
    user: dict[str, Any]; snapshots: list[dict[str, Any]] = []; events: list[dict[str, Any]] = []
    ratings: list[dict[str, Any]] = []; recommendations: list[dict[str, Any]] = []
    exported_at: str; note: str = "가명 user_key 외 개인정보 없음"
class PersonalizationDeleted(_Strict): user_key: str; consent: bool = False; deleted: dict[str, int]
```

From src/millie_rec/contracts.py: `UserState(user_id, explicit_seeds, history, session, context)` · `Catalog.meta(book_ids) -> list[dict]`.

From src/millie_rec/serving/schema.sql (SELECT/DELETE 대상): `users(user_key, created_at, consent, cell, is_new)` · `preference_snapshots(snapshot_id, user_key, created_at, reading_time, categories JSON, criterion, subcategories JSON, seeds JSON, persona JSON)` · `events(event_id, user_key, book_id, event_type, ts, …, payload JSON, quality_flag)` · `ratings(rating_id, user_key, book_id, stars, ts, recommendation_id)` · `recommendations(recommendation_id, user_key, snapshot_id, model_version, cell, forced, fallback_level, latency_total_ms, latency_breakdown JSON, weights JSON, rows JSON, ts)`.

From src/millie_rec/serving/api.py L118-126 (아날로그 — DB 결과를 pydantic 에 그대로): `HealthOut(status="ok", db_ok=ok, db_row_count=db.row_counts() if ok else {}, uptime_s=…)`.

**이 플랜이 만드는 공개 시그니처(05-06 이 verbatim 소비):**
```python
USER_KEY_RE = re.compile(r"^[A-Za-z0-9_-]{1,64}$")     # 백엔드 01 §0 user_key = 클라이언트 UUIDv4(하이픈 포함) — 안전 문자·길이 상한. demo_api.py 에 같은 값 중복 정의(revision C5, wave 1 독립성 — import 하지 않는다)
DELETE_TABLES = ("preference_snapshots", "events", "ratings", "recommendations")   # 응답 키는 §10: snapshots events ratings recommendations
HISTORY_EVENT_TYPES = ("reader_open", "qualified_read", "completion")   # state.py 와 같은 값 — 중복 정의(05-01 과 병렬, import 금지)
def build_router(*, db: Database, catalog: Catalog | None = None, state: object | None = None,
                 weights: Callable[[UserState], dict[str, float]] | None = None,
                 invalidate: Callable[[str], object] | None = None,
                 now: Callable[[], datetime] | None = None) -> APIRouter   # revision C6: cache 객체 대신 invalidate 콜백
```
</interfaces>
</context>

<feature>
  <name>열람·삭제·철회 API — GET state · GET data · DELETE personalization (백엔드 01 §8·§9·§10, D-08, SERV-07)</name>
  <files>src/millie_rec/serving/privacy_api.py, tests/serving/test_privacy_api.py</files>
  <behavior>
    테스트 앱(`create_app` 미사용 — 05-06 배선 전): `app = FastAPI(); db = Database(tmp_path / "t.db"); db.apply_schema(); app.include_router(build_router(db=db, catalog=_Cat(), weights=lambda u: {"alpha": 1.0 if u.explicit_seeds else 0.0, "beta": 1.0 if u.history else 0.0, "gamma": 0.0}, state=None, invalidate=inv_spy, now=lambda: FIXED_NOW)); client = TestClient(app)` — 기본 케이스는 `state=None`(SQL 경로). **revision C6:** 시드 데이터는 헬퍼 `_seed_user(db, "u-1")` 가 직접 INSERT 하고 **이벤트별 ts 를 `_iso(FIXED_NOW - timedelta(minutes=N))` 로 명시**: users(`"u-1"`, created_at, consent 1, cell "B", is_new 1) · 스냅샷 2개(`snap_aaaaaa` @−120분 categories `["IT","소설"]` criterion `bestseller` seeds `[1,2,3,4,5]`, `snap_bbbbbb` @−60분 categories `["인문"]` seeds `[6,7]`) · events: library_add 1(−50)·2(−45)·3(−40) · reader_open 2(−30) · completion 3(−20) · reader_open 8(−10) · detail_click 9(−5) · 총 7 · ratings 1행 · recommendations 2행. 순서가 못박히므로 `reading == [8, 2]`·`history == (8, 3, 2)` 가 결정적. `_Cat.meta(ids)` → `{"book_id", "title": f"책{b}", "image_url": f"https://img.millie.co.kr/{b}.jpg"}`. `_Spy` 는 `forget(user_key)`·`invalidate(user_key)` 호출을 `calls` 에 기록(둘 다 가진 하나의 가짜로 충분).
    - `# ── 계약 ──` `GET /api/users/u-1/state` 200 → `UserStateOut.model_validate`; `consent is True`, `cell == "B"`, `is_new is True`; `library` 키 == `{"added", "reading", "completed"}`; `added` book_id 집합 == `{1, 2, 3}`(library_add), `completed == [3]`, `reading == [8, 2]`(reader_open 있고 completion 없음, **최근순** — ts −10 < −30 순; 2 는 library_add 도 있지만 reader_open 이 있으니 reading 에도), 각 `LibraryBook.title == "책N"`·`image_url` 채움; `snapshots` 2개 `created_at` 내림차순, `snapshots[0].snapshot_id == "snap_bbbbbb" and active is True`, `snapshots[1].active is False`, `snapshots[0].categories == ["인문"]`, `snapshots[1].criterion == "bestseller"`; `user_state_weights == {"alpha": 1.0, "beta": 1.0, "gamma": 0.0}`(weights 가 받은 `UserState.explicit_seeds == (6, 7)` — 최신 스냅샷 seeds, `history` == distinct reader_open/qualified_read/completion 책 = `(8, 3, 2)` 최근순 — 테스트는 spy 로 `UserState` 를 캡처해 단정); `nearline_lag_s is None`
    - `weights=None` 으로 만든 라우터 → `user_state_weights == {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}`; `catalog=None` → `LibraryBook.title is None` 이고 200
    - **state 우선(revision C6):** `state=_FakeState()`(`user_state(user_key, *, seeds, categories, context)` 가 `UserState(None, explicit_seeds=seeds, history=(99,))` 를 돌려주는 가짜 + `forget` 기록) 를 주입하면 weights 가 받은 `UserState.history == (99,)`(SQL 이 아니라 state 값); `state=None` 이면 SQL 경로(위 `(8, 3, 2)`)
    - `GET /api/users/u-1/data` 200 → `UserDataOut.model_validate`; `user["user_key"] == "u-1" and user["cell"] == "B"`; `len(snapshots) == 2` 이고 `snapshots[0]["categories"] == ["인문"]`(JSON 컬럼은 `json.loads` 로 풀어 넣음 — 문자열이 아니다); `len(events) == 7`, `events[0]` 키에 `event_id user_key book_id event_type ts quality_flag payload` 포함, `payload` 는 dict; `len(ratings) == 1`; `len(recommendations) == 2` 이고 각 항목에 `recommendation_id ts model_version fallback_level` 키; `exported_at == FIXED_NOW iso`; `note == "가명 user_key 외 개인정보 없음"`
    - `# ── 정확성 ──` `DELETE /api/users/u-1/personalization` 200 → `PersonalizationDeleted.model_validate`; `deleted == {"snapshots": 2, "events": 7, "ratings": 1, "recommendations": 2}`; `consent is False`; DB: `SELECT consent FROM users WHERE user_key='u-1'` == 0 이고 행은 **남아 있다**; 4테이블 `COUNT(*) WHERE user_key='u-1'` 전부 0; `state_spy.calls == [("forget", "u-1")]` 이고 `inv_spy.calls == ["u-1"]`(state 주입 케이스에서)
    - 멱등: 같은 DELETE 두 번째 → 200, `deleted` 전부 0, `consent False`; 이후 `GET …/state` → 200 `consent is False`, library 3분류 전부 `[]`, `snapshots == []`
    - 다른 user 무영향: `_seed_user(db, "u-2")` 후 `DELETE u-1` → `u-2` 의 4테이블 COUNT 불변
    - `state=None, invalidate=None` 라우터에서 DELETE → 200(콜백 없음은 건너뜀)
    - `# ── 안전성 ──` 404: `GET /api/users/nobody/state`·`GET /api/users/nobody/data`·`DELETE /api/users/nobody/personalization` 전부 `404` 이고 `r.json() == {"detail": "user_key not found"}`
    - 422 형식: `GET /api/users/%20/state`(공백)·`GET /api/users/a%27%3Bb/state`(`a';b`)·65자 키 → 422; 정상 UUIDv4(`str(uuid.uuid4())`, 하이픈 포함 36자) 는 404(형식 통과 후 미존재)
    - 인젭션 값은 형식 검증에서 422 로 막히지만, 형식을 통과하는 `"u-1"` 로 시드한 뒤 `DELETE` 의 SQL 이 `?` 바인딩인지 **소스 grep**: `"execute(f" not in src` 그리고 `src.count("WHERE user_key = ?") >= 8`
    - 무흔적: 전 응답 텍스트에 `"Traceback"` 없음; `GET …/data` 응답 텍스트에 이메일 형태(`@`) 없음(가명 키 외 개인정보 없음의 소극적 확인)
  </behavior>
  <implementation>Task 2 action 그대로.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): privacy_api 스텁(3 라우트 등록, 틀린 값) + test_privacy_api.py → AssertionError</name>
  <files>src/millie_rec/serving/privacy_api.py, tests/serving/test_privacy_api.py</files>
  <read_first>
    - src/millie_rec/serving/schemas.py L238-277(§8~§10 모델 5개 — 필드 그대로)
    - src/millie_rec/serving/schema.sql 전문(5테이블 컬럼)
    - src/millie_rec/serving/api.py L118-126(`/health` 아날로그), L33-35(`_422` — 중복 정의는 하지 않는다: 경로 파라미터 검증은 `HTTPException(422, detail=[{"loc": ["path", "user_key"], "msg": …, "type": "value_error"}])` 형태로 직접)
    - src/millie_rec/serving/db.py L32-39(`connect()`), L48-54(`row_counts` — `sqlite3.Row`/tuple 양쪽 호환하도록 인덱스 접근)
    - src/millie_rec/contracts.py L122-135(`UserState`), L316-321(`Catalog`)
    - .planning/phases/05-must/05-CONTEXT.md D-08("DELETE 는 preference_snapshots·events·ratings·recommendations 행 삭제 + users.consent=false(행 유지)", "메모리 상태·캐시도 함께 제거"), Claude's Discretion "`UserStateOut.library` 3분류", "`snapshot_id` 가 그 user_key 것이 아니면 404"
    - .planning/phases/05-must/05-PATTERNS.md §5(privacy_api Analog — `row_counts` dict 형태·`?` 바인딩·`sqlite3.Row` → `dict(r)`·JSON 컬럼 `json.loads`·삭제 후 `state.forget` + 캐시 무효화)
    - ../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md §0(ID 형식·404 형태)·§8·§9·§10
    - ../.claude/rules/codex-review.md "언제" 표(privacy_api 필수)
  </read_first>
  <action>
    **1-a. `privacy_api.py` 스텁** — docstring `"""열람·삭제·철회 API(백엔드 서빙 01 §8·§9·§10, 아키텍처 01 §3-8) — APIRouter 팩토리. 삭제는 상태·캐시 콜백까지(05-CONTEXT D-08)."""`. 상수 `USER_KEY_RE`·`DELETE_TABLES`·`HISTORY_EVENT_TYPES`·`NOT_FOUND = "user_key not found"`·`ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}`. `build_router(...)` 는 `APIRouter(prefix="/api/users")` 에 3 라우트 등록: `GET /{user_key}/state` → `UserStateOut(user_key=user_key, consent=True, cell="A", is_new=False, library={"added": [], "reading": [], "completed": []})`(존재 검사 없음 → 404 테스트가 `assert 404 == 200` 으로 실패); `GET /{user_key}/data` → `UserDataOut(user={}, exported_at="")`; `DELETE /{user_key}/personalization` → `PersonalizationDeleted(user_key=user_key, deleted={})`. 형식 검증도 아직 없음(422 테스트가 RED).
    **1-b. `tests/serving/test_privacy_api.py`** (12건) — 상단 `FIXED_NOW`, `_iso`, `_Cat`, `_Spy`(`forget`·`invalidate` 를 `calls.append(("forget", k))` 식으로 기록), `_seed_user(db, key)`, `_client(tmp_path, **router_kw)`, `_no_trace(r)`.
    - `# ── 계약 ──` `test_state_parses_and_library_three_buckets_with_meta` · `test_state_snapshots_desc_with_active_latest_and_weights_from_latest_seeds_history` · `test_state_without_weights_or_catalog_degrades_gracefully` · `test_data_export_returns_all_tables_with_json_columns_decoded_and_note`
    - `# ── 정확성 ──` `test_delete_removes_four_tables_keeps_user_consent_false_calls_forget_invalidate` · `test_delete_is_idempotent_and_state_after_delete_is_empty` · `test_delete_does_not_touch_other_users` · `test_delete_without_state_or_cache_callbacks_still_200`
    - `# ── 안전성 ──` `test_unknown_user_key_is_404_on_all_three_routes` · `test_user_key_format_422_and_uuid4_passes_format` · `test_sql_is_parameter_bound_source_grep` · `test_responses_leak_no_traceback_or_pii`
    **1-c. RED 확인:** `uv run pytest tests/serving/test_privacy_api.py --no-header 2>&1 | tail -40` → 12건 대다수 failed, 사유 `AssertionError`/`E  assert`. `uv run pytest tests/test_architecture.py --no-header` → `3 passed`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_privacy_api.py --no-header 2>&1 | tail -30; uv run pytest tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/serving/test_privacy_api.py` → 12
    - RED 출력에 `AssertionError`/`E  assert` 있고 `ImportError`·`AttributeError`·`TypeError`·`sqlite3.OperationalError` 없음
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(data\|app\|retrieval\|ranking\|reranking\|evaluation\|serving\.state\|serving\.fallback\|serving\.compose\|serving\.demo_api\)" src/millie_rec/serving/privacy_api.py` → 출력 없음
  </acceptance_criteria>
  <done>신규 12건 대다수 AssertionError RED, 아키텍처 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): state·data·DELETE 구현 → 12 passed</name>
  <files>src/millie_rec/serving/privacy_api.py</files>
  <read_first>
    - Task 1 스텁·테스트
    - .planning/phases/05-must/05-PATTERNS.md §5 스니펫 · Shared A(예외 로그 후 강하)·B(동기 def)·D(타임스탬프)
  </read_first>
  <action>
    SQL 상수(전부 `?`):
    ```python
    SQL_USER = "SELECT user_key, created_at, consent, cell, is_new FROM users WHERE user_key = ?"
    SQL_SNAPS = ("SELECT snapshot_id, user_key, created_at, reading_time, categories, criterion, subcategories, seeds, persona "
                 "FROM preference_snapshots WHERE user_key = ? ORDER BY created_at DESC, snapshot_id DESC")
    SQL_EVENTS = "SELECT * FROM events WHERE user_key = ? ORDER BY ts, rowid"
    SQL_RATINGS = "SELECT * FROM ratings WHERE user_key = ? ORDER BY ts"
    SQL_RECS = "SELECT * FROM recommendations WHERE user_key = ? ORDER BY ts"
    SQL_LIB = ("SELECT book_id, event_type, MAX(ts) AS ts FROM events WHERE user_key = ? AND book_id IS NOT NULL "
               "AND event_type IN ('library_add', 'reader_open', 'qualified_read', 'completion') GROUP BY book_id, event_type ORDER BY ts DESC")
    SQL_DELETE = {t: f"DELETE FROM {t} WHERE user_key = ?" for t in DELETE_TABLES}   # 테이블 이름은 상수 튜플 — 사용자 입력 아님(row_counts 관례)
    SQL_CONSENT_OFF = "UPDATE users SET consent = 0 WHERE user_key = ?"
    JSON_COLS = ("categories", "subcategories", "seeds", "persona", "payload", "latency_breakdown", "weights", "rows")
    ```
    (`SQL_DELETE` 의 f-string 은 상수 튜플 이름만 — acceptance 의 `execute(f` 0건 조건과 충돌하지 않게 **딕셔너리 컴프리헨션에서 미리 만든 문자열**을 `execute(SQL_DELETE[t], (user_key,))` 로 쓴다.)
    헬퍼: `_rows(con, sql, key) -> list[dict]`: `con.row_factory = sqlite3.Row`(지역 설정 — 05-03 도 connect 에서 설정하지만 병렬이라 의존하지 않음) → `[{k: (_loads(v) if k in JSON_COLS and isinstance(v, str) else v) for k, v in dict(r).items()} for r in con.execute(sql, (key,)).fetchall()]`; `_loads(s)` 는 `json.loads` 실패 시 원문. `_check_key(user_key)`: `if not USER_KEY_RE.match(user_key): raise HTTPException(422, detail=[{"loc": ["path", "user_key"], "msg": "user_key must match ^[A-Za-z0-9_-]{1,64}$", "type": "value_error"}])`. `_user_or_404(con, user_key) -> dict`: `_check_key` → `SELECT` → 없으면 `HTTPException(404, detail=NOT_FOUND)`.
    `_library(con, user_key, catalog)`: `SQL_LIB` 결과로 `added = [b for library_add]`, `completed = [b for completion]`(최근순 유지, distinct), `reading = [b for reader_open if b not in completed]`; `meta = {m["book_id"]: m for m in catalog.meta(모든 id)} if catalog else {}`; `LibraryBook(book_id=b, title=meta.get(b, {}).get("title"), image_url=meta.get(b, {}).get("image_url"))`.
    `_user_state_for_weights(con, user_key, snaps)`: `seeds = tuple(snaps[0]["seeds"]) if snaps else ()`, `cats = tuple(snaps[0]["categories"]) if snaps else ()`; **`if state is not None: user = state.user_state(user_key, seeds=seeds, categories=cats)`(revision C6 state 우선) `else:`** history = `SQL_LIB` 결과 중 `event_type in HISTORY_EVENT_TYPES` 의 distinct book_id 최근순 → `UserState(user_id=None, explicit_seeds=seeds, history=tuple(history), context={"user_key": user_key})`; `w = weights(user) if weights else dict(ZERO_WEIGHTS)`; `try/except Exception: log.exception(...); w = dict(ZERO_WEIGHTS)`(weights 예외 → 0 셋, 500 없음).
    라우트(동기 `def`, `response_model` 지정):
    - `GET /{user_key}/state`: `con = db.connect(); u = _user_or_404(con, user_key); snaps = _rows(con, SQL_SNAPS, user_key)`; `UserStateOut(user_key=user_key, consent=bool(u["consent"]), cell=u["cell"], is_new=bool(u["is_new"]), library=_library(...), snapshots=[SnapshotSummary(snapshot_id=s["snapshot_id"], created_at=s["created_at"], categories=list(s["categories"] or []), criterion=s["criterion"], active=(i == 0)) for i, s in enumerate(snaps)], user_state_weights=_weights(...), nearline_lag_s=None)`.
    - `GET /{user_key}/data`: `UserDataOut(user=u, snapshots=_rows(SQL_SNAPS), events=_rows(SQL_EVENTS), ratings=_rows(SQL_RATINGS), recommendations=_rows(SQL_RECS), exported_at=_iso(clock()))`.
    - `DELETE /{user_key}/personalization`: `_user_or_404`; `with con: deleted = {("snapshots" if t == "preference_snapshots" else t): con.execute(SQL_DELETE[t], (user_key,)).rowcount for t in DELETE_TABLES}; con.execute(SQL_CONSENT_OFF, (user_key,))`; `if state is not None: state.forget(user_key)`; `if invalidate is not None: invalidate(user_key)`(revision C6 — 주입 계약, `getattr` 없음); `PersonalizationDeleted(user_key=user_key, consent=False, deleted=deleted)`.
    **GREEN 확인:** `uv run pytest tests/serving/test_privacy_api.py tests/test_architecture.py --no-header` → `15 passed`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_privacy_api.py tests/test_architecture.py --no-header && wc -l src/millie_rec/serving/privacy_api.py</automated>
  </verify>
  <acceptance_criteria>
    - 요약 줄 `15 passed`, failed 0 · `wc -l` ≤150
    - `grep -c "execute(f" src/millie_rec/serving/privacy_api.py` == 0 · `grep -c "WHERE user_key = ?" src/millie_rec/serving/privacy_api.py` ≥ 8
    - `grep -n 'USER_KEY_RE = re.compile(r"^\[A-Za-z0-9_-\]{1,64}$")' src/millie_rec/serving/privacy_api.py` 1행
    - `grep -c "\.forget(" src/millie_rec/serving/privacy_api.py` == 1 · `grep -c "invalidate(user_key)" …` == 1
    - `grep -c '"user_key not found"' src/millie_rec/serving/privacy_api.py` ≥ 1
  </acceptance_criteria>
  <done>12 + 아키텍처 3 passed, 파일 ≤150. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · 보안 자기 점검표 · 인계 시그니처 · SUMMARY</name>
  <files>src/millie_rec/serving/privacy_api.py</files>
  <read_first>
    - Task 2 결과 파일 · ../.claude/rules/codex-review.md "표준 흐름" · ../.claude/rules/python-tdd.md "Refactor"
  </read_first>
  <action>
    1. `uv run ruff format src/millie_rec/serving/privacy_api.py tests/serving/test_privacy_api.py && uv run ruff check <같은 경로>` 클린. 다른 serving 파일 포맷 금지.
    2. 보안 자기 점검(SUMMARY 에 표로): 경로 파라미터 형식 검증 ✓ / 404 미존재 ✓ / DELETE 멱등 ✓ / 응답에 가명 user_key 외 개인정보 없음(users 컬럼 5개·events 컬럼만) ✓ / f-string SQL 0 ✓ / 예외 문자열 미노출(weights 예외 → 0 셋) ✓ / `SELECT *` 컬럼은 schema.sql 정의 그대로(이메일·IP 없음) ✓.
    3. 재실행 → `15 passed`. SUMMARY 에 `build_router` 실제 시그니처(05-06 인계) + "Codex 필수 리뷰 대상(05-09): privacy_api.py — `/codex:review --scope working-tree` + `/codex:adversarial-review`" 1줄 + "DELETE 후 level 3 끝단 단정은 05-06 test_cascade" 1줄.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/privacy_api.py tests/serving/test_privacy_api.py && uv run ruff check src/millie_rec/serving/privacy_api.py tests/serving/test_privacy_api.py && uv run pytest tests/serving/test_privacy_api.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - ruff format --check · ruff check 클린 · `15 passed`
    - `git status --short` 에 이 플랜 `files_modified` 2개 외 이 Worker 변경 없음
  </acceptance_criteria>
  <done>ruff 클린 · 15 passed · 보안 점검표 · SUMMARY. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → `GET/DELETE /api/users/{user_key}/*` | 비인증. `user_key` 경로 파라미터는 임의 문자열 — 형식 검증 후 SQL 파라미터 |
| 라우터 → SQLite 5테이블 | SELECT 5 · DELETE 4 · UPDATE 1, 전부 `?` 바인딩 |
| 라우터 → `state.forget`·`cache.invalidate` | 주입 객체(05-06). 삭제권 행사의 유일한 핸들러 측 메모리 쓰기(PATTERNS §5 명시 예외) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | SQL 인젭션(경로 파라미터) | mitigate | `USER_KEY_RE` 형식 검증(422) + 전 SQL `?` 바인딩(`execute(f` 0건 acceptance, 테스트 grep). 테이블 이름은 상수 튜플 |
| T-05-05-02 | Information Disclosure | 타인 데이터 열람(`GET …/data`) | accept | 인증 없음은 설계 결정(아키 §3-8: user_key 는 클라이언트 UUIDv4 가명 키, 추측 불가 128bit). 응답 컬럼은 schema.sql 정의만 — 이메일·IP·이름 없음(`note` 문장). 테스트 `…leak_no_traceback_or_pii` |
| T-05-05-03 | Spoofing | 타인 user_key 로 DELETE | accept | 같은 근거(UUIDv4 추측 불가). 삭제는 가명 데이터에 한정, 되돌릴 수 없어도 서비스 피해 없음 |
| T-05-05-04 | Repudiation | 삭제 후 개인화 잔존 | mitigate | 4테이블 DELETE + `users.consent=0` + `state.forget` + `cache.invalidate` 4경로 동시(D-08). 끝단(다음 `/api/recommend` level 3) 은 05-06 테스트 |
| T-05-05-05 | Denial of Service | 대량 DELETE/열람 | accept | 인덱스 `idx_events_user_ts`(05-03) 로 user_key 조회 O(log n). 데모 규모 |
| T-05-05-06 | Information Disclosure | 예외 문자열(500) | mitigate | 경로 예외는 404/422 만 의도적 발생, `weights` 예외는 `log.exception` 후 0 셋. JSON 파싱 실패는 원문 유지 |
| T-05-05-07 | Elevation of Privilege | `consent` 재활성화 경로 | accept | 재동의는 `POST /api/preferences`(05-04) 가 `consent=true` 로 UPDATE — 설계된 경로(§10 "재동의 시 재생성 가능") |
</threat_model>

<verification>
- `uv run pytest tests/serving/test_privacy_api.py tests/test_architecture.py --no-header` → `15 passed`
- ruff 클린 · `wc -l` ≤150 · `execute(f` 0건 · `WHERE user_key = ?` ≥ 8
- (wave 1 종료 후 Advisor 1회) 전역 pytest · `make smoke`
- (05-09) Codex `/codex:review --scope working-tree` + `/codex:adversarial-review` — 필수
- 커밋 0
</verification>

<success_criteria>
- **SERV-07** 세 라우트가 스키마대로 응답(`UserStateOut`·`UserDataOut`·`PersonalizationDeleted`), DELETE 가 4테이블 삭제 + `consent=0` + 콜백 2개, 404·422·멱등이 테스트로 단정
- D-08 "DELETE 는 메모리 상태·캐시도 함께 제거" 코드 존재 · 재량 항목 `library` 3분류(added/reading/completed) 구현
- `schemas.py`·`contracts.py` 무변경, 의존성 0, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/05-must/05-05-SUMMARY.md`: RED/GREEN 요약 줄 + AssertionError 발췌 · 라우트 3개 표 · 보안 자기 점검표 7항 · `## TDD Gate Compliance` · **인계 시그니처 verbatim**(`build_router`) · "Codex 필수 리뷰(05-09)" · "level 3 끝단은 05-06" · 변경 파일 목록 · "커밋하지 않는다" 한 줄.
</output>
