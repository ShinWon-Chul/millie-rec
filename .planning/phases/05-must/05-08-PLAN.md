---
phase: 05-must
plan: 08
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/millie_rec/app/server.py
  - src/millie_rec/app/pipeline.py
  - src/millie_rec/app/staged.py
  - src/millie_rec/app/pipeline_kr.py
  - src/millie_rec/ranking/hybrid.py
  - src/millie_rec/reranking/guard.py
  - tests/app/test_server_state.py
  - tests/ranking/test_hybrid.py
  - tests/reranking/test_guard.py
autonomous: true
requirements: [SERV-04, SERV-09, SERV-03]
lane: 조립(Advisor 직접 — app/ 배선·Model 레인 1줄×2 승인, 04-04 관례)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/blend.py
  - src/millie_rec/reranking/mmr.py
  - src/millie_rec/data/**
  - src/millie_rec/evaluation/**
  - artifacts/serving/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/**
  - tests/app/test_server_catalog.py
  - tests/app/test_variants.py
  - tests/app/test_pipeline.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "app/server.py 가 StateStore 1개와 ServingBookStats(catalog, store) 를 만들어 create_app(state=store, book_stats=stats) 와 build_pipelines(book_stats=stats) 양쪽에 주입하므로 가드·gap 피처가 서빙 상태의 user_level·n_completed 를 본다(D-07, Advisor 확정 5)"
    - "catalog=None(아티팩트 없음) 에서도 server.py 가 기동하고 기존 test_smoke.py 두 조립 테스트가 그대로 통과한다(local-run.md)"
    - "StagedPipeline.recommend 는 retrieval·ranking·rerank 세 구간을 perf_counter 로 재어 self.last_breakdown = {'retrieval': ms, 'ranking': ms, 'rerank': ms} 에 남기고, 상수·로직·반환값은 바뀌지 않는다(D-09)"
    - "reranking/guard.py:52 와 ranking/hybrid.py:49 의 len(user.history) 대리값이 int(user.context.get('n_completed', len(user.history))) 로 바뀌어 서버(context 있음) 는 완독 수, Track A(context 없음) 는 기존 대리값을 쓴다 — 상수·시그니처·기존 테스트 무변경(D-07 재량 (c), Advisor 확정 4)"
  artifacts:
    - path: "src/millie_rec/app/server.py"
      provides: "store = StateStore() · stats = ServingBookStats(catalog, store) if catalog else None · create_app(…, book_stats=stats, state=store) · build_pipelines(book_stats=stats)"
      contains: "state=store"
      max_lines: 40
    - path: "src/millie_rec/app/staged.py"
      provides: "PopPipeline·StagedPipeline 이동(Advisor 확정 17 B4) — StagedPipeline.last_breakdown 은 threading.local 저장 + 프로퍼티(revision C2)"
      contains: "threading.local"
      max_lines: 150
    - path: "src/millie_rec/app/pipeline.py"
      provides: "from millie_rec.app.staged import PopPipeline, StagedPipeline re-export(기존 import 경로 유지) · build_pipelines(…, book_stats=None) 패스스루"
      contains: "from millie_rec.app.staged import"
      max_lines: 150
    - path: "src/millie_rec/app/pipeline_kr.py"
      provides: "build_pipelines_kr(catalog, *, weights=None, vectors=None, book_stats=None) — kw['book_stats'] = book_stats or catalog, DifficultyGuard(book_stats or catalog)"
      contains: "book_stats or catalog"
      max_lines: 150
    - path: "src/millie_rec/ranking/hybrid.py"
      provides: "n_completed = int(user.context.get(\"n_completed\", len(user.history)))"
      contains: "user.context.get(\"n_completed\""
    - path: "src/millie_rec/reranking/guard.py"
      provides: "완독 수 분기 1줄 + docstring 덧붙임 '(교체됨: context[\"n_completed\"])'"
      contains: "context.get(\"n_completed\""
    - path: "tests/app/test_server_state.py"
      provides: "server.py 주입(state·book_stats) · catalog None 기동 · last_breakdown 3키 · build_pipelines_kr book_stats 패스스루"
      min_lines: 40
  key_links:
    - from: "src/millie_rec/app/server.py"
      to: "millie_rec.serving 공개 표면"
      via: "from millie_rec.serving import Database, GlobalPopularFallback, ServingBookStats, StateStore, create_app, resolve_db_path"
      pattern: "from millie_rec.serving import .*StateStore"
    - from: "src/millie_rec/app/pipeline_kr.py"
      to: "ServingBookStats(주입)"
      via: "StagedPipeline(..., book_stats=book_stats or catalog) · DifficultyGuard(book_stats or catalog)"
      pattern: "book_stats or catalog"
    - from: "src/millie_rec/reranking/guard.py"
      to: "UserState.context['n_completed'] (serving/state.py 가 채움)"
      via: "int(user.context.get(\"n_completed\", len(user.history))) >= self.min_completed"
      pattern: "context.get\\(\"n_completed\""
---

<objective>
Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md "Phase 5: 서빙 Must 완성") 의 **조립(Advisor 직접)**: `app/server.py` 가 `StateStore`·`ServingBookStats` 를 만들어 `create_app(state=, book_stats=)` 와 파이프라인(`build_pipelines(book_stats=)`) 양쪽에 주입하고, `app/pipeline.py::StagedPipeline` 에 `last_breakdown` 타이밍(D-09) 을 기록하고, Model 레인 두 파일의 `n_completed` 대리값을 `UserState.context` 분기 1줄로 바꾼다(D-07 재량 (c), Advisor 확정 4).

Purpose: ★시간 가변 가중치·★난이도 가드가 **서버 상태**로 동작하는 마지막 배선 — 완독 이벤트가 쌓이면 가드가 풀리고(`n_completed ≥ 3`), `user_level` 이 완독 평균으로 바뀐다(D-07). `latency_breakdown` 에 `retrieval·ranking·rerank` 세분 키가 나온다(D-09, 백엔드 01 §5 예시 형태). 결정 '서버 user_key 상태'(개발일지 2026-09-06 파일 항목 D74) 의 이행. 이 플랜은 **Advisor(오케스트레이터) 세션이 실행**한다(`app/`·Model 레인 파일은 Worker 위임 금지 — `../.claude/rules/architecture.md` "Advisor 전용"). Worker 위임 시에도 브리프는 이 본문 그대로.
Output: `app/` 3파일 수정, Model 레인 2파일 1줄씩 + docstring 1구, 테스트 신규 1 + 기존 2파일에 **추가만**. 커밋 0. 개발일지 1줄은 05-09 가 기록.

**wave 2 병렬 주의:** 05-06 이 `serving/api.py` 를 같은 시각에 재구성한다. 이 플랜의 `server.py` 는 `create_app` **keyword 인자만 추가**(시그니처 불변, D-09) 하므로 05-06 전후 어느 상태에서도 import 된다. 이 플랜의 테스트는 `/api/recommend?user_key=` 5행을 단정하지 않는다(05-06 몫) — 주입·타이밍·분기만.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-PATTERNS.md
@.planning/phases/05-must/05-01-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/app/server.py
@src/millie_rec/app/pipeline.py
@src/millie_rec/app/pipeline_kr.py
@src/millie_rec/ranking/hybrid.py
@src/millie_rec/reranking/guard.py
@src/millie_rec/serving/__init__.py
@src/millie_rec/serving/state.py
@src/millie_rec/serving/book_stats.py
@tests/serving/test_smoke.py
@tests/app/test_server_catalog.py
@tests/app/test_variants.py
@tests/app/test_pipeline.py
@tests/ranking/test_hybrid.py
@tests/reranking/test_guard.py
@tests/test_architecture.py
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/local-run.md
@../.claude/rules/references.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 오직 `uv run pytest <대상> --no-header`. "app/ 조립 · contracts.py — Advisor 직접. 아키텍처 테스트 + 계약 테스트로 대체" — 이 플랜은 `type: execute` 이나 테스트를 **먼저 추가**하고(RED 확인) 구현한다. 기존 테스트 단정 수정 금지 — `tests/reranking/test_guard.py::test_stats_called_once_and_docstring_declares_proxy` 가 guard.py 소스에 `"대리값"`·`"Phase 5"`·`"len(user.history)"` 문자열을 요구하므로 **docstring 문장 유지 + 덧붙임**(Advisor 확정 4), `tests/ranking/test_hybrid.py::test_n_completed_times_gap_uses_history_length_proxy` 는 context 없는 UserState 라 기본값 경로로 그대로 통과.

**star 의존(`../.claude/rules/architecture.md`):** `app` 은 공개 표면만 import(`from millie_rec.serving import StateStore, ServingBookStats` ✅ / `from millie_rec.serving.state import …` ❌ — `test_architecture.py` L41-42). `ranking`·`reranking` 은 `contracts` 만(변경 없음). **freeze(04-CONTEXT D-14 ①)**: `retrieval/ranking/reranking` 상단 상수 무변경 — 이 플랜은 `GUARD_MIN_COMPLETED=3`·`W_NCOMP_GAP=-0.01` 을 건드리지 않는다.

**간결함(`../.claude/rules/simplicity.md`):** 최소 변경(CLAUDE.md §3-7) — 언급된 것만. `pipeline.py` 149줄이라 **`StagedPipeline`·`PopPipeline` 을 `app/staged.py` 로 지금 이동**(Advisor 확정 17 B4, Phase 4 04-04 "Advisor 확정 6" 선례) 하고 `pipeline.py` 가 re-export — `tests/app/test_pipeline.py` import 경로 무변경.

**로컬 기동(`../.claude/rules/local-run.md`):** `catalog=None` 에서 `server.py` 가 뜬다(`ServingBookStats` 는 catalog 있을 때만). `make smoke` 는 05-09 wave 2 게이트(revision A5).

**완료 기준(자기 경로만, revision A5):** `uv run ruff format --check src/millie_rec/app src/millie_rec/ranking/hybrid.py src/millie_rec/reranking/guard.py tests/app tests/ranking tests/reranking && uv run ruff check <같은 경로>` 클린 · `uv run pytest tests/app/test_server_state.py tests/ranking tests/reranking tests/test_architecture.py --no-header` failed 0. `tests/serving/test_smoke.py`·`tests/app` 전체·`make smoke` 는 05-09 게이트로 이관.
</project_rules_inline>

<interfaces>
From src/millie_rec/serving (05-01 SUMMARY 인계 — 실제 코드가 정본):
```python
from millie_rec.serving import ServingBookStats, StateStore   # __init__.__all__ 에 있음
class StateStore: def __init__(self, *, now: Callable[[], float] | None = None) -> None
class ServingBookStats: def __init__(self, catalog: Catalog, store: StateStore) -> None   # catalog 는 BookStatsSource 도 만족(CatalogKR)
```
From src/millie_rec/app/server.py L15-24 (현재):
```python
catalog = load_catalog()
app = create_app(pipelines=build_pipelines(catalog=catalog, weights=state_weights), fallback=GlobalPopularFallback(catalog),
                 catalog=catalog, db=Database(resolve_db_path()), neighbors=catalog, book_stats=catalog, weights=state_weights)
```
From src/millie_rec/app/pipeline.py L63-92 (`StagedPipeline.recommend` — 세 구간):
```python
        cands = {slot: r.retrieve(user, self.pool) for slot, r in self.channels.items()}        # retrieval
        items = blend_channels(user, cands, self.weights, book_stats=self.book_stats)          # ranking
        if self.catalog is not None: ...eligible...
        for n_, rr in enumerate(self.rerankers): items = rr.rerank(user, items, k if n_ == last else len(items))   # rerank
        return [replace(i, position=n) for n, i in enumerate(items[:k])]
```
From src/millie_rec/app/pipeline.py L131-149 (`build_pipelines(artifact=POP_ARTIFACT, *, catalog=None, weights=None)`) · pipeline_kr.py L101-123 (`build_pipelines_kr(catalog, *, weights=None, vectors=None)`, L115 `kw = {"weights": dict(CHANNEL_WEIGHTS), "book_stats": catalog, "catalog": catalog}`, L120 `DifficultyGuard(catalog)`).
From src/millie_rec/ranking/hybrid.py L49: `n_completed = len(user.history)  # D-08 대리값 — Phase 5 state.py 가 완독 이벤트 수로 교체`
From src/millie_rec/reranking/guard.py L52: `if self.book_stats is None or len(user.history) >= self.min_completed or not items:` · L5-7 docstring "발동 조건 n_completed < GUARD_MIN_COMPLETED 의 n_completed 는 Phase 4 에서 len(user.history) 대리값 — Phase 5 state.py 가 completion 이벤트 수로 교체한다(PDF 각주와 같은 문장)."
From tests/serving/test_smoke.py L164-204: `monkeypatch.setattr("millie_rec.app.pipeline.load_catalog", lambda: None)` · `monkeypatch.setattr("millie_rec.app.pipeline.build_pipelines", lambda **_: {})` — **keyword 호출 전제**(`build_pipelines(catalog=…, weights=…, book_stats=…)` 로 keyword 만 추가).
From src/millie_rec/contracts.py: `UserState.context: dict[str, str]` — 값은 문자열, `int()` 변환 필요.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 테스트 먼저 — test_server_state.py 신규 + test_hybrid/test_guard 에 context 분기 케이스 추가(RED) → 배선·타이밍·1줄 분기 구현(GREEN)</name>
  <files>src/millie_rec/app/server.py, src/millie_rec/app/pipeline.py, src/millie_rec/app/pipeline_kr.py, src/millie_rec/ranking/hybrid.py, src/millie_rec/reranking/guard.py, tests/app/test_server_state.py, tests/ranking/test_hybrid.py, tests/reranking/test_guard.py</files>
  <read_first>
    - .planning/phases/05-must/05-01-SUMMARY.md "인계 시그니처"(`StateStore`·`ServingBookStats` 생성자)
    - src/millie_rec/app/server.py 전문(26줄) · pipeline.py L63-92·L131-149 · pipeline_kr.py L101-123
    - src/millie_rec/ranking/hybrid.py L38-56(`_gap_bonus`) · src/millie_rec/reranking/guard.py L1-10(docstring)·L51-53
    - tests/ranking/test_hybrid.py L20-40(`_FakeStats`·`_channels` 헬퍼)·L93-99(대리값 테스트 — 기본값 경로 유지 확인) · tests/reranking/test_guard.py L12-40(`_FakeStats`·`TABLE`·`NEW`·`_items`)·L73-76(`test_history_ge_3_passes_through`)·L88-96(docstring 단정 3문자열)
    - tests/serving/test_smoke.py L164-204(server 재import 관례 — `sys.modules.pop` + `importlib.import_module`, keyword monkeypatch) · tests/app/test_server_catalog.py L20-28(`_server` 헬퍼 — fixture 카탈로그 주입)
    - .planning/phases/05-must/05-CONTEXT.md D-07·D-09, Claude's Discretion "`n_completed` 대리값 교체 방식" (c)
    - .planning/phases/05-must/05-PATTERNS.md §1 "`n_completed` 대리값의 현재 위치(교체 지점 2곳)", §14(server.py 아날로그·`catalog=None` 기동)
  </read_first>
  <action>
    **1-a. 테스트 추가(RED 먼저 — 각 파일 끝에 append, 기존 단정 무수정):**
    - `tests/reranking/test_guard.py` 끝: `test_context_n_completed_overrides_history_proxy` — `u = UserState(None, explicit_seeds=(50,), history=(7, 8), context={"n_completed": "3"})`(history 2 이지만 완독 3) → `DifficultyGuard(_FakeStats(TABLE)).rerank(u, _items(), 12)` 가 **패스스루**(`[i.book_id for i in out] == [i.book_id for i in _items()]`, `_FakeStats.calls == 0`); 반대로 `history=(1, 2, 3, 4)`(4권) + `context={"n_completed": "0"}` → 가드 **발동**(`_FakeStats.calls == 1`, 위반 책이 상단 N 밖). 기존 `test_history_ge_3_passes_through`(context 없음) 는 그대로.
    - `tests/ranking/test_hybrid.py` 끝: `test_context_n_completed_overrides_history_proxy_in_gap_term` — `test_n_completed_times_gap_uses_history_length_proxy` 와 같은 입력에 `context={"n_completed": "5"}` → `items[0].score == pytest.approx(0.38 + (-0.01 * 5 * 0.4))`(= 0.36), 기존 테스트(0.372) 는 그대로.
    - `tests/app/test_server_state.py` 신규(5건): `_server(tmp_path, monkeypatch, serving_dir)` 헬퍼는 `tests/app/test_server_catalog.py` L20-28 복제.
      - `test_server_injects_state_store_and_serving_book_stats(tmp_path, monkeypatch, millie_serving_sample)`(**revision A4**) — `_server` 전에 `import millie_rec.serving as sv; real = sv.create_app; captured = {}; monkeypatch.setattr("millie_rec.serving.create_app", lambda *a, **kw: (captured.update(kw), real(*a, **kw))[1])`; `server = _server(...)`; `from millie_rec.serving import ServingBookStats, StateStore`; `captured["state"] is server.store`, `isinstance(captured["book_stats"], ServingBookStats)`, `captured["book_stats"].store is server.store`, `isinstance(getattr(server, "store", None), StateStore)`. `app.state.*` 노출 단정은 **05-06 `test_cascade.py` 몫**(이 파일에서 하지 않는다)
      - `test_server_starts_without_catalog_and_stats_is_none(tmp_path, monkeypatch)` — `monkeypatch.setattr("millie_rec.app.pipeline.load_catalog", lambda: None)`; `build_pipelines` 를 `lambda **_: {}`; `server.stats is None`, `isinstance(server.store, StateStore)`, `TestClient(server.app).get("/health").status_code == 200`
      - `test_staged_pipeline_records_last_breakdown_three_keys(millie_serving_sample)` — `from millie_rec.data import CatalogKR, VectorsKR`; `from millie_rec.app.pipeline_kr import build_pipelines_kr`; `pipes = build_pipelines_kr(CatalogKR.load(p), vectors=VectorsKR.load(p / "content_vectors_kr.npz"))`; `inner = pipes["hybrid_div"].inner`(WithMeta 안쪽); `inner.recommend(UserState(None, explicit_seeds=(1, 2, 3)), 10)`; `set(inner.last_breakdown) == {"retrieval", "ranking", "rerank"}`, 값 전부 `float` 이고 `>= 0.0`; `pipes["hybrid_div"].recommend(...)` 결과 book_id 가 이 변경 전과 동일(`7 not in ids[:10]` — `test_variants.py:64` 와 같은 단정, 로직 무변경 증거)
      - `test_build_pipelines_kr_passes_book_stats_to_guard_and_blend(millie_serving_sample)` — `_Spy` 는 `CatalogKR` 을 감싼 `stats()` 호출 카운터 + `user_level` → `0.9`; `pipes = build_pipelines_kr(cat, vectors=…, book_stats=spy)`; `pipes["hybrid_div"].recommend(UserState(None, explicit_seeds=(1,), context={"n_completed": "0"}), 10)` 후 `spy.calls >= 2`(blend `_gap_bonus` 1 + guard `_violates` 1); `book_stats=None` 이면 기존처럼 catalog 자신(`pipes["hybrid_div"].inner.book_stats is cat`)
      - `test_build_pipelines_passthrough_book_stats_keyword(tmp_path, monkeypatch)` — `build_pipelines(catalog=None, weights=None, book_stats=object())` 가 예외 없이 `{}`(artifact 없음) — keyword 수용 확인
    - RED: `uv run pytest tests/app/test_server_state.py tests/ranking/test_hybrid.py tests/reranking/test_guard.py --no-header 2>&1 | tail -30` → 신규 7건 중 ≥5 failed, 사유 `AssertionError`/`E  assert`(server 속성 없음은 `AttributeError` 라 RED 로 안 침 — `getattr(server, "store", None)` 로 단정해 `assert isinstance(None, StateStore)` 가 AssertionError 가 되게 쓴다; `TypeError: unexpected keyword 'book_stats'` 도 RED 아님 → `build_pipelines` 시그니처는 테스트 전에 keyword 만 먼저 추가해도 된다 — 그 경우 SUMMARY 에 명시).
    - **B4(Advisor 확정 17) — 파일 이동을 먼저:** `PopPipeline`·`StagedPipeline` 을 `src/millie_rec/app/staged.py` 로 옮기고(`POOL`·`WeightFn`·`SOURCE_POPULARITY` 는 staged.py 가 자체 import/정의), `app/pipeline.py` 상단에 `from millie_rec.app.staged import PopPipeline, StagedPipeline  # 기존 import 경로 유지(re-export)` — `tests/app/test_pipeline.py` 의 `from millie_rec.app.pipeline import StagedPipeline …` 무변경. Phase 4 04-04 "Advisor 확정 6" 선례.

    **1-b. `app/server.py`**(26 → ≤40줄):
    ```python
    from millie_rec.serving import (Database, GlobalPopularFallback, ServingBookStats, StateStore, create_app, resolve_db_path)
    # Phase 5 D-07·D-08: 상태 1개를 create_app(state=) 와 파이프라인 book_stats 양쪽에 — 가드·gap 이 서빙 완독 수·user_level 을 본다
    catalog = load_catalog()
    store = StateStore()
    stats = ServingBookStats(catalog, store) if catalog is not None else None
    app = create_app(
        pipelines=build_pipelines(catalog=catalog, weights=state_weights, book_stats=stats),
        fallback=GlobalPopularFallback(catalog), catalog=catalog, db=Database(resolve_db_path()),
        neighbors=catalog, book_stats=stats, weights=state_weights, state=store,
    )
    app.mount("/", StaticFiles(directory=ROOT / "demo", html=True), name="demo")   # 반드시 마지막
    ```
    **1-c. `app/pipeline.py`:** `build_pipelines(artifact=POP_ARTIFACT, *, catalog=None, weights=None, book_stats: BookStatsSource | None = None)` → `build_pipelines_kr(catalog, weights=weights, book_stats=book_stats)`. **`app/staged.py`(B4) 의 `StagedPipeline`:** `__init__` 에 `self._tl = threading.local()`; `@property def last_breakdown(self) -> dict[str, float]: return getattr(self._tl, "bd", {})`(**revision C2 — 스레드풀 핸들러가 동시에 부를 때 다른 요청의 값을 읽지 않게 threading.local 저장**). `recommend`:
    ```python
        t = perf_counter()
        cands = {slot: r.retrieve(user, self.pool) for slot, r in self.channels.items()}
        t1 = perf_counter()
        items = blend_channels(user, cands, self.weights, book_stats=self.book_stats)
        if self.catalog is not None: ...(기존)
        t2 = perf_counter()
        ...(rerankers 루프 기존)
        self._tl.bd = {"retrieval": (t1 - t) * 1000, "ranking": (t2 - t1) * 1000, "rerank": (perf_counter() - t2) * 1000}  # D-09 타이밍만, threading.local(C2)
        return [replace(i, position=n) for n, i in enumerate(items[:k])]
    ```
    (`from time import perf_counter`·`import threading` 은 staged.py 에. eligible 필터는 ranking 구간에 포함 — 세 키 정의를 SUMMARY 에 1줄.) `wc -l` pipeline.py·staged.py 각 ≤150.
    **1-d. `app/pipeline_kr.py`:** `build_pipelines_kr(catalog, *, weights=None, vectors=None, book_stats: BookStatsSource | None = None)`; `bs = book_stats or catalog`; `kw = {"weights": dict(CHANNEL_WEIGHTS), "book_stats": bs, "catalog": catalog}`; `DifficultyGuard(bs)`. `WithMeta` 에 `last_breakdown` 프록시 1줄: `@property def last_breakdown(self): return getattr(self.inner, "last_breakdown", None)`(cascade 의 `getattr(pipe, "last_breakdown", None)` 가 WithMeta 를 통과해 읽는다 — D-09).
    **1-e. Model 레인 1줄×2(Advisor 확정 4):**
    - `ranking/hybrid.py` L49 → `n_completed = int(user.context.get("n_completed", len(user.history)))  # D-07: 서빙은 context(완독 수), Track A 는 대리값`
    - `reranking/guard.py` L52 → `n_completed = int(user.context.get("n_completed", len(user.history)))` 를 위 줄에 두고 `if self.book_stats is None or n_completed >= self.min_completed or not items:`; docstring L6-7 문장 유지 + 끝에 `(교체됨: context["n_completed"], Phase 5 05-08)` 덧붙임 — `"대리값"`·`"Phase 5"`·`"len(user.history)"` 세 문자열 모두 남는다.
    **1-f. GREEN(revision A5 — 자기 경로만):** `uv run pytest tests/app/test_server_state.py tests/ranking tests/reranking tests/test_architecture.py --no-header` → failed 0. `tests/serving/test_smoke.py`(L175 가 `millie_rec.app.server` 를 import)·`tests/app` 전체·`make smoke` 는 **05-09 wave 2 게이트로 이관**(05-06 이 `api.py` 를 같은 시각에 고치므로 결합 해소).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/app/test_server_state.py tests/ranking tests/reranking tests/test_architecture.py --no-header && wc -l src/millie_rec/app/server.py src/millie_rec/app/pipeline.py src/millie_rec/app/staged.py src/millie_rec/app/pipeline_kr.py</automated>
  </verify>
  <acceptance_criteria>
    - 요약 줄 failed 0(test_server_state 신규 5 · ranking 기준선 + 1 · reranking 기준선 + 1 · 아키 3) — `make smoke`·`tests/app` 전체·`test_smoke.py` 는 05-09 게이트(A5)
    - `wc -l` server.py ≤40 · pipeline.py ≤150 · staged.py ≤150 · pipeline_kr.py ≤150
    - `grep -c "from millie_rec.app.staged import" src/millie_rec/app/pipeline.py` == 1 · `grep -c "threading.local" src/millie_rec/app/staged.py` == 1
    - `grep -c "state=store" src/millie_rec/app/server.py` == 1 · `grep -c "ServingBookStats(catalog, store)" src/millie_rec/app/server.py` == 1 · `grep -c "book_stats=stats" src/millie_rec/app/server.py` == 2
    - `grep -c 'self._tl.bd = {"retrieval"' src/millie_rec/app/staged.py` == 1 · `grep -c "book_stats or catalog" src/millie_rec/app/pipeline_kr.py` == 1
    - `grep -c 'int(user.context.get("n_completed", len(user.history)))' src/millie_rec/ranking/hybrid.py src/millie_rec/reranking/guard.py` → 각 1
    - `grep -c "대리값" src/millie_rec/reranking/guard.py` ≥ 1 · `grep -c "Phase 5" …` ≥ 1 · `grep -c "len(user.history)" …` ≥ 1(기존 docstring 단정 유지)
    - `grep -n "^GUARD_MIN_COMPLETED = 3\|^W_GAP, W_GAP_POS, W_NCOMP_GAP = -0.10, -0.20, -0.01" src/millie_rec/reranking/guard.py src/millie_rec/ranking/hybrid.py` → 2행(상수 freeze 불변)
    - `git diff --stat -- src/millie_rec/serving src/millie_rec/contracts.py src/millie_rec/retrieval src/millie_rec/ranking/blend.py src/millie_rec/reranking/mmr.py` → 출력 없음
    - RED 발췌(AssertionError) 가 SUMMARY 에 있다
  </acceptance_criteria>
  <done>주입·타이밍·1줄 분기·staged.py 이동 완료, 자기 경로 테스트 passed, 상수 불변(smoke 는 05-09). 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 2: ruff · 전역 회귀 · 개발일지 1줄 초안(05-09 인계) · SUMMARY</name>
  <files>src/millie_rec/app/server.py, src/millie_rec/app/pipeline.py, src/millie_rec/app/pipeline_kr.py</files>
  <read_first>
    - Task 1 결과 · ../.assets/개발일지/README.md 작성 규칙(6요소 — 05-09 가 D77 로 기록) · ../.claude/rules/references.md
  </read_first>
  <action>
    1. `uv run ruff format src/millie_rec/app src/millie_rec/ranking/hybrid.py src/millie_rec/reranking/guard.py tests/app/test_server_state.py tests/ranking/test_hybrid.py tests/reranking/test_guard.py && uv run ruff check <같은 경로>` 클린.
    2. 자기 경로 재실행(`tests/app/test_server_state.py tests/ranking tests/reranking tests/test_architecture.py`) failed 0. 전역 pytest·`tests/app` 전체·`make smoke` 는 05-09 게이트(A5) — 여기서 돌리지 않는다.
    3. SUMMARY 에 **개발일지 D77 초안 1항목(6요소)** 을 적어 05-09 에 인계: 제목 `### D77. n_completed 대리값 교체 = UserState.context 분기 1줄(Model 레인 상수 무변경) · cascade.py 신설 · seeds cold-start 는 1행 유지  [축 ③ 추천 구조(★난이도 가드) · main §5-6 / 아키텍처 01 §3-3·§9-3 / 05-CONTEXT D-07·Advisor 확정 3·4·7]` — 문제(freeze 안에서 완독 수를 어떻게 넘기나)·동기(가드가 실제 완독 이벤트에 반응해야 PDF 문장이 참)·선택지((a) history 앞에 완독 배치 / (b) 생성 인자 / (c) context dict)·근거(context 는 계약 필드·값 str·Track A 무영향·기존 테스트 무수정)·장애(없음 또는 실제 겪은 것)·결과(hybrid.py:49·guard.py:52 각 1줄, 되돌리는 조건 = `context` 키 이름 변경 시).
    4. 변경 파일 목록(`git status --short -- src/millie_rec/app src/millie_rec/ranking src/millie_rec/reranking tests/app tests/ranking tests/reranking`).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/app src/millie_rec/ranking/hybrid.py src/millie_rec/reranking/guard.py tests/app/test_server_state.py tests/ranking/test_hybrid.py tests/reranking/test_guard.py && uv run ruff check src/millie_rec/app src/millie_rec/ranking/hybrid.py src/millie_rec/reranking/guard.py tests/app/test_server_state.py tests/ranking/test_hybrid.py tests/reranking/test_guard.py && uv run pytest tests/app/test_server_state.py tests/ranking tests/reranking tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - ruff 클린 · 요약 줄 failed 0
    - SUMMARY 에 `### D77.` 로 시작하는 6요소 초안이 있다
  </acceptance_criteria>
  <done>ruff 클린 · 회귀 0 · D77 초안 · SUMMARY. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `UserState.context["n_completed"]` → guard·hybrid | 서버(`state.py`) 가 채운 문자열. 클라이언트가 직접 넣을 수 없다(cascade 가 UserState 를 만든다) |
| `app/server.py` 모듈 import 시 부작용 | `load_catalog()`·`Database(resolve_db_path())`·`StateStore()` 생성 — 기동 1회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | `context["n_completed"]` 가 정수가 아닌 문자열 | mitigate | 값은 `state.py` 가 `str(int)` 로만 넣는다. `int()` 실패 방어는 하지 않는다(내부 계약 — 방어 코드가 Model 레인 1줄 원칙을 깨뜨림). cascade 가 `int` 를 보장하는지는 05-06 테스트 `context["n_completed"] == "…"` 로 간접 확인 |
| T-05-08-02 | Denial of Service | `last_breakdown` 기록 비용 | accept | `perf_counter()` 4회, 서브 µs |
| T-05-08-03 | Information Disclosure | `last_breakdown` 이 응답에 노출 | accept | 밀리초 3개 — 설계된 노출(백엔드 01 §5 `latency_breakdown`) |
| T-05-08-04 | Tampering | freeze 상수 변경 | mitigate | acceptance grep 으로 `GUARD_MIN_COMPLETED=3`·`W_*` 불변 확인. 상수 줄을 건드리지 않는다 |
</threat_model>

<verification>
- `uv run pytest tests/app/test_server_state.py tests/ranking tests/reranking tests/test_architecture.py --no-header` failed 0(A5 — 전역·smoke 는 05-09)
- ruff 클린 · `wc -l` 4파일 한도 내(staged.py 포함)
- freeze 상수 불변(grep) · `contracts.py`·`serving/**` 무변경(`git diff --stat`)
- 커밋 0
</verification>

<success_criteria>
- **SERV-04** 재료(reset_boost 인자는 05-06) 의 상태 객체가 실제 서버에 주입됨 · **SERV-09** `StateStore` 1개가 Nearline(05-06 lifespan) 과 파이프라인 양쪽에서 같은 객체 · **SERV-03** breakdown 세분 키(D-09) 재료
- D-07 재량 (c) 채택: `context["n_completed"]` 분기 1줄×2, 상수·시그니처·기존 테스트 무변경 · Advisor 확정 5(`ServingBookStats` 주입) · D-09(`last_breakdown` 타이밍만)
- 커밋 0, 개발일지 D77 초안 인계
</success_criteria>

<output>
완료 후 `.planning/phases/05-must/05-08-SUMMARY.md`: RED 발췌(AssertionError) · 변경 요지(server 3줄·pipeline 타이밍·pipeline_kr 패스스루·Model 1줄×2) · `last_breakdown` 세 키 정의 1줄(eligible 필터는 ranking 구간) · 개발일지 D77 초안(6요소) · "동시 진행 중 실패 목록"(있으면) · 변경 파일 목록 · "커밋하지 않는다" 한 줄.
</output>
