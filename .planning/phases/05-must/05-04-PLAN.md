---
phase: 05-must
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/serving/persona.py
  - src/millie_rec/serving/onboarding_meta.json
  - src/millie_rec/serving/onboarding_api.py
  - src/millie_rec/serving/demo_api.py
  - tests/serving/test_persona.py
  - tests/serving/test_onboarding_api.py
  - tests/serving/test_demo_api.py
autonomous: true
requirements: [SERV-04, SERV-05, SERV-06]
lane: Serving (persona·onboarding_api·demo_api Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/badges.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/privacy_api.py
  - src/millie_rec/serving/__init__.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - artifacts/serving/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_smoke.py
  - tests/serving/test_recommend_level0.py
  - tests/serving/test_api_weights.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "POST /api/preferences 는 매번 새 snap_<6hex> 스냅샷을 append 하고(restart=true 도 삭제 없음), users 행이 없으면 cell = 'A' if int(hashlib.sha256(user_key.encode()).hexdigest()[:8], 16) % 2 == 0 else 'B' 로 생성하며, consent=true 면 seeds 마다 library_add(surface='onboarding') 이벤트를 자동 기록하고 consent=false 면 seeds 를 저장하지 않는다 — 응답은 201(백엔드 01 §4, D-08 문서 확정, SERV-04)"
    - "GET /api/meta/onboarding 은 onboarding_meta.json 의 survey_variant·reading_times 5·criteria 5(id ∈ BADGE_TYPES) 에 카탈로그에서 계산한 categories(supported = eligible ≥ 20, 라우터 클로저 1회 캐시) 를 붙여 OnboardingMeta 로 응답한다(D-14, SERV-05)"
    - "GET /api/candidates/onboarding 은 선택 카테고리(≤3) 를 pop_rank 순 라운드로빈으로 n(기본 30, ≤60) 까지 채우고 eligible 만·카테고리 간 중복 없이 cand_<6hex> 를 발급해 candidate_sets 에 저장한다; catalog=None 이면 빈 items 200(D-15, SERV-05)"
    - "POST /api/events 는 pydantic → user_key 형식(422) → ts 파싱(실패 422, 성공 시 UTC Z 정규화 저장) → event_id 중복(INSERT OR IGNORE rowcount → duplicates) → ts_future(+5분)·ts_backdated(마지막 이벤트보다 1h 이상 과거)·book_ineligible 순 첫 적중 1개 플래그 후 저장 → wake() → 202 EventsAccepted — 상태 dict 쓰기는 없다(SERV-06·SERV-09, Advisor 확정 13·16)"
    - "assign_persona(categories, criterion_label) 는 항상 Persona 를 주고 description 은 받침 판정 조사(을/를·으로/로·와/과) 로 '회원님은 IT와 소설을 즐기고, 베스트셀러로 책을 고르는 독서가입니다.' 형태다 — demo/mock/preferences_response.json 과 동일(D-16, revision A1)"
  artifacts:
    - path: "src/millie_rec/serving/persona.py"
      provides: "PERSONAS 4종 · CATEGORY_TO_PERSONA · DESCRIPTION 템플릿 · _josa(받침 판정) · persona_index · assign_persona"
      exports: ["PERSONAS", "CATEGORY_TO_PERSONA", "assign_persona", "persona_index"]
      max_lines: 150
    - path: "src/millie_rec/serving/onboarding_meta.json"
      provides: "survey_variant v1 · reading_times 5 · criteria 5 · subcategories(IT·소설·철학)"
      contains: "\"survey_variant\": \"v1\""
    - path: "src/millie_rec/serving/onboarding_api.py"
      provides: "build_router(*, db, catalog=None, meta_path=META_PATH, now=None) → APIRouter(GET /api/meta/onboarding · GET /api/candidates/onboarding) · load_meta · catalog_categories_meta · new_id · SURVEY_VARIANT · N_DEFAULT/N_MAX/SUPPORTED_MIN/CATEGORIES_MAX (Advisor 확정 17 B2 신설 — 아키 §9-3 목록 외, 05-09 PROGRESS 1줄)"
      exports: ["build_router", "load_meta", "catalog_categories_meta", "new_id", "META_PATH", "SURVEY_VARIANT", "N_DEFAULT", "N_MAX", "SUPPORTED_MIN", "CATEGORIES_MAX"]
      max_lines: 150
    - path: "src/millie_rec/serving/demo_api.py"
      provides: "build_router(*, db, catalog=None, wake=None, invalidate=None, meta_path=META_PATH, now=None) → APIRouter(POST /api/preferences 201 · POST /api/events 202) · assign_cell · USER_KEY_RE(중복 정의) · 품질 게이트 상수 · _quality_flag(con, e, now_dt, catalog)"
      exports: ["build_router", "assign_cell", "USER_KEY_RE", "FLAG_TS_FUTURE", "FLAG_TS_BACKDATED", "FLAG_BOOK_INELIGIBLE", "FUTURE_SKEW_S", "BACKDATED_S"]
      max_lines: 150
    - path: "tests/serving/test_persona.py"
      provides: "매핑 4종 · sha256 결정적 · description 조사 3종 · 항상 존재"
      min_lines: 30
    - path: "tests/serving/test_onboarding_api.py"
      provides: "meta 스키마·supported·캐시 · catalog None · 후보 라운드로빈·cand_ 발급·eligible·상한 422·catalog None 빈 items"
      min_lines: 60
    - path: "tests/serving/test_demo_api.py"
      provides: "preferences 201·스냅샷 2개 append·cell sha256·library_add 자동·consent=false·invalidate/wake 호출 · events dedup·quality_flag 3종·ts 정규화·ts 파싱 실패 422·202·wake 호출 · user_key 형식 422 · 상태 미갱신 grep · extra forbid"
      min_lines: 110
  key_links:
    - from: "src/millie_rec/serving/demo_api.py"
      to: "src/millie_rec/serving/persona.py"
      via: "preferences → assign_persona(body.categories, criterion_label) → PersonaOut + preference_snapshots.persona JSON"
      pattern: "assign_persona\\("
    - from: "src/millie_rec/serving/demo_api.py"
      to: "src/millie_rec/serving/onboarding_api.py"
      via: "from millie_rec.serving.onboarding_api import META_PATH, SURVEY_VARIANT, load_meta, new_id (같은 플랜·같은 슬라이스)"
      pattern: "from millie_rec.serving.onboarding_api import"
    - from: "src/millie_rec/serving/demo_api.py · onboarding_api.py"
      to: "src/millie_rec/serving/schemas.py"
      via: "OnboardingMeta·CandidateSet·PreferencesRequest·PreferencesResponse·EventIn·EventsBatchIn·EventsAccepted 그대로(필드 추가 없음)"
      pattern: "from millie_rec.serving.schemas import"
    - from: "src/millie_rec/serving/demo_api.py"
      to: "events INSERT → wake()"
      via: "INSERT OR IGNORE … rowcount(확정 16); if accepted and wake: wake() (확정 13 — wake 는 05-06 이 call_soon_threadsafe 클로저로 주입)"
      pattern: "wake\\(\\)"
---

<objective>
Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md "Phase 5: 서빙 Must 완성") 의 **취향 설정·이벤트 적재 API** 를 TDD 한 사이클로 만든다: `serving/persona.py`(D-16 매핑 4종, 받침 조사, 항상 존재), `serving/onboarding_meta.json`(D-14 1벌), **`serving/onboarding_api.py`**(`GET /api/meta/onboarding` · `GET /api/candidates/onboarding` — Advisor 확정 17 B2 로 150줄 분할을 지금 확정), `serving/demo_api.py`(`POST /api/preferences` · `POST /api/events`). `POST /api/ratings`(Should) 는 wave 4 Plan 05-11 의 별도 파일 `ratings_api.py`(B5) — `demo_api.py` 는 이 플랜 이후 변경 없음. 두 라우터는 **팩토리 함수**로 의존을 받고 `create_app` 배선은 Plan 05-06 이 한다(Advisor 확정 2).

Purpose: ROADMAP Success Criteria 2 "취향 설정 흐름이 왕복한다: meta → candidates → preferences(스냅샷 append·cell sha256·library_add 자동)" (**SERV-04·SERV-05**, 수용 기준 '재설정 → 새 앵커·기록 보존'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)) + Success Criteria 3 "`POST /api/events` 가 품질 게이트를 통과한 이벤트를 저장" (**SERV-06**, 수용 기준 '노출 로그 4필드'). PDF 축② "온보딩 7단계 = 5종 신호" 의 서버 입구, 축③ Nearline 경계(핸들러는 INSERT + wake 만). 결정 'Should 꼬리·온보딩 메타·후보·페르소나'(개발일지 2026-09-06 파일 항목 D76).
Output: `src` 4 신규(py 3 + json 1), 테스트 3 신규. 커밋 0.

**형제 플랜 불변(Advisor 확정 2):** `db.py` 는 `Database.connect()` 만 쓴다(05-03 의 `execute` 헬퍼에 의존하지 않는다 — 쓰기는 `with con:` 으로 커밋, Advisor 확정 16). `compose.py`·`state.py`·`fallback.py`·`privacy_api.py` import 금지 — `USER_KEY_RE` 는 privacy_api 와 **같은 값을 중복 정의**(revision C5, 주석 1줄). 상태 갱신·캐시 무효화는 **주입된 callable** 을 부르기만 한다(`wake()`·`invalidate(user_key)`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/api.py
@src/millie_rec/serving/db.py
@src/millie_rec/serving/schema.sql
@tests/conftest.py
@tests/serving/test_smoke.py
@tests/test_architecture.py
@demo/config/onboarding.json
@demo/mock/preferences_response.json
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@../.claude/rules/serving.md
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/data.md
@../.claude/rules/references.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 오직 `uv run pytest <대상> --no-header`. 맨 `pytest` 금지(Python 3.9). RED 는 `AssertionError`/`E  assert`(HTTP 테스트는 상태 코드·본문 단정이 깨져야 RED — `ImportError`·`AttributeError` 는 아니다). 스텁 라우터는 **모든 라우트를 등록하고 틀린 값(빈 목록·고정 문자열)** 을 돌려준다. `pytest.skip` 금지. 기존 테스트 단정 수정 금지. SQLite `tmp_path`, 시간 `now` 주입, 네트워크 0.

**star 의존(`../.claude/rules/architecture.md`):** 세 py 파일은 `millie_rec.contracts`·`millie_rec.serving.schemas`·`millie_rec.serving.db`(타입)·서로(같은 플랜) 만 import. `data`·`app`·`ranking` 금지. `Catalog` Protocol 로 카탈로그를 받는다. `demo/` 폴더를 읽지 않는다(D-14 — `onboarding_meta.json` 은 serving 패키지 안 1벌, 텍스트 동일성은 `/contract-sync` 가 Phase 6 에서 검사).

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄 — B2 분할로 라우트 2개씩. 핸들러 본문 ≤15줄, SQL 문자열은 상단 상수, JSON 직렬화는 `json.dumps(v, ensure_ascii=False)` 1줄. 클래스 없음. `catalog_categories_meta` 는 compose.py `catalog_categories` 와 **중복 허용**(revision C11 — wave 1 독립성, `_shared` 는 3곳부터; 주석 1줄).

**서빙(`../.claude/rules/serving.md`):** "HTTP 핸들러는 전부 동기 `def`(스레드풀)." · B008 회피: 쿼리 파라미터는 `Annotated[..., Query()]`. · "A/B 셀: `int(hashlib.sha256(user_key.encode()).hexdigest()[:8], 16) % 2` — CPython `hash()` 금지. 최초 생성 시 `users.cell`에 저장, 이후 저장값." · "품질 게이트(`POST /api/events`): pydantic → `event_id` 중복 무시 → `ts` 검사(미래 +5분·1h 이상 과거 → `quality_flag`) → `Catalog.eligible()` → insert." · "요청 핸들러는 상태를 갱신하지 않고 이벤트만 적재한다." · "후보 정렬 = 선택 카테고리 ∩ 카탈로그, `pop_rank` 순." · ID 규약(백엔드 01 §0): `snap_<6hex>` `cand_<6hex>` = `uuid.uuid4().hex[:6]`; `user_key` = 클라이언트 UUIDv4 → 형식 검증 `USER_KEY_RE = ^[A-Za-z0-9_-]{1,64}$`(C5). 백엔드 01 §4: 응답 **`201`**(revision D5 확인).

**데이터 표기(`../.claude/rules/data.md`):** 응답에 `description` 없음. 표지는 `image_url` 핫링크 그대로.

**완료 기준(자기 경로만):** `uv run ruff format --check src/millie_rec/serving/persona.py src/millie_rec/serving/onboarding_api.py src/millie_rec/serving/demo_api.py tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py && uv run ruff check <같은 경로>` 클린 · `uv run pytest tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py tests/test_architecture.py --no-header` failed 0. `make smoke` 는 wave 1 종료 후 Advisor 1회.
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. schemas.py 는 변경 금지(freeze). "이 플랜이 만드는 공개 시그니처" 는 05-06·05-07(bench) 이 verbatim 소비. -->

From src/millie_rec/contracts.py (verbatim):
```python
BADGE_TYPES = ("bestseller", "review", "author", "publisher", "buzz", "light")
EVENT_TYPES = (…13종…)   # "library_add" · "impression" · "preference_book_selected" 포함
@dataclass(frozen=True, slots=True)
class Persona: name: str; work: str; quote: str; description: str
class Catalog(Protocol):
    def meta(self, book_ids: Sequence[int]) -> list[dict]: ...
    def popular(self, categories: Sequence[str] = (), n: int = 50) -> list[int]: ...
    def eligible(self, book_ids: Sequence[int]) -> list[int]: ...
```

From src/millie_rec/serving/schemas.py (verbatim, `extra="forbid"` 전부):
```python
class Criterion(_Strict): id: BadgeType; label: str
class CategoryMeta(_Strict): name: str; supported: bool; subcategories: list[str] = []
class OnboardingMeta(_Strict): survey_variant: str; reading_times: list[str]; criteria: list[Criterion]; categories: list[CategoryMeta]
class CandidateItem(_Strict): book_id: int; title: str; authors: str | None = None; image_url: str | None = None; position: int; book_format: str | None = None
class CandidateSet(_Strict): candidate_set_id: str; survey_variant: str; created_at: str; items: list[CandidateItem]
class PreferencesRequest(_Strict):
    user_key: str; consent: bool = True; reading_time: str | None = None
    categories: list[str] = Field(default_factory=list, max_length=3); criterion: BadgeType | None = None
    subcategories: list[str] = Field(default_factory=list, max_length=9); seeds: list[int] = Field(default_factory=list, max_length=30)
    candidate_set_id: str | None = None; restart: bool = False
class PersonaOut(_Strict): name: str; work: str; quote: str; description: str
class PreferencesResponse(_Strict): user_key: str; preference_snapshot_id: str; created_at: str; cell: Cell; snapshots_count: int; persona: PersonaOut | None = None
class EventIn(_Strict):
    event_id: str; user_key: str; book_id: int | None = None; event_type: EventType; ts: str; surface: str | None = None
    row_id: RowId | None = None; position: int | None = None; selected: bool | None = None; recommendation_id: str | None = None
    model_version: str | None = None; preference_snapshot_id: str | None = None; candidate_set_id: str | None = None; payload: dict[str, str] = {}
class EventsBatchIn(_Strict): events: list[EventIn] = Field(min_length=1, max_length=50)
class FlaggedEvent(_Strict): event_id: str; quality_flag: str
class EventsAccepted(_Strict): accepted: int; duplicates: int = 0; flagged: list[FlaggedEvent] = []
```

From src/millie_rec/serving/schema.sql (INSERT 대상 컬럼 그대로):
```sql
users(user_key TEXT PRIMARY KEY, created_at TEXT, consent INTEGER, cell TEXT, is_new INTEGER)
preference_snapshots(snapshot_id TEXT PRIMARY KEY, user_key, created_at, reading_time, categories, criterion, subcategories, seeds, persona)   -- JSON TEXT: categories subcategories seeds persona
events(event_id TEXT PRIMARY KEY, user_key, book_id INTEGER, event_type, ts, surface, row_id, position INTEGER, selected INTEGER,
       recommendation_id, model_version, preference_snapshot_id, candidate_set_id, payload, quality_flag)
candidate_sets(candidate_set_id TEXT PRIMARY KEY, user_key, ts, book_ids, survey_variant)   -- JSON: book_ids
```

From src/millie_rec/serving/api.py L33-35·L128-136 (관례 — 422 형태·`Annotated Query`; `_422` 는 비공개라 각 파일에 **중복 정의**):
```python
def _422(param: str, msg: str) -> HTTPException:
    return HTTPException(422, detail=[{"loc": ["query", param], "msg": msg, "type": "value_error"}])
```

From src/millie_rec/serving/db.py: `Database.connect() -> sqlite3.Connection`(thread-local). 쓰기는 `with con:` 으로 커밋(Advisor 확정 16).

From demo/mock/preferences_response.json (description 정본 문장 — revision A1): `"회원님은 IT와 소설을 즐기고, 베스트셀러로 책을 고르는 독서가입니다."`

**이 플랜이 만드는 공개 시그니처(05-06·05-07 이 verbatim 소비):**
```python
# serving/persona.py
PERSONAS: tuple[Persona, ...]                     # 4종, description="" 템플릿 전
CATEGORY_TO_PERSONA: dict[str, int]              # 밀리 분류명 → PERSONAS 인덱스
DESCRIPTION = "회원님은 {cats}{eul} 즐기고, {criterion}{ro} 책을 고르는 독서가입니다."   # 조사는 _josa 로 채움(A1)
CATS_NONE, CRITERION_NONE = "다양한 분야", "취향"
def _josa(word: str, with_final: str, without_final: str) -> str   # 받침: (ord(word[-1]) - 0xAC00) % 28 != 0 (한글 음절 아니면 받침 없음 취급); with_final == "으로" 이고 ㄹ 받침(% 28 == 8) 이면 "로"
def persona_index(categories: Sequence[str]) -> int
def assign_persona(categories: Sequence[str], criterion_label: str | None) -> Persona

# serving/onboarding_api.py (B2)
META_PATH = Path(__file__).parent / "onboarding_meta.json"
SURVEY_VARIANT = "v1"
N_DEFAULT, N_MAX = 30, 60                        # 백엔드 01 §0 "n ≤ 60" · §3 기본 30
SUPPORTED_MIN = 20                               # D-14 supported = eligible ≥ 20
CATEGORIES_MAX = 3                               # 후보 쿼리 categories ≤3
def new_id(prefix: str) -> str                   # prefix + uuid.uuid4().hex[:6]  ("snap_" · "cand_")
def load_meta(path: Path = META_PATH) -> dict
def catalog_categories_meta(catalog: Catalog | None, subcategories: dict[str, list[str]]) -> list[CategoryMeta]   # compose.catalog_categories 와 중복 허용(C11)
def build_router(*, db: Database, catalog: Catalog | None = None, meta_path: Path = META_PATH,
                 now: Callable[[], datetime] | None = None) -> APIRouter

# serving/demo_api.py
USER_KEY_RE = re.compile(r"^[A-Za-z0-9_-]{1,64}$")   # privacy_api.py 와 같은 값 중복 정의(C5, wave 1 독립성)
FUTURE_SKEW_S, BACKDATED_S = 300, 3600
FLAG_TS_FUTURE, FLAG_TS_BACKDATED, FLAG_BOOK_INELIGIBLE = "ts_future", "ts_backdated", "book_ineligible"
SURFACE_ONBOARDING = "onboarding"
def assign_cell(user_key: str) -> str            # "A" if int(hashlib.sha256(user_key.encode()).hexdigest()[:8], 16) % 2 == 0 else "B"
def _quality_flag(con: sqlite3.Connection, e: EventIn, now_dt: datetime, catalog: Catalog | None) -> str | None   # C4: catalog 인자
def build_router(*, db: Database, catalog: Catalog | None = None, wake: Callable[[], None] | None = None,
                 invalidate: Callable[[str], object] | None = None, meta_path: Path = META_PATH,
                 now: Callable[[], datetime] | None = None) -> APIRouter   # D3: invalidate 반환형 object
```
</interfaces>
</context>

<feature>
  <name>취향 설정·이벤트 API — persona(D-16·A1) · onboarding_meta.json(D-14) · onboarding_api 2 라우트(D-14·D-15) · demo_api 2 라우트(D-08·품질 게이트·C3·C4·C5)</name>
  <files>src/millie_rec/serving/persona.py, src/millie_rec/serving/onboarding_meta.json, src/millie_rec/serving/onboarding_api.py, src/millie_rec/serving/demo_api.py, tests/serving/test_persona.py, tests/serving/test_onboarding_api.py, tests/serving/test_demo_api.py</files>
  <behavior>
    **persona.py**
    - `[p.name for p in PERSONAS] == ["오디세우스", "셜록 홈즈", "돈키호테", "제인 에어"]`; `PERSONAS[0].work == "오디세이아" and PERSONAS[0].quote == "지혜로 승리하리라!"`; `PERSONAS[1].work == "주홍색 연구"`, quote `"사소한 것이 가장 중요하다."`; `PERSONAS[2].work == "돈키호테"`, quote `"이룰 수 없는 꿈을 꾸리라!"`; `PERSONAS[3].work == "제인 에어"`, quote `"나는 나 자신의 주인입니다."`
    - 매핑(PATTERNS '카탈로그 실측', Advisor 확정 12): `persona_index(["경제경영"]) == persona_index(["자기계발"]) == persona_index(["IT"]) == 0`; `["소설"]`·`["과학"]`·`["철학"]` → 1; `["인문"]`·`["역사"]`·`["사회"]` → 2; `["에세이/시"]`·`["라이프스타일"]` → 3; 지배 카테고리 = `categories[0]`: `persona_index(["인문", "IT"]) == 2`
    - 미매핑 결정적: `persona_index(["오디오북"]) == int(hashlib.sha256("오디오북".encode()).hexdigest()[:8], 16) % 4`(테스트가 같은 식으로 손계산), 두 번 호출 같은 값; `persona_index([]) == 0`
    - 조사(revision A1): `_josa("소설", "을", "를") == "을"`, `_josa("IT", "을", "를") == "를"`(한글 음절 아님 → 받침 없음), `_josa("베스트셀러", "으로", "로") == "로"`, `_josa("평가", "으로", "로") == "로"`, `_josa("취향", "으로", "로") == "으로"`, `_josa("서울", "으로", "로") == "로"`(ㄹ 받침 예외), `_josa("IT", "과", "와") == "와"`, `_josa("소설", "과", "와") == "과"`
    - `assign_persona(["IT", "소설"], "베스트셀러")` → `Persona(name="오디세우스", work="오디세이아", quote="지혜로 승리하리라!", description="회원님은 IT와 소설을 즐기고, 베스트셀러로 책을 고르는 독서가입니다.")`(**mock 정본과 동일 — A1**); `assign_persona(["소설", "IT"], "베스트셀러").description == "회원님은 소설과 IT를 즐기고, 베스트셀러로 책을 고르는 독서가입니다."`; `assign_persona(["소설"], None).description == "회원님은 소설을 즐기고, 취향으로 책을 고르는 독서가입니다."`; `assign_persona([], "리뷰, 별점 등 대중의 평가").description == "회원님은 다양한 분야를 즐기고, 리뷰, 별점 등 대중의 평가로 책을 고르는 독서가입니다."`; 결과는 항상 `Persona`(None 없음, D-16)

    **onboarding_meta.json** (정적, 화면 01 §4-2 · 백엔드 01 §2 · `demo/config/onboarding.json` S1·S3 옵션과 글자 동일):
    ```json
    {"survey_variant": "v1",
     "reading_times": ["아침, 하루를 시작할 때", "점심시간이나 짧은 휴식 시간", "저녁, 하루를 마치며", "잠들기 전", "주말이나 휴일, 여유로울 때 몰아서"],
     "criteria": [{"id": "author", "label": "좋아하는 작가"}, {"id": "publisher", "label": "좋아하는 출판사"}, {"id": "bestseller", "label": "베스트셀러"},
                  {"id": "buzz", "label": "화제작 (SNS,셀럽 추천, 수상도서)"}, {"id": "review", "label": "리뷰, 별점 등 대중의 평가"}],
     "subcategories": {"IT": ["개발/프로그래밍", "그래픽/멀티미디어", "IT 교양", "e비즈니스", "오피스 활용", "컴퓨터 수험서"],
                       "소설": ["추리/스릴러", "SF", "판타지", "영미 소설", "한국 소설", "일본 소설", "유럽 소설"],
                       "철학": ["동양", "정치/경제", "예술/문화", "서양"]}}
    ```
    `categories` 키는 **없다**(D-14 카탈로그 계산).

    **공통 가짜·조립** (두 라우터 테스트): `_Cat(extra_it: int = 0)` 20권(`tests/conftest.py::_sample_book` 형태 — `categories` 4종 순환 `소설 에세이 경제경영 인문`, 자격 없는 18·19·20, `popular(categories, n)` 은 pop_rank(=book_id) 순 필터, `meta` 5필드 + `book_format`; `extra_it` 권의 `IT` 카테고리 책 21.. 추가); `_Spy.calls: list`; 앱은 `FastAPI()` + 해당 `build_router` + `TestClient` — `create_app` 미사용(05-06 배선 전). `FIXED_NOW = datetime(2026, 9, 7, 12, 0, 0, tzinfo=UTC)`, `_iso(dt) = dt.isoformat(timespec="seconds").replace("+00:00", "Z")`, `_no_trace(r)`.

    **onboarding_api.py**
    - `# ── 계약 ──` `GET /api/meta/onboarding` 200 → `OnboardingMeta.model_validate(r.json())`; `reading_times` 5개 첫 항목 `"아침, 하루를 시작할 때"`; `[c.id for c in criteria] == ["author", "publisher", "bestseller", "buzz", "review"]` 전부 `BADGE_TYPES` 안; `categories` 이름 집합 == `{"소설", "에세이", "경제경영", "인문"}`, 전부 `supported is False`(각 5권 < 20); `_Cat(extra_it=25)` 이면 `IT.supported is True and IT.subcategories == [6개]`, `소설.subcategories == [7개]`, `에세이.subcategories == []`; **캐시(C11):** 같은 클라이언트로 두 번 호출해도 `cat.popular_calls == 1`(`_Cat` 에 카운터)
    - `catalog=None` → `categories == []` 이고 200
    - `GET /api/candidates/onboarding?categories=소설,에세이&n=6` 200 → `CandidateSet.model_validate`; `candidate_set_id` 는 `cand_` + 6hex(`len == 11`); `survey_variant == "v1"`; items book_id == `[1, 2, 5, 6, 9, 10]`(라운드로빈: 소설 1 → 에세이 2 → 소설 5 → 에세이 6 → …, D-15); `position == 0..5`; `title` 채움; `book_format in ("전자책", "오디오북", "챗북")`; `candidate_sets` 행 1개(`book_ids` JSON == 같은 6개, `survey_variant == "v1"`, `user_key` 쿼리 값 또는 NULL)
    - `# ── 정확성 ──` 라운드로빈이 독식을 막는다: `categories=소설,경제경영,인문&n=9` → 세 카테고리 각 3권; 자격 없는 18·19·20 은 어떤 조합에도 없음
    - `# ── 안전성 ──` `n=100` → 422(`le=60`); `n=0` → 422; `categories` 4개 → 422(`CATEGORIES_MAX`, `loc == ["query", "categories"]`); `categories=없는분류` → 200 `items == []`; **`catalog=None` → 200 `items == []`(revision C4 — AttributeError 없음)**; 응답에 `"Traceback"` 없음

    **demo_api.py**
    - `# ── 계약 ──` `POST /api/preferences` 본문 `{"user_key": "u-1", "consent": true, "reading_time": "저녁, 하루를 마치며", "categories": ["IT", "소설"], "criterion": "bestseller", "subcategories": ["SF"], "seeds": [1, 2, 3, 4, 5], "candidate_set_id": "cand_abc123"}` → **201**(백엔드 01 §4, D5), `PreferencesResponse.model_validate`; `preference_snapshot_id` `snap_`+6hex; `cell == assign_cell("u-1")`(테스트가 sha256 식으로 손계산 — B 셀이 나오는 키를 `USER_B` 상수로 둔다, Worker 가 `python -c` 로 계산); `snapshots_count == 1`; `persona.name == "오디세우스"`, **`persona.description == "회원님은 IT와 소설을 즐기고, 베스트셀러로 책을 고르는 독서가입니다."`**(A1, criterion 라벨은 meta json 의 `label`)
    - append(D-08·SERV-04): 같은 user_key 로 두 번째 `POST`(`restart: true`, categories ["인문"]) → `snapshots_count == 2`, 새 `snapshot_id` 다름, `SELECT COUNT(*) FROM preference_snapshots WHERE user_key='u-1'` == 2(삭제 없음), `users` 행 1개·`cell` 첫 응답과 동일(저장값), `persona.name == "돈키호테"`
    - `library_add` 자동: 첫 POST 후 `SELECT book_id, event_type, surface, preference_snapshot_id, candidate_set_id, ts FROM events WHERE user_key='u-1' ORDER BY book_id` → 5행, 전부 `("library_add", "onboarding", <snapshot_id>, "cand_abc123", <FIXED_NOW Z 문자열>)`, `event_id` 5개 서로 다름(uuid4 hex 32자); `wake_spy.calls == 1`; `inv_spy.calls == ["u-1"]`
    - `consent=false`: `{"user_key": "u-2", "consent": false, "seeds": [1, 2]}` → 201, 스냅샷 1행 `seeds == "[]"`, `events` 0행, `users.consent == 0`, `persona` 있음(빈 categories → 결정적 기본), `wake_spy` 호출 없음
    - `# ── 정확성 ──` `POST /api/events` 단건 `{"event_id": "e1", "user_key": "u-1", "book_id": 1, "event_type": "reader_open", "ts": "2026-09-07T12:00:00+00:00"}` → **202** `EventsAccepted(accepted=1, duplicates=0, flagged=[])`; **DB 의 `ts` 컬럼은 `"2026-09-07T12:00:00Z"`(revision C3 — Z 정규화)**; 같은 본문 재전송 → `accepted == 0, duplicates == 1`(확정 16 `INSERT OR IGNORE` rowcount); 배치 `{"events": [e2, e3, e2(중복)]}` → `accepted == 2, duplicates == 1`; `wake_spy.calls` 는 accepted > 0 인 호출마다 +1(중복만이면 호출 안 함)
    - 품질 게이트 3종(순서: ts_future → ts_backdated → book_ineligible, 첫 적중 1개): ts = FIXED_NOW + 6분 → `flagged == [{"event_id": …, "quality_flag": "ts_future"}]` 이고 **저장됨**; 유저 마지막 이벤트(FIXED_NOW) 보다 2h 과거 → `"ts_backdated"`; `book_id=18`(자격 없음) → `"book_ineligible"`; 정상 → 플래그 없음; `book_id` None → 플래그 없음; FIXED_NOW + 4분 → 없음; 59분 과거 → 없음
    - **ts 파싱 실패 → 422(revision C3):** `"ts": "not-a-time"` → 422, DB 에 행 없음, `wake_spy` 호출 없음(배치 중 하나라도 파싱 실패면 배치 전체 422 — INSERT 전에 전부 파싱)
    - **user_key 형식(revision C5):** preferences `user_key: "a';b"` → 422; events `user_key: "x y"`(공백) → 422; 65자 → 422; 정상 UUIDv4 하이픈 포함 36자 → 201/202
    - SERV-09 경계: 소스 grep — `"apply_event" not in src and "StateStore" not in src`
    - `# ── 안전성 ──` `extra="forbid"`: preferences 본문 `{"foo": 1}` → 422; events `event_type: "itemknn_click"` → 422; `categories` 4개 → 422; `seeds` 31개 → 422; 배치 51건 → 422; 빈 배치 → 422
    - `wake=None`·`invalidate=None`·`catalog=None` 라우터 → preferences 201·events 202(없는 콜백은 건너뜀; `catalog=None` 이면 `book_ineligible` 검사 생략 — C4 `_quality_flag(con, e, now_dt, catalog)` 의 `catalog is None` 분기)
    - SQL 인젭션 값은 C5 형식 검증에서 422 로 막힌다(`x'; DROP TABLE users; --` → 422, `users` 테이블 생존); 소스 grep `execute(f` 0건
    - 예외 문자열 미노출: 전 응답 `"Traceback"` 없음
  </behavior>
  <implementation>Task 2 action 그대로.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): persona·onboarding_api·demo_api 스텁 + onboarding_meta.json + 테스트 3파일 → AssertionError</name>
  <files>src/millie_rec/serving/persona.py, src/millie_rec/serving/onboarding_meta.json, src/millie_rec/serving/onboarding_api.py, src/millie_rec/serving/demo_api.py, tests/serving/test_persona.py, tests/serving/test_onboarding_api.py, tests/serving/test_demo_api.py</files>
  <read_first>
    - src/millie_rec/serving/schemas.py L67-129(§2~§4 모델), L190-220(§6 모델) — 필드 이름·제약 그대로
    - src/millie_rec/serving/schema.sql L5-11·L12-17·L28-30(users·preference_snapshots·events·candidate_sets 컬럼)
    - src/millie_rec/serving/api.py L33-35(`_422` — 중복 정의), L128-136(`Annotated Query` 관례)
    - src/millie_rec/serving/db.py L32-39(`connect()`), L10(`SCHEMA_PATH` — `META_PATH` 관례)
    - src/millie_rec/contracts.py L79-80(`BADGE_TYPES`), L146-153(`Persona`), L316-321(`Catalog`)
    - demo/mock/preferences_response.json(description 정본 문장 — A1) · demo/config/onboarding.json S1·S3 `options`(글자 동일성 확인용 — 서버는 읽지 않는다)
    - .planning/phases/05-must/05-CONTEXT.md D-14·D-15·D-16 + "(문서 확정, 재확인) POST /api/preferences" + "(문서 확정, 재확인) POST /api/events 품질 게이트 순서"
    - .planning/phases/05-must/05-PATTERNS.md §3(persona), §4(demo_api 골격·ID·셀·라운드로빈·supported), §8(json 텍스트 원천), "No Analog" `assign_cell`, "카탈로그 실측" 표
    - ../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md §0(ID 형식·n ≤ 60)·§2·§3·§4(201·append·library_add·consent=false)·§6(202·게이트 순서·플래그 이름)
    - tests/conftest.py L46-90(`_sample_book`) · tests/serving/test_smoke.py L54-64(`_FakeCatalog`)
  </read_first>
  <action>
    **1-a. `onboarding_meta.json`** — `<behavior>` 의 JSON 을 글자 그대로(들여쓰기 2, 한글 그대로). `categories` 키 없음.
    **1-b. `persona.py` 스텁** — docstring `"""페르소나 4종 매핑 — S6 설명 레이어, 모델 입력 아님(화면 01 §4-5, 05-CONTEXT D-16). 항상 존재. 조사는 받침 판정(revision A1)."""`. `PERSONAS` 4종 실제 값 · `CATEGORY_TO_PERSONA` 실제 매핑 · `DESCRIPTION`·`CATS_NONE`·`CRITERION_NONE`. `_josa` → `return without_final`(틀린 값), `persona_index` → `return 3`, `assign_persona` → `return PERSONAS[3]`(description 빈 채).
    **1-c. `onboarding_api.py` 스텁(B2)** — docstring `"""온보딩 메타·후보 API(백엔드 서빙 01 §2·§3) — APIRouter 팩토리. Advisor 확정 17 B2 로 demo_api 에서 분리(아키 §9-3 목록 외 신설)."""`. 상수 전부 실제 값. `new_id(prefix)` → `prefix + "000000"`. `load_meta` → 진짜(`json.loads(path.read_text(encoding="utf-8"))`). `catalog_categories_meta` → `return []`. `build_router` 는 `APIRouter(prefix="/api")` 에 2 라우트 등록: meta → `OnboardingMeta(survey_variant="v0", reading_times=[], criteria=[], categories=[])`; candidates → `CandidateSet(candidate_set_id="cand_000000", survey_variant="v0", created_at="", items=[])`(DB 쓰기 없음).
    **1-d. `demo_api.py` 스텁** — docstring `"""취향 설정·이벤트 API(백엔드 서빙 01 §4·§6) — APIRouter 팩토리. 상태 갱신 없음(SERV-09), events INSERT + wake 만(Advisor 확정 13·16)."""`. 상수 전부(`USER_KEY_RE` 중복 정의 + 주석 `# privacy_api.py 와 같은 값 — wave 1 독립성(revision C5)`). `assign_cell` → `return "A"`(고정). `_quality_flag(con, e, now_dt, catalog)` → `return None`. `build_router` 2 라우트: preferences → `PreferencesResponse(user_key=body.user_key, preference_snapshot_id="snap_000000", created_at="", cell="A", snapshots_count=0)`(status_code 201 데코레이터); events → `EventsAccepted(accepted=0)`(202). DB 쓰기·wake·invalidate·형식 검증 없음.
    **1-e. `tests/serving/test_persona.py`** (6건): `test_personas_four_names_works_quotes` · `test_persona_index_mapping_table_and_dominant_first_category` · `test_persona_index_unmapped_is_sha256_deterministic_and_empty_is_zero` · `test_josa_final_consonant_rules_including_rieul_and_non_hangul` · `test_assign_persona_description_matches_mock_sentence` · `test_assign_persona_always_returns_persona_even_without_inputs`
    **1-f. `tests/serving/test_onboarding_api.py`** (7건): `test_meta_onboarding_parses_and_lists_criteria_reading_times` · `test_meta_categories_supported_when_eligible_ge_20_subcategories_static_and_cached` · `test_meta_without_catalog_has_empty_categories` · `test_candidates_round_robin_pop_rank_and_persists_candidate_set` · `test_candidates_three_categories_share_evenly_and_exclude_ineligible` · `test_candidates_limits_422_unknown_category_empty` · `test_candidates_without_catalog_is_200_empty_no_trace`
    **1-g. `tests/serving/test_demo_api.py`** (13건): `# ── 계약 ──` `test_preferences_201_snapshot_cell_sha256_persona_sentence` · `test_preferences_appends_second_snapshot_keeps_cell_and_history` · `test_preferences_records_library_add_per_seed_with_z_ts_and_calls_wake_invalidate` · `test_preferences_consent_false_stores_no_seeds_no_events_no_wake` / `# ── 정확성 ──` `test_events_single_and_batch_dedup_202_and_ts_normalized_to_z` · `test_events_quality_flags_future_backdated_ineligible_first_hit_stored` · `test_events_unparseable_ts_is_422_nothing_stored` · `test_user_key_format_422_on_preferences_and_events_uuid4_passes` · `test_events_handler_has_no_state_writes_source_grep` / `# ── 안전성 ──` `test_extra_forbid_and_limits_422` · `test_router_without_callbacks_or_catalog_still_works` · `test_sql_injection_value_blocked_by_format_and_no_fstring_sql` · `test_no_traceback_in_responses`
    **1-h. RED 확인:** `uv run pytest tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py --no-header 2>&1 | tail -50` → 26건 대다수 failed, 사유 `AssertionError`/`E  assert`(422 계열 2~3건은 pydantic 이 스텁에서도 422 → passed 정상). `uv run pytest tests/test_architecture.py --no-header` → `3 passed`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py --no-header 2>&1 | tail -40; uv run pytest tests/test_architecture.py --no-header; uv run python -c "import json,pathlib; d=json.loads(pathlib.Path('src/millie_rec/serving/onboarding_meta.json').read_text(encoding='utf-8')); print(len(d['reading_times']), [c['id'] for c in d['criteria']], sorted(d['subcategories']), 'categories' in d)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py` → 6, 7, 13
    - RED 출력에 `AssertionError`/`E  assert` 있고 `ImportError`·`AttributeError`·`TypeError`·`sqlite3.OperationalError` 없음
    - python 한 줄 출력: `5 ['author', 'publisher', 'bestseller', 'buzz', 'review'] ['IT', '소설', '철학'] False`
    - `grep -c "아침, 하루를 시작할 때" src/millie_rec/serving/onboarding_meta.json demo/config/onboarding.json` → 각 1
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(data\|app\|retrieval\|ranking\|reranking\|evaluation\|serving\.compose\|serving\.state\|serving\.fallback\|serving\.privacy_api\)" src/millie_rec/serving/demo_api.py src/millie_rec/serving/onboarding_api.py src/millie_rec/serving/persona.py` → 출력 없음
  </acceptance_criteria>
  <done>신규 26건 중 ≥23 AssertionError RED, json 1벌 정확, 아키텍처 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): persona 조사·매핑 + onboarding_api 2 라우트 + demo_api 2 라우트(C3·C4·C5·확정 13·16) → 26 passed</name>
  <files>src/millie_rec/serving/persona.py, src/millie_rec/serving/onboarding_api.py, src/millie_rec/serving/demo_api.py</files>
  <read_first>
    - Task 1 스텁·테스트(단정 값·sha256 손계산 키)
    - .planning/phases/05-must/05-PATTERNS.md §4 골격 스니펫 · Shared B(동기 def·response_model·Annotated Query) · Shared D(ID·타임스탬프)
  </read_first>
  <action>
    **2-a. `persona.py`**
    ```python
    PERSONAS = (Persona("오디세우스", "오디세이아", "지혜로 승리하리라!", ""), Persona("셜록 홈즈", "주홍색 연구", "사소한 것이 가장 중요하다.", ""),
                Persona("돈키호테", "돈키호테", "이룰 수 없는 꿈을 꾸리라!", ""), Persona("제인 에어", "제인 에어", "나는 나 자신의 주인입니다.", ""))
    CATEGORY_TO_PERSONA = {"경제경영": 0, "자기계발": 0, "IT": 0, "소설": 1, "과학": 1, "철학": 1,
                           "인문": 2, "역사": 2, "사회": 2, "에세이/시": 3, "라이프스타일": 3}   # PATTERNS 카탈로그 실측 29종 중 11, 나머지 sha256
    DESCRIPTION = "회원님은 {cats}{eul} 즐기고, {criterion}{ro} 책을 고르는 독서가입니다."
    def _josa(word, with_final, without_final):
        """받침 판정(revision A1): 한글 음절 마지막 글자의 (code−0xAC00) % 28 != 0 이면 받침. 비한글은 받침 없음. '으로' 는 ㄹ 받침(==8) 이면 '로'."""
        if not word: return without_final
        code = ord(word[-1]) - 0xAC00
        if not 0 <= code <= 11171: return without_final
        final = code % 28
        if final == 0 or (with_final == "으로" and final == 8): return without_final
        return with_final
    def persona_index(categories):
        if not categories: return 0
        head = categories[0]
        if head in CATEGORY_TO_PERSONA: return CATEGORY_TO_PERSONA[head]
        return int(hashlib.sha256(head.encode()).hexdigest()[:8], 16) % len(PERSONAS)   # D-16 결정적
    def assign_persona(categories, criterion_label):
        base = PERSONAS[persona_index(categories)]
        cats = list(categories)[:2]
        shown = f"{cats[0]}{_josa(cats[0], '과', '와')} {cats[1]}" if len(cats) == 2 else (cats[0] if cats else CATS_NONE)
        crit = criterion_label or CRITERION_NONE
        return replace(base, description=DESCRIPTION.format(cats=shown, eul=_josa(shown, "을", "를"), criterion=crit, ro=_josa(crit, "으로", "로")))
    ```
    **2-b. `onboarding_api.py`(B2)** — `SQL_CAND_INS = "INSERT INTO candidate_sets(candidate_set_id, user_key, ts, book_ids, survey_variant) VALUES(?,?,?,?,?)"`; `_j`·`_iso`·`_422` 헬퍼(중복 정의). `catalog_categories_meta(catalog, subcategories)`: `if catalog is None: return []`; `counts = Counter(c for m in catalog.meta(catalog.popular(n=10**6)) for c in (m.get("categories") or []))  # compose.catalog_categories 와 중복 허용(revision C11)`; `return [CategoryMeta(name=c, supported=n >= SUPPORTED_MIN, subcategories=subcategories.get(c, [])) for c, n in counts.most_common()]`. `build_router(*, db, catalog=None, meta_path=META_PATH, now=None)`: `meta = load_meta(meta_path)`; `clock = now or (lambda: datetime.now(UTC))`; **`cache: dict[str, list[CategoryMeta] | None] = {"cats": None}`(C11 1회 캐시)**; `router = APIRouter(prefix="/api")`.
    - `@router.get("/meta/onboarding", response_model=OnboardingMeta) def meta_onboarding()`: `if cache["cats"] is None: cache["cats"] = catalog_categories_meta(catalog, meta["subcategories"])`; `OnboardingMeta(survey_variant=meta["survey_variant"], reading_times=meta["reading_times"], criteria=[Criterion(**c) for c in meta["criteria"]], categories=cache["cats"])`.
    - `@router.get("/candidates/onboarding", response_model=CandidateSet) def candidates(categories: Annotated[str, Query()], n: Annotated[int, Query(ge=1, le=N_MAX)] = N_DEFAULT, user_key: Annotated[str | None, Query()] = None, subcategories: Annotated[str | None, Query()] = None)`: `cats = [c.strip() for c in categories.split(",") if c.strip()]`; `if len(cats) > CATEGORIES_MAX: raise _422("categories", f"at most {CATEGORIES_MAX} categories")`; **`if catalog is None: ids = []`(C4) `else:`** `lists = [catalog.popular([c], n=n) for c in cats]`; `seen: set[int] = set(); ids = []`; `for row in itertools.zip_longest(*lists): for b in row: if b is not None and b not in seen and len(ids) < n: seen.add(b); ids.append(b)`; `ids = catalog.eligible(ids)`; items = `catalog.meta(ids)` dict 조인 → `CandidateItem(book_id, title=m.get("title") or "", authors, image_url, position=i, book_format)`; `cid = new_id("cand_")`; `with db.connect() as con: con.execute(SQL_CAND_INS, (cid, user_key, _iso(clock()), _j(ids), SURVEY_VARIANT))`(확정 16 — `with con:` 커밋); return `CandidateSet(candidate_set_id=cid, survey_variant=SURVEY_VARIANT, created_at=_iso(clock()), items=items)`.
    **2-c. `demo_api.py`** — `from millie_rec.serving.onboarding_api import META_PATH, SURVEY_VARIANT, load_meta, new_id`; `from millie_rec.serving.persona import assign_persona`. SQL 상수(전부 `?`):
    ```python
    SQL_USER = "SELECT cell, consent FROM users WHERE user_key = ?"
    SQL_USER_INS = "INSERT INTO users(user_key, created_at, consent, cell, is_new) VALUES(?,?,?,?,1)"
    SQL_USER_CONSENT = "UPDATE users SET consent = ? WHERE user_key = ?"
    SQL_SNAP_INS = ("INSERT INTO preference_snapshots(snapshot_id, user_key, created_at, reading_time, categories, "
                    "criterion, subcategories, seeds, persona) VALUES(?,?,?,?,?,?,?,?,?)")
    SQL_SNAP_COUNT = "SELECT COUNT(*) FROM preference_snapshots WHERE user_key = ?"
    SQL_EVENT_INS = ("INSERT OR IGNORE INTO events(event_id, user_key, book_id, event_type, ts, surface, row_id, position, "
                     "selected, recommendation_id, model_version, preference_snapshot_id, candidate_set_id, payload, quality_flag) "
                     "VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)")   # Advisor 확정 16: event_id PK + OR IGNORE + rowcount 로 duplicates
    SQL_LAST_TS = "SELECT MAX(ts) FROM events WHERE user_key = ?"
    ```
    헬퍼: `_j`, `_iso`, `_parse(ts) -> datetime`(`fromisoformat(ts.replace("Z", "+00:00"))`, tz 없으면 UTC), `_422(param, msg)`(loc `["body", param]`), `_check_key(user_key)`: `if not USER_KEY_RE.match(user_key): raise _422("user_key", "user_key must match ^[A-Za-z0-9_-]{1,64}$")`(C5).
    `build_router(*, db, catalog=None, wake=None, invalidate=None, meta_path=META_PATH, now=None)`: `labels = {c["id"]: c["label"] for c in load_meta(meta_path)["criteria"]}`; `clock = now or (lambda: datetime.now(UTC))`; `router = APIRouter(prefix="/api")`.
    - `@router.post("/preferences", response_model=PreferencesResponse, status_code=201) def preferences(body: PreferencesRequest)`(백엔드 01 §4 `201`, D5): `_check_key(body.user_key)`; `now_s = _iso(clock())`; `con = db.connect()`; `row = con.execute(SQL_USER, (body.user_key,)).fetchone()`; `cell = row[0] if row else assign_cell(body.user_key)`; `with con: (row is None and con.execute(SQL_USER_INS, (body.user_key, now_s, int(body.consent), cell))); con.execute(SQL_USER_CONSENT, (int(body.consent), body.user_key))`; `persona = assign_persona(body.categories, labels.get(body.criterion) if body.criterion else None)`; `sid = new_id("snap_")`; `seeds = list(body.seeds) if body.consent else []`; `with con: con.execute(SQL_SNAP_INS, (sid, body.user_key, now_s, body.reading_time, _j(body.categories), body.criterion, _j(body.subcategories), _j(seeds), _j(asdict(persona))))`; `if seeds: with con: for b in seeds: con.execute(SQL_EVENT_INS, (uuid.uuid4().hex, body.user_key, b, "library_add", now_s, SURFACE_ONBOARDING, None, None, None, None, None, sid, body.candidate_set_id, _j({}), None))`; `if seeds and wake: wake()`; `if invalidate: invalidate(body.user_key)`; `count = con.execute(SQL_SNAP_COUNT, (body.user_key,)).fetchone()[0]`; `return PreferencesResponse(user_key=body.user_key, preference_snapshot_id=sid, created_at=now_s, cell=cell, snapshots_count=count, persona=PersonaOut(**asdict(persona)))`.
    - `@router.post("/events", response_model=EventsAccepted, status_code=202) def events(body: EventIn | EventsBatchIn)`: `evs = body.events if isinstance(body, EventsBatchIn) else [body]`; **먼저 전부 검증(C3·C5 — INSERT 전):** `for e in evs: _check_key(e.user_key)`; `try: parsed = [_parse(e.ts) for e in evs] except ValueError: raise _422("ts", "ts must be ISO-8601")`; `now_dt = clock()`; `accepted = dup = 0; flagged = []`; `con = db.connect()`; `with con: for e, t in zip(evs, parsed): flag = _quality_flag(con, e, now_dt, catalog, parsed_ts=t); rc = con.execute(SQL_EVENT_INS, (e.event_id, e.user_key, e.book_id, e.event_type, _iso(t.astimezone(UTC)), e.surface, e.row_id, e.position, None if e.selected is None else int(e.selected), e.recommendation_id, e.model_version, e.preference_snapshot_id, e.candidate_set_id, _j(e.payload), flag)).rowcount; if rc == 0: dup += 1 else: accepted += 1; (flag and flagged.append(FlaggedEvent(event_id=e.event_id, quality_flag=flag)))`; `if accepted and wake: wake()`(확정 13 — `wake` 는 05-06 이 `loop.call_soon_threadsafe(nearline.wake.set)` 클로저로 준다; 여기서는 호출만); `return EventsAccepted(accepted=accepted, duplicates=dup, flagged=flagged)`.
    - `_quality_flag(con, e, now_dt, catalog, *, parsed_ts=None) -> str | None`(C4 — catalog 인자, 클로저 의존 없음): `t = parsed_ts if parsed_ts is not None else _parse(e.ts)`; `if (t - now_dt).total_seconds() > FUTURE_SKEW_S: return FLAG_TS_FUTURE`; `last = con.execute(SQL_LAST_TS, (e.user_key,)).fetchone()[0]`; `if last and (_parse(last) - t).total_seconds() >= BACKDATED_S: return FLAG_TS_BACKDATED`; `if e.book_id is not None and catalog is not None and not catalog.eligible([e.book_id]): return FLAG_BOOK_INELIGIBLE`; `return None`.
    **2-d. GREEN 확인:** `uv run pytest tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py tests/test_architecture.py --no-header` → `29 passed`(26 + 3).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py tests/test_architecture.py --no-header && wc -l src/millie_rec/serving/persona.py src/millie_rec/serving/onboarding_api.py src/millie_rec/serving/demo_api.py</automated>
  </verify>
  <acceptance_criteria>
    - 요약 줄 `29 passed`, failed 0
    - `wc -l` persona.py ≤150 · onboarding_api.py ≤150 · demo_api.py ≤150(B2)
    - `grep -c "hashlib.sha256" src/millie_rec/serving/demo_api.py` ≥ 1 이고 `grep -c "hash(" src/millie_rec/serving/demo_api.py` == 0
    - `grep -n '"ts_future", "ts_backdated", "book_ineligible"' src/millie_rec/serving/demo_api.py` 1행 · `grep -c "INSERT OR IGNORE INTO events" …` == 1 · `grep -c "execute(f" src/millie_rec/serving/demo_api.py src/millie_rec/serving/onboarding_api.py` → 각 0
    - `grep -c "apply_event\|StateStore" src/millie_rec/serving/demo_api.py` == 0(SERV-09)
    - `grep -c "status_code=201" src/millie_rec/serving/demo_api.py` == 1 · `grep -c "status_code=202" …` == 1
    - `grep -c 'USER_KEY_RE = re.compile(r"^\[A-Za-z0-9_-\]{1,64}$")' src/millie_rec/serving/demo_api.py` == 1(C5 중복 정의) · `grep -c "def _quality_flag(con, e, now_dt, catalog" src/millie_rec/serving/demo_api.py` == 1(C4)
    - `grep -c "def _josa" src/millie_rec/serving/persona.py` == 1 · `grep -c '"회원님은 IT와 소설을 즐기고, 베스트셀러로 책을 고르는 독서가입니다."' tests/serving/test_persona.py tests/serving/test_demo_api.py demo/mock/preferences_response.json` → 각 ≥1(A1 문장 동일)
    - `grep -c "def meta_onboarding\|def candidates" src/millie_rec/serving/onboarding_api.py` == 2 · `grep -c "def preferences\|def events" src/millie_rec/serving/demo_api.py` == 2
  </acceptance_criteria>
  <done>persona 6 + onboarding_api 7 + demo_api 13 + 아키텍처 3 passed, 3파일 ≤150. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · 150줄 · 인계 시그니처 · SUMMARY</name>
  <files>src/millie_rec/serving/persona.py, src/millie_rec/serving/onboarding_api.py, src/millie_rec/serving/demo_api.py</files>
  <read_first>
    - Task 2 결과 3파일 · ../.claude/rules/python-tdd.md "Refactor" · ../.claude/rules/simplicity.md "코드 규칙"
  </read_first>
  <action>
    1. `uv run ruff format src/millie_rec/serving/persona.py src/millie_rec/serving/onboarding_api.py src/millie_rec/serving/demo_api.py tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py && uv run ruff check <같은 경로>` 클린(B008: 쿼리 기본값은 `Annotated[..., Query()]` 만). 다른 serving 파일은 포맷하지 않는다.
    2. 150줄 초과 시 **보고만**(추가 분리는 Advisor). 로직·시그니처 무변경.
    3. 재실행 → `29 passed`. SUMMARY 에 두 `build_router` 실제 시그니처와 라우트 4개 표(파일·메서드·경로·status), `assign_cell`·`new_id`·`assign_persona`·`_josa` 시그니처(05-06·05-07 인계) 복사. "`POST /api/ratings` 는 wave 4 Plan 05-11 의 `ratings_api.py`(B5) — 이 파일은 이후 무변경" 1줄. "`catalog_categories_meta` ↔ compose `catalog_categories` 중복 허용(C11, 05-09 PROGRESS)" 1줄.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/persona.py src/millie_rec/serving/onboarding_api.py src/millie_rec/serving/demo_api.py tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py && uv run ruff check src/millie_rec/serving/persona.py src/millie_rec/serving/onboarding_api.py src/millie_rec/serving/demo_api.py tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py && uv run pytest tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - ruff format --check · ruff check 클린 · `29 passed`
    - `git status --short` 에 이 플랜 `files_modified` 7개 외 이 Worker 변경 없음
  </acceptance_criteria>
  <done>ruff 클린 · 29 passed · SUMMARY 작성. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → `POST /api/preferences`·`POST /api/events`·`GET /api/candidates` | 비인증 공개 API. user_key·event_id 는 클라이언트 UUID(가명), ts·book_id·categories 는 임의 입력 |
| 라우터 → SQLite | INSERT 5종. 전부 `?` 바인딩, `with con:` 커밋(확정 16) |
| 라우터 → `wake()`·`invalidate()` | 주입된 콜백(05-06 — `wake` 는 `call_soon_threadsafe` 클로저, 확정 13). 라우터는 상태 객체를 모른다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | SQL 인젭션(user_key·ts·payload) | mitigate | SQL 상수 전부 `?` 바인딩, f-string 0건(acceptance). `USER_KEY_RE` 형식 검증이 본문 user_key 에도 적용(C5) — 테스트 `…blocked_by_format…` |
| T-05-04-02 | Tampering | 스키마 외 필드·초과 크기 | mitigate | pydantic `extra="forbid"`, `categories ≤3`·`subcategories ≤9`·`seeds ≤30`·배치 `1..50`·`n ≤ 60`·`categories` 쿼리 ≤3(`_422`) |
| T-05-04-03 | Tampering | 이벤트 시각 조작(미래·과거 백데이트·파싱 불가) | mitigate | 파싱 불가 → 422(INSERT 전, C3); 정규화 Z 저장; `ts_future`(+300s)·`ts_backdated`(≥3600s) 플래그 후 저장(분석용, D-08 문서 확정) |
| T-05-04-04 | Spoofing | 다른 user_key 로 이벤트 주입 | accept | 인증 없음은 설계 결정(아키 §3-8 가명 키·"안 함: 인증"). 가명 키 외 개인정보 없음 |
| T-05-04-05 | Denial of Service | `event_id` 재전송 폭주 | mitigate | `INSERT OR IGNORE` 멱등(중복은 `duplicates` 집계, wake 없음), 배치 ≤50 |
| T-05-04-06 | Information Disclosure | 예외 문자열·밀리 텍스트 | mitigate | 예외 경로는 pydantic/`_422` 만. `description` 은 meta 에 없다. 테스트 `_no_trace` |
| T-05-04-07 | Elevation of Privilege | 셀 배정 조작 | accept | `cell` 은 서버 sha256 만, 요청 본문에 없음 |
| T-05-04-08 | Repudiation | 상태 갱신 경로 우회 | mitigate | 라우터 소스에 `apply_event`·`StateStore` 0건(테스트 grep) — 상태 변경은 Nearline 만 |
| T-05-04-09 | Denial of Service | `catalog=None` 에서 AttributeError 500 | mitigate | C4: `_quality_flag` 가 `catalog is None` 분기, candidates 는 빈 items 200(테스트) |
</threat_model>

<verification>
- `uv run pytest tests/serving/test_persona.py tests/serving/test_onboarding_api.py tests/serving/test_demo_api.py tests/test_architecture.py --no-header` → `29 passed`
- ruff 클린 · `wc -l` 세 py 파일 ≤150
- `onboarding_meta.json` 의 텍스트가 `demo/config/onboarding.json` S1·S3 와 글자 동일 · persona description 이 `demo/mock/preferences_response.json` 문장과 동일(A1)
- (wave 1 종료 후 Advisor 1회) 전역 pytest · `make smoke`
- 커밋 0
</verification>

<success_criteria>
- **SERV-04** 스냅샷 append(2개)·`cell` sha256·`library_add` 자동·`consent=false` seeds 미저장·201 · **SERV-05** meta(supported 계산·캐시)·candidates(라운드로빈·`cand_` 발급·`candidate_sets` 저장·catalog None 안전) · **SERV-06** 품질 게이트 순서·플래그 3종·dedup(확정 16)·ts Z 정규화(C3)·파싱 실패 422·202·저장
- D-14·D-15·D-16(A1 조사 헬퍼) · Advisor 확정 12·13·16·17(B2) · revision A1·C3·C4·C5·D3·D5 반영
- 상태 쓰기 0(SERV-09), `schemas.py`·`contracts.py` 무변경, 의존성 0, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/05-must/05-04-SUMMARY.md`: RED/GREEN 요약 줄 + AssertionError 발췌 · 라우트 4개 표(파일·메서드·경로·status·response_model) · 상수 값(`N_DEFAULT=30 N_MAX=60 SUPPORTED_MIN=20 FUTURE_SKEW_S=300 BACKDATED_S=3600`) · `## TDD Gate Compliance` · **인계 시그니처 verbatim**(두 `build_router`·`assign_cell`·`new_id`·`assign_persona`·`load_meta`) · sha256 손계산에 쓴 B 셀 user_key 값 · 변경 파일 목록 · "커밋하지 않는다" 한 줄.
</output>
