---
phase: 05-must
plan: 06
type: tdd
wave: 2
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/cascade.py
  - src/millie_rec/serving/rec_log.py
  - tests/serving/test_cascade.py
autonomous: true
requirements: [SERV-01, SERV-02, SERV-03, SERV-06, SERV-07, SERV-08, SERV-09]
lane: Serving (api·cascade 통합 Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/__init__.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/demo_api.py
  - src/millie_rec/serving/privacy_api.py
  - src/millie_rec/serving/persona.py
  - src/millie_rec/serving/dashboard_api.py
  - src/millie_rec/serving/bench.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - artifacts/serving/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_smoke.py
  - tests/serving/test_recommend_level0.py
  - tests/serving/test_api_weights.py
  - tests/app/**
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "GET /api/recommend?user_key=… 는 users 행·최신(또는 지정) 스냅샷·consent=true 면 level 0 으로 Must 5행(continue_reading → anchor_<seed₁> → persona_shelf → trending → fresh_picks) 을 dedup 후 rows·items(평탄화 상위 k) 로 준다 — 셀 A→hybrid, B→hybrid_div, model= 있으면 그것·forced=true(D-01~D-04, SERV-01·02)"
    - "seeds 쿼리만 있는 cold-start 경로(CLI·smoke·bench 준비) 는 기존 1행 trending 응답 그대로이고 기존 테스트 단정을 한 줄도 고치지 않는다(Advisor 확정 7); 익명·consent=false·level 2·3 은 catalog 가 있을 때 trending+fresh_picks 2행(rows[0]=trending, SERV-08)"
    - "feature+pipeline 누적 > BUDGET_MS 또는 pipeline 예외 → level 1(캐시 있으면, model_version 은 캐시된 것) → 2(catalog.popular(스냅샷 categories)) → 3(전역) — 항상 200, 예외 문자열 없음(D-10, SERV-03); 캐시는 level 0 성공마다 put, fallback 경로에서만 get"
    - "모든 응답(level 0~3, seeds·익명 포함) 이 응답 직전 recommendations 에 1행 INSERT 되고 rows JSON 은 row_id·book_id·position 만 축약, weights·latency_breakdown 은 JSON 그대로 — 노출 로그 4필드(recommendation_id·model_version·row_id·position) 의 단일 소스(D-12, SERV-06)"
    - "POST /api/events 직후 다음 /api/recommend 의 history 는 변하지 않고 NearlineLoop.run_once() 후에 변한다(SERV-09 경계); lifespan 은 apply_schema → replay → Nearline 태스크 → shutdown 취소·db.close(); DELETE …/personalization 이후 recommend 는 항상 level 3(SERV-07)"
  artifacts:
    - path: "src/millie_rec/serving/cascade.py"
      provides: "Cascade(pipelines, fallback, *, catalog, db, neighbors, store, cache, weights, default, all_categories, criteria_labels, now) · respond(...) → (RecommendResponse, forced) · Resolved · CELL_VARIANT · PIPE_K_MIN · resolve_user · variant_for — level 판정·캐시·오케스트레이션만(B3)"
      exports: ["Cascade", "Resolved", "CELL_VARIANT", "PIPE_K_MIN", "resolve_user", "variant_for"]
      max_lines: 150
    - path: "src/millie_rec/serving/rec_log.py"
      provides: "추천 로그(D-12) · rows 축약 · breakdown 병합(C2) — Advisor 확정 17 B3 신설(아키 §9-3 목록 외, 05-09 PROGRESS 1줄): SQL_REC_INS · abbreviate_rows · merge_breakdown · log_recommendation"
      exports: ["SQL_REC_INS", "abbreviate_rows", "merge_breakdown", "log_recommendation"]
      max_lines: 150
    - path: "src/millie_rec/serving/api.py"
      provides: "create_app(동일 시그니처) — Cascade 조립 · include_router 3개 · lifespan(replay·Nearline·close) · /health 채움 · /api/recommend 껍데기"
      exports: ["create_app", "K_DEFAULT", "K_MAX"]
      max_lines: 150
    - path: "tests/serving/test_cascade.py"
      provides: "user_key 5행 · 셀 variant · forced · seeds 1행 호환 · 익명 2행 · consent=false 3 · 예산→1(캐시 有)·→2(캐시 無)·예외→200 · 추천 로그 4필드 · Nearline 경계 · DELETE→3 · snapshot_id 404 · /health 채움 · lifespan close"
      min_lines: 200
  key_links:
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/cascade.py"
      via: "cascade = Cascade(...); resp, forced = cascade.respond(...)"
      pattern: "cascade\\.respond\\("
    - from: "src/millie_rec/serving/cascade.py"
      to: "src/millie_rec/serving/compose.py"
      via: "compose_rows(items=…, catalog=…, neighbors=…, level=…, seeds=…, categories=…, criterion=…, persona_name=…, continue_ids=…, all_categories=…) · build_response(rows=…, dedup_removed=…, latency_breakdown=…, user_state_weights=…)"
      pattern: "compose_rows\\("
    - from: "src/millie_rec/serving/cascade.py"
      to: "src/millie_rec/serving/fallback.py"
      via: "over_budget(elapsed) · cache.get/put · segment_popular(catalog, user, categories, k)"
      pattern: "over_budget\\("
    - from: "src/millie_rec/serving/api.py"
      to: "demo_api.build_router / privacy_api.build_router"
      via: "include_router(onboarding_router(db=db, catalog=catalog)) · include_router(demo_router(db=db, catalog=catalog, wake=wake_fn, invalidate=cache.invalidate)) · include_router(privacy_router(db=db, catalog=catalog, state=state, weights=weights, invalidate=cache.invalidate)) — 서브모듈 직접 import(D1)"
      pattern: "include_router\\("
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/nearline.py"
      via: "lifespan: nearline.replay() → asyncio.create_task(nearline.run()) → cancel → db.close()"
      pattern: "create_task\\("
---

<objective>
Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md "Phase 5: 서빙 Must 완성") 의 **통합**: wave 1 이 만든 재료(`state`·`nearline`·`book_stats`·`compose`·`fallback`·`db`·`demo_api`·`privacy_api`) 를 `serving/api.py` 가 조립한다. `api.py` 는 150줄 한도라 **user_key 해석·cascade 오케스트레이션·breakdown 병합·추천 로그를 신규 `serving/cascade.py`(≤150줄)** 에 두고(Advisor 확정 3), `api.py` 는 라우트 선언·`create_app`·lifespan·`include_router` 만 남긴다. **추천 로그 INSERT(D-12)·rows 축약·breakdown 병합은 신규 `serving/rec_log.py`(Advisor 확정 17 B3)** 에 두고 `cascade.py` 는 level 판정·캐시·오케스트레이션만 맡는다.

Purpose: ROADMAP Success Criteria 1(5행·앵커, **SERV-01·02**) · 2 후반(건너뛰기·익명 비개인화, DELETE 후 level 3, **SERV-07·08**) · 3(cascade 1→2→3 항상 200, 노출 로그 4필드, Nearline 경계, **SERV-03·06·09**) 의 **끝단(HTTP) 증거**. PDF 축③ "Offline/Nearline/Online 경계" · 축④ "추천 API 장애 ≠ 메인 장애" · ★시간 가변 가중치가 `user_state_weights` 에서 실제로 변한다(D-05~D-08). 결정 'fallback·지연 측정'(개발일지 2026-09-06 파일 항목 D75).
Output: `api.py` 재구성(≤150), `cascade.py`·`rec_log.py` 신규(각 ≤150), `tests/serving/test_cascade.py` 신규. 커밋 0. `serving/__init__.py` 는 건드리지 않는다(05-01 이 이미 공개 표면을 넣었다).

**형제 플랜(wave 2 병렬):** 05-07 은 `dashboard_api.py`·`bench.py` 를, 05-08(Advisor) 은 `app/**`·`ranking/hybrid.py`·`reranking/guard.py` 를 같은 시각에 고친다. 이 플랜은 그 파일을 읽지도 고치지도 않는다. `dashboard_api.build_router` 는 05-07 이 만드는 중이므로 **이 플랜의 `create_app` 은 `try: from millie_rec.serving.dashboard_api import build_router as dashboard_router except ImportError: dashboard_router = None` 로 선택 포함**한다(wave 2 종료 후엔 항상 존재 — 05-09 게이트가 확인).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-PATTERNS.md
@.planning/phases/05-must/05-01-SUMMARY.md
@.planning/phases/05-must/05-02-SUMMARY.md
@.planning/phases/05-must/05-03-SUMMARY.md
@.planning/phases/05-must/05-04-SUMMARY.md
@.planning/phases/05-must/05-05-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/api.py
@src/millie_rec/serving/compose.py
@src/millie_rec/serving/fallback.py
@src/millie_rec/serving/db.py
@src/millie_rec/serving/state.py
@src/millie_rec/serving/nearline.py
@src/millie_rec/serving/demo_api.py
@src/millie_rec/serving/privacy_api.py
@src/millie_rec/serving/schemas.py
@tests/conftest.py
@tests/serving/test_smoke.py
@tests/serving/test_recommend_level0.py
@tests/serving/test_api_weights.py
@tests/app/test_server_catalog.py
@tests/app/test_variants.py
@tests/test_architecture.py
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@../.claude/rules/serving.md
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/local-run.md
@../.claude/rules/references.md

<wave_gate>
## Wave 1 게이트 — 이 플랜 Task 1 시작 전 Advisor 가 1회 실행

wave 1 다섯 플랜(05-01~05-05)은 자기 경로만 검증했다. 전역 게이트를 여기서 1회 돈다(모두 `/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec` 에서):

| 명령 | 기대 |
|---|---|
| `ls .planning/phases/05-must/05-0{1,2,3,4,5}-SUMMARY.md` | 5파일 존재 |
| `cd millie-rec && codegraph sync .` (선택 — 서버 연결 시) | 인덱스 갱신(`.claude/rules/codegraph.md`) |
| `uv run ruff format --check . && uv run ruff check .` | 클린 |
| `uv run pytest --no-header` | failed 0, passed = 기준선 327 + wave 1 다섯 SUMMARY 의 신규 테스트 수 합(하드코딩하지 않는다 — revision C10; 참고 예상 05-01 23 · 05-02 16 · 05-03 17 · 05-04 26 · 05-05 12) |
| `make smoke` | `smoke: PASS`(서버 코드는 compose/fallback/db 증분만 — 기존 3점 경로 불변) |
| `uv run python -c "from millie_rec.serving import StateStore, NearlineLoop, ServingBookStats; from millie_rec.serving.demo_api import build_router; from millie_rec.serving.onboarding_api import build_router as o; from millie_rec.serving.privacy_api import build_router as p; from millie_rec.serving.badges import attach_badges; print('ok')"` | `ok` |
| `wc -l src/millie_rec/serving/*.py \| sort -n \| tail -3` | `schemas.py` 외 전부 ≤150 |

게이트 실패 시: 실패 파일의 소유 플랜(`files_modified`)에 수정 브리프를 재위임하고 그 플랜의 acceptance 부터 다시 확인한다. `make smoke` 실패 시 `lsof -i :8010`·`rm -f .uvicorn.pid` 로 잔존 프로세스 정리 후 1회 재실행.
</wave_gate>

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 오직 `uv run pytest <대상> --no-header`. 맨 `pytest` 금지(Python 3.9). RED 는 `AssertionError`/`E  assert` 만. 스텁(`Cascade.respond` 가 기존 `_personalized_response`/`_fallback_response` 로직만 감싼 상태)에서 신규 단정이 깨져야 한다. `pytest.skip` 금지. **기존 테스트 단정 수정 금지 — 이 플랜의 핵심 제약**: `tests/serving/test_smoke.py`(13건)·`test_recommend_level0.py`(7건)·`test_api_weights.py`(4건)·`tests/app/test_server_catalog.py`·`test_variants.py` 가 **전부 그대로 통과**해야 한다(Advisor 확정 7 조건부 호환). 시간 주입(`now`), SQLite `tmp_path`, `BUDGET_MS` 는 `monkeypatch.setattr("millie_rec.serving.fallback.BUDGET_MS", 1)`(D-10).

**star 의존(`../.claude/rules/architecture.md`):** `api.py`·`cascade.py` 는 `millie_rec.contracts` 와 `millie_rec.serving.*` 만 import. `app`·`data`·`ranking` 금지. `weights` 는 주입된 callable(`ranking.state_weights` 를 이름으로 알지 않는다).

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄 — `api.py` 는 `_fallback_response`·`_personalized_response` 43줄을 cascade 로 옮겨 공간을 만든다. `cascade.py` 는 `Cascade` 클래스 1개(의존 보유) + 순수 함수 3개. 함수 ≤20줄. 예외는 `log.exception` 후 강하.

**서빙(`../.claude/rules/serving.md`):** `create_app(pipelines, fallback, *, catalog=None, db, neighbors=None, book_stats=None, state=None, weights=None)` **시그니처 불변**(D-09, `tests/serving/*` 4파일이 keyword 조립). "lifespan(스키마 적용·아티팩트 로드·24h 리플레이·Nearline 태스크)도 여기." "fallback: … `consent=false`는 항상 3, 가중치 0." "A/B 셀 … 이후 저장값." "핸들러는 전부 동기 def, Nearline 만 async."

**로컬 기동(`../.claude/rules/local-run.md`):** `make smoke` 의 3점(`/health`·`/`·`/api/recommend?seeds=1,2,3&k=5`) 은 `pipelines={}`·`catalog=None` 에서도 200 — `Cascade` 는 catalog None·store None·neighbors None 전부 허용해야 한다. **revision A5:** `make smoke`·`tests/serving/test_smoke.py`(L175 가 `millie_rec.app.server` 를 import)·`tests/app` 전체는 05-08 이 `app/` 을 같은 시각에 고치므로 **05-09 wave 2 게이트로 이관** — 이 플랜은 돌리지 않는다.

**완료 기준(자기 경로, revision A5):** `uv run ruff format --check src/millie_rec/serving/api.py src/millie_rec/serving/cascade.py src/millie_rec/serving/rec_log.py tests/serving/test_cascade.py && uv run ruff check <같은 경로>` 클린 · `uv run pytest tests/serving --ignore=tests/serving/test_smoke.py --ignore=tests/serving/test_dashboard_api.py --ignore=tests/serving/test_bench.py tests/test_architecture.py --no-header` failed 0(test_smoke 는 app.server import 로 05-08 과 결합, test_dashboard_api·test_bench 는 05-07 병렬 소유 — 셋 다 05-09 게이트). 기준선: test_recommend_level0 7 + test_api_weights 4 + test_schemas 2 = 13(C10 실측) + wave 1 신규 + 22.
</project_rules_inline>

<interfaces>
<!-- wave 1 SUMMARY 의 인계 시그니처를 그대로 복사한 것(Advisor 확정 6 "같은 문자열"). SUMMARY 와 다르면 SUMMARY(실제 코드)가 정본 — 그 경우 이 블록을 고치지 말고 SUMMARY 값을 쓴 뒤 보고. -->

From src/millie_rec/serving/state.py (05-01):
```python
SESSION_WINDOW_S = 1800; REPLAY_WINDOW_S = 86400
def reset_boost(snapshots_count: int, latest_created_at: str | None, *, now: float) -> bool
class StateStore:
    def __init__(self, *, now: Callable[[], float] | None = None) -> None
    def apply_event(self, user_key: str, event_type: str, book_id: int | None, ts: str) -> None
    def user_state(self, user_key: str, *, seeds: Sequence[int] = (), categories: Sequence[str] = (), context: str | None = None) -> UserState
    def session_active(self, user_key: str) -> bool
    def n_completed(self, user_key: str) -> int
    def continue_reading(self, user_key: str) -> tuple[int, ...]
    def forget(self, user_key: str) -> None
```
From src/millie_rec/serving/nearline.py (05-01):
```python
class NearlineLoop:
    def __init__(self, db: Database, store: StateStore, *, neighbors: Neighbors | None = None, interval_s: float = NEARLINE_INTERVAL_S, now: Callable[[], float] | None = None) -> None
    wake: asyncio.Event; last_run: str | None
    def run_once(self) -> int
    def replay(self) -> int
    async def run(self) -> None
    def waker(self, loop: asyncio.AbstractEventLoop) -> Callable[[], None]
```
From src/millie_rec/serving/compose.py (05-02):
```python
ROW_SIZE = 12; ZERO_WEIGHTS
def default_variant(pipelines) -> str | None; def new_rec_id() -> str
def catalog_categories(catalog: Catalog) -> list[str]
def compose_rows(*, items, catalog, neighbors, level, seeds=(), categories=(), criterion=None, persona_name=None, continue_ids=(), all_categories=(), k=ROW_SIZE) -> tuple[tuple[Row, ...], int]
def build_response(items, *, model_version, level, k, t0, context, rows=None, dedup_removed=0, latency_breakdown=None, user_state_weights=None) -> RecommendResponse
```
From src/millie_rec/serving/fallback.py (05-03):
```python
BUDGET_MS, CACHE_TTL_S  # 모듈 바인딩(monkeypatch 대상은 fallback.BUDGET_MS)
class Level1Cache:
    def __init__(self, *, ttl_s=CACHE_TTL_S, now=None); def put(self, user_key, snapshot_id, variant, rows, model_version) -> None
    def get(self, user_key, snapshot_id, variant) -> tuple[tuple[Row, ...], str] | None; def invalidate(self, user_key) -> int
def over_budget(elapsed_ms: float) -> bool
def segment_popular(catalog, user, categories, k) -> list[ScoredItem]
class GlobalPopularFallback(catalog).recommend(user, k)   # 기존
```
From src/millie_rec/serving/db.py (05-03): `Database.connect/execute(sql, params)->int/query(sql, params)->list[Row]/close()/apply_schema()`.
From src/millie_rec/serving/onboarding_api.py (05-04, B2): `def build_router(*, db, catalog=None, meta_path=META_PATH, now=None) -> APIRouter` · `load_meta(path=META_PATH) -> dict`(criteria 라벨용 — api.py 가 여기서 import).
From src/millie_rec/serving/demo_api.py (05-04): `def build_router(*, db, catalog=None, wake: Callable[[], None] | None = None, invalidate: Callable[[str], object] | None = None, meta_path=META_PATH, now=None) -> APIRouter`(D3).
From src/millie_rec/serving/privacy_api.py (05-05, C6): `def build_router(*, db, catalog=None, state: object | None = None, weights=None, invalidate: Callable[[str], object] | None = None, now=None) -> APIRouter`.
From src/millie_rec/serving/schemas.py: `RecommendOut.from_contract(resp, *, forced=False)` · `HealthOut(status, api_version, model_version, artifacts_loaded_at, db_ok, db_row_count, nearline_last_run, uptime_s)`.
From src/millie_rec/contracts.py: `VARIANTS=("pop","cf","hybrid","hybrid_div")` `MODEL_VERSION_SUFFIX="_v1"` `MODEL_VERSION_FALLBACK="fallback_v1"` `FALLBACK_*=0..3` `BUDGET_MS=200` `UserState` `Pipeline.name/recommend(user,k)`.

**이 플랜이 만드는 공개 시그니처(05-08 Advisor·05-10 이 소비):**
```python
# serving/cascade.py
CELL_VARIANT = {"A": "hybrid", "B": "hybrid_div"}     # 백엔드 01 §5 셀 배정
PIPE_K_MIN = 2 * ROW_SIZE                             # persona_shelf 12 + fresh_picks leftover 재료
# serving/rec_log.py (B3 신설)
SQL_REC_INS = ("INSERT INTO recommendations(recommendation_id, user_key, snapshot_id, model_version, cell, forced, "
               "fallback_level, latency_total_ms, latency_breakdown, weights, rows, ts) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)")
def abbreviate_rows(rows: Sequence[Row]) -> list[dict]                  # [{"row_id", "book_id", "position"}] 만(D-12)
def merge_breakdown(bd: dict[str, float], sub: object) -> dict[str, float]   # C2: sub 가 dict 이면 retrieval·ranking·rerank 를 **추가**(pipeline 키 유지)
def log_recommendation(db: Database, resp: RecommendResponse, *, user_key: str | None, snapshot_id: str | None,
                       cell: str | None, forced: bool, now: Callable[[], float] | None = None) -> None   # 예외는 log.exception 후 무시

# serving/cascade.py
@dataclass(frozen=True)
class Resolved:
    user_key: str; found: bool; consent: bool; cell: str | None; snapshot_id: str | None
    seeds: tuple[int, ...]; categories: tuple[str, ...]; criterion: str | None; persona_name: str | None
    snapshots_count: int; latest_created_at: str | None
def resolve_user(db: Database, user_key: str, snapshot_id: str | None) -> Resolved   # snapshot_id 가 그 user 것이 아니면 HTTPException(404)
def variant_for(cell: str | None, model: str | None, pipelines: dict[str, Pipeline], default: str | None) -> str | None
class Cascade:
    def __init__(self, pipelines, fallback, *, catalog=None, db, neighbors=None, store=None, cache: Level1Cache, weights=None,
                 default: str | None, all_categories: Sequence[str] = (), criteria_labels: dict[str, str] | None = None,
                 now: Callable[[], float] | None = None) -> None      # D2: self._now = now or time.time
    def respond(self, *, user_key: str | None, snapshot_id: str | None, model: str | None, k: int, context: str | None,
                seeds: tuple[int, ...]) -> tuple[RecommendResponse, bool]      # (응답, forced)
```
</interfaces>
</context>

<feature>
  <name>api.py 통합 + cascade.py — user_key 경로·셀 배정·5행·cascade 0→1→2→3·breakdown·추천 로그·lifespan(리플레이·Nearline)·/health</name>
  <files>src/millie_rec/serving/api.py, src/millie_rec/serving/cascade.py, tests/serving/test_cascade.py</files>
  <behavior>
    테스트 조립(`test_recommend_level0.py` 관례 + wave 1 fixture): `CatalogKR.load(millie_serving_sample)`(Advisor 확정 9 — `from millie_rec.data.catalog_kr import CatalogKR`, tests 는 무엇이든 import) 를 `catalog`·`neighbors` 로, 가짜 Pipeline `_FakeHybrid(name="hybrid")`·`_FakeDiv(name="hybrid_div")`(둘 다 seen 뺀 1..17 상위 k, source "content", 서로 다른 순서 — 예: hybrid 오름차순, hybrid_div 내림차순), `_Slow(name="hybrid_div")`(`time.sleep(0.01)` 후 `_FakeDiv` 결과), `_Boom`(예외). `store = StateStore(now=lambda: clock["t"])`, `weights = lambda u, **kw: {"alpha": 1.0 if u.explicit_seeds else 0.0, "beta": 1.0 if u.history else 0.0, "gamma": 0.1 if kw.get("session_active") else 0.0, **({"reset": 1.0} if kw.get("reset_boost") else {})}`(테스트용 — kwargs 수신 확인), `_app(tmp_path, pipelines, *, catalog, state, weights)` = `create_app(pipelines=…, fallback=GlobalPopularFallback(catalog), catalog=catalog, db=Database(tmp_path/DB_FILENAME), neighbors=catalog, book_stats=None, state=store, weights=weights)`; 온보딩은 실제 HTTP `POST /api/preferences`(05-04 라우터) 로 만든다: `{"user_key": UK, "categories": ["소설", "IT"], "criterion": "bestseller", "seeds": [1, 2, 3, 4, 5]}` → 201.
    - `# ── 계약 ──` **user_key 5행(SERV-01·02):** `GET /api/recommend?user_key=UK&k=40` 200 → `RecommendOut.model_validate`; `fallback_level == 0`; `[r.row_id for r in rows] == ["continue_reading", "anchor_1", "persona_shelf", "trending", "fresh_picks"]`; 앵커 items 전부 `source == "content"`·`source_channels == ["content"]`·`reason == "『밀리 표본 도서 1』을 좋아하셨다면"`, `channel_mix == {"content": n}`; `user_key == UK`, `cell in ("A", "B")`, `preference_snapshot_id` 가 201 응답의 `snapshot_id`; `items` 는 rows 평탄화(첫 item == 첫 비어있지 않은 행의 첫 item), `len(items) <= 40`; `dedup_removed >= 0`; 자격 없는 18·19·20 없음; `latency_breakdown` 키 ⊇ `{"feature", "pipeline", "compose", "total"}`
    - **셀 variant(D-01):** `cell == "A"` 이면 `model_version == "hybrid_v1"`, `"B"` 이면 `"hybrid_div_v1"`(테스트는 sha256 손계산으로 UK 의 셀을 미리 알고 단정), `forced is False`; `model=pop`(등록 안 됨) → 기본 variant 로 200·`forced is True`; `model=hybrid` → `hybrid_v1`·`forced is True`
    - **seeds 1행 호환(Advisor 확정 7):** `GET /api/recommend?seeds=1,2,3&k=5`(user_key 없음, catalog 있음) → `len(rows) == 1`, `rows[0].row_id == "trending"`, items 5개(level 0 평탄화) — `tests/app/test_server_catalog.py:39-41` 과 같은 형태; `pipelines={}`·`catalog=None` 조립에서 같은 요청 → 기존 `test_smoke.py:100-109` 형태(`rows` 1행·items `[]`·`total` 키·ZERO_WEIGHTS)
    - **익명 2행(SERV-08):** catalog 있음 + `GET /api/recommend` → `fallback_level == 3`, `[r.row_id for r in rows] == ["trending", "fresh_picks"]`, `rows[0].items[0].title.startswith("밀리 표본 도서")`(meta 조인 — Phase 3 인계 해소), `items == []`, `user_state_weights == ZERO`; catalog None → 1행(기존)
    - **consent=false → 3(SERV-08):** `POST /api/preferences {"user_key": "skip-1", "consent": false}` 201 → `GET ?user_key=skip-1` → `fallback_level == 3`, rows 2행(trending·fresh_picks), `user_state_weights == ZERO`, `user_key == "skip-1"`, `cell` 채움
    - **user_key 있지만 스냅샷 없음(users 행도 없음)** → level 3 2행, `user_key` 그대로 echo, `cell is None`
    - `# ── 정확성 ──` **예산 → 1(캐시 有)(D-10):** `_Slow` 를 `hybrid_div` 로 조립, UK 셀이 B 라고 가정(아니면 `model=hybrid_div`); 1회 정상 호출(캐시 put) → `monkeypatch.setattr("millie_rec.serving.fallback.BUDGET_MS", 1)` → 2회 → `fallback_level == 1`, `model_version == "hybrid_div_v1"`(캐시된 것), `rows` 가 1회 응답 rows 와 **같은 row_id·book_id 순서**, `recommendation_id` 는 **다름**(새 발급), `items` 평탄화 있음
    - **예산 → 2(캐시 無):** 새 user_key(캐시 없음) + `BUDGET_MS=1` → `fallback_level == 2`, rows 2행, `rows[0].items` 의 book_id 가 전부 스냅샷 categories(`소설`·`IT` — fixture 는 소설 1·5·9·13·17) 안 → `[1, 5, 9, 13, 17]` 중에서(seeds 1·5 제외 → `[9, 13, 17]`), `model_version == "fallback_v1"`
    - **예외 → 항상 200:** `_Boom` 조립 → `fallback_level in (1, 2, 3)`(캐시 없으니 2), `r.status_code == 200`, `"boom" not in r.text and "Traceback" not in r.text`; `pipelines={"hybrid_div": _Boom()}` + catalog None + user_key 경로 → level 3 1행 200
    - **정상 경로는 캐시를 읽지 않는다:** `_FakeDiv` 조립, 1회 후 `_Boom` 으로 파이프라인을 monkeypatch 교체하지 않고 대신 `cache.get` 을 spy — 예산 안이면 `get` 호출 0, 예산 초과 시 1(테스트는 `app.state.cascade.cache` 대신 `create_app` 이 노출하지 않으므로 `monkeypatch.setattr("millie_rec.serving.fallback.Level1Cache.get", spy)` 로 클래스 메서드를 감싼다)
    - **추천 로그(D-12, SERV-06):** 위 각 경로(level 0·1·2·3·seeds·익명) 호출 후 `SELECT recommendation_id, user_key, snapshot_id, model_version, cell, forced, fallback_level, latency_total_ms, latency_breakdown, weights, rows FROM recommendations` 행 수 == 호출 수; level 0 행: `recommendation_id == 응답 recommendation_id`, `fallback_level == 0`, `json.loads(rows)` 가 `[{"row_id": …, "book_id": …, "position": …}, …]` 형태(키 정확히 3개), 첫 항목 `row_id in ROW_IDS or startswith("anchor_")`, `json.loads(weights) == 응답 user_state_weights`, `json.loads(latency_breakdown)["total"] == 응답 latency_ms`; 익명 행: `user_key IS NULL`, `fallback_level == 3`
    - **Nearline 경계(SERV-09):** level 0 응답 A(`user_state_weights.beta == 0.0`) → `POST /api/events {"event_id": "e1", "user_key": UK, "book_id": 9, "event_type": "reader_open", "ts": now}` 202 → 즉시 `GET` → `beta == 0.0` 여전히(핸들러는 INSERT 만) → 테스트는 **`monkeypatch.setattr(NearlineLoop, "run", _idle)`**(`async def _idle(self): await asyncio.Event().wait()` — 살아 있는 루프가 먼저 처리해 버리는 경쟁을 끈다, revision C7) 로 앱을 만든 뒤 **`app.state.nearline.run_once()`** 를 직접 호출(create_app 이 `app.state.nearline`·`app.state.store`·`app.state.cache` 를 노출 — 05-08 test_server_state 는 이 노출을 단정하지 않는다, A4) → `GET` → `beta == 1.0`, `continue_reading` 행 items 에 9 포함, `9 not in persona_shelf`(seen 제외)
    - **session_active·reset_boost 인자 전달(D-06·D-08):** 위 이벤트 후 `GET` 의 `user_state_weights["gamma"] == 0.1`(테스트 weights 가 `session_active=True` 를 받았다는 증거); 두 번째 `POST /api/preferences`(restart) 후 `GET` 응답 `user_state_weights` 에 `"reset": 1.0` 키(테스트 weights 가 `reset_boost=True` 수신) — 실제 `state_weights` 는 kwargs 를 받으므로 배선 증거만 본다
    - **DELETE → 3(SERV-07):** level 0 확인 → `DELETE /api/users/UK/personalization` 200 → `GET ?user_key=UK` → `fallback_level == 3`, 2행, ZERO; 이후 `POST /api/preferences`(재동의) → level 0 복귀
    - **snapshot_id 소유(재량):** `GET ?user_key=UK&snapshot_id=snap_zzzzzz` → 404 `{"detail": "snapshot_id not found for user_key"}`; 다른 user 의 snapshot_id → 404; 자기 첫 스냅샷 id → 200 `preference_snapshot_id == 그 id`(두 스냅샷 있을 때 앵커 seed 가 첫 스냅샷 seeds[0])
    - **/health(Advisor 확정 8):** catalog 있음 + pipelines 2개 → `model_version == "hybrid_div_v1"`(기본 variant), `artifacts_loaded_at` ISO 문자열(`endswith("Z")`), `nearline_last_run` None 또는 ISO; `pipelines={}`·`catalog=None` → 둘 다 None(`test_smoke.py:87` 그대로)
    - **user_key 형식:** `GET ?user_key=<65자>` → 422(privacy_api 와 같은 규칙 재사용 — cascade 가 `USER_KEY_RE` import), `user_key=""` 는 익명 취급
    - **breakdown 키(C2):** `_FakeDiv` 에 `last_breakdown = {"retrieval": 1.0, "ranking": 2.0, "rerank": 0.5}` 속성을 주면 level 0 응답 `latency_breakdown` 키 ⊇ `{"feature", "pipeline", "retrieval", "ranking", "rerank", "compose", "total"}`(`pipeline` 키 **유지** + 세분 추가 — demo 가 키를 고정해도 안전); 속성이 없으면 `{feature, pipeline, compose, total}`
    - **추천 로그 파일 분리(B3):** `rec_log.log_recommendation` 를 spy 로 감싸면 호출 수 == 응답 수; `cascade.py` 소스에 `INSERT INTO recommendations` 없음(grep)
    - `# ── 안전성 ──` **lifespan:** `with TestClient(app) as c:` 진입 시 `events` 24h 리플레이(사전 INSERT 한 reader_open 이 첫 `GET` 에서 `beta == 1.0`), `app.state.nearline.last_run` 은 루프가 30s 간격이라 None 일 수 있음(단정 안 함); 사전 INSERT 는 **별도 `sqlite3.connect(db.path)` 연결**로(테스트 스레드의 thread-local 연결과 분리, C7); 종료 후 **`Database.close` 가 1회 호출됨을 spy 로 단정**(`monkeypatch.setattr(Database, "close", spy)` — `db._local.con is None` 단정은 삭제, C7)
    - **k 상한·items 길이:** `k=100` → `len(items) <= 100`; 행 길이는 각 ≤12; **cold-start 는 `pipe.recommend(user, k)` 그대로(A6):** seeds 경로 `k=5` 에서 가짜 Pipeline 이 받은 `k == 5`(spy) — `PIPE_K_MIN` 은 `_personal` 만
    - **weights 예외:** `weights=lambda u, **kw: 1/0` → level 0 요청이 200 이고 `fallback_level in (1, 2, 3)`(예외 경로 강하), `user_state_weights == ZERO`
  </behavior>
  <implementation>Task 2 action 그대로.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): cascade.py 스텁(기존 로직 이식) + api.py 라우터 포함·lifespan 골격 + test_cascade.py → 신규 단정 AssertionError, 기존 5파일 통과</name>
  <files>src/millie_rec/serving/api.py, src/millie_rec/serving/cascade.py, src/millie_rec/serving/rec_log.py, tests/serving/test_cascade.py</files>
  <read_first>
    - .planning/phases/05-must/05-01~05-SUMMARY.md 의 "인계 시그니처" 절(위 `<interfaces>` 와 대조 — 다르면 SUMMARY 가 정본)
    - src/millie_rec/serving/api.py 전문(150줄 — L47-89 두 함수를 cascade 로 이식, L107-116 lifespan, L128-148 recommend)
    - src/millie_rec/serving/compose.py(05-02 결과 — `compose_rows`·`build_response` 실제 시그니처) · fallback.py(05-03 — `Level1Cache`·`over_budget`·`segment_popular`) · state.py·nearline.py(05-01) · demo_api.py·privacy_api.py(`build_router` 시그니처·`USER_KEY_RE`·`load_meta`)
    - src/millie_rec/serving/schema.sql L18-22(recommendations 컬럼 12개)
    - tests/serving/test_smoke.py 전문 · test_recommend_level0.py 전문 · test_api_weights.py 전문(회귀 대상 — 단정 값 숙지) · tests/app/test_server_catalog.py L31-55 · tests/app/test_variants.py L70-88
    - tests/conftest.py L92-130(`millie_serving_sample` — 자격 없는 18·19·20, 카테고리 순환 `소설 에세이 경제경영 인문`, 이웃 5개)
    - .planning/phases/05-must/05-CONTEXT.md D-01(셀 variant)·D-09(breakdown 키·`getattr(pipe, "last_breakdown", None)`)·D-10(예산 판정 시점·캐시 정책)·D-12(추천 로그 범위·rows 축약), Claude's Discretion "api.py 150줄"·"/health 채우기"·"seeds 와 user_key 함께 오면 user_key 우선"·"snapshot_id 404"
    - .planning/phases/05-must/05-PATTERNS.md §9(api.py 150줄 방법·`/health` 단정 조건), §2(lifespan 자리·웨이크 `call_soon_threadsafe`), "기존 테스트와의 충돌 지점" 표 + 권장 (i)
    - ../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md §1·§5
  </read_first>
  <action>
    **1-a. `cascade.py` 스텁(기존 로직 이식만 — 새 동작 없음):** docstring `"""user_key 해석 · cascade 0→1→2→3 · breakdown 병합 · 추천 로그(아키텍처 01 §3-11, 05-CONTEXT D-09~D-12). api.py 150줄 유지용 신설(Advisor 확정 3)."""`. 상수 `CELL_VARIANT`·`PIPE_K_MIN`·`NOT_FOUND_SNAPSHOT = "snapshot_id not found for user_key"`. `Resolved` dataclass. **`rec_log.py` 스텁(B3)**: docstring `"""추천 로그(D-12)·rows 축약·breakdown 병합 — 아키 §9-3 목록 외 신설(Advisor 확정 17 B3), cascade 150줄 유지."""`, `SQL_REC_INS` 상수, `abbreviate_rows` → `return []`, `merge_breakdown(bd, sub)` → `return dict(bd)`, `log_recommendation(...)` → `return None`(INSERT 없음). `Cascade.__init__` 에 `self._now = now or time.time`(D2). `resolve_user` → `Resolved(user_key, found=False, consent=False, cell=None, snapshot_id=None, seeds=(), categories=(), criterion=None, persona_name=None, snapshots_count=0, latest_created_at=None)`(항상 미발견). `variant_for(cell, model, pipelines, default)` → `model if model in pipelines else default`(셀 무시). `Cascade.__init__` 은 인자 저장 + `self._now`(D2). `Cascade.respond(...)`: `t0 = perf_counter(); user = UserState(user_id=None, explicit_seeds=seeds)`; `name = variant_for(None, model, self.pipelines, self.default)`; `if user.explicit_seeds and name: resp = _personalized(...)`(api.py L67-89 를 그대로 옮긴 것, `self.weights(user)`) `else: resp = _fallback(...)`(L47-64); `return resp, model is not None`. 추천 로그·5행·캐시는 아직 없음.
    **1-b. `api.py` 골격:** L47-89 두 함수 삭제 → `from millie_rec.serving.cascade import Cascade` · **서브모듈 직접 import(revision D1 — 패키지 부분 초기화 회피):** `from millie_rec.serving.demo_api import build_router as demo_router` · `from millie_rec.serving.onboarding_api import build_router as onboarding_router, load_meta` · `from millie_rec.serving.privacy_api import build_router as privacy_router` · `from millie_rec.serving.fallback import Level1Cache` · `from millie_rec.serving.nearline import NearlineLoop` · `from millie_rec.serving.compose import catalog_categories, default_variant`. `create_app` 안: `cache = Level1Cache()`; `all_cats = catalog_categories(catalog) if catalog is not None else []`(기동 1회 — T-05-02-03); `labels = {c["id"]: c["label"] for c in load_meta()["criteria"]}`; `nearline = NearlineLoop(db, state, neighbors=neighbors) if state is not None else None`; `wake_box: dict[str, Callable[[], None] | None] = {"fn": None}`; `cascade = Cascade(pipelines, fallback, catalog=catalog, db=db, neighbors=neighbors, store=state, cache=cache, weights=weights, default=default, all_categories=all_cats, criteria_labels=labels)`. lifespan: `db.apply_schema(); started["t"] = perf_counter(); task = None; if nearline is not None: nearline.replay(); loop = asyncio.get_running_loop(); wake_box["fn"] = nearline.waker(loop)`(= `lambda: loop.call_soon_threadsafe(nearline.wake.set)` — **Advisor 확정 13: 동기 핸들러가 `Event.set()` 을 직접 부르지 않는다**)`; task = asyncio.create_task(nearline.run())`; `try: yield finally: if task: task.cancel(); with contextlib.suppress(asyncio.CancelledError): await task; db.close()`. `app.state.cascade, app.state.nearline, app.state.store, app.state.cache = cascade, nearline, state, cache`(테스트·05-08 접근용). `wake_fn = lambda: wake_box["fn"]() if wake_box["fn"] else None`; `app.include_router(onboarding_router(db=db, catalog=catalog))`(B2); `app.include_router(demo_router(db=db, catalog=catalog, wake=wake_fn, invalidate=cache.invalidate))`; `app.include_router(privacy_router(db=db, catalog=catalog, state=state, weights=weights, invalidate=cache.invalidate))`(05-05 C6 시그니처 — `cache` 객체 대신 `invalidate` 콜백); `try: from millie_rec.serving.dashboard_api import build_router as dashboard_router; app.include_router(dashboard_router(db=db)) except ImportError: log.info("dashboard_api not present yet (wave 2 parallel)")`. `/health`: `model_version=f"{default}{MODEL_VERSION_SUFFIX}" if (default and catalog is not None) else None`, `artifacts_loaded_at=loaded_at if catalog is not None else None`(`loaded_at = datetime.now(UTC).isoformat(timespec="seconds").replace("+00:00", "Z")` create_app 시각), `nearline_last_run=nearline.last_run if nearline else None`. `/api/recommend`: 기존 시그니처 유지, 본문 `if model is not None and model not in VARIANTS: raise _422(...)`; `resp, forced = cascade.respond(user_key=user_key or None, snapshot_id=snapshot_id, model=model, k=k, context=context, seeds=_parse_seeds(seeds))`; `return RecommendOut.from_contract(resp, forced=forced)`. `wc -l` ≤150 확인.
    **1-c. `tests/serving/test_cascade.py`** (25건) — `<behavior>` 의 가짜·헬퍼 + `_onboard(c, user_key, **body)`·`_cell(user_key)`(sha256 손계산)·`_no_trace(r)`·`clock = {"t": 1_800_000_000.0}`.
    - `# ── 계약 ──` `test_user_key_level0_returns_five_rows_anchor_content_and_echo_fields` · `test_cell_assignment_picks_variant_and_model_query_forces` · `test_seeds_only_cold_start_keeps_single_trending_row_with_and_without_catalog` · `test_anonymous_with_catalog_gets_trending_and_fresh_picks_with_titles` · `test_consent_false_is_level3_two_rows_zero_weights` · `test_user_key_without_snapshot_is_level3_echo_key`
    - `# ── 정확성 ──` `test_budget_exceeded_serves_level1_from_cache_with_new_rec_id` · `test_budget_exceeded_without_cache_serves_level2_snapshot_categories` · `test_pipeline_exception_always_200_no_leak` · `test_normal_path_never_reads_cache_fallback_path_does` · `test_every_response_logged_in_recommendations_with_rows_abbreviated` · `test_events_do_not_change_state_until_nearline_run_once` · `test_session_active_and_reset_boost_kwargs_reach_weights` · `test_delete_personalization_then_level3_until_reconsent` · `test_snapshot_id_must_belong_to_user_else_404` · `test_health_filled_with_catalog_and_none_without`
    - `# ── 안전성 ──` `test_user_key_format_422_and_empty_is_anonymous` · `test_lifespan_replays_24h_and_closes_db` · `test_k_upper_bound_and_row_sizes` · `test_weights_exception_degrades_to_fallback_200` · `test_forced_flag_only_when_model_query` · `test_recommend_never_returns_ineligible_or_seen` · `test_cold_start_passes_k_unchanged_personal_uses_pipe_k_min`(A6) · `test_last_breakdown_merged_additively_keeps_pipeline_key`(C2) · `test_recommendation_log_lives_in_rec_log_module`(B3)
    **1-d. RED 확인:** `uv run pytest tests/serving/test_cascade.py --no-header 2>&1 | tail -50` → 25건 대다수 failed, 사유 `AssertionError`/`E  assert`(seeds 호환 1건·k 상한 1건·forced 1건 정도는 스텁에서도 통과 — 정상). **회귀:** `uv run pytest tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/serving/test_schemas.py tests/test_architecture.py --no-header` → failed 0(골격만 바뀌었으니 기존 동작 유지가 RED 단계의 조건; `test_smoke.py`·`tests/app` 은 A5 로 05-09 게이트). `wc -l src/millie_rec/serving/api.py` ≤150.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_cascade.py --no-header 2>&1 | tail -40; uv run pytest tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/serving/test_schemas.py tests/test_architecture.py --no-header; wc -l src/millie_rec/serving/api.py src/millie_rec/serving/cascade.py src/millie_rec/serving/rec_log.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/serving/test_cascade.py` → 25
    - RED 출력에 `AssertionError`/`E  assert` 있고 `ImportError`·`AttributeError`·`TypeError`·`sqlite3.OperationalError` 없음(SUMMARY 에 발췌 3건)
    - 회귀 4파일(test_recommend_level0 7 · test_api_weights 4 · test_schemas 2 · 아키텍처 3 = `16 passed`) failed 0
    - `wc -l src/millie_rec/serving/api.py` ≤150 · `grep -c "def _personalized_response\|def _fallback_response" src/millie_rec/serving/api.py` == 0(이식 완료)
    - `grep -c "include_router(" src/millie_rec/serving/api.py` ≥ 2 · `grep -c "create_task(" src/millie_rec/serving/api.py` == 1 · `grep -c "db.close()" src/millie_rec/serving/api.py` == 1
    - `grep -rn "millie_rec\.\(data\|app\|retrieval\|ranking\|reranking\|evaluation\)" src/millie_rec/serving/api.py src/millie_rec/serving/cascade.py` → 출력 없음
  </acceptance_criteria>
  <done>신규 25건 대다수 AssertionError RED, 기존 4파일 passed, api.py ≤150. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): Cascade.respond 완성(해석·셀·5행·0→1→2→3·breakdown) + rec_log.py(로그·축약·병합) → test_cascade 25 passed + serving 회귀 0(A5 범위)</name>
  <files>src/millie_rec/serving/cascade.py, src/millie_rec/serving/rec_log.py, src/millie_rec/serving/api.py</files>
  <read_first>
    - Task 1 스텁·테스트(단정 값)
    - .planning/phases/05-must/05-CONTEXT.md D-09·D-10·D-12 재독(판정 시점·캐시 읽기 정책·rows 축약 형태)
    - src/millie_rec/serving/compose.py `compose_rows` 인자 이름(05-02 실제 코드)
  </read_first>
  <action>
    **2-a. `resolve_user(db, user_key, snapshot_id)`:**
    ```python
    SQL_USER = "SELECT consent, cell FROM users WHERE user_key = ?"
    SQL_SNAP_LATEST = ("SELECT snapshot_id, created_at, categories, criterion, seeds, persona FROM preference_snapshots "
                       "WHERE user_key = ? ORDER BY created_at DESC, snapshot_id DESC LIMIT 1")
    SQL_SNAP_BY_ID = ("SELECT snapshot_id, created_at, categories, criterion, seeds, persona FROM preference_snapshots "
                      "WHERE user_key = ? AND snapshot_id = ?")
    SQL_SNAP_COUNT = "SELECT COUNT(*) FROM preference_snapshots WHERE user_key = ?"
    ```
    `u = db.query(SQL_USER, (user_key,))`; 없으면 `Resolved(found=False, …)`. 스냅샷: `snapshot_id` 있으면 `SQL_SNAP_BY_ID` → 없으면 `raise HTTPException(404, detail=NOT_FOUND_SNAPSHOT)`; 없으면 `SQL_SNAP_LATEST`(없을 수 있음 → snapshot 필드 None·빈 튜플). `persona_name = json.loads(persona)["name"] if persona else None`. `snapshots_count`·`latest_created_at`(항상 **최신** 스냅샷의 created_at — reset_boost 판정용, 지정 snapshot_id 와 무관). `found=True, consent=bool(consent), cell=cell`.
    **2-b. `variant_for(cell, model, pipelines, default)`:** `if model in pipelines: return model`; `want = CELL_VARIANT.get(cell or "")`; `return want if want in pipelines else default`.
    **2-c. `Cascade.respond`** (≤40줄, 헬퍼로 분할):
    ```python
    t0 = perf_counter(); bd: dict[str, float] = {}
    if user_key: _check_key(user_key)   # privacy_api.USER_KEY_RE 재사용 → 422(loc ["query","user_key"])
    if not user_key:                     # 익명·seeds cold-start
        resp = self._cold_start(seeds, model, k, t0, context, bd)
        return self._logged(resp, user_key=None, snapshot_id=None, cell=None, forced=model is not None)
    r = resolve_user(self.db, user_key, snapshot_id)
    if not (r.found and r.consent and r.snapshot_id):
        resp = self._nonpersonal(UserState(None, context={"user_key": user_key}), FALLBACK_GLOBAL_POP, (), k, t0, context, bd, criterion=None)
        return self._logged(replace(resp, user_key=user_key, cell=r.cell), …, forced=model is not None)
    resp = self._personal(r, model, k, t0, context, bd)
    return self._logged(replace(resp, user_key=user_key, cell=r.cell, preference_snapshot_id=r.snapshot_id), …)
    ```
    - `_cold_start(seeds, model, k, t0, context, bd)`: `user = UserState(None, explicit_seeds=seeds)`; `name = variant_for(None, model, pipelines, default)`; `if seeds and name:` 기존 level 0(**`pipe.recommend(user, k)` — k 그대로, `PIPE_K_MIN` 을 쓰지 않는다(revision A6: 기존 seeds 테스트가 `items == [4..8]` 5개를 단정)** + `weights(user)`; 예외 → level 3) → **`build_response(items, …, rows=None)`**(1행 호환); `else:` 익명: `items = fallback.recommend(user, k)`(예외 → `[]`); `if self.catalog is not None: rows, dd = compose_rows(items=items, catalog=self.catalog, neighbors=None, level=FALLBACK_GLOBAL_POP, all_categories=self.all_categories); return build_response(items, model_version=MODEL_VERSION_FALLBACK, level=3, k=k, t0=t0, context=context, rows=rows, dedup_removed=dd, latency_breakdown=bd)` `else: build_response(items, …, rows=None)`(catalog None 1행 — smoke).
    - `_nonpersonal(user, level, categories, k, t0, context, bd, *, criterion)`: `items = segment_popular(self.catalog, user, categories, k) if level == FALLBACK_SEGMENT_POP else _safe(self.fallback.recommend, user, k)`; catalog None → `build_response(items, MODEL_VERSION_FALLBACK, level, rows=None)`; 있으면 `compose_rows(items=items, catalog=…, neighbors=None, level=level, categories=categories, criterion=criterion, all_categories=…)` → `build_response(..., rows=rows, dedup_removed=dd, latency_breakdown=bd)`(weights 0 — `consent=false` 는 가중치 0, 백엔드 01 §5).
    - `_personal(r, model, k, t0, context, bd)`:
      ```python
      tf = perf_counter()
      store = self.store
      user = (store.user_state(r.user_key, seeds=r.seeds, categories=r.categories, context=context) if store
              else UserState(None, explicit_seeds=r.seeds, context={"user_key": r.user_key, "categories": ",".join(r.categories), "n_completed": "0"}))
      kw = {}
      if reset_boost(r.snapshots_count, r.latest_created_at, now=self._now()): kw["reset_boost"] = True
      if store and store.session_active(r.user_key): kw["session_active"] = True
      bd["feature"] = (perf_counter() - tf) * 1000
      name = variant_for(r.cell, model, self.pipelines, self.default)
      if name is None: return self._nonpersonal(user, FALLBACK_GLOBAL_POP, r.categories, k, t0, context, bd, criterion=r.criterion)
      pipe = self.pipelines[name]; version = f"{name}{MODEL_VERSION_SUFFIX}"
      tp = perf_counter()
      try:
          items = pipe.recommend(user, max(k, PIPE_K_MIN))   # A6: _personal 만 PIPE_K_MIN(persona_shelf 12 + fresh leftover)
          shown = self.weights(user, **kw) if self.weights else dict(ZERO_WEIGHTS)
      except Exception:
          log.exception("pipeline %s failed; cascading", name); items = None
      bd["pipeline"] = (perf_counter() - tp) * 1000
      if items is None or over_budget(bd["feature"] + bd["pipeline"]):     # C2: 예산 판정은 항상 cascade 자체 perf_counter(feature + pipeline)
          return self._degrade(r, user, k, t0, context, bd, name)
      bd = merge_breakdown(bd, getattr(pipe, "last_breakdown", None))     # C2·D-09: 판정 뒤 표시용 추가 병합만(pipeline 키 유지 + retrieval·ranking·rerank)
      tc = perf_counter()
      rows, dd = compose_rows(items=items, catalog=self.catalog, neighbors=self.neighbors, level=FALLBACK_PERSONALIZED, seeds=r.seeds,
                              categories=r.categories, criterion=r.criterion, persona_name=r.persona_name,
                              continue_ids=store.continue_reading(r.user_key) if store else (), all_categories=self.all_categories)
      self.cache.put(r.user_key, r.snapshot_id, name, rows, version)       # D-10 매 level 0 성공 시 저장
      bd["compose"] = (perf_counter() - tc) * 1000
      return build_response(items, model_version=version, level=FALLBACK_PERSONALIZED, k=k, t0=t0, context=context,
                            rows=rows, dedup_removed=dd, latency_breakdown=bd, user_state_weights=dict(shown))
      ```
      (`weights` 예외도 `try` 안 → 강하 — `test_api_weights.py:91-98` 은 seeds 경로라 `_cold_start` 가 기존처럼 level 3.) `self.catalog is None` 인 user_key 경로(테스트 `pipelines={"hybrid_div": _Boom()}`+catalog None) 는 compose 를 부르지 않고 `build_response(items, …, rows=None)`(1행) — `compose_rows` 는 catalog 필수.
    - `_degrade(r, user, k, t0, context, bd, name)`: `hit = self.cache.get(r.user_key, r.snapshot_id, name)`; `if hit: rows, version = hit; return build_response((), model_version=version, level=FALLBACK_CACHE, k=k, t0=t0, context=context, rows=rows, latency_breakdown=bd, user_state_weights=dict(ZERO_WEIGHTS))`(items 평탄화는 build_response 가 rows 에서 — level 1 은 평탄화 대상); `else: return self._nonpersonal(user, FALLBACK_SEGMENT_POP, r.categories, k, t0, context, bd, criterion=r.criterion)`; level 2 가 빈 items(카테고리 인기 없음) 면 level 3 으로 한 번 더(`if not rows[0].items`).
    - **`rec_log.py`(B3):** `abbreviate_rows(rows)` = `[{"row_id": row.row_id, "book_id": i.book_id, "position": i.position} for row in rows for i in row.items]`; `merge_breakdown(bd, sub)` = `out = dict(bd); if isinstance(sub, dict): out.update({k_: float(v) for k_, v in sub.items() if k_ in ("retrieval", "ranking", "rerank")}); return out`(**`pipeline` 키 유지 — C2**); `log_recommendation(db, resp, *, user_key, snapshot_id, cell, forced, now=None)`: `try: db.execute(SQL_REC_INS, (resp.recommendation_id, user_key, snapshot_id, resp.model_version, cell, int(forced), resp.fallback_level, resp.latency_ms, json.dumps(resp.latency_breakdown), json.dumps(resp.user_state_weights), json.dumps(abbreviate_rows(resp.rows), ensure_ascii=False), _iso(now))) except Exception: log.exception("recommendation log failed; response unaffected")`(로그 실패가 추천을 막지 않는다).
    - `cascade._logged(resp, *, user_key, snapshot_id, cell, forced) -> tuple[RecommendResponse, bool]`: `log_recommendation(self.db, resp, user_key=user_key, snapshot_id=snapshot_id, cell=cell, forced=forced, now=self._now)`; `return resp, forced`. `cascade.py` 에는 `INSERT INTO recommendations` 문자열이 없다(B3 acceptance).
    **2-d. `api.py`** — `/api/recommend` 는 이미 cascade 로 넘긴다. `create_app` 에 `cascade` 생성 인자 `now=None` 기본. 150줄 확인.
    **2-e. GREEN 확인(A5 범위):** `uv run pytest tests/serving --ignore=tests/serving/test_smoke.py --ignore=tests/serving/test_dashboard_api.py --ignore=tests/serving/test_bench.py tests/test_architecture.py --no-header` → failed 0(= 기준선 13 + wave 1 신규 + 25). `tests/serving/test_smoke.py`·`tests/app/*`·`make smoke` 는 **05-09 wave 2 게이트로 이관**(이 플랜 본문에서 돌리지 않는다 — 05-08 이 `app/` 을 같은 시각에 고친다). seeds 경로 1행 호환은 `test_recommend_level0.py`·`test_api_weights.py`(A5 범위 안) 가 지킨다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving --ignore=tests/serving/test_smoke.py --ignore=tests/serving/test_dashboard_api.py --ignore=tests/serving/test_bench.py tests/test_architecture.py --no-header && wc -l src/millie_rec/serving/api.py src/millie_rec/serving/cascade.py src/millie_rec/serving/rec_log.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/serving --ignore=tests/serving/test_smoke.py --ignore=tests/serving/test_dashboard_api.py --ignore=tests/serving/test_bench.py tests/test_architecture.py --no-header` 요약 줄 failed 0, `test_cascade.py` 25 passed 포함(`make smoke`·test_smoke·tests/app 은 05-09 게이트 — A5)
    - `wc -l` api.py ≤150 · cascade.py ≤150 · rec_log.py ≤150
    - `grep -c "over_budget(" src/millie_rec/serving/cascade.py` ≥ 1 · `grep -c "cache.put(" …` == 1 · `grep -c "cache.get(" …` == 1(fallback 경로 1곳만) · `grep -c 'getattr(pipe, "last_breakdown", None)' …` == 1 · `grep -c "PIPE_K_MIN" src/millie_rec/serving/cascade.py` == 2(정의 1 + `_personal` 사용 1 — A6)
    - `grep -c "INSERT INTO recommendations" src/millie_rec/serving/rec_log.py` == 1 · `grep -c "INSERT INTO recommendations" src/millie_rec/serving/cascade.py` == 0(B3) · `grep -c '"row_id": row.row_id, "book_id": i.book_id, "position": i.position' src/millie_rec/serving/rec_log.py` == 1
    - `grep -c "execute(f" src/millie_rec/serving/cascade.py src/millie_rec/serving/api.py src/millie_rec/serving/rec_log.py` → 각 0
    - `grep -c "call_soon_threadsafe\|waker(" src/millie_rec/serving/api.py` ≥ 1(확정 13) · `grep -c "from millie_rec.serving.onboarding_api import" src/millie_rec/serving/api.py` == 1(B2·D1)
    - `uv run pytest tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py --no-header` → `11 passed`(기존 단정 무수정 증거 — C10 실측 7 + 4)
  </acceptance_criteria>
  <done>A5 범위 serving + 아키텍처 passed, 세 파일 ≤150(smoke·test_smoke·tests/app 은 05-09). 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · 150줄 · 05-08·05-10 인계 · SUMMARY(PROGRESS 항목 후보 2줄)</name>
  <files>src/millie_rec/serving/api.py, src/millie_rec/serving/cascade.py, src/millie_rec/serving/rec_log.py</files>
  <read_first>
    - Task 2 결과 3파일 · ../.claude/rules/python-tdd.md "Refactor" · ../.claude/rules/simplicity.md
  </read_first>
  <action>
    1. `uv run ruff format src/millie_rec/serving/api.py src/millie_rec/serving/cascade.py src/millie_rec/serving/rec_log.py tests/serving/test_cascade.py && uv run ruff check <같은 경로>` 클린. `dashboard_api.py`·`bench.py`(05-07 소유) 는 포맷하지 않는다.
    2. `cascade.py` 가 그래도 150 초과면 **보고만**(추가 분리는 Advisor). 시그니처·동작 무변경.
    3. `api.py` docstring 갱신: `"""FastAPI create_app · lifespan(스키마·24h 리플레이·Nearline) · GET /health · GET /api/recommend 껍데기 · 라우터 3개 include(백엔드 서빙 01 §0·§1·§5). 로직은 cascade.py."""`.
    4. 재실행 GREEN 명령(A5 범위). SUMMARY 에: `Cascade.__init__`·`respond`·`rec_log` 3함수 실제 시그니처 · `app.state.{cascade,nearline,store,cache}` 노출 · "PROGRESS 1줄 후보 3건(05-09 Advisor 가 기록): ① `serving/cascade.py` 신설(아키 §9-3 목록 외, api.py 150줄 유지) ② `serving/rec_log.py` 신설(B3) ③ D-04 의 seeds cold-start 경로 예외(1행 trending 유지 — Advisor 확정 7)" · `dashboard_api` 선택 import 가 실제로 붙었는지(`GET /api/showcase` 200 여부 — 05-07 완료 후) · **`nearline_lag_s` 는 미채움(None, 선택 필드 — revision D4)** 1줄 · "`make smoke`·test_smoke·tests/app 은 05-09 게이트에서 실행" 1줄.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/api.py src/millie_rec/serving/cascade.py src/millie_rec/serving/rec_log.py tests/serving/test_cascade.py && uv run ruff check src/millie_rec/serving/api.py src/millie_rec/serving/cascade.py src/millie_rec/serving/rec_log.py tests/serving/test_cascade.py && uv run pytest tests/serving --ignore=tests/serving/test_smoke.py --ignore=tests/serving/test_dashboard_api.py --ignore=tests/serving/test_bench.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - ruff format --check · ruff check 클린 · A5 범위 serving failed 0
    - `git status --short` 에 이 플랜 `files_modified` 4개 외 이 Worker 변경 없음
  </acceptance_criteria>
  <done>ruff 클린 · 회귀 0 · SUMMARY(PROGRESS 후보 3줄·D4). 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → `GET /api/recommend` 쿼리(user_key·snapshot_id·model·k·context·seeds) | 비인증. `model`·`k` 는 검증, `user_key` 형식 검증, `snapshot_id` 소유 검사 |
| cascade → SQLite(`users`·`preference_snapshots` SELECT, `recommendations` INSERT) | `?` 바인딩 |
| cascade → 주입 파이프라인(`Pipeline.recommend`)·`weights` | 신뢰하되 예외·예산 초과를 강하로 흡수 |
| lifespan → Nearline 태스크·`db.close()` | 프로세스 내부 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Denial of Service | 느린·예외 파이프라인이 응답을 막음 | mitigate | D-10: `recommend()` 반환 후 누적 > `BUDGET_MS` 또는 예외 → level 1 → 2 → 3, 항상 200(테스트 3건). 스레드 강제 중단은 채택 안 함(CONTEXT Deferred) — 느린 요청 1건은 그 요청만 느리다(uvicorn 스레드풀) |
| T-05-06-02 | Information Disclosure | 예외 문자열·Traceback 노출 | mitigate | 모든 `except Exception: log.exception(...)` 후 강하. 테스트 `"boom" not in r.text and "Traceback" not in r.text` |
| T-05-06-03 | Information Disclosure | 타인 스냅샷을 `snapshot_id` 로 열람 | mitigate | `SQL_SNAP_BY_ID` 가 `user_key AND snapshot_id` 둘 다 바인딩 → 불일치 404(테스트) |
| T-05-06-04 | Information Disclosure | 캐시 오염(타인 rows) | mitigate | 캐시 키 `(user_key, snapshot_id, variant)`, 익명·consent=false 경로는 캐시 미접근(코드 경로 분리, `cache.get` 1곳) |
| T-05-06-05 | Tampering | SQL 인젭션(user_key·snapshot_id 쿼리) | mitigate | `USER_KEY_RE` 422 + 전 SQL `?` 바인딩(acceptance `execute(f` 0) |
| T-05-06-06 | Denial of Service | `k=100`·PIPE_K_MIN 으로 파이프라인 비용 | accept | `K_MAX=100`(기존), `_personal` 만 `max(k, 24)`(A6 — cold-start 는 k 그대로) — 파이프라인 풀은 app 이 200 으로 고정(POOL), k 는 잘라내기만 |
| T-05-06-07 | Repudiation | 추천 로그 누락 | mitigate | D-12 모든 응답 INSERT(테스트 행 수 == 호출 수). INSERT 실패는 로그 후 응답 유지(추천이 로그 장애의 인질이 되지 않음 — 반대 방향은 허용된 트레이드오프, SUMMARY 에 명시) |
| T-05-06-08 | Denial of Service | lifespan 종료 시 Nearline 태스크 잔존 | mitigate | `task.cancel()` + `suppress(CancelledError)` await + `db.close()`(테스트 `…closes_db`) |
</threat_model>

<verification>
- `uv run pytest tests/serving --ignore=tests/serving/test_smoke.py --ignore=tests/serving/test_dashboard_api.py --ignore=tests/serving/test_bench.py tests/test_architecture.py --no-header` failed 0(기준선 13 + wave 1 신규 + 25 — A5 범위)
- `uv run pytest tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py --no-header` → `11 passed`(단정 무수정, C10 실측)
- `make smoke`·`tests/serving/test_smoke.py`·`tests/app` 은 05-09 wave 2 게이트(A5 이관) · ruff 클린 · `wc -l` api.py·cascade.py·rec_log.py ≤150
- `curl -s "localhost:8000/api/recommend?user_key=<온보딩한 키>" | jq '.rows[].row_id'` → 5행(05-09 Advisor 실측)
- 커밋 0
</verification>

<success_criteria>
- ROADMAP Success Criteria 1(**SERV-01·02**: user_key 5행·앵커 content·reason) · 2 후반(**SERV-07** DELETE→3 · **SERV-08** 익명·consent=false 2행) · 3(**SERV-03** 1→2→3 항상 200 · **SERV-06** 추천 로그 4필드 단일 소스 · **SERV-09** 이벤트 후 run_once 전 상태 불변) 이 HTTP 테스트로 단정
- D-01(셀 variant·forced) · D-09(breakdown 3구간 + `last_breakdown` 추가 병합, 판정은 cascade perf_counter — C2) · D-10(판정 시점·캐시 put/get 정책) · D-12(전 응답 로그·rows 축약) · Advisor 확정 3(cascade.py) · 7(seeds 1행 호환, 기존 단정 무수정) · 8(/health) 이 코드에 존재
- `create_app` 시그니처 불변, `contracts.py`·`schemas.py`·`__init__.py` 무변경, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/05-must/05-06-SUMMARY.md`: RED/GREEN 요약 줄 + AssertionError 발췌 · cascade 흐름도(텍스트 6줄: 익명/seeds → cold_start · user_key → resolve → personal → degrade → rec_log.log_recommendation) · `nearline_lag_s` 미채움(D4) 1줄 · `## TDD Gate Compliance` · **인계**(`Cascade` 시그니처·`app.state.*` 노출·`CELL_VARIANT`) · PROGRESS 후보 2줄 · `make smoke` 출력 3줄 · 변경 파일 목록 · "커밋하지 않는다" 한 줄.
</output>
