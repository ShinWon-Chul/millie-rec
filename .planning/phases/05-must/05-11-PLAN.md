---
phase: 05-must
plan: 11
type: tdd
wave: 4
depends_on: ["05-09"]
files_modified:
  - src/millie_rec/serving/ratings_api.py
  - src/millie_rec/serving/api.py
  - tests/serving/test_ratings.py
autonomous: true
requirements: [SERV-12]
lane: Serving (ratings Worker — Should, 시간 부족 시 wave 4 통째로 버린다)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/cascade.py
  - src/millie_rec/serving/rec_log.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/badges.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/demo_api.py
  - src/millie_rec/serving/onboarding_api.py
  - src/millie_rec/serving/privacy_api.py
  - src/millie_rec/serving/persona.py
  - src/millie_rec/serving/dashboard_api.py
  - src/millie_rec/serving/dashboard_agg.py
  - src/millie_rec/serving/bench.py
  - src/millie_rec/serving/__init__.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - artifacts/serving/**
  - results/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_demo_api.py
  - tests/serving/test_cascade.py
  - tests/serving/test_smoke.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "POST /api/ratings 는 RatingIn(rating_id·user_key·book_id·stars 1..5·ts·recommendation_id?) 을 ratings 에 INSERT OR IGNORE 하고 같은 트랜잭션에서 rating 이벤트(event_id = uuid4 hex, surface='reader', payload={'stars': str}, ts Z 정규화) 를 events 에 자동 기록한 뒤 201 RatingOut(ok=True, rating_id) 을 준다 — 모델 라벨로는 쓰지 않는다(D-13, SERV-12)"
    - "rating_id 중복 재전송은 201·ok=True 로 멱등이고 events 행이 늘지 않는다; stars 0·6 은 422; user_key 형식 위반 422; 저장 후 wake() 가 호출된다"
    - "라우트는 신규 serving/ratings_api.py(Advisor 확정 17 B5) 에 있고 api.py 는 include_router 1줄만 늘어난다 — demo_api.py 무변경; wave 4 안에서 api.py 를 고치는 플랜은 05-11 하나다(05-10 = state·nearline·compose·cascade, 05-12 = dashboard_agg·dashboard_api → 겹침 없음)"
  artifacts:
    - path: "src/millie_rec/serving/ratings_api.py"
      provides: "build_router(*, db, wake=None, invalidate=None, now=None) → APIRouter(POST /api/ratings 201) · SQL_RATING_INS · SQL_EVENT_INS(중복 정의) · SURFACE_READER · USER_KEY_RE(중복 정의)"
      exports: ["build_router", "SURFACE_READER"]
      max_lines: 150
    - path: "src/millie_rec/serving/api.py"
      provides: "from millie_rec.serving.ratings_api import build_router as ratings_router · app.include_router(ratings_router(db=db, wake=wake_fn, invalidate=cache.invalidate)) 1줄"
      contains: "ratings_router"
      max_lines: 150
    - path: "tests/serving/test_ratings.py"
      provides: "201·ratings 행·rating 이벤트 자동·payload stars·ts Z·멱등·422(stars·extra·user_key)·wake 호출·모델 라벨 미사용(소스 grep)·create_app 통합 1건"
      min_lines: 50
  key_links:
    - from: "src/millie_rec/serving/ratings_api.py"
      to: "ratings + events INSERT"
      via: "with con: rc = con.execute(SQL_RATING_INS, …).rowcount; if rc: con.execute(SQL_EVENT_INS, (uuid4 hex, user_key, book_id, 'rating', ts_z, 'reader', …, payload {'stars': …}, None))"
      pattern: "SQL_RATING_INS"
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/ratings_api.py"
      via: "include_router(ratings_router(db=db, wake=wake_fn, invalidate=cache.invalidate))"
      pattern: "ratings_router\\("
---

<objective>
Should 꼬리 2/3(D-13): **`POST /api/ratings`** — 뷰어 시뮬레이션 화면(`#/reader/:id`, ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2) 의 1탭 별점 모달이 부르는 엔드포인트. `ratings` INSERT + `rating` 이벤트 자동 기록. 모델 라벨로는 쓰지 않는다(설계만 — 결정 '1탭 별점 UI를 "설계만" → "데모 축소판"'(개발일지 2026-09-04 파일 항목 D22)). **Advisor 확정 17 B5: 신규 `serving/ratings_api.py`** 에 라우트를 두고 `demo_api.py` 는 건드리지 않는다(150줄). `api.py` 는 `include_router` 1줄.

Purpose: **SERV-12**, Phase 6 DEMO-07 전제. 축① "클릭이 아니라 QRS — 완독 직후 만족 라벨" 의 수집 경로 존재 증거(모델 사용은 로드맵).
Output: `ratings_api.py` 신규(≤150) + `api.py` 1줄 + 테스트 1 신규. 커밋 0. 아키 §9-3 목록 외 파일 → 05-09 PROGRESS 후보 목록에 이미 포함.

**wave 4 병렬 겹침 확인:** 05-10 = `state.py`·`nearline.py`·`compose.py`·`cascade.py` / 05-12 = `dashboard_agg.py`·`dashboard_api.py` / 이 플랜 = `ratings_api.py`·`api.py` → `api.py` 는 05-11 단독, 겹침 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-04-SUMMARY.md
@.planning/phases/05-must/05-06-SUMMARY.md
@.planning/phases/05-must/05-09-SUMMARY.md
@src/millie_rec/serving/api.py
@src/millie_rec/serving/demo_api.py
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/schema.sql
@tests/serving/test_demo_api.py
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@../.claude/rules/serving.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**실행 명령:** `uv run pytest <대상> --no-header` 만, RED = `AssertionError`. 기존 테스트 단정 무수정. **star 의존:** `contracts`·`serving.schemas`·`serving.db`(타입) 만 — `demo_api` 의 `SQL_EVENT_INS`·`USER_KEY_RE`·`_parse` 는 **중복 정의**(같은 슬라이스라 import 도 가능하지만 demo_api 무변경 원칙과 파일 독립성을 위해 중복, 주석 1줄). **간결함:** ≤150. **서빙(serving.md):** "`POST /api/ratings`(Should) … `ratings` insert + `rating` 이벤트 자동 기록. 모델 라벨로는 쓰지 않는다(설계만)." 백엔드 01 §7: 응답 `201 { "ok": true, "rating_id": "rat_1c9d" }`. `rating_id` 는 클라이언트 발급(`rat_<6hex>`) — 서버는 형식을 강제하지 않는다(`RatingIn.rating_id: str`). `ts` 는 demo_api 와 같이 파싱 → Z 정규화 저장(C3), 파싱 실패 422. `user_key` 형식 422(C5). 쓰기는 `with con:`(확정 16), wake 는 주입 콜백(확정 13).
**완료 기준(자기 경로만, A5 관례):** ruff 클린(ratings_api.py·api.py·test_ratings.py) · `uv run pytest tests/serving/test_ratings.py tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/test_architecture.py --no-header` failed 0(api.py 를 고치므로 기존 recommend 회귀 2파일 포함; `test_smoke`·`make smoke` 는 wave 4 종료 후 Advisor).
</project_rules_inline>

<interfaces>
From src/millie_rec/serving/schemas.py (verbatim): `class RatingIn(_Strict): rating_id: str; user_key: str; book_id: int; stars: Annotated[int, Field(ge=1, le=5)]; ts: str; recommendation_id: str | None = None` · `class RatingOut(_Strict): ok: bool = True; rating_id: str`.
From schema.sql: `ratings(rating_id TEXT PRIMARY KEY, user_key, book_id INTEGER, stars INTEGER, ts, recommendation_id)` · `events(15 컬럼 — 05-04 SQL_EVENT_INS 와 동일)`.
From 05-06 SUMMARY(api.py 실제): `create_app` 안 `wake_fn`·`cache` 클로저, `include_router` 3개(onboarding·demo·privacy) + dashboard 선택. 이 플랜은 그 옆에 1줄 추가.
**추가 시그니처:**
```python
# serving/ratings_api.py (B5 신설)
SURFACE_READER = "reader"
SQL_RATING_INS = "INSERT OR IGNORE INTO ratings(rating_id, user_key, book_id, stars, ts, recommendation_id) VALUES(?,?,?,?,?,?)"
SQL_EVENT_INS = (… demo_api 와 같은 15 컬럼 …)   # 중복 정의(주석)
USER_KEY_RE = re.compile(r"^[A-Za-z0-9_-]{1,64}$")   # 중복 정의(C5)
def build_router(*, db: Database, wake: Callable[[], None] | None = None,
                 invalidate: Callable[[str], object] | None = None,
                 now: Callable[[], datetime] | None = None) -> APIRouter
@router.post("/ratings", response_model=RatingOut, status_code=201) def ratings(body: RatingIn) -> RatingOut
```
`api.py` 추가: `from millie_rec.serving.ratings_api import build_router as ratings_router` · `app.include_router(ratings_router(db=db, wake=wake_fn, invalidate=cache.invalidate))`.
</interfaces>
</context>

<feature>
  <name>POST /api/ratings(ratings_api.py) — ratings INSERT + rating 이벤트 자동 기록 + api.py include(SERV-12, D-13, B5)</name>
  <files>src/millie_rec/serving/ratings_api.py, src/millie_rec/serving/api.py, tests/serving/test_ratings.py</files>
  <behavior>
    라우터 단위 조립은 `test_demo_api.py` 와 같은 방식(`FastAPI` + `build_router` + `_Spy` wake/invalidate); 통합 1건은 `create_app(pipelines={}, fallback=GlobalPopularFallback(None), db=Database(tmp_path/…))` 로 `/api/ratings` 가 붙었는지.
    - `POST /api/ratings {"rating_id": "rat_1c9d00", "user_key": "u-1", "book_id": 3, "stars": 5, "ts": "2026-09-07T12:00:00+00:00", "recommendation_id": "rec_8f3a2c"}` → **201** `{"ok": true, "rating_id": "rat_1c9d00"}`; DB `ratings` 1행(`stars == 5`, `recommendation_id == "rec_8f3a2c"`, `ts == "2026-09-07T12:00:00Z"`); `events` 1행 `event_type == "rating"`, `book_id == 3`, `surface == "reader"`, `recommendation_id == "rec_8f3a2c"`, `json.loads(payload) == {"stars": "5"}`, `ts == "2026-09-07T12:00:00Z"`, `event_id` 32자 hex; `wake_spy.calls == 1`; `inv_spy.calls == ["u-1"]`(별점 뒤 캐시 무효화 — 완독 시 무효화와 같은 취지, D-10)
    - 같은 `rating_id` 재전송 → 201 `ok true`, `ratings` 1행·`events` 1행 그대로(멱등), wake·invalidate 추가 호출 없음
    - `stars: 0`·`6`·`"a"` → 422; `{"extra": 1}` → 422; `user_key: "a';b"` → 422(C5); `ts: "bad"` → 422(C3); `recommendation_id` 생략 → 201·NULL
    - 소스 grep: `ratings_api.py` 에 `blend`·`state_weights`·`score` 없음(모델 라벨 미사용); `execute(f` 0건
    - 통합: `create_app(...)` + `TestClient` 로 `POST /api/ratings` 201(api.py include 확인); `/api/recommend?seeds=1,2,3&k=5` 기존 응답 불변(회귀)
    - 응답 텍스트에 `"Traceback"` 없음
  </behavior>
  <implementation>Task 2 action.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): ratings_api 스텁(201, DB 미기록) + api.py include 1줄 + test_ratings.py 8건 → AssertionError</name>
  <files>src/millie_rec/serving/ratings_api.py, src/millie_rec/serving/api.py, tests/serving/test_ratings.py</files>
  <read_first>src/millie_rec/serving/demo_api.py(`SQL_EVENT_INS`·`_parse`·`USER_KEY_RE`·`_422` 형태 — 복제 원본) · src/millie_rec/serving/api.py(05-06 결과 — `wake_fn`·`cache`·include 위치, 현재 줄 수) · schemas.py L223-235 · schema.sql L25-27 · tests/serving/test_demo_api.py 조립 헬퍼 · 백엔드 01 §7</read_first>
  <action>`ratings_api.py`: docstring `"""POST /api/ratings(Should, 백엔드 서빙 01 §7) — ratings INSERT + rating 이벤트. 모델 라벨 미사용(D-13). Advisor 확정 17 B5 로 demo_api 와 분리."""`, 상수 4개, `build_router` 가 `APIRouter(prefix="/api")` 에 `@router.post("/ratings", response_model=RatingOut, status_code=201) def ratings(body: RatingIn) -> RatingOut: return RatingOut(rating_id=body.rating_id)`(DB 쓰기·wake·검증 없음). `api.py`: import 1줄 + `app.include_router(ratings_router(db=db, wake=wake_fn, invalidate=cache.invalidate))` 1줄(다른 include 옆) — `wc -l` ≤150 확인(넘으면 보고). 테스트 8건: `test_rating_201_persists_and_records_rating_event_with_payload_and_z_ts` · `test_rating_idempotent_on_duplicate_rating_id` · `test_rating_calls_wake_and_invalidate_once_only_when_inserted` · `test_rating_stars_out_of_range_extra_field_user_key_bad_ts_422` · `test_rating_without_recommendation_id_is_null` · `test_rating_source_has_no_model_label_usage_and_no_fstring_sql` · `test_create_app_includes_ratings_route_and_recommend_unchanged` · `test_no_traceback`. RED 확인(422·grep 2건은 통과 정상) + `uv run pytest tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/test_architecture.py --no-header` 회귀 통과.</action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_ratings.py --no-header 2>&1 | tail -20; uv run pytest tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/test_architecture.py --no-header; wc -l src/millie_rec/serving/api.py</automated>
  </verify>
  <acceptance_criteria>`grep -c "def test_" tests/serving/test_ratings.py` → 8 · RED 는 `AssertionError` 만 · 회귀 3파일 failed 0 · `grep -c "ratings_router" src/millie_rec/serving/api.py` == 2(import + include) · `wc -l src/millie_rec/serving/api.py` ≤150 · `git diff --stat -- src/millie_rec/serving/demo_api.py` → 출력 없음</acceptance_criteria>
  <done>RED 확인, 회귀 0, demo_api 무변경. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): 검증 → INSERT OR IGNORE ratings → rating 이벤트 → wake·invalidate → 8 passed</name>
  <files>src/millie_rec/serving/ratings_api.py</files>
  <read_first>Task 1 스텁·테스트</read_first>
  <action>핸들러: `_check_key(body.user_key)`(422); `try: t = _parse(body.ts) except ValueError: raise _422("ts", "ts must be ISO-8601")`; `ts_z = _iso(t.astimezone(UTC))`; `con = db.connect(); with con: rc = con.execute(SQL_RATING_INS, (body.rating_id, body.user_key, body.book_id, body.stars, ts_z, body.recommendation_id)).rowcount; if rc: con.execute(SQL_EVENT_INS, (uuid.uuid4().hex, body.user_key, body.book_id, "rating", ts_z, SURFACE_READER, None, None, None, body.recommendation_id, None, None, None, _j({"stars": str(body.stars)}), None))`; `if rc: (wake and wake()); (invalidate and invalidate(body.user_key))`; `return RatingOut(rating_id=body.rating_id)`. GREEN: `uv run pytest tests/serving/test_ratings.py tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/test_architecture.py --no-header` failed 0.</action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_ratings.py tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/test_architecture.py --no-header && wc -l src/millie_rec/serving/ratings_api.py src/millie_rec/serving/api.py</automated>
  </verify>
  <acceptance_criteria>failed 0(8 + 7 + 4 + 3 = `22 passed`) · `wc -l` ratings_api.py ≤150 · api.py ≤150 · `grep -c "INSERT OR IGNORE INTO ratings" src/millie_rec/serving/ratings_api.py` == 1 · `grep -c '"rating"' …` ≥ 1 · `grep -c "execute(f" …` == 0 · `grep -c "def ratings" src/millie_rec/serving/demo_api.py` == 0</acceptance_criteria>
  <done>8 passed + 회귀 0. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · SUMMARY</name>
  <files>src/millie_rec/serving/ratings_api.py, src/millie_rec/serving/api.py</files>
  <read_first>Task 2 결과</read_first>
  <action>`uv run ruff format src/millie_rec/serving/ratings_api.py src/millie_rec/serving/api.py tests/serving/test_ratings.py && uv run ruff check <같은 경로>` 클린(다른 wave 4 파일 포맷 금지). SUMMARY: RED/GREEN 요약·`TDD Gate Compliance`·라우트 1행·"Phase 6 DEMO-07 이 부르는 요청 JSON 예시"·"api.py 는 include 1줄만(diff 발췌)"·변경 파일·"커밋하지 않는다".</action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/ratings_api.py src/millie_rec/serving/api.py tests/serving/test_ratings.py && uv run ruff check src/millie_rec/serving/ratings_api.py src/millie_rec/serving/api.py tests/serving/test_ratings.py && uv run pytest tests/serving/test_ratings.py tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>ruff 클린 · failed 0 · `git status --short` 이 플랜 3파일 외 변경 없음</acceptance_criteria>
  <done>ruff 클린 · SUMMARY. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| 클라이언트 → `POST /api/ratings` | 비인증. `rating_id` 클라이언트 발급(멱등 키), `stars` 1..5, `user_key` 형식 검증 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-11-01 | Tampering | SQL 인젭션 | mitigate | `?` 바인딩 2문 + `USER_KEY_RE` 422(acceptance `execute(f` 0) |
| T-05-11-02 | Tampering | 별점 조작으로 모델 오염 | mitigate | 모델 라벨로 쓰지 않는다(D-13 설계만) — 소스 grep 테스트 |
| T-05-11-03 | Denial of Service | 재전송 폭주 | mitigate | `INSERT OR IGNORE` 멱등, 중복은 이벤트·wake·invalidate 없음 |
| T-05-11-04 | Information Disclosure | 예외 문자열 | accept | pydantic/`_422` 외 예외 경로 없음 |
</threat_model>

<verification>`uv run pytest tests/serving/test_ratings.py tests/serving/test_recommend_level0.py tests/serving/test_api_weights.py tests/test_architecture.py --no-header` failed 0 · ruff 클린 · `wc -l` 두 파일 ≤150 · demo_api 무변경 · 커밋 0(smoke 는 wave 4 종료 후 Advisor)</verification>
<success_criteria>**SERV-12**: `ratings` 저장 + `rating` 이벤트 자동 기록 + 멱등 + 422 + api.py include 가 테스트로 단정, 모델 라벨 미사용, B5 파일 분리</success_criteria>
<output>`.planning/phases/05-must/05-11-SUMMARY.md` — RED/GREEN·라우트·DEMO-07 요청 예시·api.py diff 1줄·변경 파일·"커밋하지 않는다".</output>
