---
phase: 05-must
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/__init__.py
  - tests/serving/test_state.py
  - tests/serving/test_nearline.py
  - tests/serving/test_book_stats.py
autonomous: true
requirements: [SERV-09, SERV-04]
lane: Serving (state·nearline·book_stats Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/demo_api.py
  - src/millie_rec/serving/privacy_api.py
  - src/millie_rec/serving/persona.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - artifacts/serving/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_smoke.py
  - tests/serving/test_recommend_level0.py
  - tests/serving/test_api_weights.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "StateStore.apply_event 는 reader_open·qualified_read·completion 만 history 에 넣고(최근순·distinct·최대 200), library_add 와 seeds 는 넣지 않는다(D-05) — 신규 사용자는 history 비어 β=0"
    - "StateStore.user_state(user_key, now 기준) 의 session 은 최근 30분(SESSION_WINDOW_S=1800) 안의 reader_open·detail_click 책만이고, session_active(user_key) 는 창 안 이벤트가 1개 이상이면 True 다(D-06)"
    - "UserState.context 에 n_completed(completion distinct 수, str)·user_key·categories(쉼표 결합) 가 들어가고, ServingBookStats.user_level 은 완독 책 difficulty 평균(결측 제외), 완독 0권이면 context categories 의 카탈로그 평균 difficulty 를 준다(D-07, Advisor 확정 4·5)"
    - "reset_boost(snapshots_count, latest_created_at, now) 는 snapshots_count ≥ 2 이고 created_at 이 24h 이내일 때만 True 다(D-08)"
    - "NearlineLoop.run_once() 는 아직 처리하지 않은 events 행(rowid 커서)만 store 에 적용하고 처리 수를 돌려주며, replay() 는 기동 시 ts ≥ now−24h 행만 적용한다 — 요청 핸들러 어디에도 store 쓰기가 없다(SERV-09 코드 증거, D-08)"
  artifacts:
    - path: "src/millie_rec/serving/state.py"
      provides: "SESSION_WINDOW_S=1800 · REPLAY_WINDOW_S · RESET_BOOST_WINDOW_S · HISTORY_MAX=200 · HISTORY_EVENT_TYPES · SESSION_EVENT_TYPES · parse_ts · reset_boost · StateStore"
      exports: ["StateStore", "reset_boost", "parse_ts", "SESSION_WINDOW_S"]
      max_lines: 150
    - path: "src/millie_rec/serving/nearline.py"
      provides: "NearlineLoop(db, store, *, neighbors=None, interval_s=NEARLINE_INTERVAL_S, now=None) — wake · last_run · run_once · replay · async run"
      exports: ["NearlineLoop"]
      max_lines: 150
    - path: "src/millie_rec/serving/book_stats.py"
      provides: "ServingBookStats(catalog, store) — stats 위임 · user_level D-07 · PRIOR_POPULAR_N"
      exports: ["ServingBookStats"]
      max_lines: 150
    - path: "src/millie_rec/serving/__init__.py"
      provides: "__all__ 에 NearlineLoop · ServingBookStats · StateStore 추가(app/server.py 가 import)"
      contains: "\"StateStore\""
    - path: "tests/serving/test_state.py"
      provides: "history 이벤트 종류 · 30분 창 · n_completed · reset_boost · forget · HISTORY_MAX"
      min_lines: 60
    - path: "tests/serving/test_nearline.py"
      provides: "run_once 커서 · 24h 리플레이 · 핸들러 미갱신 경계 · 예외 후 계속 · selected 채움"
      min_lines: 50
    - path: "tests/serving/test_book_stats.py"
      provides: "stats 위임 1회 · 완독 평균 · 카테고리 prior · 캐시 · None"
      min_lines: 30
  key_links:
    - from: "src/millie_rec/serving/nearline.py"
      to: "src/millie_rec/serving/state.py"
      via: "run_once → store.apply_event(user_key, event_type, book_id, ts)"
      pattern: "apply_event\\("
    - from: "src/millie_rec/serving/book_stats.py"
      to: "src/millie_rec/serving/state.py"
      via: "user_level → store.completed_books(user.context['user_key'])"
      pattern: "completed_books\\("
    - from: "src/millie_rec/serving/nearline.py"
      to: "src/millie_rec/serving/db.py"
      via: "db.connect() + SELECT rowid … FROM events WHERE rowid > ? (파라미터 바인딩)"
      pattern: "WHERE rowid > \\?"
---

<objective>
Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md "Phase 5: 서빙 Must 완성") 의 **user_key 상태 모델**을 TDD 한 사이클로 만든다: `serving/state.py`(메모리 dict aggregate, D-05~D-08), `serving/nearline.py`(30s ∨ 웨이크 루프 · rowid 커서 · 24h 리플레이, SERV-09), `serving/book_stats.py`(D-07 `user_level` 어댑터 최소형, Advisor 확정 5). 배선(`create_app`·lifespan·`app/server.py`)은 Plan 05-06·05-08 이 한다 — 이 플랜은 **명시 인자를 받는 클래스 3개**만 만든다(Advisor 확정 2).

Purpose: PDF 축③ "Offline/Nearline/Online 경계가 코드로 존재" — 요청 핸들러는 events INSERT 만, 상태 갱신은 Nearline 만(SERV-09). ★시간 가변 가중치(main §5-2)가 서버 응답 `user_state_weights` 에서 실제로 변하는 재료(history·session·reset_boost·session_active). 결정 '서버 user_key 상태'(개발일지 2026-09-06 파일 항목 D74) 의 코드 증거.
Output: `src` 3 신규 + `__init__` 갱신 + `tests/serving/` 3 신규. 커밋 0.

**형제 플랜 불변(wave 1 병렬, Advisor 확정 2):** 이 플랜의 모듈은 `millie_rec.contracts`·`millie_rec.serving.db`(`Database` 타입만) 외 어떤 serving 모듈도 import 하지 않는다. `compose.py`·`fallback.py`·`db.py` 본문은 05-02·05-03 이 같은 시각에 고친다 — 읽지도 고치지도 않는다.

**`__init__.py` 소유 이유:** Plan 05-08(Advisor, wave 2)이 `from millie_rec.serving import StateStore, ServingBookStats` 로 주입하려면 공개 표면이 wave 1 에 준비돼야 한다(Advisor 확정 1 의 "05-06 이 `__init__`" 을 이 플랜으로 앞당김 — 05-06 은 `__init__` 을 건드리지 않는다).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/db.py
@src/millie_rec/serving/__init__.py
@src/millie_rec/serving/schema.sql
@src/millie_rec/data/catalog_kr.py
@src/millie_rec/ranking/blend.py
@tests/conftest.py
@tests/serving/test_smoke.py
@tests/reranking/test_guard.py
@tests/test_architecture.py
@../.claude/rules/serving.md
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/local-run.md
@../.claude/rules/references.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 테스트는 오직 `uv run pytest <대상> --no-header`(`-q` 는 pyproject `addopts` 에 이미 있음 — 붙이면 `-qq` 가 되어 요약 줄이 사라진다). 맨 `pytest`·`python -m pytest`·`python3` 금지(이 머신의 맨 pytest 는 Python 3.9 → collection error). **RED 는 출력에 `AssertionError`(또는 `E  assert`) 가 있을 때만 인정.** `ImportError`·`ModuleNotFoundError`·`AttributeError`·`TypeError`·`SyntaxError` 는 RED 가 아니다 → 시그니처만 있는 스텁을 먼저 둔다. `pytest.skip` 금지. `tests/test_architecture.py`·기존 테스트 단정 수정 금지. 네이밍 `test_<기능>_<시나리오>_<기대결과>`. 파일 머리 3종 구분 주석 `# ── 계약 ──` `# ── 정확성 ──` `# ── 안전성 ──`(test_smoke.py 관례). 시간은 주입(`now` 인자), SQLite 는 `tmp_path`, 네트워크 0.

**star 의존(`../.claude/rules/architecture.md`, `tests/test_architecture.py` L37-42):** `serving/*.py` 는 `millie_rec.contracts` 와 `millie_rec.serving.<module>` 만 import. `data`·`ranking`·`reranking`·`retrieval`·`evaluation`·`app` import 금지 — `CatalogKR` 은 이름조차 쓰지 않고 `contracts.Catalog`·`BookStatsSource`·`Neighbors` Protocol 로 받는다(app 이 실제 객체 주입). `tests/` 는 무엇이든 import 가능(`CatalogKR.load(millie_serving_sample)` 허용, Advisor 확정 9).

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄(`wc -l`). 1줄 한국어 docstring + 정본 절 참조. 상수는 모듈 상단 대문자 + `#` 근거(D-xx). 함수 우선, 클래스는 상태 보유·Protocol 만족에만. 예외 계층·재시도·캐시 데코레이터 금지. 로깅은 `logging.getLogger(__name__)`, 예외는 `log.exception` 후 계속(500·문자열 노출 없음). 타입 힌트는 공개 API 에만 필수. `datetime.UTC` 는 3.11 이상(요구 3.11 충족).

**서빙(`../.claude/rules/serving.md`):** "HTTP 핸들러는 전부 동기 `def`(스레드풀). Nearline·24h 집계만 `async` + `asyncio.to_thread`. `uvicorn --workers 1`." · "Nearline: 30s 타이머 ∨ `asyncio.Event`(`POST /api/events`가 set). 요청 핸들러는 상태를 갱신하지 않고 이벤트만 적재한다(Offline/Nearline/Online 경계의 코드 증거)." · "재기동 시 최근 24h events 리플레이로 세션·after_completion 복구." · SQLite 접근은 `serving/db.py` 의 `Database.connect()` 만, 사용자 입력은 전부 `?` 바인딩.

**로컬 기동(`../.claude/rules/local-run.md`):** 이 플랜은 서버 파일을 건드리지 않으므로 `make smoke` 는 wave 1 종료 후 Advisor 1회(05-06 `<wave_gate>`). Worker 완료 기준은 자기 경로만: `uv run ruff format --check src/millie_rec/serving tests/serving && uv run ruff check src/millie_rec/serving tests/serving` 클린 · `uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py tests/test_architecture.py --no-header` failed 0.

**참조 표기(`../.claude/rules/references.md`):** 코드 주석·SUMMARY 에서 결정은 `D-05(05-CONTEXT.md)` 처럼 문서 이름과 함께. 약호 단독 금지.
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. contracts.py 는 변경 금지. 아래 공개 시그니처는 05-06·05-08 이 같은 문자열로 소비한다(Advisor 확정 6). -->

From src/millie_rec/contracts.py (verbatim):
```python
EVENT_TYPES = ("impression", "detail_click", "library_add", "reader_open", "qualified_read", "completion",
               "rating", "preference_started", "preference_step", "preference_book_selected",
               "preference_book_deselected", "preference_completed", "preference_restarted")
NEARLINE_INTERVAL_S = 30

@dataclass(frozen=True, slots=True)
class UserState:
    user_id: int | None
    explicit_seeds: tuple[int, ...] = ()
    history: tuple[int, ...] = ()
    session: tuple[int, ...] = ()
    context: dict[str, str] = field(default_factory=dict)  # 값은 str — n_completed 는 str(n)

class Catalog(Protocol):
    def meta(self, book_ids: Sequence[int]) -> list[dict]: ...
    def popular(self, categories: Sequence[str] = (), n: int = 50) -> list[int]: ...
    def eligible(self, book_ids: Sequence[int]) -> list[int]: ...

class Neighbors(Protocol):
    def neighbors(self, book_id: int, n: int = 20) -> list[tuple[int, float]]: ...

class BookStatsSource(Protocol):
    def stats(self, book_ids: Sequence[int]) -> list[BookStats]: ...
    def user_level(self, user: UserState) -> float | None: ...
```

From src/millie_rec/serving/db.py (읽기만 — 05-03 이 같은 시각에 DML 헬퍼를 추가하지만 이 플랜은 `connect()` 만 쓴다):
```python
class Database:
    def __init__(self, path: Path) -> None: ...
    def connect(self) -> sqlite3.Connection: ...   # thread-local. 쓰기는 `with con:` 으로 커밋
    def apply_schema(self) -> None: ...
```

From src/millie_rec/serving/schema.sql (events 컬럼 — 리플레이·커서 SELECT 대상):
```sql
CREATE TABLE IF NOT EXISTS events (
    event_id TEXT PRIMARY KEY, user_key TEXT, book_id INTEGER, event_type TEXT, ts TEXT,
    surface TEXT, row_id TEXT, position INTEGER, selected INTEGER,
    recommendation_id TEXT, model_version TEXT, preference_snapshot_id TEXT, candidate_set_id TEXT,
    payload TEXT, quality_flag TEXT
);
```
(TEXT PK 테이블에도 암묵 `rowid` 가 있다 — 커서로 쓴다. `ts` 는 ISO-8601 UTC `"2026-09-07T12:00:27Z"` 또는 `+00:00` 형태.)

**이 플랜이 만드는 공개 시그니처(05-06·05-08 이 verbatim 소비):**
```python
# serving/state.py
SESSION_WINDOW_S = 1800          # D-06
REPLAY_WINDOW_S = 86400          # D-08 24h
RESET_BOOST_WINDOW_S = 86400     # D-08 재설정 24h 이내
HISTORY_MAX = 200                # CONTEXT 재량 "history 상한(예: 최근 200권)"
HISTORY_EVENT_TYPES = ("reader_open", "qualified_read", "completion")   # D-05, 전부 contracts.EVENT_TYPES 안
SESSION_EVENT_TYPES = ("reader_open", "detail_click")                   # D-06
COMPLETION = "completion"
def parse_ts(ts: str) -> float                      # ISO-8601(Z 허용) → epoch 초. naive 는 UTC 로 본다
def reset_boost(snapshots_count: int, latest_created_at: str | None, *, now: float) -> bool
class StateStore:
    def __init__(self, *, now: Callable[[], float] | None = None) -> None
    def apply_event(self, user_key: str, event_type: str, book_id: int | None, ts: str) -> None   # Nearline 전용 쓰기
    def user_state(self, user_key: str, *, seeds: Sequence[int] = (), categories: Sequence[str] = (),
                   context: str | None = None) -> UserState
    def session_active(self, user_key: str) -> bool
    def n_completed(self, user_key: str) -> int
    def completed_books(self, user_key: str) -> tuple[int, ...]      # 최근순 distinct
    def continue_reading(self, user_key: str) -> tuple[int, ...]     # reader_open 있고 completion 없는 책, 최근순(D-03)
    def last_event_ts(self, user_key: str) -> float | None
    def forget(self, user_key: str) -> None                          # DELETE …/personalization(D-08)
    def __len__(self) -> int                                         # 레코드 수(테스트·/health 보조)

# serving/nearline.py
BATCH_ROWS = 1000                # 리플레이·run_once 1회 SELECT 상한(CONTEXT 재량 "리플레이 배치 크기")
class NearlineLoop:
    def __init__(self, db: Database, store: StateStore, *, neighbors: Neighbors | None = None,
                 interval_s: float = NEARLINE_INTERVAL_S, now: Callable[[], float] | None = None) -> None
    wake: asyncio.Event
    last_run: str | None             # ISO seconds, /health.nearline_last_run
    last_rowid: int                  # 처리한 마지막 events.rowid(커서)
    def run_once(self) -> int        # 동기. rowid > last_rowid 를 BATCH_ROWS 씩 전부 적용, 처리 수 반환
    def replay(self) -> int          # 동기. ts ≥ now−REPLAY_WINDOW_S 만 적용 후 last_rowid = MAX(rowid)
    async def run(self) -> None      # while True: wait_for(wake.wait(), interval_s) → clear → to_thread(run_once) → last_run
    def waker(self, loop: asyncio.AbstractEventLoop) -> Callable[[], None]   # 동기 핸들러용: loop.call_soon_threadsafe(self.wake.set) — Advisor 확정 13(핸들러가 Event.set() 을 직접 부르지 않는다)

# serving/book_stats.py
PRIOR_POPULAR_N = 200            # 카테고리 prior 표본 크기(D-07, CONTEXT 재량)
class ServingBookStats:
    def __init__(self, catalog: Catalog, store: StateStore) -> None     # catalog 는 BookStatsSource 도 만족해야 한다(CatalogKR)
    def stats(self, book_ids: Sequence[int]) -> list[BookStats]          # catalog.stats 위임 1:1
    def user_level(self, user: UserState) -> float | None
```
</interfaces>
</context>

<feature>
  <name>user_key 상태 모델 — StateStore(D-05~D-08) · NearlineLoop(SERV-09, rowid 커서·24h 리플레이) · ServingBookStats(D-07 user_level)</name>
  <files>
    src/millie_rec/serving/state.py, src/millie_rec/serving/nearline.py, src/millie_rec/serving/book_stats.py, src/millie_rec/serving/__init__.py,
    tests/serving/test_state.py, tests/serving/test_nearline.py, tests/serving/test_book_stats.py
  </files>
  <behavior>
    **state.py** (`now` 주입: `T0 = 1_800_000_000.0`, `ts(sec)` = `datetime.fromtimestamp(T0 + sec, UTC).isoformat().replace("+00:00", "Z")`)
    - `parse_ts("2026-09-07T12:00:27Z") == datetime(2026, 9, 7, 12, 0, 27, tzinfo=UTC).timestamp()`; `parse_ts("2026-09-07T12:00:27+00:00")` 같은 값; naive `"2026-09-07T12:00:27"` 도 같은 값
    - `StateStore(now=lambda: T0)`; `apply_event("u", "library_add", 1, ts(-100))` → `user_state("u").history == ()`(D-05 서재 담기는 history 아님); `apply_event("u", "reader_open", 2, ts(-90))`, `("u", "qualified_read", 3, ts(-80))`, `("u", "completion", 4, ts(-70))` → `history == (4, 3, 2)`(최근순), `apply_event("u", "reader_open", 2, ts(-60))` → `history == (2, 4, 3)`(distinct·앞으로 이동)
    - `apply_event("u", "detail_click", 5, ts(-10))`, `("u", "reader_open", 6, ts(-1900))` → `user_state("u").session == (5,)`(1900초 전은 30분 창 밖), `session_active("u") is True`; `StateStore(now=lambda: T0 + 3600)` 로 같은 이벤트면 `session == ()`·`session_active is False`
    - `n_completed("u")` == completion distinct 수: completion 4 두 번 + 7 한 번 → `2`; `completed_books("u") == (7, 4)`; `user_state("u").context["n_completed"] == "2"`
    - `continue_reading("u")`: reader_open 8·9 후 completion 9 → `(8,)`(완독은 빠진다), 최근순
    - `user_state("u", seeds=(1, 2), categories=("소설", "IT"), context="evening").context == {"user_key": "u", "n_completed": "…", "categories": "소설,IT", "reading_time": "evening"}`; `explicit_seeds == (1, 2)`; `context` 인자 None 이면 `"reading_time"` 키 없음; 모르는 user_key 는 빈 history·session, `context["n_completed"] == "0"`
    - `HISTORY_MAX`: reader_open 을 1..205 넣으면 `len(history) == 200`, `history[0] == 205`, `1 not in history`
    - `reset_boost(2, ts(-3600), now=T0) is True`; `reset_boost(1, ts(-3600), now=T0) is False`; `reset_boost(3, ts(-90000), now=T0) is False`(24h 초과); `reset_boost(2, None, now=T0) is False`
    - `forget("u")` 후 `user_state("u").history == ()` 이고 `len(store)` 1 감소; 모르는 키 forget 은 예외 없음
    - 안전성: `apply_event("u", "impression", None, ts(0))`(book_id None) 는 예외 없이 무시(last_event_ts 만 갱신); 잘못된 ts `"not-a-time"` 은 `ValueError` 를 밖으로 내지 않고 무시(`log.warning`)

    **nearline.py** (`Database(tmp_path / "t.db")` + `apply_schema()`; **`events.ts` 는 demo_api(05-04) 가 UTC `Z` 형식으로 정규화해 저장한다 — `SQL_REPLAY` 의 `ts >= ?` 문자열 비교는 그 전제(revision C3), 테스트 헬퍼 `_ts` 도 같은 형식**; 행 삽입은 테스트 헬퍼 `_insert(db, event_id, user_key, event_type, book_id, ts, **cols)` 가 `INSERT INTO events(...) VALUES(?,…)` + `con.commit()`)
    - 계약: `NearlineLoop(db, store)` 에 `wake`(`asyncio.Event`)·`last_run is None`·`last_rowid == 0` 속성; `run_once()` 는 `int`
    - 경계(SERV-09): events 에 reader_open 2건 INSERT 직후 `store.user_state("u").history == ()`(핸들러 측은 INSERT 만) → `loop.run_once() == 2` → `history == (2, 1)`; 두 번째 `run_once() == 0`(커서 전진); 3번째 행 INSERT 후 `run_once() == 1`
    - 리플레이(D-08): ts 가 `now − 25h` 인 completion 1건 + `now − 1h` 인 reader_open 1건 → `replay() == 1`, `store.user_state("u").history == (그 1건,)`(revision A3 — `StateStore` 공개 표면에 `history()` 없음), `store.n_completed("u") == 0`(25h 전 것은 미적용), 이후 `run_once() == 0`(리플레이가 `last_rowid` 를 MAX 로 올림 — 오래된 행을 다시 읽지 않는다)
    - selected 채움(CONTEXT 재량 `impression.selected`): `impression(candidate_set_id="cand_1", book_id=7, selected NULL)` + `preference_book_selected(candidate_set_id="cand_1", book_id=7)` → `run_once()` 후 `SELECT selected FROM events WHERE event_type='impression'` == `1`; 다른 `book_id` 의 impression 은 NULL 유지
    - 예외 후 계속: `store.apply_event` 를 monkeypatch 로 1회 예외 → `run_once()` 는 예외를 밖으로 내지 않고 `log.exception` 후 나머지 행 적용(반환값은 성공 적용 수)
    - `last_run`: `run_once()` 는 `last_run` 을 갱신하지 않고(동기 경로), `asyncio.run(_one_tick(loop))` — 테스트 헬퍼가 `loop.wake.set()` 후 `await asyncio.wait_for(loop.run(), timeout=…)` 대신 **`run()` 의 본체 1회분**을 검증하기 어렵다 → `run()` 은 `while True` 안에서 `await self._tick()` 를 부르게 하고 `async def _tick(self) -> None` 을 두어 테스트가 `asyncio.run(loop._tick())` 로 1회 실행: 웨이크 set 상태에서 `_tick` 이 즉시(≤ interval) 돌아 `last_run` 이 ISO 문자열이 되고 `wake.is_set() is False`
    - `waker(asyncio.get_running_loop())()` 를 다른 스레드에서 호출하면(`asyncio.to_thread`) `wake.is_set()` 이 True 가 된다(스레드 안전 웨이크)

    **book_stats.py** (가짜 `_Catalog`: `meta(ids)` → `{"book_id", "difficulty", "categories"}` 행, `popular(categories, n)` → 카테고리 필터 흉내, `stats(ids)` → `BookStats(book_id=b)` 목록 + 호출 수 `self.calls`, `eligible` 그대로)
    - `ServingBookStats(cat, store).stats([1, 2])` 는 `cat.stats` 결과 그대로 반환하고 `cat.calls == 1`(위임 1회, test_guard.py `_FakeStats.calls` 관례)
    - 완독 책 difficulty 평균: store 에 completion 1·2·3, meta difficulty `{1: 0.2, 2: 0.6, 3: None}` → `user_level(store.user_state("u")) == pytest.approx(0.4)`(결측 3 제외)
    - 완독 0권 → 카테고리 prior: `user_state("u", categories=("소설",))` 에 대해 `popular(["소설"], n=200)` 이 `[10, 11]`, difficulty `{10: 0.3, 11: 0.5}` → `0.4`; `cat.popular` 호출 수는 두 번째 `user_level` 에서도 1(캐시 dict, 키 = `tuple(categories)`)
    - 완독 0권 ∧ categories 없음 → `None`; 완독 책 전부 difficulty None ∧ categories 없음 → `None`; `context` 에 `user_key` 없으면(익명 UserState) prior 만 시도
  </behavior>
  <implementation>Task 2 action 의 파일별 상수·시그니처·알고리즘 그대로.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): 스텁 3파일 + `__init__` 이름 추가 + 테스트 3파일 → 전 신규 테스트 AssertionError</name>
  <files>src/millie_rec/serving/state.py, src/millie_rec/serving/nearline.py, src/millie_rec/serving/book_stats.py, src/millie_rec/serving/__init__.py, tests/serving/test_state.py, tests/serving/test_nearline.py, tests/serving/test_book_stats.py</files>
  <read_first>
    - src/millie_rec/contracts.py L37-53(`EVENT_TYPES`), L93(`NEARLINE_INTERVAL_S`), L122-135(`UserState` — `context: dict[str, str]`), L316-338(`Catalog`·`Neighbors`·`BookStatsSource`)
    - src/millie_rec/serving/db.py 전문(`Database.connect()` thread-local · `apply_schema()`) — 고치지 않는다
    - src/millie_rec/serving/schema.sql L12-17(events 컬럼)
    - src/millie_rec/serving/__init__.py 전문(알파벳 `__all__`)
    - .planning/phases/05-must/05-CONTEXT.md D-05·D-06·D-07·D-08, Claude's Discretion "세션 만료·history 상한·리플레이 배치 크기·Nearline 루프 예외 처리·impression selected", "배치·SQLite 동시성"
    - .planning/phases/05-must/05-PATTERNS.md §1(state.py Analog A·B, 시간 주입), §2(nearline lifespan 자리·웨이크), "No Analog" nearline 스니펫, §15 Analog A·E
    - tests/serving/test_smoke.py L125-129(`Database(tmp_path/…)` 직접 사용) · tests/reranking/test_guard.py L12-27(`_FakeStats.calls` 관례)
    - src/millie_rec/data/catalog_kr.py L117-118(현재 `user_level` → None — 이 플랜의 어댑터가 감싼다; import 금지, 읽기만)
    - ../.claude/rules/python-tdd.md 전문 · ../.claude/rules/serving.md "이벤트 루프 · fallback" 절
  </read_first>
  <action>
    **왜 스텁인가:** 모듈이 없으면 `ModuleNotFoundError`, 메서드가 없으면 `AttributeError` 로 죽어 RED 가 아니다. Task 2 와 같은 상수·시그니처, 값만 틀린 스텁을 두어 **모든 신규 테스트가 `AssertionError`** 로 실패하는 정직한 RED 를 만든다.

    **1-a. `src/millie_rec/serving/state.py` 스텁** — 1줄 docstring `"""user_key 메모리 상태(history·session·완독) — Nearline 만 쓰고 요청은 읽는다(05-CONTEXT D-05~D-08, 아키텍처 01 §3-5)."""`. 상수 6개(`<interfaces>` 값 그대로) + `COMPLETION = "completion"`. `parse_ts` 는 `return 0.0`, `reset_boost` 는 `return False`. `class StateStore` 는 `__init__(self, *, now=None)` 에 `self._now = now or time.time`, `self._users: dict[str, _Record] = {}`, `self._lock = threading.Lock()`; `_Record` 는 `@dataclass class _Record: history: list[int] = field(default_factory=list); session: list[tuple[int, float]] = …; completed: list[int] = …; reading: list[int] = …; last_event_ts: float | None = None`. 공개 메서드는 전부 시그니처만: `apply_event` → `return None`, `user_state` → `UserState(user_id=None, explicit_seeds=tuple(seeds), context={})`, `session_active` → `False`, `n_completed` → `-1`, `completed_books`·`continue_reading` → `()`, `last_event_ts` → `None`, `forget` → `None`, `__len__` → `0`.
    **1-b. `src/millie_rec/serving/nearline.py` 스텁** — docstring `"""Nearline 루프 — 30s 타이머 ∨ asyncio.Event 웨이크, rowid 커서, 24h 리플레이(SERV-09, 아키텍처 01 §3-5)."""`. `BATCH_ROWS = 1000`. `class NearlineLoop.__init__(self, db, store, *, neighbors=None, interval_s=NEARLINE_INTERVAL_S, now=None)` 에 `self.wake = asyncio.Event()`, `self.last_run: str | None = None`, `self.last_rowid = 0`, 나머지 저장. `run_once` → `return -1`, `replay` → `return -1`, `async def _tick(self)` → `return None`, `async def run(self)` → `while True: await self._tick()`, `waker(self, loop)` → `return lambda: None`(틀린 값 — 테스트는 `wake.is_set()` 을 단정).
    **1-c. `src/millie_rec/serving/book_stats.py` 스텁** — docstring `"""서빙 측 BookStatsSource 어댑터 — stats 는 카탈로그 위임, user_level 은 완독 평균·카테고리 prior(D-07). 24h 집계는 설계만(D-13)."""`. `PRIOR_POPULAR_N = 200`. `class ServingBookStats.__init__(self, catalog, store)`; `stats(self, book_ids)` → `return []`; `user_level(self, user)` → `return None`.
    **1-d. `src/millie_rec/serving/__init__.py`** — import 3줄 추가(`from millie_rec.serving.book_stats import ServingBookStats` · `from millie_rec.serving.nearline import NearlineLoop` · `from millie_rec.serving.state import StateStore`), `__all__` 문자 코드 순 유지: `"API_VERSION", "Database", "EventIn", "GlobalPopularFallback", "NearlineLoop", "RecommendOut", "ServingBookStats", "StateStore", "create_app", "resolve_db_path"`.

    **1-e. `tests/serving/test_state.py`** (11건) — 상단 `T0 = 1_800_000_000.0`, 헬퍼 `def _ts(sec: float) -> str: return datetime.fromtimestamp(T0 + sec, UTC).isoformat(timespec="seconds").replace("+00:00", "Z")`, `def _store(now: float = T0) -> StateStore: return StateStore(now=lambda: now)`.
    - `# ── 계약 ──` `test_parse_ts_accepts_z_offset_and_naive_as_utc` — 세 형태가 같은 epoch(`datetime(2026, 9, 7, 12, 0, 27, tzinfo=UTC).timestamp()`)
    - `test_user_state_context_has_user_key_n_completed_categories_reading_time` — `_store().user_state("u", seeds=(1, 2), categories=("소설", "IT"), context="evening")` 의 `explicit_seeds == (1, 2)`, `context == {"user_key": "u", "n_completed": "0", "categories": "소설,IT", "reading_time": "evening"}`; `context=None` 이면 `"reading_time" not in ctx`; `history == () and session == ()`
    - `# ── 정확성 ──` `test_history_only_reading_events_distinct_recent_first` — library_add 1 · reader_open 2 · qualified_read 3 · completion 4 → `history == (4, 3, 2)`; reader_open 2 다시 → `(2, 4, 3)`; `1 not in history`
    - `test_session_is_30min_window_and_session_active` — detail_click 5 @−10s · reader_open 6 @−1900s → `session == (5,)`, `session_active("u") is True`; `_store(T0 + 3600)` 에 같은 이벤트 → `session == ()`, `False`
    - `test_n_completed_counts_distinct_completion_books` — completion 4, 4, 7 → `n_completed == 2`, `completed_books == (7, 4)`, `context["n_completed"] == "2"`
    - `test_continue_reading_excludes_completed_recent_first` — reader_open 8 @−50 · reader_open 9 @−40 · completion 9 @−30 → `continue_reading("u") == (8,)`
    - `test_reset_boost_requires_second_snapshot_within_24h` — 4 케이스(`(2, −3600) True`, `(1, −3600) False`, `(3, −90000) False`, `(2, None) False`)
    - `test_history_capped_at_history_max` — reader_open 1..205 → `len == 200`, `history[0] == 205`, `1 not in history`
    - `# ── 안전성 ──` `test_forget_removes_record_and_unknown_key_is_noop` — apply 후 `len(store) == 1` → `forget("u")` → `len == 0`, `user_state("u").history == ()`; `forget("nobody")` 예외 없음
    - `test_event_without_book_id_only_updates_last_event_ts` — impression book_id None @0 → `last_event_ts("u") == T0`, `history == ()`, `session == ()`
    - `test_bad_ts_is_ignored_not_raised(caplog)` — `apply_event("u", "reader_open", 1, "not-a-time")` 예외 없음, `history == ()`, `caplog` 에 WARNING 1건

    **1-f. `tests/serving/test_nearline.py`** (7건) — 상단 `_db(tmp_path)` = `Database(tmp_path / "t.db")` + `apply_schema()`; `_insert(db, event_id, user_key, event_type, book_id, ts, candidate_set_id=None, selected=None)` = `with db.connect() as con: con.execute("INSERT INTO events(event_id, user_key, book_id, event_type, ts, candidate_set_id, selected) VALUES(?,?,?,?,?,?,?)", (...))`; `T0`·`_ts` 는 test_state 와 같은 정의를 **복제**(테스트 간 import 금지 관례).
    - `# ── 계약 ──` `test_loop_exposes_wake_last_run_last_rowid` — `isinstance(loop.wake, asyncio.Event)`, `last_run is None`, `last_rowid == 0`, `isinstance(loop.run_once(), int)`
    - `# ── 정확성 ──` `test_handler_side_insert_does_not_change_state_until_run_once` — 2건 INSERT → `store.user_state("u").history == ()` → `run_once() == 2` → `history == (2, 1)` → `run_once() == 0` → 3번째 INSERT → `run_once() == 1`
    - `test_replay_applies_only_last_24h_and_advances_cursor` — completion @−25h(`_ts(-90000)`) + reader_open @−1h → `replay() == 1`, `history == (reader_open 책,)`, `n_completed == 0`, `run_once() == 0`
    - `test_preference_book_selected_fills_impression_selected` — impression(cand_1, 7) · impression(cand_1, 8) · preference_book_selected(cand_1, 7) → `run_once()` 후 `SELECT book_id, selected FROM events WHERE event_type='impression' ORDER BY book_id` == `[(7, 1), (8, None)]`
    - `# ── 안전성 ──` `test_apply_exception_is_logged_and_remaining_rows_applied(monkeypatch, caplog)` — 3건 INSERT, `store.apply_event` 를 첫 호출만 `RuntimeError` 던지는 래퍼로 교체 → `run_once() == 2`, caplog 에 ERROR 1건, `"Traceback" not in` 표준 출력(로그는 caplog 에만)
    - `test_tick_runs_once_when_woken_and_sets_last_run` — `loop.wake.set()`; `asyncio.run(loop._tick())` → `isinstance(loop.last_run, str) and loop.last_run.endswith("Z") or "+00:00" in loop.last_run`, `loop.wake.is_set() is False`, INSERT 해둔 1건이 store 에 반영
    - `test_waker_is_thread_safe` — `async def main(): w = loop.waker(asyncio.get_running_loop()); await asyncio.to_thread(w); await asyncio.sleep(0); return loop.wake.is_set()`; `asyncio.run(main()) is True`

    **1-g. `tests/serving/test_book_stats.py`** (5건) — 가짜:
    ```python
    class _Catalog:
        """Catalog + BookStatsSource 가짜 — meta·popular·stats 만(test_guard.py _FakeStats.calls 관례)."""
        def __init__(self, difficulty: dict[int, float | None], by_cat: dict[str, list[int]]) -> None:
            self.d, self.by_cat, self.calls, self.popular_calls = difficulty, by_cat, 0, 0
        def meta(self, ids): return [{"book_id": b, "difficulty": self.d.get(b), "categories": []} for b in ids]
        def popular(self, categories=(), n=50):
            self.popular_calls += 1
            return [b for c in categories for b in self.by_cat.get(c, [])][:n] if categories else list(self.d)[:n]
        def eligible(self, ids): return list(ids)
        def stats(self, ids):
            self.calls += 1
            return [BookStats(book_id=int(b)) for b in ids]
        def user_level(self, user): return None
    ```
    - `# ── 계약 ──` `test_stats_delegates_once_to_catalog` — `[s.book_id for s in ServingBookStats(cat, store).stats([1, 2])] == [1, 2]`, `cat.calls == 1`
    - `# ── 정확성 ──` `test_user_level_is_mean_difficulty_of_completed_books_excluding_missing` — completion 1·2·3, d `{1: 0.2, 2: 0.6, 3: None}` → `approx(0.4)`
    - `test_user_level_falls_back_to_category_prior_when_no_completion_and_caches` — categories ("소설",), by_cat `{"소설": [10, 11]}`, d `{10: 0.3, 11: 0.5}` → `approx(0.4)`; 두 번째 호출 후 `cat.popular_calls == 1`
    - `# ── 안전성 ──` `test_user_level_none_without_completion_and_categories` — 빈 store, `user_state("u")` → `None`; 완독 책 difficulty 전부 None ∧ categories () → `None`
    - `test_user_level_anonymous_user_without_user_key_uses_prior_only` — `UserState(None, context={"categories": "소설"})` → prior 값(`0.4`); `UserState(None)` → `None`

    **1-h. RED 확인:** `uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py --no-header 2>&1 | tail -40`. 기대: 신규 23건 대다수 failed, 사유는 `AssertionError`/`E  assert` 만(스텁 `-1`·`False`·`{}` 가 단정을 깨뜨린다). `ImportError`·`AttributeError`·`TypeError` 가 보이면 스텁 시그니처를 고친다(테스트를 고치지 않는다). `uv run pytest tests/test_architecture.py --no-header` → `3 passed`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py --no-header 2>&1 | tail -30; uv run pytest tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/book_stats.py tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py` 6파일 존재
    - `grep -c "def test_" tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py` → 11, 7, 5
    - RED 출력에 `AssertionError` 또는 `E  assert` 가 있고 `ImportError`·`ModuleNotFoundError`·`AttributeError`·`TypeError`·`SyntaxError` 가 없다(요약 줄 + 발췌를 SUMMARY 에)
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(data\|app\|retrieval\|ranking\|reranking\|evaluation\)" src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/book_stats.py` → 출력 없음
    - `grep -n '^SESSION_WINDOW_S = 1800' src/millie_rec/serving/state.py` 1행 · `grep -n '^HISTORY_EVENT_TYPES = ("reader_open", "qualified_read", "completion")' src/millie_rec/serving/state.py` 1행 · `grep -n '"StateStore"' src/millie_rec/serving/__init__.py` 1행
    - `git status --short -- src/millie_rec/serving/api.py src/millie_rec/serving/compose.py src/millie_rec/serving/fallback.py src/millie_rec/serving/db.py src/millie_rec/contracts.py` → 출력 없음
  </acceptance_criteria>
  <done>신규 23건 AssertionError 계열 RED, 아키텍처 3건 passed, `__init__` 3이름 추가. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): state·nearline·book_stats 구현 → 3 테스트 파일 전부 passed</name>
  <files>src/millie_rec/serving/state.py, src/millie_rec/serving/nearline.py, src/millie_rec/serving/book_stats.py</files>
  <read_first>
    - Task 1 이 만든 스텁 3파일과 테스트 3파일(단정이 요구하는 정확한 값)
    - .planning/phases/05-must/05-PATTERNS.md "No Analog" nearline 스니펫(`wait_for(wake.wait(), timeout)` · `TimeoutError` · `call_soon_threadsafe`) · §1 시간 주입 관례(`datetime.fromisoformat(ts.replace("Z", "+00:00"))`)
    - src/millie_rec/serving/db.py L32-39(`connect()` — 스레드마다 연결 1개, Nearline 은 `to_thread` 안에서 자기 스레드 연결을 쓴다)
  </read_first>
  <action>
    각 파일 ≤150줄, 줄 ≤100자. 모듈 머리 = 1줄 docstring → stdlib → `millie_rec.contracts` → `millie_rec.serving.db`(nearline 만, 타입용).

    **2-a. `state.py`**
    ```python
    def parse_ts(ts: str) -> float:
        """ISO-8601 → epoch 초. 'Z' 허용, tz 없으면 UTC(백엔드 01 §0 "ISO-8601 UTC")."""
        dt = datetime.fromisoformat(ts.replace("Z", "+00:00"))
        if dt.tzinfo is None:
            dt = dt.replace(tzinfo=UTC)
        return dt.timestamp()

    def reset_boost(snapshots_count: int, latest_created_at: str | None, *, now: float) -> bool:
        """D-08: 최신 스냅샷이 2번째 이상 ∧ created_at 이 24h 이내 → α +0.15 인자 True."""
        if snapshots_count < 2 or not latest_created_at:
            return False
        return now - parse_ts(latest_created_at) <= RESET_BOOST_WINDOW_S
    ```
    `StateStore.apply_event(user_key, event_type, book_id, ts)`: `try: t = parse_ts(ts) except ValueError: log.warning("bad ts %r for %s; ignored", ts, user_key); return`. `with self._lock:` 안에서 `rec = self._users.setdefault(user_key, _Record())`; `rec.last_event_ts = max(rec.last_event_ts or t, t)`; `if book_id is None: return`; `b = int(book_id)`; `if event_type in HISTORY_EVENT_TYPES: _push_front(rec.history, b, HISTORY_MAX)`; `if event_type == COMPLETION: _push_front(rec.completed, b, HISTORY_MAX); rec.reading = [x for x in rec.reading if x != b]`; `elif event_type == "reader_open" and b not in rec.completed: _push_front(rec.reading, b, HISTORY_MAX)`; `if event_type in SESSION_EVENT_TYPES: rec.session.append((b, t)); rec.session = rec.session[-HISTORY_MAX:]`. 헬퍼 `def _push_front(lst: list[int], b: int, cap: int) -> None: if b in lst: lst.remove(b); lst.insert(0, b); del lst[cap:]`.
    `_session_ids(rec, now)`: `cut = now - SESSION_WINDOW_S`; 최근순 distinct: `out=[]; for b, t in reversed(rec.session): if t >= cut and b not in out: out.append(b)`; `return tuple(out)`.
    `user_state(...)`: `rec = self._users.get(user_key, _Record())`(락 안에서 복사본 읽기 — `with self._lock: rec = copy.deepcopy(...)` 는 비용이 크니 `tuple(...)` 로 즉시 스냅샷); `ctx = {"user_key": user_key, "n_completed": str(len(rec.completed)), "categories": ",".join(categories)}`; `if context: ctx["reading_time"] = context`; `return UserState(user_id=None, explicit_seeds=tuple(int(s) for s in seeds), history=tuple(rec.history), session=self._session_ids(rec, self._now()), context=ctx)`.
    `session_active` = `bool(self._session_ids(rec, now))`; `n_completed` = `len(rec.completed)`; `completed_books` = `tuple(rec.completed)`; `continue_reading` = `tuple(rec.reading)`; `last_event_ts`; `forget` = `self._users.pop(user_key, None)`; `__len__` = `len(self._users)`. 모두 `with self._lock:`.

    **2-b. `nearline.py`**
    ```python
    SQL_NEW = ("SELECT rowid, user_key, book_id, event_type, ts, candidate_set_id FROM events "
               "WHERE rowid > ? ORDER BY rowid LIMIT ?")
    SQL_REPLAY = ("SELECT rowid, user_key, book_id, event_type, ts, candidate_set_id FROM events "
                  "WHERE ts >= ? ORDER BY rowid LIMIT ?")
    SQL_MAX_ROWID = "SELECT COALESCE(MAX(rowid), 0) FROM events"
    SQL_MARK_SELECTED = ("UPDATE events SET selected = 1 WHERE event_type = 'impression' "
                         "AND user_key = ? AND candidate_set_id = ? AND book_id = ?")
    ```
    `_apply_rows(self, con: sqlite3.Connection, rows) -> int`(**Advisor revision A2 — 커넥션을 인자로 받아 `SQL_MARK_SELECTED` 의 `con` 을 해소**): `n = 0; for rowid, user_key, book_id, event_type, ts, cand in rows: try: self.store.apply_event(user_key, event_type, book_id, ts); if event_type == "preference_book_selected" and cand and book_id is not None: with con: con.execute(SQL_MARK_SELECTED, (user_key, cand, book_id)); n += 1 except Exception: log.exception("nearline apply failed rowid=%s; continuing", rowid); self.last_rowid = max(self.last_rowid, rowid)`; `return n`. (커서는 예외 행도 전진 — 같은 행을 영원히 재시도하지 않는다.)
    `run_once()`: `con = self.db.connect(); total = 0; while True: rows = con.execute(SQL_NEW, (self.last_rowid, BATCH_ROWS)).fetchall(); if not rows: return total; total += self._apply_rows(con, rows); if len(rows) < BATCH_ROWS: return total`.
    `replay()`: `con = self.db.connect()`; `since = datetime.fromtimestamp(self._now() - REPLAY_WINDOW_S, UTC).isoformat(timespec="seconds").replace("+00:00", "Z")`; 배치마다 `self._apply_rows(con, rows)`(A2); 배치 루프는 `SQL_REPLAY` + 마지막 rowid 초과 조건을 위해 **replay 전용 두 번째 인자 없이** `LIMIT BATCH_ROWS OFFSET ?` 대신 `rowid > last` 를 함께 걸어라: `SQL_REPLAY = "… WHERE ts >= ? AND rowid > ? ORDER BY rowid LIMIT ?"`, 인자 `(since, self.last_rowid, BATCH_ROWS)`; 루프 종료 후 `self.last_rowid = con.execute(SQL_MAX_ROWID).fetchone()[0]`; 처리 수 반환. `REPLAY_WINDOW_S` 는 `from millie_rec.serving.state import REPLAY_WINDOW_S, StateStore`(같은 슬라이스 내부 import 허용).
    `async def _tick(self)`: `try: await asyncio.wait_for(self.wake.wait(), timeout=self.interval_s) except TimeoutError: pass`(3.11: `asyncio.TimeoutError is TimeoutError`); `self.wake.clear()`; `try: await asyncio.to_thread(self.run_once); self.last_run = datetime.now(UTC).isoformat(timespec="seconds").replace("+00:00", "Z") except Exception: log.exception("nearline run failed; continuing")`.
    `async def run(self)`: `while True: await self._tick()`. `waker(loop)`: `return lambda: loop.call_soon_threadsafe(self.wake.set)`. `neighbors` 는 저장만(wave 4 `after_completion` 이 쓴다 — 지금은 미사용 인자, 주석 1줄).

    **2-c. `book_stats.py`**
    ```python
    class ServingBookStats:
        """catalog(BookStatsSource+Catalog) 를 감싸 user_level 만 서빙 상태로 계산한다(D-07). app 이 주입."""
        def __init__(self, catalog: Catalog, store: StateStore) -> None:
            self.catalog, self.store = catalog, store
            self._prior: dict[tuple[str, ...], float | None] = {}
        def stats(self, book_ids: Sequence[int]) -> list[BookStats]:
            return self.catalog.stats(book_ids)  # type: ignore[attr-defined]  # CatalogKR 은 세 Protocol 을 만족
        def user_level(self, user: UserState) -> float | None:
            key = user.context.get("user_key")
            completed = self.store.completed_books(key) if key else ()
            level = _mean_difficulty(self.catalog, completed) if completed else None
            if level is not None:
                return level
            cats = tuple(c for c in user.context.get("categories", "").split(",") if c)
            return self._category_prior(cats) if cats else None
    ```
    `_mean_difficulty(catalog, ids)`: `vals = [m["difficulty"] for m in catalog.meta(list(ids)) if m.get("difficulty") is not None]; return sum(vals)/len(vals) if vals else None`. `_category_prior(cats)`: `if cats not in self._prior: self._prior[cats] = _mean_difficulty(self.catalog, self.catalog.popular(list(cats), n=PRIOR_POPULAR_N))`; `return self._prior[cats]`.

    **2-d. GREEN 확인:** `uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py tests/test_architecture.py --no-header` → `26 passed`(23 + 3). 실패 시 구현을 고친다. 테스트 단정을 바꾸지 않는다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - 요약 줄 `26 passed`, failed 0
    - `wc -l src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/book_stats.py` 각 ≤150
    - `grep -c "WHERE rowid > ?" src/millie_rec/serving/nearline.py` ≥ 1 · `grep -c "call_soon_threadsafe" src/millie_rec/serving/nearline.py` ≥ 1 · `grep -c "asyncio.to_thread" src/millie_rec/serving/nearline.py` ≥ 1
    - `grep -c "completed_books(" src/millie_rec/serving/book_stats.py` ≥ 1 · `grep -n '^PRIOR_POPULAR_N = 200' src/millie_rec/serving/book_stats.py` 1행
    - `grep -c "f\"" src/millie_rec/serving/nearline.py | xargs -I{} sh -c 'grep -n "execute(f" src/millie_rec/serving/nearline.py; true'` → `execute(f` 0건(SQL 에 f-string 없음, 전부 `?` 바인딩)
  </acceptance_criteria>
  <done>3 테스트 파일 + 아키텍처 전부 passed. 파일 ≤150줄. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · 줄 수 · 공개 표면 · SUMMARY</name>
  <files>src/millie_rec/serving/state.py, src/millie_rec/serving/nearline.py, src/millie_rec/serving/book_stats.py, src/millie_rec/serving/__init__.py</files>
  <read_first>
    - Task 2 결과 3파일(정리 대상은 이 플랜이 건드린 파일만 — 최소 변경 원칙 CLAUDE.md §3-7)
    - ../.claude/rules/python-tdd.md "사이클 3. Refactor" 절
  </read_first>
  <action>
    1. `uv run ruff format src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/book_stats.py src/millie_rec/serving/__init__.py tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py && uv run ruff check <같은 경로>` — E501(100자)·B008·I(정렬)·UP 전부 클린. **다른 serving 파일은 포맷하지 않는다**(05-02~05 가 같은 시각에 편집 중).
    2. 정리 범위: 중복 SQL 문자열 상수화, `_Record` 필드 주석(어느 D 가 정의했는지), 미사용 import 제거. 로직·시그니처·상수 값 무변경.
    3. 재실행 `uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py tests/test_architecture.py --no-header` → `26 passed`.
    4. `uv run python -c "from millie_rec.serving import StateStore, NearlineLoop, ServingBookStats; print('ok')"` → `ok`(공개 표면 import 가 pandas·scipy 를 끌지 않는지 `import sys; print('pandas' in sys.modules)` → `False`).
    5. SUMMARY 에 인계용 공개 시그니처(`<interfaces>` 의 "이 플랜이 만드는 공개 시그니처" 블록을 실제 코드에서 복사)를 적는다 — 05-06·05-08 이 그대로 쓴다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/book_stats.py src/millie_rec/serving/__init__.py tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py && uv run ruff check src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/book_stats.py src/millie_rec/serving/__init__.py tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py && uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py tests/test_architecture.py --no-header && uv run python -c "import sys; from millie_rec.serving import StateStore, NearlineLoop, ServingBookStats; print('pandas' in sys.modules)"</automated>
  </verify>
  <acceptance_criteria>
    - ruff format --check · ruff check 클린(출력 없음 / `All checks passed!`)
    - `26 passed`
    - 마지막 python 명령 출력 `False`
    - `git status --short` 에 이 플랜 `files_modified` 7개 외 파일 없음(형제 플랜 파일은 형제 소유 — 이 Worker 의 diff 에 없어야 한다)
  </acceptance_criteria>
  <done>ruff 클린 · 26 passed · 공개 표면 3이름 · SUMMARY 작성. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `events` 테이블 → `NearlineLoop.run_once` | 클라이언트가 보낸 이벤트(ts·book_id 는 05-04 품질 게이트를 거쳤지만 플래그가 붙어도 저장됨) 를 상태로 반영 |
| `UserState.context` → Model 레인(guard·hybrid) | 문자열 dict. `n_completed` 는 서버가 넣은 값(클라이언트 입력 아님) |
| 메모리 `StateStore` | 프로세스 내부. Nearline 스레드 쓰기 / 핸들러 스레드 읽기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `events.ts` 조작으로 세션·리플레이 왜곡 | mitigate | `parse_ts` 실패는 무시+WARNING, 미래 ts 는 05-04 가 `ts_future` 플래그(저장은 함). 세션 창은 서버 `now()` 기준 — 미래 ts 이벤트가 세션을 영구 활성화하지 못하도록 `_session_ids` 는 `t <= now + SESSION_WINDOW_S` 상한도 건다(구현 1줄) |
| T-05-01-02 | Denial of Service | 메모리 dict 무한 성장 | mitigate | `HISTORY_MAX=200` 으로 history·completed·reading·session 상한, `forget` 으로 삭제. user_key 수는 제한 없음(과제 데모 규모, 아키 §8 리스크 수용) |
| T-05-01-03 | Denial of Service | Nearline 한 행의 예외로 루프 정지 | mitigate | `_apply_rows` 행 단위 `try/except` + 커서 전진, `_tick` 전체 `try/except` 로그 후 계속(테스트 `test_apply_exception_is_logged_and_remaining_rows_applied`) |
| T-05-01-04 | Tampering | SQL 인젭션 | mitigate | SQL 3종 전부 `?` 바인딩, 테이블·컬럼 이름 리터럴만. acceptance `execute(f` 0건 |
| T-05-01-05 | Information Disclosure | 예외 문자열 노출 | accept | 이 플랜에 HTTP 표면 없음. 로그는 서버 로그에만(`log.exception`) |
| T-05-01-06 | Repudiation | 리플레이 범위 밖 이벤트 소실 | accept | 24h 창은 D-08 결정. 원본은 SQLite 에 남는다 |
</threat_model>

<verification>
- `uv run pytest tests/serving/test_state.py tests/serving/test_nearline.py tests/serving/test_book_stats.py tests/test_architecture.py --no-header` → `26 passed`
- `uv run ruff format --check <자기 경로> && uv run ruff check <자기 경로>` 클린
- `wc -l src/millie_rec/serving/{state,nearline,book_stats}.py` 각 ≤150
- 공개 표면 `from millie_rec.serving import StateStore, NearlineLoop, ServingBookStats` 성립
- (wave 1 종료 후 Advisor 1회, 05-06 `<wave_gate>`) `uv run pytest --no-header` 327 + wave 1 신규 전부 passed · `make smoke` PASS
- 커밋 0: `git log --oneline | head -1` 불변
</verification>

<success_criteria>
- **SERV-09** 코드 증거: 상태 쓰기는 `StateStore.apply_event` 하나이고 그 호출자는 `NearlineLoop._apply_rows` 만(`grep -rn "apply_event(" src/millie_rec/serving/` → nearline.py 1곳 + 정의). 30s ∨ 웨이크 루프 `_tick`, 24h 리플레이 `replay`
- **SERV-04** 재료: `reset_boost` 조건(스냅샷 ≥2 ∧ 24h) 이 함수로 존재 — 05-06 이 `weights(user, reset_boost=True)` 로 넘긴다
- D-05(history 3종만) · D-06(1800초 창·session_active) · D-07(`n_completed` context str · `user_level` 완독 평균/카테고리 prior) · D-08(메모리 dict·Nearline 전용 쓰기·24h·reset_boost·forget) 이 코드·테스트에 존재(Advisor 확정 4·5·6)
- `contracts.py` 무변경, 의존성 추가 0, 형제 wave 1 파일 무변경, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/05-must/05-01-SUMMARY.md` 를 만든다. 포함: RED(AssertionError 발췌 3건 이상 + 요약 줄) · GREEN(파일별 구현 요지, 상수 값 `SESSION_WINDOW_S=1800 REPLAY_WINDOW_S=86400 HISTORY_MAX=200 BATCH_ROWS=1000 PRIOR_POPULAR_N=200`) · REFACTOR(ruff·`wc -l`·공개 표면 3이름) · `## TDD Gate Compliance`(커밋 게이트 생략 사유 = 프로젝트 커밋 정책(사용자 지시 2026-09-05), 대체 증거 = pytest 요약 줄 RED/GREEN 2개) · **05-06·05-08 인계용 공개 시그니처**(`StateStore`·`NearlineLoop`·`ServingBookStats`·`reset_boost` 실제 코드에서 복사) · 변경 파일 목록(`git status --short -- src/millie_rec/serving tests/serving`) · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
