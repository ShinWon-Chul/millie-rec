---
phase: 05-must
plan: 10
type: tdd
wave: 4
depends_on: ["05-09"]
files_modified:
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/cascade.py
  - tests/serving/test_after_completion.py
autonomous: true
requirements: [SERV-11]
lane: Serving (after_completion Worker — Should, 시간 부족 시 wave 4 통째로 버린다)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, Advisor 확정 10)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/book_stats.py
  - src/millie_rec/serving/demo_api.py
  - src/millie_rec/serving/privacy_api.py
  - src/millie_rec/serving/persona.py
  - src/millie_rec/serving/dashboard_api.py
  - src/millie_rec/serving/bench.py
  - src/millie_rec/serving/__init__.py
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/data/**
  - artifacts/serving/**
  - results/**
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_smoke.py
  - tests/serving/test_recommend_level0.py
  - tests/serving/test_api_weights.py
  - tests/serving/test_state.py
  - tests/serving/test_nearline.py
  - tests/serving/test_compose.py
  - tests/serving/test_cascade.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "completion 이벤트를 Nearline 이 처리하면 Neighbors.neighbors(book, 20) 을 미리 계산해 StateStore 에 after_completion 이웃으로 저장하고(새 완독이 덮어씀), 다음 GET /api/recommend 의 rows 최상단에 after_completion 행 '『X』을 완독하셨네요, 다음은'(source=content) 이 온다(D-13, SERV-11, 수용 기준 '완독 → 완독하셨네요 row(Should)')"
    - "after_completion 이 있어도 나머지 5행 순서·dedup(after_completion 이 가장 앞이라 최우선)·기존 test_cascade·test_compose 단정은 그대로 통과한다"
    - "완독 시 해당 user_key 의 level 1 캐시가 무효화되어 다음 응답이 캐시가 아닌 새 행을 만든다(D-10)"
  artifacts:
    - path: "src/millie_rec/serving/state.py"
      provides: "_Record.after_completion: tuple[int, tuple[int, ...]] | None · StateStore.set_after_completion(user_key, book_id, neighbors) · after_completion(user_key) -> tuple[int, tuple[int, ...]] | None"
      contains: "def after_completion"
      max_lines: 150
    - path: "src/millie_rec/serving/nearline.py"
      provides: "_apply_rows: event_type == 'completion' and self.neighbors → store.set_after_completion(user_key, book_id, self.neighbors.neighbors(book_id, AFTER_COMPLETION_N)) · on_completion 콜백(캐시 무효화)"
      contains: "set_after_completion("
      max_lines: 150
    - path: "src/millie_rec/serving/compose.py"
      provides: "TITLE_AFTER = '『{title}』을 완독하셨네요, 다음은' · ROW_ORDER 맨 앞 'after_completion' · compose_rows(after_completion=None) 인자"
      contains: "after_completion"
      max_lines: 150
    - path: "src/millie_rec/serving/cascade.py"
      provides: "_personal: after_completion=store.after_completion(user_key) 전달 · NearlineLoop 생성 시 on_completion=cache.invalidate 는 api.py 몫이 아니라 cascade 가 아닌 — api.py 무변경을 위해 nearline 생성자 인자 대신 NearlineLoop.on_completion 속성 대입을 05-06 lifespan 이 아닌 이 플랜의 cascade 가 하지 않는다(아래 action 참고)"
      contains: "after_completion="
      max_lines: 150
    - path: "tests/serving/test_after_completion.py"
      provides: "state set/get·덮어쓰기 · nearline completion → neighbors 호출·저장·on_completion 콜백 · compose 최상단 행·source content·dedup · cascade HTTP 끝단(events completion → run_once → rows[0].row_id == 'after_completion') · 캐시 무효화"
      min_lines: 80
  key_links:
    - from: "src/millie_rec/serving/nearline.py"
      to: "contracts.Neighbors"
      via: "self.neighbors.neighbors(book_id, AFTER_COMPLETION_N) — 유일한 이웃 경로(serving 은 retrieval 을 import 못 함)"
      pattern: "self.neighbors.neighbors\\("
    - from: "src/millie_rec/serving/cascade.py"
      to: "src/millie_rec/serving/compose.py"
      via: "compose_rows(..., after_completion=store.after_completion(r.user_key) if store else None)"
      pattern: "after_completion="
---

<objective>
Should 꼬리 1/3(D-13, 버리는 순서 최상단 = 가장 늦게 버림 — 결정 '버리는 순서 = 아키 §8 표'(개발일지 2026-09-04 파일 항목 D40)): **완독 직후 행 `after_completion`**. Nearline 이 `completion` 이벤트를 보면 `Neighbors.neighbors(book, 20)` 을 미리 계산해 `StateStore` 에 저장하고, 다음 응답 **최상단**에 "『X』을 완독하셨네요, 다음은" 행(`source=content`) 을 붙인다. 새 완독이 덮어쓴다. Phase 6 DEMO-07(뷰어 완독 → 메인 변화) 의 서버 전제.

Purpose: **SERV-11**, 수용 기준 '완독 → 완독하셨네요 row(Should)'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표). 축③ Nearline 사전 계산의 코드 증거(아키텍처 01 §3-5 "after_completion 사전 계산"). 결정 '완독 직후 트리거'(개발일지 2026-09-04 파일 항목 D12).
Output: 4파일 증분(각 ≤150 유지) + 테스트 1 신규. 커밋 0. **시간 부족 시 이 wave 통째로 버린다** — 그 경우 PDF 에 "설계만" 표기(05-09 SUMMARY 판단).

**wave 4 병렬:** 05-11 은 `demo_api.py`, 05-12 는 `dashboard_api.py` 만 고친다. 이 플랜은 `api.py` 를 고치지 않는다 — 캐시 무효화 콜백은 `NearlineLoop.on_completion: Callable[[str], None] | None` **속성**으로 두고, `cascade.Cascade.__init__` 가 `nearline` 을 모르므로 **`api.py` 무변경 방법**: `NearlineLoop.__init__` 에 `cache: object | None = None` keyword 를 추가하지 않고, 대신 `StateStore.set_after_completion` 이 완독 사실을 기록하면 `cascade._personal` 이 `store.after_completion(user_key)` 의 `(book_id, nbrs)` 를 **캐시 키 검증**에 쓴다: level 1 캐시 조회 시 `cache.get` 결과의 rows 에 `after_completion` 행이 없고 store 에는 있으면 캐시 미스로 취급(`_degrade` 1줄). 이렇게 하면 api.py·lifespan 을 건드리지 않는다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/05-must/05-CONTEXT.md
@.planning/phases/05-must/05-01-SUMMARY.md
@.planning/phases/05-must/05-02-SUMMARY.md
@.planning/phases/05-must/05-06-SUMMARY.md
@.planning/phases/05-must/05-09-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/state.py
@src/millie_rec/serving/nearline.py
@src/millie_rec/serving/compose.py
@src/millie_rec/serving/cascade.py
@tests/serving/test_state.py
@tests/serving/test_nearline.py
@tests/serving/test_compose.py
@tests/serving/test_cascade.py
@tests/conftest.py
@../.claude/rules/serving.md
@../.claude/rules/data.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/architecture.md

<wave_gate>
## Wave 3 게이트 — wave 4 세 플랜 공통 선행 조건(Advisor 1회, 05-10·11·12 중 첫 착수 전)
| 명령 | 기대 |
|---|---|
| `ls .planning/phases/05-must/05-09-SUMMARY.md` | 존재, "wave 4 착수" 판단이 "진행" |
| `uv run pytest --no-header` | failed 0(05-09 게이트 값 그대로) |
| `jq .p95 results/latency.json` | < 200 |
wave 4 세 플랜은 `files_modified` 가 겹치지 않는다(state·nearline·compose·cascade / demo_api / dashboard_api). 종료 후 Advisor 가 전역 pytest·`make smoke`·`make bench` 재실행(after_completion 행이 p95 를 바꾸는지 기록 — PDF 숫자는 05-09 값 유지, 재측정은 참고).
</wave_gate>

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** `uv run pytest <대상> --no-header` 만. RED = `AssertionError`. 기존 테스트(`test_state`·`test_nearline`·`test_compose`·`test_cascade`) 단정 수정 금지 — 증분은 **기본값이 기존 동작**(`after_completion=None` → 행 없음).
**star 의존·표기(`../.claude/rules/architecture.md`·`data.md`·`serving.md`):** 이웃은 `Neighbors` Protocol 로만. `after_completion` items 는 `source_channels=("content",)`, `channel_mix` 키 `content`("데모 카탈로그의 이웃은 콘텐츠 유사도"). `ROW_IDS` 에 `after_completion` 이미 있음(contracts 무변경).
**간결함:** 4파일 각 ≤150 유지 — 넘으면 보고(분리는 Advisor 결정).
**완료 기준(자기 경로만, revision A5):** ruff 클린(4 src + 1 test) · `uv run pytest tests/serving tests/test_architecture.py --no-header --ignore=tests/serving/test_ratings.py --ignore=tests/serving/test_dashboard.py` failed 0(wave 4 형제 05-11·05-12 의 테스트 제외). `make smoke` 는 wave 4 종료 후 Advisor 1회.
</project_rules_inline>

<interfaces>
From 05-01·05-02·05-06 SUMMARY(실제 코드 정본): `StateStore.apply_event/user_state/continue_reading/forget` · `NearlineLoop._apply_rows(rows)`(행 단위 try/except, `self.neighbors` 저장됨 — 미사용 인자) · `compose_rows(*, items, catalog, neighbors, level, seeds, categories, criterion, persona_name, continue_ids, all_categories, k)` · `ROW_ORDER` · `Cascade._personal(r, model, k, t0, context, bd)` · `Cascade._degrade(...)` · `Level1Cache.get/put/invalidate`.
From contracts: `Neighbors.neighbors(book_id, n=20) -> list[tuple[int, float]]` · `ROW_IDS[0] == "after_completion"` · `ROW_PURPOSES` 의 `"discover"`.

**이 플랜이 추가하는 시그니처:**
```python
# state.py
def set_after_completion(self, user_key: str, book_id: int, neighbors: Sequence[tuple[int, float]]) -> None   # 덮어쓰기
def after_completion(self, user_key: str) -> tuple[int, tuple[tuple[int, float], ...]] | None
# nearline.py
AFTER_COMPLETION_N = 20      # D-13 Neighbors.neighbors(book, 20)
# compose.py
TITLE_AFTER = "『{title}』을 완독하셨네요, 다음은"
ROW_ORDER = ("after_completion", "continue_reading", ROW_ANCHOR_PREFIX, "persona_shelf", "trending", "fresh_picks")
def compose_rows(*, …기존…, after_completion: tuple[int, Sequence[tuple[int, float]]] | None = None) -> tuple[tuple[Row, ...], int]
```
</interfaces>
</context>

<feature>
  <name>after_completion 행 — Nearline 사전 계산 → StateStore → compose 최상단 → cascade 배선 (D-13 · SERV-11)</name>
  <files>src/millie_rec/serving/state.py, src/millie_rec/serving/nearline.py, src/millie_rec/serving/compose.py, src/millie_rec/serving/cascade.py, tests/serving/test_after_completion.py</files>
  <behavior>
    - state: `store.after_completion("u") is None`; `set_after_completion("u", 3, [(4, 0.9), (5, 0.8)])` → `(3, ((4, 0.9), (5, 0.8)))`; 다시 `set_after_completion("u", 7, [(8, 0.5)])` → `(7, ((8, 0.5),))`(덮어쓰기); `forget("u")` 후 None
    - nearline: `_Nbrs.neighbors(b, n)` 호출 기록; events 에 `completion` book 3 INSERT → `run_once()` → `nbrs.calls == [(3, 20)]`, `store.after_completion("u")[0] == 3`, 이웃 튜플이 `_Nbrs` 반환과 동일; `neighbors=None` 인 루프는 completion 을 처리해도 `after_completion` None(기존 동작); `reader_open` 은 이웃을 부르지 않는다(`calls == []`)
    - compose: `compose_rows(..., level=0, after_completion=(3, [(4, 0.9), (5, 0.8), (6, 0.7)]))` → `rows[0].row_id == "after_completion"`, `rows[0].title == "『밀리 표본 도서 3』을 완독하셨네요, 다음은"`, `purpose == "discover"`, items book_id `[4, 5, 6]`(eligible 필터·최대 ROW_SIZE) 전부 `source == "content" and source_channels == ("content",)`, `channel_mix == {"content": 3}`, 이어서 기존 5행 순서; dedup: 4 가 앵커 이웃에도 있으면 앵커에서 빠지고 `dedup_removed` +1(after_completion 이 최우선); `after_completion=None`(기본) → 기존 5행 그대로(`test_compose.py` 회귀); level 2·3 에는 `after_completion` 을 넘겨도 무시(비개인화 2행 유지)
    - cascade HTTP 끝단(`test_cascade.py` 조립 복제 — **revision C7: `monkeypatch.setattr(NearlineLoop, "run", _idle)` 로 살아 있는 루프를 끄고**(`async def _idle(self): await asyncio.Event().wait()`) 수동 `run_once()`): 온보딩 → level 0 5행 → `POST /api/events completion book 9` 202 → `GET` 즉시 5행(변화 없음, SERV-09) → `app.state.nearline.run_once()` → `GET` → `rows[0].row_id == "after_completion"`, `rows[0].title` 에 `"밀리 표본 도서 9"`, `len(rows) == 6`, `9 not in` 어느 행 items(seen); `fallback_level == 0`
    - 캐시 무효화(D-10): `BUDGET_MS=1` monkeypatch 로 level 1 캐시 경로 진입 시 캐시된 rows 에 after_completion 이 없고 store 에 있으면 → `fallback_level == 2`(캐시 미스 취급) — 또는 구현이 `store.set_after_completion` 시 콜백으로 `cache.invalidate` 를 부르면 같은 결과; 어느 쪽이든 "완독 뒤 옛 캐시가 나오지 않는다" 를 단정
  </behavior>
  <implementation>Task 2 action.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): 시그니처 스텁(after_completion 인자·메서드·상수) + test_after_completion.py → AssertionError, 기존 serving 전부 통과</name>
  <files>src/millie_rec/serving/state.py, src/millie_rec/serving/nearline.py, src/millie_rec/serving/compose.py, src/millie_rec/serving/cascade.py, tests/serving/test_after_completion.py</files>
  <read_first>
    - 4 대상 파일 전문(05-01·02·06 결과) — 특히 `_Record` 필드, `_apply_rows`, `compose_rows` 골격과 `_dedup`, `_personal`·`_degrade`
    - tests/serving/test_cascade.py 상단 조립 헬퍼(복제용) · tests/serving/test_compose.py `_Cat`·`_Nbrs`(복제용)
    - .planning/phases/05-must/05-CONTEXT.md D-13 after_completion 문장 · D-10 "완독(Should) 시 해당 user_key 캐시 무효화"
    - ../.claude/rules/serving.md "Page Composition … Should `after_completion`(최상단)"
  </read_first>
  <action>
    스텁: `state.py` — `_Record.after_completion: tuple[int, tuple[tuple[int, float], ...]] | None = None`, `set_after_completion` 은 `return None`(저장 안 함), `after_completion` 은 `return None`. `nearline.py` — `AFTER_COMPLETION_N = 20` 상수만. `compose.py` — `TITLE_AFTER` 상수, `ROW_ORDER` 맨 앞 `"after_completion"`, `compose_rows(..., after_completion=None)` 인자만 추가(무시). `cascade.py` — 변경 없음(RED 에서 HTTP 끝단 테스트가 `rows[0].row_id == "after_completion"` 에서 실패). 테스트 9건: `test_state_set_get_overwrite_forget` · `test_nearline_completion_precomputes_neighbors_only` · `test_nearline_without_neighbors_keeps_none` · `test_compose_after_completion_row_first_content_channel` · `test_compose_after_completion_dedup_wins_over_anchor` · `test_compose_default_none_keeps_five_rows` · `test_compose_nonpersonal_ignores_after_completion` · `test_http_completion_event_then_run_once_prepends_row` · `test_http_stale_cache_not_served_after_completion`. RED 확인 + `uv run pytest tests/serving tests/test_architecture.py --no-header` 기존 전부 passed.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_after_completion.py --no-header 2>&1 | tail -25; uv run pytest tests/serving tests/test_architecture.py --no-header --ignore=tests/serving/test_after_completion.py --ignore=tests/serving/test_ratings.py --ignore=tests/serving/test_dashboard.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/serving/test_after_completion.py` → 9 · RED 출력 `AssertionError`/`E  assert` 만
    - 기존 serving + 아키텍처 failed 0
    - `grep -n '^ROW_ORDER = ("after_completion"' src/millie_rec/serving/compose.py` 1행 · `grep -c "after_completion" src/millie_rec/serving/compose.py` ≥ 3
  </acceptance_criteria>
  <done>9건 RED, 기존 전부 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): 저장·사전 계산·최상단 행·cascade 배선·캐시 정합 → 9 passed + serving 전체 회귀 0</name>
  <files>src/millie_rec/serving/state.py, src/millie_rec/serving/nearline.py, src/millie_rec/serving/compose.py, src/millie_rec/serving/cascade.py</files>
  <read_first>Task 1 스텁·테스트</read_first>
  <action>
    - `state.py`: `set_after_completion` 은 `with self._lock: rec = self._users.setdefault(user_key, _Record()); rec.after_completion = (int(book_id), tuple((int(b), float(w)) for b, w in neighbors))`; `after_completion` 은 락 안 읽기. `forget` 은 레코드 전체 삭제라 자동.
    - `nearline.py::_apply_rows`: `store.apply_event(...)` 뒤 `if event_type == "completion" and book_id is not None and self.neighbors is not None: self.store.set_after_completion(user_key, book_id, self.neighbors.neighbors(int(book_id), AFTER_COMPLETION_N))`(같은 try 안 — 이웃 예외도 행 단위 로그).
    - `compose.py`(**revision B1 — 05-02 가 이미 `_neighbor_row(row_id, title, subtitle, seed, nbrs, catalog, exclude) -> Row | None` 을 일반화해 두었다; 이 플랜은 호출 1줄만 추가**): `compose_rows` level < 2 분기 맨 앞에 `if after_completion is not None: seed, nbrs = after_completion; title = TITLE_AFTER.format(title=_title_of(catalog, seed)); a = _neighbor_row("after_completion", title, None, seed, nbrs, catalog, exclude); if a: rows_list.insert(0, a); exclude |= {i.book_id for i in a.items}`(continue_reading 보다 앞, purpose "discover", items source=content·reason=title — `_neighbor_row` 가 그대로 만든다).
    - `cascade.py::_personal`: `compose_rows(..., after_completion=store.after_completion(r.user_key) if store else None)`; `_degrade`: `hit = self.cache.get(...)`; `if hit and store and store.after_completion(r.user_key) and not any(row.row_id == "after_completion" for row in hit[0]): hit = None`(완독 뒤 옛 캐시 미스 취급 — D-10 "완독 시 캐시 무효화" 의 최소 구현, api.py 무변경).
    - GREEN: `uv run pytest tests/serving tests/test_architecture.py --no-header` failed 0.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving tests/test_architecture.py --no-header --ignore=tests/serving/test_ratings.py --ignore=tests/serving/test_dashboard.py && wc -l src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/compose.py src/millie_rec/serving/cascade.py</automated>
  </verify>
  <acceptance_criteria>
    - failed 0(test_after_completion 9 포함) · 4파일 각 ≤150
    - `grep -c "self.neighbors.neighbors(" src/millie_rec/serving/nearline.py` == 1 · `grep -c "set_after_completion(" src/millie_rec/serving/nearline.py` == 1 · `grep -c "after_completion=" src/millie_rec/serving/cascade.py` ≥ 1
    - `grep -c '"content"' src/millie_rec/serving/compose.py` ≥ 1 · `grep -c "itemknn" src/millie_rec/serving/compose.py` == 0
  </acceptance_criteria>
  <done>9 passed + 회귀 0, 4파일 ≤150. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR·검증): ruff · make smoke · SUMMARY</name>
  <files>src/millie_rec/serving/compose.py, src/millie_rec/serving/cascade.py</files>
  <read_first>Task 2 결과</read_first>
  <action>`uv run ruff format <5파일> && uv run ruff check <5파일>` 클린(다른 wave 4 파일 포맷 금지) · SUMMARY 에 RED/GREEN 요약·`TDD Gate Compliance`·"PDF 문장 후보 1줄(수용 기준 '완독 → 완독하셨네요 row' 충족)"·변경 파일·"커밋하지 않는다".</action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/compose.py src/millie_rec/serving/cascade.py tests/serving/test_after_completion.py && uv run ruff check src/millie_rec/serving/state.py src/millie_rec/serving/nearline.py src/millie_rec/serving/compose.py src/millie_rec/serving/cascade.py tests/serving/test_after_completion.py && uv run pytest tests/serving tests/test_architecture.py --no-header --ignore=tests/serving/test_ratings.py --ignore=tests/serving/test_dashboard.py</automated>
  </verify>
  <acceptance_criteria>ruff 클린 · failed 0 · `git status --short` 이 플랜 5파일 외 변경 없음</acceptance_criteria>
  <done>ruff 클린 · SUMMARY. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| `completion` 이벤트(클라이언트) → Nearline 이웃 사전 계산 | book_id 는 05-04 게이트가 eligible 검사(플래그) — 자격 없는 책도 저장되므로 compose 가 다시 `eligible` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-10-01 | Denial of Service | completion 폭주로 이웃 조회 비용 | accept | `neighbors()` 는 메모리 dict 조회(CatalogKR), 20개. Nearline 스레드에서 처리 |
| T-05-10-02 | Tampering | 자격 없는 seed 로 행 생성 | mitigate | `_after` 가 `catalog.eligible` 로 이웃 필터, seed 제목은 meta 있을 때만 |
| T-05-10-03 | Information Disclosure | 옛 캐시(완독 전 rows) 노출 | mitigate | `_degrade` 캐시 미스 취급 1줄(테스트 `…stale_cache_not_served…`) |
</threat_model>

<verification>
- `uv run pytest tests/serving tests/test_architecture.py --no-header --ignore=tests/serving/test_ratings.py --ignore=tests/serving/test_dashboard.py` failed 0 · ruff 클린 · 4파일 ≤150 · 커밋 0(smoke 는 wave 4 종료 후 Advisor)
</verification>

<success_criteria>
- **SERV-11**: completion → Nearline 사전 계산 → 다음 응답 최상단 `after_completion`(source=content) 이 HTTP 테스트로 단정, 기존 테스트 무수정
</success_criteria>

<output>`.planning/phases/05-must/05-10-SUMMARY.md` — RED/GREEN·구현 요지·PDF 문장 후보·변경 파일·"커밋하지 않는다".</output>
