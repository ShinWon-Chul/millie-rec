---
phase: 08-pdf
plan: 05
type: execute
wave: 5
depends_on: [08-04]
files_modified: [.planning/phases/07-deploy/07-04-SUMMARY.md, ../PROGRESS.md, ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md, .planning/STATE.md, .planning/ROADMAP.md]
autonomous: false
requirements: [PDF-03]
must_haves:
  truths:
    - "07-04-SUMMARY 운영 인계 절에 Sentry 확인 결과와 UptimeRobot 키워드 처리가 각 1줄 추가되어 있다"
    - "PROGRESS.md 결정 로그에 Phase 8 원고 확정 1행(장 수 · 감축 · 숫자 정정)이 있고 미결에 Sentry/C1 인계가 반영되어 있다"
    - "개발일지에 D 항목(6요소)이 실행 시점 최댓값+1 번호로 추가되어 있고 다른 항목과 번호가 겹치지 않는다"
    - "STATE.md Day 5 게이트가 ✅이고 Phase 8 상태가 기록되어 있다"
    - "사용자 승인 후 report/draft.md · draft.pre-humanize.md · notion_guide.md · demo_guide.md · .planning/** 만 명시 경로로 커밋되었고 다른 세션 미커밋 파일은 섞이지 않았다"
  artifacts:
    - path: ".planning/phases/07-deploy/07-04-SUMMARY.md"
      provides: "운영 인계 절 Sentry·키워드 결과 1줄씩"
      contains: "Sentry"
    - path: "../PROGRESS.md"
      provides: "결정 로그 1행 · 미결 갱신"
    - path: ".planning/STATE.md"
      provides: "Day 5 게이트 ✅ · Phase 8 완료 상태"
      contains: "Day 5"
  key_links:
    - from: "git commit"
      to: "report/draft.md report/draft.pre-humanize.md report/notion_guide.md report/demo_guide.md .planning/phases/08-pdf .planning/STATE.md .planning/ROADMAP.md"
      via: "명시 경로 git add(-A 금지)"
      pattern: "docs\\(08\\)"
---

<objective>
Phase 8 결과를 4곳에 기록하고(07-04-SUMMARY 운영 인계 · PROGRESS · 개발일지 · STATE/ROADMAP), 사용자 승인 후 원고·가이드·플래닝 파일만 명시 경로로 커밋한다. push는 별도 승인.

Purpose: Phase 8 'PDF 제출물'(`.planning/ROADMAP.md` "### Phase 8: PDF 제출물") 마감 기록 — 결정 D-17 결과 기록 위치 · D-18 마지막 태스크(`.planning/phases/08-pdf/08-CONTEXT.md`), 그리고 프로젝트 규칙 "커밋은 사용자 승인 후, `git add -A` 금지"(다른 세션 리뷰 데이터 파일 미커밋 상태).
Output: 기록 4곳 갱신, 커밋 1개(`docs(08): PDF 원고 확정 — …`), `.planning/phases/08-pdf/08-05-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pdf/08-CONTEXT.md
@.planning/phases/08-pdf/08-03-SUMMARY.md
@.planning/phases/08-pdf/08-04-SUMMARY.md
@.planning/phases/07-deploy/07-04-SUMMARY.md
@../PROGRESS.md
@../.claude/rules/references.md
@../.claude/docs/docs-policy.md

<interfaces>
<!-- 기록 형식 정본 -->

개발일지 D 항목 = 6요소 형식(`../.claude/docs/docs-policy.md` · 메모리 '개발일지 작성 지침': 문제 제기 → 과정 → 선택 → 트레이드오프, '무엇을·왜' 중심, AI 티 없는 문체). 제목 형식은 기존 `### D85.` 항목을 그대로 따른다(`grep -n '^### D85' ../.assets/개발일지/*.md`로 확인). 번호는 **실행 시점**에:
```
N=$(( $(grep -ho '^### D[0-9]*' ../.assets/개발일지/*.md | sed 's/### D//' | sort -n | tail -1) + 1 ))
```
(현재 최댓값 D88 — 다른 세션이 선점할 수 있으므로 하드코딩 금지.) 파일은 `ls ../.assets/개발일지/2026-09-06_*.md`로 확인한 Day 4 파일에 append, 없으면 `2026-09-06_Day5_PDF_제출물.md` 신설(제목 형식은 기존 파일 첫 줄 따름).

참조 표기(`../.claude/rules/references.md`): 약호 단독 금지 — `결정 D-16 '분량 검증 = Advisor 계측 + 사용자 Notion 확인'(.planning/phases/08-pdf/08-CONTEXT.md)` 형식. PROGRESS 결정 로그 행도 같은 형식.

git: 다른 세션 미커밋(`scripts/*_millie_reviews*.py` 5개 · `results/review_coverage.csv` · `results/review_tuples_report.json` · `artifacts/serving/review_agg_kr.json` · `report/plan_surfaces_search_feed.md` · `results/millie_edges_gate.json` 수정 · `tests/data/test_millie_review*.py` · `tests/fixtures/millie_reviews/`)은 **절대 포함하지 않는다**. `report/draft.html`(pandoc 산출)은 `git check-ignore`로 무시 확인, 아니면 add 하지 않는다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 기록 4곳 — 07-04-SUMMARY 운영 인계 · PROGRESS 결정 로그/미결 · 개발일지 D 항목 · STATE/ROADMAP</name>
  <files>.planning/phases/07-deploy/07-04-SUMMARY.md, ../PROGRESS.md, ../.assets/개발일지/2026-09-06_*.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/08-pdf/08-04-SUMMARY.md (회신 4건 · Sentry 결과 · 최종 장 수 — 기록할 사실의 출처)
    - .planning/phases/08-pdf/08-03-SUMMARY.md (장별 자수 · 숫자 정정 1건 · `/pdf-check` 판정)
    - .planning/phases/07-deploy/07-04-SUMMARY.md ("## 운영 인계" 절 — 여기에 append · "Codex 인계(C1·C5)" 줄)
    - ../PROGRESS.md (결정 로그 표 형식 · 미결 목록 — Edit 전 반드시 다시 읽는다, 다른 세션 동시 편집)
    - ../.assets/개발일지/ 최신 파일(D85 항목 형식 확인) · ../.claude/docs/docs-policy.md (개발일지 규칙)
    - .planning/STATE.md (Day 게이트 표 · Current Position — Edit 전 다시 읽는다)
    - ../.claude/rules/references.md (참조 표기)
  </read_first>
  <action>
**1-a. `07-04-SUMMARY.md` 운영 인계 절.** `## 운영 인계` 절 끝(`- **Codex 인계(C1·C5):** …` 줄 다음)에 2줄 append:
```
- **Sentry 확인(Phase 8 'PDF 제출물' 08-04, 결정 D-17 'Sentry 정상 동작 확인'(.planning/phases/08-pdf/08-CONTEXT.md)):** <08-04-SUMMARY의 결과 1줄 — "사용자 대시보드에서 SDK 연결·이벤트 확인(시각)" 또는 "로컬 probe 1회 수신 확인(시각)" 또는 "미확인 — 사유">. DSN은 기록하지 않는다.
- **UptimeRobot 키워드(Codex C2 권고):** <"`"db_ok":true`로 변경(시각)" 또는 "`"status":"ok"` 유지 — 사용자 결정">. `FREEZE_BOOK_STATS`: <있음/추가함/미설정>.
```
C5 줄에 "→ Phase 8 08-01에서 draft §4-3에 'variant 공통 전역 p95' 반영 완료" 를 덧붙인다.

**1-b. `../PROGRESS.md`.** 파일을 다시 읽고 결정 로그 표에 1행 추가(형식은 기존 행 그대로):
```
| 2026-09-06 | PDF 원고 확정 — `report/draft.md` 5장 재구성(★ callout 3 · 캡처 3×2 · 5권 표) · 1~4장 압축(장별 한글 <08-03 표 값>) · 숫자 정정 1건(잔차 분야 평균 0.502~0.505 → 0.488~0.505, `results/millie_difficulty.json` 전체 8분야) · Notion PDF <N>장(사용자 회신) · `/pdf-check` 제출 가능 | Phase 8 'PDF 제출물'(.planning/phases/08-pdf/08-01~05-SUMMARY.md) |
```
미결 목록: Sentry가 "미확인"이면 `- [ ] Sentry 이벤트 수신 미확인 — <사유>(08-04-SUMMARY)` 1행 추가; 확인됐으면 추가하지 않는다. Codex C1(익명 `candidate_sets`) 미결은 그대로 둔다(Phase 8 이후 인계). `report/draft.pre-humanize.md`·`report/notion_guide.md` 존재를 "산출물" 줄이 있으면 거기에, 없으면 결정 로그 행 안에 언급.

**1-c. 개발일지 D 항목(6요소).** 번호 `N`을 위 `<interfaces>` 명령으로 잡고, 대상 파일 끝에 append. 제목·구조는 기존 D85 항목과 동일한 6요소 형식. 내용 골자(문장은 executor가 6요소에 맞춰 쓰되 AI 티 없는 한다체, 대구·대시 남용 금지):
- 문제 제기: 16,197자 원고를 IT 면접관이 5장에서 읽어야 했고, 개발 과정 표기(Phase·Day·freeze)가 소음이었다.
- 과정: 08-CONTEXT 결정 18개 → 1~4장 압축(해석 5문장·각주 2줄·구현 1문장) → 5장 그림 장 재구성 → 계측(장별 한글 자수) → humanize → `/pdf-check` → 사용자 Notion 회신 N장.
- 선택: 비교표 8행 유지 대신 해석을 5문장으로 · ★3개는 callout · 5권은 표 · 개발 과정 표기 0, 출처는 각주로만.
- 트레이드오프: 판본 중복·variant 교집합 같은 실측 서술을 버려 정보량은 줄었지만 읽기 흐름을 얻었다. 잔차 분야 평균은 상위 4분야(0.502~0.505) 대신 전체 8분야(0.488~0.505)로 적어 설득력보다 정확성을 택했다.
- 결과·숫자: 장별 한글 자수 5개(08-03 표) · Notion PDF N장 · `/pdf-check` 제출 가능 · Sentry 결과 1줄.
- 참조: `결정 D-01~D-18(.planning/phases/08-pdf/08-CONTEXT.md)` · `report/draft.md` · `report/notion_guide.md`. 태그 `[축 ①②③④ · main §2 페이지 매핑 · §9 기억할 5가지]`.
DSN·과제 PDF 문구·`.assets` 캡처 내용은 쓰지 않는다.

**1-d. `.planning/STATE.md` · `.planning/ROADMAP.md`.** STATE를 다시 읽고 Day 게이트 표 Day 5 행을 `✅`로(`/pdf-check` 통과 · N장 · 제출 대기 09-08 22:00 KST), frontmatter `status`·`stopped_at`·Current Position을 "Phase 8 'PDF 제출물' 08-05 기록·커밋 완료 — 다음 `/gsd-verify-work 8`"로. ROADMAP Progress 표 Phase 8 행 `Plans Complete` 5/5 · Status Complete · Completed 2026-09-06(또는 실제 날짜), Phase 7 행도 실제(4/4 Complete 2026-09-06)로 정정(현재 0/TBD로 남아 있음). `**Plans:** TBD` → 5 plans + 플랜 목록(08-01~05 한 줄 목적).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && grep -q 'Sentry 확인(Phase 8' .planning/phases/07-deploy/07-04-SUMMARY.md && grep -q 'UptimeRobot 키워드(Codex C2 권고)' .planning/phases/07-deploy/07-04-SUMMARY.md && grep -q 'PDF 원고 확정' ../PROGRESS.md && test "$(grep -ho '^### D[0-9]*' ../.assets/개발일지/*.md | sed 's/### D//' | sort -n | uniq -d | wc -l | tr -d ' ')" = 0 && grep -rq 'PDF 제출물' ../.assets/개발일지/2026-09-06_*.md && grep -q 'Day 5' .planning/STATE.md && test -z "$(grep -rl 'ingest.sentry.io' .planning ../PROGRESS.md ../.assets/개발일지 2>/dev/null)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sentry 확인(Phase 8' .planning/phases/07-deploy/07-04-SUMMARY.md` = 1 · `grep -c 'UptimeRobot 키워드(Codex C2 권고)'` = 1 · `grep -c "variant 공통 전역 p95' 반영 완료"` = 1
    - `grep -c 'PDF 원고 확정' ../PROGRESS.md` = 1 · 행에 `0.488~0.505` · `/pdf-check` · `장(사용자 회신)` 포함
    - 개발일지: `grep -ho '^### D[0-9]*' ../.assets/개발일지/*.md | sed 's/### D//' | sort -n | uniq -d` 빈 출력(번호 중복 0) · 새 항목이 최댓값이고 본문에 `PDF 제출물` · `0.488~0.505` · `.planning/phases/08-pdf/08-CONTEXT.md` 포함 · `grep -c 'ingest.sentry.io'` = 0
    - STATE: Day 게이트 표 Day 5 행에 `✅` · `grep -c "gsd-verify-work 8" .planning/STATE.md` ≥ 1
    - ROADMAP: `grep -c '^| 8\. PDF 제출물 | 4~5 | 5/5 | Complete' .planning/ROADMAP.md` = 1 · `grep -c '^| 7\. 배포 | 2 · 4 | 4/4 | Complete' .planning/ROADMAP.md` = 1 · Phase 8 절 `**Plans:** TBD` 0건
    - 참조 표기: 새로 쓴 줄에 `D-1[0-9]`·`D-0[0-9]`가 나오면 같은 줄에 `08-CONTEXT.md` 경로가 있다: `grep -nE 'D-[01][0-9]' ../PROGRESS.md .planning/phases/07-deploy/07-04-SUMMARY.md | grep -vc 'CONTEXT.md'` = 0(이 태스크가 추가한 줄 기준)
    - `grep -rl 'ingest.sentry.io' .planning ../PROGRESS.md ../.assets/개발일지` 빈 출력
  </acceptance_criteria>
  <done>4곳 기록이 grep으로 확인되고 개발일지 번호 중복 0, DSN 비노출.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 커밋 승인</name>
  <files>.planning/phases/08-pdf/08-05-SUMMARY.md</files>
  <read_first>
    - `git status --short` 현재 출력 · `git diff --stat HEAD -- report/draft.md report/notion_guide.md .planning` (사용자에게 보여줄 것)
    - .planning/phases/08-pdf/08-04-SUMMARY.md (최종 장 수 — 커밋 메시지 본문에 인용)
  </read_first>
  <action>
    Task 1 기록 4곳이 끝난 뒤, 아래 `<how-to-verify>`의 명령 3개를 **Advisor가 먼저 실행해 출력을 첨부**하고 사용자에게 한글로 승인을 요청한다. 커밋 대상 목록과 제외 목록(다른 세션 미커밋 파일)을 표로 보여준다. 승인 문구를 받기 전에는 `git add`·`git commit`을 실행하지 않는다.
  </action>
  <what-built>Phase 8 산출물 전부 — `report/draft.md`(최종 원고, Notion PDF N장 확정) · `report/draft.pre-humanize.md`(백업) · `report/notion_guide.md`(조판 가이드) · `report/demo_guide.md`(§6 완독 후 추천 보는 법, 결정 D-19) · `.planning/phases/08-pdf/08-01~05-PLAN/SUMMARY.md` · `.planning/STATE.md` · `.planning/ROADMAP.md` · `.planning/phases/07-deploy/07-04-SUMMARY.md`(운영 인계 2줄). 상위 폴더 `../PROGRESS.md`·개발일지는 repo 밖이라 커밋 대상이 아니다.</what-built>
  <how-to-verify>
아래 명령 출력을 보고 커밋을 승인해 주세요(push는 하지 않습니다 — push = Railway 재배포이므로 별도 승인).

1. 커밋에 들어갈 파일만 확인:
```
cd millie-rec && git status --short -- report/draft.md report/draft.pre-humanize.md report/notion_guide.md report/demo_guide.md .planning
```
2. 다른 세션 미커밋 파일이 **빠져 있는지** 확인(이 목록은 커밋에 넣지 않습니다):
```
git status --short | grep -vE '^(.M|M.|\?\?) (report/draft\.md|report/draft\.pre-humanize\.md|report/notion_guide\.md|report/demo_guide\.md|\.planning/)'
```
3. 원고 diff 요약: `git diff --stat HEAD -- report/draft.md`

커밋 메시지(안): `docs(08): PDF 원고 확정 — 5장 재구성·1~4장 압축·Notion 가이드 (Phase 8 'PDF 제출물')`
  </how-to-verify>
  <resume-signal>"커밋 승인" 또는 수정 요청 한 줄. push까지 원하면 "커밋·push 승인"이라고 적어 주세요.</resume-signal>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && test -z "$(git diff --cached --name-only)" && echo PASS_NOT_STAGED_BEFORE_APPROVAL</automated>
  </verify>
  <acceptance_criteria>
    - 승인 회신 전 `git diff --cached --name-only` 빈 출력(스테이징 없음)
    - 사용자에게 보인 커밋 대상 목록이 `report/draft.md` · `report/draft.pre-humanize.md` · `report/notion_guide.md` · `report/demo_guide.md` · `.planning/**`만이고 제외 목록에 다른 세션 파일이 나열되어 있다(SUMMARY에 표로)
    - 사용자 회신 원문("커밋 승인" / "커밋·push 승인" / 수정 요청)이 SUMMARY에 시각과 함께 기록
  </acceptance_criteria>
  <done>사용자 승인(또는 수정 요청) 수신. 승인 전 커밋 0.</done>
</task>

<task type="auto">
  <name>Task 3: 명시 경로 커밋 (승인 후) · 결과 기록</name>
  <files>.planning/phases/08-pdf/08-05-SUMMARY.md</files>
  <read_first>
    - Task 2 사용자 회신
    - `git status --short` 현재 출력 (다른 세션 미커밋 파일 목록 재확인)
  </read_first>
  <action>
승인 회신을 받은 뒤에만 실행한다. `git add -A`·`git add .`·`git commit -a`는 **금지**(다른 세션의 밀리 리뷰 데이터 작업이 섞인다). 경로를 명시한다:
```
cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec
git check-ignore -q report/draft.html || echo "draft.html 추적 위험 — add 하지 않음"
git add report/draft.md report/draft.pre-humanize.md report/notion_guide.md report/demo_guide.md .planning/phases/08-pdf .planning/STATE.md .planning/ROADMAP.md .planning/phases/07-deploy/07-04-SUMMARY.md
git status --short --cached
```
`--cached` 출력에 위 경로 외 파일(특히 `scripts/`·`results/`·`artifacts/`·`tests/`·`report/plan_surfaces_search_feed.md`)이 있으면 `git reset -q -- <그 파일>` 후 재확인. 그 다음:
```
git commit -m "docs(08): PDF 원고 확정 — 5장 재구성·1~4장 압축·Notion 가이드 (Phase 8 'PDF 제출물')" -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
git log --oneline -1 && git show --stat --oneline HEAD | tail -n +2
```
회신이 "커밋·push 승인"이면 `git push` 실행 후 `/health` 200을 5초 간격 최대 12회 폴링해 Active 복귀 시각을 기록한다(재배포 약 5~15초 다운, `.planning/STATE.md` Downtime 참조). "커밋 승인"만이면 push 하지 않고 SUMMARY에 "push 미실행(승인 없음)".

SUMMARY에 커밋 sha · 포함 파일 목록 · 제외 확인(`git status --short`에 남은 다른 세션 파일 수) · push 여부를 적는다. 마지막에 `## ▶ Next Up` 블록(`/gsd-verify-work 8 — Phase 8 'PDF 제출물'(.planning/ROADMAP.md)` · 대안 `/gsd-audit-milestone` · `/day-end`)으로 끝낸다.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && git log --oneline -1 | grep -q 'docs(08): PDF 원고 확정' && test -z "$(git show --name-only --format= HEAD | grep -vE '^(report/draft\.md|report/draft\.pre-humanize\.md|report/notion_guide\.md|report/demo_guide\.md|\.planning/)')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1`에 `docs(08): PDF 원고 확정` 포함
    - `git show --name-only --format= HEAD`의 모든 경로가 `report/draft.md` · `report/draft.pre-humanize.md` · `report/notion_guide.md` · `report/demo_guide.md` · `.planning/**` 중 하나(다른 경로 0)
    - `git status --short`에 다른 세션 파일(`scripts/*_millie_reviews*.py` · `results/review_*` · `artifacts/serving/review_agg_kr.json` · `report/plan_surfaces_search_feed.md` · `results/millie_edges_gate.json` · `tests/data/test_millie_review*` · `tests/fixtures/millie_reviews/`)이 **그대로 남아 있다**(커밋에 안 섞임)
    - `git ls-files | grep -iE "pdf|png|assets" | grep -v '^demo/assets/brand/millie-mark.png$'` 빈 출력(저작권 게이트 유지)
    - push 했으면 SUMMARY에 push 시각과 `/health` 200 복귀 시각; 안 했으면 "push 미실행(승인 없음)"
    - SUMMARY 끝에 `## ▶ Next Up` 블록, 첫 제안 `/gsd-verify-work 8`(이름·경로 병기)
  </acceptance_criteria>
  <done>승인된 파일만 커밋되고 다른 세션 미커밋 파일은 작업 트리에 그대로, SUMMARY에 sha·파일 목록·push 여부·Next Up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 작업 트리 → git(public repo) | 커밋 = 공개. 다른 세션의 미커밋 리뷰 데이터(밀리 리뷰 파생물)가 섞이면 저작권·미실측 데이터가 공개된다 |
| 기록 파일(PROGRESS·개발일지·SUMMARY) → 미래 세션·사용자 | 비밀(DSN)이 여기에 남으면 영구 노출 |
| git push → Railway 자동 배포 | push는 배포 이벤트 — 사용자 별도 승인 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | I (정보 노출) | `git add -A`로 다른 세션 미커밋 파일 커밋 | mitigate | Task 3 명시 경로 add + `--cached` 검사 + acceptance `git show --name-only` 경로 화이트리스트 |
| T-08-04 | I | DSN이 PROGRESS·개발일지·SUMMARY에 기록 | mitigate | Task 1 "DSN은 기록하지 않는다" + acceptance `grep -rl 'ingest.sentry.io'` 빈 출력 |
| T-08-03 | I | 캡처·과제 PDF·`.assets`가 ls-files에 | mitigate | Task 3 acceptance `git ls-files | grep -iE "pdf|png|assets"` 예외 1건 외 빈 출력 |
| T-08-11 | E (권한 상승) | 승인 없는 push(= 배포) | mitigate | Task 2 체크포인트 회신 문구로 커밋/push 분리, Task 3 "커밋 승인"만이면 push 금지 |
| T-08-12 | R (부인 방지) | 개발일지 번호 충돌(다른 세션 선점) | mitigate | Task 1 실행 시점 최댓값+1, acceptance `uniq -d` 빈 출력 |
</threat_model>

<verification>
- 기록 4곳 grep(Task 1 acceptance) 전부 통과
- 커밋 1개, 포함 경로 화이트리스트만, 다른 세션 파일 작업 트리 잔존
- DSN grep 빈 출력 · 저작권 ls-files 게이트 빈 출력
- SUMMARY `## ▶ Next Up` → `/gsd-verify-work 8`
</verification>

<success_criteria>
- Phase 8 'PDF 제출물'(`.planning/ROADMAP.md`) 5플랜 전부 SUMMARY가 있고, 결정 D-17 결과 기록 위치(07-04-SUMMARY 운영 인계 또는 PROGRESS 미결)와 D-18 마지막 태스크(`.planning/phases/08-pdf/08-CONTEXT.md`)가 이행됐다.
- STATE Day 5 게이트 ✅, ROADMAP Phase 7·8 Complete, 사용자 승인 커밋 1개.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pdf/08-05-SUMMARY.md` — 기록 4곳 grep 결과 · 개발일지 번호 · 커밋 sha·파일 목록 · push 여부 · `## ▶ Next Up`.
</output>

## 결정 ↔ 태스크 매핑 (Phase 8 전체, `.planning/phases/08-pdf/08-CONTEXT.md`)

| 결정 | 내용 한 줄 | 구현 태스크 |
|---|---|---|
| D-01 ~ D-15 | 원고 압축·재구성·문체 | 08-01 · 08-02 · 08-03 |
| D-16 | 계측 + 사용자 Notion 장 수 | 08-03 Task 1·3 · 08-04 Task 2·3 |
| D-17 | Sentry 확인 → **결과 기록(07-04-SUMMARY 운영 인계 또는 PROGRESS 미결)** | 08-04 Task 2·3 · **08-05 Task 1** |
| D-18 | 제출 전 점검 → **기록·커밋(마지막 태스크)** | 08-04 Task 1·2 · **08-05 Task 1·2·3** |
| D-19 | 완독 후 추천 유저 플로우(draft 5장 + demo_guide §6) → **demo_guide.md 커밋 포함** | 08-02 Task 1 · **08-05 Task 3** |
