---
phase: 08-pdf
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [report/draft.md, report/demo_guide.md]
autonomous: true
requirements: [PDF-02]
must_haves:
  truths:
    - "5장 상단에 데모 URL(/#/ 포함)·QR 자리 표시·상태 페이지 URL 박스가 있다"
    - "5장에 사용자 시나리오 3문장(5권 선택 → 완독·1탭 별점 → SF 재설정)이 있고 캡처 6장 자리 표시의 캡션이 그 6단계와 1:1이다"
    - "★고유 설계 3개(앵커·난이도·시간 가변 가중치)가 각각 blockquote 박스로 존재하고 실측 숫자 또는 그림·표 참조를 갖는다"
    - "본인 5권 표(5행×3열)가 있고 이웃이 콘텐츠 유사도라는 1줄이 붙어 있으며 'Item-KNN 이웃 그대로' 과대 표기가 없다"
    - "5장 §5-1·§5-2 문단 벽(1,368자·1,686자)이 사라지고 5장 한글 자수가 08-01 종료 시점보다 줄었다"
    - "5장 시나리오 아래에 완독 후 추천 행까지의 클릭 경로 1줄이 데모 화면 라벨 그대로 있고, 캡션 ④·⑥에 그 경로의 단계 번호가 붙어 있으며, report/demo_guide.md에 '완독 후 추천 보는 법' 절이 있다"
  artifacts:
    - path: "report/draft.md"
      provides: "5장 그림 장 완성(URL·QR → 시나리오 → 클릭 경로 1줄 → 캡처 3×2 → ★3 + 4층 표 → 5권 표 → 로드맵 → 기억할 5가지)"
      contains: "> ★ **앵커"
    - path: "report/demo_guide.md"
      provides: "완독 후 추천 보는 법 1절(결정 D-19)"
      contains: "완독 후 추천 보는 법"
  key_links:
    - from: "report/draft.md §5-3 5권 표"
      to: "report/demo_5books.md"
      via: "시드 5권·대표 이웃 2권 동일 문자열(유사도 숫자 미기재)"
      pattern: "혼자일 수 없다면 나아갈 수 없다"
    - from: "report/draft.md ★난이도 박스"
      to: "results/millie_difficulty.json"
      via: "분야 평균 범위 0.488~0.505 · 완독 확률 40.3%~79.0% · 7,796권"
      pattern: "0\\.488~0\\.505"
    - from: "report/draft.md ★시간 가변 가중치 박스"
      to: "report/draft.md §2-2 · §4-1"
      via: "수식·상태표는 §2-2 참조, 실측은 §4-1 hybrid n≥20 0.087→0.240"
      pattern: "0\\.087→0\\.240"
---

<objective>
`report/draft.md` 5장을 **그림 장**으로 재구성한다: 데모 URL·QR·상태 페이지 박스 → 시나리오 3문장 → 캡처 3×2 자리 표시 + 캡션 6 → ★박스 3 + 난이도 4층 표 → 본인 5권 표 → 로드맵 1줄 → 기억할 5가지. 기존 §5-1·§5-2 문단 벽은 박스에 남길 문장만 빼고 삭제한다.

Purpose: Phase 8 'PDF 제출물'(`.planning/ROADMAP.md` "### Phase 8: PDF 제출물") 성공 기준 2 — ★3개 박스 · 그림 4종 중 데모 캡처 · 필수 문장(PDF-02), 그리고 과제 선택 항목 ⑥ 사용자 시나리오·⑧ 데모 보강.
Output: `report/draft.md` 5장 최종 형태, `.planning/phases/08-pdf/08-02-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pdf/08-CONTEXT.md
@.planning/phases/08-pdf/08-01-SUMMARY.md
@report/draft.md
@report/demo_5books.md
@../.claude/rules/report.md
@results/millie_difficulty.json
@results/millie_coverage.csv

<interfaces>
<!-- 5장이 인용하는 숫자 정본의 현재 값. 원고 숫자는 이 값과 글자 단위로 같아야 한다. -->

results/millie_difficulty.json (2026-09-06): n_books 9447 · n_difficulty **7796** · coverage 0.8252(→ 82.5%) · by_category 8개 분야의 difficulty_mean 최소 **0.4881**(어린이) ~ 최대 **0.5047**(에세이/시) → 원고 "0.488~0.505" · completion_prob_mean 최소 **40.30**(오디오북) ~ 최대 **79.01**(어린이) → 원고 "40.3%" · "79.0%".
  ※ 현재 draft L237의 "0.502~0.505"·"오디오북 40.3%와 경제경영 54.8%처럼 14%p"는 상위 4개 분야만 고른 표기로 정본과 어긋난다 → 이 플랜에서 전체 범위로 **정정**한다(PDF-01 "숫자는 results/에서만").

report/demo_5books.md (서빙 경로 실측, 카탈로그 9,447권, git 844fb74) — 시드 5권과 앵커 행 대표 이웃 2권(자기 판본 제외):
| 시드 | 이웃 1 | 이웃 2 |
| 『싯다르타』 | 변신 이야기 1 | 햄릿 |
| 『데미안』 | 싯다르타(최유경 역) | 변신 이야기 1 |
| 『위버멘쉬』 | 혼자일 수 없다면 나아갈 수 없다 | 차라투스트라는 이렇게 말했다 |
| 『쇼펜하우어 인생수업: 한 번뿐인 삶 이렇게 살아라』 | 쇼펜하우어의 인생 수업 | 마흔에 읽는 쇼펜하우어 |
| 『빅터 프랭클의 죽음의 수용소에서』 | 그럼에도 삶에 '예'라고 답할 때 | 청소년을 위한 빅터 프랭클의 죽음의 수용소에서 |
  ※ 현재 draft L240의 『동물농장』은 demo_5books.md 싯다르타 행에 없다 → 삭제 대상.

캡처 파일 ↔ 캡션(D-11, 파일은 `report/figures/`, repo 밖): p5_01_onboarding.png ①취향 설정(5권) · p5_02_home.png ②메인(앵커 행) · p5_03_detail.png ③책 상세 · p5_04_reader_done.png ④뷰어 완독·별점 · p5_05_refresh.png ⑤취향 재설정(SF) · p5_06_home_after.png ⑥메인 변화. QR = p5_qr.png.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5장 §5-1 데모 절 — URL·QR·상태 페이지 박스 · 시나리오 3문장 · 완독 후 추천 클릭 경로 1줄 · 캡처 3×2 자리 표시 + 캡션 6 · demo_guide 1절</name>
  <files>report/draft.md, report/demo_guide.md</files>
  <read_first>
    - report/draft.md (`# 5.`부터 끝까지 — 08-01이 1~4장을 바꿨으므로 줄 번호는 쓰지 말고 문장 문자열로 찾는다)
    - .planning/phases/08-pdf/08-CONTEXT.md (D-06 · D-07 · D-11 · D-12 · **D-19 완독 후 추천 유저 플로우** · Specifics "5장은 그림 장" — action에 문장을 풀어 썼으므로 대조용)
    - report/demo_guide.md (현재 §1~§5, 186줄 — §5 "나올 만한 질문" 뒤에 §6을 append한다)
    - ../.claude/rules/report.md (스크린샷 6장 · 한 문단 3문장 · 표지 단독 이미지 없음)
    - .planning/phases/07-deploy/07-04-SUMMARY.md ("Task 3 — 캡처 6장 + QR" 절: 캡처 파일명·촬영 조건 확인용)
  </read_first>
  <action>
`report/draft.md`의 `# 5. 도서 도메인 고유 설계 · 데모 · 로드맵`부터 파일 끝까지를 다음 골격으로 **재작성**한다. 이 태스크는 §5-1(데모 절)을 만들고 기존 §5-3 데모 문단 3개(`본인이 실제로 읽은 다섯 권을 …` · `같은 응답의 '셜록 홈즈의 서가'가 …` · `데모: https://millie-rec-production.up.railway.app (QR 이미지 …) … repo 에 넣지 않는다.`)를 삭제한다. 기존 §5-1 앵커 문단 · §5-2 난이도 문단 · 로드맵 · 기억할 5가지는 **Task 2가 다루므로 이 태스크에서는 위치만 아래로 밀고 내용은 건드리지 않는다**.

**1-a. H1 유지, §5-1 신설.** H1 `# 5. 도서 도메인 고유 설계 · 데모 · 로드맵`은 그대로. 바로 아래에 다음 H2와 내용을 넣는다(결정 D-07 '데모 URL·QR·상태 페이지 박스를 5장 데모 절 상단에', D-11 '시나리오 3문장 + 캡션 1:1', D-12 '완독 직후 행 + 1탭 별점 보충', D-06 '캡처 3×2 그리드'):

```
## 5-1. 데모 — 눌러볼 수 있는 설계 [②③]

| 데모 | QR |
|---|---|
| **https://millie-rec-production.up.railway.app/#/** — 쇼케이스 화면에서 "신규 유저로 체험하기"로 시작한다 | (QR — `report/figures/p5_qr.png`, Notion 삽입 · 폭 1/6) |

심사 기간 가동 감시(5분 간격 헬스체크): https://stats.uptimerobot.com/20M6QwPo7z

자기계발 독자가 취향 설정에서 5권을 고르면 메인 첫 행이 "『○○』을 좋아하셨다면" 앵커 행이 된다. 한 권을 완독하고 별점을 한 번 누르면 메인 최상단에 "완독하셨네요, 다음은" 행이 생긴다 — 별점은 만족 라벨 수집이고 모델 입력으로 쓰는 것은 설계만이다. 취향을 SF로 재설정하면 기존 기록은 남고 앵커·페르소나 행만 즉시 바뀐다.

완독 후 추천 보기: ① 메인 책 카드 → ② 책 상세 「바로 읽기」 → ③ 뷰어 「완독」 → ④ 별점 ★ 1탭(「나중에」도 가능) → ⑤ 메인 최상단 "『○○』을 완독하셨네요, 다음은" 행.

(그림 6 · 데모 캡처 3×2 — Notion 3 컬럼 × 2행, 각 폭 1/3. 파일은 `report/figures/p5_01_onboarding.png` ~ `p5_06_home_after.png`)

| ① 취향 설정 — 5권 선택 (`p5_01_onboarding.png`) | ② 메인 — "『○○』을 좋아하셨다면" 앵커 행 · 경로 ① (`p5_02_home.png`) | ③ 책 상세 — 「바로 읽기」 · 경로 ② (`p5_03_detail.png`) |
|---|---|---|
| ④ 뷰어 — 「완독」 후 별점 1탭 · 경로 ③~④ (`p5_04_reader_done.png`) | ⑤ 취향 재설정 — SF로 (`p5_05_refresh.png`) | ⑥ 메인 변화 — "완독하셨네요, 다음은" 행 · 새 앵커 · 경로 ⑤ (`p5_06_home_after.png`) |

그림 6. 배포본에서 그대로 찍은 화면 6장(폰 프레임). 캡션 ①~⑥은 위 시나리오의 순서와 같고, ②③④⑥에 적은 경로 번호는 바로 위 클릭 경로의 단계다.
```
클릭 경로 1줄과 캡션의 화면 문구는 데모 라벨 그대로다(결정 D-19 '완독 후 추천 유저 플로우 명시', `.planning/phases/08-pdf/08-CONTEXT.md`): 「바로 읽기」 = `demo/js/screens/d3_detail.js` L62 · 「완독」·「10분 읽기」·「어떠셨나요?」·「나중에」 = `demo/js/screens/d4_reader.js` L72·L71·L29·L32 · 행 제목 = `src/millie_rec/serving/after_completion.py` L16 `TITLE_AFTER = "『{title}』을 완독하셨네요, 다음은"` · 완독·「나중에」 뒤 자동으로 `#/home` 복귀 = `demo/js/actions.js` `rate`(L170~184)·`rateLater`(L186~189). **코드·프리셋은 바꾸지 않는다**(Day 5 규칙).

**1-b. 기존 데모 문단 3개 삭제.** `본인이 실제로 읽은 다섯 권을 취향 설정에 넣으면 …『동물농장』이 남는다. …` · `같은 응답의 '셜록 홈즈의 서가'가 4단계 파이프라인을 통과한 유일한 행이다. … 79.5ms다(\`results/latency.json\`).` · `데모: https://millie-rec-production.up.railway.app (QR 이미지 … repo 에 넣지 않는다.` 세 문단을 삭제한다(『동물농장』은 `report/demo_5books.md`에 없는 과대 표기 · p95 79.5ms는 4장 §4-3에 이미 있음 · "repo" 언급은 결정 D-10 삭제 대상). 기존 H2 `## 5-3. 데모와 로드맵`은 삭제하고 `**로드맵(설계만).** …`·`**기억할 5가지.** …` 두 문단은 남겨 파일 끝에 둔다(Task 2가 H2를 붙인다).

**1-c. 기존 §5-1·§5-2 H2 유지(내용 무변경).** `## 5-1. 앵커 — "아는 책"으로 판단하게 한다`와 `## 5-2. 난이도 — 밀리 완독지수에서 파생 (밀리 보유 데이터 활용)`은 번호가 겹치지 않게 임시로 `## 5-2. 앵커 …` · `## 5-3. 난이도 …`로 번호만 올려 새 §5-1 아래로 둔다(문단 내용은 Task 2에서 박스로 압축).

**1-d. `report/demo_guide.md` 1절 추가(결정 D-19 ②).** 파일 끝(§5 "나올 만한 질문" 뒤)에 다음을 append한다(기존 절은 무변경):
```

## 6. 완독 후 추천 보는 법

프리셋 3개(신규 유저 A · 건너뛰기 · 재설정) 중 완독을 만드는 것은 없으므로 손으로 간다: 메인 책 카드 → 책 상세 「바로 읽기」 → 뷰어 「완독」 → 별점 ★ 1탭(「나중에」도 가능) → 메인 최상단 "『○○』을 완독하셨네요, 다음은" 행. 「10분 읽기」는 유효 독서(T=15분, 데모 상수) 기록용이고 행을 만드는 것은 「완독」이다. 완독 또는 「나중에」를 누르면 화면은 자동으로 메인(`#/home`)으로 돌아온다.
```

**1-e. 검사.** 5장 범위에서 `Phase [0-9]`·`Day [0-9]`·`repo`·`동물농장`이 0건인지(단, 기존 앵커·난이도 문단의 `Phase 4`·`Day 3`·`freeze`는 Task 2가 지우므로 이 태스크에서는 데모 절 범위만 확인), URL이 `/#/`로 끝나는지 확인한다.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && R="$(awk '/^# 5\./{f=1} f' report/draft.md)" && echo "$R" | grep -q '^## 5-1\. 데모 — 눌러볼 수 있는 설계 \[②③\]$' && echo "$R" | grep -q 'https://millie-rec-production.up.railway.app/#/' && echo "$R" | grep -q '심사 기간 가동 감시(5분 간격 헬스체크): https://stats.uptimerobot.com/20M6QwPo7z' && echo "$R" | grep -q '별점은 만족 라벨 수집이고 모델 입력으로 쓰는 것은 설계만이다' && echo "$R" | grep -q '^완독 후 추천 보기: ① 메인 책 카드' && grep -q '^## 6. 완독 후 추천 보는 법' report/demo_guide.md && test "$(echo "$R" | grep -c 'p5_0[1-6]_[a-z_]*\.png')" -ge 6 && echo "$R" | grep -q '^그림 6\. ' && test "$(echo "$R" | grep -cE '동물농장|repo|railway\.app \(QR')" = 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 5장 범위 `R=$(awk '/^# 5\./{f=1} f' report/draft.md)`에서 `echo "$R" | grep -c '^## 5-1\. 데모 — 눌러볼 수 있는 설계 \[②③\]$'` = 1
    - `echo "$R" | grep -c 'https://millie-rec-production.up.railway.app/#/'` ≥ 1 · `grep -c 'https://millie-rec-production.up.railway.app[^/#]'` = 0(해시 없는 URL 잔존 금지) · `grep -c 'p5_qr.png'` = 1
    - `echo "$R" | grep -c '심사 기간 가동 감시(5분 간격 헬스체크): https://stats.uptimerobot.com/20M6QwPo7z'` = 1
    - 시나리오 3문장 문단 존재: `echo "$R" | grep -c '^자기계발 독자가 취향 설정에서 5권을 고르면'` = 1 · `grep -c '완독하셨네요, 다음은'` ≥ 1 · `grep -c '별점은 만족 라벨 수집이고 모델 입력으로 쓰는 것은 설계만이다'` = 1 · `grep -c '취향을 SF로 재설정하면 기존 기록은 남고'` = 1
    - 완독 후 추천 클릭 경로(결정 D-19): `echo "$R" | grep -c '^완독 후 추천 보기: ① 메인 책 카드 → ② 책 상세 「바로 읽기」 → ③ 뷰어 「완독」 → ④ 별점 ★ 1탭(「나중에」도 가능) → ⑤ 메인 최상단 "『○○』을 완독하셨네요, 다음은" 행\.$'` = 1 · 캡션 ④·⑥에 단계 번호: `echo "$R" | grep -c '경로 ③~④'` = 1 · `grep -c '경로 ⑤'` = 1
    - `grep -c '^## 6. 완독 후 추천 보는 법' report/demo_guide.md` = 1 · `grep -c '「10분 읽기」는 유효 독서(T=15분, 데모 상수) 기록용이고 행을 만드는 것은 「완독」이다' report/demo_guide.md` = 1 · `grep -c '^## ' report/demo_guide.md` = 6 · `git diff report/demo_guide.md`가 append(`+`)만
    - 코드·프리셋 무변경: `git status --short -- demo src` 빈 출력
    - 캡처 6장 자리 표시: `echo "$R" | grep -oE 'p5_0[1-6]_[a-z_]+\.png' | sort -u | wc -l` = 6 · 캡션 표 2행: `echo "$R" | grep -c '^| ① 취향 설정 — 5권 선택'` = 1 · `grep -c '^| ④ 뷰어 — 「완독」 후 별점 1탭'` = 1
    - `echo "$R" | grep -c '^(그림 6 · 데모 캡처 3×2'` = 1 · `grep -c '^그림 6\. 배포본에서 그대로 찍은 화면 6장'` = 1
    - 삭제 확인: `echo "$R" | grep -cE '동물농장|repo|셜록 홈즈의 서가|79\.5ms|## 5-3\. 데모와 로드맵'` = 0
    - 보존 확인(Task 2 입력): `echo "$R" | grep -c '^\*\*로드맵(설계만)\.\*\*'` = 1 · `grep -c '^\*\*기억할 5가지\.\*\*'` = 1 · `grep -c '^## 5-2\. 앵커'` = 1 · `grep -c '^## 5-3\. 난이도'` = 1
    - 1~4장(`awk '/^# 1\./{f=1} /^# 5\./{f=0} f' report/draft.md`)은 이 태스크 전후 `diff` 없음
  </acceptance_criteria>
  <done>§5-1 데모 절이 URL·QR 표 · 상태 페이지 1줄 · 시나리오 3문장 · 완독 후 추천 클릭 경로 1줄 · 캡처 3×2 자리 표시 · 캡션 6(④·⑥ 경로 단계 번호) · 그림 6 캡션으로 구성되고, `report/demo_guide.md`에 §6이 추가되었으며, 기존 데모 문단 3개와 과대 표기(동물농장·해시 없는 URL)가 사라졌다.</done>
</task>

<task type="auto">
  <name>Task 2: ★박스 3 + 난이도 4층 표 · 본인 5권 표 · 로드맵·기억할 5가지 H2 · 5장 문단 벽 삭제 · 숫자 정정</name>
  <files>report/draft.md</files>
  <read_first>
    - report/draft.md (`# 5.`부터 끝까지 — Task 1 결과 상태)
    - report/demo_5books.md (5권 표 원자료 — 시드·이웃 문자열을 그대로 옮긴다, 유사도 숫자는 옮기지 않는다)
    - results/millie_difficulty.json (`by_category` 8개의 `difficulty_mean`·`completion_prob_mean` — 박스 숫자 정본)
    - results/millie_coverage.csv (`completion_prob,0.8252` → 82.5%)
    - .planning/phases/08-pdf/08-CONTEXT.md (D-03 · D-09 · D-10 · Specifics — 대조용)
    - ../.claude/skills/pdf-check/SKILL.md (8-③ "★3개가 각각 소제목 또는 박스로 존재하고 실측·그림 참조를 갖는가")
  </read_first>
  <action>
`report/draft.md` 5장의 Task 1 결과에서, 임시 `## 5-2. 앵커 …`·`## 5-3. 난이도 …` 두 절(문단 벽 2개)과 파일 끝 `**로드맵(설계만).**`·`**기억할 5가지.**` 문단을 다음으로 재구성한다. 1~4장·§5-1은 건드리지 않는다.

**2-a. §5-2 ★고유 설계 3개(결정 D-03 '★고유 설계 = Notion callout 3 + 난이도 4층 표').** 임시 `## 5-2. 앵커 — …` H2와 그 문단, `## 5-3. 난이도 — …` H2와 그 문단을 **통째로 삭제**하고 다음으로 바꾼다. blockquote `> ★ **…**`가 Notion callout으로 전환되는 자리다(전환 방법은 08-03의 `report/notion_guide.md`에 기록). 각 박스는 3~4문장, 실측 숫자 또는 표·그림 참조를 반드시 포함한다:

```
## 5-2. ★ 도서 도메인 고유 설계 3가지 [①②③]

> ★ **앵커 — "아는 책"으로 판단하게 한다.** 모르는 책은 설명을 읽어도 감이 안 오지만, 온보딩 5권은 사용자가 이미 아는 책이다. 그래서 첫날부터 "『○○』을 좋아하셨다면" 행을 만들고, 취향을 재설정하면 새 5권이 즉시 앵커가 된다(캡처 ②·⑥, §5-3 표). 향후 앵커는 완독한 책 → 서재에 담은 책 → 취향이 비슷한 다독가의 고평점 책으로 넓히고, 어느 앵커가 효과적인지는 bandit(밴딧: 실험하며 학습)으로 고른다. 행 단위 QRS를 분리 집계하기 위해 노출 로그에 행 ID를 남긴다.

> ★ **난이도 — 밀리 완독지수에서 파생한다.** 밀리가 책마다 보여주는 완독지수(완독 확률 · 예상 시간 · 분야 평균)를 추천 피처로 되먹이므로 새 파이프라인이 필요 없다. 완독 확률을 그대로 쓰면 길이·장르에 교란되므로 분야·길이를 걷어낸 잔차로 난이도를 만들고, 사용자 수준과의 차이(gap)를 순위화 피처로 쓴다 — IRT(Item Response Theory: 문항반응이론)가 말하듯 결과를 좌우하는 것은 난이도와 능력의 차이다. 완독 확률의 분야 평균은 오디오북 40.3%에서 어린이 79.0%까지 벌어지지만 잔차 난이도의 분야 평균은 0.488~0.505에 모인다(8개 분야 · 7,796권, `results/millie_difficulty.json`). 완독 3권 미만 사용자에게는 잔차 하위 책을 상단에 올리지 않는 가드를 걸고, 난이도는 UI에 숫자로 노출하지 않는다.

| 층 | 피처 | 역할 |
|---|---|---|
| 원시 | 완독 확률 · 예상 시간 · 분야 평균(밀리 완독지수) | 이미 보여주는 값을 그대로 입력 — 새 추정 없음 |
| 분야·길이 잔차 | resid_z = 분야·예상 시간 분위 안에서의 완독 확률 잔차 | "길이 대비 못 읽고 덮는 정도" — 길이 교란 제거 |
| 사용자 수준 gap | gap = 책 난이도 − 사용자 수준(부호 유지) · gap⁺ · 완독 수 × gap | 순위화 피처 3항, 결측·수준 미확정은 가중 0 |
| 신규 가드 | 완독 <3권 ∧ 잔차 하위 → 첫 화면 상단 제외 | 증거 없는 사용자엔 모델보다 규칙이 안전 · 완독지수가 있는 책(82.5%)만 모집단 |

> ★ **시간 가변 가중치 — 증거가 쌓일수록 취향 설정의 비중이 내려간다.** 사용자 상태는 취향 설정·장기 행동·세션 세 성분의 가중합이고, 가중치는 그 사용자에게 쌓인 증거의 양과 최근성의 함수다(수식·상태표는 §2-2). 가입 직후엔 α=1로 온보딩 5권이 전부를 결정하고, 이력 n권이 쌓이면 α_n = max(0.2, 0.6·e^{−n/20})로 내려가며 나머지를 장기 행동과 세션이 나눈다. 같은 hybrid 모델이 n≥20에서 pop 대비 NDCG@10을 0.087→0.240으로 벌린 것(§4-1 표)이 이 설계가 숫자로 드러난 자리다.
```
삭제되는 기존 문장 중 **보존하지 않는 것**(결정 D-03 "문단 벽의 나머지 문장은 삭제" · D-10): `Item-KNN 이웃 그대로` · `실측(Phase 4, report/demo_5books.md)` · 판본 중복(『데미안』 2종·『싯다르타』 3종) · `model= 4 variant 상위 10은 서로 다르고(pop∩cf 0 …)` · `α=1.0·β=0·γ=0으로 표시된다` · `Day 3에 freeze했다` · `구현 결정. 가드 모집단은 …` · `ablation … 설계만` · `오디오북 40.3%와 경제경영 54.8%처럼 14%p`. 숫자 정정 근거: `results/millie_difficulty.json` `by_category` 8개 분야의 `difficulty_mean`은 0.4881(어린이)~0.5047(에세이/시), `completion_prob_mean`은 40.30(오디오북)~79.01(어린이) — 기존 "0.502~0.505"·"경제경영 54.8%"는 상위 4개 분야만 고른 표기라 전체 범위로 바꾼다.

**2-b. §5-3 본인 5권 표(결정 D-09 '5권 소형 표 + 이웃 성격 1줄').** §5-2 뒤에 다음을 넣는다:
```
## 5-3. 본인 5권 앵커 사례 [②]

| 시드(본인이 읽은 5권) | 앵커 행 대표 이웃 1 | 앵커 행 대표 이웃 2 |
|---|---|---|
| 『싯다르타』 | 변신 이야기 1 | 햄릿 |
| 『데미안』 | 싯다르타(최유경 역) | 변신 이야기 1 |
| 『위버멘쉬』 | 혼자일 수 없다면 나아갈 수 없다 | 차라투스트라는 이렇게 말했다 |
| 『쇼펜하우어 인생수업: 한 번뿐인 삶 이렇게 살아라』 | 쇼펜하우어의 인생 수업 | 마흔에 읽는 쇼펜하우어 |
| 『빅터 프랭클의 죽음의 수용소에서』 | 그럼에도 삶에 '예'라고 답할 때 | 청소년을 위한 빅터 프랭클의 죽음의 수용소에서 |

서버 응답의 앵커 행 상위 5권 중 자기 판본을 제외한 대표 2권이다(`report/demo_5books.md`, 밀리 카탈로그 9,447권). 데모 카탈로그의 이웃은 콘텐츠 유사도(제목·소개 TF-IDF)이고 실서비스에서는 Item-KNN 이웃이 이 자리에 들어간다.
```
유사도 숫자는 표에 넣지 않는다(밀리 표본 = Track B, 4장 비교표와 혼합 금지).

**2-c. §5-4 로드맵·기억할 5가지.** 파일 끝 두 문단 앞에 H2를 붙이고 로드맵을 1줄로 유지한다:
```
## 5-4. 로드맵과 기억할 5가지 [③④]

**로드맵(설계만).** Two-Tower + ANN · 다중 목표 학습 랭커 · Kafka 기반 Nearline · 텍스트 가독성 난이도 · 다독가 리뷰 통로 · Interleaving · 적응형 취향 질의(다음 질문을 불확실성 기준으로 선택).

**기억할 5가지.** ① 취향 설정 = 시작점이자 언제든 갱신되는 상태 ② 설문 7단계를 전부 다른 신호로 ③ 클릭이 목표가 아니다 ④ Top-K가 아니라 페이지 구성까지가 메인 추천 ⑤ 정확도와 실서비스 제약을 같은 수준에서. 메시지는 "Netflix/YouTube 모델을 만들었다"가 아니라 **"공식 설계 원칙을 밀리의 UI·데이터·규모에 맞게 번역했다"**.
```
(두 문단은 기존 문자열 그대로 — 변경 없음.)

**2-d. 검사.** 5장 전체에서 `Phase [0-9]`·`Day [0-9]`·`freeze`·`구현 결정`·`Item-KNN 이웃 그대로`·`0.502`·`54.8%`·`14%p`가 0건, `> ★` 3개, H2 4개(5-1~5-4) 전부 축 태그. 5장 한글 자수를 D-16 Python 1줄로 계측해 SUMMARY에 기록한다(목표: 08-01 종료 시점 5장 자수 대비 감소 · 결정 D-05 '5장 ≈1,500자 감축'은 08-03에서 최종 판정).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && R="$(awk '/^# 5\./{f=1} f' report/draft.md)" && test "$(echo "$R" | grep -c '^> ★ \*\*')" = 3 && echo "$R" | grep -q '^> ★ \*\*앵커' && echo "$R" | grep -q '^> ★ \*\*난이도' && echo "$R" | grep -q '^> ★ \*\*시간 가변 가중치' && echo "$R" | grep -q '0.488~0.505' && echo "$R" | grep -q '어린이 79.0%' && echo "$R" | grep -q '0.087→0.240' && test "$(echo "$R" | grep -c '^| 『')" = 5 && echo "$R" | grep -q '실서비스에서는 Item-KNN 이웃이 이 자리에 들어간다' && test "$(echo "$R" | grep -cE 'Phase [0-9]|Day [0-9]|freeze|구현 결정|Item-KNN 이웃 그대로|0\.502|54\.8%|14%p|repo')" = 0 && test "$(echo "$R" | grep -E '^## ' | grep -vcE '\[[①②③④]+\]$')" = 0 && test "$(echo "$R" | grep -c '^## ')" = 4 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 5장 범위 `R=$(awk '/^# 5\./{f=1} f' report/draft.md)`에서 `echo "$R" | grep -c '^> ★ \*\*'` = 3, 각각 `grep -c '^> ★ \*\*앵커 — "아는 책"으로 판단하게 한다\.\*\*'` = 1 · `grep -c '^> ★ \*\*난이도 — 밀리 완독지수에서 파생한다\.\*\*'` = 1 · `grep -c '^> ★ \*\*시간 가변 가중치 — 증거가 쌓일수록 취향 설정의 비중이 내려간다\.\*\*'` = 1
    - 각 박스에 실측·참조: 앵커 `grep -c '캡처 ②·⑥, §5-3 표'` = 1 · 난이도 `grep -c '0.488~0.505에 모인다(8개 분야 · 7,796권, `results/millie_difficulty.json`)'` = 1 · 시간 가변 `grep -c '0.087→0.240으로 벌린 것(§4-1 표)'` = 1 · `grep -c '수식·상태표는 §2-2'` = 1
    - `echo "$R" | grep -c 'IRT(Item Response Theory: 문항반응이론)'` = 1 · `grep -c 'bandit(밴딧: 실험하며 학습)'` = 1 (첫 등장 형식)
    - 난이도 4층 표: `echo "$R" | grep -c '^| 원시 |'` = 1 · `grep -c '^| 분야·길이 잔차 |'` = 1 · `grep -c '^| 사용자 수준 gap |'` = 1 · `grep -c '^| 신규 가드 |'` = 1 · 표에 `82.5%` 1회
    - 숫자 정정: `echo "$R" | grep -cE '0\.502|54\.8%|14%p|경제경영'` = 0 · `grep -c '오디오북 40.3%에서 어린이 79.0%'` = 1
    - 5권 표: `echo "$R" | grep -c '^## 5-3\. 본인 5권 앵커 사례 \[②\]$'` = 1 · `grep -c '^| 『'` = 5 · 다음 문자열 각 1회 — `변신 이야기 1`(2회 허용) · `햄릿` · `싯다르타(최유경 역)` · `혼자일 수 없다면 나아갈 수 없다` · `차라투스트라는 이렇게 말했다` · `쇼펜하우어의 인생 수업` · `마흔에 읽는 쇼펜하우어` · `그럼에도 삶에 '예'라고 답할 때` · `청소년을 위한 빅터 프랭클의 죽음의 수용소에서` · 표 셀에 소수(`0\.[0-9][0-9]`) 0개: `echo "$R" | grep '^| 『' | grep -cE '0\.[0-9]{2}'` = 0
    - `echo "$R" | grep -c '데모 카탈로그의 이웃은 콘텐츠 유사도(제목·소개 TF-IDF)이고 실서비스에서는 Item-KNN 이웃이 이 자리에 들어간다'` = 1 · `grep -c 'Item-KNN 이웃 그대로'` = 0
    - `echo "$R" | grep -c '^## 5-4\. 로드맵과 기억할 5가지 \[③④\]$'` = 1 · `grep -c '^\*\*로드맵(설계만)\.\*\*'` = 1 · `grep -c '공식 설계 원칙을 밀리의 UI·데이터·규모에 맞게 번역했다'` = 1
    - `echo "$R" | grep -cE 'Phase [0-9]|Day [0-9]|freeze|구현 결정|repo|ablation|pop∩cf'` = 0 · `echo "$R" | grep -E '^## ' | grep -vcE '\[[①②③④]+\]$'` = 0 · `grep -c '^## '` = 4
    - 전체 파일: `grep -c "^# [1-5]\." report/draft.md` = 5 · `grep -c '^```mermaid' report/draft.md` = 4 · `grep -o '그림 [0-9]' report/draft.md | sort -u | wc -l` = 6
    - 1~4장(`awk '/^# 1\./{f=1} /^# 5\./{f=0} f' report/draft.md`)은 이 태스크 전후 `diff` 없음
    - SUMMARY에 D-16 Python 1줄 출력(5장 한글 자수) 전/후 기록
  </acceptance_criteria>
  <done>5장 = §5-1 데모 · §5-2 ★박스 3 + 4층 표 · §5-3 5권 표 · §5-4 로드맵·기억할 5가지. 문단 벽 2개 소멸, 난이도 숫자가 `results/millie_difficulty.json` 전체 범위와 일치, 과대 표기 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft.md → PDF(심사자, 외부 공개) | 5장은 밀리 책 제목·데모 화면을 다루므로 저작권 경계가 가장 가깝다 |
| draft.md → Notion(사용자 조판) | 그림 파일은 repo 밖 로컬 → Notion 업로드만. 원고에는 자리 표시만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | I (정보 노출) | §5-3 5권 표 — 밀리 저작 텍스트(리뷰·책 소개) | mitigate | 표는 책 제목·역자만(공개 서지 정보), 소개문·리뷰 문장 인용 0 — acceptance의 표 문자열 고정으로 강제 |
| T-08-03 | I | §5-1 캡처 — 표지 이미지·`.assets/` 경로 | mitigate | 캡처는 `report/figures/` 자리 표시만(.gitignore, 결정 '그림은 repo 밖'(.planning/phases/07-deploy/07-CONTEXT.md D-07)), `grep -c '\.assets' report/draft.md` = 0(08-03 Task 1) |
| T-08-07 | T (변조) | ★난이도 박스 — 상위 4개 분야만 고른 수치(0.502~0.505) | mitigate | Task 2-a에서 `results/millie_difficulty.json` 전체 범위(0.488~0.505 · 40.3%~79.0%)로 정정, acceptance grep |
| T-08-08 | R (부인 방지) | §5-3 표 — 정본에 없는 이웃(『동물농장』) | mitigate | Task 1-b 삭제 · 표 문자열은 `report/demo_5books.md`와 동일, acceptance `grep -c '동물농장'` = 0 |
| T-08-13 | T (변조) | D-19 클릭 경로가 데모 실제 라벨과 어긋남(문서만 바꾸고 코드는 Day 5 동결) | mitigate | 라벨 문자열을 코드 줄 번호와 함께 action에 인라인, acceptance `git status --short -- demo src` 빈 출력(코드 무변경) |
</threat_model>

<verification>
- `grep -c '^> ★ \*\*' report/draft.md` = 3 · `grep -c "^# [1-5]\." report/draft.md` = 5 · `grep -c '^```mermaid' report/draft.md` = 4 · `grep -o '그림 [0-9]' report/draft.md | sort -u | wc -l` = 6
- `grep -cE "Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve|\[Phase|\[재확인\]" report/draft.md` = 0 (이 시점에 전체 파일 0)
- `grep -E '^## ' report/draft.md | grep -vcE '\[[①②③④]+\]$'` = 0 · `grep -c '^## ' report/draft.md` = 16
- 필수 문장 7개 전부 본문 줄(`grep -v '^#' | grep -v '^|'`)에 ≥ 1
- 5장 한글 자수(D-16 Python 1줄) < 08-01 종료 시점 값
</verification>

<success_criteria>
- 결정 D-03 · D-06 · D-07(5장 몫) · D-09 · D-10(5장 몫) · D-11 · D-12 · D-19(`.planning/phases/08-pdf/08-CONTEXT.md`)가 acceptance_criteria의 grep으로 증명된다.
- 수용 기준 PDF-02 '★3개 박스 · 필수 문장 7개 · 그림 4종'(.planning/REQUIREMENTS.md)의 ★3 박스와 데모 캡처(그림 6)가 원고에 존재한다.
- 5장 숫자가 `results/millie_difficulty.json` · `results/millie_coverage.csv` · `report/demo_5books.md`와 일치한다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pdf/08-02-SUMMARY.md` — 5장 새 구조 H2 4개 · 삭제한 문장 범주 · 숫자 정정 1건(0.502~0.505 → 0.488~0.505, 근거 `results/millie_difficulty.json` by_category) · 장별 자수 전/후 · 커밋 없음(08-05 일괄).
</output>

## 결정 ↔ 태스크 매핑 (Phase 8 전체, `.planning/phases/08-pdf/08-CONTEXT.md`)

| 결정 | 내용 한 줄 | 구현 태스크 |
|---|---|---|
| D-01 | 4장 비교표 8행 유지 · 해석 5문장 · 고정 상수 각주 1줄 | 08-01 Task 3 |
| D-02 | 3장 §3-3 불릿 2개 → 각주 2줄 | 08-01 Task 2 |
| D-03 | 5장 ★ callout 3 + 난이도 4층 표 | **08-02 Task 2** |
| D-04 | 2·3장 구현 현황 각 1문장 | 08-01 Task 1 · Task 2 |
| D-05 | 감축 목표 3·4장 ≈2,500 · 5장 ≈1,500 | 08-03 Task 1 |
| D-06 | 캡처 6장 3×2 그리드 · 캡션 1:1 · 파일은 repo 밖 | **08-02 Task 1** |
| D-07 | 데모 URL·QR·상태 페이지 박스 + 1장 URL 재기재 | 08-01 Task 1(1장) · **08-02 Task 1(5장)** |
| D-08 | mermaid 4개 유지 · 1장 노드 축약 | 08-01 Task 1 |
| D-09 | 5권 소형 표 + 이웃 성격 1줄 · 과대 표기 수정 | **08-02 Task 2** |
| D-10 | 개발 과정 표기 전부 삭제 | 08-01 Task 1·2·3 · **08-02 Task 1·2** |
| D-11 | 시나리오 3문장 + 캡션 1:1 | **08-02 Task 1** |
| D-12 | 완독 직후 행 + 1탭 별점 보충(모델 입력은 설계만) | **08-02 Task 1** |
| D-13 | 필수 문장 승격 2 · Two-Tower/LLM 분리 | 08-01 Task 1 · Task 2 |
| D-14 | 문체 규칙 · 회사 수치 제거 | 08-01 Task 3 · 08-03 Task 3 |
| D-15 | 모니터링 행 문구 · 전역 p95 구절 | 08-01 Task 3 |
| D-16 | 분량 계측 + 사용자 Notion 장 수 회신 | 08-03 Task 1·2 · 08-04 Task 2·3 |
| D-17 | Sentry 정상 동작 확인 | 08-04 Task 2·3 |
| D-18 | 제출 전 점검 목록 | 08-04 Task 1 · 08-05 |
| D-19 | 완독 후 추천 유저 플로우 — 5장 클릭 경로 1줄 + 캡션 ④·⑥ 단계 번호 + demo_guide §6 | **08-02 Task 1** |
