---
phase: 08-pdf
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified: [report/draft.md, report/draft.pre-humanize.md]
autonomous: false
requirements: [PDF-03]
must_haves:
  truths:
    - "제출 전 자동 점검 10항목(pytest failed 0 · make smoke PASS · 데모 URL 200 · /health 본문 · 상태 페이지 200 · 마커 grep 0 · 저작권 ls-files 0 · URL 2개만 · 구조 5/4/3 · 다른 세션 파일 무변경)이 실측 값과 함께 기록되어 있다"
    - "Railway Variables FREEZE_BOOK_STATS=1 설정이 사용자 회신으로 확인되었거나, 미설정이면 PROGRESS 미결 1행과 STATE Day 5 게이트 미완 표기로 기록되어 있다"
    - "사용자가 Notion PDF 장 수를 회신했고 최종 원고가 5장 이내다(초과 시 문장 차순 삭제 후 재회신까지 반복)"
    - "Railway Variables FREEZE_BOOK_STATS=1 설정 여부와 UptimeRobot 키워드 권고 처리 여부가 사용자 회신으로 기록되어 있다"
    - "Sentry SDK 연결·이벤트 수신이 사용자 대시보드 확인 또는 로컬 probe 1회로 확인되었고, DSN은 어느 파일에도 없다"
  artifacts:
    - path: "report/draft.md"
      provides: "5장 이내 확정 원고"
    - path: ".planning/phases/08-pdf/08-04-SUMMARY.md"
      provides: "점검 결과표 · 사용자 회신 5건 · Sentry 확인 결과"
      contains: "장 수"
  key_links:
    - from: "report/draft.md"
      to: "사용자 Notion PDF"
      via: "사용자 회신 'N장'"
      pattern: "장"
    - from: "로컬 probe(있을 때)"
      to: "Sentry 대시보드"
      via: "사용자가 본인 터미널에서 SENTRY_DSN 인라인 1회 실행(Claude는 명령 문자열만 제시) — 파일·플랜·SUMMARY·PROGRESS·개발일지·세션 기록 미기록"
      pattern: "millie-rec probe"
---

<objective>
제출 전 점검을 자동 항목(명령)과 사용자 항목(체크포인트 회신)으로 나눠 전부 끝낸다. 자동 점검 → 사용자에게 Notion 붙이기·PDF 장 수·Railway 변수·UptimeRobot 키워드·Sentry 상태를 한 번에 묻는다 → 회신에 따라 초과분 삭제 루프와 Sentry probe를 수행한다.

Purpose: Phase 8 'PDF 제출물'(`.planning/ROADMAP.md` "### Phase 8: PDF 제출물") 성공 기준 3(5페이지 이내, PDF-03)·4(pytest·smoke PASS · `FREEZE_BOOK_STATS=1`) — 결정 D-16 · D-17 · D-18(`.planning/phases/08-pdf/08-CONTEXT.md`).
Output: 5장 이내 확정 `report/draft.md`, 점검 결과표와 사용자 회신을 담은 `.planning/phases/08-pdf/08-04-SUMMARY.md`. 기록·커밋은 08-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pdf/08-CONTEXT.md
@.planning/phases/08-pdf/08-03-SUMMARY.md
@.planning/phases/07-deploy/07-04-SUMMARY.md
@report/draft.md
@report/notion_guide.md
@../.claude/rules/local-run.md
@../.claude/skills/pdf-check/SKILL.md

<interfaces>
<!-- 이 플랜이 호출하는 명령과 기대 출력. 코드는 만들지 않는다. -->

`uv run pytest --no-header -q` → 마지막 줄에 `failed` 없음(최근 실측 590 passed).
`make smoke` → 마지막 줄 `smoke: PASS` (Makefile L42~49: 8010 포트 기동 → /health · / · /api/recommend 200 → 종료).
`curl -s -o /dev/null -w "%{http_code}" https://millie-rec-production.up.railway.app/health` → `200`.
`curl -s -o /dev/null -w "%{http_code}" https://stats.uptimerobot.com/20M6QwPo7z` → `200`.
`git ls-files | grep -iE "pdf|png|assets" | grep -v '^demo/assets/brand/millie-mark.png$'` → 빈 출력(저작권 게이트, 07-04 예외 1건 유지).
Sentry: `src/millie_rec/app/server.py` `init_sentry()`는 `SENTRY_DSN` 환경변수가 있을 때만 `sentry_sdk.init(traces_sample_rate=0, send_default_pii=False, max_request_body_size="never")`. 배포본에 안전한 예외 유발 경로 없음 → probe는 로컬 1회 `capture_message`만.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 제출 전 자동 점검 10항목 실행·기록</name>
  <files>.planning/phases/08-pdf/08-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-pdf/08-CONTEXT.md (D-18 제출 전 점검 목록)
    - .planning/phases/08-pdf/08-03-SUMMARY.md (`/pdf-check` 결과 · 장 수 추정)
    - ../.claude/rules/local-run.md (`make smoke` 의미 · 완료 기준)
    - .planning/phases/07-deploy/07-04-SUMMARY.md ("Task 1 — 최종 게이트 12행" 표 — 같은 형식으로 기록)
  </read_first>
  <action>
`millie-rec/`에서 다음을 순서대로 실행하고 결과를 SUMMARY의 점검표(# · 검사 · 기대 · 실측)에 그대로 적는다(결정 D-18 '제출 전 점검 목록'). 실패 항목은 원인 1줄과 함께 기록하고, **코드 수정은 하지 않는다**(Day 5 규칙 — PDF 숫자를 깨는 버그가 아니면 Advisor에게 보고만).

| # | 검사 | 기대 |
|---|---|---|
| 1 | `uv run pytest --no-header -q 2>&1 \| tail -1` | `failed` 없음, `passed` 수 기록 |
| 2 | `make smoke 2>&1 \| tail -1` | `smoke: PASS` |
| 3 | `curl -s -o /dev/null -w "%{http_code}" https://millie-rec-production.up.railway.app/health` | `200` |
| 4 | `curl -s https://millie-rec-production.up.railway.app/health \| uv run python -c "import sys,json;d=json.load(sys.stdin);print(d['status'],d['db_ok'],d['model_version'])"` | `ok True hybrid_div_v1` |
| 5 | `curl -s -o /dev/null -w "%{http_code}" https://stats.uptimerobot.com/20M6QwPo7z` | `200` |
| 6 | `grep -cE "\[재확인\]\|\[Phase\|Phase [0-9]\|Day [0-9]\|freeze\|구현 완료\|make serve" report/draft.md` | `0` |
| 7 | `git ls-files \| grep -iE "pdf\|png\|assets" \| grep -v '^demo/assets/brand/millie-mark.png$'` | 빈 출력 |
| 8 | `grep -oE 'https?://[^ )*]+' report/draft.md \| sort -u` | 정확히 2개(railway `/#/` · uptimerobot) — repo 링크 질문(Task 2 항목 5) 회신으로 repo 링크가 들어갔으면 3개 |
| 9 | `grep -c "^# [1-5]\." report/draft.md` · `grep -c '^```mermaid' report/draft.md` · `grep -c '^> ★ \*\*' report/draft.md` | `5` · `4` · `3` |
| 10 | `git status --short -- results report/figures src tests demo Makefile pyproject.toml` | `results/millie_edges_gate.json` 수정 1건과 다른 세션 미추적 파일(`scripts/*_millie_reviews*.py` 등)만 — 이 플랜이 만든 변화 0 |

`/pdf-check`는 08-03에서 통과했으므로 재실행하지 않고 08-03-SUMMARY의 판정을 표에 인용한다(원고가 이 태스크 전에 바뀌지 않았음을 `git diff --stat report/draft.md`로 확인).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && uv run pytest --no-header -q 2>&1 | tail -1 | grep -v failed | grep -q passed && make smoke 2>&1 | tail -1 | grep -q 'smoke: PASS' && test "$(curl -s -o /dev/null -w '%{http_code}' https://millie-rec-production.up.railway.app/health)" = 200 && test "$(curl -s -o /dev/null -w '%{http_code}' https://stats.uptimerobot.com/20M6QwPo7z)" = 200 && test "$(grep -cE '\[재확인\]|\[Phase|Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve' report/draft.md)" = 0 && test -z "$(git ls-files | grep -iE 'pdf|png|assets' | grep -v '^demo/assets/brand/millie-mark.png$')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY에 점검표 10행이 있고 각 행에 실측 출력 원문(예: `590 passed`, `smoke: PASS`, `200`)이 있다
    - 1: `uv run pytest --no-header -q | tail -1`에 `failed` 없음 · 2: `smoke: PASS` · 3·5: `200` · 4: `ok True hybrid_div_v1` · 6: `0` · 7: 빈 출력 · 8: 2개 · 9: `5` `4` `3`
    - 10: 이 플랜이 `src/ tests/ demo/ Makefile pyproject.toml results/ report/figures`를 바꾸지 않았다(전후 `git status --short` 동일)
    - `git diff --stat report/draft.md`가 Task 1 전후 동일(원고 무변경)
  </acceptance_criteria>
  <done>자동 점검 10행 전부 기대값과 일치하고 SUMMARY에 실측 원문으로 기록됨. 불일치가 있으면 원인 1줄과 함께 Advisor 보고 항목으로 표시.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 확인 5건 — Notion PDF 장 수 · Railway FREEZE_BOOK_STATS · UptimeRobot 키워드 · Sentry 상태 · 공개 repo 링크 여부</name>
  <files>.planning/phases/08-pdf/08-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-pdf/08-04-SUMMARY.md (Task 1 점검표 — 사용자에게 제시할 현재 상태)
    - report/notion_guide.md (사용자가 따라갈 절차 — 안내문의 절 번호와 일치해야 한다)
    - .planning/phases/08-pdf/08-CONTEXT.md (D-16 · D-17 · D-18 · Deferred repo 링크 — 질문 5건의 근거)
  </read_first>
  <action>
    자동 점검(Task 1) 10행이 전부 통과한 뒤에만 이 체크포인트를 띄운다. 질문 5를 위해 먼저 `grep -nE 'Goodbooks|CC BY-SA|밀리' README.md` 출력을 확인해 안내문에 현재 출처 문구 유무를 1줄 덧붙인다. 사용자에게 아래 `<how-to-verify>` 5개 항목을 **한글로, 번호 그대로** 제시하고 회신을 기다린다. 회신 5줄을 받으면 원문·시각을 SUMMARY 회신 표에 적고 Task 3으로 넘어간다. 회신이 일부만 오면 빠진 항목만 다시 한 줄로 묻는다. Notion MCP로 페이지를 대신 만들어 달라는 요청이 오면 `report/notion_guide.md` §0의 선택지대로 진행한다(원고 문자열은 draft.md 그대로, 그림은 사용자 업로드).
  </action>
  <what-built>`report/draft.md` 최종 원고(08-01~03: 압축·5장 재구성·윤문·`/pdf-check` 통과)와 `report/notion_guide.md`(Notion 조판 순서). 자동 점검 10행(Task 1) 통과. 남은 것은 Claude가 대신할 수 없는 사용자 대시보드·Notion 작업 5건이다(결정 D-16 · D-17 · D-18).</what-built>
  <how-to-verify>
아래 5가지를 확인하고 각각 한 줄씩 회신해 주세요.

1. **Notion → PDF 장 수(결정 D-16 '분량 검증', `.planning/phases/08-pdf/08-CONTEXT.md`)**
   `millie-rec/report/notion_guide.md`의 순서대로 `report/draft.md`를 Notion 새 페이지(비공개)에 붙여 넣고, mermaid 4개 언어를 Mermaid로, `> ★` 3개를 callout으로, 그림 3종(`eval_bar.png` · `p5_qr.png` · 캡처 6장 3×2)을 `millie-rec/report/figures/`에서 넣은 뒤 Export → PDF · A4 · Scale 100%로 내보내세요. 내보낸 PDF의 **장 수**를 알려 주세요(예: "5장"). 6장 이상이면 어느 장이 넘쳤는지도 한 단어로(예: "6장, 4장이 넘침").

2. **Railway Variables `FREEZE_BOOK_STATS=1`(결정 D-18)**
   Railway 대시보드 → 프로젝트 efficient-ambition → 서비스 → Variables에 `FREEZE_BOOK_STATS` = `1`이 있는지 확인해 주세요. 없으면 추가하고 재배포가 끝난 뒤 알려 주세요(재배포 약 5~15초 다운). 회신: "FREEZE 있음" / "FREEZE 추가함" / "FREEZE 미설정 유지".

3. **UptimeRobot 키워드 권고(Codex 2회차 C2, `.planning/phases/07-deploy/07-04-SUMMARY.md` "Task 2 — Codex" 표)**
   UptimeRobot 모니터의 Keyword가 `"status":"ok"`입니다. `"db_ok":true`로 바꾸면 DB 고장 시에도 200을 주는 문제를 코드 없이 감시할 수 있습니다(권고, 필수 아님). 회신: "키워드 바꿈" / "키워드 유지".

4. **Sentry 정상 동작(결정 D-17 'Sentry 정상 동작 확인')**
   Sentry 프로젝트 대시보드 → Issues / Settings → Client Keys에서 ① SDK가 연결된 프로젝트인지 ② 최근 이벤트(에러 또는 메시지)가 1건 이상 있는지 확인해 주세요. 배포본은 예외가 없어 이벤트가 0일 수 있습니다. 회신: "Sentry 이벤트 있음" / "Sentry 이벤트 없음". 없음이면 다음 태스크에서 제가 명령 한 줄을 드리고, 그 명령의 빈칸에 DSN을 넣어 **본인 터미널에서** 한 번 실행해 주시면 됩니다(DSN은 채팅·파일에 남기지 않습니다).

5. **공개 repo 링크를 PDF에 넣을지(08-CONTEXT Deferred 항목)**
   `millie-rec/README.md`의 데이터 출처 문구(Goodbooks-10k CC BY-SA 4.0 귀속 · 밀리 공개 페이지 수치·메타만)를 확인한 뒤, 5장 데모 박스 아래에 `코드: https://github.com/<owner>/millie-rec` 1줄을 넣을지 정해 주세요. 회신: "repo 링크 넣음 <URL>" / "repo 링크 안 넣음". (안 넣음이면 원고 무변경.)
  </how-to-verify>
  <resume-signal>5줄 회신 — "N장 …" / "FREEZE …" / "키워드 …" / "Sentry 이벤트 있음|없음" / "repo 링크 넣음 <URL>|안 넣음". 문제가 있으면 그 항목만 자유롭게 적어 주세요.</resume-signal>
  <verify>
    <automated>grep -q '장' .planning/phases/08-pdf/08-04-SUMMARY.md && grep -q 'FREEZE' .planning/phases/08-pdf/08-04-SUMMARY.md && grep -q 'Sentry' .planning/phases/08-pdf/08-04-SUMMARY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 사용자 회신 5건(장 수 · FREEZE · 키워드 · Sentry · repo 링크)이 SUMMARY 회신 표에 원문·시각으로 기록되어 있다
    - 안내문이 한글이고 `report/notion_guide.md` 절 번호(§1~§6)와 어긋나지 않는다
    - 회신 전에 Task 3을 시작하지 않았다(SUMMARY 시각 순서)
  </acceptance_criteria>
  <done>사용자 회신 5건 수신·기록. 원고·코드 변경 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 회신 반영 — 5장 초과분 문장 차순 삭제 루프 · Sentry probe(사용자 터미널, 필요 시) · FREEZE 미설정 분기 · repo 링크 1줄 · 회신 기록</name>
  <files>report/draft.md, report/draft.pre-humanize.md, .planning/phases/08-pdf/08-04-SUMMARY.md</files>
  <read_first>
    - Task 2 사용자 회신(5줄)
    - report/draft.md · report/draft.pre-humanize.md (삭제 시 백업도 같은 문장을 지워 동기화)
    - .planning/phases/08-pdf/08-CONTEXT.md (D-16 "초과분은 문장 차순 삭제(폰트·여백 축소 금지)" · D-17 probe 절차 · Claude's Discretion 삭제 우선순위)
    - .planning/phases/08-pdf/08-03-SUMMARY.md (장별 자수 표 · 이미 지운 문장 목록 — 중복 삭제 방지)
    - src/millie_rec/app/server.py (`init_sentry()` — DSN 처리 방식 확인용, 수정 금지)
  </read_first>
  <action>
**3-a. 장 수 회신 처리(결정 D-16).**
- 회신이 "5장" 이하 → 원고 확정. SUMMARY에 "Notion PDF N장(사용자 회신 <시각>)".
- 회신이 6장 이상 → 넘친 장(회신에 있으면 그 장, 없으면 08-03 자수 표에서 상한 초과폭이 가장 큰 장)에서 아래 차순으로 지운다(결정 D-05 정정 · `.planning/phases/08-pdf/08-CONTEXT.md`). 08-03 Task 1-b가 이미 지운 문장은 건너뛴다(08-03-SUMMARY 목록 참조).
  - **5장이 넘쳤으면(가장 흔한 경우 — 5장은 새 요소로 순증):** ① 캡처 캡션 6개를 각 1구절로(`① 취향 설정 — 5권 선택` → `① 취향 설정`; 경로 번호는 ②③④⑥에 숫자만 남김) → ② 난이도 4층 피처 표를 지우고 ★난이도 박스 안 1문장으로 흡수("원시 완독지수 → 분야·길이 잔차 → 사용자 수준 gap → 신규 가드의 4층으로 쓴다") → ③ 시나리오 3문장 → 2문장(완독·별점 문장과 재설정 문장을 하나로) → ④ 앵커 박스 4번째 문장(행 단위 QRS·행 ID) → ⑤ 로드맵 1줄. **절대 보존:** ★3 박스(제목 + 핵심 1문장) · 5권 표 · 데모 URL 2곳·상태 페이지 URL · 완독 후 추천 클릭 경로 1줄(D-19) · 기억할 5가지 · 그림 자리 표시 6개(mermaid 4 · 그림 5 · 그림 6).
  - **1~4장이 넘쳤으면:** ① 해석·부연 문장(2장 "Netflix 홈 원칙" 재언급 · 4장 §4-2 Interleaving 확장 문장 · 3장 스택 표 "이 선택의 이유" 셀 뒷문장 · 1장 §1-2 Netflix 80%·$1B 문장) → ② 각주 부연(4장 측정 조건 각주의 "온보딩 시뮬레이션 = …" · 3장 각주 2의 "가동은 5분 간격 헬스체크로 감시한다"는 5장 상태 페이지 줄과 중복이라 삭제 가능) → ③ 표 행 축약(3장 §3-3 대응표 Kubernetes 행 · 스택 표 "데이터 처리" 행 · 4장 §4-2 표 Discovery 행). **절대 보존:** 필수 문장 7 · 숫자와 출처 · mermaid 4 · 비교표 8행 · 4축 표 · 1장 URL 1줄 · 그림 5 자리 표시.
  - 한 회차에 넘친 장에서 최소 150 한글자(캡션 축약처럼 면적을 줄이는 항목은 자수와 무관하게 1항목)를 지운 뒤 같은 삭제를 `report/draft.pre-humanize.md`에도 적용하고(두 파일 `diff`는 윤문 차이만), 3-d의 grep을 다시 통과시킨 다음 사용자에게 "다시 붙여 넣고 장 수 회신"을 **한글 한 줄**로 요청한다(Task 2 형식). 5장 이내가 될 때까지 반복하며 회차별 삭제 문장 목록을 SUMMARY에 남긴다.

**3-b. Sentry 회신 처리(결정 D-17).**
- "Sentry 이벤트 있음" → SUMMARY에 "Sentry: 사용자 대시보드에서 SDK 연결·이벤트 확인(<시각>)". 끝.
- "Sentry 이벤트 없음" → **Claude는 명령 문자열만 제시**하고 실행은 사용자가 **본인 터미널**에서 1회 한다. Claude의 Bash는 사용자 셸의 `export`를 상속하지 못하고, `!` 접두 실행은 DSN을 세션 기록에 남기므로 둘 다 쓰지 않는다(Claude가 DSN이 든 명령을 실행하는 경로 없음). 한글 안내(그대로):
  "아래 명령의 `<붙여넣기>` 자리에 Sentry → Settings → Client Keys의 DSN을 넣어 **본인 터미널에서 한 번** 실행한 뒤 '보냄'이라고 회신해 주세요. DSN은 채팅에 붙이지 마세요. 마지막 `history -d …`는 셸 기록에서 그 줄을 지우는 것이고, zsh에서 동작하지 않으면 `~/.zsh_history`에서 해당 줄을 직접 지워 주세요."
```
cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && SENTRY_DSN='<붙여넣기>' uv run python -c "import sentry_sdk,os;sentry_sdk.init(dsn=os.environ['SENTRY_DSN'],traces_sample_rate=0,send_default_pii=False);sentry_sdk.capture_message('millie-rec probe');sentry_sdk.flush();print('sent')"; unset SENTRY_DSN; history -d $(history 1 | awk '{print $1}') 2>/dev/null
```
  '보냄' 회신 후 Claude는 **대시보드 수신 여부만** 한글로 묻는다: "Sentry Issues에 `millie-rec probe` 메시지가 보이나요? (최대 1~2분)" → "보임"이면 SUMMARY에 "Sentry: 사용자 터미널 probe 1회 수신 확인(<시각>)". "안 보임"이면 DSN 프로젝트 불일치 가능성 1줄과 함께 `../PROGRESS.md` 미결 후보로 SUMMARY에 적고 08-05에서 기록한다. **DSN 값은 어떤 파일(플랜·SUMMARY·PROGRESS·개발일지·STATE·스크래치패드)에도 쓰지 않고 Claude가 요청하지도 않는다.** 디버그 엔드포인트·의도적 500·코드 변경은 하지 않는다.

**3-c. 나머지 회신 기록 · 조건 분기.** FREEZE(있음/추가함/미설정) · 키워드(바꿈/유지) · repo 링크(넣음/안 넣음)를 SUMMARY 회신 표에 적는다.
- **FREEZE 분기(ROADMAP 성공 기준 4):** "있음"/"추가함"이면 SUMMARY에 "FREEZE_BOOK_STATS=1 확인(<시각>)". "미설정 유지"면 ① SUMMARY에 "화면 book_stats 숫자가 이벤트로 변할 수 있음 — PDF 숫자(`results/`)는 무관" 1줄 ② 08-05 Task 1이 쓸 **PROGRESS 미결 1행 문구**를 SUMMARY에 준비: `- [ ] Railway Variables FREEZE_BOOK_STATS=1 미설정 — 화면 book_stats가 이벤트로 변할 수 있음(ROADMAP Phase 8 성공 기준 4, 08-04-SUMMARY)` ③ STATE Day 5 게이트는 08-05에서 `✅`가 아닌 `⚠ FREEZE 미설정` 표기로 남긴다고 SUMMARY에 명시.
- **repo 링크 분기(Task 2 항목 5 회신):** "넣음 <URL>"이면 5장 §5-1 상태 페이지 줄 바로 아래에 `코드: <URL>` 1줄을 추가하고(`report/draft.pre-humanize.md`에도 동일) URL 개수 기대치를 3으로 바꿔 SUMMARY에 적는다. "안 넣음"이면 원고 무변경.
최종 grep 재확인:
```
grep -cE "\[재확인\]|\[Phase|Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve" report/draft.md   # 0
grep -rc 'ingest.sentry.io' .planning/phases/08-pdf report ../PROGRESS.md ../.assets/개발일지 2>/dev/null | grep -v ':0$'   # 빈 출력
```
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && test "$(grep -cE '\[재확인\]|\[Phase|Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve' report/draft.md)" = 0 && test -z "$(grep -rl 'ingest.sentry.io' .planning/phases/08-pdf report ../PROGRESS.md ../.assets/개발일지 2>/dev/null)" && test "$(grep -c '^> ★ \*\*' report/draft.md)" = 3 && test "$(grep -c '^```mermaid' report/draft.md)" = 4 && test "$(grep -cE '^\| (pop|cf|hybrid|hybrid_div) ' report/draft.md)" = 8 && grep -q '장 수' .planning/phases/08-pdf/08-04-SUMMARY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY에 회신 표 5행(장 수 · FREEZE · 키워드 · Sentry · repo 링크)과 각 회신 원문·시각
    - 최종 장 수 회신 ≤ 5 — 초과 회차가 있었으면 회차별 삭제 문장 목록과 재회신 시각
    - 삭제가 있었으면 `report/draft.md`와 `report/draft.pre-humanize.md`에서 같은 문장이 지워졌음: `diff <(grep -c '' report/draft.md) <(grep -c '' report/draft.pre-humanize.md)` 줄 수 차이 ≤ 2(윤문 차이만)
    - 보존: `grep -c '^> ★ \*\*' report/draft.md` = 3 · `grep -c '^```mermaid' report/draft.md` = 4 · `grep -cE '^\| (pop|cf|hybrid|hybrid_div) ' report/draft.md` = 8 · `grep -c '^| 『' report/draft.md` = 5 · 필수 문장 7개 본문 줄 각 ≥ 1 · URL 2개
    - `grep -cE "\[재확인\]|\[Phase|Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve" report/draft.md` = 0
    - `grep -rl 'ingest.sentry.io' .planning/phases/08-pdf report ../PROGRESS.md ../.assets/개발일지` 빈 출력(DSN 비노출) · `git diff --stat -- src` 빈 출력(코드 0)
    - Sentry 결과 1줄이 SUMMARY에 있다: "대시보드 확인" 또는 "사용자 터미널 probe 1회 수신 확인" 또는 "미확인 — 사유" · 플랜·SUMMARY 어디에도 Claude가 DSN을 받거나 실행한 기록이 없다(`grep -c 'export SENTRY_DSN' .planning/phases/08-pdf/08-04-SUMMARY.md` = 0)
    - FREEZE 회신이 "미설정 유지"면 SUMMARY에 PROGRESS 미결 1행 문구와 "STATE Day 5 ⚠ FREEZE 미설정" 지시가 있다; "있음/추가함"이면 "FREEZE_BOOK_STATS=1 확인(시각)" 1줄
    - repo 링크 "넣음"이면 `grep -c '^코드: https://github.com/' report/draft.md` = 1 이고 URL 집계 3개; "안 넣음"이면 0 · 2개
  </acceptance_criteria>
  <done>사용자 회신 5건이 기록되고, 원고가 5장 이내로 확정되었으며, Sentry 동작이 확인(또는 미확인 사유 기록)되었고, FREEZE 미설정이면 미결·STATE 표기 지시가 준비되었고, DSN은 어디에도 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 셸 환경변수 → Sentry 외부 서비스 | DSN(비밀)이 프로세스 환경으로만 흘러야 한다 — 파일·채팅·히스토리에 남으면 노출 |
| draft.md → Notion(사용자 페이지) → PDF | 표지 이미지가 든 캡처가 Notion에 업로드된다 — 페이지 비공개 유지 |
| 로컬 → 배포본(Railway) | 점검은 GET만. 코드·변수 변경은 사용자 대시보드 몫 |
| 사용자 터미널 → Sentry | DSN은 사용자 터미널 프로세스 환경에만 존재. Claude 세션·Bash 도구는 DSN을 보지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | I (정보 노출) | Sentry DSN — 플랜·SUMMARY·PROGRESS·개발일지·Claude 세션 기록·셸 기록 | mitigate | Task 3-b: Claude는 명령 문자열만 제시, 사용자가 본인 터미널에서 DSN 인라인 1회 실행 후 `unset` + `history -d`, 채팅에 붙이지 말라는 한글 안내, Claude 실행 경로·`!` 접두 금지, acceptance `grep -rl 'ingest.sentry.io'` 빈 출력 |
| T-08-03 | I | 캡처(밀리 표지) — Notion 페이지 공개 | mitigate | Task 2 안내문 "Notion 새 페이지(비공개)", 가이드 §4 "비공개 유지" |
| T-08-10 | D (서비스 거부) | 배포본에 의도적 500·디버그 엔드포인트 추가 유혹 | mitigate | Task 3-b "디버그 엔드포인트·의도적 500·코드 변경 금지", acceptance `git diff --stat -- src` 빈 출력 |
| T-08-07 | T (변조) | 초과분 삭제가 필수 문장·숫자·그림을 지움 | mitigate | Task 3-a 절대 삭제 금지 목록 + acceptance 보존 grep |
</threat_model>

<verification>
- 자동 점검 10행 전부 기대값(SUMMARY 표)
- 사용자 회신 5건 기록, 최종 장 수 ≤ 5
- DSN grep 빈 출력 · `git diff --stat -- src tests demo` 빈 출력
- 원고 보존 grep(★3 · mermaid 4 · 비교표 8 · 5권 표 5 · 필수 문장 7 · URL 2)
</verification>

<success_criteria>
- Phase 8 'PDF 제출물'(`.planning/ROADMAP.md`) 성공 기준 3 '5페이지 이내'와 4 'pytest·smoke PASS · FREEZE_BOOK_STATS=1'이 사용자 회신과 명령 출력으로 증명된다.
- 결정 D-16 · D-17 · D-18(`.planning/phases/08-pdf/08-CONTEXT.md`) 전부 SUMMARY에 결과 1줄 이상.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pdf/08-04-SUMMARY.md` — 점검표 10행(실측 원문) · 회신 표 5행 · 삭제 루프 회차별 목록(있으면) · Sentry 결과 1줄 · 커밋 없음(08-05).
</output>

## 결정 ↔ 태스크 매핑 (Phase 8 전체, `.planning/phases/08-pdf/08-CONTEXT.md`)

| 결정 | 내용 한 줄 | 구현 태스크 |
|---|---|---|
| D-01 ~ D-04 | 1~4장 압축 · ★박스 · 구현 1문장 | 08-01 Task 1·2·3 · 08-02 Task 2 |
| D-05 | 감축 목표(정정: 3장 ≤1,100 · 4장 ≤1,150 · 5장 산문 ≤1,100) · 5장 전용 삭제 차순 | 08-03 Task 1 · **08-04 Task 3(초과분 삭제 루프, 5장 차순 우선)** |
| D-06 · D-07 | 캡처 3×2 · URL·QR 박스 | 08-01 Task 1 · 08-02 Task 1 · 08-03 Task 2 |
| D-08 · D-09 | mermaid 4 · 5권 표 | 08-01 Task 1 · 08-02 Task 2 |
| D-10 | 개발 과정 표기 삭제 | 08-01 · 08-02 · 08-03 Task 1 · **08-04 Task 1(최종 grep #6)** |
| D-11 · D-12 · D-13 | 시나리오 · 별점 보충 · 필수 문장 승격 | 08-02 Task 1 · 08-01 Task 1·2 |
| D-14 · D-15 | 문체 · 회사 수치 · 모니터링 행 | 08-01 Task 3 · 08-03 Task 1·3 |
| D-16 | 계측 + **사용자 Notion 장 수 회신** | 08-03 Task 1·3 · **08-04 Task 2(회신) · Task 3(초과분 삭제)** |
| D-17 | **Sentry 정상 동작 확인** | **08-04 Task 2(대시보드 회신) · Task 3(probe, DSN 비기록)** |
| D-18 | **제출 전 점검 목록** | **08-04 Task 1(자동 10행) · Task 2(FREEZE·키워드)** · 08-05(기록·커밋) |
| D-19 | 완독 후 추천 유저 플로우(draft 5장 · demo_guide §6) | 08-02 Task 1 |
