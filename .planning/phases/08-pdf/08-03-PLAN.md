---
phase: 08-pdf
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified: [report/draft.md, report/draft.pre-humanize.md, report/notion_guide.md]
autonomous: true
requirements: [PDF-01, PDF-03]
must_haves:
  truths:
    - "draft.md의 모든 소수·ms·%·권수 숫자가 results/latest_states.csv · latest.csv · latency.json · millie_coverage.csv · millie_difficulty.json · millie_edges_gate.json · demo_5books.md 값과 글자 단위로 일치한다(재측정 0회)"
    - "장별 한글 자수가 계측되어 있고 3·4·5장이 결정 D-05의 감축 목표(3·4장 합 ≈2,500자 · 5장 ≈1,500자 감축) 안에 들어왔거나 미달 사유와 삭제 문장 목록이 SUMMARY에 있다"
    - "humanize 윤문 뒤 /pdf-check 전 항목 통과, 숫자·mermaid·표·H1/H2·필수 문장 7개·백틱 식별자가 백업과 동일하다"
    - "report/notion_guide.md에 붙여넣기 순서 · Mermaid 언어 · callout 전환 3곳 · 캡처 3×2 · QR · eval_bar · PDF 내보내기 설정 · 장 수 회신 방법이 있다"
  artifacts:
    - path: "report/draft.md"
      provides: "제출 원고 최종본(사용자 장 수 회신 전)"
      contains: "> ★ **앵커"
    - path: "report/draft.pre-humanize.md"
      provides: "humanize 직전 백업(복원 정본)"
    - path: "report/notion_guide.md"
      provides: "Notion 조판 안내"
      min_lines: 30
  key_links:
    - from: "report/draft.md"
      to: "results/latest_states.csv"
      via: "8행 × 3지표 24개 숫자 + n 2,000/1,990 전수 대조 스크립트"
      pattern: "0\\.205"
    - from: "report/draft.md"
      to: "report/draft.pre-humanize.md"
      via: "humanize 후 구조 줄(#·|·```·$$)·숫자 다중집합·mermaid 본문 diff 빈 출력"
      pattern: "^# [1-5]\\."
---

<objective>
원고를 계측하고 마감한다: 장별 자수 계측 → 감축 목표 미달분 문장 차순 삭제 → 숫자 전수 대조 → 저작권 grep → Notion 조판 가이드 작성 → 백업 갱신 → `/humanize-korean` 윤문 → `/pdf-check` → 바뀐 절 복원 → `make pdf`로 장 수 추정.

Purpose: Phase 8 'PDF 제출물'(`.planning/ROADMAP.md` "### Phase 8: PDF 제출물") 성공 기준 1(숫자 출처, PDF-01)·3(`/pdf-check` 통과 · 5페이지, PDF-03)의 Advisor 몫. 사용자 Notion 확인은 08-04.
Output: `report/draft.md` 최종본 · `report/draft.pre-humanize.md` 갱신 · `report/notion_guide.md` 신설 · `.planning/phases/08-pdf/08-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pdf/08-CONTEXT.md
@.planning/phases/08-pdf/08-02-SUMMARY.md
@report/draft.md
@../.claude/rules/report.md
@../.claude/skills/pdf-check/SKILL.md
@../.claude/docs/docs-policy.md
@../.claude/skills/notion-authoring/SKILL.md
@results/latest_states.csv
@results/latest.csv
@results/latency.json
@results/millie_coverage.csv
@results/millie_difficulty.json
@results/millie_edges_gate.json
@report/demo_5books.md

<interfaces>
<!-- 숫자 정본 → 원고 표기 대응표. Task 1의 대조 기준. -->

| 정본 | 값 | 원고 표기 |
|---|---|---|
| results/latest_states.csv | 8행 recall/ndcg/ild 24개 · n_users 2000/1990 | 소수 3자리 그대로 · `2,000` `1,990` |
| results/latest.csv | n0 4행(= 위 n0) | 그림 5 캡션 `n=2,000` |
| results/latency.json | p95 79.517 | `79.5ms` (p50·p99 미인용) |
| results/millie_coverage.csv | completion_prob 0.8252 · _n_records 9447 | `82.5%` · `9,447권` |
| results/millie_difficulty.json | n_difficulty 7796 · by_category difficulty_mean 0.4881~0.5047 · completion_prob_mean 40.30~79.01 | `7,796권` · `0.488~0.505` · `40.3%` `79.0%` |
| results/millie_edges_gate.json | same_category_share 0.3564 | `0.356` |
| 유도값 | 0.240−0.087 · (0.110−0.107)/0.110 · 0.684−0.606 | `0.153` · `2.7%` · (0.078은 08-01에서 삭제됨 — 남아 있으면 허용) |
| report/demo_5books.md | 시드 5권 · 이웃 문자열 | §5-3 표 |

인용 금지: 서버·mock `latency_ms` · 관제 대시보드 수치 · 회사 수치(회원 수 · 24만 권) · Goodbooks 외 다른 데이터셋 수치.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 장별 자수 계측 → 감축 목표 판정·문장 차순 삭제 → 숫자 전수 대조 → 저작권·마커 grep</name>
  <files>report/draft.md</files>
  <read_first>
    - report/draft.md (전체, 08-02 결과)
    - .planning/phases/08-pdf/08-CONTEXT.md (D-05 감축 목표 · D-16 계측 방법 · Claude's Discretion "감축 시 어느 문장을 먼저 지울지(필수 문장 7 · ★3 · 숫자 출처 보존)")
    - .planning/phases/08-pdf/08-01-SUMMARY.md · 08-02-SUMMARY.md (장별 자수 전/후 기록 — 기준값)
    - results/latest_states.csv · results/latest.csv · results/latency.json · results/millie_coverage.csv · results/millie_difficulty.json · results/millie_edges_gate.json · report/demo_5books.md (대조 정본 — `cat`으로 읽기만, `/eval-run`·`make eval`·`make bench`·`make millie*` 실행 금지 = 숫자 고정)
    - ../.claude/rules/report.md (장별 한글 ≈1,000~1,100자 + 표 2~4 + mermaid 1 · 5장 초과 시 문장을 지운다, 폰트·여백 축소 금지)
  </read_first>
  <action>
**1-a. 계측(결정 D-16 '분량 검증 = Advisor 계측 + 사용자 Notion 확인').** 다음 1줄을 실행해 장별 (한글자 수, 전체 문자 수)를 얻고 SUMMARY 표에 기록한다(awk는 한글을 못 셈):
```
uv run python -c "import re,sys;t=open('report/draft.md',encoding='utf-8').read();parts=re.split(r'^# (?=[1-5]\.)',t,flags=re.M)[1:];[print(p.split(chr(10))[0][:12],len(re.findall(r'[가-힣]',p)),len(p)) for p in parts]"
```
기준값(HEAD `aa5cc91`, 이 페이즈 시작 전): 1장 1,054 / 2장 1,065 / 3장 1,471 / 4장 1,371 / 5장 1,193 한글자(전체 문자 2,311 / 2,626 / 4,148 / 4,314 / 2,568). 결정 D-05 목표를 전체 문자 기준으로 환산: **3장+4장 합계 ≤ 8,462−2,500 ≈ 5,960자**, **5장 ≤ 2,568−1,500 ≈ 1,070자 + 새로 들어간 표·캡션 면적**(5장은 표 3개·캡션이 추가되므로 한글자 기준 **본문 산문 ≤ 1,100자**로 판정). 표·mermaid·캡션은 산문 상한에서 빼고 면적으로 본다.

**1-b. 미달 시 문장 차순 삭제(Claude's Discretion).** 어떤 장이 상한을 넘으면 그 장에서 다음 우선순위로 문장을 지운다 — ① 해석·부연 문장(예: 2장 "Netflix 홈 원칙" 재언급, 4장 §4-2 세 번째 불릿의 Interleaving 확장 방향 문장, 3장 §3-2 표의 "이 선택의 이유" 셀 뒷문장) → ② 각주의 부연(4장 측정 조건 각주의 "온보딩 시뮬레이션 = …" 구절) → ③ 표 행 축약(3장 §3-3 대응표의 Kubernetes 행, 3장 스택 표의 "데이터 처리" 행). **절대 삭제 금지**: 필수 문장 7개 · `> ★` 박스 3개 · 숫자와 그 출처(`results/…`) · mermaid 4개 · 비교표 8행 · 5권 표 · 4축 표 · 데모 URL·상태 페이지 URL · 기억할 5가지. 지운 문장은 SUMMARY에 목록으로 남긴다. 목표에 이미 들어왔으면 지우지 않는다.

**1-c. 숫자 전수 대조(수용 기준 PDF-01 '숫자는 results/에서만', `.planning/REQUIREMENTS.md`).** 다음 스크립트로 원고의 숫자를 뽑아 정본과 대조하고 결과를 SUMMARY 표에 붙인다:
```
uv run python - <<'EOF'
import re,csv,json
t=open('report/draft.md',encoding='utf-8').read()
rows=list(csv.DictReader(open('results/latest_states.csv')))
need={r[k] for r in rows for k in ('recall@20','ndcg@10','ild@10')}
miss=[v for v in need if v not in t]
print('latest_states 24개 중 원고에 없는 값:',miss)
lat=json.load(open('results/latency.json'));print('p95 정본',round(lat['p95'],1),'원고 79.5ms 존재',('79.5ms' in t))
cov={r['field']:r['non_null_ratio'] for r in csv.reader(open('results/millie_coverage.csv'))}
print('coverage 정본',cov['completion_prob'],'원고 82.5% 존재',('82.5%' in t),'| 9447 원고 9,447 존재',('9,447' in t))
d=json.load(open('results/millie_difficulty.json'));bc=d['by_category']
print('difficulty by_category',min(c['difficulty_mean'] for c in bc),max(c['difficulty_mean'] for c in bc),'원고 0.488~0.505',('0.488~0.505' in t))
print('completion by_category',min(c['completion_prob_mean'] for c in bc),max(c['completion_prob_mean'] for c in bc),'원고 40.3%/79.0%',('40.3%' in t and '79.0%' in t),'| n_difficulty',d['n_difficulty'],('7,796' in t))
e=json.load(open('results/millie_edges_gate.json'));print('same_category_share',e['same_category_share'],'원고 0.356',('0.356' in t))
print('금지 숫자 잔존:',[s for s in ('24만','890만','0.502','54.8%','14%p','latency_ms') if s in t])
print('원고 소수 전체:',sorted(set(re.findall(r'\b0\.\d{3}\b',t))))
EOF
```
`miss`가 비어 있지 않거나 `False`가 하나라도 나오면 **원고를 정본 값으로 고친다**(정본을 바꾸지 않는다). `원고 소수 전체` 목록의 각 값이 정본(위 `<interfaces>` 표)에 있거나 유도값(0.153 · 0.078)인지 하나씩 확인해 SUMMARY에 "출처 확인 N/N".

**1-d. 저작권·마커·경로 grep(위협 T-08-01·03·05).** 다음이 전부 0인지 확인하고 아니면 해당 문장을 고친다:
```
grep -cE "Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve|\[Phase|\[재확인\]" report/draft.md
grep -c '\.assets' report/draft.md
grep -ciE 'kt\+밀리|사전과제_2605|scratchpad|repo' report/draft.md
grep -cE '24만|890만' report/draft.md
grep -oE 'https?://[^ )]+' report/draft.md | sort -u   # 허용 URL 2개(railway /#/ · stats.uptimerobot)만 나와야 한다
```
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && test "$(grep -cE 'Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve|\[Phase|\[재확인\]' report/draft.md)" = 0 && test "$(grep -c '\.assets' report/draft.md)" = 0 && test "$(grep -cE '24만|890만|0\.502|54\.8%|latency_ms' report/draft.md)" = 0 && test "$(grep -oE 'https?://[^ )]+' report/draft.md | sort -u | wc -l | tr -d ' ')" = 2 && uv run python -c "
import csv;t=open('report/draft.md',encoding='utf-8').read();rows=list(csv.DictReader(open('results/latest_states.csv')));need={r[k] for r in rows for k in ('recall@20','ndcg@10','ild@10')};assert all(v in t for v in need),'latest_states 값 누락';assert all(s in t for s in ('79.5ms','82.5%','9,447','7,796','0.488~0.505','40.3%','79.0%','0.356')),'정본 표기 누락';print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - D-16 Python 1줄 출력이 SUMMARY에 표로 있고(5행), 3장+4장 전체 문자 합 ≤ 5,960 **또는** 5장 본문 한글 ≤ 1,100 미달 시 삭제한 문장 목록과 남은 초과분·사유가 SUMMARY에 있다
    - 1-c 스크립트 출력에서 `latest_states 24개 중 원고에 없는 값: []` · 모든 `존재` 항목 `True` · `금지 숫자 잔존: []`
    - `grep -cE "Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve|\[Phase|\[재확인\]" report/draft.md` = 0
    - `grep -c '\.assets' report/draft.md` = 0 · `grep -ciE 'kt\+밀리|사전과제_2605|scratchpad|repo' report/draft.md` = 0 · `grep -cE '24만|890만' report/draft.md` = 0
    - `grep -oE 'https?://[^ )]+' report/draft.md | sort -u` = 정확히 `https://millie-rec-production.up.railway.app/#/`와 `https://stats.uptimerobot.com/20M6QwPo7z` 2개
    - 보존 확인: `grep -c '^> ★ \*\*' report/draft.md` = 3 · `grep -c '^```mermaid' report/draft.md` = 4 · `grep -cE '^\| (pop|cf|hybrid|hybrid_div) ' report/draft.md` = 8 · `grep -c '^| 『' report/draft.md` = 5 · `grep -c '^| ① 추천 목표 |' report/draft.md` = 1 · 필수 문장 7개 각 본문 줄 ≥ 1
    - `results/` · `report/demo_5books.md` · `report/figures/`는 이 태스크 전후 `git status --short results report/figures report/demo_5books.md` 변화 없음(재측정·재생성 0)
  </acceptance_criteria>
  <done>장별 자수 표 · 삭제 문장 목록(있으면) · 숫자 출처 확인 N/N · 저작권 grep 0 · URL 2개만 — 전부 SUMMARY에 기록.</done>
</task>

<task type="auto">
  <name>Task 2: report/notion_guide.md 작성 — Notion 조판 안내</name>
  <files>report/notion_guide.md</files>
  <read_first>
    - report/draft.md (조판 대상의 최종 구조: H1 5 · H2 16 · mermaid 4 · `> ★` 3 · 그림 자리 표시 3종(QR · 캡처 표 · eval_bar) · 표 목록)
    - ../.claude/skills/notion-authoring/SKILL.md (md → Notion 변환 규칙: `> ` → callout · 코드펜스 → code · em dash 치환은 **로컬 md 무변경**)
    - ../.claude/rules/report.md (조판 = Notion 붙여넣기 → PDF 내보내기 5장 · mermaid 코드 블록 언어 = Mermaid · 폰트·여백 축소 금지)
    - .planning/phases/08-pdf/08-CONTEXT.md (D-06 · D-07 · D-16 · Specifics)
  </read_first>
  <action>
`report/notion_guide.md`를 **신설**한다(Write). 내용은 사용자가 Notion에서 따라 할 순서이며 아래 절과 문장을 그대로 포함한다(한국어, 명령·경로는 원문):

```
# Notion 조판 가이드 — report/draft.md → PDF 5장

## 0. 준비
- 원고: `millie-rec/report/draft.md` 최종본(커밋 예정). 그림: `millie-rec/report/figures/`(repo 밖) — `p5_01_onboarding.png` ~ `p5_06_home_after.png` · `p5_qr.png` · `eval_bar.png`.
- Notion 새 페이지(비공개) 제목 = "밀리의서재 메인 영역 추천 시스템 설계". 원고 첫 줄 `# 밀리의서재 …` H1은 페이지 제목으로 쓰고 본문에는 붙이지 않는다.
- 원하면 Advisor가 Notion MCP로 페이지를 생성할 수 있다(선택 — 한 줄로 요청).

## 1. 붙여넣기 순서 (장 = H1 `# 1.`~`# 5.`)
1. draft.md를 전체 선택 → Notion 빈 페이지에 붙여넣기(마크다운 자동 변환). 표는 Notion 표로, `---`는 구분선으로 들어간다.
2. 장 경계: 각 `# N.` heading 앞에 구분선이 있는지 확인. Notion PDF 내보내기는 페이지 나누기를 자동으로 하지 않으므로 장 수는 내보내기 결과로 확인한다.

## 2. mermaid 4개 — 코드 블록 언어를 Mermaid로
- 붙여넣기 후 ```` ```mermaid ```` 블록은 코드 블록이 된다. 각 블록 왼쪽 위 언어 선택 → **Mermaid**. 오른쪽 위 "Preview"(또는 Split)로 전환하면 렌더된다. 4곳: 1장 §1-4(그림 1) · 2장 §2-1(그림 2) · 3장 §3-1(그림 3) · 4장 §4-1(그림 4).
- 렌더가 너무 크면 블록 폭은 그대로 두고 문장을 지운다(폰트·여백 축소 금지).

## 3. `> ★` blockquote 3개 → callout 전환 (5장 §5-2)
- Notion은 `> ` 인용을 인용 블록으로 받는다. 각 인용 블록에서 `/callout`으로 전환(블록 메뉴 → Turn into → Callout) 또는 내용을 잘라 새 callout에 붙인다. 아이콘 ★, 배경은 앵커=노랑 · 난이도=파랑 · 시간 가변 가중치=초록(Claude's Discretion, 바꿔도 됨).
- 3곳: `★ 앵커` · `★ 난이도` · `★ 시간 가변 가중치`. 전환 뒤 원고의 `> ` 기호가 남지 않았는지 확인.

## 4. 그림 삽입 (자리 표시 줄을 지우고 이미지를 넣는다)
- **4장 그림 5 비교 막대**: `(그림 5 · 비교 막대 — report/figures/eval_bar.png, …)` 줄을 지우고 그 자리에 `eval_bar.png` 업로드 → 폭 1/2(가운데 정렬). 캡션은 원고의 `그림 5. …` 줄을 이미지 캡션으로 옮긴다.
- **5장 QR**: 표 `| 데모 | QR |`의 오른쪽 셀 `(QR — report/figures/p5_qr.png, …)`를 지우고 `p5_qr.png` 업로드(폭 1/6). 표 안 이미지가 어색하면 표를 2컬럼 블록으로 바꾸고 오른쪽 컬럼에 이미지.
- **5장 그림 6 캡처 3×2**: `(그림 6 · 데모 캡처 3×2 — …)` 줄과 캡션 표(①~⑥)를 지우고, 3컬럼 블록 2개(위 ①②③ · 아래 ④⑤⑥)에 `p5_01`~`p5_06` 순서대로 업로드. 각 이미지 캡션 = 원고 표 셀 문구(`① 취향 설정 — 5권 선택` …). 파일명 괄호는 캡션에 넣지 않는다. 각 폭 1/3, 6장 전체가 약 1/3페이지.
- 이미지에 밀리 표지가 들어 있으므로 Notion 페이지는 **비공개 유지**, PDF만 제출한다.

## 5. PDF 내보내기
- 오른쪽 위 ··· → Export → Format **PDF** · Include content **Everything** · Page format **A4** · Scale **100%**. 폰트·여백은 바꾸지 않는다.
- 내보낸 PDF 장 수를 확인한다(macOS 미리보기 하단 또는 `pdfinfo <file> | grep Pages`).

## 6. 장 수 회신 (Advisor에게 한 줄)
- "N장" 한 줄로 회신한다. 5장 초과면 Advisor가 원고의 문장을 우선순위대로 지운 뒤(해석 문단 → 각주 부연 → 표 행 축약, 필수 문장 7·★3·숫자 출처·mermaid 4는 보존) 다시 붙여넣기를 요청한다. 5장 이내면 그 PDF가 제출본이다.

## 7. 제출 전 마지막 확인 (PDF에서)
- 데모 URL `https://millie-rec-production.up.railway.app/#/`와 상태 페이지 `https://stats.uptimerobot.com/20M6QwPo7z`가 링크로 눌린다.
- `report/figures/` 같은 로컬 경로 문자열이 PDF에 남아 있지 않다(자리 표시 줄을 지웠는지).
- 캡션 ①~⑥이 §5-1 시나리오 순서와 같다.
```
파일 끝에 `참조: .planning/phases/08-pdf/08-CONTEXT.md 결정 D-06 '캡처 3×2' · D-07 'URL·QR 박스' · D-16 '분량 검증'` 1줄.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && test -f report/notion_guide.md && test "$(grep -c '^## ' report/notion_guide.md)" -ge 7 && grep -q 'Mermaid' report/notion_guide.md && grep -q '/callout' report/notion_guide.md && grep -q 'eval_bar.png' report/notion_guide.md && grep -q 'p5_qr.png' report/notion_guide.md && grep -q 'A4' report/notion_guide.md && grep -q 'Scale \*\*100%\*\*' report/notion_guide.md && grep -q 'N장' report/notion_guide.md && test "$(wc -l < report/notion_guide.md)" -ge 30 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f report/notion_guide.md` · `wc -l < report/notion_guide.md` ≥ 30 · `grep -c '^## ' report/notion_guide.md` ≥ 7
    - 다음 문자열 각 ≥ 1: `Mermaid` · `/callout` · `3컬럼 블록` · `eval_bar.png` · `p5_qr.png` · `p5_01` · `A4` · `Scale **100%**` · `N장` · `비공개 유지` · `https://millie-rec-production.up.railway.app/#/` · `https://stats.uptimerobot.com/20M6QwPo7z` · `Notion MCP`
    - `grep -c '\.assets' report/notion_guide.md` = 0 · `grep -ciE 'kt\+밀리|사전과제_2605' report/notion_guide.md` = 0
    - `report/draft.md`는 이 태스크에서 무변경(`git diff --stat report/draft.md` 전후 동일)
  </acceptance_criteria>
  <done>사용자가 가이드만 보고 Notion 붙여넣기 → mermaid 4 렌더 → callout 3 → 그림 3종 삽입 → PDF A4 100% 내보내기 → 장 수 회신까지 할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 3: 백업 갱신 → /humanize-korean 윤문 → /pdf-check → 바뀐 절 복원 → make pdf 장 수 추정</name>
  <files>report/draft.md, report/draft.pre-humanize.md</files>
  <read_first>
    - report/draft.md (Task 1 결과 = 윤문 대상)
    - report/draft.pre-humanize.md (현재 240줄 구버전 — 이 태스크가 덮어쓴다)
    - ../.claude/docs/docs-policy.md §"문체 마감 — humanize-korean 스킬" (게이트: 윤문 후 반드시 `/pdf-check`, 바뀌면 백업에서 그 절만 복원 · 호출 시 제약 4개 한글로)
    - ../.claude/skills/pdf-check/SKILL.md (8항목 — 이 태스크의 acceptance로 옮김)
    - ../.claude/rules/report.md (필수 문장 7개 · 영어 용어 형식 · 문단 3문장)
  </read_first>
  <action>
**3-a. 백업 갱신.** `cp report/draft.md report/draft.pre-humanize.md`. 확인: `diff report/draft.md report/draft.pre-humanize.md` 빈 출력.

**3-b. `/humanize-korean` 호출(전역 스킬, 결정 D-14 '문체 규칙 유지').** Skill 도구로 `/humanize-korean report/draft.md`를 **standard** 경로로 실행하며 다음 제약을 한글로 인자에 넘긴다(그대로 복사):
```
대상: millie-rec/report/draft.md 전체. 경로 standard. 제약 4개를 반드시 지켜라.
① 불변: 숫자·수식($$ … $$)·mermaid 코드 블록 4개(내용 한 글자도)·표(행·열·셀 값)·H1/H2 제목(축 태그 [①]~[④] 포함)·백틱 식별자·파일 경로·URL 2개·"> ★ **…**" blockquote 3개의 첫 문장(굵은 제목).
② 영어 용어 병기 형식 `용어(풀네임: 설명)` 유지, 두 번째부터 약어만 규칙도 유지.
③ 보존 문장(글자 그대로): "CTR(Click-Through Rate: 클릭률)은 진단 지표이지 목적이 아니다" / "취향 재설정 ≠ 프로필 초기화" / "요청마다 무거운 계산을 다시 하지 않는다" / "추천 API 장애가 메인 장애가 되어선 안 된다" / "Two-Tower는 넣지 않았다" / "LLM(Large Language Model: 대규모 언어 모델)은 넣지 않았다" / "미선택 책은 negative가 아니다" / "공식 설계 원칙을 밀리의 UI·데이터·규모에 맞게 번역했다" / 5장 시나리오 문단 3문장 / 5권 표 아래 2문장.
④ 한다체 격식 유지, 한 문단 3문장 이내 유지. 대구·대시 남용·부정 종결 반복·번역투만 고친다.
결과는 _workspace/{run_id}/final.md에 두고 덮어쓰기는 내가 한다.
```
결과 `../_workspace/<run_id>/final.md`(프로젝트 루트 `밀리의서재/` 아래, repo 밖)를 `report/draft.md`에 덮어쓴다(`cp`). 스킬이 없거나 실패하면 윤문을 건너뛰고 SUMMARY에 "humanize 미실행 — 사유"를 적는다(원고는 백업과 동일 상태로 유지).

**3-c. 구조 불변 검사(윤문이 바꾸면 안 되는 것).** 다음 4개 `diff`가 **전부 빈 출력**이어야 한다. 하나라도 차이가 나면 차이가 난 장(`# N.` 범위)을 `report/draft.pre-humanize.md`에서 통째로 복원한다(절 단위 복원, 결정 D-14 게이트):
```
diff <(grep -E '^#|^\||^```|^\$\$' report/draft.pre-humanize.md) <(grep -E '^#|^\||^```|^\$\$' report/draft.md)
diff <(awk '/^```mermaid/,/^```$/' report/draft.pre-humanize.md) <(awk '/^```mermaid/,/^```$/' report/draft.md)
diff <(grep -oE '[0-9]+(,[0-9]{3})*(\.[0-9]+)?%?' report/draft.pre-humanize.md | sort | uniq -c) <(grep -oE '[0-9]+(,[0-9]{3})*(\.[0-9]+)?%?' report/draft.md | sort | uniq -c)
diff <(grep -oE '`[^`]+`' report/draft.pre-humanize.md | sort | uniq -c) <(grep -oE '`[^`]+`' report/draft.md | sort | uniq -c)
```
그리고 필수 문장 7개 + 마무리 문장이 본문 줄에 있는지 `grep -c`로 확인(각 ≥ 1). 없어진 문장이 있으면 그 장을 복원한다.

**3-d. `/pdf-check` 실행.** Skill 도구로 `/pdf-check millie-rec/report/draft.md`를 실행하고 출력 체크리스트 표를 SUMMARY에 붙인다. ❌ 항목이 있으면 고친다 — 단 문장 다듬기 제안은 반영하지 않는다(pdf-check 규칙). 8항목: ① 필수 4조건 ② 선택 7조건 ③ 필수 문장 7 ④ 숫자 출처 ⑤ 형식(H1 5 · 장별 자수 · mermaid ≥3 · 용어 형식 · 출처 절/`[n]` 각주 잔존 ❌ · 마커 · 저작권) ⑥ 기억할 5가지 ⑦ 4축 가시성(첫 문단 4문장 · H2 태그 · ★3 박스 실측 참조 · 제약 인과문). 최종 판정 "제출 가능"이어야 한다.

**3-e. `make pdf`로 장 수 추정.** `cd report && pandoc draft.md -s --mathjax -H mermaid.html --metadata title="밀리의서재 메인 추천 시스템 설계" -o draft.html`(`make pdf`는 `open`까지 하므로 헤드리스에서는 위 명령만) → `report/draft.html` 생성 확인. 브라우저를 열 수 없으면 `wc -m report/draft.md`와 장별 자수로 "A4 5장 ≈ 한글 6,000~6,500자 + 표·그림" 기준의 추정치를 SUMMARY에 적는다(정확한 판정은 08-04 사용자 Notion 회신). `report/draft.html`은 `.gitignore` 대상인지 `git check-ignore report/draft.html`로 확인, 추적 대상이면 삭제한다.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && test -z "$(diff <(grep -E '^#|^\||^```|^\$\$' report/draft.pre-humanize.md) <(grep -E '^#|^\||^```|^\$\$' report/draft.md))" && test -z "$(diff <(awk '/^```mermaid/,/^```$/' report/draft.pre-humanize.md) <(awk '/^```mermaid/,/^```$/' report/draft.md))" && test -z "$(diff <(grep -oE '[0-9]+(,[0-9]{3})*(\.[0-9]+)?%?' report/draft.pre-humanize.md | sort | uniq -c) <(grep -oE '[0-9]+(,[0-9]{3})*(\.[0-9]+)?%?' report/draft.md | sort | uniq -c))" && B="$(grep -v '^#' report/draft.md | grep -v '^|')" && for s in 'CTR(Click-Through Rate: 클릭률)은 진단 지표이지 목적이 아니다' '취향 재설정 ≠ 프로필 초기화' '요청마다 무거운 계산을 다시 하지 않는다' '추천 API 장애가 메인 장애가 되어선 안 된다' 'Two-Tower는 넣지 않았다' 'LLM(Large Language Model: 대규모 언어 모델)은 넣지 않았다' '미선택 책은 negative가 아니다'; do echo "$B" | grep -qF "$s" || { echo "MISSING: $s"; exit 1; }; done && test "$(grep -c "^# [1-5]\." report/draft.md)" = 5 && test "$(grep -c '^```mermaid' report/draft.md)" = 4 && test "$(grep -c '^> ★ \*\*' report/draft.md)" = 3 && test "$(grep -cE '\[재확인\]|\[Phase' report/draft.md)" = 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `report/draft.pre-humanize.md`가 Task 1 종료 시점의 `report/draft.md`와 동일했음이 SUMMARY에 기록(`diff` 빈 출력 확인 시각)
    - 3-c의 `diff` 4개 전부 빈 출력(구조 줄 · mermaid 본문 · 숫자 다중집합 · 백틱 식별자 다중집합)
    - 필수 문장 7개 + `공식 설계 원칙을 밀리의 UI·데이터·규모에 맞게 번역했다`가 본문 줄(`grep -v '^#' | grep -v '^|'`)에 각 ≥ 1
    - `grep -c "^# [1-5]\." report/draft.md` = 5 · `grep -c '^```mermaid' report/draft.md` = 4 · `grep -c '^> ★ \*\*' report/draft.md` = 3 · `grep -E '^## ' report/draft.md | grep -vcE '\[[①②③④]+\]$'` = 0
    - `grep -cE "\[재확인\]|\[Phase|Phase [0-9]|Day [0-9]|freeze|구현 완료|make serve" report/draft.md` = 0 · `grep -c '\.assets' report/draft.md` = 0 · `grep -cE '^\[[0-9]+\]|^## 참고|^## 출처' report/draft.md` = 0(출처 절·`[n]` 각주 없음)
    - `/pdf-check` 출력의 최종 판정이 "제출 가능"이고 체크리스트 표가 SUMMARY에 붙어 있다(❌ 0)
    - `test -f report/draft.html` 또는 SUMMARY에 pandoc 실패 사유 · 장 수 추정치 1줄("≈N장, 근거: 장별 자수 …")
    - humanize를 실행했으면 SUMMARY에 run_id와 바뀐 문장 수(diff 줄 수), 복원한 장 목록(없으면 "복원 0"); 실행 못 했으면 사유 1줄
    - `git status --short results report/figures` 변화 없음
  </acceptance_criteria>
  <done>윤문된 원고가 백업과 구조·숫자·식별자 동일, `/pdf-check` 제출 가능 판정, 장 수 추정 기록, 백업 파일이 최신.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft.md → PDF(심사자) | Task 1-d의 grep이 이 경계를 넘는 금지 문자열을 막는다 |
| draft.md → 외부 스킬(humanize-korean 워크스페이스 `../_workspace/`) | 원고가 repo 밖 작업 폴더로 복사된다 — 과제 PDF 문구·비밀은 원고에 없어야 한다(Task 1-d 선행) |
| draft.md → results/ (읽기 전용) | 재측정·재생성은 숫자 freeze 위반 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I (정보 노출) | draft.md — 과제 PDF 원문 문구·파일명 | mitigate | Task 1-d `grep -ciE 'kt\+밀리|사전과제_2605'` = 0, `/pdf-check` ⑤ 저작권 항목 |
| T-08-03 | I | draft.md·notion_guide.md — `.assets/` 경로·표지 이미지 | mitigate | `grep -c '\.assets'` = 0 두 파일 모두 · 가이드에 "Notion 페이지 비공개 유지" 문구 |
| T-08-05 | R (검증 불가 수치) | draft.md — 회사 수치 | mitigate | Task 1-d `grep -cE '24만|890만'` = 0 |
| T-08-07 | T (변조) | humanize 윤문이 숫자·표·mermaid·필수 문장을 바꿈 | mitigate | Task 3-a 백업 → 3-c diff 4개 빈 출력 + 문장 grep → 실패 시 장 단위 복원 → `/pdf-check` |
| T-08-09 | T | `/eval-run`·`make eval`·`make bench` 재실행으로 results/ 변경 | mitigate | Task 1 read_first에 실행 금지 명시, acceptance `git status --short results` 무변화 |
</threat_model>

<verification>
- `/pdf-check` 최종 판정 "제출 가능", ❌ 0
- 숫자 대조 스크립트: 누락 `[]` · `존재` 전부 True · 금지 숫자 `[]`
- 구조 diff 4개 빈 출력(백업 vs 윤문본)
- `report/notion_guide.md` 존재 · 7절 이상
- 장별 자수 표 · 장 수 추정치 SUMMARY 기록
</verification>

<success_criteria>
- 수용 기준 PDF-01 '숫자는 results/에서만'과 PDF-03 '/pdf-check 전 항목 통과'(.planning/REQUIREMENTS.md)의 Advisor 측 조건이 충족되고, 5페이지 판정만 08-04 사용자 회신으로 넘어간다.
- 결정 D-05 · D-14 · D-16(Advisor 계측 부분)(`.planning/phases/08-pdf/08-CONTEXT.md`)이 SUMMARY 기록으로 증명된다.
</success_criteria>

<output>
After completion, create `.planning/phases/08-pdf/08-03-SUMMARY.md` — 장별 자수 표(전/후) · 삭제 문장 목록 · 숫자 출처 확인 N/N · `/pdf-check` 체크리스트 표 · humanize run_id·복원 목록 · 장 수 추정 · 커밋 없음(08-05 일괄).
</output>

## 결정 ↔ 태스크 매핑 (Phase 8 전체, `.planning/phases/08-pdf/08-CONTEXT.md`)

| 결정 | 내용 한 줄 | 구현 태스크 |
|---|---|---|
| D-01 | 4장 비교표 8행 · 해석 5문장 · 고정 상수 각주 | 08-01 Task 3 |
| D-02 | 3장 §3-3 각주 2줄 | 08-01 Task 2 |
| D-03 | ★ callout 3 + 4층 표 | 08-02 Task 2 |
| D-04 | 구현 현황 각 1문장 | 08-01 Task 1·2 |
| D-05 | 감축 목표 3·4장 ≈2,500 · 5장 ≈1,500 | **08-03 Task 1** |
| D-06 | 캡처 3×2 · 캡션 1:1 | 08-02 Task 1 · **08-03 Task 2(가이드)** |
| D-07 | URL·QR·상태 페이지 박스 | 08-01 Task 1 · 08-02 Task 1 · **08-03 Task 2(가이드)** |
| D-08 | mermaid 4 유지 · 1장 축약 | 08-01 Task 1 · **08-03 Task 2(Mermaid 언어)** |
| D-09 | 5권 표 | 08-02 Task 2 |
| D-10 | 개발 과정 표기 삭제 | 08-01 · 08-02 · **08-03 Task 1(최종 grep)** |
| D-11 | 시나리오 3문장 | 08-02 Task 1 |
| D-12 | 완독 직후 행 + 별점 | 08-02 Task 1 |
| D-13 | 필수 문장 승격·분리 | 08-01 Task 1·2 |
| D-14 | 문체 규칙 · 회사 수치 제거 | 08-01 Task 3 · **08-03 Task 1(890만·24만 grep) · Task 3(humanize)** |
| D-15 | 모니터링 행 · 전역 p95 | 08-01 Task 3 |
| D-16 | 계측 + 사용자 Notion 확인 | **08-03 Task 1(계측) · Task 3(make pdf)** · 08-04 Task 2·3 |
| D-17 | Sentry 확인 | 08-04 Task 2·3 |
| D-18 | 제출 전 점검 | **08-03 Task 3(/pdf-check)** · 08-04 Task 1 · 08-05 |
