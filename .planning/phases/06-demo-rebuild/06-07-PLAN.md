---
phase: 06-demo-rebuild
plan: 07
type: execute
wave: 4
depends_on: ["06-06"]
files_modified:
  - .planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md
  - ../PROGRESS.md            # 미결 절에 제안 2줄 append 만(Edit 전 재읽기 — ../CLAUDE.md §6)
autonomous: false
requirements: [DEMO-09, DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-05, DEMO-06, DEMO-07, DEMO-08]
lane: 조립(Advisor 직접 — 전역 게이트·Playwright 완주·캡처·인계, 05-09 관례)
no_commit: true   # 커밋하지 않는다 — 사용자 승인 후 Phase 5 산출물과 일괄(사용자 지시 2026-09-05)
must_not_touch:
  - src/**
  - tests/**                  # tests/demo 포함 — 코드는 고치지 않는다, 실패는 소유 플랜에 수정 브리프
  - demo/**                   # 코드 무수정. 어긋남은 06-01~06-06 소유 플랜 재위임
  - Makefile
  - pyproject.toml
  - Dockerfile
  - artifacts/**
  - results/**
  - report/**
  - .planning/REQUIREMENTS.md # DEMO-09 문구 수정은 PROGRESS 미결 제안으로만(06-CONTEXT D-09)
  - .planning/ROADMAP.md
  - ../.assets/**

must_haves:
  truths:
    - "구 형태 grep(브리프 정규식 그대로) 이 demo/ 전체에서 0건이다(DEMO-01·06-CONTEXT D-07)"
    - "/contract-sync 6항이 전부 ✅ — 이름(VARIANTS·ROW_IDS·fallback) · 형태(계약 10 mock + popular.json model_validate) · 문서 · freeze 기록 · Track B(description 0·book_format 3값·badge.type 6종·itemknn 0) · 생성기(_manifest.json)(DEMO-02)"
    - "uv run pytest --no-header 전체 Green(tests/demo 포함) · uv run ruff check demo/scripts tests/demo 0 · make smoke PASS · wc -l demo JS/CSS 전부 ≤250 · 색 리터럴은 tokens.css 외 0(base.css v1 잔존 3건은 판단 기록)"
    - "Advisor 스크래치패드(repo 밖) Playwright 로 ?source=mock 쇼케이스→…→대시보드 완주(06-06 체크리스트 1~14 순서) 콘솔 에러 0 · ?capture=1|2 스크린샷 6장(화면 02 §7) 저장(DEMO-09 mock 완주 + 캡처)"
    - "make serve 후 ?source=api 에서 #/ → #/home 크래시 0(현 서버 라우트 /health·/api/recommend·/ 기준 — 없는 엔드포인트는 '서버 준비 중' 문구) — api 완주 게이트는 Phase 7 '배포'(.planning/ROADMAP.md) 로 이관(06-CONTEXT D-09)"
    - "PROGRESS.md 미결 절에 2줄 append(① Makefile mock 타깃 → uv run python demo/scripts/make_mock.py · cli.py mock 부재 ② REQUIREMENTS DEMO-09 문구 'mock 완주(Phase 6) + api 완주(Phase 7 배포 전 게이트)') + /contract-sync 항목 추가 제안(serving/onboarding_meta.json ↔ config/onboarding.json 텍스트 동일성) 이 기록된다"
  artifacts:
    - path: ".planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md"
      provides: "게이트 결과표(grep·contract-sync 6항·pytest·ruff·smoke·wc·색 리터럴) · Playwright 완주 로그·스크린샷 경로 6 · api 크래시 0 관측 · 수정 브리프 재위임 목록 · DEMO-01~09 재확인표"
      min_lines: 60
    - path: "../PROGRESS.md"
      provides: "미결 절 append 2줄 + /contract-sync 항목 제안 1줄(Edit 전 재읽기)"
      contains: "uv run python demo/scripts/make_mock.py"
  key_links:
    - from: ".planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md"
      to: "demo/**"
      via: "게이트 명령 출력 인용(수정은 하지 않음)"
      pattern: "contract-sync"
    - from: "../PROGRESS.md"
      to: "Makefile mock · .planning/REQUIREMENTS.md DEMO-09"
      via: "미결 제안 2줄(Phase 5 세션 Advisor 가 병합)"
      pattern: "DEMO-09"
---
<objective>
Phase 6 '데모 재구성'(.planning/ROADMAP.md "Phase 6: 데모 재구성") 의 **전역 게이트 + Advisor 실측 + 인계**. 코드는 고치지 않는다 — 게이트 실패는 소유 플랜(06-01~06-06 `files_modified`) 에 수정 브리프로 되돌린다(`../.claude/docs/workflow.md` 검증 사다리). 산출물은 `06-VERIFICATION-NOTES.md`(게이트 결과·Playwright 로그·스크린샷 경로·재위임 목록·DEMO-01~09 재확인표) 와 `PROGRESS.md` 미결 2줄 + 제안 1줄이다. 이 플랜은 Advisor(오케스트레이터) 세션이 직접 실행한다(`autonomous: false` — Playwright 캡처·api 관측·Codex 생략 판단을 사람이 본다).

Purpose: ROADMAP Success Criteria 5 — "`?source=mock` 완주 콘솔 에러 0 · `?capture=1|2` 동작 · `uv run pytest -q`·`make smoke` PASS"(**DEMO-09**, 06-CONTEXT D-09 로 mock 완주가 Phase 6 판정, api 완주는 Phase 7 게이트) + DEMO-01~08 전부의 **최종 재확인**(각 ID 의 기계 판정 명령 1개씩). PDF 캡처 6장(화면 02 §7 — 온보딩 S3+인스펙터 · 메인 배지·앵커 · hybrid vs hybrid_div · 뷰어 완독→별점→다음은 · 서재 타임라인·철회 · 대시보드 · 쇼케이스) 의 원본을 스크래치패드에 만든다(repo 에는 넣지 않는다 — `report/figures/` 는 Phase 8 몫). `../.claude/rules/codex-review.md` 표에 따라 `demo/` 는 Codex 리뷰 **생략**(기록만).
Output: 파일 2개(코드 0). 커밋 0.

**Phase 5 병렬 주의:** `make serve` 는 Phase 5 wave 진행 상황에 따라 라우트가 다르다(현재 `/health`·`/api/recommend`·`/` 확정, 나머지는 도착 중). api 모드 관측은 "크래시 0 + 404 목록 기록" 까지만 — 완주 판정 아님. `uv run pytest --no-header` 전체 수치는 Phase 5 신규 테스트로 변동하므로 "failed 0" 만 판정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-demo-rebuild/06-CONTEXT.md
@.planning/phases/06-demo-rebuild/06-01-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-02-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-03-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-04-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-05-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-06-SUMMARY.md
@../.claude/skills/contract-sync/SKILL.md
@../.claude/rules/demo.md
@../.claude/rules/codex-review.md
@../.claude/rules/references.md
@../.claude/docs/workflow.md
@../CLAUDE.md
@../PROGRESS.md
@../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md

<file_ownership>
## Phase 6 파일 소유 표 (겹침 0 — 같은 wave 는 서로소)

| 파일 | 만듦/고침/삭제 | 플랜 | wave |
|---|---|---|---|
| `demo/scripts/make_mock.py` · `tests/demo/test_make_mock.py` · `demo/mock/*.json`(13개 — 계약 10 + catalog_kr·neighbors_kr + _manifest) · `demo/mock/books.json` 삭제 | 재작성·신규·재생성 | 06-01 | 1 |
| `demo/js/router.js` · `demo/js/app.js` · `demo/js/actions.js` · `demo/js/presets.js` · `demo/js/screens/ui.js` · `demo/js/screens/onboarding_step.js` · `demo/js/screens/s0_start.js` · `demo/js/screens/s6_persona.js` · `demo/index.html` · `demo/css/base.css` · `demo/config/onboarding.json` · `demo/README.md` | 신규·재작성·최소 수정 | 06-02 | 2 |
| `demo/js/api.js` · `demo/js/mock.js` · `demo/js/mock_store.js` | 재작성·신규 | 06-03 | 2 |
| `demo/js/screens/d4_reader.js` · `demo/js/screens/d5_library.js` · `demo/css/reader.css` · `demo/css/library.css` | 신규 | 06-04 | 2 |
| `demo/js/screens/d7_dashboard.js` · `demo/js/screens/d8_showcase.js` · `demo/css/dashboard.css` · `demo/css/tokens.css` | 신규·토큰 추가 | 06-05 | 2 |
| `demo/js/screens/d2_home.js`(← `s7_home.js` 삭제) · `demo/js/screens/d3_detail.js`(← `s8_detail.js` 삭제) · `demo/js/inspector.js` · `demo/css/home.css` · `demo/css/inspector.css` | 신규·삭제·확장 | 06-06 | 3 |
| `.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md` · `../PROGRESS.md`(미결 절 2줄 append 만) | Advisor | **06-07(이 플랜)** | 4 |
</file_ownership>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전역 기계 게이트 — 구 형태 grep · /contract-sync 6항 · pytest · ruff · smoke · wc · 색 리터럴 · codegraph sync</name>
  <files>.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md</files>
  <read_first>
    - .planning/phases/06-demo-rebuild/06-06-SUMMARY.md "수정 브리프 제안" 표(이미 알려진 어긋남)
    - ../.claude/skills/contract-sync/SKILL.md process 1~6(검사 항목 원문)
    - ../.claude/rules/codegraph.md 프리플라이트(`cd millie-rec && codegraph sync .` — Worker 변경 후)
    - .planning/REQUIREMENTS.md L70-78 (DEMO-01~09 원문 — 재확인표 기준)
  </read_first>
  <action>
`06-VERIFICATION-NOTES.md` 를 만들며 아래 명령을 순서대로 실행하고 **출력 원문**을 표로 인용한다(`cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec`).
1. **구 형태 grep(DEMO-01·D-07):** `cd demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' --include='*.js' --include='*.json' --include='*.html' --include='*.py' --include='*.md' --include='*.css' . | wc -l` → 0. 0 이 아니면 파일별 소유 플랜을 표에.
2. **`/contract-sync`(DEMO-02):** 스킬을 실행하고 6항 표를 그대로 인용. 최소 직접 확인: (1) `ls demo/mock/recommend_{pop,cf,hybrid,hybrid_div}.json` 4개 · 각 `model_version == "<v>_v1"` · `grep -rnE '"(pop|cf|hybrid|hybrid_div)_mock"' demo | wc -l` 0 (2) `uv run python -c` 로 계약 mock 10 + `demo/fallback/popular.json` `model_validate`(06-01 verify 명령 재사용) (3) 백엔드 01 §5·§13 키 집합 vs mock — 차집합 0 (4) `grep -n 'freeze' ../PROGRESS.md | head -3` (5) `python3 -c "import json;d=json.load(open('demo/mock/catalog_kr.json'));import collections;print(collections.Counter(b['book_format'] for b in d))"` → `전자책|오디오북|챗북` 만 · `grep -c '"description"\|"curator_note"' demo/mock/catalog_kr.json` 0 · `grep -rn 'itemknn' demo/mock demo/js | wc -l` 0 · `grep -rhoE '"type": "[a-z_]+"' demo/mock/recommend_*.json demo/mock/showcase.json | sort -u` ⊂ 6종 (6) `python3 -c "import json;m=json.load(open('demo/mock/_manifest.json'));print(m['seed'], sorted(m['outputs']))"` + `stat -f %m demo/scripts/make_mock.py demo/mock/showcase.json`(산출물 mtime ≥ 스크립트).
3. **테스트·린트·스모크:** `uv run pytest --no-header 2>&1 | tail -2`(failed 0) · `uv run pytest tests/demo --no-header 2>&1 | tail -1` · `uv run ruff check demo/scripts tests/demo` · `make smoke`(PASS 3줄).
4. **크기·리터럴·구문:** `wc -l demo/js/*.js demo/js/screens/*.js demo/css/*.css | awk '$1>250'`(빈 출력) · `grep -rnE '#[0-9a-fA-F]{3,8}\b' demo/css --include='*.css' | grep -v tokens.css`(base.css v1 잔존 `#1F2024`·`#000`·`#FFFFFF`/`#1A1A1A`·`#1A1B1E` 만 허용 — 목록 기록, 그 외 있으면 소유 플랜 재위임) · `for f in demo/js/*.js demo/js/screens/*.js; do node --check "$f"; done` · `grep -rn 'innerHTML' demo/js/screens | wc -l` 0 · `grep -rn 'href="javascript' demo/js | wc -l` 0.
5. **codegraph:** `cd millie-rec && codegraph sync .` 실행 결과 1줄(불가 시 사유).
6. **DEMO-01~09 재확인표:** ID · 판정 명령 · 결과 ✅/❌ — DEMO-01(1번 grep 0 + `grep -c 'const API_BASE = ""' demo/js/api.js` 1) · DEMO-02(2번 6항) · DEMO-03(`grep -c '\["' demo/js/router.js` 8 + Task 2 완주) · DEMO-04(`python3 -c` progress 값 + Task 2 체크 3·건너뛰기) · DEMO-05(Task 2 체크 4·5·6 + `grep -c 'data-type=' demo/css/home.css` ≥ 6) · DEMO-06(Task 2 체크 1) · DEMO-07(Task 2 체크 7·8) · DEMO-08(Task 2 체크 13) · DEMO-09(Task 2 완주 + 캡처 + 3번 pytest·smoke).
7. **Codex:** `../.claude/rules/codex-review.md` 표 — `demo/`·문서 = 생략. 한 줄 기록: "Codex 리뷰 생략(demo 레인, 계약·serving 변경 0 — `src/**` diff 없음: `git status --short src | wc -l` 이 Phase 5 변경만 포함함을 확인)".
❌ 항목은 "재위임 목록" 표(소유 플랜 · 파일 · 기대 · 실제 · 브리프 1줄)에 적고, 이 플랜은 코드를 고치지 않는다.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && test -f .planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md && grep -c 'contract-sync\|DEMO-0[1-9]\|make smoke\|uv run pytest' .planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md && cd demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' --include='*.js' --include='*.json' --include='*.html' --include='*.py' --include='*.md' --include='*.css' . | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `06-VERIFICATION-NOTES.md` 존재, ≥60줄, 절 제목 `## 1. 구 형태 grep` `## 2. /contract-sync` `## 3. pytest·ruff·smoke` `## 4. 크기·리터럴·구문` `## 5. codegraph` `## 6. DEMO-01~09 재확인` `## 7. Codex` `## 8. 재위임 목록` 8개 존재
    - 전역 구 형태 grep 출력 `0` 이 노트에 인용됨
    - `/contract-sync` 6항 표에 ❌ 0(❌ 가 있으면 재위임 목록에 대응 행 존재)
    - `uv run pytest --no-header` 요약에 `failed` 없음 · `make smoke` 출력 `smoke: /health 200`·`smoke: / 200`·`smoke: /api/recommend 200` 3줄 인용
    - `wc -l` 250 초과 0 · 색 리터럴 tokens.css 외 목록이 base.css v1 잔존만
    - DEMO-01~09 재확인표 9행 전부 존재(결과 열 채움)
  </acceptance_criteria>
  <done>기계 게이트 전부 실행·인용, 실패는 재위임 목록으로 정리.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Advisor Playwright 완주(repo 밖 스크래치패드) + ?capture 스크린샷 6장 + api 모드 크래시 0 관측</name>
  <files>.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md</files>
  <read_first>
    - ../.claude/rules/demo.md "검증" 절(Advisor 는 repo 밖 스크래치패드 Playwright · `wait_until="domcontentloaded"` + 셀렉터 대기 · 이미지 CDN 대기 금지)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §7 캡처 계획(6장 · `?capture=1` 폰만 · `?capture=2` 폰+인스펙터) · §8 "검증(Advisor)" 동선
    - .planning/phases/06-demo-rebuild/06-06-SUMMARY.md 체크리스트 17항목 표(Worker 수동 결과 — 같은 순서로 자동화)
    - ../.claude/rules/data.md "`playwright` 는 dev 전용 · `uv run --with playwright …`(pyproject 미등재 임시 설치)"
  </read_first>
  <action>
Advisor 가 순서대로 실행한다(사람 확인 전 자동 부분):
(1) `make demo-serve`(8080) 를 백그라운드로 띄운다. 스크래치패드 `/private/tmp/claude-501/-Users-shinwonchul-Documents----------/efbff966-5b6b-43c9-8c6d-4b679a3b0218/scratchpad/pw_demo06/run.py` 에 Playwright 스크립트를 두고 `uv run --with playwright python run.py` 로 실행(`playwright install chromium` 이 필요하면 같은 임시 환경에서). `page.on("console", …)` 로 `error` 수준 메시지, `page.on("pageerror", …)` 로 uncaught 를 리스트에 모은다. 모든 이동은 `page.goto(url, wait_until="domcontentloaded")` + `page.wait_for_selector(...)`.
(2) 동선(06-06 체크리스트 1~14 순서): `#/`(`.dt tbody tr` 4개 대기 · 본문에 `split_mode:` 텍스트) → `[data-to="#/onboarding"]` → `[data-act="start"]` → S1 `.option` 첫째 → `[data-act="next"]` → S2 `.option:not([disabled])` 3개 → next → S3 `.option` 3번째(베스트셀러) → next → S4 next → S5 `.book` 30개 대기 → 5개 클릭 → next → S6 `[data-act="toHome"]` → `#/home`(`.row[data-row^="anchor_"]` 대기 · `.badge[data-type]` ≥1 · `.tile__reason` ≥1 · 인스펙터 텍스트 `rec_`) → `input[value="pop"]` check → 인스펙터 텍스트 `pop_v1` → `input[value=""]` check → `.row[data-row^="anchor_"] .tile` 첫째 클릭 → 해시 `#/book/` · `.sheet` 대기 → `[data-act="read"]` → 해시 `#/reader/` · `[data-act="read10"]` 2회 → 텍스트 `도달 ✓` → `[data-act="complete"]` → `.rating__star[data-stars="4"]` → `.row[data-row="after_completion"]` 대기 → 다른 `.tile` → `[data-act="read"]` → `[data-to="#/home"]` → `.row[data-row="continue_reading"]` 대기 → `[data-to="#/library"]` → `.lib__tab` 3 · `.timeline__item` 1 → `[data-to="#/refresh"]` → `.navbar__label` 텍스트 `취향 다시 설정` → S1~S5(다른 카테고리) → S5 CTA 텍스트 `새 취향으로 추천 받기` → S6 → home → `[data-to="#/library"]` → `.timeline__item` 2 → `[data-act="mydata"]` → `.modal pre` → `[data-act="closeModal"]` → `[data-act="withdraw"]` → `[data-act="withdrawConfirm"]` → `.banner` 텍스트 `비개인화 인기 도서` · `.row` 2개 · 인스펙터 `fallback_v1` → `#/dashboard`(`.kpi__card` 6 · `.dt tbody tr` 4 · 텍스트 `BUDGET 200ms`) → `#/` → `[data-preset="skipUser"]` → `.banner` 텍스트 `비개인화 인기 도서`(06-06 #14) → `#/` → `[data-preset="newUser"]` → `.row[data-row^="anchor_"]` → `#/` → `[data-preset="resetUser"]` → `#/library` `.timeline__item` 2.
(3) 스크린샷(`device_scale_factor=2`) 6장을 스크래치패드 `pw_demo06/shots/` 에: ① `?source=mock&capture=2#/onboarding` S3 선택 후 → `p1_onboarding_s3_inspector.png` ② `?source=mock&capture=1#/home` 개인화 → `p3_home_badges_anchor.png` ③ `input[value="hybrid"]` / `input[value="hybrid_div"]` 각각 → `p5_home_hybrid.png` · `p5_home_hybrid_div.png` ④ D4 완독 직전 · 별점 모달 · `after_completion` 행 → `p3_reader_1.png` `p3_reader_2.png` `p3_reader_3.png` ⑤ `?capture=1#/library`(타임라인 2) → `p3_library_timeline.png` ⑥ `#/dashboard` · `#/` 전폭 → `p4_dashboard.png` · `p4_showcase.png`. repo 에는 넣지 않는다.
(4) `?source=mock&capture=1` 에서 `page.is_visible(".topbar")`·`.inspector` 둘 다 False, `capture=2` 에서 `.inspector` True 를 단정.
(5) api 관측: `make serve`(8000) 후 `http://localhost:8000/?source=api#/` → `#/home` 만 이동 — `pageerror` 0 · 404 URL 목록(`page.on("response")` status 404) · `#/home` 행 수·인스펙터 `model_version` 텍스트를 기록. 완주 판정 아님.
(6) 결과를 `06-VERIFICATION-NOTES.md` `## 9. Playwright 완주`(콘솔 에러 목록·404 목록) `## 10. 캡처 6장`(경로) `## 11. api 모드 관측` 절에 원문 기록. ❌ 는 `## 8. 재위임 목록` 에 추가. 그 뒤 아래 `<how-to-verify>` 를 사용자에게 제시하고 멈춘다.
  </action>
  <what-built>06-01~06-06 이 만든 데모 8페이지(`demo/`) 의 mock 모드 완주 자동화 결과 + 캡처 6장 + api 모드 관측. Worker 는 자동화 금지였으므로 Playwright 는 이 Advisor 단계에서만(`../.claude/rules/demo.md` 검증 절).</what-built>
  <how-to-verify>
    1. `06-VERIFICATION-NOTES.md` §9 의 콘솔 에러 목록이 비어 있는지(`favicon.ico` 404 만 허용 목록) 확인한다.
    2. §10 의 스크린샷 6장을 열어 화면 02 §7 캡처 계획과 대응하는지 눈으로 확인한다 — 특히 `p3_home_badges_anchor.png` 에 배지·"『…』을 좋아하셨다면" 행, `p4_showcase.png` 에 비교표 4행·`split_mode: holdout`·본인 5권 → 추천 10.
    3. `capture=1` 에서 상단바·인스펙터가 없고 `capture=2` 에서 인스펙터가 남는지(§9 단정 결과) 확인한다.
    4. §11 api 관측의 404 목록이 Phase 5 '서빙 Must 완성'(.planning/phases/05-must/) 진행 상황과 맞는지(아직 없는 엔드포인트만) 확인한다.
  </how-to-verify>
  <resume-signal>"approved"(완주 콘솔 에러 0 · 캡처 6장 저장) 또는 실패 항목과 재위임 대상 플랜(06-0N)을 한글로 적어 주세요.</resume-signal>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && grep -c '## 9. Playwright 완주\|## 10. 캡처 6장\|## 11. api 모드 관측' .planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md && ls /private/tmp/claude-501/-Users-shinwonchul-Documents----------/efbff966-5b6b-43c9-8c6d-4b679a3b0218/scratchpad/pw_demo06/shots/*.png | wc -l && git status --short pyproject.toml uv.lock | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `06-VERIFICATION-NOTES.md` 에 `## 9.` `## 10.` `## 11.` 절 3개 존재, §9 에 "콘솔 에러 0건" 또는 목록·"404 목록" 소절 존재
    - 스크래치패드 `pw_demo06/shots/*.png` ≥ 9장(6 항목 중 ③·④ 가 복수 컷) · repo 안(`git status --short | grep -i png`) 0
    - `capture=1`/`capture=2` 가시성 단정 결과 2줄 기록
    - api 관측 절에 `pageerror 0` 과 404 URL 목록(빈 목록 가능) 존재
    - `git status --short pyproject.toml uv.lock | wc -l` == 이 플랜 전 값(임시 설치가 lock 을 바꾸지 않음)
  </acceptance_criteria>
  <done>mock 완주 콘솔 에러 0(사용자 확인) · 캡처 6장 스크래치패드 저장 · api 관측 기록. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 3: PROGRESS.md 미결 append(2줄 + /contract-sync 항목 제안 1줄) · 인계 정리</name>
  <files>../PROGRESS.md, .planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md</files>
  <read_first>
    - ../PROGRESS.md (**Edit 직전에 다시 읽는다** — Phase 5 세션이 동시에 편집한다, ../CLAUDE.md §6. 미결 절 제목·형식 확인)
    - ../CLAUDE.md §6 세션 규칙(다른 세션은 PROGRESS.md 미결에 제안으로만)
    - .planning/phases/06-demo-rebuild/06-CONTEXT.md D-02(Makefile mock·cli.py mock 부재) · D-09(DEMO-09 문구 분리) · D-10(onboarding_meta 동일성 검사 → /contract-sync)
    - ../.claude/rules/references.md (이름 + 경로 병기)
  </read_first>
  <action>
`../PROGRESS.md` 를 **다시 읽은 뒤** 미결 절 끝에 append 만(기존 줄 무수정):
```
- [제안·Phase 6 세션] `Makefile mock` 타깃이 `uv run python -m millie_rec.app.cli mock` 을 부르지만 `app/cli.py` 에 `mock` 명령이 없다 — 타깃 본문을 `uv run python demo/scripts/make_mock.py` 로 바꾸거나 `cli.py` 에 위임 명령 추가(Advisor 결정). 생성기 진입점은 현재 직접 실행만(06-CONTEXT D-02, .planning/phases/06-demo-rebuild/06-CONTEXT.md).
- [제안·Phase 6 세션] `.planning/REQUIREMENTS.md` DEMO-09 문구를 "`?source=mock` 완주(Phase 6 '데모 재구성' 판정) + `?source=api` 완주(Phase 7 '배포' Day 2 스켈레톤 전 로컬 게이트)" 로 1줄 수정(06-CONTEXT D-09). `/contract-sync` 에 항목 7 "`serving/onboarding_meta.json` ↔ `demo/config/onboarding.json` 의 reading_times·criteria label·subcategories 텍스트 동일성" 추가 제안(06-CONTEXT D-10 · 05-CONTEXT D-14).
```
`06-VERIFICATION-NOTES.md` 끝에 `## 12. 인계` 절: Phase 7 '배포'(.planning/ROADMAP.md "Phase 7: 배포") 사전 게이트 = `make serve` + `?source=api` 완주(이 노트 §11 의 404 목록이 사라져야 함) · Phase 8 'PDF 제출물' 캡처 원본 경로(스크래치패드 → `report/figures/` 복사는 Phase 8) · 재위임 목록 상태(전부 해소면 "0건") · 커밋은 사용자 승인 후 Phase 5 산출물과 일괄(`git status --short demo tests/demo | wc -l` 수치 기록). STATE.md 갱신은 GSD 도구(`gsd-tools.cjs state …`)의 최소 갱신만 — 직접 편집하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && grep -c 'uv run python demo/scripts/make_mock.py' ../PROGRESS.md && grep -c 'DEMO-09' ../PROGRESS.md && grep -c 'onboarding_meta.json' ../PROGRESS.md && grep -c '## 12. 인계' .planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md && git diff --stat ../PROGRESS.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[제안·Phase 6 세션\]' ../PROGRESS.md` == 2 · `grep -c 'uv run python demo/scripts/make_mock.py' ../PROGRESS.md` ≥ 1 · `grep -c 'DEMO-09' ../PROGRESS.md` ≥ 1 · `grep -c 'onboarding_meta.json' ../PROGRESS.md` ≥ 1
    - `git diff ../PROGRESS.md | grep '^-' | grep -v '^---' | wc -l` == 0 (삭제·수정 줄 0 — append 만)
    - `06-VERIFICATION-NOTES.md` 에 `## 9.`~`## 12.` 절 존재 · 캡처 경로 6개 이상 나열 · 재위임 목록 상태 1줄
    - `git status --short src | wc -l` 이 이 페이즈 전(Phase 5 변경) 과 같다 — 이 플랜이 `src/**` 를 건드리지 않았음
    - 커밋 0: `git log -1 --format=%s` 가 이 페이즈 시작 전과 같다
  </acceptance_criteria>
  <done>PROGRESS 미결 2줄 + 제안, 인계 절, 코드 무수정, 커밋 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Advisor Playwright(스크래치패드) → 로컬 데모(8080/8000) | repo 밖 임시 설치(`uv run --with playwright`) — 런타임·Dockerfile 미포함 |
| `../PROGRESS.md` 동시 편집(Phase 5 세션) | Edit 전 재읽기 · append 만 |
| 스크린샷 → repo | 넣지 않는다(Phase 8 이 `report/figures/` 로) — 밀리 표지 이미지가 캡처에 포함되는 문제는 Phase 8 판단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | 게이트 실패를 이 플랜이 직접 고쳐 소유권·검증 분리가 깨짐 | mitigate | `must_not_touch demo/**·tests/**` · 재위임 목록으로만 · acceptance `git status --short src` 불변 |
| T-06-07-02 | Tampering | `PROGRESS.md` 동시 편집 충돌로 Phase 5 세션 기록 유실 | mitigate | Edit 직전 재읽기 · append 만(`git diff` 삭제 줄 0 단정) |
| T-06-07-03 | Information Disclosure | 캡처에 밀리 표지 이미지·가명 키 | accept | 스크래치패드 보관(repo 밖) · 표지는 밀리 CDN 핫링크 화면 표시(저작권 고지는 D8) · PDF 삽입 판단은 Phase 8 |
| T-06-07-04 | Denial of Service | Playwright 가 이미지 CDN 대기로 멈춤 | mitigate | `wait_until="domcontentloaded"` + 셀렉터 대기만(`demo.md`) |
| T-06-07-05 | Repudiation | "완주" 주장에 증거 없음 | mitigate | 콘솔 에러 목록·404 목록·스크린샷 경로·명령 출력을 노트에 원문 인용 |
| T-06-07-06 | Elevation of Privilege | `uv run --with playwright` 가 pyproject/lock 을 바꿈 | mitigate | `--with` 는 임시 환경 — `git status --short pyproject.toml uv.lock` 이 이 플랜 전후 동일 |
</threat_model>

<verification>
- 전역 구 형태 grep 0 · `/contract-sync` 6항 ✅ · `uv run pytest --no-header` failed 0 · `uv run ruff check demo/scripts tests/demo` 0 · `make smoke` PASS 3줄 · `wc -l` ≤250 · 색 리터럴 tokens.css 외 = base.css v1 잔존만 · `node --check` 전부 0
- Playwright mock 완주 콘솔 에러 0 · 캡처 6장 경로 · `capture=1|2` 표시 확인 · api 모드 크래시 0 + 404 목록(관측)
- DEMO-01~09 재확인표 9행 · 재위임 목록(0건 또는 소유 플랜별 브리프)
- PROGRESS 미결 append 2줄(+제안) · `src/**` 무변경 · 커밋 0
</verification>

<success_criteria>
- **DEMO-09**(06-CONTEXT D-09 판정): `?source=mock` 쇼케이스→대시보드 완주 콘솔 에러 0 + `?capture=1|2` 동작 + `/contract-sync` 전부 ✅ + `uv run pytest -q`·`make smoke` PASS
- DEMO-01~08 재확인표 전부 ✅(❌ 는 재위임 후 재실행하여 ✅ 로 — 그 전에는 Phase 6 미완)
- `?source=api` 완주는 Phase 7 '배포'(.planning/ROADMAP.md) 사전 게이트로 이관 기록 · Makefile/REQUIREMENTS 정정은 PROGRESS 미결 제안으로 Phase 5 세션 Advisor 에 인계
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-rebuild/06-07-SUMMARY.md` — 게이트 결과 요약·캡처 경로·재위임 상태·PROGRESS append 원문·커밋 0. 마지막에 `## ▶ Next Up`(`../.claude/docs/workflow.md`): `/gsd-verify-work 6 — Phase 6 '데모 재구성'(.planning/ROADMAP.md)` · 대안 `/gsd-plan-phase 7 — Phase 7 '배포'(.planning/ROADMAP.md, 사전 게이트 = ?source=api 완주)` · `/gsd-code-review 6`.
</output>
