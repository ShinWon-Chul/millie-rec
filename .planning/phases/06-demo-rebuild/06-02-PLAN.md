---
phase: 06-demo-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - demo/js/router.js
  - demo/js/app.js
  - demo/js/actions.js
  - demo/js/presets.js
  - demo/js/screens/ui.js
  - demo/js/screens/onboarding_step.js
  - demo/js/screens/s0_start.js
  - demo/js/screens/s6_persona.js
  - demo/index.html
  - demo/css/base.css
  - demo/config/onboarding.json
  - demo/README.md
autonomous: true
requirements: [DEMO-03, DEMO-04, DEMO-09]
lane: Demo (라우터·앱 셸 Worker — wave 2 병렬 4 중 1)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(05 관례, 사용자 지시 2026-09-05)
must_not_touch:
  - src/**
  - tests/**
  - Makefile
  - pyproject.toml
  - Dockerfile
  - artifacts/**
  - results/**
  - report/**
  - demo/js/api.js            # 06-03
  - demo/js/mock.js           # 06-03
  - demo/js/mock_store.js     # 06-03
  - demo/js/inspector.js      # 06-06
  - demo/js/screens/s7_home.js   # 06-06 이 d2_home.js 로 개명·삭제
  - demo/js/screens/s8_detail.js # 06-06 이 d3_detail.js 로 개명·삭제
  - demo/js/screens/d*.js     # 06-04·06-05·06-06
  - demo/css/tokens.css       # 06-05
  - demo/css/home.css         # 06-06
  - demo/css/inspector.css    # 06-06
  - demo/css/onboarding.css
  - demo/css/reader.css       # 06-04
  - demo/css/library.css      # 06-04
  - demo/css/dashboard.css    # 06-05
  - demo/mock/**              # 06-01 생성기 산출물 — 손편집 금지
  - demo/fallback/popular.json
  - demo/scripts/make_mock.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - ../PROGRESS.md
  - ../.assets/**

must_haves:
  truths:
    - "해시 8개(#/ #/onboarding #/home #/book/:id #/reader/:id #/library #/refresh #/dashboard)가 router.parse 로 {page, params} 가 되고 알 수 없는 해시는 showcase 로 간다(DEMO-03)"
    - "app.js 는 state 1개·setState 1개·render 1개 패턴이며 D7·D8 은 #page(전폭), 나머지는 #phone + #inspector 에 그린다(화면 02 §1·§4)"
    - "취향 설정 7단계(S0~S6)는 config/onboarding.json 1벌 + onboarding_step.js 공용 렌더러로 그려지고, S0 건너뛰기는 consent=false 로 #/home 에 진입한다(DEMO-04)"
    - "#/refresh 는 같은 단계 머신을 resetting=true 로 재사용한다 — 헤더 '취향 다시 설정' · S0 생략 · CTA '새 취향으로 추천 받기' · 진입 시 preference_restarted(화면 02 §2 D6)"
    - "log(event_type, fields) 는 EventIn 형태(ts 필드, event_id UUID, payload 문자열 dict)로 sessionStorage millie_events 에 쌓고 api.postEvents 로 50개 단위 전송한다 — timestamp 키 0건"
    - "?capture=1 은 body.is-capture(상단바·인스펙터 숨김), ?capture=2 는 body.is-capture-2(상단바만 숨김·인스펙터 유지) 로 동작한다(DEMO-09 캡처 모드)"
    - "userKey 는 boot 에서 localStorage millie_user_key 의 UUIDv4 로 항상 채워지고(없으면 생성) 익명 상태에서도 이벤트가 user_key 를 가진다"
  artifacts:
    - path: "demo/js/router.js"
      provides: "ROUTES 표 8개 · parse(hash) → {page, params} · listen(onRoute)"
      exports: ["parse", "listen", "ROUTES"]
      max_lines: 30
    - path: "demo/js/app.js"
      provides: "state v2 · setState · render(#phone/#inspector/#page 분기) · log/flush(EventIn·millie_events) · onRoute(페이지별 데이터 로드) · boot(userKey·source·steps·meta·capture)"
      max_lines: 250
    - path: "demo/js/actions.js"
      provides: "createActions(ctx) → data-act 딕셔너리(부록 B 전부) + 온보딩 단계 머신(ORDER S1~S5 · togglePick · next · completeOnboarding)"
      exports: ["createActions"]
      max_lines: 250
    - path: "demo/js/presets.js"
      provides: "run(name, ctx) — newUser(자동 온보딩 IT·소설·철학 + 베스트셀러 + 시드 5) · skipUser(consent=false) · resetUser(newUser + reader_open 2 + 재설정 스냅샷 2)"
      exports: ["run", "PRESET_NAMES"]
      max_lines: 120
    - path: "demo/js/screens/ui.js"
      provides: "기존 export 유지 + tabbar(active) · demoLabel(text) · banner(text) · modal(inner)"
      exports: ["esc", "cover", "badge", "josa", "icon", "statusbar", "navbar", "cta", "tabbar", "demoLabel", "banner", "modal"]
      max_lines: 130
    - path: "demo/index.html"
      provides: "CSS 링크 8개(reader·library·dashboard 추가) · #phone · #inspector · #page · #toast · 상단바 model_version"
      contains: "id=\"page\""
    - path: "demo/css/base.css"
      provides: "기존 + .tabbar · .demo-label · .banner · .toast · .modal-wrap/.modal · body.is-page(#page 전폭·.layout 숨김) · body.is-capture-2 · 색 리터럴 0(토큰만)"
      max_lines: 250
    - path: "demo/config/onboarding.json"
      provides: "progress 캡처 실측(S1 0.0 · S2 null · S3 0.4 · S4 0.4 · S5 0.84) · S5 optionsFrom 'GET /api/candidates/onboarding' · 텍스트 외 변경 없음"
      contains: "\"progress\": 0.84"
    - path: "demo/README.md"
      provides: "v2 실행법(make demo-serve · make serve) · URL 파라미터(source·api·capture 1|2) · 8페이지 라우트 표 · 생성기 명령 uv run python demo/scripts/make_mock.py · 구 형태 문자열 0"
      min_lines: 40
  key_links:
    - from: "demo/js/app.js"
      to: "demo/js/router.js"
      via: "router.listen(onRoute) — hashchange → setState({route}) → 페이지별 로드"
      pattern: "listen\\("
    - from: "demo/js/app.js"
      to: "demo/js/api.js"
      via: "api.resolveSource · loadSteps · health · loadMeta · getRecommend · postEvents … (부록 C 시그니처)"
      pattern: "api\\.(getRecommend|postEvents|resolveSource)"
    - from: "demo/js/actions.js"
      to: "demo/config/onboarding.json"
      via: "state.steps[].field/min/max 로 단계 머신 — 화면 텍스트는 JSON 에서만"
      pattern: "steps"
    - from: "demo/js/app.js"
      to: "sessionStorage millie_events"
      via: "log() → 큐 push → flush() 50개 단위 api.postEvents"
      pattern: "millie_events"
---
<objective>
Phase 6 '데모 재구성'(.planning/ROADMAP.md "Phase 6: 데모 재구성") 의 **앱 셸**을 만든다: 해시 라우터(`js/router.js` ≤30줄) · 새 `js/app.js`(state v2 + setState + render + log/flush + 라우트별 로드) · `js/actions.js`(data-act 위임 딕셔너리 + 온보딩 단계 머신) · `js/presets.js`(D8 프리셋 3) · `index.html`/`base.css` 셸(탭바·데모 라벨·배너·토스트·모달·전폭 페이지·캡처 2모드) · D1/D6 공유에 필요한 `onboarding_step.js`·`s0_start.js`·`s6_persona.js` 최소 수정 · `config/onboarding.json` 진행바 실측값 · `README.md` 재작성.

Purpose: ROADMAP Success Criteria 3 — "해시 라우팅 8페이지가 `state`+`render()` 패턴으로 동작 · 취향 설정 7단계는 `config/onboarding.json` 1벌 + 공용 렌더러 · 건너뛰기는 `consent=false`"(**DEMO-03·DEMO-04**) + Success Criteria 5 의 `?capture=1|2` 모드(**DEMO-09**, 완주 판정은 06-07). 06-CONTEXT D-04(`app.js` 새로 쓴다 — 계승은 state/setState/render/log 패턴만, S0~S6 머신은 단계 파일로) 이행. PDF 캡처 6장의 프레임(화면 02 §7).
Output: 신규 4(`router.js` `actions.js` `presets.js` + `app.js` 재작성) · 수정 6(`ui.js` `onboarding_step.js` `s0_start.js` `s6_persona.js` `index.html` `base.css`) · `config/onboarding.json` 값 5개 · `README.md` 재작성. 커밋 0.

**wave 2 병렬 불변:** `api.js`·`mock.js`(06-03) · `d4/d5`(06-04) · `d7/d8`·`tokens.css`(06-05) 는 같은 시각에 다른 Worker 가 만든다 — 이 플랜은 그 파일을 **읽지도 고치지도 않고** 아래 `<interfaces>` 부록의 시그니처만 믿고 import 한다. `d2_home.js`·`d3_detail.js`·`inspector.js` 는 wave 3(06-06)이 만든다 — `app.js` 는 그 이름으로 import 하고, wave 2 동안 브라우저 완주는 기대하지 않는다(정적 검증 `node --check` + grep). 브라우저 완주는 06-06 체크리스트·06-07 게이트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-rebuild/06-CONTEXT.md
@.planning/phases/06-demo-rebuild/06-PATTERNS.md
@.planning/phases/06-demo-rebuild/06-01-PLAN.md
@../.claude/rules/demo.md
@../.claude/rules/references.md
@../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md
@../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md
@src/millie_rec/serving/schemas.py
@src/millie_rec/contracts.py
@demo/js/app.js
@demo/js/screens/ui.js
@demo/js/screens/onboarding_step.js
@demo/js/screens/s0_start.js
@demo/js/screens/s6_persona.js
@demo/index.html
@demo/css/base.css
@demo/config/onboarding.json
@demo/README.md

<file_ownership>
## Phase 6 파일 소유 표 (겹침 0 — 같은 wave 는 서로소)

| 파일 | 만듦/고침/삭제 | 플랜 | wave |
|---|---|---|---|
| `demo/scripts/make_mock.py` · `tests/demo/test_make_mock.py` · `demo/mock/*.json`(13개 — 계약 10 + catalog_kr·neighbors_kr + _manifest) · `demo/mock/books.json` 삭제 | 재작성·신규·재생성 | 06-01 | 1 |
| `demo/js/router.js` · `demo/js/app.js` · `demo/js/actions.js` · `demo/js/presets.js` · `demo/js/screens/ui.js` · `demo/js/screens/onboarding_step.js` · `demo/js/screens/s0_start.js` · `demo/js/screens/s6_persona.js` · `demo/index.html` · `demo/css/base.css` · `demo/config/onboarding.json` · `demo/README.md` | 신규·재작성·최소 수정 | **06-02(이 플랜)** | 2 |
| `demo/js/api.js` · `demo/js/mock.js` · `demo/js/mock_store.js` | 재작성·신규 | 06-03 | 2 |
| `demo/js/screens/d4_reader.js` · `demo/js/screens/d5_library.js` · `demo/css/reader.css` · `demo/css/library.css` | 신규 | 06-04 | 2 |
| `demo/js/screens/d7_dashboard.js` · `demo/js/screens/d8_showcase.js` · `demo/css/dashboard.css` · `demo/css/tokens.css` | 신규·토큰 추가 | 06-05 | 2 |
| `demo/js/screens/d2_home.js`(← `s7_home.js` 삭제) · `demo/js/screens/d3_detail.js`(← `s8_detail.js` 삭제) · `demo/js/inspector.js` · `demo/css/home.css` · `demo/css/inspector.css` | 신규·삭제·확장 | 06-06 | 3 |
| `.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md` · `../PROGRESS.md`(미결 절 2줄 append 만) | Advisor | 06-07 | 4 |
</file_ownership>

<interfaces>
<!-- 공통 인터페이스 부록 — 06-02 ~ 06-06 다섯 플랜에 **동일한 텍스트**로 들어간다. 한 곳만 바꾸면 병렬 Worker 가 어긋난다. -->
<!-- 정본: 화면 구성 02 §4 상태 모델 v2 · §5 이벤트 13종 · 화면 구성 01 §6 · serving/schemas.py(응답 형태) · contracts.py(이름). -->

## A. `state` v2 — `demo/js/app.js` 의 단일 상태 객체 (06-02 소유, 다른 파일은 읽기만)
```js
const state = {
  route: { page: "showcase", params: {} },  // router.parse(location.hash). page ∈ showcase|onboarding|home|book|reader|library|refresh|dashboard
  source: "mock",                            // "mock" | "api"
  userKey: null,                             // localStorage["millie_user_key"] UUIDv4 — boot 에서 항상 채움(익명도 키 있음)
  cell: null,                                // "A" | "B" — postPreferences 응답 · getUserState 응답
  consent: null,                             // null | true | false (건너뛰기 = false)
  screen: "S0",                              // D1/D6 내부 단계 "S0".."S6"
  resetting: false,                          // D6(#/refresh) 모드 — 헤더 "취향 다시 설정", S0 생략, CTA "새 취향으로 추천 받기"
  prefs: { readingTime: null, categories: [], criterion: null, subcategories: [], seedBooks: [] },  // criterion = S3 **라벨**(config 텍스트)
  steps: [],                                 // config/onboarding.json .steps
  meta: null,                                // OnboardingMeta {survey_variant, reading_times[], criteria[{id,label}], categories[{name,supported,subcategories[]}]}
  candidateSet: { id: null, items: [], impressions: [] },  // items = CandidateItem[], impressions = [{book_id, position, selected}]
  snapshots: [],                             // [{ id, createdAt, prefs, persona, cell }] — append only(재설정도 push)
  model: null,                               // null = 셀 배정(A→hybrid, B→hybrid_div) · VARIANTS 문자열 = 인스펙터 라디오 강제(forced)
  recommend: null,                           // RecommendOut 원문. api 클라이언트 fallback 시 + client_fallback_reason: string
  detail: null,                              // { item: ItemOut, rowId } | null — D3 바텀시트. rowId ∈ RecommendOut.rows[].row_id | "library"(D5 서재 타일 — item 은 LibraryBook 기반 최소 ItemOut, 부록 B card)
  reading: null,                             // { bookId, progressPct, virtualMinutes, qualified: false, completed: false } | null — D4
  ratings: {},                               // { [bookId]: stars }
  library: { added: [], reading: [], completed: [] },  // UserStateOut.library (LibraryBook[])
  libraryTab: "added",                       // D5 활성 탭 added|reading|completed
  userState: null,                           // UserStateOut 원문(D5 타임라인·U_t)
  mydata: null,                              // UserDataOut 원문(D5 "내 데이터 보기" 모달)
  dashboard: null,                           // DashboardOut 원문(D7)
  showcase: null,                            // ShowcaseOut 원문(D8)
  health: null,                              // HealthOut | null (상단바 model_version)
  nearlineLagSec: null,                      // RecommendOut.nearline_lag_s
  events: [],                                // 인스펙터 로그 [{ t: "HH:MM:SS", event_type, detail }] 최근 200
  toast: null,                               // string | null — 3초 후 null
  banner: null,                              // string | null — D2 상단 Banner 문구(예: "비개인화 인기 도서")
  modal: null,                               // null | "rating" | "mydata" | "withdraw"
};
```
원칙: `setState(patch)` 하나 · `render()` 하나 · 화면 파일은 `render(state) → HTML 문자열` 만, fetch·상태 변경 금지(`../.claude/rules/demo.md`).

## B. `data-act` 액션 이름 (화면 파일이 찍고 `app.js`/`actions.js` 가 위임 처리 — click 1곳 + change 1곳)
| data-act | 추가 data-* | 처리 | 이벤트(EVENT_TYPES 13종) |
|---|---|---|---|
| `start` | — | S0 → S1 | `preference_started` |
| `skip` | — | consent=false → `#/home`(level 3) | — |
| `back` | — | 단계 뒤로(S1 에서 resetting 이면 `#/home`) | — |
| `pick` | `data-step` `data-val` | S1~S4 선택 토글 | `preference_step`(payload {step}) |
| `pickbook` | `data-book` `data-pos` | S5 시드 토글 + impressions[].selected | `preference_book_selected` / `preference_book_deselected` |
| `next` | — | 다음 단계(S4→S5 진입 시 getCandidates + `impression`×30, S5 완료 시 postPreferences) | `preference_completed` |
| `toHome` | — | S6 CTA → `#/home` | — |
| `nav` | `data-to="#/home"` 등 해시 | `location.hash = to` | — |
| `preset` | `data-preset="newUser"\|"skipUser"\|"resetUser"` | presets.run(name) | (프리셋이 만드는 이벤트) |
| `card` | `data-book` `data-row` `data-pos` | detail 설정 → `#/book/:id`. 행 탐색은 `(state.recommend?.rows ?? [])` 로 null 방어(철회 직후 `recommend: null` 이어도 TypeError 0). `data-row="library"`(D5 서재 타일) 이면 `state.library` 의 added·reading·completed 에서 `LibraryBook{book_id,title,image_url}` 을 찾아 ItemOut 형태의 최소 detail(`book_id`·`title`·`image_url` 채움, `authors`·`reason`·`badge`·`source`·`book_format`·`difficulty` null, `source_channels` [], `score`·`position` 0) 을 만들고 `rowId: "library"` 로 둔다(d3 는 rowId 를 "내 서재" 로 표시). 못 찾으면 무동작 | `detail_click`(서재 타일은 `row_id`·`position` 없이 `surface: "library"`) |
| `closeSheet` | — | `#/home` | — |
| `read` | `data-book` | `#/reader/:id` 진입(reading 초기화) | `reader_open` |
| `library` | `data-book` | 서재 담기 | `library_add`(surface "main") |
| `read10` | — | progressPct +8, virtualMinutes +10; ≥15분 첫 도달 시 1회 | `qualified_read` |
| `complete` | — | completed=true, modal="rating" | `completion` |
| `rate` | `data-stars` 1..5 | postRating → toast "다음 책을 준비하고 있어요" → `#/home` | `rating` |
| `rateLater` | — | 토스트 → `#/home` | — |
| `libTab` | `data-tab` | libraryTab 변경 | — |
| `mydata` | — | getUserData → modal="mydata" | — |
| `withdraw` | — | modal="withdraw" | — |
| `withdrawConfirm` | — | deletePersonalization → consent=false → banner "비개인화 인기 도서" → `#/home` | — |
| `closeModal` | — | modal=null | — |
| `model` | (radio `value`) | state.model=value → getRecommend(forced) | — |
| `noop` | — | — | — |
- D6 시작(`#/refresh` 진입)은 라우터가 `preference_restarted` 를 기록한다. D2 행 렌더 직후 `impression`(row_id·position·recommendation_id) 은 `app.js` 가 recommendation_id 당 1회 기록한다.

## C. `api.js` 공개 함수 (06-03 소유) — 전부 `async`, 첫 인자 `source`; 실패는 엔드포인트별로 잡아 `null` 반환(uncaught 0). `getRecommend` 만 4초 타임아웃·네트워크 오류 시 `fallback/popular.json` + `client_fallback_reason`
```js
export const config;                                        // { source: "mock"|"api"|null, apiBase: string(기본 "" — ?api= 덮어쓰기), capture: "1"|"2"|null }
export async function resolveSource();                      // 명시 ?source 우선 · 없으면 GET /health 200 → "api", 아니면 "mock"
export async function loadSteps();                          // config/onboarding.json → steps[]
export async function health(source);                       // HealthOut | null           (mock: {status:"ok", api_version:"v2", model_version:"hybrid_div_v1", db_ok:true, uptime_s:0} 형태)
export async function loadMeta(source);                     // OnboardingMeta
export async function getCandidates(source, { categories, subcategories = [], n = 30, userKey });  // CandidateSet
export async function postPreferences(source, body);        // body = PreferencesRequest{user_key, consent, reading_time, categories, criterion(id), subcategories, seeds, candidate_set_id, restart} → PreferencesResponse
export async function getRecommend(source, { userKey, snapshotId, model, k = 40, context });  // RecommendOut (+client_fallback_reason)
export async function postEvents(source, events);           // EventIn[] ≤50 → EventsAccepted | null
export async function postRating(source, body);             // RatingIn{rating_id, user_key, book_id, stars, ts, recommendation_id} → RatingOut | null
export async function getUserState(source, userKey);        // UserStateOut | null
export async function getUserData(source, userKey);         // UserDataOut | null
export async function deletePersonalization(source, userKey); // PersonalizationDeleted | null
export async function getDashboard(source);                 // DashboardOut | null   (api 404 → null → 화면 "이 항목은 아직 없음")
export async function getShowcase(source);                  // ShowcaseOut | null
```
경로(api 모드): `/health` · `/api/meta/onboarding` · `/api/candidates/onboarding?categories=&n=&user_key=&subcategories=` · `POST /api/preferences`(201) · `/api/recommend?user_key=&snapshot_id=&model=&k=&context=` · `POST /api/events`(202, `{events:[…]}` 배치) · `POST /api/ratings` · `/api/users/{key}/state` · `/api/users/{key}/data` · `DELETE /api/users/{key}/personalization` · `/api/dashboard` · `/api/showcase`. 타임스탬프 필드는 **`ts`**.

## D. `mock.js` 공개 함수 (06-03 소유) — `api.js` 가 `source === "mock"` 일 때 같은 이름으로 위임(source 인자 없음)
```js
export async function init();            // mock/catalog_kr.json + mock/neighbors_kr.json + mock/showcase.json 1회 로드(Promise 캐시)
export function byId(bookId);            // 카드 dict | undefined
export function health(); loadMeta(); getCandidates({categories, subcategories, n, userKey}); postPreferences(body);
export function getRecommend({userKey, snapshotId, model, k, context}); postEvents(events); postRating(body);
export function getUserState(userKey); getUserData(userKey); deletePersonalization(userKey); getDashboard(); getShowcase();
```
반환 형태는 `serving/schemas.py`·`schemas_should.py` 모델과 키 집합 1:1(여분 키 금지). 세션 저장은 `mock_store.js`(sessionStorage `millie_mock_db`).

## E. 화면 파일 규약
- `demo/js/screens/d2_home.js` `d3_detail.js` `d4_reader.js` `d5_library.js` `d7_dashboard.js` `d8_showcase.js`: `export function render(state) → string`. import 는 `./ui.js` 만(`d3_detail.js` 는 배경용 `./d2_home.js` 추가 허용).
- `onboarding_step.js`: `render(state, step, meta)` · `s0_start.js`: `render(state)` · `s6_persona.js`: `render(state)`.
- `ui.js`(06-02 소유) 추가 export: `tabbar(active)`(`"home"|"library"` → `<nav class="tabbar">` 홈·서재 2탭, `data-act="nav" data-to="#/home|#/library"`) · `demoLabel(text = "데모 전용 · 실제 밀리 화면 아님")` → `<div class="demo-label">` · `banner(text)` → `<div class="banner">` · `modal(inner)` → `<div class="modal-wrap"><div class="modal">…</div></div>`. 기존 `esc cover badge josa icon statusbar navbar cta` 유지.
- 모든 사용자·카탈로그 문자열은 `esc()` 를 거쳐 삽입한다. `innerHTML` 에 원문 금지(XSS). `title ?? "(제목 없음)"` 방어.
- D7·D8 은 `#page` 컨테이너(전폭), 나머지는 `#phone`. 렌더 대상 결정은 `app.js` 만.

## F. 저장 키·상수
- `localStorage["millie_user_key"]` — UUIDv4(`crypto.randomUUID()`), 개인정보 아님.
- `sessionStorage["millie_events"]` — `app.js` 이벤트 송신 큐(EventIn[]; 50개 단위 `postEvents`).
- `sessionStorage["millie_mock_db"]` — `mock_store.js` 의 mock 서버 상태(users·snapshots·events·ratings·recommendations).
- 상수(값은 `contracts.py` 와 동일): `BUDGET_MS = 200` · `QUALIFIED_READ_MINUTES = 15` · `ROW_SIZE = 12` · `VARIANTS = ["pop","cf","hybrid","hybrid_div"]` · `BADGE_TYPES = ["bestseller","review","author","publisher","buzz","light"]` · EVENT_TYPES 13종(`impression detail_click library_add reader_open qualified_read completion rating preference_started preference_step preference_book_selected preference_book_deselected preference_completed preference_restarted`).
- `EventIn` 필드: `{event_id(uuid), user_key, book_id?, event_type, ts(ISO), surface?, row_id?, position?, selected?, recommendation_id?, model_version?, preference_snapshot_id?, candidate_set_id?, payload: {string: string}}` — 그 외 키 금지.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: router.js 신규 + config/onboarding.json 진행바 실측값 + ui.js 공용 컴포넌트 4개 + index.html/base.css 셸</name>
  <files>demo/js/router.js, demo/config/onboarding.json, demo/js/screens/ui.js, demo/index.html, demo/css/base.css</files>
  <read_first>
    - demo/js/screens/ui.js (기존 export 8개 — 시그니처 불변)
    - demo/index.html (현행 30줄 — `#phone` `#inspector` `.layout` `.topbar` 유지)
    - demo/css/base.css (현행 100줄 — `body.is-capture` L97-100 재사용, 클래스 접두 관례 `.block__elem.is-state`)
    - demo/config/onboarding.json (현행 progress 0.2/0.4/0.6/0.6/0.8 — 값만 바꾼다)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §1 라우팅 표 · §2 D1 "진행바 값을 캡처 실측에 맞춘다: S1 0% · S2 진행바 없음 · S3 40% · S4 40% · S5 84%" · §3 공통 컴포넌트 표(TabBar 탭 2개 홈·서재 활성 `--accent` · DemoLabel 상단 24px 띠 `--text-3` · Toast 3초 · Banner 32px `--accent-dim`) · §6 D7·D8 제목 28px/700
    - ../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md §3 PC 레이아웃(`[폰 390px][인스펙터 560px]`, 페이지 배경 `--page-bg`, 인스펙터 `--panel-bg`)
    - ../.claude/rules/demo.md (라우팅 목록·`?capture=1` 폰만·`?capture=2` 폰+인스펙터·색 리터럴은 tokens.css 에만)
  </read_first>
  <action>
**(a) `demo/js/router.js` 신규(≤30줄):**
```js
// 해시 → {page, params}. 라우트 표는 이 파일에만. 기본 "#/" = showcase(../.claude/rules/demo.md).
export const ROUTES = [
  ["", "showcase"], ["onboarding", "onboarding"], ["home", "home"], ["book/:id", "book"],
  ["reader/:id", "reader"], ["library", "library"], ["refresh", "refresh"], ["dashboard", "dashboard"],
];
export function parse(hash = location.hash) {
  const path = hash.replace(/^#\/?/, "").replace(/\/$/, "");
  for (const [pat, page] of ROUTES) {
    const a = pat.split("/"), b = path.split("/");
    if (a.length !== b.length) continue;
    const params = {}; let ok = true;
    a.forEach((seg, i) => { if (seg.startsWith(":")) params[seg.slice(1)] = Number(b[i]); else if (seg !== b[i]) ok = false; });
    if (ok) return { page, params };
  }
  return { page: "showcase", params: {} };
}
export function listen(onRoute) { addEventListener("hashchange", () => onRoute(parse())); onRoute(parse()); }
```
`:id` 는 `Number()`(카탈로그 `book_id` 는 int). `""` 와 `"/"` 모두 showcase.

**(b) `demo/config/onboarding.json` — 값만 변경(키·텍스트 불변):** `S1.progress: 0.0` · `S2.progress: null` · `S3.progress: 0.4` · `S4.progress: 0.4` · `S5.progress: 0.84` · `S5.optionsFrom: "GET /api/candidates/onboarding"`(화면 01 §4-2 정본 문자열, `/api` 접두어). 그 외 문자 1개도 바꾸지 않는다(S3 options 5개는 `serving/onboarding_meta.json` criteria label 과 동일해야 한다 — `/contract-sync` 검사 대상).

**(c) `demo/js/screens/ui.js` — 기존 8 export 뒤에 4개 추가(파일 ≤130줄):**
```js
export function tabbar(active) {
  const tab = (id, to, label) => `<button class="tabbar__tab${active === id ? " is-on" : ""}" data-act="nav" data-to="${to}">${label}</button>`;
  return `<nav class="tabbar">${tab("home", "#/home", "홈")}${tab("library", "#/library", "서재")}</nav>`;
}
export const demoLabel = (text = "데모 전용 · 실제 밀리 화면 아님") => `<div class="demo-label">${esc(text)}</div>`;
export const banner = (text) => (text ? `<div class="banner">${esc(text)}</div>` : "");
export const modal = (inner) => `<div class="modal-wrap"><div class="modal-wrap__dim" data-act="closeModal"></div><div class="modal">${inner}</div></div>`;
```
`modal(inner)` 의 `inner` 는 호출자가 이미 `esc()` 한 마크업만 넘긴다(주석 1줄).

**(d) `demo/index.html`:** `<link>` 5개 뒤에 `css/reader.css` `css/library.css` `css/dashboard.css` 3줄 추가(순서: tokens → base → onboarding → home → inspector → reader → library → dashboard). 상단바 `model:` 의 `<b id="bar-model">` 옆에 `<span class="topbar__meta">version: <b id="bar-version">—</b></span>` 추가(`/health.model_version`). `<main class="layout">` 뒤에 `<section id="page" class="page" hidden></section>` 과 `<div id="toast" class="toast" hidden></div>` 추가. `<title>` 을 `밀리의서재 메인 추천 시스템 — 설계 데모` 로.

**(e) `demo/css/base.css` 추가(색 리터럴 금지 — `var(--…)` 만; `--demo-label` `--banner-bg` `--banner-text` `--kpi-bg` `--dim` 은 06-05 가 `tokens.css` 에 넣는다, 이 파일은 참조만):**
- `.tabbar { display:flex; flex:0 0 auto; height:56px; border-top:1px solid var(--line); background:var(--bg); }` `.tabbar__tab { flex:1; font-size:13px; color:var(--text-2); }` `.tabbar__tab.is-on { color:var(--accent); font-weight:600; }`
- `.demo-label { flex:0 0 auto; height:24px; line-height:24px; text-align:center; font-size:11px; color:var(--text); background:var(--demo-label); opacity:.9; }` (화면 02 §3: 24px 띠)
- `.banner { margin:8px var(--pad); padding:0 12px; height:32px; line-height:32px; border-radius:8px; background:var(--banner-bg); color:var(--banner-text); font-size:13px; }`
- `.toast { position:fixed; left:50%; bottom:48px; transform:translateX(-50%); padding:10px 16px; border-radius:var(--r-pill); background:var(--surface); color:var(--text); font-size:14px; z-index:50; }` `.toast[hidden]{display:none}`
- `.modal-wrap { position:absolute; inset:0; z-index:20; }` `.modal-wrap__dim { position:absolute; inset:0; background:var(--dim); }` `.modal { position:absolute; left:20px; right:20px; top:50%; transform:translateY(-50%); background:var(--surface); border-radius:20px; padding:20px; max-height:70%; overflow:auto; }` `.modal pre { font-family:var(--mono); font-size:11px; white-space:pre-wrap; }`
- 전폭 페이지: `.page { display:none; padding:32px 40px 60px; max-width:1360px; margin:0 auto; }` `body.is-page .layout { display:none; }` `body.is-page .page { display:block; }` `.page h1 { font-size:28px; font-weight:700; margin:0 0 8px; }`(화면 02 §6 "D7·D8 제목 28px/700")
- 캡처: 기존 `body.is-capture` 유지 + `body.is-capture-2 .topbar { display:none; }` `body.is-capture-2 { background: var(--page-bg); }`(인스펙터 유지).
딤 색도 토큰이다 — `var(--dim)`(06-05 가 `tokens.css` 에 `--dim: rgba(0,0,0,.6)` 추가). 이 파일에 새로 쓰는 줄에는 `rgba(`·`#RRGGBB` 리터럴 0건(`../.claude/rules/demo.md` 색 리터럴은 tokens.css 에만).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/router.js && node --check js/screens/ui.js && wc -l js/router.js js/screens/ui.js css/base.css && python3 -c "import json;s=json.load(open('config/onboarding.json'))['steps'];print([x['progress'] for x in s], s[4]['optionsFrom'])" && grep -c 'id="page"\|id="toast"\|id="bar-version"\|reader.css\|library.css\|dashboard.css' index.html && grep -nE '#[0-9a-fA-F]{3,8}\b' css/base.css | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/router.js` 종료 0 · `wc -l demo/js/router.js` ≤ 30 · `grep -c '\["reader/:id", "reader"\]' demo/js/router.js` == 1
    - `python3 -c "…"` 출력 `[0.0, None, 0.4, 0.4, 0.84] GET /api/candidates/onboarding`
    - `grep -c '"좋아하는 작가"\|"베스트셀러"\|"리뷰, 별점 등 대중의 평가"' demo/config/onboarding.json` == 3 (S3 텍스트 불변)
    - `grep -cE '^export (function|const) (tabbar|demoLabel|banner|modal)' demo/js/screens/ui.js` == 4 · 기존 `esc cover badge josa icon statusbar navbar cta` export 8개 유지(`grep -cE '^export (function|const) (esc|cover|badge|josa|icon|statusbar|navbar|cta)\b'` == 8)
    - `grep -c 'id="page"\|id="toast"\|id="bar-version"\|reader.css\|library.css\|dashboard.css' demo/index.html` == 6
    - `grep -nE '#[0-9a-fA-F]{3,8}\b' demo/css/base.css | grep -vE '#000\b|#1F2024|#FFFFFF|#1A1A1A|#1A1B1E' | wc -l` == 0 (v1 잔존 리터럴 5개만 허용 — 새로 추가하는 줄에는 리터럴 0; 잔존분 토큰화는 06-07 이 판단)
    - `grep -c 'is-capture-2\|body.is-page\|\.tabbar\|\.demo-label\|\.banner\|\.toast\|\.modal' demo/css/base.css` ≥ 7
    - `grep -c 'var(--dim)' demo/css/base.css` == 1 · `grep -c 'rgba(0,0,0,.6)' demo/css/base.css` == 0 (딤 색은 06-05 `tokens.css` 의 `--dim` 토큰) · `grep -c 'color:var(--text-3); background:var(--demo-label)' demo/css/base.css` == 0 (`.demo-label` 의 color 선언은 1회)
  </acceptance_criteria>
  <done>라우터·설정값·공용 컴포넌트·셸 CSS/HTML 이 존재하고 정적 검사 통과.</done>
</task>

<task type="auto">
  <name>Task 2: app.js 새로 작성 + actions.js + presets.js — state v2·render 분기·log/flush·라우트별 로드·온보딩 단계 머신·프리셋 3</name>
  <files>demo/js/app.js, demo/js/actions.js, demo/js/presets.js</files>
  <read_first>
    - demo/js/app.js (현행 275줄 — 계승: `setState`(L32-35) · `render` 골격(L70-77) · `log` 골격(L40-47, `timestamp`→`ts`) · 이벤트 위임 2곳(L254-262) · `boot`(L264-275) · `togglePick`(L118-134) · `loadCandidates`(L79-90) · `completeOnboarding`(L102-116). 폐기: `PRESET_PREFS` 라벨 `"베스트셀러"`(→ presets.js 에서 라벨 유지, 전송 시 id 변환) · `ORDER` 선형 머신은 actions.js 로 · `state.screen` 페이지 라우팅 · `consent_granted`/`preference_skipped`/`recommend` 이벤트(EVENT_TYPES 에 없음))
    - src/millie_rec/serving/schemas.py `EventIn`(L157-171) · `PreferencesRequest`(L94-104) · `RatingIn` · `RecommendOut`
    - src/millie_rec/contracts.py L38-62 EVENT_TYPES 13종 · VARIANTS · QUALIFIED_READ_MINUTES=15 · BUDGET_MS=200
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D1(이벤트 순서) · D2(impression) · D4(read10 +8%/+10분, 15분 도달 qualified_read 1회) · D5(철회 → level 3 + 배너 "비개인화 인기 도서") · D6(preference_restarted → … → preference_completed, α +0.15) · §4 상태 v2 · §5 표
    - ../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md §6 상태 모델(원칙) · §4-1 S0 "건너뛰기 → consent=false → D2"
    - .planning/phases/06-demo-rebuild/06-PATTERNS.md §2-1(계승 패턴 코드 발췌) · §3-3(이름 정본)
  </read_first>
  <action>
**`demo/js/app.js` 새로 쓴다(≤250줄).** import:
```js
import * as api from "./api.js";
import * as router from "./router.js";
import * as inspector from "./inspector.js";
import { createActions } from "./actions.js";
import * as presets from "./presets.js";
import { statusbar, banner, modal, esc } from "./screens/ui.js";
import { render as s0 } from "./screens/s0_start.js";
import { render as stepView } from "./screens/onboarding_step.js";
import { render as s6 } from "./screens/s6_persona.js";
import { render as d2 } from "./screens/d2_home.js";
import { render as d3 } from "./screens/d3_detail.js";
import { render as d4 } from "./screens/d4_reader.js";
import { render as d5 } from "./screens/d5_library.js";
import { render as d7 } from "./screens/d7_dashboard.js";
import { render as d8 } from "./screens/d8_showcase.js";
```
- `const state = {…}` — 부록 A 그대로(필드 이름·초기값 전부). `const VARIANTS = ["pop","cf","hybrid","hybrid_div"]`, `const QUALIFIED_READ_MINUTES = 15`.
- `function setState(patch) { Object.assign(state, patch); render(); }` · `const snap = () => state.snapshots[state.snapshots.length - 1]` · `const criterionId = () => state.meta?.criteria.find((c) => c.label === state.prefs.criterion)?.id ?? null`.
- **`log(event_type, fields = {})`**: `const ev = { event_id: crypto.randomUUID(), user_key: state.userKey, event_type, ts: new Date().toISOString(), recommendation_id: state.recommend?.recommendation_id ?? null, model_version: state.recommend?.model_version ?? null, preference_snapshot_id: snap()?.id ?? null, ...fields }` (fields 로 `book_id row_id position selected candidate_set_id surface payload` 만 허용 — `payload` 값은 전부 문자열). `state.events.push({ t: ev.ts.slice(11, 19), event_type, detail: [ev.book_id && `book=${ev.book_id}`, ev.row_id && `row=${ev.row_id}`, ev.position != null && `pos=${ev.position}`, ev.payload && JSON.stringify(ev.payload)].filter(Boolean).join(" ") })`; 200개 초과 시 앞에서 자른다. 큐: `const q = JSON.parse(sessionStorage.getItem("millie_events") || "[]"); q.push(ev); sessionStorage.setItem("millie_events", JSON.stringify(q));` → `flush()`: 큐를 50개씩 잘라 `api.postEvents(state.source, batch)` (await 하지 않음, `.catch(() => {})`), 성공 여부와 무관하게 큐를 비운다(이벤트 유실은 화면을 막지 않는다 — 주석). `flush` 는 `log` 마다 호출(배치는 큐 길이 기준).
- **`render()`**: `const pc = state.route.page === "dashboard" || state.route.page === "showcase"; document.body.classList.toggle("is-page", pc);` → pc 이면 `$page.hidden = false; $page.innerHTML = state.route.page === "dashboard" ? d7(state) : d8(state);` 아니면 `$page.hidden = true; $phone.innerHTML = statusbar() + screenHTML() + (state.modal ? modalHTML() : ""); $insp.innerHTML = inspector.render(state);`. 상단바: `#bar-model` = `state.model ?? (state.cell === "A" ? "hybrid" : state.cell === "B" ? "hybrid_div" : "auto")`, `#bar-version` = `state.health?.model_version ?? "—"`, `#bar-source` 텍스트·`dataset.source`. 토스트: `$toast.hidden = !state.toast; $toast.textContent = state.toast ?? ""`.
- **`screenHTML()`**: `page === "home"` → `d2(state)` · `"book"` → `d3(state)` · `"reader"` → `d4(state)` · `"library"` → `d5(state)` · `"onboarding"|"refresh"` → `state.screen === "S0" ? s0(state) : state.screen === "S6" ? s6(state) : stepView(state, state.steps.find((s) => s.id === state.screen), state.meta)`. `modalHTML()`: `state.modal === "mydata"` → `modal(`<h3>내 데이터</h3><pre>${esc(JSON.stringify(state.mydata, null, 1))}</pre><button class="cta is-on" data-act="closeModal">닫기</button>`)` · `"withdraw"` → `modal(`<h3>맞춤 추천 동의를 철회할까요?</h3><p>스냅샷·이벤트·별점·추천 로그가 삭제되고 이후에는 비개인화 인기 도서만 보입니다.</p><button class="cta is-on" data-act="withdrawConfirm">철회</button><button class="sheet__ghost" data-act="closeModal">취소</button>`)` · `"rating"` 은 D4 화면 파일이 그린다(app 은 그리지 않음).
- **`requestRecommend()`**: `state.recommend = await api.getRecommend(state.source, { userKey: state.userKey, snapshotId: snap()?.id ?? null, model: state.model, k: 40, context: state.prefs.readingTime })`; `state.nearlineLagSec = state.recommend?.nearline_lag_s ?? null`; `state.cell = state.recommend?.cell ?? state.cell`; 배너 결정 순서(**위에서 먼저 맞는 것 1개**): ① 응답이 `client_fallback_reason` 를 가지면 `state.banner = "추천 서버 응답이 없어 정적 인기 목록으로 대체했습니다 (fallback_level 3 · client)"` ② else if `state.consent === false || (fallback_level === 3 && !snap()?.id)` → `state.banner = "비개인화 인기 도서"`(건너뛰기·철회·스냅샷 없는 익명 — 06-06 체크리스트 12·14 와 06-07 Playwright 가 **이 문구**를 단정한다, 화면 02 §2 D5) ③ else if `fallback_level ≥ 1` → `state.banner = \`개인화 응답 지연 — 캐시/인기 도서로 대체 (level ${fallback_level})\``(동의한 사용자의 서버측 폴백만) ④ else `null`. `withdrawConfirm`·`skipUser` 가 `#/home` 으로 보낸 뒤 `requestRecommend()` 가 배너를 다시 계산해도 ②가 ③보다 앞이라 "비개인화 인기 도서" 가 유지된다. `impressions`: `recommendation_id` 가 `impressedRecId` 와 다르면 `rows.forEach(r => r.items.forEach(i => log("impression", { book_id: i.book_id, row_id: r.row_id, position: i.position, surface: "home" })))` 후 `impressedRecId = recommendation_id`.
- **`onRoute(route)`** — `state.route = route` 후 페이지별: `showcase` → `state.showcase ??= await api.getShowcase(state.source)` · `onboarding` → `Object.assign(state, { resetting: false, screen: "S0" })`(진행 중 상태 유지: `screen` 이 S1~S6 이고 `prefs` 가 채워져 있으면 유지) · `refresh` → 첫 진입(`!state.resetting`)이면 `log("preference_restarted", { payload: { from: snap()?.id ?? "none" } })`; `state.prefs = { readingTime: null, categories: [], criterion: null, subcategories: [], seedBooks: [] }; state.resetting = true; state.screen = "S1"` · `home` → `if (!state.recommend || state.route.params !== …)` — 단순히 `await requestRecommend()`(매 진입 재조회; D4 완독 후 after_completion 반영) · `book` → `const id = route.params.id; if (state.detail?.item.book_id !== id) { const found = (state.recommend?.rows ?? []).flatMap(r => r.items.map(i => [r.row_id, i])).find(([, i]) => i.book_id === id); state.detail = found ? { item: found[1], rowId: found[0] } : null; } if (!state.detail) location.hash = "#/home"`(`card` 액션이 서재 타일에서 만든 `rowId: "library"` detail 은 book_id 가 같으면 덮어쓰지 않는다 — 부록 B) · `reader` → `if (state.reading?.bookId !== route.params.id) { state.reading = { bookId: route.params.id, progressPct: 0, virtualMinutes: 0, qualified: false, completed: false }; log("reader_open", { book_id: route.params.id, row_id: state.detail?.rowId ?? null, surface: "detail" }); }` · `library` → `state.userState = await api.getUserState(state.source, state.userKey); state.library = state.userState?.library ?? { added: [], reading: [], completed: [] }` · `dashboard` → `state.dashboard = await api.getDashboard(state.source)`. 끝에 `render()`. 모든 await 는 `try { … } catch (e) { console.warn("[route]", route.page, e); }` 로 감싸 uncaught 0.
- **이벤트 위임 2곳(현행 그대로)**: click → `ACTIONS[el.dataset.act]?.(el)`(`INPUT` 제외), change → radio 만. `const ACTIONS = createActions({ state, setState, render, log, snap, criterionId, requestRecommend, api, presets, VARIANTS, QUALIFIED_READ_MINUTES, toast })`. `toast(msg)`: `state.toast = msg; render(); setTimeout(() => { state.toast = null; render(); }, 3000)`.
- **`boot()`**: `state.userKey = localStorage.getItem("millie_user_key") || (() => { const k = crypto.randomUUID(); localStorage.setItem("millie_user_key", k); return k; })();` → `const cap = api.config.capture; if (cap === "1") document.body.classList.add("is-capture"); if (cap === "2") document.body.classList.add("is-capture-2");` → `state.source = await api.resolveSource()` → `state.health = await api.health(state.source)` → `state.steps = await api.loadSteps()` → `state.meta = await api.loadMeta(state.source)`(null 이면 `{ survey_variant: "v1", reading_times: [], criteria: [], categories: [] }`) → `router.listen(onRoute)`. 전체 `try/catch` + `console.warn`.
- **금지 문자열**(D-07 grep 0건): `timestamp` · `hf.space` · `"/recommend"` · `static_popular` · `goodbooks` · `consent_granted` · `preference_skipped` · `resetMockState`.

**`demo/js/actions.js` 신규(≤250줄)** — `export function createActions(ctx) { const { state, setState, render, log, snap, criterionId, requestRecommend, api, presets, VARIANTS, QUALIFIED_READ_MINUTES, toast } = ctx; const ORDER = ["S1","S2","S3","S4","S5"]; const step = (id) => state.steps.find((s) => s.id === id); … return ACTIONS; }`
- `togglePick(stepId, val)` 현행 L118-134 그대로(S2 변경 시 subcategories 정리; `log("preference_step", { payload: { step: stepId, field: st.field } })`).
- `loadCandidates()`: `const res = await api.getCandidates(state.source, { categories: state.prefs.categories, subcategories: state.prefs.subcategories, n: 30, userKey: state.userKey }); state.candidateSet = { id: res.candidate_set_id, items: res.items, impressions: res.items.map((b) => ({ book_id: b.book_id, position: b.position, selected: false })) }; res.items.forEach((b) => log("impression", { book_id: b.book_id, position: b.position, selected: false, candidate_set_id: res.candidate_set_id, surface: "onboarding" }));`
- `completeOnboarding()`: `const res = await api.postPreferences(state.source, { user_key: state.userKey, consent: true, reading_time: state.prefs.readingTime, categories: state.prefs.categories, criterion: criterionId(), subcategories: state.prefs.subcategories, seeds: state.prefs.seedBooks, candidate_set_id: state.candidateSet.id, restart: state.resetting }); state.cell = res.cell; state.snapshots.push({ id: res.preference_snapshot_id, createdAt: res.created_at, prefs: { ...state.prefs }, persona: res.persona, cell: res.cell }); state.consent = true; log("preference_completed", { candidate_set_id: state.candidateSet.id, payload: { seeds: String(state.prefs.seedBooks.length), restart: String(state.resetting) } }); setState({ screen: "S6" });` (`library_add`×5 는 **서버가 자동 기록** — 클라이언트는 보내지 않는다, 화면 02 §5 "서버 자동")
- `next()`: S1 에서 `state.consent = true`; 마지막(S5)이면 `completeOnboarding()`; 다음이 S5 면 `await loadCandidates()`; `setState({ screen: nextId })`.
- ACTIONS(부록 B 전부): `start`(`log("preference_started")` → S1) · `skip`(`state.consent = false; state.snapshots = []; location.hash = "#/home"`) · `back`(S1 이고 resetting → `#/home`, S1 이고 !resetting → S0, 그 외 이전 단계) · `pick` · `pickbook`(현행 L167-176 + 해제 시 `preference_book_deselected`) · `next` · `toHome`(`state.resetting = false; location.hash = "#/home"`) · `nav`(`location.hash = el.dataset.to`) · `preset`(`presets.run(el.dataset.preset, ctx)`) · `card`(`const bookId = Number(el.dataset.book), rowId = el.dataset.row; if (rowId === "library") { const b = [...state.library.added, ...state.library.reading, ...state.library.completed].find((x) => x.book_id === bookId); if (!b) return; state.detail = { item: { book_id: b.book_id, title: b.title ?? null, image_url: b.image_url ?? null, authors: null, reason: null, badge: null, source: null, source_channels: [], score: 0, position: 0, book_format: null, difficulty: null }, rowId: "library" }; log("detail_click", { book_id: bookId, surface: "library" }); location.hash = "#/book/" + bookId; return; } const row = (state.recommend?.rows ?? []).find((r) => r.row_id === rowId); const item = row?.items.find((i) => i.book_id === bookId); if (!item) return; log("detail_click", { book_id: item.book_id, row_id: row.row_id, position: item.position }); state.detail = { item, rowId: row.row_id }; location.hash = "#/book/" + item.book_id` — 부록 B: `state.recommend` 가 null(철회 직후) 이어도 TypeError 0, 서재 타일은 `LibraryBook` 기반 최소 detail) · `closeSheet`(`#/home`) · `read`(`location.hash = "#/reader/" + el.dataset.book` — `reader_open` 은 라우터가 기록) · `library`(`log("library_add", { book_id: Number(el.dataset.book), surface: "main" }); state.library.added.push({ book_id, title: state.detail?.item.title ?? null, image_url: … }); toast("서재에 담았어요")`) · `read10`(`const r = state.reading; r.progressPct = Math.min(100, r.progressPct + 8); r.virtualMinutes += 10; if (!r.qualified && r.virtualMinutes >= QUALIFIED_READ_MINUTES) { r.qualified = true; log("qualified_read", { book_id: r.bookId, payload: { virtual_minutes: String(r.virtualMinutes) } }); } render()`) · `complete`(`r.completed = true; r.progressPct = 100; log("completion", { book_id: r.bookId }); setState({ modal: "rating" })`) · `rate`(`const stars = Number(el.dataset.stars); state.ratings[r.bookId] = stars; await api.postRating(state.source, { rating_id: "rat_" + crypto.randomUUID().replace(/-/g, "").slice(0, 6), user_key: state.userKey, book_id: r.bookId, stars, ts: new Date().toISOString(), recommendation_id: state.recommend?.recommendation_id ?? null }); log("rating", { book_id: r.bookId, payload: { stars: String(stars) } }); state.modal = null; toast("다음 책을 준비하고 있어요"); location.hash = "#/home"`) · `rateLater`(모달 닫고 토스트 → `#/home`) · `libTab`(`setState({ libraryTab: el.dataset.tab })`) · `mydata`(`state.mydata = await api.getUserData(state.source, state.userKey); setState({ modal: "mydata" })`) · `withdraw`(`setState({ modal: "withdraw" })`) · `withdrawConfirm`(`await api.deletePersonalization(state.source, state.userKey); Object.assign(state, { consent: false, snapshots: [], recommend: null, modal: null, banner: "비개인화 인기 도서", library: { added: [], reading: [], completed: [] } }); location.hash = "#/home"`) · `closeModal` · `model`(`state.model = VARIANTS.includes(el.value) ? el.value : null; await requestRecommend(); render()`) · `noop`.

**`demo/js/presets.js` 신규(≤120줄)** — `export const PRESET_NAMES = ["newUser", "skipUser", "resetUser"]; export async function run(name, ctx)`. 공통 `resetAll(ctx)`: `sessionStorage.removeItem("millie_mock_db"); sessionStorage.removeItem("millie_events"); localStorage.removeItem("millie_user_key"); state.userKey = crypto.randomUUID(); localStorage.setItem("millie_user_key", state.userKey); Object.assign(state, { consent: null, snapshots: [], recommend: null, detail: null, reading: null, ratings: {}, library: {added:[],reading:[],completed:[]}, events: [], banner: null, model: null, cell: null, resetting: false, prefs: {readingTime:null, categories:[], criterion:null, subcategories:[], seedBooks:[]}, candidateSet: {id:null, items:[], impressions:[]} })`. `PRESET = { readingTime: state.meta.reading_times[2] /* "저녁, 하루를 마치며" */, categories: ["IT", "소설", "철학"], criterionLabel: state.meta.criteria.find(c => c.id === "bestseller").label, subcategories: ["개발/프로그래밍", "SF", "서양"] }` — 텍스트는 meta 에서 가져온다(하드코딩은 카테고리 이름 3개·세부 3개만, `supported` 아니면 첫 supported 카테고리로 대체). `runOnboarding(ctx)`: `log("preference_started", { payload: { preset: "1" } }); state.consent = true; state.prefs = { ...PRESET, criterion: PRESET.criterionLabel, seedBooks: [] }` → `ACTIONS` 의 `loadCandidates` 재사용 불가(클로저) → presets 는 `api.getCandidates` 를 직접 호출해 같은 형태로 `state.candidateSet` 채우고 impression 30 로그 → 상위 5 선택(`preference_book_selected`×5, impressions[].selected=true) → `api.postPreferences` 같은 body 로 → snapshots push · cell · `preference_completed`. `newUser` = resetAll → runOnboarding → `location.hash = "#/home"`. `skipUser` = resetAll → `state.consent = false` → `#/home`. `resetUser` = resetAll → runOnboarding → `await requestRecommend()` → 첫 개인화 행(`rows.find(r => r.items.length && r.row_id !== "trending")`) 상위 2권에 `log("reader_open", { book_id, row_id, surface: "preset" })` → `log("preference_restarted", { payload: { from: snap().id } })` → `state.resetting = true` → runOnboarding(카테고리는 `["인문", "역사", "에세이/시"]` 로 바꿔 두 번째 스냅샷이 다르게) → `state.resetting = false` → `#/home`. 프리셋은 D8 시작 버튼 옆 3개(`data-act="preset" data-preset=…`, 06-05 가 그린다).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/app.js && node --check js/actions.js && node --check js/presets.js && wc -l js/app.js js/actions.js js/presets.js && grep -nE 'timestamp|hf\.space|"/recommend"|static_popular|[Gg]oodbooks|consent_granted|preference_skipped|resetMockState' js/app.js js/actions.js js/presets.js | wc -l && grep -c 'millie_events\|millie_user_key\|is-capture-2\|crypto.randomUUID' js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` 3파일 종료 0 · `wc -l demo/js/app.js` ≤ 250 · `wc -l demo/js/actions.js` ≤ 250 · `wc -l demo/js/presets.js` ≤ 120
    - `grep -nE 'timestamp|hf\.space|"/recommend"|static_popular|[Gg]oodbooks|consent_granted|preference_skipped|resetMockState' demo/js/app.js demo/js/actions.js demo/js/presets.js | wc -l` == 0
    - `grep -c '^const state = {' demo/js/app.js` == 1 · `grep -c 'function setState' demo/js/app.js` == 1 · `grep -c '^function render' demo/js/app.js` == 1
    - `grep -c 'route: { page: "showcase", params: {} }' demo/js/app.js` == 1 · `grep -c 'reading: null' demo/js/app.js` == 1 · `grep -c 'nearlineLagSec: null' demo/js/app.js` == 1
    - `grep -c '"millie_events"' demo/js/app.js` ≥ 2 · `grep -c '"millie_user_key"' demo/js/app.js` ≥ 1 · `grep -c 'ts: new Date().toISOString()' demo/js/app.js demo/js/actions.js` ≥ 2
    - `grep -c 'is-capture-2' demo/js/app.js` == 1 · `grep -c '"is-page"' demo/js/app.js` == 1
    - `grep -cE '^\s+(start|skip|back|pick|pickbook|next|toHome|nav|preset|card|closeSheet|read|library|read10|complete|rate|rateLater|libTab|mydata|withdraw|withdrawConfirm|closeModal|model|noop)\s*[:(]' demo/js/actions.js` == 24 (부록 B 24개 전부)
    - `grep -c 'restart: state.resetting' demo/js/actions.js` == 1 · `grep -c 'criterion: criterionId()' demo/js/actions.js` == 1 (라벨 → id 변환)
    - `grep -c '"preference_book_deselected"' demo/js/actions.js` == 1 · `grep -c 'library_add' demo/js/actions.js` == 1 (onboarding 시드의 library_add 는 서버 자동 — 클라이언트 완료 시 전송 코드 없음: `grep -c 'source=onboarding\|surface: "onboarding" }' demo/js/actions.js` 의 library_add 문맥 0)
    - `grep -c 'export const PRESET_NAMES = \["newUser", "skipUser", "resetUser"\]' demo/js/presets.js` == 1
    - `grep -c 'import { render as d2 } from "./screens/d2_home.js"' demo/js/app.js` == 1 · `s7_home\|s8_detail` 0건
    - `grep -c 'rowId === "library"' demo/js/actions.js` == 1 · `grep -c 'state.recommend?.rows ?? \[\]' demo/js/actions.js` ≥ 1 · `grep -c 'state.recommend.rows' demo/js/actions.js demo/js/app.js` == 0 (null 방어 — 부록 B card)
    - `grep -c 'state.consent === false ||' demo/js/app.js` == 1 이고 그 줄이 `fallback_level >= 1`(또는 `>= 1` 판정) 줄보다 **앞**에 있다(`grep -n` 줄 번호 비교 — 배너 우선순위 ②→③)
    - **SUMMARY 필수 표 2개(없으면 태스크 미완):** (a) 부록 B 액션 24개 ↔ `actions.js` 처리 분기(키 이름·시작 줄 번호) 대비표 (b) 부록 C 함수 15개(`config` `resolveSource` `loadSteps` `health` `loadMeta` `getCandidates` `postPreferences` `getRecommend` `postEvents` `postRating` `getUserState` `getUserData` `deletePersonalization` `getDashboard` `getShowcase`) ↔ `app.js`·`actions.js`·`presets.js` 호출 위치(파일:줄) 대비표 — 호출하지 않는 함수는 "미호출" 로 적는다
  </acceptance_criteria>
  <done>app.js/actions.js/presets.js 가 부록 A·B·C·F 와 문자 단위로 맞고 정적 검사 통과, 구 형태 문자열 0.</done>
</task>

<task type="auto">
  <name>Task 3: D1/D6 공유 최소 수정(onboarding_step·s0·s6) + README.md 재작성</name>
  <files>demo/js/screens/onboarding_step.js, demo/js/screens/s0_start.js, demo/js/screens/s6_persona.js, demo/README.md</files>
  <read_first>
    - demo/js/screens/onboarding_step.js (현행 85줄 — L57 `b.format === "PDF"` · L77 `step.progress * 100` · L76 navbar label)
    - demo/js/screens/s0_start.js · s6_persona.js (현행)
    - demo/README.md (현행 124줄 — `hf.space`·`books.json`·`REPLACE-ME`·`static_popular` 문구가 있다 → 전부 제거)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D6 "헤더 '취향 다시 설정', S0 생략, CTA '새 취향으로 추천 받기'" · §1 라우팅 표 · §7 캡처 계획
    - ../.claude/rules/demo.md 실행법·URL 파라미터
  </read_first>
  <action>
**`onboarding_step.js`(최소 수정, 3곳):** ① L57 `b.format === "PDF" ? '<span class="cover__pdf">PDF</span>' : ""` → `` ["오디오북", "챗북"].includes(b.book_format) ? `<span class="cover__pdf">${esc(b.book_format)}</span>` : "" `` (CandidateItem.book_format; "전자책" 은 표시 없음. `.cover__pdf` 클래스는 CSS 재사용 — 이름 변경은 onboarding.css 소유 밖이라 하지 않는다) ② L77 진행바: `step.progress == null ? "" : `<div class="progress"><div class="progress__fill" style="width:${step.progress * 100}%"></div></div>`` (S2 는 진행바 없음) ③ L83 CTA 텍스트: `const ctaText = state.resetting && step.id === "S5" ? "새 취향으로 추천 받기" : step.cta;` → `cta(ctaText, sel.length >= step.min, "next")`. L76 `navbar({ label: state.resetting ? "취향 다시 설정" : "" })` 유지. `esc(b.title ?? "(제목 없음)")`.

**`s0_start.js`:** 변경은 시그니처 `render(state)` 로만(인자 미사용 허용). 문구 유지("나와 비슷한 책 속의 주인공을 찾아볼까요?" · "건너뛰기" · "지금 시작하기").

**`s6_persona.js`:** `persona__who` 를 `${esc(state.userKey)}님은` → `회원님은`(UUID 를 화면에 찍지 않는다 — 가명 키라도 노출 최소화) · CTA 는 `data-act="toHome"` 유지 · `state.resetting` 이면 CTA 텍스트 `새 취향으로 추천 받기`(D6 완료). 그 외 불변.

**`demo/README.md` 재작성(≥40줄)** — 절: `# 밀리의서재 메인 추천 시스템 — 설계 데모(정적 프론트)` · 한 줄 요약(8페이지 해시 SPA, 폰 프레임 + 인스펙터, D7·D8 전폭) · **실행법**(`make demo-serve` → `http://localhost:8080/?source=mock` · `make serve` → `http://localhost:8000/?source=api` · `file://` 금지) · **URL 파라미터 표**(`source` mock|api — 생략 시 `/health` 200 이면 api · `api` 덮어쓰기 · `capture` `1` 폰만 / `2` 폰+인스펙터) · **라우트 표 8행**(화면 02 §1 그대로: `#/` 쇼케이스 · `#/onboarding` · `#/home` · `#/book/:id` · `#/reader/:id` · `#/library` · `#/refresh` · `#/dashboard`) · **폴더 구조**(`js/` router·app·actions·presets·api·mock·mock_store·inspector · `js/screens/` ui·s0·onboarding_step·s6·d2~d8 · `css/` 8파일 · `config/onboarding.json`(화면 텍스트 정본) · `mock/`(생성기 산출물 — 손편집 금지) · `fallback/popular.json`(`make millie-export` 산출) · `scripts/make_mock.py`) · **mock 재생성**: `uv run python demo/scripts/make_mock.py`(`make mock` 은 쓰지 않는다 — Makefile 정정은 PROGRESS 미결) · **계약**: `serving/schemas.py` 가 정본, `/contract-sync` 로 검증, `latency_ms` float·`ts` 필드 · **규칙 링크**: `../.claude/rules/demo.md`, 화면 구성 01·02 · **데이터 고지**: `data.md` 2트랙 고정 문장 1문단 + "표지는 밀리 CDN 핫링크, 텍스트 미저장". 구 형태 문자열(`hf.space` `REPLACE-ME` `books.json` `static_popular` `zygmuntz` `CSV_URL`) 0건. 데이터 고지 문장은 `../.claude/rules/serving.md` 2트랙 고지 **원문 그대로** "평가 비교표 = Goodbooks-10k(CC BY-SA 4.0) · 데모 카탈로그 = 밀리의서재 공개 도서 페이지(수치·메타·표지 URL만, 텍스트 미노출, 요청 시 삭제) · …" — 06-07 구 형태 정규식은 `goodbooks` 단어를 검사하지 않으므로 우회 표현을 쓰지 않는다(D-07 의 판정은 Goodbooks **영문 책 제목·저자** 가 화면·mock 에 없는 것이고, 데이터셋 귀속 표기는 라이선스 의무다).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/screens/onboarding_step.js && node --check js/screens/s0_start.js && node --check js/screens/s6_persona.js && grep -c 'book_format\|progress == null\|새 취향으로 추천 받기' js/screens/onboarding_step.js && grep -nE 'hf\.space|REPLACE-ME|books\.json|static_popular|zygmuntz|CSV_URL' README.md | wc -l && grep -c 'Goodbooks-10k(CC BY-SA 4.0)' README.md && grep -c '#/reader/:id\|#/dashboard\|make demo-serve\|uv run python demo/scripts/make_mock.py\|capture' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` 3파일 종료 0
    - `grep -c 'b.format' demo/js/screens/onboarding_step.js` == 0 · `grep -c 'book_format' demo/js/screens/onboarding_step.js` ≥ 1 · `grep -c 'progress == null' demo/js/screens/onboarding_step.js` == 1 · `grep -c '"새 취향으로 추천 받기"' demo/js/screens/onboarding_step.js demo/js/screens/s6_persona.js` == 2
    - `grep -c 'state.userKey' demo/js/screens/s6_persona.js` == 0 · `grep -c '회원님은' demo/js/screens/s6_persona.js` == 1
    - `grep -nE 'hf\.space|REPLACE-ME|books\.json|static_popular|zygmuntz|CSV_URL' demo/README.md | wc -l` == 0 · `grep -c 'Goodbooks-10k(CC BY-SA 4.0)' demo/README.md` == 1 (2트랙 고지 원문) · `grep -c 'make mock' demo/README.md` ≤ 1 이고 그 1건은 "쓰지 않는다" 문맥(`grep -n 'make mock' demo/README.md` 확인)
    - `grep -c '#/onboarding\|#/home\|#/book/:id\|#/reader/:id\|#/library\|#/refresh\|#/dashboard' demo/README.md` ≥ 7 · `grep -c 'uv run python demo/scripts/make_mock.py' demo/README.md` ≥ 1 · `wc -l demo/README.md` ≥ 40
    - 구 형태 전역 grep(06-07 정규식) 을 이 플랜 소유 파일에 한정해 실행: `cd demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' js/router.js js/app.js js/actions.js js/presets.js js/screens/ui.js js/screens/onboarding_step.js js/screens/s0_start.js js/screens/s6_persona.js index.html css/base.css config/onboarding.json README.md | wc -l` == 0
  </acceptance_criteria>
  <done>D1/D6 공유 수정 3파일 · README 재작성 · 이 플랜 소유 파일 전체에서 구 형태 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL 쿼리(`?source` `?api` `?capture`) · 해시 → `app.js`/`router.js` | 비신뢰 입력. `:id` 는 `Number()`, `?api` 는 사용자 명시 시만 |
| 카탈로그·응답 문자열(title·authors·reason·persona.description) → DOM | XSS 표면 — `esc()` 필수 |
| `localStorage`/`sessionStorage` → 상태 복원 | 같은 origin 스크립트만 접근. 가명 키·이벤트 큐만 |
| `app.js` → `api.postEvents`(서버) | 이벤트 본문은 EventIn 13종·문자열 payload 만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | 카탈로그 제목·저자·reason·persona 문장의 `innerHTML` 삽입(XSS) | mitigate | 모든 삽입은 `ui.esc()` 경유(부록 E) · `modal(inner)` 는 호출자가 `esc()` 한 마크업만 · 06-07 게이트 `grep -n 'innerHTML' demo/js/screens/*.js` 0(화면 파일은 문자열 반환만) |
| T-06-02-02 | Spoofing | `?api=<URL>` 로 임의 서버에 이벤트·user_key 전송 | accept | 기본 `API_BASE=""` 같은 origin(`demo.md`); `?api` 는 사용자 본인 명시 시만 · 전송 내용은 가명 키·이벤트만(시크릿 없음) |
| T-06-02-03 | Information Disclosure | `localStorage["millie_user_key"]` UUID 노출 | accept | 개인정보 아님(가명 키, 백엔드 01 §0). 화면에는 찍지 않는다(s6 `회원님은`) |
| T-06-02-04 | Tampering | 해시 `#/book/abc` 등 잘못된 `:id` | mitigate | `Number()` → `NaN` 이면 `detail` 못 찾음 → `#/home` 리다이렉트(onRoute `book` 분기) |
| T-06-02-05 | Denial of Service | `impression` 40건/렌더로 이벤트 폭주 | mitigate | `recommendation_id` 당 1회(`impressedRecId`) · 50개 배치 · 큐는 전송 후 비움 |
| T-06-02-06 | Repudiation | 이벤트 유실(서버 다운) 이 화면을 막음 | accept | `postEvents` 는 await 하지 않고 `.catch(() => {})` — "이벤트 유실은 화면을 막지 않는다"(현행 주석 계승); 대시보드 수신율 지표가 유실을 드러낸다 |
| T-06-02-07 | Information Disclosure | `sessionStorage["millie_events"]` 에 쌓이는 이벤트 | accept | 가명 키·book_id·이벤트 종류만. 탭 종료 시 소멸 |
</threat_model>

<verification>
- `node --check` 대상 8개 JS 전부 종료 0 · `wc -l` 상한(router 30 · app/actions 250 · presets 120 · ui 130 · base.css 250)
- 이 플랜 소유 12파일에서 06-07 구 형태 정규식 0건
- 부록 B 24 액션 이름이 `actions.js` 에 전부 존재 · 부록 A 필드가 `app.js` state 리터럴에 존재
- `config/onboarding.json` progress `[0.0, null, 0.4, 0.4, 0.84]` · S3 텍스트 불변
- (브라우저 완주는 06-06 체크리스트·06-07 Advisor 게이트 — 이 플랜은 정적 검증까지)
</verification>

<success_criteria>
- DEMO-03: 라우터 8페이지 + `state`/`render()` 패턴 코드 존재(정적 판정)
- DEMO-04: 7단계 = `config/onboarding.json` + `onboarding_step.js` 1벌, `skip` → `consent=false` → `#/home`
- DEMO-09 부분: `?capture=1|2` body class 토글 코드 존재(완주는 06-07)
- 06-CONTEXT D-04 이행(app.js 새로, S0~S6 머신은 actions.js·단계 파일로) · D-07 grep 0건(소유 파일)
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-rebuild/06-02-SUMMARY.md` — 변경 파일 목록(커밋 0) · `wc -l` 표 · 구 형태 grep 0 출력 인용 · **부록 B 24 액션 ↔ actions.js 분기 대비표 · 부록 C 15 함수 ↔ 호출 위치 대비표(Task 2 acceptance 필수)** · 부록 A 필드 대비 확인 표 · wave 3 에 넘기는 주의(`d2_home.js` 등 import 이름 · `rowId === "library"` detail).
</output>
