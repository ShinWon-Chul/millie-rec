---
phase: 06-demo-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - demo/js/api.js
  - demo/js/mock.js
  - demo/js/mock_store.js
autonomous: true
requirements: [DEMO-01, DEMO-02]
lane: Demo (데이터 계층 Worker — wave 2 병렬 4 중 2)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(05 관례, 사용자 지시 2026-09-05)
must_not_touch:
  - src/**
  - tests/**
  - Makefile
  - pyproject.toml
  - Dockerfile
  - artifacts/**
  - results/**
  - report/**
  - demo/js/app.js            # 06-02
  - demo/js/actions.js        # 06-02
  - demo/js/presets.js        # 06-02
  - demo/js/router.js         # 06-02
  - demo/js/inspector.js      # 06-06
  - demo/js/screens/**        # 06-02·04·05·06
  - demo/css/**               # 06-02·04·05·06
  - demo/index.html           # 06-02
  - demo/config/onboarding.json
  - demo/README.md            # 06-02
  - demo/mock/**              # 06-01 산출물 — 읽기만
  - demo/fallback/popular.json
  - demo/scripts/make_mock.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - ../PROGRESS.md
  - ../.assets/**

must_haves:
  truths:
    - "api.js 는 API_BASE=\"\"(같은 origin)·/api 접두어·ts 필드·4초 타임아웃을 쓰고 hf.space·\"/recommend\"·timestamp 문자열이 0건이다(DEMO-01)"
    - "api.js 의 13개 공개 함수(부록 C)는 source=api|mock 을 같은 시그니처로 덮고, api 모드의 실패는 엔드포인트별로 잡혀 null 을 돌려주며(uncaught 0), getRecommend 만 타임아웃·네트워크 오류 시 fallback/popular.json + client_fallback_reason 을 준다"
    - "mock.js 는 catalog_kr.json·neighbors_kr.json·showcase.json 만 읽고, 05-CONTEXT D-01~D-04 의 5행 compose(continue_reading → anchor_<seed₁> → persona_shelf → trending → fresh_picks · ROW_SIZE 12 · book_id+정규화 제목 dedup · 비개인화는 trending+fresh_picks 만)를 점수 계산 없이 흉내낸다(06-CONTEXT D-01·D-05)"
    - "mock 응답은 serving/schemas.py 형태 그대로(latency_ms float 38.0 · latency_breakdown · cell sha256 · forced · user_state_weights 표시값 · dedup_removed · nearline_lag_s 1.0) 이며 여분 키가 없다"
    - "배지 6종은 badges.py 와 같은 조건(criterion 은 배지 타입 id · review 3단 폴백 · author/publisher 는 seeds 일치 시만 · buzz=millie_label · light 없음), 페르소나는 persona.py 와 같은 표·문장이다"
    - "mock_store.js 가 sessionStorage millie_mock_db 에 users·snapshots·events·ratings·recommendations 를 유지해 UserStateOut·UserDataOut·PersonalizationDeleted·DashboardOut(세션 집계, 06-CONTEXT D-03)을 서버와 같은 형태로 돌려준다"
  artifacts:
    - path: "demo/js/api.js"
      provides: "config · resolveSource · loadSteps · health loadMeta getCandidates postPreferences getRecommend postEvents postRating getUserState getUserData deletePersonalization getDashboard getShowcase — API_BASE=\"\" · TIMEOUT_MS=4000 · get/post/del 래퍼"
      exports: ["config", "resolveSource", "loadSteps", "health", "loadMeta", "getCandidates", "postPreferences", "getRecommend", "postEvents", "postRating", "getUserState", "getUserData", "deletePersonalization", "getDashboard", "getShowcase"]
      max_lines: 150
    - path: "demo/js/mock.js"
      provides: "init · byId · 서버 규칙 시뮬레이션(candidates 라운드로빈 · preferences 스냅샷 append + cell sha256 + persona · recommend 5행 compose + 배지 + dedup + weights + after_completion · showcase 로드) — 부록 D 시그니처"
      max_lines: 250
    - path: "demo/js/mock_store.js"
      provides: "sessionStorage millie_mock_db — users/snapshots/events/ratings/recommendations 저장 · library 버킷 · state/data/delete · dashboard 세션 집계(kpi 6·ab_table·latency·quality·events_recent 50·impressions_log·by_variant·by_hour)"
      max_lines: 250
  key_links:
    - from: "demo/js/api.js"
      to: "demo/js/mock.js"
      via: "source === \"mock\" → await mock.init() → mock.<same name>(args)"
      pattern: "mock\\.init\\(\\)"
    - from: "demo/js/api.js"
      to: "demo/fallback/popular.json"
      via: "getRecommend catch → local(\"fallback/popular.json\") + client_fallback_reason"
      pattern: "fallback/popular\\.json"
    - from: "demo/js/mock.js"
      to: "demo/mock/neighbors_kr.json"
      via: "anchor_<seed₁>·after_completion 행 = neighbors[seed] top-20 → eligible → dedup → 12"
      pattern: "neighbors_kr\\.json"
    - from: "demo/js/mock.js"
      to: "demo/js/mock_store.js"
      via: "postPreferences → store.addSnapshot · getRecommend → store.logRecommendation · postEvents → store.addEvents"
      pattern: "from \"./mock_store.js\""
---
<objective>
Phase 6 '데모 재구성'(.planning/ROADMAP.md "Phase 6: 데모 재구성") 의 **데이터 계층**을 만든다: `js/api.js` 재작성(새 HTTP 계약 — `API_BASE=""`·`/api` 접두어·`ts`·4초 타임아웃·엔드포인트 13개·실패 격리) · `js/mock.js` 폐기 후 새로(밀리 카탈로그 위의 브라우저 내 상태 시뮬레이션 — 05-CONTEXT 서버 규칙을 JS 표시 규칙으로) · 신규 `js/mock_store.js`(mock 서버 상태 = sessionStorage, 세션 집계 대시보드). 두 계층은 `app.js`(06-02) 가 부록 C 시그니처로 호출한다.

Purpose: ROADMAP Success Criteria 1 — "`js/api.js` 가 `API_BASE=""`·`/api` 접두어·`ts` 필드·4초 타임아웃 클라이언트 fallback 을 쓴다 · 구 형태 grep 0"(**DEMO-01**) + Success Criteria 2 의 후반 "mock 이 `serving/schemas.py` 형태"(**DEMO-02**). 06-CONTEXT D-01(경량 시뮬레이션·점수 계산 없음)·D-03(D7 = 세션 이벤트 직접 집계, 고정 숫자 금지)·D-05(`mock.js` 폐기 후 새로, 배지는 표시 규칙으로 이중 구현 예외) 이행. "심사자가 mock 으로 눌러봐도 가짜가 티나지 않아야 한다"(06-CONTEXT specifics).
Output: `api.js` 재작성(≤150) · `mock.js` 새로(≤250) · `mock_store.js` 신규(≤250). 커밋 0.

**wave 2 병렬 불변:** `app.js`·화면 파일·CSS 는 다른 Worker 가 같은 시각에 만든다 — 읽지도 고치지도 않는다. 이 플랜의 두 모듈은 `./screens/ui.js` 의 `josa` 만 import 한다(현행 mock.js 관례; `ui.js` 의 `josa` 시그니처는 06-02 가 바꾸지 않는다). 검증은 `node --check` + Node 스크립트(브라우저 API 없는 순수 함수 단위) + grep.

**점진 도착 원칙(브리프):** `?source=api` 에서 Phase 5 '서빙 Must 완성'(.planning/phases/05-must/) 이 아직 만들지 않은 엔드포인트의 404·`after_completion` 부재·`personal_case` null 은 정상 — `api.js` 는 함수별로 `null` 을 돌려 화면이 "서버 준비 중 / 이 항목은 아직 없음" 을 그리게 하고 콘솔 uncaught 0 을 지킨다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-rebuild/06-CONTEXT.md
@.planning/phases/06-demo-rebuild/06-PATTERNS.md
@.planning/phases/06-demo-rebuild/06-01-PLAN.md
@.planning/phases/05-must/05-CONTEXT.md
@../.claude/rules/demo.md
@../.claude/rules/data.md
@../.claude/rules/references.md
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/schemas_should.py
@src/millie_rec/serving/badges.py
@src/millie_rec/serving/persona.py
@src/millie_rec/serving/rows.py
@src/millie_rec/serving/compose.py
@src/millie_rec/serving/fallback.py
@src/millie_rec/serving/privacy_api.py
@src/millie_rec/contracts.py
@demo/js/api.js
@demo/js/mock.js
@demo/js/screens/ui.js
@demo/fallback/popular.json

<file_ownership>
## Phase 6 파일 소유 표 (겹침 0 — 같은 wave 는 서로소)

| 파일 | 만듦/고침/삭제 | 플랜 | wave |
|---|---|---|---|
| `demo/scripts/make_mock.py` · `tests/demo/test_make_mock.py` · `demo/mock/*.json`(13개 — 계약 10 + catalog_kr·neighbors_kr + _manifest) · `demo/mock/books.json` 삭제 | 재작성·신규·재생성 | 06-01 | 1 |
| `demo/js/router.js` · `demo/js/app.js` · `demo/js/actions.js` · `demo/js/presets.js` · `demo/js/screens/ui.js` · `demo/js/screens/onboarding_step.js` · `demo/js/screens/s0_start.js` · `demo/js/screens/s6_persona.js` · `demo/index.html` · `demo/css/base.css` · `demo/config/onboarding.json` · `demo/README.md` | 신규·재작성·최소 수정 | 06-02 | 2 |
| `demo/js/api.js` · `demo/js/mock.js` · `demo/js/mock_store.js` | 재작성·신규 | **06-03(이 플랜)** | 2 |
| `demo/js/screens/d4_reader.js` · `demo/js/screens/d5_library.js` · `demo/css/reader.css` · `demo/css/library.css` | 신규 | 06-04 | 2 |
| `demo/js/screens/d7_dashboard.js` · `demo/js/screens/d8_showcase.js` · `demo/css/dashboard.css` · `demo/css/tokens.css` | 신규·토큰 추가 | 06-05 | 2 |
| `demo/js/screens/d2_home.js`(← `s7_home.js` 삭제) · `demo/js/screens/d3_detail.js`(← `s8_detail.js` 삭제) · `demo/js/inspector.js` · `demo/css/home.css` · `demo/css/inspector.css` | 신규·삭제·확장 | 06-06 | 3 |
| `.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md` · `../PROGRESS.md`(미결 절 2줄 append 만) | Advisor | 06-07 | 4 |
</file_ownership>

<interfaces>
<!-- 공통 인터페이스 부록 — 06-02 ~ 06-06 다섯 플랜에 **동일한 텍스트**로 들어간다. 한 곳만 바꾸면 병렬 Worker 가 어긋난다. -->
<!-- 정본: 화면 구성 02 §4 상태 모델 v2 · §5 이벤트 13종 · 화면 구성 01 §6 · serving/schemas.py(응답 형태) · contracts.py(이름). -->

## A. `state` v2 — `demo/js/app.js` 의 단일 상태 객체 (06-02 소유, 다른 파일은 읽기만)
```js
const state = {
  route: { page: "showcase", params: {} },  // router.parse(location.hash). page ∈ showcase|onboarding|home|book|reader|library|refresh|dashboard
  source: "mock",                            // "mock" | "api"
  userKey: null,                             // localStorage["millie_user_key"] UUIDv4 — boot 에서 항상 채움(익명도 키 있음)
  cell: null,                                // "A" | "B" — postPreferences 응답 · getUserState 응답
  consent: null,                             // null | true | false (건너뛰기 = false)
  screen: "S0",                              // D1/D6 내부 단계 "S0".."S6"
  resetting: false,                          // D6(#/refresh) 모드 — 헤더 "취향 다시 설정", S0 생략, CTA "새 취향으로 추천 받기"
  prefs: { readingTime: null, categories: [], criterion: null, subcategories: [], seedBooks: [] },  // criterion = S3 **라벨**(config 텍스트)
  steps: [],                                 // config/onboarding.json .steps
  meta: null,                                // OnboardingMeta {survey_variant, reading_times[], criteria[{id,label}], categories[{name,supported,subcategories[]}]}
  candidateSet: { id: null, items: [], impressions: [] },  // items = CandidateItem[], impressions = [{book_id, position, selected}]
  snapshots: [],                             // [{ id, createdAt, prefs, persona, cell }] — append only(재설정도 push)
  model: null,                               // null = 셀 배정(A→hybrid, B→hybrid_div) · VARIANTS 문자열 = 인스펙터 라디오 강제(forced)
  recommend: null,                           // RecommendOut 원문. api 클라이언트 fallback 시 + client_fallback_reason: string
  detail: null,                              // { item: ItemOut, rowId } | null — D3 바텀시트. rowId ∈ RecommendOut.rows[].row_id | "library"(D5 서재 타일 — item 은 LibraryBook 기반 최소 ItemOut, 부록 B card)
  reading: null,                             // { bookId, progressPct, virtualMinutes, qualified: false, completed: false } | null — D4
  ratings: {},                               // { [bookId]: stars }
  library: { added: [], reading: [], completed: [] },  // UserStateOut.library (LibraryBook[])
  libraryTab: "added",                       // D5 활성 탭 added|reading|completed
  userState: null,                           // UserStateOut 원문(D5 타임라인·U_t)
  mydata: null,                              // UserDataOut 원문(D5 "내 데이터 보기" 모달)
  dashboard: null,                           // DashboardOut 원문(D7)
  showcase: null,                            // ShowcaseOut 원문(D8)
  health: null,                              // HealthOut | null (상단바 model_version)
  nearlineLagSec: null,                      // RecommendOut.nearline_lag_s
  events: [],                                // 인스펙터 로그 [{ t: "HH:MM:SS", event_type, detail }] 최근 200
  toast: null,                               // string | null — 3초 후 null
  banner: null,                              // string | null — D2 상단 Banner 문구(예: "비개인화 인기 도서")
  modal: null,                               // null | "rating" | "mydata" | "withdraw"
};
```
원칙: `setState(patch)` 하나 · `render()` 하나 · 화면 파일은 `render(state) → HTML 문자열` 만, fetch·상태 변경 금지(`../.claude/rules/demo.md`).

## B. `data-act` 액션 이름 (화면 파일이 찍고 `app.js`/`actions.js` 가 위임 처리 — click 1곳 + change 1곳)
| data-act | 추가 data-* | 처리 | 이벤트(EVENT_TYPES 13종) |
|---|---|---|---|
| `start` | — | S0 → S1 | `preference_started` |
| `skip` | — | consent=false → `#/home`(level 3) | — |
| `back` | — | 단계 뒤로(S1 에서 resetting 이면 `#/home`) | — |
| `pick` | `data-step` `data-val` | S1~S4 선택 토글 | `preference_step`(payload {step}) |
| `pickbook` | `data-book` `data-pos` | S5 시드 토글 + impressions[].selected | `preference_book_selected` / `preference_book_deselected` |
| `next` | — | 다음 단계(S4→S5 진입 시 getCandidates + `impression`×30, S5 완료 시 postPreferences) | `preference_completed` |
| `toHome` | — | S6 CTA → `#/home` | — |
| `nav` | `data-to="#/home"` 등 해시 | `location.hash = to` | — |
| `preset` | `data-preset="newUser"\|"skipUser"\|"resetUser"` | presets.run(name) | (프리셋이 만드는 이벤트) |
| `card` | `data-book` `data-row` `data-pos` | detail 설정 → `#/book/:id`. 행 탐색은 `(state.recommend?.rows ?? [])` 로 null 방어(철회 직후 `recommend: null` 이어도 TypeError 0). `data-row="library"`(D5 서재 타일) 이면 `state.library` 의 added·reading·completed 에서 `LibraryBook{book_id,title,image_url}` 을 찾아 ItemOut 형태의 최소 detail(`book_id`·`title`·`image_url` 채움, `authors`·`reason`·`badge`·`source`·`book_format`·`difficulty` null, `source_channels` [], `score`·`position` 0) 을 만들고 `rowId: "library"` 로 둔다(d3 는 rowId 를 "내 서재" 로 표시). 못 찾으면 무동작 | `detail_click`(서재 타일은 `row_id`·`position` 없이 `surface: "library"`) |
| `closeSheet` | — | `#/home` | — |
| `read` | `data-book` | `#/reader/:id` 진입(reading 초기화) | `reader_open` |
| `library` | `data-book` | 서재 담기 | `library_add`(surface "main") |
| `read10` | — | progressPct +8, virtualMinutes +10; ≥15분 첫 도달 시 1회 | `qualified_read` |
| `complete` | — | completed=true, modal="rating" | `completion` |
| `rate` | `data-stars` 1..5 | postRating → toast "다음 책을 준비하고 있어요" → `#/home` | `rating` |
| `rateLater` | — | 토스트 → `#/home` | — |
| `libTab` | `data-tab` | libraryTab 변경 | — |
| `mydata` | — | getUserData → modal="mydata" | — |
| `withdraw` | — | modal="withdraw" | — |
| `withdrawConfirm` | — | deletePersonalization → consent=false → banner "비개인화 인기 도서" → `#/home` | — |
| `closeModal` | — | modal=null | — |
| `model` | (radio `value`) | state.model=value → getRecommend(forced) | — |
| `noop` | — | — | — |
- D6 시작(`#/refresh` 진입)은 라우터가 `preference_restarted` 를 기록한다. D2 행 렌더 직후 `impression`(row_id·position·recommendation_id) 은 `app.js` 가 recommendation_id 당 1회 기록한다.

## C. `api.js` 공개 함수 (06-03 소유) — 전부 `async`, 첫 인자 `source`; 실패는 엔드포인트별로 잡아 `null` 반환(uncaught 0). `getRecommend` 만 4초 타임아웃·네트워크 오류 시 `fallback/popular.json` + `client_fallback_reason`
```js
export const config;                                        // { source: "mock"|"api"|null, apiBase: string(기본 "" — ?api= 덮어쓰기), capture: "1"|"2"|null }
export async function resolveSource();                      // 명시 ?source 우선 · 없으면 GET /health 200 → "api", 아니면 "mock"
export async function loadSteps();                          // config/onboarding.json → steps[]
export async function health(source);                       // HealthOut | null           (mock: {status:"ok", api_version:"v2", model_version:"hybrid_div_v1", db_ok:true, uptime_s:0} 형태)
export async function loadMeta(source);                     // OnboardingMeta
export async function getCandidates(source, { categories, subcategories = [], n = 30, userKey });  // CandidateSet
export async function postPreferences(source, body);        // body = PreferencesRequest{user_key, consent, reading_time, categories, criterion(id), subcategories, seeds, candidate_set_id, restart} → PreferencesResponse
export async function getRecommend(source, { userKey, snapshotId, model, k = 40, context });  // RecommendOut (+client_fallback_reason)
export async function postEvents(source, events);           // EventIn[] ≤50 → EventsAccepted | null
export async function postRating(source, body);             // RatingIn{rating_id, user_key, book_id, stars, ts, recommendation_id} → RatingOut | null
export async function getUserState(source, userKey);        // UserStateOut | null
export async function getUserData(source, userKey);         // UserDataOut | null
export async function deletePersonalization(source, userKey); // PersonalizationDeleted | null
export async function getDashboard(source);                 // DashboardOut | null   (api 404 → null → 화면 "이 항목은 아직 없음")
export async function getShowcase(source);                  // ShowcaseOut | null
```
경로(api 모드): `/health` · `/api/meta/onboarding` · `/api/candidates/onboarding?categories=&n=&user_key=&subcategories=` · `POST /api/preferences`(201) · `/api/recommend?user_key=&snapshot_id=&model=&k=&context=` · `POST /api/events`(202, `{events:[…]}` 배치) · `POST /api/ratings` · `/api/users/{key}/state` · `/api/users/{key}/data` · `DELETE /api/users/{key}/personalization` · `/api/dashboard` · `/api/showcase`. 타임스탬프 필드는 **`ts`**.

## D. `mock.js` 공개 함수 (06-03 소유) — `api.js` 가 `source === "mock"` 일 때 같은 이름으로 위임(source 인자 없음)
```js
export async function init();            // mock/catalog_kr.json + mock/neighbors_kr.json + mock/showcase.json 1회 로드(Promise 캐시)
export function byId(bookId);            // 카드 dict | undefined
export function health(); loadMeta(); getCandidates({categories, subcategories, n, userKey}); postPreferences(body);
export function getRecommend({userKey, snapshotId, model, k, context}); postEvents(events); postRating(body);
export function getUserState(userKey); getUserData(userKey); deletePersonalization(userKey); getDashboard(); getShowcase();
```
반환 형태는 `serving/schemas.py`·`schemas_should.py` 모델과 키 집합 1:1(여분 키 금지). 세션 저장은 `mock_store.js`(sessionStorage `millie_mock_db`).

## E. 화면 파일 규약
- `demo/js/screens/d2_home.js` `d3_detail.js` `d4_reader.js` `d5_library.js` `d7_dashboard.js` `d8_showcase.js`: `export function render(state) → string`. import 는 `./ui.js` 만(`d3_detail.js` 는 배경용 `./d2_home.js` 추가 허용).
- `onboarding_step.js`: `render(state, step, meta)` · `s0_start.js`: `render(state)` · `s6_persona.js`: `render(state)`.
- `ui.js`(06-02 소유) 추가 export: `tabbar(active)`(`"home"|"library"` → `<nav class="tabbar">` 홈·서재 2탭, `data-act="nav" data-to="#/home|#/library"`) · `demoLabel(text = "데모 전용 · 실제 밀리 화면 아님")` → `<div class="demo-label">` · `banner(text)` → `<div class="banner">` · `modal(inner)` → `<div class="modal-wrap"><div class="modal">…</div></div>`. 기존 `esc cover badge josa icon statusbar navbar cta` 유지.
- 모든 사용자·카탈로그 문자열은 `esc()` 를 거쳐 삽입한다. `innerHTML` 에 원문 금지(XSS). `title ?? "(제목 없음)"` 방어.
- D7·D8 은 `#page` 컨테이너(전폭), 나머지는 `#phone`. 렌더 대상 결정은 `app.js` 만.

## F. 저장 키·상수
- `localStorage["millie_user_key"]` — UUIDv4(`crypto.randomUUID()`), 개인정보 아님.
- `sessionStorage["millie_events"]` — `app.js` 이벤트 송신 큐(EventIn[]; 50개 단위 `postEvents`).
- `sessionStorage["millie_mock_db"]` — `mock_store.js` 의 mock 서버 상태(users·snapshots·events·ratings·recommendations).
- 상수(값은 `contracts.py` 와 동일): `BUDGET_MS = 200` · `QUALIFIED_READ_MINUTES = 15` · `ROW_SIZE = 12` · `VARIANTS = ["pop","cf","hybrid","hybrid_div"]` · `BADGE_TYPES = ["bestseller","review","author","publisher","buzz","light"]` · EVENT_TYPES 13종(`impression detail_click library_add reader_open qualified_read completion rating preference_started preference_step preference_book_selected preference_book_deselected preference_completed preference_restarted`).
- `EventIn` 필드: `{event_id(uuid), user_key, book_id?, event_type, ts(ISO), surface?, row_id?, position?, selected?, recommendation_id?, model_version?, preference_snapshot_id?, candidate_set_id?, payload: {string: string}}` — 그 외 키 금지.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.js 재작성 — API_BASE="" · /api 접두어 · 13 함수 · 실패 격리 · 4초 fallback</name>
  <files>demo/js/api.js</files>
  <read_first>
    - demo/js/api.js (현행 113줄 — 계승: `get()` 타임아웃 래퍼 L18-28 · `local()` L30-33 · `post()` L100-113 · `config` 골격. 폐기: `API_BASE="https://REPLACE-ME.hf.space"` · `resetMockState` · `ensurePool(mock/books.json)` · `"/recommend"` 접두어 없는 경로 · `postEvent` 단건)
    - ../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md §0 공통 규약(접두어 `/api/*` 예외 `/health`·`/metrics`·`/docs` · 타임스탬프 `ts` · 오류 422/404) · §2~§13 경로·쿼리 이름
    - src/millie_rec/serving/schemas.py (요청 모델 `PreferencesRequest` `EventIn` `EventsBatchIn{events ≤50}` `RatingIn` · 응답 모델 이름)
    - ../.claude/rules/demo.md (`API_BASE=""` · `?source` `?api` `?capture=1|2` · 클라이언트 fallback 규칙)
  </read_first>
  <action>
`demo/js/api.js` 를 새로 쓴다(≤150줄). 상단 주석 1줄: `// fetch 래퍼. 부록 C 의 13개 함수가 source=api|mock 양쪽을 덮는다 — 응답 형태는 항상 serving/schemas.py.`
```js
import * as mock from "./mock.js";
const API_BASE = "";           // 같은 origin — FastAPI 가 demo/ 를 / 에 서빙(../.claude/rules/demo.md). ?api=<URL> 로만 덮어쓴다
const TIMEOUT_MS = 4000;       // 서버가 늦어도 메인 화면은 4초 안에 fallback(main 설계서 §7)
const qs = new URLSearchParams(location.search);
export const config = {
  source: ["api", "mock"].includes(qs.get("source")) ? qs.get("source") : null,
  apiBase: (qs.get("api") || API_BASE).replace(/\/$/, ""),
  capture: ["1", "2"].includes(qs.get("capture")) ? qs.get("capture") : null,
};
async function req(path, init = {}) {          // get/post/del 공용 — AbortController 4초
  const ctl = new AbortController(); const timer = setTimeout(() => ctl.abort(), TIMEOUT_MS);
  try {
    const res = await fetch(config.apiBase + path, { ...init, signal: ctl.signal,
      headers: init.body ? { "Content-Type": "application/json" } : undefined });
    if (!res.ok) throw new Error(`HTTP ${res.status} ${path}`);
    return res.status === 204 ? null : await res.json();
  } finally { clearTimeout(timer); }
}
const get = (p) => req(p);
const post = (p, body) => req(p, { method: "POST", body: JSON.stringify(body) });
const del = (p) => req(p, { method: "DELETE" });
const local = (p) => fetch(p).then((r) => { if (!r.ok) throw new Error(`HTTP ${r.status} ${p}`); return r.json(); });
/** 엔드포인트별 실패 격리 — Phase 5 가 아직 만들지 않은 경로(404)·네트워크 오류는 null(화면이 "서버 준비 중" 을 그린다). uncaught 0. */
const soft = (p) => p.catch((err) => { console.warn("[api]", String(err.message || err)); return null; });
```
공개 함수(부록 C 그대로, 각 함수는 `if (source === "mock") { await mock.init(); return mock.<name>(…); }` 분기 뒤 api 경로):
- `resolveSource()`: `if (config.source) return config.source; try { await get("/health"); return "api"; } catch { return "mock"; }`
- `loadSteps()` = `local("config/onboarding.json").then((d) => d.steps)`
- `health(s)` → mock: `mock.health()` / api: `soft(get("/health"))`
- `loadMeta(s)` → `soft(get("/api/meta/onboarding"))`
- `getCandidates(s, { categories, subcategories = [], n = 30, userKey })` → `const p = new URLSearchParams({ categories: categories.join(","), subcategories: subcategories.join(","), n: String(n), user_key: userKey || "" }); return soft(get(`/api/candidates/onboarding?${p}`));`
- `postPreferences(s, body)` → `soft(post("/api/preferences", body))` (201)
- `getRecommend(s, { userKey, snapshotId, model, k = 40, context })` → `const p = new URLSearchParams({ user_key: userKey || "", k: String(k) }); if (snapshotId) p.set("snapshot_id", snapshotId); if (model) p.set("model", model); if (context) p.set("context", context); try { return await get(`/api/recommend?${p}`); } catch (err) { const fb = await local("fallback/popular.json"); return { ...fb, client_fallback_reason: String(err.message || err) }; }` — **이 함수만 fallback**, `client_fallback_reason` 는 응답 객체에만 붙이고 어떤 파일에도 쓰지 않는다.
- `postEvents(s, events)` → `if (!events.length) return null; return soft(post("/api/events", { events: events.slice(0, 50) }));` (202)
- `postRating(s, body)` → `soft(post("/api/ratings", body))`
- `getUserState(s, userKey)` → `soft(get(`/api/users/${encodeURIComponent(userKey)}/state`))` · `getUserData` → `/data` · `deletePersonalization` → `soft(del(`/api/users/${encodeURIComponent(userKey)}/personalization`))`
- `getDashboard(s)` → `soft(get("/api/dashboard"))` · `getShowcase(s)` → `soft(get("/api/showcase"))`
금지 문자열: `hf.space` `REPLACE-ME` `"/recommend"`(접두어 없는) `timestamp` `books.json` `resetMockState` `postEvent(`(단건).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/api.js && wc -l js/api.js && grep -c 'const API_BASE = ""' js/api.js && grep -c 'TIMEOUT_MS = 4000' js/api.js && grep -cE '^export (async function|const|function) (config|resolveSource|loadSteps|health|loadMeta|getCandidates|postPreferences|getRecommend|postEvents|postRating|getUserState|getUserData|deletePersonalization|getDashboard|getShowcase)\b' js/api.js && grep -nE 'hf\.space|REPLACE-ME|"/recommend"|\btimestamp\b|books\.json|resetMockState|postEvent\(' js/api.js | wc -l && grep -c '"/api/' js/api.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/api.js` 종료 0 · `wc -l demo/js/api.js` ≤ 150
    - `grep -c 'const API_BASE = ""' demo/js/api.js` == 1 · `grep -c 'TIMEOUT_MS = 4000' demo/js/api.js` == 1
    - export 15개(부록 C 13 + `config` + `loadSteps`): `grep -cE '^export (async function|const|function) (config|resolveSource|loadSteps|health|loadMeta|getCandidates|postPreferences|getRecommend|postEvents|postRating|getUserState|getUserData|deletePersonalization|getDashboard|getShowcase)\b' demo/js/api.js` == 15
    - `grep -nE 'hf\.space|REPLACE-ME|"/recommend"|\btimestamp\b|books\.json|resetMockState|postEvent\(' demo/js/api.js | wc -l` == 0
    - `grep -c '"/api/' demo/js/api.js` ≥ 9 · `grep -c '"/health"' demo/js/api.js` ≥ 1 · `grep -c 'fallback/popular.json' demo/js/api.js` == 1 · `grep -c 'client_fallback_reason' demo/js/api.js` == 1
    - `grep -c '{ events: events.slice(0, 50) }' demo/js/api.js` == 1 · `grep -c 'encodeURIComponent(userKey)' demo/js/api.js` == 3
  </acceptance_criteria>
  <done>api.js 가 DEMO-01 문구 그대로(`API_BASE=""`·`/api`·`ts` 는 호출자·4초 fallback)이며 13 함수·실패 격리.</done>
</task>

<task type="auto">
  <name>Task 2: mock_store.js 신규 — sessionStorage mock DB · library 버킷 · state/data/delete · 세션 집계 DashboardOut</name>
  <files>demo/js/mock_store.js</files>
  <read_first>
    - src/millie_rec/serving/privacy_api.py (library 버킷 규칙: `READ_TYPES=("reader_open","qualified_read")` · `completed = completion 책` · `reading = READ_TYPES 책 − completed` · `added = library_add 책` · `snapshots[i].active = i==0(최신)` · `UserDataOut` 필드 · `PersonalizationDeleted.deleted{table: n}`)
    - src/millie_rec/serving/schemas.py `UserStateOut` `UserDataOut` `PersonalizationDeleted` `EventsAccepted` `RatingOut`
    - src/millie_rec/serving/schemas_should.py `DashboardOut` `KpiValue` `AbRow` `LatencyBlock`(필드·타입 — `by_hour: list[dict[str,int]]` · `quality: dict[str,float]`)
    - .planning/phases/05-must/05-CONTEXT.md D-13(kpi 6 이름·`events_recent` 50·`by_variant`·`by_hour`·`quality` 3키·`ab_table` 셀×신규/기존·`mde_note` 고정 문장) · D-08(cell sha256 · `users` 행) · 이벤트 품질 게이트(`event_id` 중복 = duplicates · `book_ineligible`)
    - .planning/phases/06-demo-rebuild/06-CONTEXT.md D-03(세션 이벤트 직접 집계 — 노출 수신율·fallback 비율·행별 read-start·첫 완독 도달 · 고정 예시 숫자 금지 · 하단 문구)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D7 표(KPI 6 · A/B 표 · latency · 품질 · 이벤트 스트림 50 · 노출 로그)
  </read_first>
  <action>
`demo/js/mock_store.js` 신규(≤250줄). 상단 주석: `// mock 모드의 "서버 상태" — sessionStorage millie_mock_db 1키. 서버(SQLite 7테이블)의 축소판이며 형태는 serving/schemas 와 1:1.`
```js
const KEY = "millie_mock_db";
const EMPTY = () => ({ users: {}, snapshots: [], events: [], ratings: [], recommendations: [] });
let db = null;
function load() { if (!db) { try { db = JSON.parse(sessionStorage.getItem(KEY)) || EMPTY(); } catch { db = EMPTY(); } } return db; }
function save() { sessionStorage.setItem(KEY, JSON.stringify(db)); }
export const hex6 = () => crypto.randomUUID().replace(/-/g, "").slice(0, 6);   // 서버 ID 형식 rec_/snap_/cand_/rat_ + 6hex
export async function sha256Hex8(text) {  // sha256 첫 8 hex — cellFor(셀, 05-CONTEXT D-08)·mock.js assignPersona(미매핑 카테고리, D-16) 공용. 서버 hashlib.sha256(...).hexdigest()[:8] 과 같은 값
  const buf = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(text));
  return [...new Uint8Array(buf)].slice(0, 4).map((b) => b.toString(16).padStart(2, "0")).join("");
}
export async function cellFor(userKey) {  // 05-CONTEXT D-08 그대로: int(sha256(user_key)[:8],16) % 2 == 0 → "A"
  return parseInt(await sha256Hex8(userKey), 16) % 2 === 0 ? "A" : "B";
}
```
export 함수(전부 동기, `load()` 후 `save()`):
- `upsertUser(userKey, { consent, cell })` → `db.users[userKey] = { user_key, consent, cell, created_at: (기존 ?? now) }` · `getUser(userKey)`.
- `addSnapshot(row)` — `row = { snapshot_id: "snap_"+hex6(), user_key, created_at: now, reading_time, categories, criterion(id), subcategories, seeds, persona, candidate_set_id, consent }` push(**삭제 없음 — append only**, 05-CONTEXT D-08) · `snapshotsOf(userKey)` 최신순 · `latestSnapshot(userKey)`.
- `addEvents(events)` → `EventsAccepted`: `event_id` 가 이미 있으면 `duplicates++`(저장 안 함), 없으면 push·`accepted++`; `flagged` = `book_id` 가 `isEligible(book_id)`(주입 callable, `setCatalog(byId)` 로 등록) 아니면 `{event_id, quality_flag: "book_ineligible"}`(플래그 붙어도 저장). 반환 `{ accepted, duplicates, flagged }`.
- `eventsOf(userKey, types = null)` 최신순 · `booksBy(userKey, types)` distinct book_id 최신순.
- `library(userKey)` → privacy_api 규칙: `completed = booksBy(uk, ["completion"])`, `reading = booksBy(uk, ["reader_open","qualified_read"]).filter(b => !completed.includes(b))`, `added = booksBy(uk, ["library_add"])`. `LibraryBook` 조립은 mock.js 가 카탈로그로 한다 → 여기서는 id 배열 반환.
- `continueIds(userKey)` = `reader_open` 책 − `completion` 책, 최신순(05-CONTEXT D-03 continue_reading).
- `lastCompleted(userKey)` → 최신 `completion` book_id | null.
- `sessionActive(userKey, now)` → 최근 1800초 안 `reader_open`·`detail_click` 1개 이상.
- `nHistory(userKey)` = `booksBy(uk, ["reader_open","qualified_read","completion"]).length` · `nCompleted(userKey)`.
- `addRating(row)` → push, `rating` 이벤트는 mock.js 가 `addEvents` 로 함께 넣는다. `ratingsOf(userKey)`.
- `logRecommendation(row)` — `{ recommendation_id, user_key, ts, model_version, cell, forced, fallback_level, latency_ms, latency_breakdown, rows: [{row_id, book_id, position}], n_items }` push(05-CONTEXT D-12 모든 응답 1회).
- `userData(userKey)` → `UserDataOut{ user: users[uk] ?? { user_key }, snapshots, events, ratings, recommendations, exported_at: now, note: "가명 user_key 외 개인정보 없음" }`.
- `deleteUser(userKey)` → `PersonalizationDeleted{ user_key, consent: false, deleted: { preference_snapshots: n1, events: n2, ratings: n3, recommendations: n4 } }`; `users[uk].consent = false`(행 유지).
- `reset()` → `db = EMPTY(); save()`.
- **`dashboard(now)` → `DashboardOut`(세션 집계, 06-CONTEXT D-03):**
  - `const recs = db.recommendations, ev = db.events; const pct = (a, b) => (b ? Math.round((a / b) * 1000) / 1000 : 0);` `percentile(arr, p)` 정렬 후 `arr[Math.min(arr.length-1, Math.floor(p*arr.length))]` (빈 배열 → 0).
  - `kpi`(이름 6개 고정, `KpiValue{value, n, note}`): `qualified_reading_start_rate = pct(distinct(user,book) qualified_read, distinct(user,book) reader_open)` n=reader_open distinct 수, note "QRS = 가상 15분 도달 / 뷰어 진입" · `first_completion_rate_new = pct(스냅샷 1개인 user 중 completion 있는 수, 스냅샷 1개인 user 수)` n · `fallback_rate = pct(recs.filter(r => r.fallback_level >= 1).length, recs.length)` · `p95_latency_ms = percentile(recs.map(r => r.latency_ms), .95)` note **"mock 상수 · 참고용 — PDF 숫자 아님"** · `error_rate = 0` n=recs.length note "mock 은 오류를 내지 않는다" · `active_user_keys = distinct user_key in ev` n=ev.length.
  - `ab_table`: `["A","B"] × ["new","existing"]`(new = 그 user 의 스냅샷 1개) → `AbRow{ cell, segment, n(user 수), primary: QRS rate, reader_open: pct(reader_open user 수, n), completion: pct(completion user 수, n), rating_mean: 평균 stars|null, first_completion: completion rate, p95_ms, fallback_rate }`; n=0 인 셀은 null 값. **행 4개 항상 존재**(표가 비지 않게).
  - `mde_note = "데모 표본으로 검정하지 않음 — MDE +1%p 검출에 셀당 n만 명"`(06-CONTEXT D-03 문구 그대로).
  - `latency = { p50, p95, p99 (recs.latency_ms 백분위), by_stage: { feature: [p50,p95], retrieval: …, ranking: …, rerank: …, compose: … } }`(breakdown 키별 백분위 — mock 은 상수라 같은 값).
  - `quality = { impression_receipt_rate: pct(impression 이벤트 수(surface "home"), recs 의 n_items 합), flagged_events: 플래그 수, feature_freshness_s: 1.0 }`.
  - `events_recent` = 최근 50(최신순, EventIn 그대로) · `impressions_log` = `impression` 중 `candidate_set_id` 있는 것 최근 50 `{event_id, candidate_set_id, book_id, position, selected, ts}` · `by_variant` = `{model_version: count}` · `by_hour` = `[{ hour: h, n: count }]` 24행 중 count>0 만 · `generated_at = now` · `window = "session"`.
  숫자는 전부 세션 실측 — 고정 예시 값 금지(`../.claude/rules/simplicity.md` 숫자 = results/ 규칙과 충돌).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/mock_store.js && wc -l js/mock_store.js && grep -cE '^export (async )?function (sha256Hex8|cellFor|upsertUser|addSnapshot|addEvents|library|continueIds|lastCompleted|sessionActive|nHistory|nCompleted|addRating|logRecommendation|userData|deleteUser|reset|dashboard)\b' js/mock_store.js && grep -c '"millie_mock_db"' js/mock_store.js && grep -c 'qualified_reading_start_rate\|first_completion_rate_new\|fallback_rate\|p95_latency_ms\|error_rate\|active_user_keys' js/mock_store.js && grep -c 'MDE +1%p 검출에 셀당 n만 명' js/mock_store.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/mock_store.js` 종료 0 · `wc -l demo/js/mock_store.js` ≤ 250
    - export 17개: `grep -cE '^export (async )?function (sha256Hex8|cellFor|upsertUser|addSnapshot|addEvents|library|continueIds|lastCompleted|sessionActive|nHistory|nCompleted|addRating|logRecommendation|userData|deleteUser|reset|dashboard)\b' demo/js/mock_store.js` == 17
    - `grep -c '"millie_mock_db"' demo/js/mock_store.js` == 1 · `grep -c 'crypto.subtle.digest("SHA-256"' demo/js/mock_store.js` == 1
    - kpi 6 이름 전부 존재: `grep -c 'qualified_reading_start_rate\|first_completion_rate_new\|fallback_rate\|p95_latency_ms\|error_rate\|active_user_keys' demo/js/mock_store.js` ≥ 6
    - `grep -c '데모 표본으로 검정하지 않음 — MDE +1%p 검출에 셀당 n만 명' demo/js/mock_store.js` == 1 · `grep -c 'PDF 숫자 아님' demo/js/mock_store.js` ≥ 1
    - `grep -c '가명 user_key 외 개인정보 없음' demo/js/mock_store.js` == 1 · `grep -c 'book_ineligible' demo/js/mock_store.js` == 1
    - 고정 예시 숫자 없음: `grep -nE 'value: [1-9][0-9]*\.[0-9]+' demo/js/mock_store.js | wc -l` == 0
    - Node 단위 실행(브라우저 API 대체): `node -e 'globalThis.sessionStorage={_:{},getItem(k){return this._[k]??null},setItem(k,v){this._[k]=v}};globalThis.crypto=require("crypto").webcrypto;import("./js/mock_store.js").then(async m=>{m.reset();m.upsertUser("u",{consent:true,cell:await m.cellFor("u")});m.addEvents([{event_id:"e1",user_key:"u",event_type:"reader_open",book_id:1,ts:new Date().toISOString(),payload:{}},{event_id:"e1",user_key:"u",event_type:"reader_open",book_id:1,ts:"x",payload:{}}]);const d=m.dashboard(new Date().toISOString());console.log(Object.keys(d.kpi).length, d.ab_table.length, m.library("u").reading.length, JSON.stringify(m.addEvents([])));})'` (demo 디렉터리에서, `package.json` 없이 ESM 로드가 안 되면 `--input-type=module` 로 대체) 출력 `6 4 1 {"accepted":0,"duplicates":0,"flagged":[]}` — 실행 결과를 SUMMARY 에 인용(불가 시 사유 기록)
  </acceptance_criteria>
  <done>mock_store 가 서버 7테이블의 축소판으로 동작하고 DashboardOut 세션 집계를 낸다.</done>
</task>

<task type="auto">
  <name>Task 3: mock.js 폐기 후 새로 — 카탈로그 로드 · candidates 라운드로빈 · preferences(스냅샷·cell·persona) · recommend 5행 compose(dedup·배지·weights·after_completion·비개인화) · state/data/delete/dashboard/showcase 위임</name>
  <files>demo/js/mock.js</files>
  <read_first>
    - demo/js/mock.js (현행 206줄 — 계승: `init/byId/index Map` L19-33 · `interleave` L95-109 · dedup 루프 골격 L179-191. 폐기: `PERSONAS` 라벨 비교 · `badgeFor` 의 `"rating"`·라벨 비교 · `ROW_TITLES.similar_readers` · `byYear`·`ratings_count` 정렬 · `model_version "_mock"` · `latency_ms` 객체 · `itemknn`)
    - src/millie_rec/serving/rows.py (행 상수·`neighbor_row`·`fresh_row` 라운드로빈·`personal_rows` 순서 — JS 로 1:1)
    - src/millie_rec/serving/compose.py L1-70 (`ROW_ORDER` · `normalize_title` · `dedup_rows` — book_id ∧ 정규화 제목 · `ZERO_WEIGHTS`)
    - src/millie_rec/serving/fallback.py L13-16 (`TRENDING_TITLE` · purpose `fallback` · `SOURCE_POPULARITY`)
    - src/millie_rec/serving/badges.py (배지 규칙 전문 — 문구 f-string 을 템플릿 리터럴로)
    - src/millie_rec/serving/persona.py (`PERSONAS` · `CATEGORY_TO_PERSONA` · `DESCRIPTION` · `CATS_NONE="다양한 분야"` · `CRITERION_NONE="취향"` · `_josa`)
    - src/millie_rec/serving/schemas.py (`RecommendOut` `RowOut` `ItemOut` `BadgeOut` `CandidateSet` `PreferencesResponse` `HealthOut` 필드 — 여분 키 금지)
    - .planning/phases/05-must/05-CONTEXT.md D-01~D-04(5행·dedup·비개인화) · D-13(after_completion 최상단 "『X』을 완독하셨네요, 다음은") · D-15(후보 라운드로빈 30) · D-16(페르소나)
    - .planning/phases/06-demo-rebuild/06-PATTERNS.md §2-4 (weights 표시 공식 후보 · A/B 셀 · 응답 조립 골격)
    - demo/js/screens/ui.js L30-37 (`josa(word, withFinal, without)`)
    - demo/fallback/popular.json 상단 40줄(level 3 응답 실물 키 집합)
  </read_first>
  <action>
`demo/js/mock.js` 를 새로 쓴다(≤250줄). `import { josa } from "./screens/ui.js"; import * as store from "./mock_store.js";` 상수(값은 `contracts.py`·`rows.py` 와 동일):
```js
const ROW_SIZE = 12, ANCHOR_NEIGHBORS = 20, K_ITEMS = 40, SUPPORTED_MIN = 20, SESSION_WINDOW_S = 1800;
const VARIANTS = ["pop", "cf", "hybrid", "hybrid_div"];
const TITLE = { continue_reading: "이어 읽기", trending: "지금 많이 읽는 책", fresh_picks: "새로운 발견", persona: "{name}의 서가", personaDefault: "회원님의 서가" };
const SUBTITLE_ANCHOR = "결이 비슷한 책", REASON_ANCHOR = (t) => `『${t}』을 좋아하셨다면`, TITLE_AFTER = (t) => `『${t}』을 완독하셨네요, 다음은`;
const LATENCY = { feature: 4.0, retrieval: 12.0, ranking: 9.0, rerank: 6.0, compose: 7.0, total: 38.0 };  // 표시값 — PDF 숫자 아님(백엔드 01 §0)
const NEARLINE_LAG_S = 1.0;   // mock 은 즉시 반영, 표시용 결정적 상수
const PERSONAS = [["오디세우스","오디세이아","지혜로 승리하리라!"],["셜록 홈즈","주홍색 연구","사소한 것이 가장 중요하다."],["돈키호테","돈키호테","이룰 수 없는 꿈을 꾸리라!"],["제인 에어","제인 에어","나는 나 자신의 주인입니다."]];
const CATEGORY_TO_PERSONA = { 경제경영: 0, 자기계발: 0, IT: 0, 소설: 1, 과학: 1, 철학: 1, 인문: 2, 역사: 2, 사회: 2, "에세이/시": 3, 라이프스타일: 3 };
const REVIEW_MIN_COUNT = 3, REVIEW_MIN_COUNT_NO_RATING = 10;
```
- **`init()`**: `Promise.all([catalog_kr.json, neighbors_kr.json, showcase.json].map(local))` 1회 캐시 → `pool`(pop_rank 순), `index = new Map`, `nbrs`, `showcaseData`; `store.setCatalog((id) => index.has(id))`. `byId(id)`. `byPop(cats = null)` = pool 을 카테고리 교집합으로 필터(pop_rank 순). `allCategories` = 카테고리별 권수 내림차순 이름 배열.
- **`health()`** → `{ status: "ok", api_version: "v2", model_version: "hybrid_div_v1", artifacts_loaded_at: null, db_ok: true, db_row_count: {}, nearline_last_run: null, uptime_s: 0 }`(HealthOut 키 그대로).
- **`loadMeta()`** → `local("mock/meta_onboarding.json")`(06-01 산출, D-10).
- **`getCandidates({ categories, n = 30, userKey })`** → 05-CONTEXT D-15 라운드로빈: `lists = categories.map(c => byPop([c]).map(b => b.book_id))` → `zip_longest` 방식으로 1권씩 돌며 중복 제거 `n` 까지 → `CandidateSet{ candidate_set_id: "cand_" + store.hex6(), survey_variant: "v1", created_at: now, items: ids.map((id, i) => ({ book_id, title, authors, image_url, position: i, book_format })) }`(CandidateItem 6키만). `subcategories` 는 필터에 쓰지 않는다.
- **`async assignPersona(categories, criterionLabel)`** — persona.py·05-CONTEXT D-16 1:1: `const head = cats[0] ?? ""; const idx = CATEGORY_TO_PERSONA[head] ?? parseInt(await store.sha256Hex8(head), 16) % 4`(서버 `int(hashlib.sha256(head.encode()).hexdigest()[:8], 16) % 4` 와 같은 값 — `cellFor` 가 쓰는 digest 헬퍼 재사용, 추가 비용 0; 06-01 `make_mock.py assign_persona` 도 같은 식이라 mock/api/생성기 세 곳의 페르소나가 1:1. `codePointAt` 해시 금지); `shown = cats.length >= 2 ? `${cats[0]}${josa(cats[0], "과", "와")} ${cats[1]}` : cats[0] ?? "다양한 분야"`; `crit = criterionLabel ?? "취향"`; `description = `회원님은 ${shown}${josa(shown, "을", "를")} 즐기고, ${crit}${josa(crit, "으로", "로")} 책을 고르는 독서가입니다.``. 반환 `{ name, work, quote, description }`.
- **`postPreferences(body)`**(비동기 — cell sha256): `const cell = await store.cellFor(body.user_key); store.upsertUser(body.user_key, { consent: body.consent, cell });` `criterionLabel` = `meta.criteria.find(c => c.id === body.criterion)?.label`(meta 는 `loadMeta()` 캐시) → `persona = await assignPersona(body.categories, criterionLabel)` → `snap = store.addSnapshot({ user_key, reading_time, categories, criterion: body.criterion, subcategories, seeds: body.consent ? body.seeds : [], persona, candidate_set_id, consent })` → `body.consent` 이면 seeds 마다 `store.addEvents([{ event_id: crypto.randomUUID(), user_key, book_id, event_type: "library_add", ts: now, surface: "onboarding", payload: {} }])`(서버 자동 기록 흉내) → `PreferencesResponse{ user_key, preference_snapshot_id: snap.snapshot_id, created_at: snap.created_at, cell, snapshots_count: store.snapshotsOf(uk).length, persona }`.
- **`badgeFor(card, criterion, seedAuthors, seedPublishers)`** — badges.py 1:1: `best = card.pop_rank != null ? { type: "bestseller", text: `인기 ${card.pop_rank}위` } : null; if (criterion === "bestseller") return best; if (criterion === "review") { const n = card.review_count ?? 0; if (card.average_rating != null && n >= 3) return { type: "review", text: `★${card.average_rating.toFixed(1)} · 리뷰 ${n}` }; if (n >= 10) return { type: "review", text: `리뷰 ${n}` }; return best; } if (criterion === "author" && seedAuthors.has(card.authors)) return { type: "author", text: `${card.authors} 작가` }; if (criterion === "publisher" && seedPublishers.has(card.publisher)) return { type: "publisher", text: `${card.publisher} 출판` }; if (criterion === "buzz" && card.millie_label) return { type: "buzz", text: String(card.millie_label) }; return null;` (`light` 는 생성하지 않는다 — Should, 서버 미구현).
- **`item(card, position, source, reason = null, score = null)`** → ItemOut 12키 정확히: `{ book_id, score: score ?? ROW_SIZE - position, source, reason, title: card.title ?? null, authors, image_url, position, source_channels: [source], badge: null, book_format, difficulty }`.
- **`neighborRow(rowId, title, subtitle, seed, exclude, purpose = "discover")`** — rows.py `neighbor_row`: `(nbrs[String(seed)] ?? []).filter(([d]) => index.has(d) && !exclude.has(d) && d !== seed).slice(0, ROW_SIZE)` → items `source "content"`, `reason = title` → `{ row_id, title, purpose, items, subtitle, channel_mix: { content: n } }`; items 0 이면 null.
- **`popRow(rowId, title, purpose, ids, exclude)`** — `ids.filter(!exclude).slice(0,12)` → items `source "popularity"`, `channel_mix { popularity: n }`.
- **`freshRow(categories, exclude)`** — 선택 카테고리 밖: `others = allCategories.filter(c => !chosen.has(c))` → 카테고리별 `byPop([c]).slice(0, 30)` 풀 라운드로빈(현행 `interleave` 골격) → 12 → `{ row_id: "fresh_picks", title: "새로운 발견", purpose: "explore", … popularity }`.
- **`normalizeTitle(s)`** = `String(s ?? "").replace(/[^\p{L}\p{N}_]/gu, "").toLowerCase()` · **`dedup(rows)`** — compose.py `dedup_rows`: 앞 행 우선, `book_id` 또는 정규화 제목이 이미 보였으면 제거·`removed++`, position 재부여, `channel_mix` 재계산(`source_channels` 집계). 빈 행도 남긴다(프론트가 숨김).
- **`weightsFor(userKey, snapshots, consent, now)`** — 표시 규칙(스코어링 아님, 주석 명시): `if (!consent || !snapshots.length) return { alpha: 0, beta: 0, gamma: 0 }; const n = store.nHistory(uk); let a = Math.max(0.2, 0.7 - 0.05 * n), b = 0.2 + 0.05 * n, g = 0.1; if (store.sessionActive(uk, now)) g += 0.1; const latest = snapshots[0]; if (snapshots.length >= 2 && Date.now() - Date.parse(latest.created_at) < 86400e3) a += 0.15; const s = a + b + g; return { alpha: round3(a / s), beta: round3(b / s), gamma: round3(g / s) };`
- **`getRecommend({ userKey, snapshotId, model, k = 40, context })`**: `const snaps = store.snapshotsOf(uk); const user = store.getUser(uk); const snap = snapshotId ? snaps.find(s => s.snapshot_id === snapshotId) ?? snaps[0] : snaps[0]; const personalized = !!(user?.consent && snap && snap.consent);` `forced = VARIANTS.includes(model); variant = forced ? model : (user?.cell === "B" ? "hybrid_div" : "hybrid")`.
  - **비개인화(level 3, SERV-08)**: `rows = [popRow("trending", "지금 많이 읽는 책", "fallback", pool ids, ∅), freshRow([], usedIds)]`(fresh 는 전 카테고리 라운드로빈) · `model_version = "fallback_v1"` · `weights 0` · `fallback_level 3` · `cell = user?.cell ?? null` · `preference_snapshot_id null` · `items []`.
  - **개인화(level 0)**: `seeds = snap.seeds; exclude = new Set(seeds); persona = snap.persona; criterion = snap.criterion; seedCards = seeds.map(byId).filter(Boolean); seedAuthors = new Set(seedCards.map(c => c.authors).filter(Boolean)); seedPublishers = …publisher`. 행: ① `after_completion`(Should, 05-10): `const done = store.lastCompleted(uk); if (done && byId(done)) rows.push(neighborRow("after_completion", TITLE_AFTER(byId(done).title), null, done, exclude, "resume"))` **최상단** ② `continue_reading`: `popRow`-형 items(source `"content"` 가 아니라 source `null`·`source_channels []`? — 서버 rows.py 는 `ScoredItem(b, score, position)` 로 source 없음 → JS 도 `source: null, source_channels: []`) from `store.continueIds(uk)`, title "이어 읽기", purpose resume, `channel_mix {}`; 비면 `items: []` 그대로 push ③ `anchor_<seed₁>`: `seed1 = seeds[0]; neighborRow(`anchor_${seed1}`, REASON_ANCHOR(byId(seed1)?.title ?? String(seed1)), SUBTITLE_ANCHOR, seed1, exclude)` ④ `persona_shelf`(title `${persona?.name ?? "회원님"}의 서가`, discover): 표시 규칙 — `pop` → `byPop(snap.categories)` 12 (popularity) · `cf`/`hybrid` → 전 시드 이웃 합집합(weight 내림차순, `source "content"`) 채운 뒤 부족분 `byPop(snap.categories)` (popularity) · `hybrid_div` → `hybrid` 결과를 카테고리(`card.categories[0]`) 라운드로빈(`interleave`) — 12권, `channel_mix` 는 집계 ⑤ `trending`(전체 pop_rank 순, exclude) ⑥ `freshRow(snap.categories, exclude)`. 각 행 push 후 `exclude` 에 그 행 book_id 추가. → `attachBadges`(criterion 있으면 모든 items 에 `badgeFor`) → `dedup` → `items = rows.flatMap(r => r.items).slice(0, k)` → `model_version = variant + "_v1"` · `fallback_level 0` · `weights = weightsFor(...)` · `nearline_lag_s = NEARLINE_LAG_S`.
  - 공통 응답(RecommendOut 15키 정확히): `{ recommendation_id: "rec_" + store.hex6(), model_version, preference_snapshot_id: snap?.snapshot_id ?? null, user_key: uk, cell, forced, fallback_level, context: context ?? snap?.reading_time ?? null, latency_ms: LATENCY.total, latency_breakdown: { ...LATENCY }, user_state_weights, dedup_removed, nearline_lag_s, items, rows }` → `store.logRecommendation({ … rows: rows.flatMap(r => r.items.map(i => ({ row_id: r.row_id, book_id: i.book_id, position: i.position }))), n_items: 합 })` → 반환. `itemknn` 문자열 0.
- **`postEvents(events)`** → `store.addEvents(events)` · **`postRating(body)`** → `store.addRating(body); store.addEvents([{ event_id: crypto.randomUUID(), user_key: body.user_key, book_id: body.book_id, event_type: "rating", ts: body.ts, recommendation_id: body.recommendation_id ?? null, payload: { stars: String(body.stars) } }]); return { ok: true, rating_id: body.rating_id }`.
- **`getUserState(userKey)`** → `UserStateOut{ user_key, consent: user?.consent ?? false, cell: user?.cell ?? await store.cellFor(uk), is_new: snaps.length <= 1, library: { added, reading, completed } (각 `LibraryBook{book_id, title, image_url}` — byId 조인, 없으면 title null), snapshots: snaps.map((s, i) => ({ snapshot_id, created_at, categories, criterion, active: i === 0 })), user_state_weights: weightsFor(...), nearline_lag_s: NEARLINE_LAG_S }`. 사용자 행 없고 스냅샷 없으면 그래도 형태를 준다(404 대신 — mock 은 익명도 서재 화면을 그린다).
- **`getUserData(uk)`** → `store.userData(uk)` · **`deletePersonalization(uk)`** → `store.deleteUser(uk)` · **`getDashboard()`** → `store.dashboard(now)` · **`getShowcase()`** → `showcaseData`(`personal_case` null 이면 그대로 — 화면이 "5권 선정 대기").
금지: `itemknn` · `"rating"` 배지 타입 · `_mock` 접미 · `Math.random` 으로 latency 지터(결정적 상수만) · `ratings_count`·`year`·`format` 키.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/mock.js && wc -l js/mock.js && grep -cE '^export (async )?function (init|byId|health|loadMeta|getCandidates|postPreferences|getRecommend|postEvents|postRating|getUserState|getUserData|deletePersonalization|getDashboard|getShowcase)\b' js/mock.js && grep -nE 'itemknn|\btype: "rating"|_mock"|ratings_count|\bformat:|Math\.random\(\) - 0\.5' js/mock.js | wc -l && grep -c 'catalog_kr.json\|neighbors_kr.json\|showcase.json' js/mock.js && grep -c '"fallback_v1"\|latency_ms: LATENCY.total\|"after_completion"\|"persona_shelf"\|"fresh_picks"\|"trending"\|"continue_reading"' js/mock.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/mock.js` 종료 0 · `wc -l demo/js/mock.js` ≤ 250
    - export 14개: `grep -cE '^export (async )?function (init|byId|health|loadMeta|getCandidates|postPreferences|getRecommend|postEvents|postRating|getUserState|getUserData|deletePersonalization|getDashboard|getShowcase)\b' demo/js/mock.js` == 14
    - `grep -nE 'itemknn|\btype: "rating"|_mock"|ratings_count|\bformat:|Math\.random\(\) - 0\.5' demo/js/mock.js | wc -l` == 0
    - `grep -c 'mock/catalog_kr.json' demo/js/mock.js` == 1 · `grep -c 'mock/neighbors_kr.json' demo/js/mock.js` == 1 · `grep -c 'mock/showcase.json' demo/js/mock.js` == 1 · `grep -c 'books.json' demo/js/mock.js` == 0
    - 행 5 + Should 1 문자열: `grep -c '"continue_reading"\|"persona_shelf"\|"trending"\|"fresh_picks"\|"after_completion"' demo/js/mock.js` ≥ 5 · `grep -c '`anchor_${' demo/js/mock.js` == 1
    - `grep -c '"fallback_v1"' demo/js/mock.js` == 1 · `grep -c 'latency_ms: LATENCY.total' demo/js/mock.js` == 1 · `grep -c 'NEARLINE_LAG_S = 1.0' demo/js/mock.js` == 1
    - 배지 문구 1:1: `grep -c '인기 ${card.pop_rank}위' demo/js/mock.js` == 1 · `grep -c '★${card.average_rating.toFixed(1)} · 리뷰 ${n}' demo/js/mock.js` == 1 · `grep -c '} 작가`' demo/js/mock.js` == 1 · `grep -c '} 출판`' demo/js/mock.js` == 1 · `grep -c '"light"' demo/js/mock.js` == 0
    - 페르소나 문장 1:1: `grep -c '즐기고, ${crit}${josa(crit, "으로", "로")} 책을 고르는 독서가입니다.' demo/js/mock.js` == 1 · `grep -c '"다양한 분야"' demo/js/mock.js` == 1 · `grep -c 'store.sha256Hex8' demo/js/mock.js` == 1 · `grep -c 'codePointAt' demo/js/mock.js` == 0 (미매핑 카테고리 해시 = 서버 sha256, 05-CONTEXT D-16)
    - dedup: `grep -c 'replace(/\[^\\p{L}\\p{N}_\]/gu, "")' demo/js/mock.js` == 1 · `grep -c 'dedup_removed' demo/js/mock.js` ≥ 1
    - 응답 키 집합 확인(정적): `grep -c 'recommendation_id: "rec_"' demo/js/mock.js` == 1 · `grep -c 'user_state_weights' demo/js/mock.js` ≥ 1 · `grep -c 'nearline_lag_s' demo/js/mock.js` ≥ 2
    - Node 단위 실행: `sessionStorage`/`crypto`/`fetch` 를 셈(fetch 는 `demo/mock/*.json` 을 `fs` 로 읽는 stub) → `import("./js/mock.js")` 후 `await init(); const r = await postPreferences({user_key:"u1",consent:true,reading_time:"저녁, 하루를 마치며",categories:["소설","인문"],criterion:"bestseller",subcategories:[],seeds:[1012,2765,1446,1222,2292],candidate_set_id:null,restart:false}); const rec = await getRecommend({userKey:"u1"}); console.log(r.cell, rec.rows.map(x=>x.row_id+":"+x.items.length).join(" "), rec.fallback_level, rec.model_version, typeof rec.latency_ms, Object.keys(rec).length)` → 기대 `A|B` · `continue_reading:0 anchor_1012:12 persona_shelf:12 trending:12 fresh_picks:12`(dedup 으로 일부 <12 가능) · `0` · `hybrid_v1|hybrid_div_v1` · `number` · `15`. 이어 `await getRecommend({userKey:"anon"})` → `rows` 2개 `trending fresh_picks`, `model_version "fallback_v1"`, `fallback_level 3`. 출력을 SUMMARY 에 인용(ESM 로드 불가 시 사유 기록·06-06 브라우저 체크리스트로 이관)
  </acceptance_criteria>
  <done>mock.js 가 서버 규칙(5행·dedup·배지·페르소나·cell·비개인화·after_completion) 을 표시 규칙으로 재현하고 RecommendOut 15키 형태를 낸다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 서버(`/api/*`) | `api.js` 가 유일한 송신 지점. 가명 키·이벤트·별점만. 시크릿 없음 |
| `?api=<URL>` 덮어쓰기 | 사용자 명시 시만 다른 origin |
| `demo/mock/*.json`(정적) → `mock.js` | 06-01 생성기 산출물 — 계약 형태만 신뢰 |
| `sessionStorage millie_mock_db` | 같은 origin·탭 한정. 가명 키·이벤트만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Denial of Service | 서버 지연·다운이 메인 화면을 막음 | mitigate | `getRecommend` 4초 `AbortController` → `fallback/popular.json` + `client_fallback_reason`; 그 외 함수는 `soft()` 로 null(화면 "서버 준비 중") — uncaught 0 |
| T-06-03-02 | Tampering | `user_key` 를 URL 경로에 그대로 삽입(경로 조작) | mitigate | `encodeURIComponent(userKey)` 3곳 · 서버는 `USER_KEY_RE` 422 |
| T-06-03-03 | Spoofing | `?api=` 로 제3자 서버에 이벤트 전송 | accept | 기본 `API_BASE=""`(같은 origin) · 명시 시만 · 전송 데이터에 개인정보 없음(백엔드 01 §0 가명 키) |
| T-06-03-04 | Information Disclosure | mock 응답에 밀리 설명문·리뷰 텍스트 | mitigate | `catalog_kr.json` 이 카드 16키만(06-01 테스트) · `item()` 은 ItemOut 12키 화이트리스트로만 조립 |
| T-06-03-05 | Tampering | `client_fallback_reason` 같은 디버그 키가 계약 파일·서버로 새어 나감 | mitigate | `api.js` 응답 객체에만 붙임(파일 쓰기 없음) · `mock.js` 는 붙이지 않음 · `/contract-sync` 2항 |
| T-06-03-06 | Repudiation | mock 대시보드가 고정 예시 숫자로 "실측처럼" 보임 | mitigate | `mock_store.dashboard` 는 세션 이벤트 집계만 · `p95_latency_ms.note "mock 상수 · 참고용 — PDF 숫자 아님"` · `mde_note` 고정 고지 |
| T-06-03-07 | Information Disclosure | `getUserData` 가 다른 user_key 의 데이터 반환 | mitigate | mock_store 전 조회는 `user_key ===` 필터 · api 모드는 서버 `privacy_api` 가 경계 |
| T-06-03-08 | Denial of Service | `catalog_kr.json`(≈2~3MB) 반복 로드 | mitigate | `init()` Promise 1회 캐시 |
</threat_model>

<verification>
- `node --check` 3파일 종료 0 · `wc -l` 상한(api 150 · mock 250 · mock_store 250)
- api.js: `API_BASE=""` · `TIMEOUT_MS=4000` · export 15 · 구 형태 0 · `/api/` ≥ 9 · fallback 1곳
- mock.js: export 14 · 행 문자열 6 · 배지·페르소나 문구 1:1 · `itemknn` 0 · Node 단위 실행 출력 인용(가능 시)
- mock_store.js: export 17(`sha256Hex8` 포함) · kpi 6 · MDE 문구 · 고정 숫자 0 · Node 단위 실행 출력 인용(가능 시)
- 이 플랜 소유 3파일에서 06-07 구 형태 정규식 0건: `cd demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' js/api.js js/mock.js js/mock_store.js | wc -l` == 0
</verification>

<success_criteria>
- DEMO-01 기계 판정: `API_BASE=""`·`/api` 접두어·`ts`(호출자 EventIn)·4초 fallback · 구 형태 0
- DEMO-02 후반: mock 응답이 `serving/schemas.py` 키 집합 1:1(RecommendOut 15키·CandidateSet·PreferencesResponse·UserStateOut·UserDataOut·PersonalizationDeleted·DashboardOut·HealthOut)
- 06-CONTEXT D-01·D-03·D-05 이행 · 05-CONTEXT D-01~D-04·D-08·D-13·D-15·D-16 규칙이 JS 로 존재
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-rebuild/06-03-SUMMARY.md` — 변경 파일 목록(커밋 0) · `wc -l` · export 목록 · Node 단위 실행 출력(또는 불가 사유) · 서버 규칙 ↔ JS 함수 대응표(compose/badges/persona/privacy_api) · 06-06 에 넘기는 주의(`client_fallback_reason` 판정, `continue_reading` 빈 행).
</output>
