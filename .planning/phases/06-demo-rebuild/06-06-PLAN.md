---
phase: 06-demo-rebuild
plan: 06
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04", "06-05"]
files_modified:
  - demo/js/screens/d2_home.js
  - demo/js/screens/d3_detail.js
  - demo/js/screens/s7_home.js       # 삭제(→ d2_home.js)
  - demo/js/screens/s8_detail.js     # 삭제(→ d3_detail.js)
  - demo/js/inspector.js
  - demo/css/home.css
  - demo/css/inspector.css
autonomous: true
requirements: [DEMO-05, DEMO-03]
lane: Demo (메인·상세·인스펙터 통합 Worker — wave 3 단독, mock 완주 수동 체크리스트)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(05 관례, 사용자 지시 2026-09-05)
must_not_touch:
  - src/**
  - tests/**
  - Makefile
  - pyproject.toml
  - Dockerfile
  - artifacts/**
  - results/**
  - report/**
  - demo/js/app.js            # 06-02 — 어긋남 발견 시 SUMMARY 에 수정 브리프 제안(직접 수정 금지)
  - demo/js/actions.js        # 06-02
  - demo/js/presets.js        # 06-02
  - demo/js/router.js         # 06-02
  - demo/js/api.js            # 06-03
  - demo/js/mock.js           # 06-03
  - demo/js/mock_store.js     # 06-03
  - demo/js/screens/ui.js     # 06-02
  - demo/js/screens/onboarding_step.js
  - demo/js/screens/s0_start.js
  - demo/js/screens/s6_persona.js
  - demo/js/screens/d4_reader.js # 06-04
  - demo/js/screens/d5_library.js # 06-04
  - demo/js/screens/d7_dashboard.js # 06-05
  - demo/js/screens/d8_showcase.js  # 06-05
  - demo/css/tokens.css       # 06-05
  - demo/css/base.css         # 06-02
  - demo/css/onboarding.css
  - demo/css/reader.css       # 06-04
  - demo/css/library.css      # 06-04
  - demo/css/dashboard.css    # 06-05
  - demo/index.html           # 06-02
  - demo/config/onboarding.json
  - demo/README.md
  - demo/mock/**
  - demo/fallback/popular.json
  - demo/scripts/make_mock.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - ../PROGRESS.md
  - ../.assets/**

must_haves:
  truths:
    - "메인 화면(#/home, 화면 구성 02 §2)이 rows 를 순서대로 그리고 빈 continue_reading 은 숨기며, 카드에 배지 6종(data-type bestseller/review/author/publisher/buzz/light)·앵커 reason·저자 병기·title ?? '(제목 없음)' 가 있다(DEMO-05 전반)"
    - "메인 상단에 '○○님, ○○ 독서 어때요?' · 프로필 칩(→ #/library) · 셀 배지 'cell A · 데모 표시용' · fallback_level ≥ 1 또는 consent=false 또는 client fallback 시 Banner 가 보이고, 하단 TabBar(홈 활성) 가 있다"
    - "책 상세(#/book/:id) 는 D2 위에 바텀시트로 표지·제목·저자·배지·book_format·reason + source_channels 문구·버튼 3(바로 읽기 read · 서재 담기 library · 비활성 '이 책 추천하지 않기' 툴팁 'production roadmap — 선호 교정 루프') 을 그린다"
    - "인스펙터가 recommendation_id·model_version·snapshot_id·cell(forced 표기)·fallback_level(client 표기)·latency_breakdown 막대 + latency_ms 총합(예산선 200ms)·α/β/γ·행별 channel_mix·dedup_removed·모델 전환 라디오 4·'Nearline 반영 n초' 를 보이고, static_popular·Goodbooks·프리셋 섹션이 없다(DEMO-05 후반, 06-CONTEXT D-07b)"
    - "인스펙터 신호 해석은 route.page/screen 기준(S0~S6·home·book·reader·library·refresh) 이고 D3 detail_click·D4 타임라인(reader_open → qualified_read → completion → rating)·'T=15분은 데모 상수…'·positivity bias 문구를 보인다"
    - "Worker 수동 체크리스트(make demo-serve → ?source=mock D8→D1→D2→D3→D4→D2→D5→D6→D2→D7) 가 항목별 결과와 함께 SUMMARY 에 기록되고 콘솔 에러 0 이다"
  artifacts:
    - path: "demo/js/screens/d2_home.js"
      provides: "render(state) — 헤더(인사·프로필 칩·셀 배지)·Banner·행 렌더(빈 행 숨김·subtitle)·카드(배지·reason·저자·format pill·(Should) 난이도 점)·TabBar(home)"
      exports: ["render"]
      max_lines: 120
    - path: "demo/js/screens/d3_detail.js"
      provides: "render(state) — d2 배경 + BottomSheet(표지·제목·저자·배지·book_format·reason/source_channels 문구·버튼 3·(Should) 난이도 적합도)"
      exports: ["render"]
      max_lines: 90
    - path: "demo/js/inspector.js"
      provides: "render(state) — 신호 해석(page/screen)·이벤트 로그·파이프라인 latency_breakdown·U_t·모델 전환/응답 ID(cell·forced·nearline)·행별 channel_mix·D3/D4 항목"
      exports: ["render"]
      max_lines: 200
    - path: "demo/css/home.css"
      provides: "기존 + .badge[data-type=review|author|publisher|light] · .cell-chip · .tile__fmt · .dots(DifficultyDots) · .sheet__disabled · .sheet__meta small"
      max_lines: 160
    - path: "demo/css/inspector.css"
      provides: "기존 − .presets/.preset + .timeline-mini · .insp-sec__flag(forced/client)"
      max_lines: 110
  key_links:
    - from: "demo/js/screens/d2_home.js"
      to: "state.recommend.rows[] (RowOut/ItemOut)"
      via: "rows.filter(items.length) → row(title·subtitle) → tile(badge·reason·authors)"
      pattern: "state\\.recommend"
    - from: "demo/js/screens/d3_detail.js"
      to: "demo/js/screens/d2_home.js"
      via: "import { render as home } — 배경 위 시트"
      pattern: "from \"\\./d2_home\\.js\""
    - from: "demo/js/inspector.js"
      to: "state.recommend.latency_breakdown / user_state_weights / cell / forced / nearline_lag_s / dedup_removed"
      via: "섹션 5개 — 값이 없어도 크래시 0(D-07b ②)"
      pattern: "latency_breakdown"
---
<objective>
Phase 6 '데모 재구성'(.planning/ROADMAP.md "Phase 6: 데모 재구성") 의 **마지막 조립**: `s7_home.js` → `js/screens/d2_home.js`(행 5+Should 1·배지 6종·reason·저자 병기·fallback 배너·셀 배지·빈 `continue_reading` 숨김·TabBar) · `s8_detail.js` → `js/screens/d3_detail.js`(바텀시트·source_channels 문구·바로 읽기 → `#/reader/:id`·서재 담기·비활성 "추천하지 않기") · `js/inspector.js` 확장(6섹션 — `latency_breakdown` 막대·`cell`/`forced`·행별 `channel_mix`·`dedup_removed`·모델 라디오·"Nearline 반영 n초"·D3/D4 항목·`static_popular` 제거) · `home.css`·`inspector.css` 수정. 그리고 **wave 2 산출물 4개와 처음 만나는 지점**이라 `make demo-serve` → `?source=mock` 완주 수동 체크리스트를 수행해 어긋남을 SUMMARY 에 기록한다(다른 플랜 파일은 고치지 않고 수정 브리프 제안).

Purpose: ROADMAP Success Criteria 4 전반 — "메인 화면 카드에 배지 6종과 앵커 reason 이 보이고 인스펙터가 `recommendation_id`·`model_version`·`fallback_level`·`latency_breakdown`·α/β/γ·`channel_mix`·모델 전환 라디오를 보인다"(**DEMO-05**) + **DEMO-03** 의 D2·D3 페이지. ★앵커 행(main 설계서 §5-7)·★시간 가변 가중치(§5-2)·배지 개인화(화면 01 §4-4) 가 화면에 보이는 자리. 06-CONTEXT D-07(개명 + 새 계약 필드) · D-07b(①`static_popular`→`fallback_v1` 표시 ②`latency_ms` float 라 막대 비어도 크래시 0 ③같은 제목 다른 book_id — book_id 로만 동일성·저자 병기 ④`title ?? '(제목 없음)'`) 이행. PDF 캡처 "메인 — 배지·앵커 row" · "hybrid vs hybrid_div 나란히"(화면 02 §7).
Output: 신규 2(d2·d3) · 삭제 2(s7·s8) · 확장 1(inspector) · 수정 2(css). 커밋 0.

**wave 3 단독:** 다른 Worker 없음. 그러나 `app.js`·`api.js`·`mock.js`·d4/d5/d7/d8 은 **읽기만** — 어긋남(예: `data-act` 이름, state 필드) 을 발견하면 이 플랜 파일 안에서 맞출 수 있으면 맞추고, 상대 파일을 고쳐야 하면 SUMMARY "수정 브리프 제안" 절에 파일·줄·기대값을 적어 06-07 Advisor 에게 넘긴다(`../.claude/docs/workflow.md` 검증 실패 = 수정 브리프 재위임).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-rebuild/06-CONTEXT.md
@.planning/phases/06-demo-rebuild/06-PATTERNS.md
@.planning/phases/06-demo-rebuild/06-02-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-03-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-04-SUMMARY.md
@.planning/phases/06-demo-rebuild/06-05-SUMMARY.md
@../.claude/rules/demo.md
@../.claude/rules/references.md
@../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md
@../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md
@src/millie_rec/serving/schemas.py
@src/millie_rec/contracts.py
@demo/js/app.js
@demo/js/actions.js
@demo/js/screens/ui.js
@demo/js/screens/s7_home.js
@demo/js/screens/s8_detail.js
@demo/js/inspector.js
@demo/css/home.css
@demo/css/inspector.css

<file_ownership>
## Phase 6 파일 소유 표 (겹침 0 — 같은 wave 는 서로소)

| 파일 | 만듦/고침/삭제 | 플랜 | wave |
|---|---|---|---|
| `demo/scripts/make_mock.py` · `tests/demo/test_make_mock.py` · `demo/mock/*.json`(13개 — 계약 10 + catalog_kr·neighbors_kr + _manifest) · `demo/mock/books.json` 삭제 | 재작성·신규·재생성 | 06-01 | 1 |
| `demo/js/router.js` · `demo/js/app.js` · `demo/js/actions.js` · `demo/js/presets.js` · `demo/js/screens/ui.js` · `demo/js/screens/onboarding_step.js` · `demo/js/screens/s0_start.js` · `demo/js/screens/s6_persona.js` · `demo/index.html` · `demo/css/base.css` · `demo/config/onboarding.json` · `demo/README.md` | 신규·재작성·최소 수정 | 06-02 | 2 |
| `demo/js/api.js` · `demo/js/mock.js` · `demo/js/mock_store.js` | 재작성·신규 | 06-03 | 2 |
| `demo/js/screens/d4_reader.js` · `demo/js/screens/d5_library.js` · `demo/css/reader.css` · `demo/css/library.css` | 신규 | 06-04 | 2 |
| `demo/js/screens/d7_dashboard.js` · `demo/js/screens/d8_showcase.js` · `demo/css/dashboard.css` · `demo/css/tokens.css` | 신규·토큰 추가 | 06-05 | 2 |
| `demo/js/screens/d2_home.js`(← `s7_home.js` 삭제) · `demo/js/screens/d3_detail.js`(← `s8_detail.js` 삭제) · `demo/js/inspector.js` · `demo/css/home.css` · `demo/css/inspector.css` | 신규·삭제·확장 | **06-06(이 플랜)** | 3 |
| `.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md` · `../PROGRESS.md`(미결 절 2줄 append 만) | Advisor | 06-07 | 4 |
</file_ownership>

<interfaces>
<!-- 공통 인터페이스 부록 — 06-02 ~ 06-06 다섯 플랜에 **동일한 텍스트**로 들어간다. 한 곳만 바꾸면 병렬 Worker 가 어긋난다. -->
<!-- 정본: 화면 구성 02 §4 상태 모델 v2 · §5 이벤트 13종 · 화면 구성 01 §6 · serving/schemas.py(응답 형태) · contracts.py(이름). -->

## A. `state` v2 — `demo/js/app.js` 의 단일 상태 객체 (06-02 소유, 다른 파일은 읽기만)
```js
const state = {
  route: { page: "showcase", params: {} },  // router.parse(location.hash). page ∈ showcase|onboarding|home|book|reader|library|refresh|dashboard
  source: "mock",                            // "mock" | "api"
  userKey: null,                             // localStorage["millie_user_key"] UUIDv4 — boot 에서 항상 채움(익명도 키 있음)
  cell: null,                                // "A" | "B" — postPreferences 응답 · getUserState 응답
  consent: null,                             // null | true | false (건너뛰기 = false)
  screen: "S0",                              // D1/D6 내부 단계 "S0".."S6"
  resetting: false,                          // D6(#/refresh) 모드 — 헤더 "취향 다시 설정", S0 생략, CTA "새 취향으로 추천 받기"
  prefs: { readingTime: null, categories: [], criterion: null, subcategories: [], seedBooks: [] },  // criterion = S3 **라벨**(config 텍스트)
  steps: [],                                 // config/onboarding.json .steps
  meta: null,                                // OnboardingMeta {survey_variant, reading_times[], criteria[{id,label}], categories[{name,supported,subcategories[]}]}
  candidateSet: { id: null, items: [], impressions: [] },  // items = CandidateItem[], impressions = [{book_id, position, selected}]
  snapshots: [],                             // [{ id, createdAt, prefs, persona, cell }] — append only(재설정도 push)
  model: null,                               // null = 셀 배정(A→hybrid, B→hybrid_div) · VARIANTS 문자열 = 인스펙터 라디오 강제(forced)
  recommend: null,                           // RecommendOut 원문. api 클라이언트 fallback 시 + client_fallback_reason: string
  detail: null,                              // { item: ItemOut, rowId } | null — D3 바텀시트. rowId ∈ RecommendOut.rows[].row_id | "library"(D5 서재 타일 — item 은 LibraryBook 기반 최소 ItemOut, 부록 B card)
  reading: null,                             // { bookId, progressPct, virtualMinutes, qualified: false, completed: false } | null — D4
  ratings: {},                               // { [bookId]: stars }
  library: { added: [], reading: [], completed: [] },  // UserStateOut.library (LibraryBook[])
  libraryTab: "added",                       // D5 활성 탭 added|reading|completed
  userState: null,                           // UserStateOut 원문(D5 타임라인·U_t)
  mydata: null,                              // UserDataOut 원문(D5 "내 데이터 보기" 모달)
  dashboard: null,                           // DashboardOut 원문(D7)
  showcase: null,                            // ShowcaseOut 원문(D8)
  health: null,                              // HealthOut | null (상단바 model_version)
  nearlineLagSec: null,                      // RecommendOut.nearline_lag_s
  events: [],                                // 인스펙터 로그 [{ t: "HH:MM:SS", event_type, detail }] 최근 200
  toast: null,                               // string | null — 3초 후 null
  banner: null,                              // string | null — D2 상단 Banner 문구(예: "비개인화 인기 도서")
  modal: null,                               // null | "rating" | "mydata" | "withdraw"
};
```
원칙: `setState(patch)` 하나 · `render()` 하나 · 화면 파일은 `render(state) → HTML 문자열` 만, fetch·상태 변경 금지(`../.claude/rules/demo.md`).

## B. `data-act` 액션 이름 (화면 파일이 찍고 `app.js`/`actions.js` 가 위임 처리 — click 1곳 + change 1곳)
| data-act | 추가 data-* | 처리 | 이벤트(EVENT_TYPES 13종) |
|---|---|---|---|
| `start` | — | S0 → S1 | `preference_started` |
| `skip` | — | consent=false → `#/home`(level 3) | — |
| `back` | — | 단계 뒤로(S1 에서 resetting 이면 `#/home`) | — |
| `pick` | `data-step` `data-val` | S1~S4 선택 토글 | `preference_step`(payload {step}) |
| `pickbook` | `data-book` `data-pos` | S5 시드 토글 + impressions[].selected | `preference_book_selected` / `preference_book_deselected` |
| `next` | — | 다음 단계(S4→S5 진입 시 getCandidates + `impression`×30, S5 완료 시 postPreferences) | `preference_completed` |
| `toHome` | — | S6 CTA → `#/home` | — |
| `nav` | `data-to="#/home"` 등 해시 | `location.hash = to` | — |
| `preset` | `data-preset="newUser"\|"skipUser"\|"resetUser"` | presets.run(name) | (프리셋이 만드는 이벤트) |
| `card` | `data-book` `data-row` `data-pos` | detail 설정 → `#/book/:id`. 행 탐색은 `(state.recommend?.rows ?? [])` 로 null 방어(철회 직후 `recommend: null` 이어도 TypeError 0). `data-row="library"`(D5 서재 타일) 이면 `state.library` 의 added·reading·completed 에서 `LibraryBook{book_id,title,image_url}` 을 찾아 ItemOut 형태의 최소 detail(`book_id`·`title`·`image_url` 채움, `authors`·`reason`·`badge`·`source`·`book_format`·`difficulty` null, `source_channels` [], `score`·`position` 0) 을 만들고 `rowId: "library"` 로 둔다(d3 는 rowId 를 "내 서재" 로 표시). 못 찾으면 무동작 | `detail_click`(서재 타일은 `row_id`·`position` 없이 `surface: "library"`) |
| `closeSheet` | — | `#/home` | — |
| `read` | `data-book` | `#/reader/:id` 진입(reading 초기화) | `reader_open` |
| `library` | `data-book` | 서재 담기 | `library_add`(surface "main") |
| `read10` | — | progressPct +8, virtualMinutes +10; ≥15분 첫 도달 시 1회 | `qualified_read` |
| `complete` | — | completed=true, modal="rating" | `completion` |
| `rate` | `data-stars` 1..5 | postRating → toast "다음 책을 준비하고 있어요" → `#/home` | `rating` |
| `rateLater` | — | 토스트 → `#/home` | — |
| `libTab` | `data-tab` | libraryTab 변경 | — |
| `mydata` | — | getUserData → modal="mydata" | — |
| `withdraw` | — | modal="withdraw" | — |
| `withdrawConfirm` | — | deletePersonalization → consent=false → banner "비개인화 인기 도서" → `#/home` | — |
| `closeModal` | — | modal=null | — |
| `model` | (radio `value`) | state.model=value → getRecommend(forced) | — |
| `noop` | — | — | — |
- D6 시작(`#/refresh` 진입)은 라우터가 `preference_restarted` 를 기록한다. D2 행 렌더 직후 `impression`(row_id·position·recommendation_id) 은 `app.js` 가 recommendation_id 당 1회 기록한다.

## C. `api.js` 공개 함수 (06-03 소유) — 전부 `async`, 첫 인자 `source`; 실패는 엔드포인트별로 잡아 `null` 반환(uncaught 0). `getRecommend` 만 4초 타임아웃·네트워크 오류 시 `fallback/popular.json` + `client_fallback_reason`
```js
export const config;                                        // { source: "mock"|"api"|null, apiBase: string(기본 "" — ?api= 덮어쓰기), capture: "1"|"2"|null }
export async function resolveSource();                      // 명시 ?source 우선 · 없으면 GET /health 200 → "api", 아니면 "mock"
export async function loadSteps();                          // config/onboarding.json → steps[]
export async function health(source);                       // HealthOut | null           (mock: {status:"ok", api_version:"v2", model_version:"hybrid_div_v1", db_ok:true, uptime_s:0} 형태)
export async function loadMeta(source);                     // OnboardingMeta
export async function getCandidates(source, { categories, subcategories = [], n = 30, userKey });  // CandidateSet
export async function postPreferences(source, body);        // body = PreferencesRequest{user_key, consent, reading_time, categories, criterion(id), subcategories, seeds, candidate_set_id, restart} → PreferencesResponse
export async function getRecommend(source, { userKey, snapshotId, model, k = 40, context });  // RecommendOut (+client_fallback_reason)
export async function postEvents(source, events);           // EventIn[] ≤50 → EventsAccepted | null
export async function postRating(source, body);             // RatingIn{rating_id, user_key, book_id, stars, ts, recommendation_id} → RatingOut | null
export async function getUserState(source, userKey);        // UserStateOut | null
export async function getUserData(source, userKey);         // UserDataOut | null
export async function deletePersonalization(source, userKey); // PersonalizationDeleted | null
export async function getDashboard(source);                 // DashboardOut | null   (api 404 → null → 화면 "이 항목은 아직 없음")
export async function getShowcase(source);                  // ShowcaseOut | null
```
경로(api 모드): `/health` · `/api/meta/onboarding` · `/api/candidates/onboarding?categories=&n=&user_key=&subcategories=` · `POST /api/preferences`(201) · `/api/recommend?user_key=&snapshot_id=&model=&k=&context=` · `POST /api/events`(202, `{events:[…]}` 배치) · `POST /api/ratings` · `/api/users/{key}/state` · `/api/users/{key}/data` · `DELETE /api/users/{key}/personalization` · `/api/dashboard` · `/api/showcase`. 타임스탬프 필드는 **`ts`**.

## D. `mock.js` 공개 함수 (06-03 소유) — `api.js` 가 `source === "mock"` 일 때 같은 이름으로 위임(source 인자 없음)
```js
export async function init();            // mock/catalog_kr.json + mock/neighbors_kr.json + mock/showcase.json 1회 로드(Promise 캐시)
export function byId(bookId);            // 카드 dict | undefined
export function health(); loadMeta(); getCandidates({categories, subcategories, n, userKey}); postPreferences(body);
export function getRecommend({userKey, snapshotId, model, k, context}); postEvents(events); postRating(body);
export function getUserState(userKey); getUserData(userKey); deletePersonalization(userKey); getDashboard(); getShowcase();
```
반환 형태는 `serving/schemas.py`·`schemas_should.py` 모델과 키 집합 1:1(여분 키 금지). 세션 저장은 `mock_store.js`(sessionStorage `millie_mock_db`).

## E. 화면 파일 규약
- `demo/js/screens/d2_home.js` `d3_detail.js` `d4_reader.js` `d5_library.js` `d7_dashboard.js` `d8_showcase.js`: `export function render(state) → string`. import 는 `./ui.js` 만(`d3_detail.js` 는 배경용 `./d2_home.js` 추가 허용).
- `onboarding_step.js`: `render(state, step, meta)` · `s0_start.js`: `render(state)` · `s6_persona.js`: `render(state)`.
- `ui.js`(06-02 소유) 추가 export: `tabbar(active)`(`"home"|"library"` → `<nav class="tabbar">` 홈·서재 2탭, `data-act="nav" data-to="#/home|#/library"`) · `demoLabel(text = "데모 전용 · 실제 밀리 화면 아님")` → `<div class="demo-label">` · `banner(text)` → `<div class="banner">` · `modal(inner)` → `<div class="modal-wrap"><div class="modal">…</div></div>`. 기존 `esc cover badge josa icon statusbar navbar cta` 유지.
- 모든 사용자·카탈로그 문자열은 `esc()` 를 거쳐 삽입한다. `innerHTML` 에 원문 금지(XSS). `title ?? "(제목 없음)"` 방어.
- D7·D8 은 `#page` 컨테이너(전폭), 나머지는 `#phone`. 렌더 대상 결정은 `app.js` 만.

## F. 저장 키·상수
- `localStorage["millie_user_key"]` — UUIDv4(`crypto.randomUUID()`), 개인정보 아님.
- `sessionStorage["millie_events"]` — `app.js` 이벤트 송신 큐(EventIn[]; 50개 단위 `postEvents`).
- `sessionStorage["millie_mock_db"]` — `mock_store.js` 의 mock 서버 상태(users·snapshots·events·ratings·recommendations).
- 상수(값은 `contracts.py` 와 동일): `BUDGET_MS = 200` · `QUALIFIED_READ_MINUTES = 15` · `ROW_SIZE = 12` · `VARIANTS = ["pop","cf","hybrid","hybrid_div"]` · `BADGE_TYPES = ["bestseller","review","author","publisher","buzz","light"]` · EVENT_TYPES 13종(`impression detail_click library_add reader_open qualified_read completion rating preference_started preference_step preference_book_selected preference_book_deselected preference_completed preference_restarted`).
- `EventIn` 필드: `{event_id(uuid), user_key, book_id?, event_type, ts(ISO), surface?, row_id?, position?, selected?, recommendation_id?, model_version?, preference_snapshot_id?, candidate_set_id?, payload: {string: string}}` — 그 외 키 금지.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: d2_home.js(← s7_home.js) + d3_detail.js(← s8_detail.js) + home.css — 배지 6종·reason·저자·배너·셀 배지·TabBar·바텀시트</name>
  <files>demo/js/screens/d2_home.js, demo/js/screens/d3_detail.js, demo/js/screens/s7_home.js, demo/js/screens/s8_detail.js, demo/css/home.css</files>
  <read_first>
    - demo/js/screens/s7_home.js (53줄 — 계승: `tile()`·`row()` 구조. 바꿈: `item.format`→`book_format`, `static_popular`→`client_fallback_reason`, `data-act="detail"`→`"card"`, `data-act="reset"` 칩 → `nav #/library`, 하드코딩 `subtitle()` 제거 → `r.subtitle`)
    - demo/js/screens/s8_detail.js (29줄 — 계승: 배경 `home(state)` + 시트 구조. 바꿈: `read`/`library` 에 `data-book`, `complete` 버튼 제거(완독은 D4), 비활성 버튼 추가)
    - demo/css/home.css (103줄 — `.badge[data-type="bestseller"|"buzz"]` 만 있음 → 4종 추가 · `.fb-note` 는 Banner 로 대체 가능)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D2 표(상단 "○○님, 저녁 독서 어때요?"·프로필 칩(→ D5)·셀 배지 `cell B · 데모 표시용` · 행 Must 5/Should 4 · 카드 표지 108px·제목 2줄·배지(review 스니펫)·reason 1줄·(Should) 난이도 점 · fallback 배너 "개인화 응답 지연 — 캐시/인기 도서로 대체 (level n)") · D3 표(상단 표지·제목·저자·평점·배지·`book_format` · 추천 이유 `reason` + `source_channels` — co-read 문구 금지 · 난이도 적합도(Should) · 버튼 바로 읽기·서재 담기·이 책 추천하지 않기(비활성, 툴팁 "production roadmap — 선호 교정 루프")) · §3 `TabBar` `Badge`(+`light` "가볍게") `DifficultyDots` ●●● 3분위 `BottomSheet` 70% 딤
    - ../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md §4-4 배지 6종 표 · §5 "메인 행(D2) 행 제목 18px/700 + 우측 `>`, 카드 폭 108px, 배지 11px pill"
    - src/millie_rec/serving/schemas.py `RowOut{row_id,title,purpose,items,subtitle,channel_mix}` · `ItemOut{book_id,score,source,reason,title,authors,image_url,position,source_channels,badge,book_format,difficulty}` · `RecommendOut.fallback_level`·`cell`
    - demo/js/screens/ui.js (`esc cover badge icon tabbar banner`) · demo/js/app.js(06-02 산출 — `state.banner` 문구 결정 위치, `card`/`read`/`library` 액션 데이터 속성 기대값)
  </read_first>
  <action>
**`demo/js/screens/d2_home.js` 신규(≤120줄)** — `git mv` 가 아니라 새 파일 + `s7_home.js` 삭제(`rm`). `import { badge, cover, esc, icon, tabbar, banner } from "./ui.js";`
```js
const FMT_PILL = ["오디오북", "챗북"];   // 전자책은 표시 없음(data.md book_format 3값)
export const dots = (d) => (d == null ? "" : `<span class="dots" title="난이도 ${d.toFixed(2)}">${[1, 2, 3].map((i) => `<i class="${d >= (i - 0.5) / 3 ? "is-on" : ""}"></i>`).join("")}</span>`);  // Should · difficulty null 이면 미표시(demo.md)
function tile(item, rowId) {
  return `<button class="tile" data-act="card" data-book="${item.book_id}" data-row="${esc(rowId)}" data-pos="${item.position ?? 0}">
    ${cover(item, FMT_PILL.includes(item.book_format) ? `<span class="tile__fmt">${esc(item.book_format)}</span>` : "")}
    <div class="tile__title">${esc(item.title ?? "(제목 없음)")}</div>
    <div class="tile__author">${esc(item.authors ?? "")}</div>
    ${badge(item.badge)}${dots(item.difficulty)}
    ${item.reason ? `<div class="tile__reason">${esc(item.reason)}</div>` : ""}
  </button>`;
}
function row(r) {
  return `<section class="row" data-row="${esc(r.row_id)}"><div class="row__head"><span class="row__title">${esc(r.title)}</span><span class="row__chev">${icon("chev", 16, 16)}</span></div>
    ${r.subtitle ? `<p class="row__sub">${esc(r.subtitle)}</p>` : ""}
    <div class="row__strip">${r.items.map((it) => tile(it, r.row_id)).join("")}</div></section>`;
}
export function render(state) {
  const rec = state.recommend; const snap = state.snapshots[state.snapshots.length - 1];
  const name = snap?.persona?.name ?? "회원"; const ctx = (state.prefs.readingTime ?? rec?.context ?? "").split(",")[0];
  const hello = state.consent === false ? "지금 많이 읽는 책을 모았어요" : `${name}님, ${ctx || "오늘"} 독서 어때요?`;
  const rows = (rec?.rows ?? []).filter((r) => r.items.length);   // 빈 continue_reading 은 숨김(05-CONTEXT D-03)
  return `<div class="screen"><div class="screen__body">
    <div class="home__head"><div class="home__hello">${esc(hello)}<small>${state.consent === false ? "취향을 등록하면 나에게 맞는 책을 추천해 드려요" : esc(state.prefs.readingTime ?? "")}</small></div>
      <button class="profile-chip" data-act="nav" data-to="#/library">내 서재</button></div>
    ${rec?.cell ? `<span class="cell-chip">cell ${esc(rec.cell)}${rec.forced ? " · forced" : ""} · 데모 표시용</span>` : ""}
    ${banner(state.banner)}
    ${rows.length ? rows.map(row).join("") : `<p class="row__sub">${rec ? "추천 행이 비어 있습니다" : "추천을 불러오는 중…"}</p>`}
    <div style="height:24px"></div></div>${tabbar("home")}</div>`;
}
```
- 배너 문구는 `app.js` 가 `state.banner` 로 결정(부록 A) — 이 파일은 `banner(state.banner)` 만. `client_fallback_reason`·`static_popular` 판정 코드는 두지 않는다(D-07b ①).
- 카드 동일성은 `book_id`(`data-book`) 로만 — 제목 비교 없음(D-07b ③). 저자 항상 병기.

**`demo/js/screens/d3_detail.js` 신규(≤90줄)** + `s8_detail.js` 삭제. `import { badge, cover, esc } from "./ui.js"; import { render as home, dots } from "./d2_home.js";`
```js
const CHANNEL_TEXT = { content: "콘텐츠 유사도 이웃 — 제목·소개 TF-IDF(협업 필터링 아님)", popularity: "지금 많이 읽는 책 — 인기 순위(pop_rank) 기반" };  // co-read("함께 읽음") 문구 금지(data.md)
export function render(state) {
  const d = state.detail; if (!d) return home(state);
  const it = d.item; const ch = (it.source_channels ?? []).map((c) => CHANNEL_TEXT[c] ?? c).join(" · ");
  return `${home(state)}<div class="sheet-wrap"><div class="sheet-wrap__dim" data-act="closeSheet"></div>
    <div class="sheet"><div class="sheet__grip"></div>
      <div class="sheet__top"><div class="sheet__cover">${cover(it)}</div><div class="sheet__meta">
        <h2 class="sheet__title">${esc(it.title ?? "(제목 없음)")}</h2><p class="sheet__author">${esc(it.authors ?? "")}</p>
        <small>${esc(it.book_format ?? "")}${it.book_format && d.rowId ? " · " : ""}${esc(d.rowId === "library" ? "내 서재" : d.rowId ?? "")}</small>${badge(it.badge)}${dots(it.difficulty)}</div></div>
      ${it.reason ? `<p class="sheet__reason">${esc(it.reason)}</p>` : ""}
      ${ch ? `<p class="sheet__channels">${esc(ch)}</p>` : ""}
      ${it.difficulty == null ? `<p class="sheet__fit">난이도 적합도 — 완독지수 없음 · 카테고리 평균 기준</p>` : `<p class="sheet__fit">난이도 적합도 ${dots(it.difficulty)} ${it.difficulty < 0.34 ? "가볍게" : it.difficulty < 0.67 ? "비슷한 수준" : "조금 도전적"}</p>`}
      <div class="sheet__actions"><button class="cta is-on" data-act="read" data-book="${it.book_id}">바로 읽기</button>
        <button class="sheet__ghost" data-act="library" data-book="${it.book_id}">서재 담기</button></div>
      <button class="sheet__disabled" disabled title="production roadmap — 선호 교정 루프">이 책 추천하지 않기</button>
    </div></div>`;
}
```
- 평점(`average_rating`) 은 `ItemOut` 에 없으므로 배지(`review` 스니펫)로 대신 — 별도 평점 표시 없음(주석 1줄).
- `complete` 버튼 없음(완독은 D4).

**`demo/css/home.css`(≤160줄, 색 리터럴 0 — 기존 리터럴이 있으면 토큰으로 바꾸지 않고 유지, 새 줄만 토큰):** 추가 — `.badge[data-type="review"] { border-color:var(--accent); color:var(--accent) }` `.badge[data-type="author"], .badge[data-type="publisher"] { border-color:var(--text-2); color:var(--text-2) }` `.badge[data-type="light"] { background:var(--light-badge); color:var(--on-accent); border-color:var(--light-badge) }` · `.cell-chip { display:inline-block; margin:0 var(--pad) 8px; padding:2px 8px; border-radius:var(--r-pill); border:1px solid var(--line); font-family:var(--mono); font-size:11px; color:var(--text-2) }` · `.tile__fmt { position:absolute; right:4px; top:4px; padding:1px 6px; border-radius:var(--r-pill); background:var(--text); color:var(--on-accent); font-size:10px }` · `.dots { display:inline-flex; gap:2px; margin-left:6px; vertical-align:middle }` `.dots i { width:6px; height:6px; border-radius:50%; background:var(--difficulty-off) }` `.dots i.is-on { background:var(--difficulty-on) }` · `.sheet__channels, .sheet__fit { font-size:12px; color:var(--text-2); margin:4px 0 }` `.sheet__meta small { display:block; font-size:12px; color:var(--text-3); margin:2px 0 6px }` `.sheet__disabled { width:100%; height:40px; margin-top:8px; color:var(--text-3); border:1px dashed var(--line); border-radius:var(--r-pill); cursor:not-allowed }` · `.sheet__finish` 규칙 삭제(버튼 없음) · `.fb-note` 삭제(Banner 로 대체).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/screens/d2_home.js && node --check js/screens/d3_detail.js && test ! -e js/screens/s7_home.js && test ! -e js/screens/s8_detail.js && wc -l js/screens/d2_home.js js/screens/d3_detail.js css/home.css && grep -c 'data-act="card"\|data-to="#/library"\|tabbar("home")\|banner(state.banner)\|(제목 없음)\|데모 표시용\|r.subtitle' js/screens/d2_home.js && grep -c 'data-act="read" data-book\|data-act="library" data-book\|production roadmap — 선호 교정 루프\|data-act="closeSheet"' js/screens/d3_detail.js && grep -nE 'static_popular|client_fallback_reason|item\.format\b|data-act="detail"|data-act="complete"|함께 읽|innerHTML' js/screens/d2_home.js js/screens/d3_detail.js | wc -l && grep -c 'data-type="review"\|data-type="light"\|\.cell-chip\|\.dots\|\.sheet__disabled' css/home.css</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` 2파일 종료 0 · `s7_home.js`·`s8_detail.js` 없음 · `wc -l d2_home.js` ≤ 120 · `d3_detail.js` ≤ 90 · `home.css` ≤ 160
    - d2: `grep -c 'data-act="card"' …` == 1 · `data-to="#/library"` == 1 · `tabbar("home")` == 1 · `banner(state.banner)` == 1 · `(제목 없음)` == 1 · `데모 표시용` == 1 · `r.subtitle` ≥ 1 · `filter((r) => r.items.length)` == 1 · `export const dots` == 1
    - d3: `grep -c 'data-act="read" data-book' …` == 1 · `data-act="library" data-book` == 1 · `production roadmap — 선호 교정 루프` == 1 · `data-act="closeSheet"` == 1 · `import { render as home, dots } from "./d2_home.js"` == 1 · `카테고리 평균 기준` == 1
    - 구 형태·금지: `grep -nE 'static_popular|client_fallback_reason|item\.format\b|data-act="detail"|data-act="complete"|함께 읽|innerHTML|fetch\(' demo/js/screens/d2_home.js demo/js/screens/d3_detail.js | wc -l` == 0
    - home.css: `grep -c 'data-type="review"\|data-type="author"\|data-type="publisher"\|data-type="light"' demo/css/home.css` == 4 · `.cell-chip` 1 · `.dots` ≥ 2 · `.sheet__disabled` 1 · `.sheet__finish` 0 · `.fb-note` 0 · 새로 추가한 줄에 `#RRGGBB` 0(`git diff demo/css/home.css | grep '^+' | grep -E '#[0-9a-fA-F]{3,8}\b' | wc -l` == 0)
    - `grep -rn 's7_home\|s8_detail' demo/js | wc -l` == 0 (다른 파일의 import 도 없음 — 있으면 SUMMARY 수정 브리프)
  </acceptance_criteria>
  <done>D2·D3 가 새 계약 필드로 그려지고 배지 6종 CSS·비활성 버튼·TabBar 가 있다.</done>
</task>

<task type="auto">
  <name>Task 2: inspector.js 확장 + inspector.css — page/screen 신호 해석·latency_breakdown 막대·cell/forced·channel_mix·dedup·Nearline·D3/D4 항목·프리셋 섹션 제거</name>
  <files>demo/js/inspector.js, demo/css/inspector.css</files>
  <read_first>
    - demo/js/inspector.js (132줄 — 계승: `sec()` L29-32 · `logSection` L56-63 · `latencySection` 골격 L65-82 · `weightsSection` L84-99 · `modelSection` L101-122. 바꿈: `SIGNALS` 키 S7/S8/S9 → page 기준 · `PRESETS` 섹션 삭제(L21-25·L125-126) · L48 Goodbooks 문구 삭제 · L66 `latency_ms` 객체 → `latency_breakdown` · L106/L110 `static_popular` → `client_fallback_reason` 판정 + `model_version` 그대로 표시)
    - demo/css/inspector.css (73줄 — `.presets .preset` 삭제, `.lat*` `.weights` `.chan` `.kv` 유지)
    - ../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md §4-1 표(S0~S6 신호 해석 문구 — 현행 SIGNALS 그대로) · §3 인스펙터 6섹션 도면
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D2 인스펙터 행(recommendation_id · model_version · snapshot_id · cell(`forced` 표기) · fallback_level · latency_breakdown 막대 + latency_ms 총합(예산선 200ms) · α/β/γ · 행별 channel_mix · dedup_removed · 모델 라디오 · "Nearline 반영 n초") · D3 인스펙터(`detail_click` · (Should) book_stats — 결측 "완독지수 없음") · D4 인스펙터(타임라인 reader_open → qualified_read → completion → rating · "T=15분은 데모 상수, production은 로그 분포로 보정" · "리뷰 리워드는 positivity bias 유발 → 라벨은 별점 원본, 리워드는 UX 제안만") · D6 "snap_01 → snap_02, α +0.15"
    - src/millie_rec/contracts.py `BUDGET_MS=200` · `VARIANTS`
    - demo/js/app.js (06-02 산출 — `state.route.page`·`state.screen`·`state.resetting`·`state.events[{t,event_type,detail}]`·`state.reading`·`state.nearlineLagSec` 이름 확인)
  </read_first>
  <action>
**`demo/js/inspector.js` 확장(≤200줄).** `import { esc } from "./screens/ui.js"; const BUDGET_MS = 200; const MODELS = [["pop","Pop"],["cf","CF"],["hybrid","Hybrid"],["hybrid_div","+Diversity"]];`
- `SIGNALS`: S0~S6 은 현행 문구 그대로(화면 01 §4-1) · 신규 키 `home: { type: "Serving — Page Composition", read: "recommendation_id / model_version / snapshot_id / cell / fallback_level · latency_breakdown · U_t · 행별 channel_mix · dedup_removed" }` · `book: { type: "Implicit positive (detail_click)", read: "카드 탭 → detail_click 기록. 바로 읽기 → reader_open → Nearline 세션·continue_reading" }` · `reader: { type: "Implicit positive (reader_open · qualified_read · completion)", read: "가상 15분 도달 = Qualified Reading Start(주 지표 분자) · 완독 → Nearline 웨이크 → after_completion · β↑", extra: "T=15분은 데모 상수, production은 로그 분포로 보정 · 리뷰 리워드는 positivity bias 유발 → 라벨은 별점 원본, 리워드는 UX 제안만" }` · `library: { type: "열람·삭제·철회권 + U_t", read: "GET …/state · GET …/data · DELETE …/personalization → consent=false · 이후 level 3" }` · `refresh: { type: "Preference Refresh ≠ Profile Reset", read: "snap_01 → snap_02 · 독서 기록·이어 읽기 유지 · α +0.15(24h)" }`. 키 선택: `page ∈ {onboarding, refresh}` 면 `state.resetting ? "refresh" : state.screen`(단, S 단계 문구 위에 refresh 해석을 겹침 — 현행 L34-37 로직 계승), 그 외 `page`. L48 Goodbooks 출판사 문구 **삭제**. 미지원 카테고리 행은 `state.meta.categories` 에서 `supported:false` 를 골라 계산(`state.unsupported` 는 v2 state 에 없음).
- `logSection` 그대로(최근 20 / 총 n).
- **`latencySection`(D-07b ②):** `const rec = state.recommend; if (!rec) return ""; const br = rec.latency_breakdown ?? {}; const total = Number(rec.latency_ms ?? br.total ?? 0); const stages = ["feature", "retrieval", "ranking", "rerank", "pipeline", "compose"].filter((k) => br[k] != null);` 막대는 stages 만(비어 있으면 total 막대 1개만) · `totalPct = Math.min(100, total / BUDGET_MS * 100)` · 힌트: `state.source === "mock" ? "mock 지연은 결정적 표시값 — PDF 숫자가 아님(PDF p95 는 results/latency.json)" : "서버 실측 · 참고용 — PDF p95 는 로컬 bench(results/latency.json)"`. 숫자는 `Number(v).toFixed(1)`.
- `weightsSection` 그대로 + 힌트에 `state.snapshots.length >= 2 ? " · 재설정 스냅샷 ≥2 → α 부스트 후보" : ""` (resetBoost 필드는 v2 에 없음 — 값은 응답 `user_state_weights` 가 정본).
- **`modelSection`:** rows = `["source", `${state.source}${rec?.client_fallback_reason ? " → client fallback (" + rec.client_fallback_reason + ")" : ""}`]` · `recommendation_id` · `model_version`(그대로 — `fallback_v1` 도 그대로) · `snapshot_id` · `["cell", rec?.cell ? `${rec.cell}${rec.forced ? " · forced (model= 지정)" : " · 셀 배정"}` : "—"]` · `["fallback_level", rec ? `${rec.fallback_level}${rec.client_fallback_reason ? " (client)" : ""}` : "—"]` · `["dedup_removed", rec ? `${rec.dedup_removed}권 (book_id ∧ 정규화 제목)` : "—"]` · `["Nearline 반영", rec?.nearline_lag_s != null ? `${Number(rec.nearline_lag_s).toFixed(1)}초` : "—"]`. 라디오 4 + **"auto(셀 배정)"** 라디오 1(`value=""`, `state.model === null` 이면 checked — `actions.model` 이 빈 값을 `null` 로 처리, 부록 B). 행별 channel_mix 는 현행 L113-117 그대로(`content`·`popularity` 만 나와야 함 — `itemknn` 이 보이면 SUMMARY 에 06-03 수정 브리프).
- **신규 `journeySection`(D3·D4):** `page === "book"` 이면 `detail: book_id · row_id · position` + `state.events` 중 `detail_click` 최근 1건 · `page === "reader"` 이면 타임라인 `["reader_open", "qualified_read", "completion", "rating"]` 각 단계가 `state.events` 에 있으면 `●` 없으면 `○` + `state.reading` 의 `virtualMinutes/qualified/completed` + 두 문구(`T=15분…`, `positivity bias…`). 그 외 페이지는 "".
- `render(state)`: `signalSection + journeySection + logSection + latencySection + weightsSection + modelSection`(프리셋 섹션 없음 — 프리셋은 D8).
- 금지 문자열: `static_popular` `Goodbooks` `PRESETS` `presets` `resetBoost` `S7` `S8` `S9`.

**`demo/css/inspector.css`(≤110줄):** `.presets`·`.preset`·`.preset:hover` 규칙 삭제 · 추가 `.timeline-mini { display:flex; gap:10px; font-family:var(--mono); font-size:12px; margin:6px 0 }` `.timeline-mini .is-on { color:var(--accent) }` `.insp-flag { display:inline-block; margin-left:6px; padding:0 6px; border-radius:var(--r-pill); border:1px solid var(--warn); color:var(--warn); font-size:10px }`. 색 리터럴 새로 추가 0.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/inspector.js && wc -l js/inspector.js css/inspector.css && grep -c 'latency_breakdown\|client_fallback_reason\|forced\|dedup_removed\|nearline_lag_s\|Nearline 반영\|T=15분은 데모 상수\|positivity bias\|value=""' js/inspector.js && grep -nE 'static_popular|[Gg]oodbooks|PRESETS|presets|resetBoost|SIGNALS\.S[789]|"S7"|"S8"|"S9"' js/inspector.js | wc -l && grep -c '\.presets\|\.preset' css/inspector.css</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/inspector.js` 종료 0 · `wc -l demo/js/inspector.js` ≤ 200 · `wc -l demo/css/inspector.css` ≤ 110
    - `grep -c 'latency_breakdown' demo/js/inspector.js` ≥ 1 · `client_fallback_reason` ≥ 2 · `forced` ≥ 1 · `dedup_removed` ≥ 1 · `nearline_lag_s` ≥ 1 · `Nearline 반영` == 1 · `T=15분은 데모 상수, production은 로그 분포로 보정` == 1 · `positivity bias` == 1 · `value=""` == 1(auto 라디오) · `BUDGET_MS = 200` == 1
    - `grep -nE 'static_popular|[Gg]oodbooks|PRESETS|presets|resetBoost|"S7"|"S8"|"S9"' demo/js/inspector.js | wc -l` == 0
    - `grep -c '\["pop", "Pop"\], \["cf", "CF"\], \["hybrid", "Hybrid"\], \["hybrid_div", "+Diversity"\]' demo/js/inspector.js` == 1
    - `grep -c 'home:\|book:\|reader:\|library:\|refresh:' demo/js/inspector.js` ≥ 5 (page 기준 SIGNALS)
    - `grep -c '\.presets\|\.preset' demo/css/inspector.css` == 0 · `grep -c '\.timeline-mini\|\.insp-flag' demo/css/inspector.css` ≥ 2 · `git diff demo/css/inspector.css | grep '^+' | grep -E '#[0-9a-fA-F]{3,8}\b' | wc -l` == 0
  </acceptance_criteria>
  <done>인스펙터 6섹션이 새 계약 필드로 확장되고 프리셋·Goodbooks·static_popular 가 사라졌다.</done>
</task>

<task type="auto">
  <name>Task 3: mock 완주 수동 체크리스트(Worker — 자동화 금지) + 정적 게이트 + 어긋남 기록</name>
  <files>demo/js/screens/d2_home.js, demo/js/screens/d3_detail.js, demo/js/inspector.js, demo/css/home.css, demo/css/inspector.css</files>
  <read_first>
    - ../.claude/rules/demo.md "검증" 절(Worker 는 Playwright 등 자동화 금지 — `make demo-serve` 후 수동 체크리스트 보고 · 콘솔 에러 0)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §0 심사자 동선 · §8 "검증(Advisor)" 절
    - .planning/phases/06-demo-rebuild/06-02-SUMMARY.md ~ 06-05-SUMMARY.md (wave 2 가 남긴 "06-06 에 넘기는 주의")
  </read_first>
  <action>
1) **정적 게이트(전체 demo/):** `cd demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' --include='*.js' --include='*.json' --include='*.html' --include='*.py' --include='*.md' --include='*.css' . | wc -l` → 0 이어야 한다(0 이 아니면 소유 플랜별로 SUMMARY 에 기록) · `for f in js/*.js js/screens/*.js; do node --check "$f"; done` 전부 0 · `wc -l js/*.js js/screens/*.js css/*.css` 전부 ≤250 · `grep -rnE '#[0-9a-fA-F]{3,8}\b' css --include='*.css' | grep -v tokens.css | grep -v 'base.css' | wc -l`(base.css 의 v1 잔존 hex 리터럴 5개(4줄)는 06-07 이 판단) · `grep -rn 'innerHTML' js/screens | wc -l` == 0 · `grep -rn 'href="javascript' js | wc -l` == 0.

2) **수동 체크리스트 — `make demo-serve` → Chrome `http://localhost:8080/?source=mock`, DevTools Console 열어 둔다.** 항목마다 "무엇을 누르면 무엇이 보여야 한다" 1줄, 결과 ✅/❌ + 콘솔 에러 수를 SUMMARY 표로:
   1. `#/`(D8): 데모 라벨 · 헤더 · 철학 문장 · 비교표 4행(pop 0.063 … hybrid_div 0.116) + `split_mode: holdout` 캡션 · 단계↔지표 그림 3+1칸 · 본인 5권 → 추천 10(reason "『싯다르타』을 좋아하셨다면" 류) · 기억할 5가지 · 시작 버튼 + 프리셋 3 · 로드맵 · 데이터 고지가 보인다.
   2. "신규 유저로 체험하기" → `#/onboarding` S0: 상태바 · "나와 비슷한 책 속의 주인공을 찾아볼까요?" · 우상단 건너뛰기 · 인스펙터 S0 해석.
   3. 지금 시작하기 → S1(진행바 0%) 5택1 → 동의하고 다음 → S2(진행바 없음) 카테고리 ≤3 선택(미지원은 비활성) → S3(40%) 기준 1 → S4(40%) 세부 칩 → S5(84%) 30권 그리드(3분야 라운드로빈 — 소설 독식 아님) 5권 선택 → 인스펙터 노출 로그 30건·selected 5 → CTA "내서재에 담고 내 취향 확인하기" → S6 페르소나(이름·《작품》·설명 문장에 실제 카테고리·기준 삽입).
   4. "○○가 추천하는 책 보기" → `#/home`(D2): 인사 "○○님, 저녁 독서 어때요?" · 셀 배지 `cell A|B · 데모 표시용` · 행 순서 `anchor_<seed₁>`("『시드』을 좋아하셨다면" + 부제 "결이 비슷한 책") → `persona_shelf`("○○의 서가") → `trending` → `fresh_picks`(이어 읽기 행은 비어서 숨김) · 카드에 배지(S3 기준에 따른 타입)·저자·reason · 탭바 홈 활성 · 인스펙터: recommendation_id `rec_…` · model_version `hybrid_v1|hybrid_div_v1` · cell · fallback_level 0 · latency 막대 5 + total 38.0 + 예산선 · α/β/γ(α 최대) · 행별 channel_mix(`content`·`popularity` 만) · dedup_removed · Nearline 반영 1.0초.
   5. 인스펙터 라디오 `Pop` → persona_shelf 책이 바뀌고 model_version `pop_v1` · cell 행에 `forced` · `auto` 로 되돌리면 셀 배정 복귀.
   6. 앵커 행 카드 탭 → `#/book/:id`(D3): 시트에 표지·제목·저자·format·배지·reason·채널 문구("콘텐츠 유사도 이웃…") · 비활성 "이 책 추천하지 않기" 툴팁 · 인스펙터 detail_click 1건.
   7. 바로 읽기 → `#/reader/:id`(D4): 데모 라벨 "실제 뷰어 아님" · 진행률 0% · "10분 읽기" ×2 → 16%·20분·"도달 ✓"(qualified_read 1회만) · 인스펙터 타임라인 `● reader_open ● qualified_read ○ completion ○ rating`.
   8. "완독" → 별점 모달 "어떠셨나요?" ★×5 → ★4 탭 → 토스트 "다음 책을 준비하고 있어요" → `#/home` 최상단에 `after_completion` "『X』을 완독하셨네요, 다음은" 행 · `continue_reading` 은 여전히 숨김(완독 책 제외) · β 증가.
   9. 다른 카드 → 바로 읽기 → 뒤로(`#/home`) → `continue_reading` "이어 읽기" 행이 나타남(1권).
   10. 탭바 서재 → `#/library`(D5): 탭 담은 책(시드 5 + 서재 담기) · 읽는 중(1) · 완독(1) · 타임라인 `snap_…` 1개 active · U_t 막대 · 버튼 3.
   11. "취향 다시 설정" → `#/refresh`(D6): 헤더 "취향 다시 설정" · S0 없이 S1 부터 · 다른 카테고리 선택 → S5 CTA "새 취향으로 추천 받기" → S6 → `#/home`: 앵커 행 시드가 바뀜 · 인스펙터 snapshot_id 새 값 · 서재 타임라인 2개(active 최신).
   12. 서재 → "내 데이터 보기" → JSON 모달(snapshots 2·events n·ratings 1) → 닫기 → "맞춤 추천 동의 철회" → 확인 모달 → 철회 → `#/home`: 배너 "비개인화 인기 도서" · 행 `trending`+`fresh_picks` 만 · model_version `fallback_v1` · fallback_level 3 · 가중치 0.
   13. `#/dashboard`(D7): 데모 라벨 · KPI 6(QRS rate 1/2=50.0% n=2 · 첫 완독 도달률 · fallback % · p95 38.0ms "서버 실측 · 참고용" · 오류율 0 · 활성 user_key 1) · A/B 표 4행 + MDE 고지 · latency 막대 + BUDGET 200 · 품질 3 · 이벤트 스트림 · 노출 로그(cand_… selected true/false) · 모델별 분포.
   14. `#/` 로 돌아가 프리셋 "건너뛰기 유저" → `#/home` 배너 "비개인화 인기 도서"(fallback_level 3 · model_version `fallback_v1` — 12번과 같은 문구, 06-02 배너 우선순위 ②) · "신규 유저 A" → 자동 온보딩 → `#/home` 개인화 · "재설정 유저" → 타임라인 2개·`continue_reading` 있음.
   15. `?source=mock&capture=1` `#/home` → 상단바·인스펙터 숨김, 폰만 · `?capture=2` → 인스펙터 유지·상단바만 숨김.
   16. 전 과정 Console 에러(uncaught·404 제외 빨간 줄) 0 — 404 는 `fallback`·`mock` 경로 외에는 없어야 함.
   17. (참고) `make serve` 가 가능하면 `http://localhost:8000/?source=api` `#/` → `#/home` 만: 크래시 0, `/api/showcase`·`/api/meta/onboarding` 404 시 "이 항목은 아직 없음/서버 준비 중" 문구 — Phase 5 진행 상황에 따라 다르므로 결과만 기록(완주 게이트는 06-07·Phase 7).

3) **어긋남 기록:** ❌ 항목마다 원인 파일(소유 플랜)·기대값·실제값을 SUMMARY "수정 브리프 제안" 표에 1줄. 이 플랜 소유 5파일 안의 원인은 즉시 고치고 재확인. 다른 파일은 고치지 않는다.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' --include='*.js' --include='*.json' --include='*.html' --include='*.py' --include='*.md' --include='*.css' . | wc -l && for f in js/*.js js/screens/*.js; do node --check "$f" || echo "FAIL $f"; done && wc -l js/*.js js/screens/*.js css/*.css | grep -v total | awk '$1>250{print "OVER", $0}' && grep -rn 'innerHTML' js/screens | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 전역 구 형태 grep(위 정규식) 출력 `0`
    - `node --check` 전 JS 종료 0(`FAIL` 출력 없음) · `wc -l` 250 초과 파일 0(`OVER` 출력 없음)
    - `grep -rn 'innerHTML' demo/js/screens | wc -l` == 0 · `grep -rn 'href="javascript' demo/js | wc -l` == 0
    - SUMMARY 에 체크리스트 17항목 표(항목 · 기대 · 결과 ✅/❌ · 콘솔 에러 수) 가 있고, ❌ 마다 "수정 브리프 제안" 행(파일·플랜·기대값·실제값) 이 있다
    - 체크리스트 1~16 중 이 플랜 소유 파일이 원인인 ❌ 는 0(재확인 후)
  </acceptance_criteria>
  <done>정적 게이트 통과 · mock 완주 체크리스트 결과가 기록되고 남은 어긋남은 소유 플랜별 수정 브리프로 06-07 에 넘어간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `RecommendOut` 문자열(title·authors·reason·row title·badge.text·channel_mix 키) → DOM | XSS 표면 — 카탈로그·서버 응답 모두 비신뢰 텍스트로 취급 |
| 인스펙터 라디오 `model=` → `state.model` | 값은 VARIANTS 4 + "" 로 한정(actions 가 검증) |
| 표지 `image_url` 핫링크 | `*.millie.co.kr` 만(생성기 eligible) · `referrerpolicy=no-referrer` · 실패 시 플레이스홀더 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | 카탈로그 제목·저자·reason·행 제목·배지 텍스트 DOM 삽입(XSS) | mitigate | 전부 `esc()` · 화면 파일 `innerHTML` 0(게이트) · `data-*` 속성값도 `esc()` |
| T-06-06-02 | Tampering | `data-book` 조작으로 다른 책 라우팅 | accept | `book_id` int · 존재하지 않으면 `#/home` 리다이렉트(06-02) · 데모 데이터만 |
| T-06-06-03 | Information Disclosure | 인스펙터에 `client_fallback_reason`(오류 메시지) 표시 | accept | 메시지는 `HTTP 404 /api/…`·`aborted` 같은 클라이언트 측 문자열만(서버 Traceback 아님 — 서버는 예외 문자열을 내지 않는다, 05-06 T-05-06-02) |
| T-06-06-04 | Spoofing | 표지 이미지가 다른 호스트로 핫링크 | mitigate | 생성기 `is_eligible` 로 `*.millie.co.kr` 만 · `ui.cover` `referrerpolicy="no-referrer"` · onerror 플레이스홀더 |
| T-06-06-05 | Denial of Service | `latency_breakdown` 빈 객체·`latency_ms` float 로 인스펙터 크래시(D-07b ②) | mitigate | `br[k] != null` 필터 · `total` 막대만 fallback · `Number(...).toFixed(1)` |
| T-06-06-06 | Repudiation | mock latency 를 실측으로 오인 | mitigate | 힌트 "mock 지연은 결정적 표시값 — PDF 숫자가 아님" · api 모드 "서버 실측 · 참고용" |
</threat_model>

<verification>
- 전역 구 형태 grep 0 · `node --check` 전 JS 0 · `wc -l` ≤250 전부 · 화면 파일 `innerHTML` 0
- d2·d3·inspector 의 acceptance grep 전부 통과 · `s7_home.js`·`s8_detail.js` 삭제 · 다른 파일에 그 import 0
- 수동 체크리스트 17항목 결과 표 + 수정 브리프 제안 표가 SUMMARY 에 존재, 콘솔 에러 0(mock)
</verification>

<success_criteria>
- DEMO-05: 배지 6종 CSS·reason·인스펙터 항목(recommendation_id·model_version·fallback_level·latency_breakdown·α/β/γ·channel_mix·라디오) 전부 마크업에 존재하고 mock 완주에서 보인다
- DEMO-03: D2·D3 페이지 렌더러 존재, 8페이지 완주 체크리스트 통과(이 플랜 소유 파일 원인 ❌ 0)
- 06-CONTEXT D-07·D-07b 이행(개명·삭제·`static_popular` 제거·float 방어·book_id 동일성·`(제목 없음)`)
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-rebuild/06-06-SUMMARY.md` — 변경·삭제 파일 목록(커밋 0) · 정적 게이트 출력 인용 · 체크리스트 17항목 표 · 수정 브리프 제안 표(파일·소유 플랜·기대값·실제값) · Advisor(06-07) 에게 남기는 관측(api 모드 404 목록 등).
</output>
