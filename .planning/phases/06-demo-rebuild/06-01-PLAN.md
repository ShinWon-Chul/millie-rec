---
phase: 06-demo-rebuild
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - demo/scripts/make_mock.py
  - tests/demo/test_make_mock.py
  - demo/mock/_manifest.json
  - demo/mock/catalog_kr.json
  - demo/mock/neighbors_kr.json
  - demo/mock/meta_onboarding.json
  - demo/mock/candidates_onboarding.json
  - demo/mock/preferences_response.json
  - demo/mock/recommend_pop.json
  - demo/mock/recommend_cf.json
  - demo/mock/recommend_hybrid.json
  - demo/mock/recommend_hybrid_div.json
  - demo/mock/state.json
  - demo/mock/dashboard.json
  - demo/mock/showcase.json
  - demo/mock/books.json          # 삭제(구 Goodbooks mock)
autonomous: true
requirements: [DEMO-02]
lane: Demo (make_mock 생성기 Worker — 표준 라이브러리만, tests/demo 신설)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(05 관례, 사용자 지시 2026-09-05)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/**
  - tests/conftest.py
  - tests/serving/**
  - tests/app/**
  - tests/ranking/**
  - tests/reranking/**
  - tests/data/**
  - tests/evaluation/**
  - tests/test_architecture.py
  - Makefile
  - pyproject.toml
  - Dockerfile
  - artifacts/**
  - results/**
  - report/**
  - demo/fallback/popular.json   # 소유권 scripts/export_millie_fallback.py — 생성기는 읽고 검증만
  - demo/js/**
  - demo/css/**
  - demo/index.html
  - demo/config/onboarding.json
  - demo/README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - ../PROGRESS.md
  - ../.assets/**

must_haves:
  truths:
    - "uv run python demo/scripts/make_mock.py 를 실 아티팩트로 1회 실행하면 demo/mock/ 에 산출 13개(카탈로그·이웃 catalog_kr·neighbors_kr 2 + 계약 10: meta_onboarding·candidates_onboarding·preferences_response·recommend_{pop,cf,hybrid,hybrid_div}·state·dashboard·showcase + _manifest.json 1) 이 생기고 구 books.json 은 없다(DEMO-02, 06-CONTEXT D-06)"
    - "계약 파일 10개(catalog_kr·neighbors_kr 제외)는 serving.schemas / schemas_should 의 pydantic 모델(extra=forbid)로 model_validate 가 전부 통과한다 — latency_ms float · book_format · badge.type ∈ BADGE_TYPES · criteria {id,label} · cell · snapshots_count 존재(DEMO-02 원문)"
    - "catalog_kr.json 은 eligible 도서만(title 있음 ∧ 표지 호스트 *.millie.co.kr ∧ adult-cover 아님) · 카드 필드 16개만 · description·curator_note·seg_dist·millie_id·tags·shelf_count 0건(../.claude/rules/data.md 저작권)"
    - "neighbors_kr.json 은 책별 [dst, weight] 상위 20개(source 제외 — 06-PATTERNS §0-C, 용량), weight 내림차순·소수 4자리, dst 는 eligible 만"
    - "같은 입력으로 두 번 생성하면 _manifest.json 을 제외한 모든 산출 JSON 이 바이트 단위로 같다(결정성 — 시각·난수 없음)"
    - "showcase.json 의 personal_case 는 SEEDS=(1012,2765,1446,1222,2292) 5권 + 콘텐츠 유사도 이웃 추천 10권(reason '『…』을 좋아하셨다면', badge bestseller) 이고 anchor_case 키는 없다(D-08 충돌 해소 — 스키마가 정본)"
    - "생성기는 demo/fallback/popular.json 을 쓰지 않는다 — 존재하면 RecommendOut 으로 검증만, 없으면 경고 1줄(06-PATTERNS §0-A)"
  artifacts:
    - path: "demo/scripts/make_mock.py"
      provides: "build(artifacts, out, config, *, eval_table=None, latency=None, fallback=None, seeds=SEEDS) -> dict[str,int] · main() argparse(--artifacts --out --config) · 표준 라이브러리만"
      exports: ["build", "main", "SEEDS", "CARD_KEYS", "is_eligible"]
      max_lines: 350
    - path: "tests/demo/test_make_mock.py"
      provides: "계약(model_validate 10파일) · 정확성(카드 키 집합·top-20 내림차순·결정성·eval 문자열→float·personal_case) · 안전성(18·19·20 제외·금지 키 0·구 형태 키 0·popular.json 미생성)"
      min_lines: 90
    - path: "demo/mock/_manifest.json"
      provides: "generated_at · seed 42 · inputs{파일: md5} · outputs{파일: bytes} — /contract-sync 6항(생성기 사용 확인) 근거"
      contains: "\"seed\": 42"
    - path: "demo/mock/showcase.json"
      provides: "ShowcaseOut — eval_table 4행 float · split_mode holdout · personal_case 5+10 · data_notice 2트랙"
      contains: "\"personal_case\""
  key_links:
    - from: "demo/scripts/make_mock.py"
      to: "artifacts/serving/books_kr.json"
      via: "json.load → is_eligible 필터 → CARD_KEYS 화이트리스트 → catalog_kr.json"
      pattern: "books_kr\\.json"
    - from: "demo/scripts/make_mock.py"
      to: "artifacts/serving/item_edges_kr.json"
      via: "책별 weight 내림차순 상위 20 → neighbors_kr.json"
      pattern: "item_edges_kr\\.json"
    - from: "demo/scripts/make_mock.py"
      to: "demo/config/onboarding.json"
      via: "S1 options → reading_times · S3 options → criteria[].label (id 순서 author publisher bestseller buzz review)"
      pattern: "onboarding\\.json"
    - from: "tests/demo/test_make_mock.py"
      to: "src/millie_rec/serving/schemas.py"
      via: "RecommendOut·OnboardingMeta·CandidateSet·PreferencesResponse·UserStateOut.model_validate (읽기 전용 import)"
      pattern: "from millie_rec\\.serving\\.schemas import"
---
<objective>
Phase 6 '데모 재구성'(.planning/ROADMAP.md "Phase 6: 데모 재구성") 의 **입력 데이터 층**을 TDD 한 사이클로 만든다: `demo/scripts/make_mock.py` 를 Goodbooks CSV 생성기(407줄, v1)에서 **밀리 아티팩트(`artifacts/serving/*.json`) → `demo/mock/*.json` 변환기**로 재작성하고, 신규 `tests/demo/test_make_mock.py` 가 `tests/conftest.py` 의 `millie_serving_sample` 세션 fixture(20권 tmp 산출물) 로 계약·정확성·안전성을 단정한다. wave 2 의 4개 플랜(라우터·데이터 계층·뷰어/서재·대시보드/쇼케이스) 전부가 이 플랜의 산출 JSON 을 입력으로 쓴다.

Purpose: ROADMAP Success Criteria 2 — "`scripts/make_mock.py` 가 `artifacts/serving/books_kr.json` 기준으로 mock 10개를 재생성하고 `/contract-sync` 에서 `serving/schemas.py` 검증을 전부 통과한다"(**DEMO-02**). PDF 축② 데모 카탈로그 = 밀리 표본(2트랙 규칙, `../CLAUDE.md` §3-6) · 쇼케이스 D8 이 Phase 5 없이도 실측 숫자(`eval_table.json`)로 뜨는 근거(06-CONTEXT D-08). Phase 3 인계 "`make mock` 실행 금지" 의 원인(v1 생성기의 `fallback/popular.json` 덮어쓰기)을 제거한다.
Output: `demo/scripts/make_mock.py` 재작성(≤350줄) · `tests/demo/test_make_mock.py` 신규 · `demo/mock/` 13파일(계약 10 + `catalog_kr`·`neighbors_kr` 2 + `_manifest.json`) 재생성 · 구 `demo/mock/books.json` 삭제. 커밋 0.

**실측 우선(06-PATTERNS.md §0 — CONTEXT 보다 우선):** (A) `demo/fallback/popular.json` 은 이미 밀리 기반(`fallback_v1`·40권) 이고 소유권은 `scripts/export_millie_fallback.py` — **이 생성기는 그 파일을 쓰지 않는다**(06-CONTEXT D-06 의 "popular.json 삭제·재생성" 폐기). (B) fixture 는 폴더가 아니라 `tests/conftest.py` 의 `millie_serving_sample(tmp_path_factory) -> Path` 세션 fixture(book_id 1..20, `pop_rank == book_id`, 자격 없는 3권 = 18 호스트 `cdn.example.com` · 19 title None · 20 adult-cover). (C) `item_edges_kr.json` 은 `{str(book_id): [[dst, weight, source], …]}` 책당 평균 37개 → 상위 20 절단, 산출은 `[dst, weight]` 2원소(source 제외 — 용량 ≈2MB 목표).

**형제 플랜 불변:** wave 1 단독. `src/**` 는 읽기만(테스트가 `millie_rec.serving.schemas` 를 import 하는 것은 허용 — `tests/` 는 무엇이든 import 가능, `../.claude/rules/architecture.md`). 생성기 자체는 `millie_rec` 를 import 하지 않는다(demo 레인은 HTTP 계약만 안다 — 스키마 검증은 테스트와 `/contract-sync` 가 한다).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-rebuild/06-CONTEXT.md
@.planning/phases/06-demo-rebuild/06-PATTERNS.md
@../.claude/rules/demo.md
@../.claude/rules/data.md
@../.claude/rules/python-tdd.md
@../.claude/rules/references.md
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/schemas_should.py
@src/millie_rec/serving/badges.py
@src/millie_rec/serving/persona.py
@src/millie_rec/serving/onboarding_meta.json
@src/millie_rec/serving/rows.py
@src/millie_rec/contracts.py
@tests/conftest.py
@tests/data/test_millie_export.py
@demo/scripts/make_mock.py
@demo/config/onboarding.json
@artifacts/serving/eval_table.json

<file_ownership>
## Phase 6 파일 소유 표 (겹침 0 — 같은 wave 는 서로소)

| 파일 | 만듦/고침/삭제 | 플랜 | wave |
|---|---|---|---|
| `demo/scripts/make_mock.py` · `tests/demo/test_make_mock.py` · `demo/mock/*.json`(13개 — 계약 10 + catalog_kr·neighbors_kr + _manifest) · `demo/mock/books.json` 삭제 | 재작성·신규·재생성 | 06-01 | 1 |
| `demo/js/router.js` · `demo/js/app.js` · `demo/js/actions.js` · `demo/js/presets.js` · `demo/js/screens/ui.js` · `demo/js/screens/onboarding_step.js` · `demo/js/screens/s0_start.js` · `demo/js/screens/s6_persona.js` · `demo/index.html` · `demo/css/base.css` · `demo/config/onboarding.json` · `demo/README.md` | 신규·재작성·최소 수정 | 06-02 | 2 |
| `demo/js/api.js` · `demo/js/mock.js` · `demo/js/mock_store.js` | 재작성·신규 | 06-03 | 2 |
| `demo/js/screens/d4_reader.js` · `demo/js/screens/d5_library.js` · `demo/css/reader.css` · `demo/css/library.css` | 신규 | 06-04 | 2 |
| `demo/js/screens/d7_dashboard.js` · `demo/js/screens/d8_showcase.js` · `demo/css/dashboard.css` · `demo/css/tokens.css` | 신규·토큰 추가 | 06-05 | 2 |
| `demo/js/screens/d2_home.js`(← `s7_home.js` 삭제) · `demo/js/screens/d3_detail.js`(← `s8_detail.js` 삭제) · `demo/js/inspector.js` · `demo/css/home.css` · `demo/css/inspector.css` | 신규·삭제·확장 | 06-06 | 3 |
| `.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md` · `../PROGRESS.md`(미결 절 2줄 append 만) | Advisor | 06-07 | 4 |

`demo/fallback/popular.json` 은 어느 플랜도 쓰지 않는다(소유권 `scripts/export_millie_fallback.py`).
</file_ownership>

<interfaces>
<!-- 이 플랜이 만드는 계약 외 파일 2개의 형태 — 06-03 mock.js 가 그대로 읽는다. -->

`demo/mock/catalog_kr.json` — 배열, 각 원소는 카드 dict **정확히 16키**(`CARD_KEYS`, 이 순서):
```json
{"book_id": 1004, "title": "불편한 편의점", "authors": "김호연",
 "image_url": "https://img.millie.co.kr/200x/service/cover/...jpg",
 "categories": ["소설"], "publisher": "나무옆의자", "book_format": "전자책",
 "pop_rank": 1, "millie_label": "밀리 픽", "average_rating": 4.3, "review_count": 128,
 "completion_prob": 0.71, "category_avg_prob": 0.62, "expected_min": 210,
 "difficulty": 0.42, "formats": ["전자책", "오디오북"]}
```
결측은 `null`(JSON). `difficulty` 는 `difficulty_source == "category_prior"` 인 책이 `null`(데이터 규칙 그대로 — 생성기가 값을 바꾸지 않는다).

`demo/mock/neighbors_kr.json` — `{ "<book_id>": [[dst_book_id, weight], …] }`, 책당 ≤20, `weight` 내림차순(소수 4자리), `dst` 는 eligible 책만. 키는 문자열(JSON 규칙), 첫 원소 int. `catalog_kr.json`·`neighbors_kr.json` 은 사람이 읽지 않으므로 **압축 직렬화**(`indent=None, separators=(",", ":")`), 계약 파일 10개는 `indent=1`.

`demo/mock/_manifest.json` — `{"generated_at": ISO-8601, "seed": 42, "generator": "demo/scripts/make_mock.py", "inputs": {"books_kr.json": "<md5>", "item_edges_kr.json": "<md5>", "eval_table.json": "<md5>", "onboarding.json": "<md5>"}, "outputs": {"catalog_kr.json": <bytes>, …}}` — 계약 파일이 아니므로 자유 형태. **계약 파일 상단에 `_note` 를 넣지 않는다**(`extra="forbid"`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): tests/demo/test_make_mock.py — 계약·정확성·안전성 3종을 먼저 쓴다</name>
  <files>tests/demo/test_make_mock.py</files>
  <read_first>
    - tests/conftest.py L92-140 (`millie_serving_sample` fixture — 20권, 자격 없는 18·19·20, edges 책당 5개 `[dst, w, source]`, `popularity_kr.json` rank == book_id)
    - tests/data/test_millie_export.py L1-120 (`importlib.util` 로 스크립트 로드 `_load()` · module fixture · `EXPECTED_BOOK_KEYS` 손으로 적기 관례 · 누출 0건 단정)
    - src/millie_rec/serving/schemas.py (모델 이름: `OnboardingMeta` `CandidateSet` `PreferencesResponse` `RecommendOut` `UserStateOut` — 전부 `extra="forbid"`)
    - src/millie_rec/serving/schemas_should.py (`DashboardOut` `ShowcaseOut` `EvalRow.recall_at_20: float`)
    - src/millie_rec/contracts.py L38-92 (`BADGE_TYPES` 6종 · `VARIANTS` · `ROW_IDS` · `ROW_ANCHOR_PREFIX`)
    - artifacts/serving/eval_table.json (rows[].`"recall@20": "0.063"` **문자열**, `meta.split_mode`)
    - ../.claude/rules/python-tdd.md (Red = `AssertionError`, `uv run pytest --no-header`, `-q` 중복 금지)
  </read_first>
  <behavior>
    - 계약: `built` fixture 산출물 10개(`meta_onboarding.json`→`OnboardingMeta`, `candidates_onboarding.json`→`CandidateSet`, `preferences_response.json`→`PreferencesResponse`, `recommend_{pop,cf,hybrid,hybrid_div}.json`→`RecommendOut`, `state.json`→`UserStateOut`, `dashboard.json`→`DashboardOut`, `showcase.json`→`ShowcaseOut`) 가 `model_validate` 통과 · `recommend_hybrid_div.json` 의 `model_version == "hybrid_div_v1"` · `isinstance(latency_ms, float)` · `latency_breakdown["total"] == latency_ms` · `rows[].row_id ∈ ROW_IDS ∪ anchor_*` · 배지 `type ∈ BADGE_TYPES` · `source_channels` 에 `"itemknn"` 0건 · `meta_onboarding.json["criteria"]` 의 id 순서 == `["author","publisher","bestseller","buzz","review"]` · `preferences_response.json` 에 `cell ∈ {"A","B"}`·`snapshots_count` 존재
    - 정확성: `catalog_kr.json` 각 행 키 집합 == `CARD_KEYS`(테스트에 손으로 적은 16개) · 행 수 == 17(20 − 자격 없는 3) · `neighbors_kr.json` 각 값 길이 ≤20·weight 비증가·dst ∈ catalog book_id 집합·원소 길이 2 · `showcase.json["eval_table"]["rows"][0]["recall_at_20"] == 0.063`(float, 테스트가 tmp 에 쓴 예시 표) · `split_mode == "holdout"` · `metric_mapping` 3행 stage 순서 `["Candidate Retrieval","Ranking","Re-ranking"]` · `personal_case["seeds"]` 길이 5(`seeds=(1,2,3,4,5)` 인자) · `1 ≤ len(recommendations) ≤ 10` · 각 추천 `reason` 이 `"『"` 로 시작하고 `"』을 좋아하셨다면"` 로 끝남 · 두 번 build 한 출력 디렉터리의 `_manifest.json` 제외 전 파일 바이트 동일(`filecmp`/`read_bytes` 비교)
    - 안전성: `catalog_kr.json` 의 book_id 집합에 18·19·20 없음 · **`catalog_kr.json`·`neighbors_kr.json` 원문(`read_text`)** 에 `"description"` `"curator_note"` `"seg_dist"` `"millie_id"` `"tags"` `"shelf_count"` 0건(FORBIDDEN 은 카탈로그 축약본 2파일만 — `preferences_response.json` 의 `persona.description` 은 `PersonaOut` 필수 필드라 전 파일 검사 대상이 아니다, 위협 T-06-01-01 의 대상은 `catalog_kr.json`) · 산출 디렉터리 전 파일 원문에 구 형태·v1 흔적 `"format":` `"timestamp"` `"type": "rating"` `"readingTimes"` `static_popular` `CSV_URL` `zygmuntz` `hf.space` 0건(대소문자 무시 — **`goodbooks` 는 검사하지 않는다**: `showcase.json` 의 `data_notice` 에 정본 2트랙 고지 "평가 비교표 = Goodbooks-10k(CC BY-SA 4.0) · …"(`../.claude/rules/serving.md`) 가 반드시 들어간다) · `out/"fallback"` 디렉터리가 **생기지 않음**(`not (out.parent / "fallback").exists()` — build 는 `out` 아래만 쓴다) · `showcase.json` 에 `"anchor_case"` 0건 · 구 `books.json` 이 `out` 에 없음
  </behavior>
  <action>
`tests/demo/test_make_mock.py` 를 새로 만든다(`tests/demo/__init__.py` 는 만들지 않는다 — `tests/serving/`·`tests/data/` 도 없다, rootdir conftest 가 fixture 를 공급한다). 파일 상단 docstring 1줄: `"""make_mock — 계약(model_validate 10파일) · 정확성(카드 키·top-20·결정성·eval float) · 안전성(자격·누출·구 형태)."""`.

```python
import importlib.util, json, sys
from pathlib import Path
import pytest
from millie_rec.contracts import BADGE_TYPES, ROW_ANCHOR_PREFIX, ROW_IDS
from millie_rec.serving.schemas import (CandidateSet, OnboardingMeta, PreferencesResponse, RecommendOut, UserStateOut)
from millie_rec.serving.schemas_should import DashboardOut, ShowcaseOut

ROOT = Path(__file__).resolve().parents[2]
SCRIPT = ROOT / "demo" / "scripts" / "make_mock.py"
CONFIG = ROOT / "demo" / "config" / "onboarding.json"      # 읽기만
INELIGIBLE = {18, 19, 20}                                   # conftest 주석 그대로
CARD_KEYS = ["book_id", "title", "authors", "image_url", "categories", "publisher", "book_format",
             "pop_rank", "millie_label", "average_rating", "review_count", "completion_prob",
             "category_avg_prob", "expected_min", "difficulty", "formats"]   # 스크립트 상수를 import 하지 않고 손으로 적는다
CONTRACT_FILES = {"meta_onboarding.json": OnboardingMeta, "candidates_onboarding.json": CandidateSet,
                  "preferences_response.json": PreferencesResponse, "recommend_pop.json": RecommendOut,
                  "recommend_cf.json": RecommendOut, "recommend_hybrid.json": RecommendOut,
                  "recommend_hybrid_div.json": RecommendOut, "state.json": UserStateOut,
                  "dashboard.json": DashboardOut, "showcase.json": ShowcaseOut}
FORBIDDEN = ('"description"', '"curator_note"', '"seg_dist"', '"millie_id"', '"tags"', '"shelf_count"')   # catalog_kr.json·neighbors_kr.json 원문에만 적용(persona.description 은 PersonaOut 필수 필드)
FORBIDDEN_FILES = ("catalog_kr.json", "neighbors_kr.json")
OLD_SHAPE = ('"format":', '"timestamp"', '"type": "rating"', '"readingTimes"', 'static_popular', 'CSV_URL', 'zygmuntz', 'hf.space', '"anchor_case"')   # goodbooks 는 넣지 않는다 — data_notice 정본 문장(serving.md 2트랙 고지)
EVAL_TABLE = {"rows": [{"variant": v, "recall@20": r, "ndcg@10": n, "ild@10": i, "n_users": "2000",
                        "split_mode": "holdout", "model_version": f"{v}_v1"}
                       for v, r, n, i in (("pop", "0.063", "0.054", "0.764"), ("cf", "0.117", "0.107", "0.643"),
                                          ("hybrid", "0.118", "0.110", "0.606"), ("hybrid_div", "0.116", "0.107", "0.684"))],
              "meta": {"dataset": "goodbooks-10k", "split_mode": "holdout", "n_users": 2000, "seed": 42}}

def _load():
    spec = importlib.util.spec_from_file_location("make_mock", SCRIPT)
    module = importlib.util.module_from_spec(spec); sys.modules["make_mock"] = module; spec.loader.exec_module(module)
    return module

@pytest.fixture(scope="module")
def built(tmp_path_factory, millie_serving_sample) -> Path:
    """fixture 20권 → tmp/mock. 실 artifacts/serving·demo/mock 은 읽지도 쓰지도 않는다."""
    work = tmp_path_factory.mktemp("make_mock")
    eval_path = work / "eval_table.json"; eval_path.write_text(json.dumps(EVAL_TABLE), encoding="utf-8")
    out = work / "demo" / "mock"
    _load().build(millie_serving_sample, out, CONFIG, eval_table=eval_path, seeds=(1, 2, 3, 4, 5))
    return out
```
테스트 함수(이름 규칙 `test_<기능>_<시나리오>_<기대>`): `test_contract_files_validate_against_schemas(built)` — `for name, model in CONTRACT_FILES.items(): model.model_validate(json.loads(...))` · `test_recommend_shape_hybrid_div(built)`(model_version·float·total·row_id·badge·itemknn 0) · `test_meta_criteria_ids_in_badge_order(built)` · `test_preferences_has_cell_and_snapshots_count(built)` · `test_catalog_rows_have_exactly_card_keys_and_only_eligible(built)`(키 집합 == set(CARD_KEYS) · len == 17 · `INELIGIBLE.isdisjoint(ids)`) · `test_neighbors_top20_desc_eligible(built)` · `test_showcase_eval_table_floats_and_personal_case(built)` · `test_build_is_deterministic(tmp_path_factory, millie_serving_sample)`(같은 인자로 두 번째 out 에 build → `_manifest.json` 제외 파일별 `read_bytes()` 동일) · `test_outputs_exclude_forbidden_and_old_shape(built)`(FORBIDDEN 은 `catalog_kr.json`·`neighbors_kr.json` 원문에만 대소문자 무시 0건 — `persona.description` 이 있는 `preferences_response.json` 등 계약 파일은 제외 · OLD_SHAPE 는 산출 전 파일 대소문자 무시 0건, `not (built / "books.json").exists()`, `not (built.parent / "fallback").exists()`).

RED 확인: `uv run pytest tests/demo --no-header` → 전 테스트가 **`AssertionError` 또는 `AttributeError: module 'make_mock' has no attribute 'build'`** 로 실패해야 한다(현 make_mock.py 에 `build` 가 없음). `ImportError`·collection error 는 Red 가 아니다 — 그 경우 인터프리터(`uv run python --version` == 3.11.x) 와 import 경로를 먼저 고친다. 출력 요약 줄을 SUMMARY 에 붙인다.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && uv run pytest tests/demo --no-header 2>&1 | tail -5 | grep -E "failed|error" </automated>
  </verify>
  <acceptance_criteria>
    - `tests/demo/test_make_mock.py` 존재, `grep -c "^def test_" tests/demo/test_make_mock.py` ≥ 9
    - `grep -n "millie_serving_sample" tests/demo/test_make_mock.py` ≥ 2건(fixture 인자)
    - `grep -n "from millie_rec.serving.schemas_should import DashboardOut, ShowcaseOut" tests/demo/test_make_mock.py` 1건
    - `grep -n "artifacts/serving\|demo/mock" tests/demo/test_make_mock.py | grep -v "^.*#" | wc -l` == 0 (실 산출물 경로를 읽지 않는다 — docstring 제외)
    - `uv run pytest tests/demo --no-header` 종료 코드 ≠ 0 이고 출력에 `AssertionError` 또는 `has no attribute 'build'` 가 있다(Red 증거, SUMMARY 에 인용)
    - `tests/demo/__init__.py` 가 없다
  </acceptance_criteria>
  <done>Red 출력(요약 줄 `N failed`)이 SUMMARY 에 인용되고, 테스트 파일이 위 9개 이상의 단정을 담는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): demo/scripts/make_mock.py 재작성 — 밀리 아티팩트 → mock 13파일(계약 10 + catalog·neighbors 2 + _manifest), popular.json 은 검증만</name>
  <files>demo/scripts/make_mock.py</files>
  <read_first>
    - demo/scripts/make_mock.py (현행 v1 — 계승: `main()` argparse + `write()` 클로저 L295-306, `josa()` L151-159, dedup 루프 L281-292, `SEED = 42`. 폐기: `CSV_URL`·`load_pool`·`assign`·`UNSUPPORTED`·`CATEGORIES`·`SUBCATEGORIES`·`WEIGHTS`·`PERSONAS` dict·`badge_for` 의 `"rating"`·`fallback/popular.json` 쓰기 블록 L369-402·`mock/books.json` `_note`)
    - scripts/export_millie_fallback.py L51-76 (`is_eligible` · `_item` ItemOut 12필드 — 읽기 전용, 복사해서 쓴다)
    - src/millie_rec/serving/rows.py (행 상수: `ROW_SIZE=12` `ANCHOR_NEIGHBORS=20` `TITLE_CONTINUE="이어 읽기"` `TITLE_FRESH="새로운 발견"` `TITLE_PERSONA="{name}의 서가"` `SUBTITLE_ANCHOR="결이 비슷한 책"` `REASON_ANCHOR="『{title}』을 좋아하셨다면"` · purpose 매핑)
    - src/millie_rec/serving/fallback.py L13-16 (`TRENDING_TITLE="지금 많이 읽는 책"` · purpose `"fallback"` · source `"popularity"`)
    - src/millie_rec/serving/badges.py (배지 규칙 — `REVIEW_MIN_COUNT=3` `REVIEW_MIN_COUNT_NO_RATING=10`, 문구 f-string 그대로)
    - src/millie_rec/serving/persona.py (`PERSONAS` 4종 · `CATEGORY_TO_PERSONA` · `DESCRIPTION` · `_josa` — 그대로 복사)
    - src/millie_rec/serving/onboarding_meta.json (`criteria` 5개 id·label 순서 · `subcategories` 정적 dict — 그대로 복사)
    - src/millie_rec/serving/schemas.py · schemas_should.py (필드 집합 — 여분 키 1개도 금지)
    - demo/config/onboarding.json (S1 `options` 5개 = reading_times · S3 `options` 5개 = criteria label)
    - artifacts/serving/eval_table.json (변환 입력 형태)
    - ../.claude/rules/data.md "고정 문장"·"라이선스·귀속" 절 (data_notice 문구 출처)
    - .planning/phases/06-demo-rebuild/06-PATTERNS.md §2-12 (생성 목록·pydantic 대응표)
  </read_first>
  <action>
`demo/scripts/make_mock.py` 를 **새로 쓴다**(표준 라이브러리만: `argparse json hashlib datetime pathlib urllib.parse itertools re`). ≤350줄. `millie_rec` import 금지. 상단 docstring 1줄 + 상수:

```python
SEED = 42
SEEDS = (1012, 2765, 1446, 1222, 2292)   # 본인 5권(STATE.md Day 3 게이트 · report/demo_5books.md) — 06-CONTEXT D-08
CARD_KEYS = ("book_id","title","authors","image_url","categories","publisher","book_format","pop_rank",
             "millie_label","average_rating","review_count","completion_prob","category_avg_prob",
             "expected_min","difficulty","formats")
ROW_SIZE, ANCHOR_NEIGHBORS, K_ITEMS, N_CANDIDATES, SUPPORTED_MIN = 12, 20, 40, 30, 20
VARIANTS = ("pop","cf","hybrid","hybrid_div"); MODEL_SUFFIX = "_v1"
CRITERIA_IDS = ("author","publisher","bestseller","buzz","review")   # serving/onboarding_meta.json 순서 = S3 options 순서
SUBCATEGORIES = {"IT": [...6], "소설": [...7], "철학": [...4]}          # serving/onboarding_meta.json 과 문자 단위 동일
PERSONAS = (("오디세우스","오디세이아","지혜로 승리하리라!"),("셜록 홈즈","주홍색 연구","사소한 것이 가장 중요하다."),
            ("돈키호테","돈키호테","이룰 수 없는 꿈을 꾸리라!"),("제인 에어","제인 에어","나는 나 자신의 주인입니다."))
CATEGORY_TO_PERSONA = {"경제경영":0,"자기계발":0,"IT":0,"소설":1,"과학":1,"철학":1,"인문":2,"역사":2,"사회":2,"에세이/시":3,"라이프스타일":3}
DESCRIPTION = "회원님은 {cats}{eul} 즐기고, {criterion}{ro} 책을 고르는 독서가입니다."
LATENCY = {"feature": 4.0, "retrieval": 12.0, "ranking": 9.0, "rerank": 6.0, "compose": 7.0, "total": 38.0}  # 표시값 — PDF 숫자 아님
FIXED_TS = "2026-09-06T00:00:00+00:00"   # 결정성: 계약 파일 안의 시각은 전부 이 상수
DEMO_USER_KEY = "00000000-0000-4000-8000-000000000042"
PHILOSOPHY = "사용자가 취향 설정에서 직접 알려준 선호를 cold-start(콜드 스타트, 초기 데이터 부족 상태)의 강한 prior(사전 정보)로 사용하되, 영구적인 취향 label(라벨)로 고정하지 않는다. 실제 독서 행동과 현재 세션 의도가 축적되면 행동 신호의 비중을 높이고, 취향 설정을 다시 수행하면 새로운 explicit preference state(명시적 선호 상태)를 생성해 즉시 반영한다. 이 사용자 상태로 Candidate Retrieval(후보 생성) → Ranking(순위화) → Re-ranking(재순위화) → Page Composition(페이지 구성)을 수행해 '클릭할 책'이 아니라 '실제로 읽기 시작할 책'을 메인에 노출한다."
METRIC_MAPPING = [{"stage":"Candidate Retrieval","metric":"Recall@20"},{"stage":"Ranking","metric":"NDCG@10"},{"stage":"Re-ranking","metric":"ILD@10"}]
ROADMAP = ["Reviewer-affinity","Interleaving","선호 교정 루프","텍스트 난이도","Two-Tower/ANN","Kafka/K8s","피크 autoscaling"]
DATA_NOTICE = "평가 비교표 = Goodbooks-10k(CC BY-SA 4.0) · 데모 카탈로그 = 밀리의서재 공개 도서 페이지(수치·메타·표지 URL만, 텍스트 미노출, 요청 시 삭제) · 데모 이웃 = 콘텐츠 유사도(협업 필터링 아님) · 개인정보 무수집 · 서버 latency는 참고값 · 표지는 밀리 CDN 링크"
MDE_NOTE = "데모 표본으로 검정하지 않음 — MDE +1%p 검출에 셀당 n만 명"
MEMORABLE_5 = [
  {"claim":"취향 설정 = cold-start 입력이자 언제든 갱신되는 explicit preference state","how_to_verify":"취향 재설정 후 인스펙터 snap_01 → snap_02, α +0.15","route":"#/refresh"},
  {"claim":"설문 답변을 전부 같은 feature로 보지 않음 — semantic / item seed / selection-policy / context / UX layer 분리","how_to_verify":"취향 설정 각 단계의 인스펙터 신호 해석","route":"#/onboarding"},
  {"claim":"클릭이 목표가 아니다 — Qualified Reading Start 중심 KPI + primary/secondary/guardrail 분리","how_to_verify":"뷰어에서 가상 15분 도달 시 qualified_read, 대시보드 KPI 첫 카드","route":"#/dashboard"},
  {"claim":"Top-K에서 끝나지 않음 — Page Composition까지가 메인 추천 문제","how_to_verify":"메인 5행(이어 읽기·앵커·서가·인기·새로운 발견)과 dedup_removed","route":"#/home"},
  {"claim":"정확도와 production constraint(지연·품질·개인정보·비용)를 같은 수준에서 다룸","how_to_verify":"인스펙터 latency 예산선 200ms · 서재 동의 철회 → level 3","route":"#/library"}]
```

함수(각각 관심사 1개, 순수 함수, 시각·난수 없음):
- `is_eligible(row)` — `export_millie_fallback.is_eligible` 그대로: `bool(title) and host.endswith(".millie.co.kr") and "adult-cover" not in url`.
- `load_catalog(artifacts) -> list[dict]` — `books_kr.json` → eligible 만 → `{k: row.get(k) for k in CARD_KEYS}` (`book_id` int 강제, `formats` 없으면 `[]`). **`pop_rank` 순 정렬**.
- `load_neighbors(artifacts, ids: set[int]) -> dict[str, list]` — `item_edges_kr.json` → 각 src 의 엣지를 `dst ∈ ids` 로 필터 → `weight` 내림차순 → `[:20]` → `[int(dst), round(float(w), 4)]`(source 제외).
- `normalize_title(s)` — `re.sub(r"[^\w]", "", str(s or "")).casefold()`(compose.py 와 같은 정의).
- `josa(word, with_final, without)` — persona.py `_josa` 그대로(ㄹ 받침 + "으로" → "로").
- `badge_for(card, criterion, seed_authors, seed_publishers) -> dict|None` — badges.py 1:1(`{"type": …, "text": …}`; `light` 없음).
- `assign_persona(categories, criterion_label) -> dict` — persona.py 1:1(`{"name","work","quote","description"}`, 미매핑은 `int(hashlib.sha256(head.encode()).hexdigest()[:8],16) % 4`).
- `item(card, position, *, source, reason=None, score=None) -> dict` — ItemOut 12필드 정확히: `book_id score source reason title authors image_url position source_channels badge book_format difficulty`. `score` 기본 `float(ROW_SIZE - position)`. `source_channels = [source]`. `badge` 는 나중에 붙인다(None).
- `neighbor_row(row_id, title, subtitle, seed, nbrs, cards_by_id, exclude, purpose="discover")` — rows.py 와 같은 규칙: eligible(=카탈로그에 있음) ∧ 미제외 ∧ seed 아님 → 12 → items `source="content"`, `reason=title`, `channel_mix={"content": n}`.
- `round_robin(pools: list[list[int]], n, used: set) -> list[int]` — `itertools.zip_longest` 로 카테고리 라운드로빈(onboarding_api._round_robin 과 동일).
- `dedup(rows) -> (rows, removed)` — book_id ∧ 정규화 제목, 앞 행 우선, position 재부여, `channel_mix` 재계산.
- `compose(variant, cards, by_id, nbrs, seeds, categories, criterion, persona_name) -> RecommendOut dict` — 5행: `continue_reading`(items `[]`, "이어 읽기", resume) → `anchor_<seed₁>`(REASON_ANCHOR 로 title/reason, subtitle "결이 비슷한 책") → `persona_shelf`(`"{persona}의 서가"`, discover — variant 별 표시 규칙: `pop` = 선택 카테고리 인기순 · `cf`/`hybrid` = 전 시드 이웃 합집합 weight 내림차순 `source="content"` 후 인기 보충 `source="popularity"` · `hybrid_div` = `hybrid` 결과를 카테고리 라운드로빈) → `trending`("지금 많이 읽는 책", fallback, pop_rank 순, popularity) → `fresh_picks`("새로운 발견", explore, 선택 카테고리 밖 카테고리 라운드로빈, popularity). 배지 부착(criterion = "bestseller" 예시) → dedup → `items` = 행 순서 평탄화 상위 40. 응답: `recommendation_id="rec_"+sha256(variant)[:6]` · `model_version=variant+"_v1"` · `preference_snapshot_id="snap_"+sha256("demo")[:6]` · `user_key=DEMO_USER_KEY` · `cell` = `"A" if int(sha256(DEMO_USER_KEY)[:8],16)%2==0 else "B"` · `forced=True` · `fallback_level=0` · `context="저녁, 하루를 마치며"` · `latency_ms=LATENCY["total"]` · `latency_breakdown=LATENCY` · `user_state_weights={"alpha":0.7,"beta":0.2,"gamma":0.1}` · `dedup_removed` · `nearline_lag_s=1.0`.
- `meta_onboarding(cards, config) -> dict` — `survey_variant="v1"` · `reading_times = config.steps[S1].options` · `criteria = [{"id": CRITERIA_IDS[i], "label": config.steps[S3].options[i]}]` · `categories = [{"name": c, "supported": count ≥ 20, "subcategories": SUBCATEGORIES.get(c, [])}]` (카테고리 = eligible 카탈로그의 categories 전부, 권수 내림차순·동률은 이름순).
- `candidates(cards, categories=("소설","인문","자기계발"), n=30)` — 라운드로빈 pop_rank 순 → `CandidateSet{candidate_set_id="cand_"+sha256("demo")[:6], survey_variant="v1", created_at=FIXED_TS, items[{book_id,title,authors,image_url,position,book_format}]}`.
- `preferences_response(persona) -> dict` — `{user_key, preference_snapshot_id, created_at=FIXED_TS, cell, snapshots_count=1, persona}`.
- `state(cards_by_id, seeds) -> dict` — `UserStateOut{user_key, consent=True, cell, is_new=False, library={"added":[LibraryBook×5 seeds],"reading":[],"completed":[]}, snapshots=[{snapshot_id, created_at, categories, criterion="bestseller", active=True}], user_state_weights, nearline_lag_s=1.0}`.
- `dashboard_empty() -> dict` — `DashboardOut{generated_at=FIXED_TS, window="all", kpi={6개 이름: {"value":0.0,"n":0,"note":…}}, ab_table=[], mde_note=MDE_NOTE, latency={"p50":0.0,"p95":0.0,"p99":0.0,"by_stage":{}}, quality={"impression_receipt_rate":0.0,"flagged_events":0.0,"feature_freshness_s":0.0}, events_recent=[], impressions_log=[], by_variant={}, by_hour=[]}`. kpi 이름 6개 = `qualified_reading_start_rate` `first_completion_rate_new` `fallback_rate` `p95_latency_ms`(note "mock 상수 · 참고용") `error_rate` `active_user_keys`.
- `showcase(eval_table_path, latency_path, cards_by_id, nbrs, seeds) -> dict` — `eval_table.json` rows → `EvalRow{variant, recall_at_20=float(row["recall@20"]), ndcg_at_10=float(row["ndcg@10"]), ild_at_10=float(row["ild@10"]), p95_ms=None}`(`latency_path` 가 있고 파싱되면 `p95` 병기, 없으면 None) · `split_mode = meta.split_mode` · `source={"metrics":"results/latest.csv","p95":"results/latency.json"}` · `personal_case` = seeds 가 전부 카탈로그에 있으면 `{"seeds":[ShowcaseBook{book_id,title,image_url,reason=None,badge=None}×5], "recommendations": 시드별 이웃 top-20 을 라운드로빈으로 돌며 시드·중복(book_id ∧ 정규화 제목) 제외 상위 10 → ShowcaseBook{book_id,title,image_url,reason=REASON_ANCHOR.format(title=시드 제목), badge=badge_for(card,"bestseller",…)}}`, 하나라도 없으면 `None`(화면 "5권 선정 대기") · `memorable_5=MEMORABLE_5` · `roadmap=ROADMAP` · `data_notice=DATA_NOTICE` · `philosophy=PHILOSOPHY`.
- `validate_fallback(path)` — 존재하면 `json.load` 후 키 집합이 `{"recommendation_id","model_version","preference_snapshot_id","user_key","cell","forced","fallback_level","context","latency_ms","latency_breakdown","user_state_weights","dedup_removed","nearline_lag_s","items","rows"}` 인지·`model_version=="fallback_v1"`·`isinstance(latency_ms, float)` 만 확인해 stdout 1줄(`[make_mock] fallback/popular.json ok (n rows)`) — **쓰지 않는다**. 없으면 `[make_mock] warn: fallback/popular.json 없음 — make millie-export 가 만든다`.
- `build(artifacts: Path, out: Path, config: Path, *, eval_table: Path | None = None, latency: Path | None = None, fallback: Path | None = None, seeds=SEEDS) -> dict[str, int]` — `out.mkdir(parents=True)` → **`out` 안의 `*.json` 전부 삭제**(D-06: 구 books.json·meta_onboarding.json·preferences_response.json·recommend_*.json 제거) → 위 함수로 12파일 작성(계약 10 + `catalog_kr`·`neighbors_kr` 2; 계약 10개는 `json.dumps(o, ensure_ascii=False, indent=1)`, `catalog_kr.json`·`neighbors_kr.json` 은 `json.dumps(o, ensure_ascii=False, separators=(",", ":"))`, `encoding="utf-8"`, 파일별 바이트 크기 반환) → `_manifest.json` 마지막(`generated_at=datetime.now(UTC).isoformat()`, `seed`, `inputs` md5, `outputs` bytes) → `validate_fallback(fallback)` (fallback 인자가 None 이면 `out.parent / "fallback" / "popular.json"` 이 **존재할 때만** 검증, 디렉터리를 만들지 않는다).
- `main()` — `argparse`: `--artifacts`(기본 `artifacts/serving`, 스크립트 위치 기준 `Path(__file__).resolve().parents[2] / "artifacts" / "serving"`) · `--out`(기본 `demo/mock`) · `--config`(기본 `demo/config/onboarding.json`) · `--latency`(기본 `results/latency.json`, 없으면 무시). 끝에 stdout 1줄 `[make_mock] wrote N files → {out} ({총 KB} KB)`.

GREEN 확인: `uv run pytest tests/demo --no-header` → `N passed`. 그 뒤 **실 아티팩트로 1회 실행**: `uv run python demo/scripts/make_mock.py` → `ls demo/mock` 13파일(계약 10 + catalog·neighbors 2 + `_manifest.json`) · `du -sh demo/mock` · `uv run python -c` 로 10파일 `model_validate`(아래 verify 명령). `demo/fallback/popular.json` 의 mtime 이 실행 전후 같아야 한다(`stat -f %m` 비교 — 쓰지 않았다는 증거).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && uv run pytest tests/demo --no-header 2>&1 | tail -3 && uv run python demo/scripts/make_mock.py && uv run python -c "
import json,pathlib
from millie_rec.serving.schemas import *; from millie_rec.serving.schemas_should import *
M={'meta_onboarding.json':OnboardingMeta,'candidates_onboarding.json':CandidateSet,'preferences_response.json':PreferencesResponse,'recommend_pop.json':RecommendOut,'recommend_cf.json':RecommendOut,'recommend_hybrid.json':RecommendOut,'recommend_hybrid_div.json':RecommendOut,'state.json':UserStateOut,'dashboard.json':DashboardOut,'showcase.json':ShowcaseOut}
for n,m in M.items(): m.model_validate(json.loads(pathlib.Path('demo/mock',n).read_text('utf-8'))); print('ok',n)
RecommendOut.model_validate(json.loads(pathlib.Path('demo/fallback/popular.json').read_text('utf-8'))); print('ok fallback/popular.json')
"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/demo --no-header` 출력 요약 줄에 `passed` 가 있고 `failed` 가 없다
    - `wc -l demo/scripts/make_mock.py` ≤ 350
    - `grep -cE "^(import|from) (millie_rec|pandas|numpy|requests)" demo/scripts/make_mock.py` == 0 (표준 라이브러리만)
    - `grep -n "SEEDS = (1012, 2765, 1446, 1222, 2292)" demo/scripts/make_mock.py` 1건
    - `grep -n 'def build(' demo/scripts/make_mock.py` 1건 · `grep -n 'def is_eligible' demo/scripts/make_mock.py` 1건
    - `grep -nE 'popular\.json' demo/scripts/make_mock.py | grep -E 'write|dump|open\(.*"w' | wc -l` == 0 (popular.json 쓰기 코드 없음)
    - `grep -n 'anchor_case\|"rating"\|CSV_URL\|zygmuntz\|hf\.space\|"timestamp"\|readingTimes\|static_popular' demo/scripts/make_mock.py | wc -l` == 0 (`Goodbooks-10k` 는 `DATA_NOTICE` 정본 문장이라 금지하지 않는다 — `grep -c 'Goodbooks-10k(CC BY-SA 4.0)' demo/scripts/make_mock.py` == 1)
    - 실 실행 후 `ls demo/mock | sort | tr '\n' ' '` == `_manifest.json candidates_onboarding.json catalog_kr.json dashboard.json meta_onboarding.json neighbors_kr.json preferences_response.json recommend_cf.json recommend_hybrid.json recommend_hybrid_div.json recommend_pop.json showcase.json state.json ` (13개 — books.json 없음)
    - 위 verify 의 `uv run python -c` 가 `ok` 11줄(계약 10 + fallback/popular.json)을 찍고 예외 0
    - `python3 -c "import json;d=json.load(open('demo/mock/catalog_kr.json'));print(len(d), sorted(d[0]))"` → 9,400권대(9,444 예상) · 키 16개
    - `python3 -c "import json;d=json.load(open('demo/mock/showcase.json'));print(len(d['personal_case']['seeds']), len(d['personal_case']['recommendations']), d['eval_table']['rows'][0])"` → `5 10 {'variant': 'pop', 'recall_at_20': 0.063, …}`
    - `grep -c '"description"\|"curator_note"' demo/mock/catalog_kr.json` == 0
    - `du -sh demo/mock` ≤ 6M (압축 catalog ≈2~2.5MB + neighbors ≈2MB) · `python3 -c "import json;d=json.load(open('demo/mock/neighbors_kr.json'));v=next(iter(d.values()));print(len(v[0]), len(d))"` → `2 94xx`
    - `stat -f %m demo/fallback/popular.json` 이 실행 전과 같다(SUMMARY 에 두 값 인용)
  </acceptance_criteria>
  <done>tests/demo 전부 Green, 실 아티팩트로 13파일 생성·검증 통과, popular.json 무변경, 구 books.json 삭제.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REFACTOR): 350줄 상한·관심사 점검 · 결정성 재확인 · Green 유지</name>
  <files>demo/scripts/make_mock.py</files>
  <read_first>
    - demo/scripts/make_mock.py (Task 2 결과)
    - ../.claude/rules/simplicity.md "코드 규칙"(함수 우선·상수 상단·docstring 1줄·주석은 왜만)
  </read_first>
  <action>
통과 상태에서만 정리한다(범위 = 이 파일 1개, 최소 변경): ① 상수 블록을 파일 상단에 모으고 각 상수에 "왜" 주석 1줄(예: `# 표시값 — PDF 숫자 아님(백엔드 01 §0)`) ② 함수마다 docstring 1줄 ③ 중복 코드(라운드로빈·item 조립) 가 두 번째 등장하면 함수로 뺀다 — 세 번째 전에는 뺄 필요 없음 ④ `main()` 은 인자 파싱과 `build()` 호출만. 정리 후 `uv run pytest tests/demo --no-header` 재통과 + `uv run python demo/scripts/make_mock.py` 재실행 → `_manifest.json` 의 `outputs` 바이트 수가 Task 2 실행과 같은지 비교(결정성 실측). `uv run ruff check demo/scripts/make_mock.py tests/demo/test_make_mock.py`(demo 는 pyproject 에서 exclude 지만 경로 명시는 검사된다) → 위반 0. `uv run ruff format demo/scripts/make_mock.py tests/demo/test_make_mock.py` 적용.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec && uv run ruff check demo/scripts/make_mock.py tests/demo/test_make_mock.py && uv run pytest tests/demo --no-header 2>&1 | tail -2 && uv run pytest --no-header 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check demo/scripts/make_mock.py tests/demo/test_make_mock.py` 출력 `All checks passed!`
    - `uv run pytest tests/demo --no-header` 요약 `N passed`, N ≥ 9
    - `uv run pytest --no-header` 전체 요약 줄이 `failed` 없이 끝난다(기준선 327 passed 이상 — Phase 5 동시 실행으로 숫자는 더 클 수 있다; `tests/demo` 가 더해진 수치를 SUMMARY 에 기록)
    - `wc -l demo/scripts/make_mock.py` ≤ 350
    - `python3 -c "import json;print(json.load(open('demo/mock/_manifest.json'))['seed'])"` == `42`
  </acceptance_criteria>
  <done>ruff 0 · tests/demo Green · 전체 pytest Green · 결정성 실측(두 실행의 outputs 바이트 동일) SUMMARY 기록.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `artifacts/serving/*.json`(🧊 freeze, 읽기) → 생성기 | 신뢰 입력이지만 밀리 저작 텍스트가 섞여 들어올 경로 — 화이트리스트로 차단 |
| 생성기 → `demo/mock/*.json`(public repo · 브라우저 노출) | 저작권·개인정보 노출 표면. 계약 형태(`extra="forbid"`) |
| 생성기 ↔ `demo/fallback/popular.json` | 다른 스크립트 소유 — 쓰기 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | `catalog_kr.json` 에 밀리 설명문·큐레이터 노트·리뷰 텍스트 유출(저작권, `../.claude/rules/data.md`) | mitigate | `CARD_KEYS` 16개 화이트리스트로만 조립(블랙리스트 아님) + 테스트 `FORBIDDEN` 6키 0건 단정 |
| T-06-01-02 | Information Disclosure | 성인 표지·`title` 없는 책이 데모 카탈로그에 노출 | mitigate | `is_eligible` 필터(fixture 18·19·20 제외 단정) — 서버 `Catalog.eligible()` 과 같은 규칙 |
| T-06-01-03 | Tampering | 구 mock(Goodbooks 영문 제목·`badge.type "rating"`) 잔존 → 화면에 Goodbooks 영문 책 제목 | mitigate | `build()` 가 `out/*.json` 전부 삭제 후 재생성 · 테스트 `OLD_SHAPE`(v1 흔적 `CSV_URL`·`zygmuntz`·`hf.space`·`static_popular`) 0건 · 06-07 grep 게이트. `goodbooks` 단어 자체는 검사하지 않는다(데이터셋 귀속 고지 `data_notice` 는 정본) |
| T-06-01-04 | Tampering | 생성기가 `demo/fallback/popular.json` 을 덮어써 export 산출물 파손(Phase 3 인계 사유) | mitigate | 쓰기 코드 0(acceptance grep) · mtime 불변 확인 · `validate_fallback` 은 읽기만 |
| T-06-01-05 | Repudiation | mock 을 손으로 고쳐 계약과 어긋난 채 심사 | mitigate | `_manifest.json`(입력 md5·출력 bytes) 로 `/contract-sync` 6항이 재생성 여부 판정 · `demo.md` 손편집 금지 |
| T-06-01-06 | Denial of Service | `catalog_kr.json`·`neighbors_kr.json` 이 커서 브라우저 첫 로드 지연 | accept | 카드 필드만·top-20 절단·source 제외·압축 직렬화로 각 ≈2MB(`du -sh demo/mock` ≤ 6M). 데모는 Chrome 데스크톱만(`demo.md`) |
| T-06-01-07 | Information Disclosure | `state.json`·`preferences_response.json` 에 실제 사용자 키 | accept | `DEMO_USER_KEY` 고정 예시 UUID — 개인정보 아님 |
</threat_model>

<verification>
- `uv run pytest tests/demo --no-header` → `N passed`(N ≥ 9), Red 출력이 SUMMARY 에 먼저 인용됨(`AssertionError`/`has no attribute 'build'`)
- `uv run python demo/scripts/make_mock.py` → 13파일(계약 10 + catalog·neighbors + `_manifest`) · `books.json` 없음
- 계약 파일 10개 `model_validate` 통과(= `/contract-sync` 2항) · `demo/fallback/popular.json` 도 `RecommendOut` 통과 · mtime 불변
- `uv run pytest --no-header` 전체 Green · `uv run ruff check demo/scripts/make_mock.py tests/demo/test_make_mock.py` 0
- `make mock` 을 실행하지 않았다(SUMMARY 에 명시 — Makefile 정정은 06-07 PROGRESS 제안)
</verification>

<success_criteria>
- DEMO-02 기계 판정: mock 10개 재생성 + schemas 검증 전부 통과 + `latency_ms` float · `book_format` · `badge.type` 6종 · criteria `{id,label}` · `cell`·`snapshots_count`
- 06-CONTEXT D-02(카드 필드만 ≈2MB·저작 텍스트 0)·D-06(구 mock 삭제·재생성, popular.json 은 §0-A 로 제외)·D-08(showcase.json — `personal_case` 로 적용)·D-10(메타는 config/onboarding.json 에서) 이행
- wave 2 의 입력(catalog_kr·neighbors_kr·showcase·meta_onboarding) 이 `demo/mock/` 에 존재
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-rebuild/06-01-SUMMARY.md` — 변경 파일 목록(커밋 0), Red/Green 출력 인용, `du -sh demo/mock`, popular.json mtime 전후, `_manifest.json` outputs 요약, 전체 pytest 수치.
</output>
