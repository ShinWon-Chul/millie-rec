---
phase: 06-demo-rebuild
plan: 08
type: execute
wave: 5
gap_closure: true
depends_on: ["06-07"]
source: 06-UAT.md Gap 1
autonomous: true
requirements: [DEMO-07, DEMO-06]
lane: 계약(Advisor 승인 예외) + Serving 1파일 + Demo
files_modified:
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/privacy_api.py
  - demo/scripts/make_mock.py
  - demo/js/mock.js
  - demo/js/screens/d5_library.js
  - demo/js/screens/d8_showcase.js
  - demo/mock/showcase.json
  - demo/mock/state.json
  - tests/serving/test_privacy_api.py
  - tests/demo/test_make_mock.py
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/data/**
  - src/millie_rec/app/**
  - src/millie_rec/serving/** (위 3파일 제외 — 특히 Dockerfile·railway·server.py 는 Phase 7 세션)
  - Makefile
  - pyproject.toml
  - .planning/STATE.md
  - .planning/ROADMAP.md
---

<objective>
`/gsd-verify-work 6` Gap 1 을 닫는다 — 내 서재(#/library) 담은 책 5권과 쇼케이스(#/) 본인 5권에
저자가 "저자 미상" 으로 뜨는 문제. 원인은 `LibraryBook`·`ShowcaseBook` 두 응답 모델에 저자
필드가 없다는 것 하나다(`06-UAT.md` Gap 1 진단 참조).

**계약 freeze 예외 승인됨** — 사용자 결정 2026-09-06(`/gsd-verify-work 6` 응답 2번),
`PROGRESS.md` 세션 간 알림, 개발일지 D81. 허용 형태는 기본값 있는 optional 추가뿐이다
(`.claude/rules/architecture.md` "필드 추가는 기본값 있는 optional 로만. 삭제·이름 변경 금지").
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: 계약 2줄 + 서버 1단어 — RED → GREEN</name>
  <files>src/millie_rec/serving/schemas.py, schemas_should.py, privacy_api.py, tests/serving/test_privacy_api.py</files>
  <action>
RED — `tests/serving/test_privacy_api.py` 에, 가짜 카탈로그가 `authors` 를 주면
`GET /api/users/{key}/state` 의 `library["added"][0].authors` 로 그대로 나온다는 테스트를 먼저
추가한다. 기존 픽스처(46행 근처)의 카탈로그 dict 에 `"authors": f"저자{b}"` 를 더한다.
카탈로그가 없을 때 `authors is None` 인 것도 함께 단정한다(기존 264행 패턴).

GREEN —
1. `schemas.py:239 LibraryBook` 에 `authors: str | None = None` 추가(`title` 다음 줄).
2. `schemas_should.py:75 ShowcaseBook` 에 `authors: str | None = None` 추가(`title` 다음 줄).
3. `privacy_api.py:25` `BOOK_KEYS = ("title", "image_url")` → `("title", "authors", "image_url")`.
   80행 `cards` 는 `m.get(b, {}).get(k)` 로 키를 훑으므로 그 외 서버 변경은 없다 —
   `CatalogKR.meta()`(`data/catalog_kr.py:73`) 가 `books_kr.json` 원소를 통째로 돌려주고
   거기에 `authors` 가 이미 있다.
  </action>
  <acceptance_criteria>
    - 새 테스트가 GREEN 전에 실패하고(AssertionError) GREEN 후 통과
    - `uv run pytest tests/serving -q` 전부 통과 — 기존 단정 무수정
    - 두 모델의 기존 필드 이름·순서·타입 무변경, 추가는 optional 1개씩
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: 생성기 + mock 재생성 — RED → GREEN</name>
  <files>demo/scripts/make_mock.py, tests/demo/test_make_mock.py, demo/mock/showcase.json, demo/mock/state.json</files>
  <action>
RED — `tests/demo/test_make_mock.py` 에 `showcase.json` 의 `personal_case.seeds[*]` 와
`recommendations[*]` 가 전부 비어 있지 않은 `authors` 를 갖는다는 테스트를 추가한다.

GREEN — `make_mock.py:475 personal_case()` 의 `books` dict 와 `recs` dict 양쪽에
`"authors": by_id[...]["authors"]` 를 넣는다. `state.json` 의 library 카드에도 같은 필드가
필요하면 함께 채운다. 그 뒤 `uv run python demo/scripts/make_mock.py` 로 재생성한다.
**`make mock`·`make millie` 는 실행 금지**(카탈로그 Day 3 freeze).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/demo -q` 통과
    - 재생성한 `showcase.json` 의 `ShowcaseOut.model_validate` 통과, `state.json` 의
      `UserStateOut.model_validate` 통과 (여분 키 0 — `_Strict`)
    - 재실행 시 `_manifest.json` 을 뺀 산출물이 바이트 단위 동일(결정성 유지)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 화면 2개 — 저자 표시</name>
  <files>demo/js/mock.js, demo/js/screens/d5_library.js, demo/js/screens/d8_showcase.js</files>
  <action>
1. `mock.js:238` `card()` 에 `authors: byId(b)?.authors ?? null` 추가(`getUserState` 의 library).
2. `d5_library.js:26 authorOf()` — 응답이 준 `b.authors` 를 **먼저** 쓰고, 없을 때만 기존
   `state.recommend.rows` 조회로 떨어진다. `tile()` 37행은 그 결과를 쓴다.
3. `d8_showcase.js:52` `.case__author` — `book_id N` 대신 저자를 보이고, `authors` 가 없을
   때만 `book_id N` 으로 떨어진다. 동명 도서 구분(결정 D-07b ③)의 원래 의도가 저자 병기다.
  </action>
  <acceptance_criteria>
    - 세 파일 각각 ≤250줄 유지 · `node --check` 통과
    - 모든 문자열이 `esc()` 를 거친다 · `title ?? "(제목 없음)"` 방어 유지
    - 라우팅·dedup 은 계속 `book_id` 로만 한다(제목·저자로 동일성 판단 금지)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `uv run pytest --no-header` failed 0 · `uv run ruff check demo/scripts tests/demo` 0
- `make smoke` PASS 3줄
- `demo/mock/*.json` 계약 10 + `fallback/popular.json` = `model_validate` 11/11
- `make demo-serve` → `?source=mock` 에서 `#/library` 담은 책과 `#/` 본인 5권에 실제 저자가
  보이고 "저자 미상" 이 사라진다
- 구 형태 grep 0건 유지 · `wc -l` 250 초과 0
</verification>

<success_criteria>
- Gap 1 종료: 서재·쇼케이스에서 저자가 표시된다
- 계약 변경은 optional 2개뿐 — 기존 필드 무변경, 여분 키 0
- `src/` 편집은 위 3파일로 제한(Phase 7 '배포' 세션과 충돌 0)
</success_criteria>
