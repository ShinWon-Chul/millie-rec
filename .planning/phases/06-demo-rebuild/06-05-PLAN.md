---
phase: 06-demo-rebuild
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - demo/js/screens/d7_dashboard.js
  - demo/js/screens/d8_showcase.js
  - demo/css/dashboard.css
  - demo/css/tokens.css
autonomous: true
requirements: [DEMO-06, DEMO-08]
lane: Demo (대시보드·쇼케이스 PC 페이지 Worker — wave 2 병렬 4 중 4)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(05 관례, 사용자 지시 2026-09-05)
must_not_touch:
  - src/**
  - tests/**
  - Makefile
  - pyproject.toml
  - Dockerfile
  - artifacts/**
  - results/**
  - report/**
  - demo/js/app.js            # 06-02
  - demo/js/actions.js        # 06-02
  - demo/js/presets.js        # 06-02
  - demo/js/router.js         # 06-02
  - demo/js/api.js            # 06-03
  - demo/js/mock.js           # 06-03
  - demo/js/mock_store.js     # 06-03
  - demo/js/inspector.js      # 06-06
  - demo/js/screens/ui.js     # 06-02
  - demo/js/screens/onboarding_step.js
  - demo/js/screens/s0_start.js
  - demo/js/screens/s6_persona.js
  - demo/js/screens/s7_home.js
  - demo/js/screens/s8_detail.js
  - demo/js/screens/d2_home.js   # 06-06
  - demo/js/screens/d3_detail.js # 06-06
  - demo/js/screens/d4_reader.js # 06-04
  - demo/js/screens/d5_library.js # 06-04
  - demo/css/base.css         # 06-02
  - demo/css/home.css         # 06-06
  - demo/css/inspector.css    # 06-06
  - demo/css/onboarding.css
  - demo/css/reader.css       # 06-04
  - demo/css/library.css      # 06-04
  - demo/index.html           # 06-02
  - demo/config/onboarding.json
  - demo/README.md
  - demo/mock/**
  - demo/fallback/popular.json
  - demo/scripts/make_mock.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - ../PROGRESS.md
  - ../.assets/**

must_haves:
  truths:
    - "쇼케이스 화면(#/, 화면 구성 02 §2)이 헤더 + main §1 철학 문장 · Track A 비교표 4행(variant·Recall@20·NDCG@10·ILD@10·p95) + 'split_mode: holdout' 캡션 + 출처 캡션(results/latest.csv · results/latency.json) · 단계↔지표 1:1 대응 CSS 그림(Candidate Retrieval→Recall@20 / Ranking→NDCG@10 / Re-ranking→ILD@10) · 2트랙 데이터 고지를 그린다(DEMO-06)"
    - "쇼케이스는 personal_case 가 있으면 본인 5권 → 추천 10(reason·배지) + 캡션 '데모 이웃 = 콘텐츠 유사도', null 이면 '5권 선정 대기'; memorable_5 카드 · 로드맵 회색 카드 · 시작 버튼(nav #/onboarding) + 프리셋 3(preset newUser/skipUser/resetUser) 을 그린다"
    - "관제 대시보드 화면(#/dashboard, 화면 구성 02 §2)이 DashboardOut 한 형태만 렌더한다 — KPI 카드 6 + n 병기 · A/B 표 4행 + mde_note 고지 · latency 막대 + 예산선 200ms · 품질 패널 3 · 이벤트 스트림 50 · 노출 로그 · '서버 실측 · 참고용' 라벨 · CSS 막대만(DEMO-08). state.dashboard 가 null 이면 '이 항목은 아직 없음(서버 준비 중)' 을 그린다"
    - "D7·D8 상단에 DemoLabel '데모 전용 · 실제 밀리 화면 아님' 이 있고, 숫자는 전부 응답(showcase/dashboard)에서만 온다 — 하드코딩 숫자 0"
    - "tokens.css 에 화면 02 §6 토큰 12개 + 모달 딤 `--dim` 1개(06-02 base.css 가 참조) 가 그대로 추가되고, 색 리터럴은 tokens.css 에만 있다"
  artifacts:
    - path: "demo/js/screens/d8_showcase.js"
      provides: "render(state) — 헤더·철학·MetricsTable·metric_mapping 그림·personal_case·memorable_5·시작 버튼+프리셋 3·roadmap·data_notice"
      exports: ["render"]
      max_lines: 200
    - path: "demo/js/screens/d7_dashboard.js"
      provides: "render(state) — KpiCard×6·A/B DataTable·latency BarChart+예산선·quality·events_recent·impressions_log·by_variant; null 방어"
      exports: ["render"]
      max_lines: 200
    - path: "demo/css/dashboard.css"
      provides: ".page-head · .kpi* · .dt(DataTable) · .bar* · .mapping* · .case* · .memo* · .roadmap* · .start* · .stream* — D7·D8 공용"
      max_lines: 250
    - path: "demo/css/tokens.css"
      provides: "기존 + §6 추가분 --difficulty-on/off --star/--star-off --banner-bg/--banner-text --kpi-bg/--kpi-good/--kpi-warn/--kpi-bad --light-badge --demo-label + 딤 --dim"
      contains: "--demo-label: #6E6E73;"
  key_links:
    - from: "demo/js/screens/d8_showcase.js"
      to: "state.showcase (ShowcaseOut)"
      via: "eval_table.rows[] · split_mode · source · metric_mapping · personal_case · memorable_5 · roadmap · data_notice"
      pattern: "state\\.showcase"
    - from: "demo/js/screens/d7_dashboard.js"
      to: "state.dashboard (DashboardOut)"
      via: "kpi{name:{value,n,note}} · ab_table · mde_note · latency · quality · events_recent · impressions_log · by_variant"
      pattern: "state\\.dashboard"
    - from: "demo/js/screens/d8_showcase.js"
      to: "demo/js/presets.js(06-02)"
      via: "data-act=\"preset\" data-preset=newUser|skipUser|resetUser 버튼 3"
      pattern: "data-act=\"preset\""
---
<objective>
Phase 6 '데모 재구성'(.planning/ROADMAP.md "Phase 6: 데모 재구성") 의 **PC 전폭 페이지 2개**를 만든다: `js/screens/d8_showcase.js`(심사자가 처음 보는 화면 — 철학 문장·Track A 비교표·단계↔지표 그림·본인 5권·시작 버튼·고지) · `js/screens/d7_dashboard.js`(관제 대시보드 — `DashboardOut` 한 형태) · `css/dashboard.css`(둘 공용) · `css/tokens.css` §6 토큰 추가(색 리터럴은 이 파일에만).

Purpose: ROADMAP Success Criteria 4 후반 — "쇼케이스 화면에 Track A 비교표(`split_mode` 캡션)·단계↔지표 대응 그림·중심 설계 철학 문장·2트랙 데이터 고지"(**DEMO-06**, Must) + Should 꼬리 **DEMO-08** "관제 대시보드가 `GET /api/dashboard` 표시". D8 은 PDF P4·P5 의 근거 캡처(화면 02 §7 "쇼케이스 — 평가 비교표·본인 5권"), 데모 첫 화면 = PDF 첫 문단(화면 02 §2 D8). 06-CONTEXT D-08(showcase.json 실측 숫자 — Phase 5 없이도) · D-03(대시보드 숫자 = 세션 집계·고정 예시 금지) 이행. 숫자 규칙: 비교표 = Goodbooks(Track A) 만, p95 = `results/latency.json` 만, D7 수치는 "참고용" 라벨(`../.claude/rules/serving.md`).
Output: 신규 3(d7·d8·dashboard.css) + `tokens.css` 토큰 13개 추가(§6 12 + `--dim`). 커밋 0.

**wave 2 병렬 불변:** `ui.js` 신규 `demoLabel` 은 06-02 가 같은 시각에 추가 — 부록 E 시그니처로 import. 프리셋 실행은 06-02 `presets.js` — 이 플랜은 `data-act="preset"` 버튼만. `state.showcase`/`state.dashboard` 는 `app.js` 가 채운다(부록 A). 판정은 `node --check`·grep·`wc -l`; 브라우저는 06-06·06-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-rebuild/06-CONTEXT.md
@.planning/phases/06-demo-rebuild/06-PATTERNS.md
@.planning/phases/06-demo-rebuild/06-01-PLAN.md
@../.claude/rules/demo.md
@../.claude/rules/serving.md
@../.claude/rules/references.md
@../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md
@../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@src/millie_rec/serving/schemas_should.py
@src/millie_rec/contracts.py
@demo/js/screens/ui.js
@demo/js/inspector.js
@demo/css/inspector.css
@demo/css/tokens.css
@demo/mock/showcase.json
@demo/mock/dashboard.json

<file_ownership>
## Phase 6 파일 소유 표 (겹침 0 — 같은 wave 는 서로소)

| 파일 | 만듦/고침/삭제 | 플랜 | wave |
|---|---|---|---|
| `demo/scripts/make_mock.py` · `tests/demo/test_make_mock.py` · `demo/mock/*.json`(13개 — 계약 10 + catalog_kr·neighbors_kr + _manifest) · `demo/mock/books.json` 삭제 | 재작성·신규·재생성 | 06-01 | 1 |
| `demo/js/router.js` · `demo/js/app.js` · `demo/js/actions.js` · `demo/js/presets.js` · `demo/js/screens/ui.js` · `demo/js/screens/onboarding_step.js` · `demo/js/screens/s0_start.js` · `demo/js/screens/s6_persona.js` · `demo/index.html` · `demo/css/base.css` · `demo/config/onboarding.json` · `demo/README.md` | 신규·재작성·최소 수정 | 06-02 | 2 |
| `demo/js/api.js` · `demo/js/mock.js` · `demo/js/mock_store.js` | 재작성·신규 | 06-03 | 2 |
| `demo/js/screens/d4_reader.js` · `demo/js/screens/d5_library.js` · `demo/css/reader.css` · `demo/css/library.css` | 신규 | 06-04 | 2 |
| `demo/js/screens/d7_dashboard.js` · `demo/js/screens/d8_showcase.js` · `demo/css/dashboard.css` · `demo/css/tokens.css` | 신규·토큰 추가 | **06-05(이 플랜)** | 2 |
| `demo/js/screens/d2_home.js`(← `s7_home.js` 삭제) · `demo/js/screens/d3_detail.js`(← `s8_detail.js` 삭제) · `demo/js/inspector.js` · `demo/css/home.css` · `demo/css/inspector.css` | 신규·삭제·확장 | 06-06 | 3 |
| `.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md` · `../PROGRESS.md`(미결 절 2줄 append 만) | Advisor | 06-07 | 4 |
</file_ownership>

<interfaces>
<!-- 공통 인터페이스 부록 — 06-02 ~ 06-06 다섯 플랜에 **동일한 텍스트**로 들어간다. 한 곳만 바꾸면 병렬 Worker 가 어긋난다. -->
<!-- 정본: 화면 구성 02 §4 상태 모델 v2 · §5 이벤트 13종 · 화면 구성 01 §6 · serving/schemas.py(응답 형태) · contracts.py(이름). -->

## A. `state` v2 — `demo/js/app.js` 의 단일 상태 객체 (06-02 소유, 다른 파일은 읽기만)
```js
const state = {
  route: { page: "showcase", params: {} },  // router.parse(location.hash). page ∈ showcase|onboarding|home|book|reader|library|refresh|dashboard
  source: "mock",                            // "mock" | "api"
  userKey: null,                             // localStorage["millie_user_key"] UUIDv4 — boot 에서 항상 채움(익명도 키 있음)
  cell: null,                                // "A" | "B" — postPreferences 응답 · getUserState 응답
  consent: null,                             // null | true | false (건너뛰기 = false)
  screen: "S0",                              // D1/D6 내부 단계 "S0".."S6"
  resetting: false,                          // D6(#/refresh) 모드 — 헤더 "취향 다시 설정", S0 생략, CTA "새 취향으로 추천 받기"
  prefs: { readingTime: null, categories: [], criterion: null, subcategories: [], seedBooks: [] },  // criterion = S3 **라벨**(config 텍스트)
  steps: [],                                 // config/onboarding.json .steps
  meta: null,                                // OnboardingMeta {survey_variant, reading_times[], criteria[{id,label}], categories[{name,supported,subcategories[]}]}
  candidateSet: { id: null, items: [], impressions: [] },  // items = CandidateItem[], impressions = [{book_id, position, selected}]
  snapshots: [],                             // [{ id, createdAt, prefs, persona, cell }] — append only(재설정도 push)
  model: null,                               // null = 셀 배정(A→hybrid, B→hybrid_div) · VARIANTS 문자열 = 인스펙터 라디오 강제(forced)
  recommend: null,                           // RecommendOut 원문. api 클라이언트 fallback 시 + client_fallback_reason: string
  detail: null,                              // { item: ItemOut, rowId } | null — D3 바텀시트. rowId ∈ RecommendOut.rows[].row_id | "library"(D5 서재 타일 — item 은 LibraryBook 기반 최소 ItemOut, 부록 B card)
  reading: null,                             // { bookId, progressPct, virtualMinutes, qualified: false, completed: false } | null — D4
  ratings: {},                               // { [bookId]: stars }
  library: { added: [], reading: [], completed: [] },  // UserStateOut.library (LibraryBook[])
  libraryTab: "added",                       // D5 활성 탭 added|reading|completed
  userState: null,                           // UserStateOut 원문(D5 타임라인·U_t)
  mydata: null,                              // UserDataOut 원문(D5 "내 데이터 보기" 모달)
  dashboard: null,                           // DashboardOut 원문(D7)
  showcase: null,                            // ShowcaseOut 원문(D8)
  health: null,                              // HealthOut | null (상단바 model_version)
  nearlineLagSec: null,                      // RecommendOut.nearline_lag_s
  events: [],                                // 인스펙터 로그 [{ t: "HH:MM:SS", event_type, detail }] 최근 200
  toast: null,                               // string | null — 3초 후 null
  banner: null,                              // string | null — D2 상단 Banner 문구(예: "비개인화 인기 도서")
  modal: null,                               // null | "rating" | "mydata" | "withdraw"
};
```
원칙: `setState(patch)` 하나 · `render()` 하나 · 화면 파일은 `render(state) → HTML 문자열` 만, fetch·상태 변경 금지(`../.claude/rules/demo.md`).

## B. `data-act` 액션 이름 (화면 파일이 찍고 `app.js`/`actions.js` 가 위임 처리 — click 1곳 + change 1곳)
| data-act | 추가 data-* | 처리 | 이벤트(EVENT_TYPES 13종) |
|---|---|---|---|
| `start` | — | S0 → S1 | `preference_started` |
| `skip` | — | consent=false → `#/home`(level 3) | — |
| `back` | — | 단계 뒤로(S1 에서 resetting 이면 `#/home`) | — |
| `pick` | `data-step` `data-val` | S1~S4 선택 토글 | `preference_step`(payload {step}) |
| `pickbook` | `data-book` `data-pos` | S5 시드 토글 + impressions[].selected | `preference_book_selected` / `preference_book_deselected` |
| `next` | — | 다음 단계(S4→S5 진입 시 getCandidates + `impression`×30, S5 완료 시 postPreferences) | `preference_completed` |
| `toHome` | — | S6 CTA → `#/home` | — |
| `nav` | `data-to="#/home"` 등 해시 | `location.hash = to` | — |
| `preset` | `data-preset="newUser"\|"skipUser"\|"resetUser"` | presets.run(name) | (프리셋이 만드는 이벤트) |
| `card` | `data-book` `data-row` `data-pos` | detail 설정 → `#/book/:id`. 행 탐색은 `(state.recommend?.rows ?? [])` 로 null 방어(철회 직후 `recommend: null` 이어도 TypeError 0). `data-row="library"`(D5 서재 타일) 이면 `state.library` 의 added·reading·completed 에서 `LibraryBook{book_id,title,image_url}` 을 찾아 ItemOut 형태의 최소 detail(`book_id`·`title`·`image_url` 채움, `authors`·`reason`·`badge`·`source`·`book_format`·`difficulty` null, `source_channels` [], `score`·`position` 0) 을 만들고 `rowId: "library"` 로 둔다(d3 는 rowId 를 "내 서재" 로 표시). 못 찾으면 무동작 | `detail_click`(서재 타일은 `row_id`·`position` 없이 `surface: "library"`) |
| `closeSheet` | — | `#/home` | — |
| `read` | `data-book` | `#/reader/:id` 진입(reading 초기화) | `reader_open` |
| `library` | `data-book` | 서재 담기 | `library_add`(surface "main") |
| `read10` | — | progressPct +8, virtualMinutes +10; ≥15분 첫 도달 시 1회 | `qualified_read` |
| `complete` | — | completed=true, modal="rating" | `completion` |
| `rate` | `data-stars` 1..5 | postRating → toast "다음 책을 준비하고 있어요" → `#/home` | `rating` |
| `rateLater` | — | 토스트 → `#/home` | — |
| `libTab` | `data-tab` | libraryTab 변경 | — |
| `mydata` | — | getUserData → modal="mydata" | — |
| `withdraw` | — | modal="withdraw" | — |
| `withdrawConfirm` | — | deletePersonalization → consent=false → banner "비개인화 인기 도서" → `#/home` | — |
| `closeModal` | — | modal=null | — |
| `model` | (radio `value`) | state.model=value → getRecommend(forced) | — |
| `noop` | — | — | — |
- D6 시작(`#/refresh` 진입)은 라우터가 `preference_restarted` 를 기록한다. D2 행 렌더 직후 `impression`(row_id·position·recommendation_id) 은 `app.js` 가 recommendation_id 당 1회 기록한다.

## C. `api.js` 공개 함수 (06-03 소유) — 전부 `async`, 첫 인자 `source`; 실패는 엔드포인트별로 잡아 `null` 반환(uncaught 0). `getRecommend` 만 4초 타임아웃·네트워크 오류 시 `fallback/popular.json` + `client_fallback_reason`
```js
export const config;                                        // { source: "mock"|"api"|null, apiBase: string(기본 "" — ?api= 덮어쓰기), capture: "1"|"2"|null }
export async function resolveSource();                      // 명시 ?source 우선 · 없으면 GET /health 200 → "api", 아니면 "mock"
export async function loadSteps();                          // config/onboarding.json → steps[]
export async function health(source);                       // HealthOut | null           (mock: {status:"ok", api_version:"v2", model_version:"hybrid_div_v1", db_ok:true, uptime_s:0} 형태)
export async function loadMeta(source);                     // OnboardingMeta
export async function getCandidates(source, { categories, subcategories = [], n = 30, userKey });  // CandidateSet
export async function postPreferences(source, body);        // body = PreferencesRequest{user_key, consent, reading_time, categories, criterion(id), subcategories, seeds, candidate_set_id, restart} → PreferencesResponse
export async function getRecommend(source, { userKey, snapshotId, model, k = 40, context });  // RecommendOut (+client_fallback_reason)
export async function postEvents(source, events);           // EventIn[] ≤50 → EventsAccepted | null
export async function postRating(source, body);             // RatingIn{rating_id, user_key, book_id, stars, ts, recommendation_id} → RatingOut | null
export async function getUserState(source, userKey);        // UserStateOut | null
export async function getUserData(source, userKey);         // UserDataOut | null
export async function deletePersonalization(source, userKey); // PersonalizationDeleted | null
export async function getDashboard(source);                 // DashboardOut | null   (api 404 → null → 화면 "이 항목은 아직 없음")
export async function getShowcase(source);                  // ShowcaseOut | null
```
경로(api 모드): `/health` · `/api/meta/onboarding` · `/api/candidates/onboarding?categories=&n=&user_key=&subcategories=` · `POST /api/preferences`(201) · `/api/recommend?user_key=&snapshot_id=&model=&k=&context=` · `POST /api/events`(202, `{events:[…]}` 배치) · `POST /api/ratings` · `/api/users/{key}/state` · `/api/users/{key}/data` · `DELETE /api/users/{key}/personalization` · `/api/dashboard` · `/api/showcase`. 타임스탬프 필드는 **`ts`**.

## D. `mock.js` 공개 함수 (06-03 소유) — `api.js` 가 `source === "mock"` 일 때 같은 이름으로 위임(source 인자 없음)
```js
export async function init();            // mock/catalog_kr.json + mock/neighbors_kr.json + mock/showcase.json 1회 로드(Promise 캐시)
export function byId(bookId);            // 카드 dict | undefined
export function health(); loadMeta(); getCandidates({categories, subcategories, n, userKey}); postPreferences(body);
export function getRecommend({userKey, snapshotId, model, k, context}); postEvents(events); postRating(body);
export function getUserState(userKey); getUserData(userKey); deletePersonalization(userKey); getDashboard(); getShowcase();
```
반환 형태는 `serving/schemas.py`·`schemas_should.py` 모델과 키 집합 1:1(여분 키 금지). 세션 저장은 `mock_store.js`(sessionStorage `millie_mock_db`).

## E. 화면 파일 규약
- `demo/js/screens/d2_home.js` `d3_detail.js` `d4_reader.js` `d5_library.js` `d7_dashboard.js` `d8_showcase.js`: `export function render(state) → string`. import 는 `./ui.js` 만(`d3_detail.js` 는 배경용 `./d2_home.js` 추가 허용).
- `onboarding_step.js`: `render(state, step, meta)` · `s0_start.js`: `render(state)` · `s6_persona.js`: `render(state)`.
- `ui.js`(06-02 소유) 추가 export: `tabbar(active)`(`"home"|"library"` → `<nav class="tabbar">` 홈·서재 2탭, `data-act="nav" data-to="#/home|#/library"`) · `demoLabel(text = "데모 전용 · 실제 밀리 화면 아님")` → `<div class="demo-label">` · `banner(text)` → `<div class="banner">` · `modal(inner)` → `<div class="modal-wrap"><div class="modal">…</div></div>`. 기존 `esc cover badge josa icon statusbar navbar cta` 유지.
- 모든 사용자·카탈로그 문자열은 `esc()` 를 거쳐 삽입한다. `innerHTML` 에 원문 금지(XSS). `title ?? "(제목 없음)"` 방어.
- D7·D8 은 `#page` 컨테이너(전폭), 나머지는 `#phone`. 렌더 대상 결정은 `app.js` 만.

## F. 저장 키·상수
- `localStorage["millie_user_key"]` — UUIDv4(`crypto.randomUUID()`), 개인정보 아님.
- `sessionStorage["millie_events"]` — `app.js` 이벤트 송신 큐(EventIn[]; 50개 단위 `postEvents`).
- `sessionStorage["millie_mock_db"]` — `mock_store.js` 의 mock 서버 상태(users·snapshots·events·ratings·recommendations).
- 상수(값은 `contracts.py` 와 동일): `BUDGET_MS = 200` · `QUALIFIED_READ_MINUTES = 15` · `ROW_SIZE = 12` · `VARIANTS = ["pop","cf","hybrid","hybrid_div"]` · `BADGE_TYPES = ["bestseller","review","author","publisher","buzz","light"]` · EVENT_TYPES 13종(`impression detail_click library_add reader_open qualified_read completion rating preference_started preference_step preference_book_selected preference_book_deselected preference_completed preference_restarted`).
- `EventIn` 필드: `{event_id(uuid), user_key, book_id?, event_type, ts(ISO), surface?, row_id?, position?, selected?, recommendation_id?, model_version?, preference_snapshot_id?, candidate_set_id?, payload: {string: string}}` — 그 외 키 금지.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tokens.css §6 토큰 12 + --dim 추가 + dashboard.css(D7·D8 공용) 신규</name>
  <files>demo/css/tokens.css, demo/css/dashboard.css</files>
  <read_first>
    - demo/css/tokens.css (현행 43줄 — `:root` 블록 끝 `}` 앞에 추가)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §6 토큰 블록(그대로 복사) · "D7·D8: 제목 28px/700, KPI 32px monospace, 본문 15px, 카드 `--kpi-bg` 반경 16px" · §3 `KpiCard BarChart DataTable MetricsTable`(`--kpi-bg`, monospace 숫자)
    - demo/css/inspector.css (`.kv` grid `148px 1fr` · `.lat*` 막대 · `.log` monospace — 클래스 관례 참고만, 인스펙터 CSS 는 06-06 소유)
    - ../.claude/rules/demo.md ("디자인 값은 `css/tokens.css` 에만. 다른 CSS 파일에 색상 리터럴 금지")
  </read_first>
  <action>
**`demo/css/tokens.css`** — `:root { … }` 블록의 마지막 `--fs-author: 14px;` 뒤에 화면 02 §6 블록을 **그대로** 추가(주석 1줄 `/* 화면 02 §6 추가분 — 난이도·별점·배너·KPI·데모 라벨 + 모달 딤 */`):
```css
  --difficulty-on: var(--accent);  --difficulty-off: var(--line);
  --star: #F5E06E;  --star-off: #3A3A3C;
  --banner-bg: var(--accent-dim);  --banner-text: #FFF3B0;
  --kpi-bg: #1C1D20;  --kpi-good: #7ED37E;  --kpi-warn: #F5C46E;  --kpi-bad: #F57E7E;
  --light-badge: #9AD8FF;
  --demo-label: #6E6E73;
  --dim: rgba(0,0,0,.6);   /* 모달·시트 딤 — base.css .modal-wrap__dim 이 참조(06-02). 색 리터럴은 이 파일에만 */
```
기존 토큰 값은 바꾸지 않는다. `--dim` 은 §6 표 밖의 추가 1개(06-02 `base.css` 가 `rgba` 리터럴 대신 `var(--dim)` 을 쓰기 위함).

**`demo/css/dashboard.css` 신규(≤250줄, 색 리터럴 0 — 전부 `var()`)**, D7·D8 공용:
- 페이지 헤더: `.page-head { display:flex; align-items:baseline; justify-content:space-between; gap:16px; margin-bottom:20px }` `.page-head__meta { font-family:var(--mono); font-size:12px; color:var(--text-2) }` `.page-head__meta a { color:var(--accent) }` `.page .demo-label { margin:-32px -40px 20px }`(전폭 띠) `.page section { margin-top:28px }` `.page h2 { font-size:20px; font-weight:700; margin:0 0 12px }` `.page .caption { font-size:12px; color:var(--text-3); margin:8px 0 0 }` `.page .ref-label { display:inline-block; font-size:11px; color:var(--text-3); border:1px solid var(--line); border-radius:var(--r-pill); padding:1px 8px; margin-left:8px; vertical-align:middle }`
- 철학: `.philosophy { font-size:17px; line-height:1.6; max-width:960px; margin:0 0 8px; color:var(--text) }`
- KPI(화면 02 §6: 32px monospace, 카드 `--kpi-bg` 반경 16px): `.kpi { display:grid; grid-template-columns:repeat(6, 1fr); gap:14px }` `.kpi__card { background:var(--kpi-bg); border-radius:16px; padding:16px }` `.kpi__name { font-size:12px; color:var(--text-2) }` `.kpi__v { font-family:var(--mono); font-size:32px; font-weight:600; margin:6px 0 2px }` `.kpi__n { font-size:12px; color:var(--text-3) }` `.kpi__note { font-size:11px; color:var(--text-3); margin-top:6px }` `.kpi__card.is-good .kpi__v { color:var(--kpi-good) }` `.kpi__card.is-warn .kpi__v { color:var(--kpi-warn) }` `.kpi__card.is-bad .kpi__v { color:var(--kpi-bad) }`
- 표(DataTable·MetricsTable): `.dt { width:100%; border-collapse:collapse; font-size:14px }` `.dt th, .dt td { padding:8px 12px; border-bottom:1px solid var(--line); text-align:left }` `.dt th { font-size:12px; color:var(--text-2); font-weight:600 }` `.dt td.num { font-family:var(--mono); text-align:right }` `.dt tr.is-best td { color:var(--accent) }`
- 막대(BarChart — CSS 만): `.bar { display:grid; grid-template-columns:110px 1fr 70px; gap:8px; align-items:center; margin:6px 0; font-size:13px }` `.bar__track { position:relative; height:10px; border-radius:5px; background:var(--line) }` `.bar__fill { position:absolute; left:0; top:0; height:100%; border-radius:5px; background:var(--accent) }` `.bar__fill.is-p95 { opacity:.55 }` `.bar__v { font-family:var(--mono); text-align:right }` `.bar__budget { position:relative; height:16px; font-size:11px; color:var(--kpi-bad) }` `.bar__budget span { position:absolute; transform:translateX(-100%); border-right:2px solid var(--kpi-bad); padding-right:6px }`
- 단계↔지표 그림(metric_mapping): `.mapping { display:grid; grid-template-columns:repeat(4, 1fr); gap:28px; align-items:stretch; margin:12px 0 }` `.mapping__stage { background:var(--kpi-bg); border-radius:12px; padding:14px; text-align:center; position:relative }` `.mapping__stage + .mapping__stage::before { content:"→"; position:absolute; left:-14px; top:50%; transform:translate(-50%,-50%); color:var(--text-3) }` `.mapping__name { font-weight:700 }` `.mapping__metric { display:block; margin-top:8px; font-family:var(--mono); color:var(--accent) }` `.mapping__stage.is-compose .mapping__metric { color:var(--text-3) }`
- 본인 5권(case): `.case { display:grid; grid-template-columns:5fr 7fr; gap:24px }` `.case__col h3 { font-size:15px; color:var(--text-2); margin:0 0 8px }` `.case__grid { display:grid; grid-template-columns:repeat(5, 1fr); gap:10px }` `.case__book { font-size:12px }` `.case__book .cover { width:100% }` `.case__reason { font-size:11px; color:var(--text-2); margin-top:4px }` `.case__wait { color:var(--text-3); padding:24px; text-align:center; border:1px dashed var(--line); border-radius:12px }`
- 기억할 5가지·로드맵·프리셋: `.memo { display:grid; grid-template-columns:repeat(5, 1fr); gap:12px }` `.memo__card { background:var(--kpi-bg); border-radius:16px; padding:14px; font-size:13px }` `.memo__claim { font-weight:700; margin-bottom:6px }` `.memo__how { color:var(--text-2) }` `.memo__card a { color:var(--accent); font-size:12px }` `.roadmap { display:flex; flex-wrap:wrap; gap:8px }` `.roadmap__card { border:1px solid var(--line); color:var(--text-3); border-radius:var(--r-pill); padding:6px 12px; font-size:12px }` `.roadmap__tag { font-family:var(--mono); font-size:10px; margin-left:6px }` `.start { display:flex; gap:12px; align-items:center; flex-wrap:wrap }` `.start .cta { width:auto; padding:0 28px }` `.start__preset { height:40px; border-radius:var(--r-pill); border:1px solid var(--line); padding:0 16px; font-size:13px; color:var(--text-2) }` `.start__preset:hover { border-color:var(--accent); color:var(--accent) }`
- 이벤트 스트림·노출 로그: `.stream { font-family:var(--mono); font-size:12px; max-height:320px; overflow:auto; background:var(--kpi-bg); border-radius:12px; padding:10px 14px }` `.stream__row { display:grid; grid-template-columns:70px 170px 1fr; gap:10px; padding:3px 0; border-bottom:1px solid var(--panel-line) }` `.stream__row b { color:var(--accent); font-weight:500 }` `.empty { color:var(--text-3); padding:24px; text-align:center; border:1px dashed var(--line); border-radius:12px }`
- 두 열 레이아웃: `.two { display:grid; grid-template-columns:1fr 1fr; gap:24px }`
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && grep -c -- '--difficulty-on: var(--accent);\|--star: #F5E06E;\|--banner-text: #FFF3B0;\|--kpi-bg: #1C1D20;\|--kpi-good: #7ED37E;\|--kpi-warn: #F5C46E;\|--kpi-bad: #F57E7E;\|--light-badge: #9AD8FF;\|--demo-label: #6E6E73;\|--dim: rgba(0,0,0,.6);' css/tokens.css && wc -l css/tokens.css css/dashboard.css && grep -nE '#[0-9a-fA-F]{3,8}\b' css/dashboard.css | wc -l && grep -c '\.kpi__v\|\.dt {\|\.bar__budget\|\.mapping__stage\|\.case__grid\|\.memo__card\|\.roadmap__card\|\.start__preset\|\.stream__row' css/dashboard.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--star: #F5E06E;' demo/css/tokens.css` == 1 · `--star-off: #3A3A3C;` 1 · `--banner-text: #FFF3B0;` 1 · `--kpi-bg: #1C1D20;` 1 · `--kpi-good: #7ED37E;` 1 · `--kpi-warn: #F5C46E;` 1 · `--kpi-bad: #F57E7E;` 1 · `--light-badge: #9AD8FF;` 1 · `--demo-label: #6E6E73;` 1 · `--difficulty-on: var(--accent);` 1 · `--difficulty-off: var(--line);` 1 · `--banner-bg: var(--accent-dim);` 1 · `--dim: rgba(0,0,0,.6);` 1 (13개)
    - `grep -c -- '--accent: #F5E06E;' demo/css/tokens.css` == 1 (기존 값 불변) · `wc -l demo/css/tokens.css` ≤ 60
    - `wc -l demo/css/dashboard.css` ≤ 250 · `grep -nE '#[0-9a-fA-F]{3,8}\b' demo/css/dashboard.css | wc -l` == 0
    - `grep -c '\.kpi__v\|\.dt {\|\.bar__budget\|\.mapping__stage\|\.case__grid\|\.memo__card\|\.roadmap__card\|\.start__preset\|\.stream__row' demo/css/dashboard.css` ≥ 9
    - `grep -c 'font-size:32px' demo/css/dashboard.css` == 1 · `grep -c 'border-radius:16px' demo/css/dashboard.css` ≥ 2
  </acceptance_criteria>
  <done>토큰 13개(§6 12 + `--dim`) 추가, D7·D8 공용 CSS 존재, 색 리터럴은 tokens.css 에만.</done>
</task>

<task type="auto">
  <name>Task 2: d8_showcase.js — 철학·비교표·단계↔지표 그림·본인 5권·기억할 5가지·시작+프리셋·로드맵·데이터 고지</name>
  <files>demo/js/screens/d8_showcase.js</files>
  <read_first>
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D8 표(헤더 "밀리의서재 메인 추천 시스템 — 설계 데모" + 철학 2줄 · 비교표 행 variant 4·열 Recall@20/NDCG@10/ILD@10 + p95 · 캡션 `split_mode: temporal (train.ts.max < test.ts.min 단언 통과)` 또는 `holdout` · 위에 "후보생성→Recall · 랭킹→NDCG · 재순위화→ILD" 1:1 그림 · 카드 5 · 본인 5권 → 추천 10 + 캡션 "데모 이웃 = 콘텐츠 유사도" · 시작 버튼 "신규 유저로 체험하기" + 프리셋 3(신규 유저 A / 건너뛰기 유저 / 재설정 유저) · 로드맵 회색 `production roadmap` · 데이터 고지)
    - src/millie_rec/serving/schemas_should.py `ShowcaseOut{philosophy, eval_table{split_mode, source{metrics,p95}, rows[EvalRow{variant, recall_at_20, ndcg_at_10, ild_at_10, p95_ms?}]}, metric_mapping[{stage,metric}], personal_case{seeds[ShowcaseBook], recommendations[ShowcaseBook{book_id,title,image_url,reason,badge}]}?, memorable_5[{claim,how_to_verify,route}], roadmap[], data_notice}`
    - demo/mock/showcase.json (06-01 산출 실물 — 값 확인용, 하드코딩 금지)
    - demo/js/inspector.js L6 `MODELS = [["pop","Pop"],["cf","CF"],["hybrid","Hybrid"],["hybrid_div","+Diversity"]]`(표 행 라벨 재사용 — variant 문자열은 contracts.VARIANTS 와 동일)
    - demo/js/screens/ui.js (`esc cover badge demoLabel`)
  </read_first>
  <action>
`demo/js/screens/d8_showcase.js` 신규(≤200줄). `import { esc, cover, badge, demoLabel } from "./ui.js";`
```js
const MODELS = { pop: "Pop", cf: "CF", hybrid: "Hybrid", hybrid_div: "+Diversity" };   // contracts.VARIANTS 와 동일 키
const STAGES_COMPOSE = "Page Composition";   // metric_mapping 3행 뒤에 지표 없는 4번째 칸(설계 §5-1 [4]) — 텍스트 "지표 없음 · 행 구성"
const num = (v, d = 3) => (v == null ? "—" : Number(v).toFixed(d));
function header(s, state) {
  return `<div class="page-head"><div><h1>밀리의서재 메인 추천 시스템 — 설계 데모</h1>
    <p class="philosophy">${esc(s.philosophy)}</p></div>
    <div class="page-head__meta">model_version ${esc(state.health?.model_version ?? "—")} · source ${esc(state.source)} · <a href="/docs" target="_blank" rel="noreferrer">/docs</a></div></div>`;
}
function mapping(s) {   // 단계↔지표 1:1 대응 그림(CSS) — main 설계서 §6
  const cells = s.metric_mapping.map((m) => `<div class="mapping__stage"><div class="mapping__name">${esc(m.stage)}</div><span class="mapping__metric">${esc(m.metric)}</span></div>`).join("");
  return `<div class="mapping">${cells}<div class="mapping__stage is-compose"><div class="mapping__name">${STAGES_COMPOSE}</div><span class="mapping__metric">지표 없음 · 행 구성</span></div></div>`;
}
function metricsTable(s) {
  const t = s.eval_table; const best = (k) => Math.max(...t.rows.map((r) => r[k] ?? -1));
  const rows = t.rows.map((r) => `<tr${r.recall_at_20 === best("recall_at_20") ? ' class="is-best"' : ""}><td>${esc(MODELS[r.variant] ?? r.variant)} <small class="mono">${esc(r.variant)}</small></td>
      <td class="num">${num(r.recall_at_20)}</td><td class="num">${num(r.ndcg_at_10)}</td><td class="num">${num(r.ild_at_10)}</td><td class="num">${r.p95_ms == null ? "—" : num(r.p95_ms, 1)}</td></tr>`).join("");
  return `<section><h2>평가 3지표 비교표 <span class="ref-label">Track A</span></h2>${mapping(s)}
    <table class="dt"><thead><tr><th>variant</th><th>Recall@20</th><th>NDCG@10</th><th>ILD@10</th><th>p95 ms</th></tr></thead><tbody>${rows}</tbody></table>
    <p class="caption">split_mode: ${esc(t.split_mode)}${t.split_mode === "temporal" ? " (train.ts.max < test.ts.min 단언 통과)" : ""} · 출처 ${esc(t.source.metrics)} · p95 출처 ${esc(t.source.p95)}(로컬 bench${t.rows.every((r) => r.p95_ms == null) ? " — 아직 없음" : ""})</p></section>`;
}
function personalCase(s) {
  const pc = s.personal_case;
  if (!pc) return `<section><h2>본인 5권 정성 케이스</h2><div class="case__wait">5권 선정 대기</div></section>`;
  const book = (b) => `<div class="case__book">${cover(b)}<div>${esc(b.title ?? "(제목 없음)")}</div>${b.reason ? `<div class="case__reason">${esc(b.reason)}</div>` : ""}${badge(b.badge)}</div>`;
  return `<section><h2>본인 5권 정성 케이스 <span class="ref-label">Track B · 밀리 카탈로그</span></h2>
    <div class="case"><div class="case__col"><h3>내가 읽은 5권</h3><div class="case__grid">${pc.seeds.map(book).join("")}</div></div>
    <div class="case__col"><h3>추천 상위 ${pc.recommendations.length}</h3><div class="case__grid">${pc.recommendations.map(book).join("")}</div></div></div>
    <p class="caption">데모 이웃 = 콘텐츠 유사도(협업 필터링 아님)</p></section>`;
}
function memorable(s) { if (!s.memorable_5?.length) return "";
  return `<section><h2>심사자가 기억할 5가지</h2><div class="memo">${s.memorable_5.map((m) => `<div class="memo__card"><div class="memo__claim">${esc(m.claim)}</div><div class="memo__how">${esc(m.how_to_verify)}</div><a href="${esc(m.route)}">${esc(m.route)} →</a></div>`).join("")}</div></section>`; }
function start() {
  return `<section><h2>직접 눌러보기</h2><div class="start">
    <button class="cta is-on" data-act="nav" data-to="#/onboarding">신규 유저로 체험하기</button>
    <button class="start__preset" data-act="preset" data-preset="newUser">신규 유저 A</button>
    <button class="start__preset" data-act="preset" data-preset="skipUser">건너뛰기 유저</button>
    <button class="start__preset" data-act="preset" data-preset="resetUser">재설정 유저</button>
    <button class="start__preset" data-act="nav" data-to="#/dashboard">관제 대시보드</button></div>
    <p class="caption">동선: 취향 설정 → 메인 → 상세 → 뷰어(완독·별점) → 메인 "다음은" 행 → 서재(재설정·철회) → 대시보드</p></section>`;
}
function roadmap(s) { if (!s.roadmap?.length) return "";
  return `<section><h2>설계만 — production roadmap</h2><div class="roadmap">${s.roadmap.map((r) => `<span class="roadmap__card">${esc(r)}<span class="roadmap__tag">roadmap</span></span>`).join("")}</div></section>`; }
export function render(state) {
  const s = state.showcase;
  const head = demoLabel();
  if (!s) return `${head}<div class="page-head"><h1>밀리의서재 메인 추천 시스템 — 설계 데모</h1></div><div class="empty">쇼케이스 데이터를 불러오는 중… (api 모드에서 GET /api/showcase 가 아직 없으면 이 항목은 아직 없음)</div>${start()}`;
  return `${head}${header(s, state)}${metricsTable(s)}${start()}${personalCase(s)}${memorable(s)}${roadmap(s)}
    <section><h2>데이터 고지</h2><p class="caption" style="font-size:13px">${esc(s.data_notice)}</p></section>`;
}
```
- 숫자·문장은 전부 `state.showcase` 에서 — 파일 안에 지표 값·철학 문장·고지 문장 하드코딩 금지(캡션 접속사·라벨만 허용).
- `null` 상태(api 모드 404)도 시작 버튼은 항상 그린다(심사자 동선 유지).
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/screens/d8_showcase.js && wc -l js/screens/d8_showcase.js && grep -c 'data-preset="newUser"\|data-preset="skipUser"\|data-preset="resetUser"\|data-to="#/onboarding"\|split_mode: \|데모 이웃 = 콘텐츠 유사도\|5권 선정 대기\|s.data_notice\|s.philosophy\|metric_mapping' js/screens/d8_showcase.js && grep -nE 'innerHTML|fetch\(|0\.0[0-9]{2}|사용자가 취향 설정에서|Goodbooks-10k' js/screens/d8_showcase.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/screens/d8_showcase.js` 종료 0 · `wc -l demo/js/screens/d8_showcase.js` ≤ 200 · import 는 `./ui.js` 1줄만
    - `grep -c 'data-preset="newUser"' …` == 1 · `data-preset="skipUser"` == 1 · `data-preset="resetUser"` == 1 · `data-to="#/onboarding"` == 1 · `신규 유저로 체험하기` == 1
    - `grep -c 'split_mode: ' …` == 1 · `grep -c 'train.ts.max < test.ts.min 단언 통과' …` == 1 · `grep -c '데모 이웃 = 콘텐츠 유사도' …` == 1 · `grep -c '5권 선정 대기' …` == 1 · `grep -c 's.data_notice\|s.philosophy\|s.metric_mapping\|s.memorable_5\|s.roadmap' …` ≥ 5
    - 비교표 열 4: `grep -c '<th>Recall@20</th><th>NDCG@10</th><th>ILD@10</th><th>p95 ms</th>' …` == 1 · `grep -c 'hybrid_div: "+Diversity"' …` == 1
    - 하드코딩 금지: `grep -nE 'innerHTML|fetch\(|0\.0[0-9]{2}|사용자가 취향 설정에서|Goodbooks-10k' demo/js/screens/d8_showcase.js | wc -l` == 0
    - `grep -c 'demoLabel()' …` == 1 · `grep -c '(제목 없음)' …` == 1
  </acceptance_criteria>
  <done>쇼케이스가 ShowcaseOut 만으로 화면 02 §2 D8 표의 Must·Should·Could 요소를 그린다.</done>
</task>

<task type="auto">
  <name>Task 3: d7_dashboard.js — DashboardOut 한 형태 렌더(KPI 6·A/B 표·latency 막대+예산선·품질·스트림·노출 로그·by_variant) + null 상태</name>
  <files>demo/js/screens/d7_dashboard.js</files>
  <read_first>
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D7 표(KPI 카드 6 — QRS rate · 첫 완독 도달률(신규) · fallback % · p95 latency("서버 실측 · 참고용") · 오류율 · 활성 user_key, 각 카드 n 병기 · A/B 표 셀 A/B × 신규/기존 Primary·Secondary·Guardrail + 하단 고지 · latency 패널 단계별 p50/p95 막대 + 예산선, 헤더 "서버 실측(참고용) · PDF 수치는 로컬 bench" · 품질 패널 impression 수신율·quality_flag 건수·feature freshness · 이벤트 스트림 최근 50(recommendation_id·model_version·snapshot_id 강조) · 노출 로그 candidate_set_id/position/selected + "미선택 ≠ negative" · `survey_variant=v1` · 모델별 요청 분포 · 링크) · "차트는 CSS 막대"
    - src/millie_rec/serving/schemas_should.py `DashboardOut{generated_at, window, kpi: {name: KpiValue{value,n,note}}, ab_table[AbRow{cell,segment,n,primary,reader_open,completion,rating_mean,first_completion,p95_ms,fallback_rate}], mde_note, latency{p50,p95,p99,by_stage{stage:[p50,p95]}}, quality{impression_receipt_rate,flagged_events,feature_freshness_s}, events_recent[], impressions_log[], by_variant{}, by_hour[]}`
    - .planning/phases/05-must/05-CONTEXT.md D-13 kpi 이름 6개(`qualified_reading_start_rate` `first_completion_rate_new` `fallback_rate` `p95_latency_ms` `error_rate` `active_user_keys`)
    - src/millie_rec/contracts.py L86 `BUDGET_MS = 200`
    - demo/js/inspector.js L65-82 `latencySection`(막대 계산·예산선 마크업 골격)
    - demo/mock/dashboard.json (빈 집계 형태 — 값 하드코딩 금지)
  </read_first>
  <action>
`demo/js/screens/d7_dashboard.js` 신규(≤200줄). `import { esc, demoLabel } from "./ui.js"; const BUDGET_MS = 200;  // contracts.BUDGET_MS`
```js
const KPI = [   // 이름 6개는 05-CONTEXT D-13 · 표시 라벨·형식만 여기
  ["qualified_reading_start_rate", "Qualified Reading Start rate", "pct"], ["first_completion_rate_new", "첫 완독 도달률 (신규)", "pct"],
  ["fallback_rate", "fallback %", "pct"], ["p95_latency_ms", "p95 latency", "ms"], ["error_rate", "오류율", "pct"], ["active_user_keys", "활성 user_key", "int"],
];
const fmt = (v, kind) => (v == null ? "—" : kind === "pct" ? `${(v * 100).toFixed(1)}%` : kind === "ms" ? `${Number(v).toFixed(1)}ms` : String(Math.round(v)));
const tone = (name, v) => (name === "p95_latency_ms" ? (v > BUDGET_MS ? "is-bad" : "is-good") : name === "fallback_rate" || name === "error_rate" ? (v > 0.1 ? "is-warn" : "is-good") : "");
function kpis(d) {
  return `<div class="kpi">${KPI.map(([name, label, kind]) => { const k = d.kpi[name] ?? {}; return `<div class="kpi__card ${tone(name, k.value)}"><div class="kpi__name">${esc(label)}${name === "p95_latency_ms" ? '<span class="ref-label">서버 실측 · 참고용</span>' : ""}</div>
      <div class="kpi__v">${fmt(k.value, kind)}</div><div class="kpi__n">n = ${k.n ?? "—"}</div>${k.note ? `<div class="kpi__note">${esc(k.note)}</div>` : ""}</div>`; }).join("")}</div>`;
}
function abTable(d) {
  const pct = (v) => (v == null ? "—" : `${(v * 100).toFixed(1)}%`);
  const rows = (d.ab_table ?? []).map((r) => `<tr><td>${esc(r.cell)}</td><td>${esc(r.segment)}</td><td class="num">${r.n}</td><td class="num">${pct(r.primary)}</td><td class="num">${pct(r.reader_open)}</td><td class="num">${pct(r.completion)}</td><td class="num">${r.rating_mean == null ? "—" : Number(r.rating_mean).toFixed(2)}</td><td class="num">${pct(r.first_completion)}</td><td class="num">${r.p95_ms == null ? "—" : Number(r.p95_ms).toFixed(1)}</td><td class="num">${pct(r.fallback_rate)}</td></tr>`).join("");
  return `<section><h2>A/B 표 <span class="ref-label">Primary · Secondary · Guardrail</span></h2><table class="dt"><thead><tr><th>cell</th><th>segment</th><th>n</th><th>QRS(primary)</th><th>reader_open</th><th>완독</th><th>별점 평균</th><th>첫 완독</th><th>p95 ms</th><th>fallback</th></tr></thead><tbody>${rows || '<tr><td colspan="10" class="empty">아직 표본이 없습니다</td></tr>'}</tbody></table>
    <p class="caption">${esc(d.mde_note ?? "")}</p></section>`;
}
function latency(d) {
  const L = d.latency; const stages = Object.entries(L.by_stage ?? {}); const max = Math.max(BUDGET_MS, L.p95 ?? 0, ...stages.map(([, v]) => v[1] ?? 0));
  const bar = (name, p50, p95) => `<div class="bar"><span>${esc(name)}</span><span class="bar__track"><span class="bar__fill is-p95" style="width:${Math.min(100, (p95 / max) * 100)}%"></span><span class="bar__fill" style="width:${Math.min(100, (p50 / max) * 100)}%"></span></span><span class="bar__v">${Number(p50).toFixed(1)} / ${Number(p95).toFixed(1)}</span></div>`;
  return `<section><h2>latency <span class="ref-label">서버 실측(참고용) · PDF 수치는 로컬 bench</span></h2>
    ${bar("total", L.p50, L.p95)}${stages.map(([k, v]) => bar(k, v[0], v[1])).join("")}
    <div class="bar__budget"><span style="left:${Math.min(100, (BUDGET_MS / max) * 100)}%">BUDGET ${BUDGET_MS}ms</span></div>
    <p class="caption">막대 = p50 / 옅은 막대 = p95 · p99 ${Number(L.p99).toFixed(1)}ms</p></section>`;
}
function quality(d) { const q = d.quality ?? {};
  return `<section><h2>데이터 품질</h2><table class="dt"><tbody>
    <tr><td>impression 수신율 (응답 item 수 대비)</td><td class="num">${q.impression_receipt_rate == null ? "—" : `${(q.impression_receipt_rate * 100).toFixed(1)}%`}</td></tr>
    <tr><td>quality_flag 건수</td><td class="num">${q.flagged_events ?? "—"}</td></tr>
    <tr><td>feature freshness</td><td class="num">${q.feature_freshness_s == null ? "—" : `${q.feature_freshness_s}s`}</td></tr></tbody></table></section>`; }
function streams(d) {
  const ev = (d.events_recent ?? []).map((e) => `<div class="stream__row"><span>${esc(String(e.ts ?? "").slice(11, 19))}</span><span><b>${esc(e.event_type)}</b> ${e.book_id ?? ""}</span><span>${esc(e.recommendation_id ?? "")} ${esc(e.model_version ?? "")} ${esc(e.preference_snapshot_id ?? "")}</span></div>`).join("");
  const imp = (d.impressions_log ?? []).map((i) => `<div class="stream__row"><span>${esc(String(i.ts ?? "").slice(11, 19))}</span><span>${esc(i.candidate_set_id ?? "")}</span><span>book ${i.book_id} · pos ${i.position} · selected ${i.selected}</span></div>`).join("");
  const byv = Object.entries(d.by_variant ?? {}).map(([k, v]) => `<div class="bar"><span>${esc(k)}</span><span class="bar__track"><span class="bar__fill" style="width:${Math.min(100, (v / Math.max(1, ...Object.values(d.by_variant))) * 100)}%"></span></span><span class="bar__v">${v}</span></div>`).join("");
  return `<div class="two"><section><h2>이벤트 스트림 <span class="ref-label">최근 ${(d.events_recent ?? []).length}</span></h2><div class="stream">${ev || '<div class="empty">아직 이벤트가 없습니다</div>'}</div></section>
    <section><h2>노출 로그 <span class="ref-label">survey_variant=v1</span></h2><div class="stream">${imp || '<div class="empty">아직 노출 로그가 없습니다</div>'}</div><p class="caption">candidate_set_id / position / selected — 미선택 ≠ negative</p></section></div>
    <section><h2>모델별 요청 분포</h2>${byv || '<div class="empty">아직 요청이 없습니다</div>'}</section>`;
}
export function render(state) {
  const d = state.dashboard;
  const head = `${demoLabel()}<div class="page-head"><div><h1>관제 대시보드</h1><p class="caption">generated_at ${esc(d?.generated_at ?? "—")} · window ${esc(d?.window ?? "—")} · source ${esc(state.source)}</p></div>
    <div class="page-head__meta"><a href="#/">← 쇼케이스</a> · <a href="#/home">메인</a> · <a href="/docs" target="_blank" rel="noreferrer">/docs</a></div></div>`;
  if (!d) return `${head}<div class="empty">이 항목은 아직 없음 — 서버 준비 중(GET /api/dashboard). mock 모드에서는 세션 이벤트가 집계됩니다.</div>`;
  return `${head}${kpis(d)}${abTable(d)}${latency(d)}${quality(d)}${streams(d)}`;
}
```
- `by_hour` 는 Could — 그리지 않는다(화면 02 §9). 숫자 하드코딩 0(`BUDGET_MS=200` 상수만).
- 모든 문자열 `esc()`. `null`·빈 배열 전부 방어.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/screens/d7_dashboard.js && wc -l js/screens/d7_dashboard.js && grep -c 'qualified_reading_start_rate\|first_completion_rate_new\|"fallback_rate"\|p95_latency_ms\|"error_rate"\|active_user_keys' js/screens/d7_dashboard.js && grep -c 'BUDGET_MS = 200\|서버 실측 · 참고용\|PDF 수치는 로컬 bench\|미선택 ≠ negative\|survey_variant=v1\|d.mde_note\|이 항목은 아직 없음' js/screens/d7_dashboard.js && grep -nE 'innerHTML|fetch\(|value: [0-9]' js/screens/d7_dashboard.js | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/screens/d7_dashboard.js` 종료 0 · `wc -l demo/js/screens/d7_dashboard.js` ≤ 200 · import 는 `./ui.js` 1줄만
    - kpi 이름 6개: `grep -c '"qualified_reading_start_rate"\|"first_completion_rate_new"\|"fallback_rate"\|"p95_latency_ms"\|"error_rate"\|"active_user_keys"' demo/js/screens/d7_dashboard.js` == 6
    - `grep -c 'BUDGET_MS = 200' …` == 1 · `grep -c '서버 실측 · 참고용' …` == 1 · `grep -c 'PDF 수치는 로컬 bench' …` == 1 · `grep -c '미선택 ≠ negative' …` == 1 · `grep -c 'survey_variant=v1' …` == 1 · `grep -c 'd.mde_note' …` == 1 · `grep -c '이 항목은 아직 없음' …` == 1
    - A/B 표 열 10: `grep -c '<th>cell</th><th>segment</th><th>n</th>' …` == 1 · `grep -c 'n = ${k.n' …` == 1 (n 병기)
    - `grep -nE 'innerHTML|fetch\(|value: [0-9]' demo/js/screens/d7_dashboard.js | wc -l` == 0 · `grep -c 'by_hour' …` == 0
    - 이 플랜 소유 4파일에서 06-07 구 형태 정규식 0건: `cd demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' js/screens/d7_dashboard.js js/screens/d8_showcase.js css/dashboard.css css/tokens.css | wc -l` == 0
  </acceptance_criteria>
  <done>대시보드가 DashboardOut 한 형태(api 서버·mock 세션 집계 공용)를 그리고 null 상태를 방어한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `state.showcase`·`state.dashboard`(서버/mock 응답) → DOM | 제목·reason·이벤트 문자열 삽입 — XSS 표면 |
| `memorable_5[].route` → `<a href>` | 응답 문자열이 href 로 — `esc()` + 해시 라우트만 기대 |
| 외부 링크(`/docs`) | `target=_blank rel=noreferrer` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | 응답 문자열(title·reason·event_type·note) DOM 삽입(XSS) | mitigate | 전부 `esc()` · `innerHTML` 0 · 화면 파일은 문자열 반환만 |
| T-06-05-02 | Tampering | `memorable_5[].route` 에 `javascript:` URL | mitigate | `esc()` 로 인용부호 이스케이프 + 서버/mock 은 `#/…` 만 생성(06-01 MEMORABLE_5 상수); 06-07 게이트가 `href="#/` 만 있는지 grep |
| T-06-05-03 | Repudiation | 대시보드·비교표 숫자를 PDF 숫자로 오인 | mitigate | D7 라벨 "서버 실측 · 참고용"·"PDF 수치는 로컬 bench" · D8 출처 캡션(`results/latest.csv`·`results/latency.json`) · 숫자 하드코딩 0(응답에서만) |
| T-06-05-04 | Information Disclosure | 이벤트 스트림에 user_key 노출 | accept | 가명 UUID(개인정보 아님, 백엔드 01 §0) · 스트림은 event_type·book_id·rec_id 만 표시(user_key 컬럼 없음) |
| T-06-05-05 | Denial of Service | `state.dashboard === null`(api 404)·빈 배열로 렌더 크래시 | mitigate | null 분기 "이 항목은 아직 없음" · 전 필드 `?.`/`?? []` |
| T-06-05-06 | Spoofing | 외부 링크 새 창의 `window.opener` | mitigate | `rel="noreferrer"` |
</threat_model>

<verification>
- `node --check` 2 JS 종료 0 · `wc -l` 상한(d7 200 · d8 200 · dashboard.css 250 · tokens.css 60)
- tokens.css §6 12개 + `--dim` 문자 단위 존재(13개) · dashboard.css 색 리터럴 0
- D8: 프리셋 3·시작 버튼·split_mode 캡션·2트랙 고지·단계↔지표 그림·5권/대기 분기 존재 · 숫자·문장 하드코딩 0
- D7: kpi 6 이름·A/B 10열·예산선 200·라벨 3종·null 상태 존재
- 구 형태 0(소유 4파일)
</verification>

<success_criteria>
- DEMO-06 기계 판정: 비교표(`split_mode` 캡션)·단계↔지표 그림·철학 문장(`s.philosophy`)·2트랙 고지(`s.data_notice`) 마크업 존재
- DEMO-08: `DashboardOut` 렌더러 존재(api·mock 공용 한 형태)
- 화면 02 §2 D7·D8 표의 Must·Should 요소 전부 존재, Could(`by_hour`·문장 카드 3) 는 생략
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-rebuild/06-05-SUMMARY.md` — 변경 파일 목록(커밋 0) · `wc -l` · 토큰 13 확인(§6 12 + `--dim`) · 화면 02 §2 D7/D8 표 ↔ 마크업 대응표 · 06-06 체크리스트에 넘길 항목(프리셋 3 버튼 동작은 06-02 presets.js 의존).
</output>
