---
phase: 06-demo-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - demo/js/screens/d4_reader.js
  - demo/js/screens/d5_library.js
  - demo/css/reader.css
  - demo/css/library.css
autonomous: true
requirements: [DEMO-07, DEMO-03]
lane: Demo (뷰어·서재 화면 Worker — wave 2 병렬 4 중 3)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(05 관례, 사용자 지시 2026-09-05)
must_not_touch:
  - src/**
  - tests/**
  - Makefile
  - pyproject.toml
  - Dockerfile
  - artifacts/**
  - results/**
  - report/**
  - demo/js/app.js            # 06-02
  - demo/js/actions.js        # 06-02
  - demo/js/presets.js        # 06-02
  - demo/js/router.js         # 06-02
  - demo/js/api.js            # 06-03
  - demo/js/mock.js           # 06-03
  - demo/js/mock_store.js     # 06-03
  - demo/js/inspector.js      # 06-06
  - demo/js/screens/ui.js     # 06-02
  - demo/js/screens/onboarding_step.js
  - demo/js/screens/s0_start.js
  - demo/js/screens/s6_persona.js
  - demo/js/screens/s7_home.js
  - demo/js/screens/s8_detail.js
  - demo/js/screens/d2_home.js   # 06-06
  - demo/js/screens/d3_detail.js # 06-06
  - demo/js/screens/d7_dashboard.js # 06-05
  - demo/js/screens/d8_showcase.js  # 06-05
  - demo/css/tokens.css       # 06-05
  - demo/css/base.css         # 06-02
  - demo/css/home.css         # 06-06
  - demo/css/inspector.css    # 06-06
  - demo/css/onboarding.css
  - demo/css/dashboard.css    # 06-05
  - demo/index.html           # 06-02
  - demo/config/onboarding.json
  - demo/README.md
  - demo/mock/**
  - demo/fallback/popular.json
  - demo/scripts/make_mock.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - ../PROGRESS.md
  - ../.assets/**

must_haves:
  truths:
    - "뷰어 시뮬레이션 화면(#/reader/:id, 화면 구성 02 §2)은 본문 없이 DemoLabel '데모 전용 · 실제 밀리 화면 아님' · 제목 · 진행률 바 + '진행률 n%' · 회색 placeholder + 안내 문구 · 버튼 '10분 읽기'(data-act read10)·'완독'(complete)·뒤로 를 그린다(DEMO-07 Must 부분)"
    - "가상 시간 표기 'T=15분 도달 시 qualified_read' 와 현재 virtualMinutes/qualified 상태가 화면에 보이고, 페이지 번호는 없다(가짜 페이지 번호 금지, ../.claude/rules/demo.md)"
    - "state.modal === 'rating' 이면 RatingModal('어떠셨나요?' · ★×5 44px · data-act rate data-stars 1..5 · '나중에' rateLater)이 뷰어 위에 그려진다(DEMO-07 Should 별점)"
    - "내 서재 화면(#/library, 화면 구성 02 §2)은 탭 3(담은 책·읽는 중·완독, data-act libTab) · 스냅샷 Timeline(snapshots[] active 강조 · '재설정해도 독서 기록은 유지됩니다') · U_t 카드(α/β/γ 막대 + '완독이 쌓일수록 행동(β) 비중이 커집니다') · 버튼 3(취향 다시 설정 → nav #/refresh · 내 데이터 보기 mydata · 맞춤 추천 동의 철회 withdraw) 을 그린다(DEMO-03 8페이지 중 D4·D5)"
    - "두 화면 파일은 render(state) → 문자열만 반환하고 fetch·상태 변경·innerHTML 을 쓰지 않으며 모든 문자열은 esc() 를 거친다; title ?? '(제목 없음)' 방어"
    - "reader.css·library.css 에 색 리터럴이 없다(토큰만 — --star --star-off --difficulty-on 등은 06-05 tokens.css 가 정의)"
  artifacts:
    - path: "demo/js/screens/d4_reader.js"
      provides: "render(state) — DemoLabel · 진행률 · placeholder · 버튼 3 · 가상 시간/T=15 표기 · RatingModal(state.modal === 'rating')"
      exports: ["render"]
      max_lines: 120
    - path: "demo/js/screens/d5_library.js"
      provides: "render(state) — 탭 3 + LibraryBook 타일 · Timeline · U_t 카드 · 버튼 3 · TabBar(library 활성)"
      exports: ["render"]
      max_lines: 150
    - path: "demo/css/reader.css"
      provides: ".reader* (본문 placeholder·진행률·조작 버튼) · .rating* (모달·별 44px)"
      max_lines: 120
    - path: "demo/css/library.css"
      provides: ".lib* (탭·그리드) · .timeline* · .ut* (가중치 막대) · .lib__actions"
      max_lines: 150
  key_links:
    - from: "demo/js/screens/d4_reader.js"
      to: "state.reading"
      via: "{ bookId, progressPct, virtualMinutes, qualified, completed } → 진행률·표기"
      pattern: "state\\.reading"
    - from: "demo/js/screens/d4_reader.js"
      to: "demo/js/actions.js(read10·complete·rate·rateLater)"
      via: "data-act 속성만 — 처리는 06-02"
      pattern: "data-act=\"read10\""
    - from: "demo/js/screens/d5_library.js"
      to: "state.library / state.userState"
      via: "UserStateOut.library{added,reading,completed} · snapshots[] · user_state_weights"
      pattern: "state\\.userState"
---
<objective>
Phase 6 '데모 재구성'(.planning/ROADMAP.md "Phase 6: 데모 재구성") 의 **밀리 앱에 없는 화면 D4 + 개인정보 화면 D5**를 만든다: `js/screens/d4_reader.js` + `css/reader.css`(뷰어 시뮬레이션 — 독서 행동 이벤트를 만들어내는 장치 · 별점 모달) · `js/screens/d5_library.js` + `css/library.css`(내 서재 — 3버킷 · 스냅샷 타임라인 · U_t 카드 · 열람/재설정/철회 버튼). 화면 파일은 `render(state) → HTML` 만, 이벤트는 `data-act` 로 06-02 `actions.js` 에 위임(부록 B).

Purpose: ROADMAP Should 꼬리 **DEMO-07** — "뷰어 시뮬레이션의 '10분 읽기'·'완독' 이 `reader_open`·`qualified_read`(15분)·`completion` 이벤트를 만들고 1탭 별점 모달이 `POST /api/ratings` 를 호출"(이벤트·POST 는 06-02 actions 가 하고, 이 플랜은 그 버튼·모달을 그린다) + **DEMO-03** 8페이지 중 D4·D5. PDF 축① QRS(Qualified Reading Start) 를 심사자가 직접 만들어 보는 장치(main 설계서 §4) · 축④ 실서비스 "개인정보 — 열람·삭제·철회권"(main §7, 화면 02 §2 D5) · ★시간 가변 가중치의 U_t 카드(main §5-2). 캡처 계획(화면 02 §7) "뷰어 완독 → 별점 → '다음은' 행(3컷)" · "서재 — 스냅샷 타임라인·철회".
Output: 신규 4파일. 커밋 0.

**wave 2 병렬 불변:** `ui.js` 의 신규 `tabbar demoLabel banner modal` 은 06-02 가 같은 시각에 추가한다 — 이 플랜은 부록 E 의 시그니처로 import 만 한다(`import { esc, cover, tabbar, demoLabel, modal, navbar, icon } from "./ui.js"`). 토큰 `--star --star-off --difficulty-on --difficulty-off --kpi-bg --demo-label --banner-bg` 는 06-05 가 `tokens.css` 에 넣는다 — 이 플랜의 CSS 는 `var()` 로 참조만. 브라우저 확인은 wave 3 이후(06-06 체크리스트·06-07 게이트); 이 플랜의 판정은 `node --check`·grep·`wc -l`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-demo-rebuild/06-CONTEXT.md
@.planning/phases/06-demo-rebuild/06-PATTERNS.md
@../.claude/rules/demo.md
@../.claude/rules/references.md
@../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md
@../.assets/설계서/화면 구성 및 디자인/01_기술스택_및_화면설계.md
@src/millie_rec/serving/schemas.py
@src/millie_rec/contracts.py
@demo/js/screens/ui.js
@demo/js/screens/s8_detail.js
@demo/js/screens/s7_home.js
@demo/js/screens/onboarding_step.js
@demo/js/inspector.js
@demo/css/home.css
@demo/css/onboarding.css
@demo/css/inspector.css
@demo/css/tokens.css

<file_ownership>
## Phase 6 파일 소유 표 (겹침 0 — 같은 wave 는 서로소)

| 파일 | 만듦/고침/삭제 | 플랜 | wave |
|---|---|---|---|
| `demo/scripts/make_mock.py` · `tests/demo/test_make_mock.py` · `demo/mock/*.json`(13개 — 계약 10 + catalog_kr·neighbors_kr + _manifest) · `demo/mock/books.json` 삭제 | 재작성·신규·재생성 | 06-01 | 1 |
| `demo/js/router.js` · `demo/js/app.js` · `demo/js/actions.js` · `demo/js/presets.js` · `demo/js/screens/ui.js` · `demo/js/screens/onboarding_step.js` · `demo/js/screens/s0_start.js` · `demo/js/screens/s6_persona.js` · `demo/index.html` · `demo/css/base.css` · `demo/config/onboarding.json` · `demo/README.md` | 신규·재작성·최소 수정 | 06-02 | 2 |
| `demo/js/api.js` · `demo/js/mock.js` · `demo/js/mock_store.js` | 재작성·신규 | 06-03 | 2 |
| `demo/js/screens/d4_reader.js` · `demo/js/screens/d5_library.js` · `demo/css/reader.css` · `demo/css/library.css` | 신규 | **06-04(이 플랜)** | 2 |
| `demo/js/screens/d7_dashboard.js` · `demo/js/screens/d8_showcase.js` · `demo/css/dashboard.css` · `demo/css/tokens.css` | 신규·토큰 추가 | 06-05 | 2 |
| `demo/js/screens/d2_home.js`(← `s7_home.js` 삭제) · `demo/js/screens/d3_detail.js`(← `s8_detail.js` 삭제) · `demo/js/inspector.js` · `demo/css/home.css` · `demo/css/inspector.css` | 신규·삭제·확장 | 06-06 | 3 |
| `.planning/phases/06-demo-rebuild/06-VERIFICATION-NOTES.md` · `../PROGRESS.md`(미결 절 2줄 append 만) | Advisor | 06-07 | 4 |
</file_ownership>

<interfaces>
<!-- 공통 인터페이스 부록 — 06-02 ~ 06-06 다섯 플랜에 **동일한 텍스트**로 들어간다. 한 곳만 바꾸면 병렬 Worker 가 어긋난다. -->
<!-- 정본: 화면 구성 02 §4 상태 모델 v2 · §5 이벤트 13종 · 화면 구성 01 §6 · serving/schemas.py(응답 형태) · contracts.py(이름). -->

## A. `state` v2 — `demo/js/app.js` 의 단일 상태 객체 (06-02 소유, 다른 파일은 읽기만)
```js
const state = {
  route: { page: "showcase", params: {} },  // router.parse(location.hash). page ∈ showcase|onboarding|home|book|reader|library|refresh|dashboard
  source: "mock",                            // "mock" | "api"
  userKey: null,                             // localStorage["millie_user_key"] UUIDv4 — boot 에서 항상 채움(익명도 키 있음)
  cell: null,                                // "A" | "B" — postPreferences 응답 · getUserState 응답
  consent: null,                             // null | true | false (건너뛰기 = false)
  screen: "S0",                              // D1/D6 내부 단계 "S0".."S6"
  resetting: false,                          // D6(#/refresh) 모드 — 헤더 "취향 다시 설정", S0 생략, CTA "새 취향으로 추천 받기"
  prefs: { readingTime: null, categories: [], criterion: null, subcategories: [], seedBooks: [] },  // criterion = S3 **라벨**(config 텍스트)
  steps: [],                                 // config/onboarding.json .steps
  meta: null,                                // OnboardingMeta {survey_variant, reading_times[], criteria[{id,label}], categories[{name,supported,subcategories[]}]}
  candidateSet: { id: null, items: [], impressions: [] },  // items = CandidateItem[], impressions = [{book_id, position, selected}]
  snapshots: [],                             // [{ id, createdAt, prefs, persona, cell }] — append only(재설정도 push)
  model: null,                               // null = 셀 배정(A→hybrid, B→hybrid_div) · VARIANTS 문자열 = 인스펙터 라디오 강제(forced)
  recommend: null,                           // RecommendOut 원문. api 클라이언트 fallback 시 + client_fallback_reason: string
  detail: null,                              // { item: ItemOut, rowId } | null — D3 바텀시트. rowId ∈ RecommendOut.rows[].row_id | "library"(D5 서재 타일 — item 은 LibraryBook 기반 최소 ItemOut, 부록 B card)
  reading: null,                             // { bookId, progressPct, virtualMinutes, qualified: false, completed: false } | null — D4
  ratings: {},                               // { [bookId]: stars }
  library: { added: [], reading: [], completed: [] },  // UserStateOut.library (LibraryBook[])
  libraryTab: "added",                       // D5 활성 탭 added|reading|completed
  userState: null,                           // UserStateOut 원문(D5 타임라인·U_t)
  mydata: null,                              // UserDataOut 원문(D5 "내 데이터 보기" 모달)
  dashboard: null,                           // DashboardOut 원문(D7)
  showcase: null,                            // ShowcaseOut 원문(D8)
  health: null,                              // HealthOut | null (상단바 model_version)
  nearlineLagSec: null,                      // RecommendOut.nearline_lag_s
  events: [],                                // 인스펙터 로그 [{ t: "HH:MM:SS", event_type, detail }] 최근 200
  toast: null,                               // string | null — 3초 후 null
  banner: null,                              // string | null — D2 상단 Banner 문구(예: "비개인화 인기 도서")
  modal: null,                               // null | "rating" | "mydata" | "withdraw"
};
```
원칙: `setState(patch)` 하나 · `render()` 하나 · 화면 파일은 `render(state) → HTML 문자열` 만, fetch·상태 변경 금지(`../.claude/rules/demo.md`).

## B. `data-act` 액션 이름 (화면 파일이 찍고 `app.js`/`actions.js` 가 위임 처리 — click 1곳 + change 1곳)
| data-act | 추가 data-* | 처리 | 이벤트(EVENT_TYPES 13종) |
|---|---|---|---|
| `start` | — | S0 → S1 | `preference_started` |
| `skip` | — | consent=false → `#/home`(level 3) | — |
| `back` | — | 단계 뒤로(S1 에서 resetting 이면 `#/home`) | — |
| `pick` | `data-step` `data-val` | S1~S4 선택 토글 | `preference_step`(payload {step}) |
| `pickbook` | `data-book` `data-pos` | S5 시드 토글 + impressions[].selected | `preference_book_selected` / `preference_book_deselected` |
| `next` | — | 다음 단계(S4→S5 진입 시 getCandidates + `impression`×30, S5 완료 시 postPreferences) | `preference_completed` |
| `toHome` | — | S6 CTA → `#/home` | — |
| `nav` | `data-to="#/home"` 등 해시 | `location.hash = to` | — |
| `preset` | `data-preset="newUser"\|"skipUser"\|"resetUser"` | presets.run(name) | (프리셋이 만드는 이벤트) |
| `card` | `data-book` `data-row` `data-pos` | detail 설정 → `#/book/:id`. 행 탐색은 `(state.recommend?.rows ?? [])` 로 null 방어(철회 직후 `recommend: null` 이어도 TypeError 0). `data-row="library"`(D5 서재 타일) 이면 `state.library` 의 added·reading·completed 에서 `LibraryBook{book_id,title,image_url}` 을 찾아 ItemOut 형태의 최소 detail(`book_id`·`title`·`image_url` 채움, `authors`·`reason`·`badge`·`source`·`book_format`·`difficulty` null, `source_channels` [], `score`·`position` 0) 을 만들고 `rowId: "library"` 로 둔다(d3 는 rowId 를 "내 서재" 로 표시). 못 찾으면 무동작 | `detail_click`(서재 타일은 `row_id`·`position` 없이 `surface: "library"`) |
| `closeSheet` | — | `#/home` | — |
| `read` | `data-book` | `#/reader/:id` 진입(reading 초기화) | `reader_open` |
| `library` | `data-book` | 서재 담기 | `library_add`(surface "main") |
| `read10` | — | progressPct +8, virtualMinutes +10; ≥15분 첫 도달 시 1회 | `qualified_read` |
| `complete` | — | completed=true, modal="rating" | `completion` |
| `rate` | `data-stars` 1..5 | postRating → toast "다음 책을 준비하고 있어요" → `#/home` | `rating` |
| `rateLater` | — | 토스트 → `#/home` | — |
| `libTab` | `data-tab` | libraryTab 변경 | — |
| `mydata` | — | getUserData → modal="mydata" | — |
| `withdraw` | — | modal="withdraw" | — |
| `withdrawConfirm` | — | deletePersonalization → consent=false → banner "비개인화 인기 도서" → `#/home` | — |
| `closeModal` | — | modal=null | — |
| `model` | (radio `value`) | state.model=value → getRecommend(forced) | — |
| `noop` | — | — | — |
- D6 시작(`#/refresh` 진입)은 라우터가 `preference_restarted` 를 기록한다. D2 행 렌더 직후 `impression`(row_id·position·recommendation_id) 은 `app.js` 가 recommendation_id 당 1회 기록한다.

## C. `api.js` 공개 함수 (06-03 소유) — 전부 `async`, 첫 인자 `source`; 실패는 엔드포인트별로 잡아 `null` 반환(uncaught 0). `getRecommend` 만 4초 타임아웃·네트워크 오류 시 `fallback/popular.json` + `client_fallback_reason`
```js
export const config;                                        // { source: "mock"|"api"|null, apiBase: string(기본 "" — ?api= 덮어쓰기), capture: "1"|"2"|null }
export async function resolveSource();                      // 명시 ?source 우선 · 없으면 GET /health 200 → "api", 아니면 "mock"
export async function loadSteps();                          // config/onboarding.json → steps[]
export async function health(source);                       // HealthOut | null           (mock: {status:"ok", api_version:"v2", model_version:"hybrid_div_v1", db_ok:true, uptime_s:0} 형태)
export async function loadMeta(source);                     // OnboardingMeta
export async function getCandidates(source, { categories, subcategories = [], n = 30, userKey });  // CandidateSet
export async function postPreferences(source, body);        // body = PreferencesRequest{user_key, consent, reading_time, categories, criterion(id), subcategories, seeds, candidate_set_id, restart} → PreferencesResponse
export async function getRecommend(source, { userKey, snapshotId, model, k = 40, context });  // RecommendOut (+client_fallback_reason)
export async function postEvents(source, events);           // EventIn[] ≤50 → EventsAccepted | null
export async function postRating(source, body);             // RatingIn{rating_id, user_key, book_id, stars, ts, recommendation_id} → RatingOut | null
export async function getUserState(source, userKey);        // UserStateOut | null
export async function getUserData(source, userKey);         // UserDataOut | null
export async function deletePersonalization(source, userKey); // PersonalizationDeleted | null
export async function getDashboard(source);                 // DashboardOut | null   (api 404 → null → 화면 "이 항목은 아직 없음")
export async function getShowcase(source);                  // ShowcaseOut | null
```
경로(api 모드): `/health` · `/api/meta/onboarding` · `/api/candidates/onboarding?categories=&n=&user_key=&subcategories=` · `POST /api/preferences`(201) · `/api/recommend?user_key=&snapshot_id=&model=&k=&context=` · `POST /api/events`(202, `{events:[…]}` 배치) · `POST /api/ratings` · `/api/users/{key}/state` · `/api/users/{key}/data` · `DELETE /api/users/{key}/personalization` · `/api/dashboard` · `/api/showcase`. 타임스탬프 필드는 **`ts`**.

## D. `mock.js` 공개 함수 (06-03 소유) — `api.js` 가 `source === "mock"` 일 때 같은 이름으로 위임(source 인자 없음)
```js
export async function init();            // mock/catalog_kr.json + mock/neighbors_kr.json + mock/showcase.json 1회 로드(Promise 캐시)
export function byId(bookId);            // 카드 dict | undefined
export function health(); loadMeta(); getCandidates({categories, subcategories, n, userKey}); postPreferences(body);
export function getRecommend({userKey, snapshotId, model, k, context}); postEvents(events); postRating(body);
export function getUserState(userKey); getUserData(userKey); deletePersonalization(userKey); getDashboard(); getShowcase();
```
반환 형태는 `serving/schemas.py`·`schemas_should.py` 모델과 키 집합 1:1(여분 키 금지). 세션 저장은 `mock_store.js`(sessionStorage `millie_mock_db`).

## E. 화면 파일 규약
- `demo/js/screens/d2_home.js` `d3_detail.js` `d4_reader.js` `d5_library.js` `d7_dashboard.js` `d8_showcase.js`: `export function render(state) → string`. import 는 `./ui.js` 만(`d3_detail.js` 는 배경용 `./d2_home.js` 추가 허용).
- `onboarding_step.js`: `render(state, step, meta)` · `s0_start.js`: `render(state)` · `s6_persona.js`: `render(state)`.
- `ui.js`(06-02 소유) 추가 export: `tabbar(active)`(`"home"|"library"` → `<nav class="tabbar">` 홈·서재 2탭, `data-act="nav" data-to="#/home|#/library"`) · `demoLabel(text = "데모 전용 · 실제 밀리 화면 아님")` → `<div class="demo-label">` · `banner(text)` → `<div class="banner">` · `modal(inner)` → `<div class="modal-wrap"><div class="modal">…</div></div>`. 기존 `esc cover badge josa icon statusbar navbar cta` 유지.
- 모든 사용자·카탈로그 문자열은 `esc()` 를 거쳐 삽입한다. `innerHTML` 에 원문 금지(XSS). `title ?? "(제목 없음)"` 방어.
- D7·D8 은 `#page` 컨테이너(전폭), 나머지는 `#phone`. 렌더 대상 결정은 `app.js` 만.

## F. 저장 키·상수
- `localStorage["millie_user_key"]` — UUIDv4(`crypto.randomUUID()`), 개인정보 아님.
- `sessionStorage["millie_events"]` — `app.js` 이벤트 송신 큐(EventIn[]; 50개 단위 `postEvents`).
- `sessionStorage["millie_mock_db"]` — `mock_store.js` 의 mock 서버 상태(users·snapshots·events·ratings·recommendations).
- 상수(값은 `contracts.py` 와 동일): `BUDGET_MS = 200` · `QUALIFIED_READ_MINUTES = 15` · `ROW_SIZE = 12` · `VARIANTS = ["pop","cf","hybrid","hybrid_div"]` · `BADGE_TYPES = ["bestseller","review","author","publisher","buzz","light"]` · EVENT_TYPES 13종(`impression detail_click library_add reader_open qualified_read completion rating preference_started preference_step preference_book_selected preference_book_deselected preference_completed preference_restarted`).
- `EventIn` 필드: `{event_id(uuid), user_key, book_id?, event_type, ts(ISO), surface?, row_id?, position?, selected?, recommendation_id?, model_version?, preference_snapshot_id?, candidate_set_id?, payload: {string: string}}` — 그 외 키 금지.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: d4_reader.js + reader.css — 뷰어 시뮬레이션(진행률·10분 읽기·완독·가상 시간·T=15 표기) + RatingModal</name>
  <files>demo/js/screens/d4_reader.js, demo/css/reader.css</files>
  <read_first>
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D4 표(상단 제목·진행률 바·"진행률 5%"·탭바 숨김 · 본문 회색 placeholder + "아래 버튼으로 독서를 시뮬레이션합니다" · 조작 "10분 읽기"(+8%, +10분)·"완독"·뒤로 · 별점 모달 "어떠셨나요?" ★×5 1탭 · "나중에" · 완독 후 토스트 "다음 책을 준비하고 있어요") · §3 공통 컴포넌트(`RatingModal` 별 44px · `DemoLabel` 24px) · §6 토큰(`--star` `--star-off` `--demo-label`)
    - ../.claude/rules/demo.md ("가짜 페이지 번호 금지(D4 는 '진행률 %')" · DemoLabel 문구 "데모 전용 · 실제 밀리 화면 아님")
    - src/millie_rec/contracts.py L90 `QUALIFIED_READ_MINUTES = 15  # 데모 상수. production 은 로그 분포로 보정`
    - demo/js/screens/s8_detail.js (시트·액션 버튼 마크업 골격 `.sheet__actions .cta.is-on / .sheet__ghost`) · demo/js/screens/onboarding_step.js L77 진행바 마크업(`.progress .progress__fill`) · demo/css/home.css `.sheet*` 규칙(딤·시트 반경) · demo/css/onboarding.css `.progress`
    - demo/js/screens/ui.js (`esc cover navbar icon` 시그니처 · 부록 E 신규 `demoLabel modal`)
  </read_first>
  <action>
**`demo/js/screens/d4_reader.js` 신규(≤120줄).** `import { esc, cover, demoLabel, modal, icon } from "./ui.js"; const QUALIFIED_READ_MINUTES = 15; // contracts.QUALIFIED_READ_MINUTES 와 같은 값 — 데모 상수`
```js
function book(state) {   // 카드 정보는 detail(D3 경유) 또는 recommend rows 에서 book_id 로 찾는다 — 제목만으로 동일성 판단 금지(06-CONTEXT D-07b)
  const id = state.reading?.bookId;
  if (state.detail?.item?.book_id === id) return state.detail.item;
  return (state.recommend?.rows ?? []).flatMap((r) => r.items).find((i) => i.book_id === id) ?? { book_id: id, title: null, authors: null, image_url: null };
}
function ratingModal() {
  const stars = [1, 2, 3, 4, 5].map((s) => `<button class="rating__star" data-act="rate" data-stars="${s}" aria-label="${s}점">★</button>`).join("");
  return modal(`<h3 class="rating__title">어떠셨나요?</h3><div class="rating__stars">${stars}</div>
    <button class="rating__later" data-act="rateLater">나중에</button>
    <p class="rating__note">1탭 즉시 전송 · 라벨은 별점 원본, 리워드는 UX 제안만</p>`);
}
export function render(state) {
  const r = state.reading ?? { progressPct: 0, virtualMinutes: 0, qualified: false, completed: false };
  const b = book(state);
  const title = b.title ?? "(제목 없음)";
  return `${demoLabel("데모 전용 · 실제 뷰어 아님")}
  <div class="navbar"><button class="navbar__back" data-act="nav" data-to="#/home">${icon("back", 20, 23)}</button><span class="navbar__label">${esc(title)}</span><span></span></div>
  <div class="screen reader">
    <div class="progress"><div class="progress__fill" style="width:${r.progressPct}%"></div></div>
    <div class="screen__body">
      <div class="reader__head">${cover(b)}<div><h1 class="reader__title">${esc(title)}</h1>
        <p class="reader__author">${esc(b.authors ?? "")}</p>
        <p class="reader__pct">진행률 ${r.progressPct}%</p></div></div>
      <div class="reader__paper"><p>아래 버튼으로 독서를 시뮬레이션합니다</p></div>
      <dl class="reader__sim">
        <dt>가상 독서 시간</dt><dd>${r.virtualMinutes}분</dd>
        <dt>Qualified Reading Start</dt><dd>${r.qualified ? "도달 ✓" : `T=${QUALIFIED_READ_MINUTES}분 도달 시 qualified_read 1회`}</dd>
        <dt>상태</dt><dd>${r.completed ? "완독 (completion)" : r.qualified ? "유효 독서 중" : "열람 (reader_open)"}</dd>
      </dl>
      <p class="reader__note">T=15분은 데모 상수, production은 로그 분포로 보정 (main 설계서 §4)</p>
    </div>
    <div class="screen__foot reader__actions">
      <button class="cta is-on" data-act="read10"${r.completed ? " disabled" : ""}>10분 읽기 <small>+8% · +10분</small></button>
      <button class="sheet__ghost" data-act="complete"${r.completed ? " disabled" : ""}>완독</button>
    </div>
  </div>
  ${state.modal === "rating" ? ratingModal() : ""}`;
}
```
- 탭바 없음(전체화면, 화면 02 §1 "D4 뷔어는 전체화면으로 숨김"). 페이지 번호·쪽수 문자열 금지(`페이지|쪽|p\.` 없음).
- 뒤로는 `ui.navbar()` 를 쓰지 않고 위처럼 `data-act="nav" data-to="#/home"` 버튼을 직접 넣는다(`navbar()` 의 back 은 `data-act="back"` 고정 = 온보딩 단계 뒤로 전용).

**`demo/css/reader.css` 신규(≤120줄, 색 리터럴 0):** `.reader__head { display:flex; gap:14px; padding:12px 0 }` `.reader__head .cover { width:72px; flex:0 0 auto }` `.reader__title { font-size:18px; font-weight:700; margin:0 }` `.reader__author { color:var(--text-2); font-size:var(--fs-author); margin:4px 0 }` `.reader__pct { font-family:var(--mono); font-size:13px; color:var(--accent) }` `.reader__paper { height:260px; border-radius:16px; background:var(--surface); display:flex; align-items:center; justify-content:center; color:var(--text-3); font-size:14px; margin:12px 0 }` `.reader__sim { display:grid; grid-template-columns:140px 1fr; gap:8px 12px; font-size:13px }` `.reader__sim dt { color:var(--text-2) }` `.reader__sim dd { margin:0; font-family:var(--mono) }` `.reader__note { font-size:12px; color:var(--text-3) }` `.reader__actions { display:flex; gap:10px }` `.reader__actions .cta small { font-size:12px; opacity:.7; margin-left:6px }` · 별점: `.rating__title { font-size:20px; font-weight:700; margin:0 0 16px; text-align:center }` `.rating__stars { display:flex; justify-content:center; gap:8px }` `.rating__star { width:44px; height:44px; font-size:36px; line-height:44px; color:var(--star-off) }` `.rating__star:hover, .rating__star:focus { color:var(--star) }` `.rating__later { display:block; margin:16px auto 0; color:var(--text-2) }` `.rating__note { font-size:11px; color:var(--text-3); text-align:center; margin:12px 0 0 }`.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/screens/d4_reader.js && wc -l js/screens/d4_reader.js css/reader.css && grep -c 'data-act="read10"\|data-act="complete"\|data-act="rate"\|data-act="rateLater"\|어떠셨나요?\|진행률 ${r.progressPct}%\|QUALIFIED_READ_MINUTES = 15\|데모 전용' js/screens/d4_reader.js && grep -nE 'innerHTML|fetch\(|페이지 [0-9]|[0-9]+쪽' js/screens/d4_reader.js | wc -l && grep -nE '#[0-9a-fA-F]{3,8}\b' css/reader.css | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/screens/d4_reader.js` 종료 0 · `wc -l demo/js/screens/d4_reader.js` ≤ 120 · `wc -l demo/css/reader.css` ≤ 120
    - `grep -c 'export function render(state)' demo/js/screens/d4_reader.js` == 1 · `grep -cE '^import .* from "\./ui\.js"' demo/js/screens/d4_reader.js` == 1 이고 다른 import 0(`grep -c '^import' == 1`)
    - `grep -c 'data-act="read10"' demo/js/screens/d4_reader.js` == 1 · `data-act="complete"` == 1 · `data-act="rate"` == 1 · `data-act="rateLater"` == 1 · `data-to="#/home"` ≥ 1
    - `grep -c '어떠셨나요?' demo/js/screens/d4_reader.js` == 1 · `grep -c '아래 버튼으로 독서를 시뮬레이션합니다' …` == 1 · `grep -c 'T=15분은 데모 상수, production은 로그 분포로 보정' …` == 1 · `grep -c '데모 전용 · 실제 뷰어 아님' …` == 1 · `grep -c '(제목 없음)' …` == 1
    - `grep -nE 'innerHTML|fetch\(|localStorage|sessionStorage|페이지 [0-9]|[0-9]+쪽' demo/js/screens/d4_reader.js | wc -l` == 0
    - `grep -c 'tabbar' demo/js/screens/d4_reader.js` == 0 (전체화면)
    - `grep -nE '#[0-9a-fA-F]{3,8}\b' demo/css/reader.css | wc -l` == 0 · `grep -c 'var(--star)' demo/css/reader.css` ≥ 1 · `grep -c 'width:44px' demo/css/reader.css` == 1
  </acceptance_criteria>
  <done>뷰어 화면·별점 모달 마크업/CSS 존재, 정적 검사 통과, 액션 이름 부록 B 와 일치.</done>
</task>

<task type="auto">
  <name>Task 2: d5_library.js + library.css — 탭 3 · 스냅샷 Timeline · U_t 카드 · 열람/재설정/철회 버튼 · TabBar</name>
  <files>demo/js/screens/d5_library.js, demo/css/library.css</files>
  <read_first>
    - ../.assets/설계서/화면 구성 및 디자인/02_화면구성_v2_8페이지.md §2 D5 표(탭 담은 책·읽는 중·완독 · 타임라인 `snap_01 (09-04 20:11) IT·소설·철학 / 베스트셀러` → … · "재설정해도 독서 기록은 유지됩니다" · U_t α/β/γ 막대 + "완독이 쌓일수록 행동(β) 비중이 커집니다" · 버튼 취향 다시 설정(→ D6)·내 데이터 보기(JSON 모달)·맞춤 추천 동의 철회(확인 → DELETE → D2 level 3 + 배너)) · §3(`Timeline` 세로 · `TabBar` 홈·서재 · `BookCard` 108px)
    - src/millie_rec/serving/schemas.py `UserStateOut{user_key, consent, cell, is_new, library{added,reading,completed}: LibraryBook{book_id,title,image_url}, snapshots[SnapshotSummary{snapshot_id, created_at, categories, criterion, active}], user_state_weights, nearline_lag_s}`
    - demo/js/screens/s7_home.js L7-16 `tile()` 108px 카드 마크업 · demo/css/home.css `.tile*` · demo/js/inspector.js L84-99 `weightsSection`(막대·라벨 "explicit (온보딩 선택)" "long-term (독서 기록)" "session (현재 세션)") · demo/css/inspector.css `.weights .weight*`
    - demo/js/screens/ui.js (`esc cover tabbar navbar`)
  </read_first>
  <action>
**`demo/js/screens/d5_library.js` 신규(≤150줄).** `import { esc, cover, tabbar, navbar } from "./ui.js";`
```js
const TABS = [["added", "담은 책"], ["reading", "읽는 중"], ["completed", "완독"]];
const CRITERION_LABEL = { author: "좋아하는 작가", publisher: "좋아하는 출판사", bestseller: "베스트셀러", buzz: "화제작", review: "리뷰·별점" };  // 타임라인 표시용 축약(id → 라벨). 화면 텍스트 정본은 meta.criteria — state.meta 가 있으면 그 label 을 우선
function tile(b) {  // LibraryBook — title null 방어(D-07b ④). 카드는 book_id 로 라우팅. data-row="library" 는 06-02 card 액션이 state.library 에서 최소 detail 을 만든다(부록 B)
  return `<button class="tile" data-act="card" data-book="${b.book_id}" data-row="library" data-pos="0">
    ${cover(b)}<div class="tile__title">${esc(b.title ?? "(제목 없음)")}</div></button>`;
}
function tabs(state) { const books = state.library?.[state.libraryTab] ?? [];
  return `<div class="lib__tabs">${TABS.map(([id, label]) => `<button class="lib__tab${state.libraryTab === id ? " is-on" : ""}" data-act="libTab" data-tab="${id}">${label} <small>${(state.library?.[id] ?? []).length}</small></button>`).join("")}</div>
  <div class="lib__grid">${books.length ? books.map(tile).join("") : `<p class="lib__empty">${state.libraryTab === "added" ? "취향 설정에서 고른 책과 서재에 담은 책이 여기에 모입니다" : state.libraryTab === "reading" ? "뷰어에서 읽기 시작한 책" : "완독한 책"}</p>`}</div>`; }
function timeline(state) { const snaps = state.userState?.snapshots ?? [];
  const label = (id) => state.meta?.criteria?.find((c) => c.id === id)?.label ?? CRITERION_LABEL[id] ?? id ?? "—";
  const items = snaps.map((s) => `<li class="timeline__item${s.active ? " is-on" : ""}">
      <span class="timeline__id">${esc(s.snapshot_id)}</span><span class="timeline__t">${esc(String(s.created_at).slice(5, 16).replace("T", " "))}</span>
      <span class="timeline__cats">${esc((s.categories ?? []).join("·"))} / ${esc(label(s.criterion))}</span></li>`).join("");
  return `<section class="lib__sec"><h2>취향 히스토리</h2>${snaps.length ? `<ol class="timeline">${items}</ol>` : `<p class="lib__empty">아직 취향 설정이 없습니다</p>`}
    <p class="lib__hint">재설정해도 독서 기록은 유지됩니다</p></section>`; }
function utCard(state) { const w = state.userState?.user_state_weights ?? { alpha: 0, beta: 0, gamma: 0 };
  const bar = (k, v, n) => `<div class="ut__row"><span class="ut__k">${k}</span><span class="ut__track"><span class="ut__fill" style="width:${Math.round((v ?? 0) * 100)}%"></span></span><span class="ut__v">${Number(v ?? 0).toFixed(2)}</span><span class="ut__n">${n}</span></div>`;
  return `<section class="lib__sec"><h2>U_t 가중치</h2>${bar("α", w.alpha, "explicit (온보딩 선택)")}${bar("β", w.beta, "long-term (독서 기록)")}${bar("γ", w.gamma, "session (현재 세션)")}
    <p class="lib__hint">완독이 쌓일수록 행동(β) 비중이 커집니다</p></section>`; }
export function render(state) {
  return `${navbar({ back: false, label: "내 서재" })}
  <div class="screen"><div class="screen__body">
    ${state.consent === false ? `<p class="lib__banner">맞춤 추천 동의가 없어 비개인화 인기 도서만 보입니다</p>` : ""}
    ${tabs(state)}${timeline(state)}${utCard(state)}
    <div class="lib__actions">
      <button class="cta is-on" data-act="nav" data-to="#/refresh">취향 다시 설정</button>
      <button class="sheet__ghost" data-act="mydata">내 데이터 보기</button>
      <button class="lib__danger" data-act="withdraw"${state.consent === false ? " disabled" : ""}>맞춤 추천 동의 철회</button>
    </div><div style="height:16px"></div>
  </div>${tabbar("library")}</div>`;
}
```
- 상태가 `null`(api 모드에서 서버 준비 전) 이어도 크래시 없이 빈 상태 문구를 그린다(`?.` 방어 전부).
- 모달(내 데이터 JSON · 철회 확인) 은 `app.js` 가 `state.modal` 로 그린다(부록 A) — 이 파일은 버튼만.

**`demo/css/library.css` 신규(≤150줄, 색 리터럴 0):** `.lib__tabs { display:flex; gap:6px; margin:8px 0 12px }` `.lib__tab { flex:1; height:36px; border-radius:var(--r-pill); border:1px solid var(--line); font-size:13px; color:var(--text-2) }` `.lib__tab.is-on { border-color:var(--accent); color:var(--accent) }` `.lib__tab small { opacity:.7; margin-left:4px }` `.lib__grid { display:grid; grid-template-columns:repeat(3, 1fr); gap:12px; min-height:120px }` `.lib__grid .tile { width:auto }` `.lib__empty { grid-column:1 / -1; color:var(--text-3); font-size:13px; text-align:center; padding:24px 0 }` `.lib__sec { margin-top:20px }` `.lib__sec h2 { font-size:18px; font-weight:700; margin:0 0 8px }` `.lib__hint { font-size:12px; color:var(--text-3); margin:8px 0 0 }` `.lib__banner { margin:8px 0; padding:8px 12px; border-radius:8px; background:var(--banner-bg); color:var(--banner-text); font-size:13px }` · 타임라인: `.timeline { margin:0; padding:0 0 0 14px; border-left:2px solid var(--line); list-style:none }` `.timeline__item { position:relative; padding:6px 0 6px 12px; font-size:13px; color:var(--text-2) }` `.timeline__item::before { content:""; position:absolute; left:-19px; top:11px; width:8px; height:8px; border-radius:50%; background:var(--line) }` `.timeline__item.is-on { color:var(--text) }` `.timeline__item.is-on::before { background:var(--accent) }` `.timeline__id { font-family:var(--mono); margin-right:8px }` `.timeline__t { font-family:var(--mono); color:var(--text-3); margin-right:8px }` · U_t: `.ut__row { display:grid; grid-template-columns:20px 1fr 44px 1fr; gap:8px; align-items:center; margin:6px 0; font-size:12px }` `.ut__k { font-weight:700 }` `.ut__track { height:8px; border-radius:4px; background:var(--line); overflow:hidden }` `.ut__fill { display:block; height:100%; background:var(--accent) }` `.ut__v { font-family:var(--mono); text-align:right }` `.ut__n { color:var(--text-3) }` · `.lib__actions { display:flex; flex-direction:column; gap:10px; margin-top:20px }` `.lib__danger { height:44px; border-radius:var(--r-pill); border:1px solid var(--kpi-bad); color:var(--kpi-bad); font-size:14px }` `.lib__danger:disabled { opacity:.4 }`.
  </action>
  <verify>
    <automated>cd /Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/demo && node --check js/screens/d5_library.js && wc -l js/screens/d5_library.js css/library.css && grep -c 'data-act="libTab"\|data-act="mydata"\|data-act="withdraw"\|data-to="#/refresh"\|재설정해도 독서 기록은 유지됩니다\|완독이 쌓일수록 행동(β) 비중이 커집니다\|tabbar("library")' js/screens/d5_library.js && grep -nE 'innerHTML|fetch\(|localStorage|sessionStorage' js/screens/d5_library.js | wc -l && grep -nE '#[0-9a-fA-F]{3,8}\b' css/library.css | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `node --check demo/js/screens/d5_library.js` 종료 0 · `wc -l demo/js/screens/d5_library.js` ≤ 150 · `wc -l demo/css/library.css` ≤ 150
    - `grep -c 'export function render(state)' demo/js/screens/d5_library.js` == 1 · import 는 `./ui.js` 1줄만
    - `grep -c 'data-act="libTab"' demo/js/screens/d5_library.js` == 1(map 안 1회) · `data-act="mydata"` == 1 · `data-act="withdraw"` == 1 · `data-to="#/refresh"` == 1 · `tabbar("library")` == 1
    - 문구: `grep -c '재설정해도 독서 기록은 유지됩니다' …` == 1 · `grep -c '완독이 쌓일수록 행동(β) 비중이 커집니다' …` == 1 · `grep -c '"담은 책"\|"읽는 중"\|"완독"' …` ≥ 3 · `grep -c '취향 다시 설정\|내 데이터 보기\|맞춤 추천 동의 철회' …` == 3
    - `grep -c 'explicit (온보딩 선택)\|long-term (독서 기록)\|session (현재 세션)' demo/js/screens/d5_library.js` == 3
    - `grep -c '(제목 없음)' demo/js/screens/d5_library.js` == 1 · `grep -nE 'innerHTML|fetch\(|localStorage|sessionStorage' demo/js/screens/d5_library.js | wc -l` == 0
    - `grep -nE '#[0-9a-fA-F]{3,8}\b' demo/css/library.css | wc -l` == 0 · `grep -c 'var(--kpi-bad)' demo/css/library.css` ≥ 1
    - 이 플랜 소유 4파일에서 06-07 구 형태 정규식 0건: `cd demo && grep -rnE 'hf\.space|"/recommend"|\btimestamp\b|"format"\s*:|"type"\s*:\s*"rating"|\btype:\s*"rating"|static_popular|REPLACE-ME|books\.json|zygmuntz|CSV_URL' js/screens/d4_reader.js js/screens/d5_library.js css/reader.css css/library.css | wc -l` == 0
  </acceptance_criteria>
  <done>서재 화면 마크업/CSS 존재, null 상태 방어, 액션 이름 부록 B 와 일치.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `state.library`·`state.userState`(서버/mock 응답 문자열) → DOM | title·snapshot_id·categories 삽입 — XSS 표면 |
| 별점 모달 → `data-stars` | 사용자 입력은 버튼 5개로 한정(1..5) — 자유 입력 없음 |
| 철회 버튼 → `withdraw` → `withdrawConfirm` | 2단계 확인(모달) — 실수 삭제 방지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | 카탈로그 제목·스냅샷 카테고리 문자열의 DOM 삽입(XSS) | mitigate | 모든 삽입 `esc()` · `innerHTML` 0(acceptance grep) · 화면 파일은 문자열 반환만 |
| T-06-04-02 | Tampering | `data-stars` 조작(DevTools 로 6점) | accept | 서버 `RatingIn.stars ge=1 le=5` 422 · mock 은 표시만 — 모델 라벨로 쓰지 않는다(설계만) |
| T-06-04-03 | Repudiation | 동의 철회 버튼 오클릭으로 데이터 삭제 | mitigate | `withdraw` → 확인 모달(`app.js`) → `withdrawConfirm` 2단계 · 이미 `consent=false` 면 버튼 disabled |
| T-06-04-04 | Information Disclosure | "내 데이터 보기" 모달에 타인 데이터 | mitigate | `getUserData(source, state.userKey)` — 자기 키만(06-03) · 화면은 `state.mydata` 만 그림 |
| T-06-04-05 | Denial of Service | `state.userState === null`(api 모드 404) 로 렌더 크래시 | mitigate | 전 필드 `?.`·기본값 · 빈 상태 문구("아직 취향 설정이 없습니다") |
| T-06-04-06 | Spoofing | 뷰어가 "실제 밀리 화면" 으로 오해됨 | mitigate | `DemoLabel` "데모 전용 · 실제 뷰어 아님" 상단 고정 · 페이지 번호 금지 |
</threat_model>

<verification>
- `node --check` 2 JS 종료 0 · `wc -l` 상한(d4 120 · d5 150 · reader.css 120 · library.css 150)
- 액션 이름(read10 complete rate rateLater libTab mydata withdraw nav) 이 부록 B 와 문자 단위 일치
- 문구 6종(어떠셨나요? · 아래 버튼으로 독서를 시뮬레이션합니다 · T=15분은 데모 상수… · 재설정해도 독서 기록은 유지됩니다 · 완독이 쌓일수록… · 데모 전용) 존재
- 색 리터럴 0 · `innerHTML`/`fetch` 0 · 구 형태 0
</verification>

<success_criteria>
- DEMO-07 화면 측: 10분 읽기·완독 버튼·가상 시간·T=15 표기·1탭 별점 모달이 존재(이벤트·POST 는 06-02)
- DEMO-03 중 D4·D5 페이지 렌더러 존재(`render(state) → string`)
- 화면 02 §2 D4·D5 표의 요소 전부 마크업에 존재
</success_criteria>

<output>
After completion, create `.planning/phases/06-demo-rebuild/06-04-SUMMARY.md` — 변경 파일 목록(커밋 0) · `wc -l` · 액션·문구 대비표 · 06-06 체크리스트에 넘길 D4/D5 확인 항목(뒤로 = `nav #/home` 우회 사용).
</output>
