---
phase: 03-millie-catalog
plan: 05
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/export_millie_vectors.py             # NEW — TF-IDF → TruncatedSVD 128 → L2 float32 npz, ≤150줄
  - scripts/export_millie_fallback.py            # NEW — demo/fallback/popular.json(RecommendOut level 3 형태), ≤150줄
  - tests/data/test_millie_vectors.py            # NEW
  - tests/data/test_millie_fallback.py           # NEW
autonomous: true
requirements: [DATA-07]
lane: Data-B (scripts/ Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - scripts/build_millie_catalog.py
  - scripts/millie_difficulty.py
  - scripts/build_millie_edges.py
  - scripts/build_millie_popularity.py
  - scripts/export_millie_serving.py
  - scripts/millie_parse.py
  - scripts/collect_millie.py
  - data/id_map.csv
  - data/processed/**
  - artifacts/**
  - demo/**                                      # popular.json 실 갱신은 Plan 06 `make millie`
  - tests/data/test_millie_catalog.py
  - tests/data/test_millie_edges.py
  - tests/data/test_millie_difficulty.py
  - tests/data/test_millie_popularity.py
  - tests/data/test_millie_export.py
  - tests/fixtures/millie/**
  - src/millie_rec/**
  - tests/test_architecture.py
  - tests/conftest.py
  - Makefile
  - pyproject.toml
  - ../.claude/**
  - CLAUDE.md

must_haves:
  truths:
    - "export_millie_vectors.py 가 books_kr.parquet 의 title+description+curator_note 문자 2~4gram TF-IDF(build_millie_edges 와 같은 설정)를 TruncatedSVD(n_components=min(128, n-1, n_features-1), random_state=SEED) 로 축소하고 L2 정규화한 float32 를 np.savez(book_ids=int64[N], vectors=float32[N,dim]) 로 쓴다 — 같은 입력이면 바이트 동일(재현성)"
    - "export_millie_fallback.py 가 books_kr.parquet 에서 eligible(title ∧ *.millie.co.kr ∧ adult-cover 아님) 도서 pop_rank 상위 40 을 trending 1행에 담은 RecommendOut level 3 형태 json 을 쓰고, serving/schemas.py RecommendOut.model_validate 가 그대로 통과한다(현 v1 mock 의 latency_ms dict·items.format 키 실패 41건 해소)"
    - "popular.json 의 문자열 상수(row_id 'trending' · 제목 '지금 많이 읽는 책' · purpose 'fallback' · source 'popularity' · model_version 'fallback_v1' · fallback_level 3)가 serving/fallback.py·contracts 값과 같다(테스트가 동일성 단정)"
    - "두 스크립트는 millie_rec.contracts 만 import 한다(serving·data import 0) — 검증은 tests 에서"
  artifacts:
    - path: "scripts/export_millie_vectors.py"
      provides: "vectors(texts) -> float32[N,dim] · export_vectors(books_path, out_path) -> int"
      exports: ["vectors", "export_vectors", "SVD_DIM", "NGRAM_RANGE", "TEXT_COLS"]
      max_lines: 150
    - path: "scripts/export_millie_fallback.py"
      provides: "is_eligible(row) · payload(books, n) -> dict · export_fallback(books_path, out_path) -> int"
      exports: ["payload", "export_fallback", "N_ITEMS", "is_eligible"]
      max_lines: 150
    - path: "tests/data/test_millie_fallback.py"
      provides: "RecommendOut.model_validate 통과 · 상수 동일성 · eligible 필터 · 순서·점수"
      min_lines: 50
  key_links:
    - from: "scripts/export_millie_vectors.py::vectors"
      to: "scripts/build_millie_edges.py similarity 설정"
      via: "TfidfVectorizer(analyzer='char_wb', ngram_range=(2,4), min_df=1) 값 중복 정의(scripts 간 import 함정 회피)"
      pattern: "char_wb"
    - from: "tests/data/test_millie_fallback.py"
      to: "src/millie_rec/serving/schemas.py::RecommendOut"
      via: "RecommendOut.model_validate(json.loads(out.read_text()))"
      pattern: "RecommendOut.model_validate"
    - from: "scripts/export_millie_fallback.py"
      to: "demo/fallback/popular.json (Plan 06 실 갱신)"
      via: "main --out 기본값 ROOT/'demo'/'fallback'/'popular.json'"
      pattern: "popular.json"
---

<objective>
Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md "Phase 3: 밀리 카탈로그 빌드") Success Criteria 4 의 나머지 두 산출물(DATA-07): (1) `content_vectors_kr.npz`(다양성 벡터, MMR·ILD 용 `ItemVectors` 입력) 을 만드는 `scripts/export_millie_vectors.py`(신규) (2) `demo/fallback/popular.json` 을 **`RecommendOut` 이 그대로 파싱하는 level 3 형태**로 다시 쓰는 `scripts/export_millie_fallback.py`(신규). `export_millie_serving.py`(Plan 04) 가 150줄을 넘지 않게 D-16 대로 파일을 나눈다. TDD 는 합성 픽스처 체인 + tmp 경로 — 실 `artifacts/serving/`·`demo/fallback/` 은 쓰지 않는다(Plan 06 `make millie`).

Purpose: 결정 D-12 'content_vectors_kr.npz = 같은 TF-IDF 를 TruncatedSVD 128 로 축소한 L2 float32 + book_ids'(.planning/phases/03-millie-catalog/03-CONTEXT.md) · D-14 eligible 규칙(popular.json 40권 선정에 인라인) · Claude's Discretion "demo/fallback/popular.json 새 스키마 = RecommendOut level 3" 이행. 03-PATTERNS 실측: 현 `demo/fallback/popular.json` 은 `RecommendOut.model_validate` 에 41건 실패(`latency_ms` dict·items `format` 키) — 새 파일은 통과가 완료 기준(`/contract-sync` 카탈로그 항목). PDF 각주 "다양성 벡터 = TF-IDF SVD 128".
Output: 스크립트 2 + 테스트 2. 커밋 0.

**하지 않는 것:** `demo/js/inspector.js` 의 `"static_popular"` 문자열 불일치 수정(Phase 6 '데모 재구성' 몫 — SUMMARY 인계 노트) · `make_mock.py` popular.json 블록 제거(Phase 6) · 실데이터 실행 · Makefile. **인계 노트 필수(SUMMARY):** Phase 6 '데모 재구성' 전까지 `make mock` 을 실행하지 않는다 — `demo/scripts/make_mock.py` 가 구형 v1 `popular.json` 을 재생성해 이 플랜의 RecommendOut 파일을 덮어쓴다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-PATTERNS.md
@.planning/phases/03-millie-catalog/03-01-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/fallback.py
@src/millie_rec/serving/compose.py
@scripts/build_millie_edges.py
@scripts/export_millie_serving.py
@demo/fallback/popular.json
@demo/js/api.js
@../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md
@../.claude/rules/data.md
@../.claude/rules/architecture.md
@../.claude/rules/serving.md
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**소유권 예외(`../.claude/rules/architecture.md` 산출물 표):** "`demo/` | demo 레인 | serving·app은 `demo/`를 읽지 않는다. `artifacts/serving/` export가 `demo/fallback/popular.json`을 갱신하는 것만 허용" — 이 스크립트가 그 예외의 구현이다. `demo/` 의 다른 파일은 읽지도 고치지도 않는다.

**HTTP 계약(`../.claude/rules/serving.md`):** "응답 = `contracts` DTO 직렬화. `schemas.py`(pydantic)가 요청 검증 + DTO↔JSON 변환의 유일한 장소. **`latency_ms`는 float 총합, 단계별은 `latency_breakdown`**." / 매니페스트: "`content_vectors_kr.npz`(MMR·ILD) … + `demo/fallback/popular.json`".

**데이터(`../.claude/rules/data.md`):** "`retrieval/content.py`의 `ItemVectors`(MMR·ILD)는 같은 TF-IDF 벡터를 쓴다. … 다양성 벡터 `content_vectors_kr.npz` = 같은 TF-IDF의 TruncatedSVD 128 L2 dense(D-12)." / "표지는 `*.millie.co.kr` 핫링크만 … 성인 표지 플레이스홀더·title 없는 책은 `Catalog.eligible()`에서 제외."

**python.md:** "난수는 `millie_rec.contracts.SEED` 하나. 같은 입력 → 같은 결과여야 PDF 숫자가 재현된다." → `TruncatedSVD(random_state=SEED)`.

**scripts 경계(03-PATTERNS §0):** 스크립트는 `millie_rec.contracts` 만 import(`millie_rec.serving` 금지 — popular.json 검증은 테스트가 `RecommendOut` 으로). 같은 값의 상수 중복 정의는 허용("중복 < 결합").

**TDD(`../.claude/rules/python-tdd.md`):** Red = AssertionError(`Failed:` 포함), `uv run pytest <대상> --no-header`.
</project_rules_inline>

<interfaces>
**RecommendOut(serving/schemas.py L164-179, `extra="forbid"`):** `recommendation_id: str|None` · `model_version: str` · `preference_snapshot_id` · `user_key` · `cell: "A"|"B"|None` · `forced: bool=False` · `fallback_level: int 0..3` · `context` · `latency_ms: float` · `latency_breakdown: dict[str,float]={}` · `user_state_weights: dict[str,float]={}` · `dedup_removed: int=0` · `nearline_lag_s: float|None` · `items: list[ItemOut]=[]` · `rows: list[RowOut]=[]`.
**ItemOut(L138-152):** `book_id score source reason title authors image_url position source_channels: list[str] badge book_format difficulty` — **`format` 키 금지**(현 파일 실패 원인). **RowOut(L155-161):** `row_id`(ROW_IDS 안 또는 `anchor_`) `title purpose`(ROW_PURPOSES) `items subtitle channel_mix: dict[str,int]`.

**서버 level 3 응답의 값(정본 — serving/fallback.py L7-10·L37-45, compose.py L20·L44-60):** `TRENDING_ROW_ID="trending"` · `TRENDING_TITLE="지금 많이 읽는 책"` · `TRENDING_PURPOSE="fallback"` · `SOURCE_POPULARITY="popularity"` · `channel_mix={"popularity": len(items)}` · `ZERO_WEIGHTS={"alpha":0.0,"beta":0.0,"gamma":0.0}` · `items=()`(level 3 은 평탄화 없음) · `latency_breakdown={"total": ms}` · `recommendation_id="rec_"+6hex`. contracts: `MODEL_VERSION_FALLBACK="fallback_v1"` · `FALLBACK_GLOBAL_POP=3`.

**`GlobalPopularFallback.recommend` 점수 관례(L24-33):** `score=float(len(ids) - i)`, `position=i`, `source_channels=("popularity",)`.

**TF-IDF 설정(build_millie_edges.py L22·L43-56, 값 중복 정의):** `NGRAM_RANGE=(2,4)` · `TfidfVectorizer(analyzer="char_wb", ngram_range=NGRAM_RANGE, min_df=1)` · 텍스트 = `title description curator_note` 공백 join(`_text`: str 이 아니면 "").

**`TruncatedSVD` 함정(03-PATTERNS):** `n_components` 는 `< n_features` 이고 실질 `≤ n_samples-1` → `dim = min(SVD_DIM, n_books - 1, n_features - 1)`; 픽스처 12권이면 11.

**현 demo 소비자(참고, 수정 금지):** `demo/js/api.js` L86-87 은 파일을 spread 만 함(`{ ...fb, client_fallback_reason }`). `demo/js/inspector.js` L106·110 은 `model_version === "static_popular"` 라벨 — Phase 6 몫. `latency_ms` 가 float 이 되면 인스펙터 막대가 비지만 크래시 없음(03-PATTERNS "데모 영향").

픽스처 12권 자격: `NO_IMAGE_BOOK`(a3b4…000b) 은 image_url 키 없음 → 비자격 → eligible 11권. `FIRST_BOOK`(불편한 편의점 (재수집), shelf 9,814) pop_rank 1. 다른 11권 image_url 은 `https://img.millie.co.kr/...`(픽스처 확인 — 다르면 SUMMARY 에 실제 자격 수 기록).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): test_millie_vectors 5건 + test_millie_fallback 7건 → 스텁 위에서 AssertionError</name>
  <files>tests/data/test_millie_vectors.py, tests/data/test_millie_fallback.py, scripts/export_millie_vectors.py (스텁), scripts/export_millie_fallback.py (스텁)</files>
  <read_first>
    - src/millie_rec/serving/schemas.py L130-187 (ItemOut·RowOut·RecommendOut) · src/millie_rec/serving/fallback.py (상수 4개·점수 관례) · src/millie_rec/serving/compose.py L20 ZERO_WEIGHTS
    - demo/fallback/popular.json 앞 60줄(현 v1 형태 — 무엇이 계약 위반인지: `latency_ms` dict, items `format`)
    - scripts/build_millie_edges.py L22·L43-56 (TF-IDF 설정 값)
    - tests/data/test_millie_edges.py L27-48 (`_load`·픽스처 체인)
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-12·D-14 · "Specific Ideas — content_vectors_kr.npz"
  </read_first>
  <action>
    **1-a. 스텁 2파일.**
    `scripts/export_millie_vectors.py`: docstring `"""books_kr.parquet → artifacts/serving/content_vectors_kr.npz (DATA-07, D-12: 같은 TF-IDF 의 TruncatedSVD 128 L2 float32 — MMR·ILD 다양성 벡터)."""`, 상수 `SVD_DIM = 128  # D-12. PDF 각주 "다양성 벡터 = TF-IDF SVD 128"` · `NGRAM_RANGE = (2, 4)  # build_millie_edges.py 와 같은 값(중복 정의 — scripts 간 import 함정)` · `TEXT_COLS = ("title", "description", "curator_note")` · `MAX_BYTES = 50 * 1024 * 1024` · `VECTORS_NAME = "content_vectors_kr.npz"`; `def build_texts(books) -> list[str]`(edges 와 같은 join) · `def vectors(texts: list[str]) -> np.ndarray: return np.zeros((len(texts), 1), dtype=np.float32)`(스텁) · `def export_vectors(books_path: Path, out_path: Path) -> int`(스텁: `np.savez(out_path, book_ids=np.array([], dtype=np.int64), vectors=np.zeros((0, 1), np.float32))`, 크기 반환) · `main()` `--books`(DIR_PROCESSED/"books_kr.parquet") `--out`(DIR_ARTIFACTS/"serving"/VECTORS_NAME). import: `argparse Path numpy pandas` + `from millie_rec.contracts import DIR_ARTIFACTS, DIR_PROCESSED, SEED` (sklearn 은 GREEN 에서 함수 안 또는 상단).
    `scripts/export_millie_fallback.py`: docstring `"""books_kr.parquet → demo/fallback/popular.json — RecommendOut level 3 형태 (DATA-07, 백엔드 01 fallback 절). architecture.md 소유권 예외: export 가 demo/fallback/popular.json 을 갱신하는 것만 허용. 문자열 상수는 serving/fallback.py 와 같은 값(scripts 는 millie_rec.serving 을 import 하지 않는다 — 테스트가 동일성을 단정)."""`, 상수 `N_ITEMS = 40` · `RECOMMENDATION_ID = "rec_static"  # 정적 파일 표시(서버는 rec_<6hex>)` · `TRENDING_ROW_ID = "trending"` · `TRENDING_TITLE = "지금 많이 읽는 책"` · `TRENDING_PURPOSE = "fallback"` · `SOURCE_POPULARITY = "popularity"` · `ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}` · `COVER_HOST_SUFFIX = ".millie.co.kr"` · `ADULT_COVER_MARK = "adult-cover"  # data/catalog_kr.py is_eligible 와 같은 규칙(D-14) 중복` · `DEFAULT_OUT = ROOT / "demo" / "fallback" / "popular.json"`; `def is_eligible(row: dict) -> bool: return False`(스텁) · `def payload(books: pd.DataFrame, n: int = N_ITEMS) -> dict: return {}` · `def export_fallback(books_path: Path, out_path: Path) -> int`(스텁: `{}` 를 쓴다) · `main()` `--books` `--out`(DEFAULT_OUT). import: `argparse json math Path urlsplit numpy pandas` + `from millie_rec.contracts import DIR_PROCESSED, FALLBACK_GLOBAL_POP, MODEL_VERSION_FALLBACK, ROOT`.

    **1-b. `tests/data/test_millie_vectors.py`** (docstring `"""content_vectors_kr.npz — 계약(키·dtype·shape) · 정확성(L2·재현성·id 정렬) · 안전성(소표본 dim 가드·크기)."""`). module fixture `books`(체인) · `vec_mod = _load("export_millie_vectors")` · `npz(tmp_path_factory)`: books 를 parquet 로 저장 → `size = vec_mod.export_vectors(bp, out)` → `np.load(out)`. 5건:
    - `test_npz_has_book_ids_and_vectors_with_dtypes(npz)` — `set(z.files) == {"book_ids", "vectors"}`, `z["book_ids"].dtype == np.int64`, `z["vectors"].dtype == np.float32`, `z["book_ids"].shape[0] == z["vectors"].shape[0] == 12`
    - `test_dim_is_min_of_128_and_sample_guard(npz)` — `z["vectors"].shape[1] == 11`(12권 → n-1; TF-IDF 특성 수는 수천이라 n-1 이 구속)
    - `test_rows_are_l2_normalized(npz)` — `np.linalg.norm(z["vectors"], axis=1) == pytest.approx(1.0, abs=1e-4)`
    - `test_book_ids_sorted_and_match_books(npz, books)` — `z["book_ids"].tolist() == sorted(books["book_id"].astype(int))`
    - `test_export_is_deterministic_and_under_cap(tmp_path, vec_mod, books)` — 두 번 export → 두 npz 의 `vectors` `np.array_equal`; 반환 size == `out.stat().st_size` < 50MB

    **1-c. `tests/data/test_millie_fallback.py`** (docstring `"""demo/fallback/popular.json — 계약(RecommendOut 파싱·상수 동일) · 정확성(eligible 상위 40·pop_rank 순·점수) · 안전성(format 키 없음·description 없음)."""`). import `from millie_rec.serving.schemas import RecommendOut` · `from millie_rec.serving import fallback as fb`(또는 `from millie_rec.serving.fallback import TRENDING_ROW_ID, TRENDING_TITLE, TRENDING_PURPOSE, SOURCE_POPULARITY`) · `from millie_rec.contracts import FALLBACK_GLOBAL_POP, MODEL_VERSION_FALLBACK`. module fixture `out(tmp_path_factory)`: 체인 → parquet → `fb_mod = _load("export_millie_fallback")` → `fb_mod.export_fallback(bp, tmp/"popular.json")` → `(fb_mod, json.loads(text), text)`. 7건:
    - `test_validates_as_recommend_out(out)` — `RecommendOut.model_validate(d)` 예외 없음; `d["fallback_level"] == FALLBACK_GLOBAL_POP == 3`; `d["model_version"] == MODEL_VERSION_FALLBACK`; `d["items"] == []`; `isinstance(d["latency_ms"], float)`; `d["latency_breakdown"] == {"total": 0.0}`; `d["user_state_weights"] == {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}`
    - `test_constants_match_serving_fallback(out)` — `fb_mod.TRENDING_ROW_ID == fb.TRENDING_ROW_ID` · `TRENDING_TITLE` · `TRENDING_PURPOSE` · `SOURCE_POPULARITY` 4개 동일; `d["rows"][0]["row_id"] == fb.TRENDING_ROW_ID and d["rows"][0]["title"] == fb.TRENDING_TITLE and d["rows"][0]["purpose"] == fb.TRENDING_PURPOSE`
    - `test_items_are_eligible_top_by_pop_rank(out, books)` — `items = d["rows"][0]["items"]`; `len(items) == 11`(픽스처 자격 11권 — 실제 값이 다르면 픽스처의 image_url 을 세어 상수를 맞추고 SUMMARY 에 이유); `items[0]["title"] == "불편한 편의점 (재수집)"`; `[i["book_id"] for i in items]` 가 books 의 eligible 행을 pop_rank 오름차순으로 정렬한 것과 동일; `d["rows"][0]["channel_mix"] == {"popularity": 11}`
    - `test_scores_positions_and_channels(out)` — `[i["score"] for i in items] == [float(11 - k) for k in range(11)]`, `[i["position"] for i in items] == list(range(11))`, 전부 `source == "popularity"` · `source_channels == ["popularity"]` · `badge is None` · `reason is None`
    - `test_item_keys_are_item_out_fields_only(out)` — 모든 item `set(i) == {"book_id","score","source","reason","title","authors","image_url","position","source_channels","badge","book_format","difficulty"}`(`format` 없음)
    - `test_no_author_text_leaks(out)` — `text.count('"description"') == 0 and text.count('"curator_note"') == 0` 그리고 픽스처 description 첫 20자 없음
    - `test_n_cap_and_nan_difficulty_null(out, fb_mod, books)` — `fb_mod.payload(books, n=3)["rows"][0]["items"]` 길이 3; category_prior 책이 items 에 있으면 `difficulty is None`, millie_index 책은 `0 < difficulty < 1`

    **1-d. RED:** `uv run pytest tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py --no-header 2>&1 | tail -40` → 12 failed, `AssertionError`(스텁의 빈 npz/빈 dict — `d["fallback_level"]` KeyError 가 먼저 나면 그 테스트 첫 줄에 `assert "fallback_level" in d` 를 둔다). `ImportError`·`SyntaxError` 0.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test -f scripts/export_millie_vectors.py && test -f scripts/export_millie_fallback.py && uv run pytest tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/data/test_millie_vectors.py` == 5; `grep -c "def test_" tests/data/test_millie_fallback.py` == 7
    - `grep -c "RecommendOut.model_validate" tests/data/test_millie_fallback.py` ≥ 1; `grep -c "TRENDING_TITLE" tests/data/test_millie_fallback.py` ≥ 1
    - RED 출력에 `AssertionError`/`E  assert` 가 있고 `ImportError`·`ModuleNotFoundError`·`SyntaxError`·`NameError` 없음
    - `grep -c "SVD_DIM = 128" scripts/export_millie_vectors.py` == 1; `grep -c 'N_ITEMS = 40' scripts/export_millie_fallback.py` == 1; `grep -c "millie_rec.serving\|millie_rec.data\|millie_rec.app" scripts/export_millie_vectors.py scripts/export_millie_fallback.py` → 0 0
    - `git status --short demo artifacts` → 출력 없음
  </acceptance_criteria>
  <done>12건 RED. 두 스텁이 contracts 만 import. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): SVD 벡터 export + RecommendOut 형태 popular.json export 구현 → 12 passed, 두 파일 ≤150줄</name>
  <files>scripts/export_millie_vectors.py, scripts/export_millie_fallback.py</files>
  <read_first>
    - 두 스텁 · 두 테스트 파일(단정이 계약)
    - scripts/export_millie_serving.py `_clean`(L43-53) — 같은 함수를 fallback 스크립트에 **복사**(scripts 간 import 회피)
    - src/millie_rec/serving/fallback.py L21-45 (점수·행 형태 정본)
  </read_first>
  <action>
    **2-a. `export_millie_vectors.py`:**
    ```python
    from sklearn.decomposition import TruncatedSVD
    from sklearn.feature_extraction.text import TfidfVectorizer
    from sklearn.preprocessing import normalize

    def _text(v: object) -> str: return v if isinstance(v, str) and v else ""
    def build_texts(books: pd.DataFrame) -> list[str]:
        return [" ".join(p for p in (_text(r[c]) for c in TEXT_COLS) if p) for r in books[list(TEXT_COLS)].to_dict("records")]
    def vectors(texts: list[str]) -> np.ndarray:
        """TF-IDF(char_wb 2~4gram) → TruncatedSVD(min(128, n-1, features-1), SEED) → 행 L2 → float32."""
        tfidf = TfidfVectorizer(analyzer="char_wb", ngram_range=NGRAM_RANGE, min_df=1).fit_transform(texts)
        dim = max(1, min(SVD_DIM, tfidf.shape[0] - 1, tfidf.shape[1] - 1))  # 소형 픽스처 가드
        reduced = TruncatedSVD(n_components=dim, random_state=SEED).fit_transform(tfidf)
        return normalize(reduced).astype(np.float32)
    def export_vectors(books_path: Path, out_path: Path) -> int:
        books = pd.read_parquet(books_path).sort_values("book_id").reset_index(drop=True)
        out_path.parent.mkdir(parents=True, exist_ok=True)
        np.savez(out_path, book_ids=books["book_id"].to_numpy(dtype=np.int64), vectors=vectors(build_texts(books)))
        size = out_path.stat().st_size
        assert size < MAX_BYTES, f"{out_path.name} {size}B ≥ {MAX_BYTES}B"
        return size
    ```
    `main` 은 `print(f"{args.out.name} {size/1024:.1f}KB dim=… → {args.out}")`. `np.savez` 는 `.npz` 확장자를 강제로 붙이므로 `out_path` 가 이미 `.npz` 로 끝나야 한다(기본값·테스트 모두 `.npz`).

    **2-b. `export_millie_fallback.py`:**
    ```python
    def _clean(value): ...  # export_millie_serving._clean 복사(NA/NaN→None, ndarray→list, numpy 스칼라→파이썬)
    def is_eligible(row: dict) -> bool:
        url = row.get("image_url") or ""
        host = urlsplit(url).hostname or ""
        return bool(row.get("title")) and host.endswith(COVER_HOST_SUFFIX) and ADULT_COVER_MARK not in url
    def _item(row: dict, position: int, n: int) -> dict:
        return {"book_id": int(row["book_id"]), "score": float(n - position), "source": SOURCE_POPULARITY, "reason": None,
                "title": _clean(row.get("title")), "authors": _clean(row.get("authors")), "image_url": _clean(row.get("image_url")),
                "position": position, "source_channels": [SOURCE_POPULARITY], "badge": None,
                "book_format": _clean(row.get("book_format")), "difficulty": _clean(row.get("difficulty"))}
    def payload(books: pd.DataFrame, n: int = N_ITEMS) -> dict:
        """서버 level 3 응답(compose.build_response + fallback.trending_row)과 같은 형태. items 평탄화 없음."""
        rows = [r for r in books.sort_values("pop_rank").to_dict("records") if is_eligible(r)][:n]
        items = [_item(r, i, len(rows)) for i, r in enumerate(rows)]
        return {"recommendation_id": RECOMMENDATION_ID, "model_version": MODEL_VERSION_FALLBACK, "preference_snapshot_id": None,
                "user_key": None, "cell": None, "forced": False, "fallback_level": FALLBACK_GLOBAL_POP, "context": None,
                "latency_ms": 0.0, "latency_breakdown": {"total": 0.0}, "user_state_weights": dict(ZERO_WEIGHTS),
                "dedup_removed": 0, "nearline_lag_s": None, "items": [],
                "rows": [{"row_id": TRENDING_ROW_ID, "title": TRENDING_TITLE, "purpose": TRENDING_PURPOSE, "items": items,
                          "subtitle": None, "channel_mix": {SOURCE_POPULARITY: len(items)} if items else {}}]}
    def export_fallback(books_path: Path, out_path: Path) -> int:
        out_path.parent.mkdir(parents=True, exist_ok=True)
        out_path.write_text(json.dumps(payload(pd.read_parquet(books_path)), ensure_ascii=False, indent=1), encoding="utf-8")
        return out_path.stat().st_size
    ```
    `pop_rank` 가 Int64 라 `sort_values` 전에 `pd.to_numeric(...).fillna(10**9)` 로 결측을 뒤로. `score=float(n - position)` 의 `n` 은 실제 선정 수(`len(rows)`, fallback.py `len(ids) - i` 관례).

    **2-c. GREEN 확인:** `uv run pytest tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py --no-header` → `12 passed`. `uv run ruff format scripts/export_millie_vectors.py scripts/export_millie_fallback.py tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py && uv run ruff check <같은 4파일>`. `wc -l` 두 스크립트 ≤150. 회귀 확인은 **자기 테스트 2파일만** — 같은 wave 의 Plan 03·04 가 `tests/data/` 안에서 RED 상태일 수 있으므로 전체 `tests/data` 실행은 Advisor 의 wave 종료 기준선(Plan 06 verification)에서만 한다.

    **2-d. 실 실행 금지:** 두 스크립트를 실데이터로 **실행하지 않는다**(`artifacts/serving/`·`demo/fallback/popular.json` 은 Plan 06 `make millie` 가 갱신). `git status --short demo artifacts` 빈 출력 확인.

    **2-e. 인계 노트(SUMMARY):** "`demo/js/inspector.js` L106·L110 `model_version === "static_popular"` 라벨은 새 파일(`fallback_v1`)과 불일치 — Phase 6 '데모 재구성'(.planning/ROADMAP.md) 에서 `MODEL_VERSION_FALLBACK` 로 교체. `latency_ms` float 화로 인스펙터 단계 막대가 비지만 크래시 없음(03-PATTERNS 데모 영향)."
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check scripts/export_millie_vectors.py scripts/export_millie_fallback.py tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py && uv run pytest tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py --no-header 2>&1 | tail -3 && [ "$(wc -l < scripts/export_millie_vectors.py)" -le 150 ] && [ "$(wc -l < scripts/export_millie_fallback.py)" -le 150 ] && [ -z "$(git status --short demo artifacts)" ] && echo "demo/artifacts untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py --no-header` → `12 passed`
    - `wc -l` 두 스크립트 ≤ 150; `grep -c "TruncatedSVD(n_components=dim, random_state=SEED)" scripts/export_millie_vectors.py` == 1; `grep -c 'analyzer="char_wb"' scripts/export_millie_vectors.py` == 1
    - `grep -c '"latency_ms": 0.0' scripts/export_millie_fallback.py` == 1; `grep -c '"format"' scripts/export_millie_fallback.py` == 0; `grep -c "MODEL_VERSION_FALLBACK" scripts/export_millie_fallback.py` ≥ 2
    - `grep -c "millie_rec.serving\|millie_rec.data\|millie_rec.app" scripts/export_millie_vectors.py scripts/export_millie_fallback.py` → 0 0
    - `git status --short demo artifacts` → 출력 없음; `git log --oneline | head -1` 불변
  </acceptance_criteria>
  <done>SVD 128 벡터 npz 와 RecommendOut 형태 popular.json 이 합성 체인으로 증명됨(12 passed). 실 산출물은 Plan 06. Phase 6 인계 노트 기록. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `books_kr.parquet` → `demo/fallback/popular.json` | 정적 파일이 클라이언트에 그대로 내려간다(캐시·공개). 밀리 저작 텍스트·외부 표지 호스트가 새는 경로 |
| `popular.json` → `demo/js/api.js` spread | 스키마가 틀리면 데모 화면 전체가 깨진다(가용성) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-18 | Information Disclosure | `_item` 필드 | mitigate | ItemOut 12키 화이트리스트만 조립 — description·curator_note 는 키·값 모두 0건 단언(`test_no_author_text_leaks`) |
| T-03-19 | Tampering | 외부 호스트 표지 삽입(정적 파일) | mitigate | `is_eligible` 인라인(D-14 규칙과 같은 값) — 40권 선정 전 필터 |
| T-03-20 | Denial of Service | 계약 불일치로 데모 크래시 | mitigate | `RecommendOut.model_validate` 통과가 완료 기준(`extra="forbid"` — `format` 키 금지) |
| T-03-21 | Repudiation | SVD 난수로 벡터가 실행마다 달라짐 | mitigate | `random_state=SEED` + 재현성 테스트(두 번 export 바이트 동일) |
| T-03-22 | Denial of Service | npz 크기 | mitigate | `size < MAX_BYTES` 단언(5,5k×128 float32 ≈ 2.8MB) |
</threat_model>

<verification>
**이 플랜(Worker)이 직접 확인하는 것:** `uv run ruff format <자기 4파일> && uv run ruff check <자기 4파일>` 클린 · `uv run pytest tests/data/test_millie_vectors.py tests/data/test_millie_fallback.py --no-header` failed 0(전체 `tests/data` 는 Advisor wave 종료 기준선) · `demo/`·`artifacts/` 무변경.

**공통 기준선(Advisor 가 wave 2 종료 시 1회 — 03-03·03-04 와 경합 방지):** `uv run ruff format --check . && uv run ruff check .` · `uv run pytest --no-header`(failed 0) · `make smoke`(`smoke: PASS`).
</verification>

<success_criteria>
- `content_vectors_kr.npz` 생성기(SVD 128·L2·float32·SEED)와 `popular.json` 생성기(RecommendOut level 3)가 12 passed
- 두 스크립트 ≤150줄, contracts 만 import, 실 산출물 무변경, 커밋 0
- Phase 6 인계 노트(`static_popular` 라벨) SUMMARY 에
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-05-SUMMARY.md` — RED 발췌 · GREEN 요약 · 두 스크립트 CLI 인자(Plan 06 Makefile `millie-export` 가 호출: `export_millie_vectors.py --books --out` · `export_millie_fallback.py --books --out`) · 픽스처 자격 수(11) · Phase 6 인계 노트 · "커밋하지 않는다".
</output>
