---
phase: 03-millie-catalog
plan: 08
type: tdd
wave: 5
depends_on: ["03-06"]
gap_closure: true
gap_source: ".planning/phases/03-millie-catalog/03-UAT.md — Test 9 issue · Gaps[0] 'GET /api/recommend 상위 항목과 카탈로그 title 이 실제 밀리 도서 제목이다'"
files_modified:
  - scripts/millie_parse.py                       # _NAV_NOISE 에 배지 라벨 7종 추가 + _NAV_NOISE_RE(`^종료 D-\d+$` 카운트다운 배지, 실측 9건) 스킵 1줄(순증 ≤8줄)
  - tests/fixtures/millie/badge_docent.txt        # NEW — book 213(35d1ad08e9c94e13) 헤더 '도슨트북 + 종이책에서 읽던 지점 바로 이어읽기'
  - tests/fixtures/millie/badge_free_chatbook.txt # NEW — book 1550(20a553027dc141c2) 헤더 '무료 + 챗북'
  - tests/data/test_millie_parse.py               # EXPECTED 2키 추가 + 배지 헤더 테스트 1건 (기존 단정 무수정)
  - scripts/build_millie_catalog.py               # BADGE_TITLES 상수 + coverage() n_badge_title·badge_titles (순증 ≤6줄, 286줄 예외 파일)
  - tests/data/test_millie_catalog.py             # 게이트 _n_badge_title 행 + 단언 · 합성 카운터 테스트 · 상수 drift 테스트 (기존 단정 무수정)
  - scripts/collect_millie.py                     # collect(only=) + _read_recollect() + --recollect (ruff format 후 순증 ≤30줄, 새 함수 1개)
  - scripts/millie_recollect_list.py              # NEW ≤60줄 — JSONL 최신 줄 기준 title ∈ BADGE_TITLES → data/raw/recollect_badge_titles.txt
  - tests/data/test_millie_recollect.py           # NEW — 목록 추출·--recollect 파일 파서·collect 시그니처
  - data/raw/recollect_badge_titles.txt           # gitignore(data/raw/*) — Task 3 산출
  - data/raw/millie_pages.jsonl                   # gitignore — Task 3 재수집 append(기존 줄 삭제 0)
  - data/processed/books_kr.parquet               # Task 3 재빌드(= 03-06 Task 4 최종 스냅샷)
  - data/processed/item_edges_kr.parquet
  - data/processed/popularity_kr.parquet
  - data/id_map.csv                               # Task 3 재빌드 시 append-only(신규 유효분만)
  - artifacts/serving/books_kr.json               # 커밋 대상(승인 후)
  - artifacts/serving/item_edges_kr.json
  - artifacts/serving/popularity_kr.json
  - artifacts/serving/content_vectors_kr.npz
  - demo/fallback/popular.json
  - results/millie_coverage.csv                   # _n_badge_title 행 신설
  - results/millie_edges_gate.json
  - report/draft.md                               # 03-06 Task 4-d 수치 교체(마커 제거)
  - ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md   # §2 최종 스냅샷 행 · §3 로그(재수집)
  - ../.assets/개발일지/2026-09-06_Day2_<주제>.md  # (또는 실행일 파일) D 항목 1건 신규 + D66 결과 줄
autonomous: false
requirements: [DATA-02, DATA-03]
lane: Task 1·2 = Data-B(scripts/·tests/data/·tests/fixtures/millie/ Opus Worker) · Task 3 = 조립(Advisor 직접 — 배치 중단·재수집 실행·make millie·문서)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md)). 커밋은 사용자 승인 후 Advisor
tdd_gate_evidence: pytest-output
must_not_touch:
  - Makefile                                      # Advisor 전용 — 이 플랜은 타깃 변경 0(재수집은 uv run 직접 호출)
  - src/millie_rec/**
  - tests/test_architecture.py
  - tests/conftest.py
  - tests/fixtures/millie/chatbook.txt            # 기존 픽스처 8건 무수정
  - tests/fixtures/millie/no_completion.txt
  - tests/fixtures/millie/no_rating.txt
  - tests/fixtures/millie/normal_1_소설.txt
  - tests/fixtures/millie/normal_2_에세이_시.txt
  - tests/fixtures/millie/normal_3_경제경영.txt
  - tests/fixtures/millie/normal_4_x.txt
  - tests/fixtures/millie/variant_header.txt
  - tests/fixtures/millie/sample_records.jsonl
  - tests/data/test_millie_edges.py
  - tests/data/test_millie_difficulty.py
  - tests/data/test_millie_popularity.py
  - tests/data/test_millie_export.py
  - scripts/millie_difficulty.py
  - scripts/build_millie_edges.py
  - scripts/build_millie_popularity.py
  - scripts/export_millie_serving.py
  - scripts/export_millie_vectors.py
  - scripts/export_millie_fallback.py
  - demo/**
  - pyproject.toml
  - Dockerfile
  - ../.claude/**
  - CLAUDE.md
  - .planning/STATE.md                            # Advisor 가 갱신
  - .planning/ROADMAP.md

must_haves:
  truths:
    - "parse_book_text 가 헤더 첫 줄이 밀리 기능 배지(읽던 지점 그대로 이어듣기·도슨트북·무료·오브제북·웹소설·웹툰·오디오웹소설)인 페이지에서도 실제 제목·부제를 title·subtitle 로 낸다 — 배지 픽스처 2건(badge_docent: '12가지 인생의 법칙 (40만 부 기념 스페셜 에디션)' / badge_free_chatbook: '따박따박 경제상식 [ETF 첫걸음]')이 기존 파서 테스트 8종 전부와 신규 배지 테스트를 통과한다"
    - "커버리지 게이트(tests/data/test_millie_catalog.py::test_coverage_gate)가 title 이 배지(BADGE_TITLES 7종 또는 `^종료 D-\\d+$`)인 카탈로그 행 수를 results/millie_coverage.csv 에 _n_badge_title 행으로 기록한다(Task 2). `assert n_badge == 0` 단언은 Task 3 재수집 직전에 Advisor 가 추가해 RED(실측 728~815+건)를 한 번 확인한 뒤 재수집·재빌드로 GREEN 이 된다 — Task 2~3 사이 전역 스위트는 green 유지(local-run.md), pytest.skip 으로 숨기지 않는다"
    - "scripts/collect_millie.py --recollect <ids.txt> 가 파일의 millie_id 만 다시 렌더해 millie_pages.jsonl 에 append 하고(멱등 skip 우회 · sitemap 미조회 · 링크 확장 없음 · 요청 간 DELAY_S 2.5초 · UA · 4xx 3연속 중단 · --shard K/N 그대로) 원래 source 를 보존한다; build_millie_catalog.read_records 가 millie_id 별 마지막 줄을 채택해 새 값이 이긴다"
    - "scripts/millie_recollect_list.py 가 JSONL(최신 줄 우선)에서 title ∈ BADGE_TITLES 인 (millie_id, source) 를 data/raw/recollect_badge_titles.txt 로 쓴다 — parquet 스냅샷이 아니라 JSONL 을 읽어 배치가 그 뒤 수집한 배지 제목도 잡는다"
    - "배치 종료(또는 Day 2 09-06 오전 사용자 승인 후 중단) → 재수집 2샤드 → make millie 1회 → 게이트(강화분 포함) 통과 → GET /api/recommend?seeds=1,2,3&k=5 level 0 상위 5 제목에 배지 라벨 0건 → report/draft.md '[숫자는 … 교체]' 마커 0건 · 트래킹 03 §2 최종 스냅샷 행 · 개발일지 D 항목 — 03-06 Task 4 의 최종 스냅샷과 한 흐름"
    - "배지 라벨 집합은 실측 7종만(추측 확장 0). 재수집·재빌드 후에도 배지 제목이 남으면 그 수치(_n_badge_title)와 잔여 제목 상위 목록을 SUMMARY 에 기록하고 사용자에게 보고한다 — 게이트를 완화하거나 skip 하지 않는다"
  artifacts:
    - path: "scripts/millie_parse.py"
      provides: "_NAV_NOISE ⊇ 배지 라벨 7종 + 기존 7종"
      contains: "오디오웹소설"
      max_lines: 275
    - path: "tests/fixtures/millie/badge_docent.txt"
      provides: "배지 헤더(도슨트북 + 종이책에서 읽던 지점 바로 이어읽기) 실페이지 축약 픽스처, 리뷰·필명 0"
      contains: "12가지 인생의 법칙"
    - path: "tests/fixtures/millie/badge_free_chatbook.txt"
      provides: "배지 헤더(무료 + 챗북) 실페이지 축약 픽스처"
      contains: "따박따박 경제상식 [ETF 첫걸음]"
    - path: "scripts/build_millie_catalog.py"
      provides: "BADGE_TITLES frozenset(7종) · coverage()['n_badge_title']"
      contains: "BADGE_TITLES = "
      max_lines: 300
    - path: "scripts/collect_millie.py"
      provides: "collect(limit, expand, shard=None, only=None) · _read_recollect(path) -> dict[str, str] · argparse --recollect"
      contains: "--recollect"
      max_lines: 325
    - path: "scripts/millie_recollect_list.py"
      provides: "badge_title_rows(records) -> list[tuple[str, str]] · main() → data/raw/recollect_badge_titles.txt"
      exports: ["badge_title_rows", "main"]
      max_lines: 60
    - path: "tests/data/test_millie_recollect.py"
      provides: "목록 추출(합성 레코드) · _read_recollect 파일 파서 · collect 시그니처 only"
      min_lines: 30
    - path: "results/millie_coverage.csv"
      provides: "최종 스냅샷 게이트 실측(_n_badge_title 포함)"
      contains: "_n_badge_title"
  key_links:
    - from: "scripts/millie_parse.py::_header"
      to: "_NAV_NOISE"
      via: "while 루프가 '로그인' 뒤 빈 줄·노이즈를 연속 스킵 — 배지 라벨이 집합에 있으면 블록 첫 줄이 실제 제목이 된다"
      pattern: "lines\\[i\\] in _NAV_NOISE"
    - from: "tests/data/test_millie_catalog.py::test_coverage_gate"
      to: "data/processed/millie_raw_coverage.json['n_badge_title']"
      via: "report.get('n_badge_title') → csv 행 기록 → assert == 0"
      pattern: "_n_badge_title"
    - from: "scripts/millie_recollect_list.py"
      to: "scripts/build_millie_catalog.py::read_records · BADGE_TITLES"
      via: "sys.path.insert(SCRIPTS_DIR) + import (03-01 SUMMARY 패턴)"
      pattern: "from build_millie_catalog import"
    - from: "scripts/collect_millie.py --recollect"
      to: "data/raw/millie_pages.jsonl (append) → build_millie_catalog.read_records 마지막 줄 우선 dedup"
      via: "collect(only={millie_id: source}) — done=set() 로 멱등 skip 우회, expand=False"
      pattern: "only"
    - from: "Task 3 make millie"
      to: "GET /api/recommend?seeds=1,2,3&k=5 rows[0].items[*].title"
      via: "load_catalog → CatalogKR → CatalogPopPipeline(03-06)"
      pattern: "\"fallback_level\":0"
---

<objective>
UAT 갭 1건 폐쇄 — 카탈로그 title 오염(배지 라벨). `scripts/millie_parse.py::_header` 가 '로그인' 뒤 선행 노이즈 집합 `_NAV_NOISE` 에 없는 밀리 기능 배지 라벨 7종을 title 로 잡아 8,708권 중 728권(8.4%, 현재 JSONL 기준 815건)이 '읽던 지점 그대로 이어듣기'·'도슨트북'·'무료' 같은 제목을 갖고, `/api/recommend` level 0 상위 5 에 2권이 노출됐다(`.planning/phases/03-millie-catalog/03-UAT.md` Test 9 · Gaps). 원문 라인은 저장하지 않으므로(raw HTML 미보관, `../.claude/rules/data.md`) 진짜 제목은 **해당 URL 재수집**으로만 복구된다 — 결정 D-06 '파서 미스 비율 ≤0.5% · 초과 시 파서 수정·해당 URL 재수집'(.planning/phases/03-millie-catalog/03-CONTEXT.md) 의 절차를 배지 제목 탐지기에 그대로 적용한다.

세 갈래: (1) **파서 수정(TDD)** — 배지 헤더 실페이지 픽스처 2건으로 RED(title == 배지 라벨) → `_NAV_NOISE` 7종 추가로 GREEN (2) **게이트 강화 + 재수집 수단** — `test_coverage_gate` 에 `title ∉ BADGE_TITLES` 단언·`_n_badge_title` 행, `collect_millie.py --recollect <ids.txt>`, JSONL 기반 대상 목록 스크립트 (3) **재수집·최종 스냅샷(사람 게이트)** — 배치 종료 후 재수집 2샤드 → `make millie` 1회 → 게이트 → 03-06 Task 4-d 수치 교체. Task 1·2 는 배치와 무관하게 지금, Task 3 만 사람 게이트 뒤.

Purpose: ROADMAP Phase 3 성공 기준 4("`/api/recommend` 가 밀리 책으로 응답")를 **제목이 실제 도서 제목인 채로** 충족시키고, DATA-02 계약 컬럼 `title` 의 의미 정합·DATA-03 게이트가 재발을 자동 탐지하게 한다. 데모 화면·본인 5권·앵커 이웃(TF-IDF 입력 = title+description)이 전부 title 위에 서므로 Phase 4·6 전에 닫아야 한다.
Output: 파서 1문장 수정 · 픽스처 2건 · 게이트 강화 · 재수집 옵션 · 목록 스크립트 · 최종 스냅샷 산출물(03-06 Task 4 와 통합) · 문서 3곳. 커밋 0.

**하지 않는 것:** 배지 제목 행을 카탈로그에서 **필터로 제거하지 않는다**(815권을 조용히 버리는 것 — 결정 D-05 유효 레코드 정의 불변, 복구는 재수집만) · 배지 라벨 추측 확장(실측 7종만, `../.claude/rules/simplicity.md` "근거 없는 상수 금지") · `pytest.skip`·임계 완화로 게이트 숨기기 · Makefile 타깃 신설(Advisor 전용, 재수집은 `uv run` 직접) · `millie_parse.py`·`collect_millie.py`·`build_millie_catalog.py` 150줄 분리(결정 D-16) · 배치가 도는 동안 재수집 프로세스 추가(총 요청률 2샤드 상한, 결정 '밀리 수집 배치를 2프로세스 샤드로 병렬화'(개발일지 2026-09-05 파일 항목 D65)).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-UAT.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-06-PLAN.md
@.planning/phases/03-millie-catalog/03-06-SUMMARY.md
@.planning/phases/03-millie-catalog/03-01-SUMMARY.md
@scripts/millie_parse.py
@scripts/collect_millie.py
@scripts/build_millie_catalog.py
@tests/data/test_millie_parse.py
@tests/data/test_millie_catalog.py
@tests/fixtures/millie/chatbook.txt
@../.claude/rules/data.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/references.md

<project_rules_inline>
**TDD(`../.claude/rules/python-tdd.md`):** "`scripts/`(밀리 적재) | 파서·게이트 함수는 fixture(`tests/fixtures/millie/`) 기반 테스트 필수. 네트워크 호출은 테스트 안 함" / "**Red는 `AssertionError`(의도한 단정 실패) 메시지로 확인**한다. `ImportError`·`SyntaxError`·collection error는 Red가 아니다" / "실행 명령 `uv run pytest <대상> --no-header` — `-q` 는 pyproject addopts 에 이미 있다. CLI 에 `-q` 를 또 붙이면 요약 줄이 사라진다" / 금지: "테스트를 통과시키려고 기존 테스트의 단정을 고치는 것" · "`pytest.skip`으로 실패를 숨기는 것".

**데이터(`../.claude/rules/data.md`):** "`millie_parse.py`(순수 함수, 라벨 기반, 위치 인덱스 금지) · `collect_millie.py`(체크포인트 JSONL, 프로세스당 요청 간 2.5초 — 병렬 2 샤드 `--shard K/N` 허용, UA `millie-rec-demo-assignment/0.1 (+eddy@cdri.pro)`, 4xx 연속 3회 중단)" / "raw HTML 미보관. 예외: `tests/fixtures/millie/` 파서 픽스처(각 ≤3KB, 리뷰·필명 제거)" / "미저장: 리뷰 텍스트·필명·큐레이터 실명" / "`data/raw/*` `data/processed/*` gitignore, `data/id_map.csv`·`results/`·`artifacts/serving/` 커밋" / "재수집·재빌드에도 기존 행 변동 0(diff 검증)".

**간결함(`../.claude/rules/simplicity.md`):** "설정은 모듈 상단 대문자 상수" / "content_sim 최소 cosine 임계는 두지 않는다(근거 없는 상수 금지)" → 배지 라벨도 실측만 / "파일 150줄 이하 … 예외" — 결정 D-16 으로 `millie_parse`(270)·`collect_millie`(294)·`build_millie_catalog`(286) 예외, **순증 최소**(collect ≤20줄, build ≤6줄, parse ≤3줄). 새 로직은 새 파일(`millie_recollect_list.py`).

**최소 변경(../CLAUDE.md §2-9):** "언급된 기능만 최소한의 코드 변경으로 … 기존 함수 시그니처·반환 타입·데이터 흐름을 바꾸지 않는다" → `collect()` 는 **기본값 있는 keyword 인자 `only=None` 추가**만(only 가 None 이면 모든 분기가 기존 코드와 동일 — 도는 배치의 재시작 경로 불변). 기존 픽스처 8건·기존 테스트 단정 무수정.

**참조 표기(`../.claude/rules/references.md`):** 번호 단독 금지 — `결정 D-06 '파서 미스 비율 ≤0.5% · 초과 시 파서 수정·해당 URL 재수집'(.planning/phases/03-millie-catalog/03-CONTEXT.md)` 형식. 개발일지 새 항목 전 `grep -h '^### D' ../.assets/개발일지/*.md | tail -3`(실측 2026-09-05 밤: 마지막 D66 → 신규는 D67).

**Advisor 전용:** Makefile · `src/**` · `../.claude/**` · `CLAUDE.md` · `.planning/STATE.md`·`ROADMAP.md` — Worker 는 수정하지 않고 보고한다.
</project_rules_inline>

<interfaces>
**현재 `scripts/millie_parse.py`(270줄) — 고칠 곳 1문장(L23):**
```python
_NAV_NOISE = _set("전자책|오디오북|챗북|종이책|종이책에서 읽던 지점 바로 이어읽기|미서비스|pdf")
```
`_header(lines)` L68-99: `start=_find(lines, eq="로그인")` → `while i < len(lines) and (not lines[i] or lines[i] in _NAV_NOISE): i += 1` → `_HEADER_END` 앵커까지 `block` → 뒤에서 pub_date·category·publisher·rating·authors pop → `title = block[0]`, `subtitle = block[1]`. **배지 라벨이 집합에 들어가면 while 이 연속으로 건너뛴다** — 실측 배지 행의 subtitle 은 실제 제목 또는 또 다른 배지 라벨(종이책에서 읽던 지점 바로 이어읽기·읽던 지점 그대로 이어듣기·미서비스·전자책·챗북·도슨트북·오디오북)이라 **설명 줄 별도 추가 불필요**(09-05 밤 parquet 실측: '도슨트북' 116건의 subtitle = 종이책에서 읽던 지점 바로 이어읽기 37 · 읽던 지점 그대로 이어듣기 18 · 미서비스 4 · 실제 제목 나머지 / '무료' 64건 = 전자책 39 · 챗북 16 · 도슨트북 7 · 오디오북 2 / '읽던 지점 그대로 이어듣기' 444건 = 미서비스 13 · 실제 제목 나머지).
`_formats` L190-202: '도서 타입 선택' 뒤 `FORMAT_TOKENS`(전자책·오디오북·챗북) 만 수집, 다른 토큰(도슨트북)에서 break. `_best` L205-224: `X 분야 BEST` 뒤 `_BEST_NOISE`(무료 포함) 스킵, `own_title` 만나면 break, (제목, 저자) 쌍 ≤10.

**배지 라벨 7종(실측, `.planning/phases/03-millie-catalog/03-UAT.md` Gaps · parquet 09-05 20:28 집계):** `읽던 지점 그대로 이어듣기` 444 · `도슨트북` 116 · `무료` 64 · `오브제북` 30 · `웹소설` 29 · `웹툰` 27 · `오디오웹소설` 18 = 728. 현재 JSONL(9,244줄, 09-05 21:1x) 기준 815건(source: best 807 · sitemap 6 · awards 2) — 배치가 돌수록 늘어난다(도는 배치는 구 파서를 메모리에 올려 두었으므로 파서 수정이 반영되지 않는다 → 재수집 대상은 **배치 종료 시점 JSONL** 에서 뽑는다).

**실페이지 헤더 라인(스크래치 `/private/tmp/claude-501/-Users-shinwonchul-Documents----------/bafe3926-70ba-4589-b7e1-d30e43022306/scratchpad/page_213.json`·`page_1550.json`, 키 `millie_id`·`lines`; Worker 는 이 두 파일만 읽어 픽스처를 만든다):**
- **page_213 (`35d1ad08e9c94e13`, 541줄):** L8 `로그인` · L9 `전자책` · L10 `오디오북` · L11 `챗북` · L12 `도슨트북` · L13 `종이책에서 읽던 지점 바로 이어읽기` · L14 `12가지 인생의 법칙 (40만 부 기념 스페셜 에디션)` · L16 `혼돈의 해독제` · L18 `조던B.피터슨 지음, 강주헌 옮김` · L20 `4.1` · L22 `메이븐` · L24 `인문` · L26 `2023.02.10` · L28 `이 책이 담긴 서재 11만+` · L30-31 `바로 읽기`/`내서재에 담기` · L34-58 `성별 · 연령별 인기 분포` 블록(0.8/10대/1.1 … 60대~/1.1, 남성·여성·(단위: %)) · L60-65 `1위`/`30대 여성`/`2위`/`30대 남성` · L70-84 `책 추천`·`책 소개`·`리뷰 189`·`밀리 완독지수`·`높음`·`완독할 확률(%)`·`마니아`·`인문 분야 평균 대비`·`완독할 확률 33%`·`평균 52% 대비`·`완독 예상 시간 384분`·`평균 105분 대비` · L128-133 `책 소개` 본문 2줄 + `책 소개 더보기` · L202-205 `종이책 출간일`/`2018.10.30`/`전자책 출간일`/`2023.02.10` · L259 `한 줄 리뷰 189` · L431-447 `인문 분야 BEST` + (오디세이아…/호메로스…), (오뒷세이아/호메로스…), (나는 왜 무기력을…/에리히프롬…) · L533-540 `도서 타입 선택`/`전자책`/`오디오북`/`챗북`/`도슨트북`/`바로 읽기`/`내서재에 담기`. **제외할 것:** L256-258·L261 이후 리뷰 영역(`별점을 남겨주세요`·`좋아요`·`팔로우`), 필명(`현명한 성인군자_1025849`·`똑똑한 주인공_1016605`), `저자 소개` 본문, 키워드·인플루언서 블록.
- **page_1550 (`20a553027dc141c2`, 386줄):** L8 `로그인` · L9 `무료` · L11 `챗북` · L13 `따박따박 경제상식 [ETF 첫걸음]` · L15 `3화. 재테크 초보도 할 수 있는 ETF 투자 첫걸음` · L17 `밀리의서재 편집부` · L19 `4.8` · L21 `밀리의 서재` · L23 `챗북` · L25 `2025.04.14` · L27 `이 책이 담긴 서재 2.2만+` · L29-30 `챗북 읽기`/`내서재에 담기` · L71-95 성별·연령 블록(1/10대/1.6 … 60대~/1.2) · L97-102 `1위`/`30대 여성`/`2위`/`20대 여성` · L107-109 `책 추천`·`책 소개`·`리뷰 23` · L122-133 `밀리 완독지수`·`높음`·`완독할 확률(%)`·`홀릭`·`챗북 분야 평균 대비`·`완독할 확률 64%`·`평균 61% 대비`·`완독 예상 시간 21분`·`평균 22분 대비` · L181-200 `책 소개` 본문(`[밀리의 서재X데일리바이트] …` 등, `\u200b` 줄은 버린다) + `책 소개 더보기` · L208-209 `전자책 출간일`/`2025.04.14` · L210 `한 줄 리뷰 23` · L281-297 `챗북 분야 BEST`/`챗북`/`챗북`/`단 3개의 미국 ETF로 은퇴하라`/`김지훈(포메뽀꼬)`/`챗북`/`무료`/`챗북`/`따박따박 경제상식 [ETF 첫걸음]`(자기 자신 — `_best` 가 own_title 에서 break) · L380-385 `도서 타입 선택`/`챗북`/`챗북 읽기`/`내서재에 담기`. **제외:** L220 이후 리뷰(`별점을 남겨주세요`·`좋아요`·`팔로우`), 필명(`일기를 쓰는 토리_82683` 등), L31-69 시리즈 목록(`총 26권` 이하 — 파서 무관, 크기 절약).

**기존 픽스처 형식(`tests/fixtures/millie/chatbook.txt`):** 1행 `# url: https://www.millie.co.kr/v4/book/<16hex>` · 2행 `# status: 200` · 3행부터 렌더 텍스트(빈 줄 유지, 리뷰·필명 제거). `test_millie_parse._parse()` 가 `# url:` 을 정규식으로 읽는다. `test_all_fixtures_present` 가 `sorted(*.txt stem) == NAMES(= sorted(EXPECTED))` 를 단언 → **픽스처를 추가하면 EXPECTED 에 키를 추가해야 한다**(기존 단정 수정이 아니다). EXPECTED 튜플 순서: `(제목, 카테고리, 출간일, 별점, 완독확률, 예상시간, 라벨, 포맷수, 서재수, 리뷰수)`.

**파라미터화 테스트 8종이 새 픽스처에도 돈다 — 픽스처가 만족해야 할 것:** `test_core_fields`(위 튜플) · `test_identity_and_types`(publisher·authors 비어 있지 않음, `"지음" not in authors.split("/")[0].split(",")[0]`) · `test_completion_block_is_all_or_nothing`(4개 전부 또는 0개) · `test_description_and_formats`(description 20~400자, formats 비어 있지 않음 ⊂ 3종) · `test_best_block`(쌍 1~10개, best_title != title) · `test_segments`(6구간 합 90~110, top_segment `\d0대~? (남성|여성)`) · `test_no_review_or_nickname_leakage`(파싱값에 `팔로우|좋아요|님의 추천|추천사입니다|별점을 남겨주세요|저자 소개` 없음, `_\d{4,}` 없음).

**`scripts/build_millie_catalog.py`(286줄) 고칠 곳:** 상단 상수 구간(L37-41 `MAX_TITLELESS_RATIO` 아래)에 `BADGE_TITLES` · `coverage(records, meta)`(L232-249) dict 에 `n_badge_title`·`badge_titles` 2키. `read_records(jsonl) -> (valid, meta)` 는 그대로(millie_id 별 **마지막 줄** 채택 — `kept[rec["millie_id"]] = rec` 덮어쓰기). `SCRIPTS_DIR`/`sys.path.insert` + `import millie_difficulty  # noqa: E402` 가 scripts 간 import 관례.

**`scripts/collect_millie.py`(294줄) 고칠 곳:** `collect(limit, expand, shard=None)` L146 · L155 `sitemap_ids = discover_from_sitemap(log)` · L156 `done = load_done()` · L159 `seen = set(sitemap_ids)` · L160 `source = dict.fromkeys(sitemap_ids, "sitemap")` · L161 `frontier = deque(i for i in sitemap_ids if i not in done)` · L164 `if DISCOVERED.exists():` · `main()` L280-290 argparse(`--limit`·`--no-expand`·`--shard`). 렌더 루프(L201-263)는 retry 2회·4xx streak·`rec.update(image_url, status, collected_at, best_links, source=source.get(mid, "sitemap"))`·JSONL append·`done.add`·DELAY_S 를 이미 갖는다 — **재수집은 이 루프를 그대로 탄다**.

**테스트 로더 관례(`tests/data/test_millie_catalog.py::_load`):** `importlib.util.spec_from_file_location(name, ROOT/"scripts"/f"{name}.py")` → `sys.modules[name] = module` → exec. `collect_millie` 는 모듈 상단에서 playwright 를 import 하지 않는다(`collect()` 안에서만) → 테스트에서 로드 가능.

**게이트 테스트(`test_coverage_gate` L373-410):** `REAL_BOOKS`·`REAL_COVERAGE` 없으면 skip → `report = json.loads(REAL_COVERAGE)` → csv 기록(`_n_records`·`_category_distinct`·`_categories_ge_20`·`_n_skipped_empty`·`_n_skipped_titleless`·`_n_success`) → 단언(THRESHOLDS·카테고리·titleless ≤0.005). 테스트 파일 상단 상수는 스크립트를 import 하지 않고 손으로 적는 패턴(`THRESHOLDS`·`MIN_*`).

**`results/millie_coverage.csv` 현재(09-05 20:28):** `title,1.0000` … `_n_records,8708` · `_n_skipped_empty,31` · `_n_skipped_titleless,1` · `_n_success,8740`.

**배치 상태(09-05 21:1x 실측):** `pgrep -fl collect_millie` → 2샤드(`--shard 0/2`·`1/2`) + `caffeinate -i -s` 래퍼 2개 진행 중 · JSONL 9,244줄 · discovered 8,810줄. 배치 종료 감시 루프가 별도 셸에서 돌고 있다(`PASS2_DONE` 출력).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 파서 — 배지 헤더 픽스처 2건 RED(title == 배지 라벨) → `_NAV_NOISE` 7종 + `_NAV_NOISE_RE`(`종료 D-N`) 추가 GREEN, 기존 파서 테스트 무수정</name>
  <files>tests/fixtures/millie/badge_docent.txt, tests/fixtures/millie/badge_free_chatbook.txt, tests/data/test_millie_parse.py, scripts/millie_parse.py</files>
  <read_first>
    - scripts/millie_parse.py L18-35(`_set`·`_NAV_NOISE`·`_HEADER_END`·`_BEST_NOISE`) · L68-99(`_header`) · L190-224(`_formats`·`_best`)
    - tests/data/test_millie_parse.py 전문(EXPECTED 형식·파라미터화 8종·LEAKS)
    - tests/fixtures/millie/chatbook.txt (형식 본보기: `# url:`·`# status:` 2줄 + 본문, 빈 줄 유지)
    - 스크래치 `page_213.json`·`page_1550.json`(위 interfaces 의 라인 번호 — 픽스처 재료. 이 두 파일 외 탐색 금지)
    - ../.claude/rules/python-tdd.md "Red는 AssertionError" · ../.claude/rules/data.md "픽스처 각 ≤3KB, 리뷰·필명 제거"
  </read_first>
  <behavior>
    - `_parse("badge_docent")`: title `"12가지 인생의 법칙 (40만 부 기념 스페셜 에디션)"` · subtitle `"혼돈의 해독제"` · authors `"조던B.피터슨, 강주헌 옮김"`(`지음` 제거) · publisher `"메이븐"` · category `"인문"` · pub_date `"2023.02.10"` · average_rating 4.1 · completion_prob 33 · expected_min 384 · millie_label `"마니아"` · formats `["전자책","오디오북","챗북"]`(도슨트북에서 break, 3개) · shelf_count 110000 · review_count 189 · top_segment `"30대 여성"` · seg_dist 6구간(합 99.4) · description 20~400자(`2018년 전 세계를 뒤흔든 …` 시작) · best_titles ≥1 쌍(첫 쌍 `("오디세이아 (영화 <오디세이> 원작)", "호메로스 지음 / 페테르 파울 루벤스 외 그림 / 박문재 옮김")`)
    - `_parse("badge_free_chatbook")`: title `"따박따박 경제상식 [ETF 첫걸음]"` · subtitle `"3화. 재테크 초보도 할 수 있는 ETF 투자 첫걸음"` · authors `"밀리의서재 편집부"` · publisher `"밀리의 서재"` · category `"챗북"` · pub_date `"2025.04.14"` · average_rating 4.8 · completion_prob 64 · expected_min 21 · millie_label `"홀릭"` · formats `["챗북"]` · shelf_count 22000 · review_count 23 · top_segment `"30대 여성"` · description 20~400자(`[밀리의 서재X데일리바이트]` 시작) · best_titles == `[("단 3개의 미국 ETF로 은퇴하라", "김지훈(포메뽀꼬)")]`(자기 제목에서 break, `무료`·`챗북` 은 `_BEST_NOISE` 스킵)
    - 두 픽스처 모두 `title not in BADGE_LABELS` · `subtitle not in BADGE_LABELS`(BADGE_LABELS = 테스트 파일 상단 튜플 7종)
    - **카운트다운 배지(Advisor 결정, 실측 9건 — title `종료 D-4`/`종료 D-5`, subtitle 이 실제 제목):** `badge_docent` 픽스처 텍스트의 `로그인` 바로 다음 줄에 `종료 D-4` 를 끼워 넣은 문자열(테스트 안에서 합성 — 픽스처 파일 추가 없음)을 `parse_book_text(text, url)` 에 넣어도 title·subtitle 이 위와 같다
    - 기존 픽스처 8건의 EXPECTED 값·단정 전부 그대로 통과(`_NAV_NOISE` 확장은 '제목 앞' 구간에서만 작동 — 실제 제목이 배지 라벨과 같은 책은 없다)
    - 파일 크기: 두 픽스처 각 ≤3,072 bytes · 본문에 `팔로우|좋아요|별점을 남겨주세요|저자 소개` 줄 0 · `_\d{4,}` 0
  </behavior>
  <action>
    **1-a. 픽스처 2건(먼저).** 스크래치 json 의 `lines` 에서 interfaces 에 적힌 라인 번호 구간만 골라 이어 붙인다(구간 사이는 빈 줄 1개, 원문 빈 줄은 그대로). 1행 `# url: https://www.millie.co.kr/v4/book/35d1ad08e9c94e13`(1550 은 `20a553027dc141c2`), 2행 `# status: 200`, 3행부터 본문. **반드시 포함:** 헤더(`로그인` ~ `내서재에 담기`) · 성별·연령 블록(`성별 · 연령별 인기 분포` ~ `(단위: %)`) · `1위`~`2위` 4줄 · `책 추천`·`책 소개`·`리뷰 N` 3줄 · 완독지수 블록(`밀리 완독지수` ~ `평균 N분 대비`, 라벨 줄 `마니아`/`홀릭` 과 `X 분야 평균 대비` 포함) · 두 번째 `책 소개` + 본문 1~2줄 + `책 소개 더보기` · `전자책 출간일` + 날짜 · `한 줄 리뷰 N` · `X 분야 BEST` 블록(213 은 쌍 3개까지, 1550 은 L282-297 그대로 — 자기 제목 줄 포함) · `도서 타입 선택` 블록 끝까지. **반드시 제외:** 리뷰 영역·필명·`저자 소개`·`키워드 PICK`·인플루언서·시리즈 목록(1550 L31-69)·`\u200b` 줄. 저장 후 `wc -c tests/fixtures/millie/badge_*.txt` 각 ≤3072, `grep -c "팔로우\|좋아요\|별점을 남겨주세요\|저자 소개" tests/fixtures/millie/badge_*.txt` 각 0, `grep -cE "_[0-9]{4,}" …` 각 0.

    **1-b. RED — `tests/data/test_millie_parse.py`.** (i) 모듈 상단 `LEAKS` 아래에 `BADGE_LABELS = ("읽던 지점 그대로 이어듣기", "도슨트북", "무료", "오브제북", "웹소설", "웹툰", "오디오웹소설")  # 실측 7종(03-UAT Gaps) — 추측 확장 금지`. (ii) `EXPECTED` 에 2키 추가(기존 8키 한 글자도 바꾸지 않는다): `"badge_docent": ("12가지 인생의 법칙 (40만 부 기념 스페셜 에디션)", "인문", "2023.02.10", 4.1, 33, 384, "마니아", 3, 110000, 189)` · `"badge_free_chatbook": ("따박따박 경제상식 [ETF 첫걸음]", "챗북", "2025.04.14", 4.8, 64, 21, "홀릭", 1, 22000, 23)`. `NAMES = sorted(EXPECTED)` 가 자동으로 10건이 된다. (iii) 신규 테스트 1건(파일 끝):
    ```python
    @pytest.mark.parametrize(
        "name,title,subtitle",
        [
            ("badge_docent", "12가지 인생의 법칙 (40만 부 기념 스페셜 에디션)", "혼돈의 해독제"),
            ("badge_free_chatbook", "따박따박 경제상식 [ETF 첫걸음]", "3화. 재테크 초보도 할 수 있는 ETF 투자 첫걸음"),
        ],
    )
    def test_badge_header_lines_are_not_title(name: str, title: str, subtitle: str) -> None:
        """헤더 첫 줄이 기능 배지(도슨트북·무료 …)인 페이지 — 배지는 title·subtitle 이 아니다 (03-UAT Test 9 gap)."""
        d = _parse(name)
        assert d["title"] == title
        assert d["subtitle"] == subtitle
        assert d["title"] not in BADGE_LABELS and d["subtitle"] not in BADGE_LABELS


    def test_countdown_badge_line_is_not_title() -> None:
        """'종료 D-N' 카운트다운 배지(실측 9건, 09-05) — 정규식 스킵. 픽스처 텍스트에 한 줄을 끼워 합성."""
        text = (FIXTURES / "badge_docent.txt").read_text(encoding="utf-8").replace("로그인\n", "로그인\n종료 D-4\n", 1)
        d = parse_book_text(text, "https://www.millie.co.kr/v4/book/35d1ad08e9c94e13")
        assert d["title"] == "12가지 인생의 법칙 (40만 부 기념 스페셜 에디션)"
        assert d["subtitle"] == "혼돈의 해독제"
    ```
    (`FIXTURES`·`parse_book_text` 이름은 현 테스트 파일의 것을 그대로 쓴다 — 다르면 맞춘다.)
    `uv run pytest tests/data/test_millie_parse.py --no-header 2>&1 | tail -25` → 기대: `test_badge_header_lines_are_not_title` 2건 + `test_core_fields[badge_*]` 2건이 **`AssertionError`** 로 failed(`test_best_block[badge_free_chatbook]` 은 수정 전에도 `무료` 가 `_BEST_NOISE` 라 통과 — RED 목록에 없어도 정상), 기존 8픽스처 케이스 전부 passed. `assert d["title"] == title` 의 실제값이 `'도슨트북'` / `'무료'` 인 줄을 SUMMARY 에 발췌. ImportError·collection error 0.

    **1-c. GREEN — `scripts/millie_parse.py` L22-23 만.** 주석 1줄 갱신 + 집합 확장(100자 줄 제한 — 두 문자열을 `|` 로 잇거나 `_set(...) | _set(...)`):
    ```python
    # 제목 앞 네비·배지 잡음 (챗북·전자책은 카테고리 자리에도 오므로 '제목 앞' 구간에서만 버린다)
    # + 기능 배지 라벨 7종(실측 2026-09-05, 03-UAT Test 9): 헤더 첫 줄이 배지면 title 이 오염된다
    _NAV_NOISE = _set("전자책|오디오북|챗북|종이책|종이책에서 읽던 지점 바로 이어읽기|미서비스|pdf") | _set(
        "읽던 지점 그대로 이어듣기|도슨트북|무료|오브제북|웹소설|웹툰|오디오웹소설"
    )
    ```
    그리고 `_NAV_NOISE_RE = re.compile(r"^종료 D-\d+$")  # 한정 이벤트 카운트다운 배지(실측 9건, 09-05)` 를 `_NAV_NOISE` 아래에 두고, `_header` 의 선행 스킵 while 조건을 `(not lines[i] or lines[i] in _NAV_NOISE or _NAV_NOISE_RE.match(lines[i]))` 로 **한 줄만** 바꾼다. 다른 함수·상수 무변경(`_BEST_NOISE` 에 이미 `무료` 있음). `uv run pytest tests/data/test_millie_parse.py --no-header` → 전부 passed(기존 8픽스처 × 8종 + 신규). `uv run ruff format scripts/millie_parse.py tests/data/test_millie_parse.py && uv run ruff check scripts/millie_parse.py tests/data/test_millie_parse.py`.

    **1-d. 회귀 확인:** `uv run pytest tests/data --no-header 2>&1 | tail -3` — 이 시점에 `test_coverage_gate` 는 아직 Task 2 전이라 통과(파서 수정은 JSONL 기존 레코드에 영향 없음). `wc -l scripts/millie_parse.py` ≤ 275.
  </action>
  <verify>
    <automated>set -o pipefail; cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check scripts/millie_parse.py tests/data/test_millie_parse.py && uv run pytest tests/data/test_millie_parse.py --no-header 2>&1 | tail -2 && [ "$(wc -c < tests/fixtures/millie/badge_docent.txt)" -le 3072 ] && [ "$(wc -c < tests/fixtures/millie/badge_free_chatbook.txt)" -le 3072 ] && [ "$(grep -c '팔로우\|좋아요\|별점을 남겨주세요\|저자 소개' tests/fixtures/millie/badge_docent.txt tests/fixtures/millie/badge_free_chatbook.txt | awk -F: '{s+=$2} END {print s}')" = "0" ] && grep -c "오디오웹소설" scripts/millie_parse.py && [ "$(wc -l < scripts/millie_parse.py)" -le 275 ]</automated>
  </verify>
  <acceptance_criteria>
    - RED 출력(SUMMARY 발췌)에 `test_badge_header_lines_are_not_title` 2건 `AssertionError`(실제값 `'도슨트북'`·`'무료'`), `ImportError`·`Interrupted` 0; GREEN: `uv run pytest tests/data/test_millie_parse.py --no-header` failed 0
    - `ls tests/fixtures/millie/*.txt | wc -l` == 10; 각 배지 픽스처 ≤3,072 bytes; 1행 `# url: https://www.millie.co.kr/v4/book/35d1ad08e9c94e13` / `…/20a553027dc141c2`, 2행 `# status: 200`
    - `grep -c "오디오웹소설" scripts/millie_parse.py` == 1(`_NAV_NOISE` 정의 안); `grep -c '_NAV_NOISE_RE = re.compile' scripts/millie_parse.py` == 1; `git diff --numstat scripts/millie_parse.py` 추가 ≤8 · 삭제 ≤3; `_header` 본문 diff = while 조건 1줄만, `_formats`·`_best` diff 0; `grep -c "def test_" tests/data/test_millie_parse.py` 가 기존 + 2
    - `git diff tests/data/test_millie_parse.py | grep "^-" | grep -vc "^---"` == 0(삭제 줄 0 — 기존 EXPECTED 8키·단정 무수정); `grep -c "BADGE_LABELS = (" tests/data/test_millie_parse.py` == 1
    - 기존 픽스처 8건 `git status --short tests/fixtures/millie/` 에 `M` 0
  </acceptance_criteria>
  <done>배지 헤더 실페이지 2건이 파서 회귀 픽스처로 남고, `_NAV_NOISE` 7종 추가로 title·subtitle 이 실제 제목이 됨. 기존 테스트·픽스처 무수정. 커밋 없음.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 게이트 강화(`_n_badge_title` 행 기록 — 단언은 Task 3-b0) · 재수집 대상 목록 스크립트 · `collect_millie.py --recollect`(only 모드, ruff format 후 순증 ≤30줄)</name>
  <files>scripts/build_millie_catalog.py, tests/data/test_millie_catalog.py, scripts/millie_recollect_list.py, scripts/collect_millie.py, tests/data/test_millie_recollect.py</files>
  <read_first>
    - scripts/build_millie_catalog.py L37-41(상수) · L120-150(`read_records`) · L232-249(`coverage`)
    - tests/data/test_millie_catalog.py L1-95(상수·`_load`·fixture) · L296-345(`_extra_lines`·`test_valid_record_filter_counts_empty_and_titleless` — 합성 레코드 추가 관례) · L373-410(`test_coverage_gate`)
    - scripts/collect_millie.py L146-176(`collect` 진입부) · L280-290(`main`)
    - .planning/phases/03-millie-catalog/03-01-SUMMARY.md "scripts 간 import" 패턴
    - ../.claude/rules/simplicity.md 설정=대문자 상수 · ../.claude/rules/data.md 완화 규칙(2.5초·UA·4xx 3회)
  </read_first>
  <behavior>
    - `build_millie_catalog.BADGE_TITLES == frozenset({"읽던 지점 그대로 이어듣기","도슨트북","무료","오브제북","웹소설","웹툰","오디오웹소설"})` + `BADGE_TITLE_RE = re.compile(r"^종료 D-\d+$")` + `def is_badge_title(title) -> bool`(집합 또는 정규식); `coverage(records, meta)["n_badge_title"]` = `is_badge_title(title)` 인 레코드 수, `["badge_titles"]` = sorted 리스트. 목록 스크립트(`millie_recollect_list.badge_title_rows`)도 `is_badge_title` 을 쓴다(순증 상한 ≤7 → ≤12 로 조정)
    - 합성: FIXTURE 12건 + status 200·title `"도슨트북"`·shelf_count 있음 레코드 1건 → `build()` 후 `millie_raw_coverage.json["n_badge_title"] == 1`, 그 레코드는 **parquet 행으로 남는다**(필터하지 않는다 — 복구는 재수집), `n_records == 13`
    - drift: `set(catalog.BADGE_TITLES) == set(BADGE_TITLES_TEST)` · `_load("millie_parse")._NAV_NOISE >= set(BADGE_TITLES_TEST)`
    - 실데이터 게이트: `results/millie_coverage.csv` 에 `_n_badge_title,<n>` 행 기록만(현 산출물은 report 에 키 없음 → `-1`). **`assert n_badge == 0` 은 이 태스크에서 넣지 않는다** — Task 3-b0 에서 Advisor 가 추가(전역 스위트 green 유지, 체커 WARNING 1)
    - `millie_recollect_list.badge_title_rows(records)` → `[(millie_id, source)]` millie_id 사전순, title ∉ BADGE_TITLES 제외, source 없으면 `"recollect"`; `main()` 이 `data/raw/recollect_badge_titles.txt` 에 `millie_id\tsource\n` 로 쓰고 `recollect targets=<n> / valid=<N> → <path>` 출력
    - `collect_millie._read_recollect(path)` → `{millie_id: source}`; 빈 줄·`#` 주석 무시, 두 번째 열 없으면 `"recollect"`; `inspect.signature(collect_millie.collect).parameters` 에 `only`(기본 None); `python scripts/collect_millie.py --help` 에 `--recollect`
  </behavior>
  <action>
    **2-a. RED — 테스트 먼저(3곳).**
    (i) `tests/data/test_millie_catalog.py`: 상단 상수 구간(`MIN_CATEGORIES_WITH_ENOUGH_BOOKS` 아래)에 `BADGE_TITLES = ("읽던 지점 그대로 이어듣기", "도슨트북", "무료", "오브제북", "웹소설", "웹툰", "오디오웹소설")  # 스크립트 상수를 손으로 복사(게이트 독립) — 실측 7종, 03-UAT Test 9`. 신규 테스트 2건(`test_valid_record_filter_counts_empty_and_titleless` 아래):
    ```python
    BADGE_ID = "ffffffff00000003"
    COUNTDOWN_ID = "ffffffff00000004"  # 카운트다운 배지("종료 D-N") — Advisor 결정 09-05, 실측 9건

    def test_raw_coverage_counts_badge_titles_but_keeps_rows(tmp_path, catalog):
        """title 이 배지 라벨인 유효 레코드는 카운터(n_badge_title)로 드러내되 행은 남긴다 — 복구는 재수집(03-UAT gap)."""
        raw = tmp_path / "raw"; raw.mkdir()
        badge = {"millie_id": BADGE_ID, "status": 200, "title": "도슨트북", "subtitle": "종이책에서 읽던 지점 바로 이어읽기",
                 "category": "인문", "shelf_count": 110000, "image_url": "https://img.millie.co.kr/x.jpg",
                 "collected_at": "2026-09-05T02:00:00+00:00", "source": "best:0f1e2d3c4b5a6001",
                 "url": f"https://www.millie.co.kr/v4/book/{BADGE_ID}"}
        countdown = {**badge, "millie_id": COUNTDOWN_ID, "title": "종료 D-4", "subtitle": "야간비행",
                     "category": "소설", "shelf_count": 3000, "url": f"https://www.millie.co.kr/v4/book/{COUNTDOWN_ID}"}
        jsonl = raw / "millie_pages.jsonl"
        jsonl.write_text(FIXTURE.read_text("utf-8").rstrip("\n") + "\n" + json.dumps(badge, ensure_ascii=False) + "\n"
                         + json.dumps(countdown, ensure_ascii=False) + "\n", encoding="utf-8")
        (raw / "discovered_urls.txt").write_text(f"{BADGE_ID}\n{COUNTDOWN_ID}\n", encoding="utf-8")
        out = tmp_path / "processed"
        df = catalog.build(jsonl, out, raw_dir=raw)
        report = json.loads((out / "millie_raw_coverage.json").read_text("utf-8"))
        assert report.get("n_badge_title") == 2  # "도슨트북" 1(집합) + "종료 D-4" 1(정규식)
        assert sorted(report.get("badge_titles", [])) == sorted(BADGE_TITLES)
        assert {BADGE_ID, COUNTDOWN_ID} <= set(df["millie_id"]) and report["n_records"] == 14

    def test_badge_title_constants_do_not_drift(catalog):
        """빌더 상수 == 테스트 상수, 파서 _NAV_NOISE 가 그 집합을 포함 — 세 곳 중복 정의의 drift 방지."""
        assert set(catalog.BADGE_TITLES) == set(BADGE_TITLES)
        assert _load("millie_parse")._NAV_NOISE >= set(BADGE_TITLES)
    ```
    (`catalog.build` 시그니처·`FIXTURE` 경로는 기존 테스트 그대로; 필드 형식은 `_extra_lines()` 의 titleless 레코드를 본뜬다.) `test_coverage_gate` 안: csv `writer.writerow(["_n_success", …])` **바로 아래**에 `writer.writerow(["_n_badge_title", report.get("n_badge_title", -1)])` **기록 1줄만** 추가한다. 단언(`assert n_badge == 0`)은 **이 태스크에서 넣지 않는다** — Task 3-b0 에서 Advisor 가 재수집 직전에 추가한다(전역 스위트를 하루 동안 red 로 두지 않기 위해 — local-run.md "모든 브리프 완료 기준 pytest 통과"). 기존 단정·csv 행 한 글자도 바꾸지 않는다.
    (ii) `tests/data/test_millie_recollect.py`(신규, docstring `"""03-UAT Test 9 gap — 재수집 대상 목록(millie_recollect_list) · --recollect 파일 파서 · collect(only=) 시그니처. 네트워크 0."""`): `_load` 는 `test_millie_catalog.py` 의 것을 복제(4줄). 테스트 4건:
    - `test_badge_title_rows_picks_only_badge_titles_sorted`: records `[{"millie_id":"b","title":"도슨트북","source":"best:x"}, {"millie_id":"a","title":"무료"}, {"millie_id":"c","title":"불편한 편의점","source":"sitemap"}, {"millie_id":"d","title":"종료 D-5","source":"best:y"}]` → `[("a","recollect"),("b","best:x"),("d","best:y")]`
    - `test_recollect_list_main_writes_tsv(tmp_path, monkeypatch, capsys)`: FIXTURE 12건 + 배지 레코드 1건(위 (i) 의 `badge` dict 재사용 — 같은 파일 안 상수) 을 tmp jsonl 로 쓰고 `monkeypatch.setattr(sys, "argv", ["x", "--jsonl", str(jsonl), "--out", str(out)])` → `mod.main()` → `out.read_text() == "ffffffff00000003\tbest:0f1e2d3c4b5a6001\n"`, stdout 에 `recollect targets=1`
    - `test_read_recollect_parses_id_and_source(tmp_path)`: 파일 `"aaaa\tbest:x\n\n# c\nbbbb\n"` → `{"aaaa": "best:x", "bbbb": "recollect"}`
    - `test_collect_accepts_only_keyword`: `p = inspect.signature(mod.collect).parameters; assert "only" in p and p["only"].default is None`
    **RED 를 AssertionError 로 만들기 위한 스텁(테스트 작성 직후, 구현 전):** `scripts/millie_recollect_list.py` 는 `badge_title_rows(records): return []`·`main(): pass` 빈 스텁으로 먼저 만들고, `scripts/collect_millie.py` 에는 `def _read_recollect(path): return {}` 빈 스텁만 먼저 둔다(시그니처 `only` 는 GREEN 에서). 각 recollect 테스트 첫 단정은 `assert hasattr(mod, "_read_recollect")` / `assert out.exists()` 처럼 존재 검사로 시작해 AttributeError·FileNotFoundError 가 아닌 AssertionError 로 떨어지게 한다. `uv run pytest tests/data/test_millie_catalog.py tests/data/test_millie_recollect.py --no-header 2>&1 | tail -30` → 기대 RED: `test_raw_coverage_counts_badge_titles_but_keeps_rows`(`assert None == 1`) · `test_badge_title_constants_do_not_drift`(`AttributeError` 가 나면 **먼저 `BADGE_TITLES = frozenset()` 빈 스텁**을 스크립트에 넣어 AssertionError 로 만든다) · `test_coverage_gate`(`배지 라벨 title -1건`) · recollect 4건(`_load("millie_recollect_list")` 는 파일이 없으면 FileNotFoundError → **빈 `main`·`badge_title_rows` 스텁 파일을 먼저 만들어** AssertionError 로) — collection error 0. 출력 SUMMARY 발췌.

    **2-b. GREEN — `scripts/build_millie_catalog.py`(순증 ≤12줄):** `MAX_TITLELESS_RATIO` 아래
    ```python
    # 03-UAT Test 9: 헤더 첫 줄이 기능 배지인 페이지의 title 오염(실측 7종, 2026-09-05). 카탈로그에서 걸러내지 않고
    # 카운터로 드러낸다 — 복구는 해당 URL 재수집(D-06 절차). millie_parse._NAV_NOISE 와 값 중복(scripts 간 import 함정)
    BADGE_TITLES = frozenset("읽던 지점 그대로 이어듣기|도슨트북|무료|오브제북|웹소설|웹툰|오디오웹소설".split("|"))
    BADGE_TITLE_RE = re.compile(r"^종료 D-\d+$")  # 한정 이벤트 카운트다운 배지(실측 9건, 09-05) — millie_parse._NAV_NOISE_RE 와 값 중복


    def is_badge_title(title: object) -> bool:
        """title 이 기능 배지 라벨(집합 7종) 또는 카운트다운 배지(정규식)인가 — 게이트·재수집 목록이 공유."""
        return isinstance(title, str) and (title in BADGE_TITLES or bool(BADGE_TITLE_RE.match(title)))
    ```
    (`import re` 가 없으면 stdlib 블록에 추가.) `coverage()` dict 의 `"max_titleless_ratio"` 뒤에 `"n_badge_title": sum(1 for r in records if is_badge_title(r.get("title"))),` · `"badge_titles": sorted(BADGE_TITLES),`. `read_records`·`_row`·`build` 무변경. 순증 ≤12줄.

    **2-c. GREEN — `scripts/millie_recollect_list.py`(신규 ≤60줄):**
    ```python
    """배지 제목 재수집 대상 목록 — JSONL 최신 줄 기준 title ∈ BADGE_TITLES 인 millie_id\tsource (03-UAT Test 9 gap).

    parquet 스냅샷이 아니라 JSONL 을 읽는다: 배치는 구 파서를 메모리에 올린 채 돌아 그 뒤 수집분에도 배지 제목이 있다.
    출력은 collect_millie.py --recollect 의 입력. source 열을 보존해 재수집 레코드의 출처가 바뀌지 않게 한다.
    """
    import argparse, sys  # (ruff I 규칙에 맞춰 한 줄씩)
    from pathlib import Path
    from millie_rec.contracts import DIR_RAW
    SCRIPTS_DIR = str(Path(__file__).resolve().parent)
    if SCRIPTS_DIR not in sys.path: sys.path.insert(0, SCRIPTS_DIR)
    from build_millie_catalog import is_badge_title, read_records  # noqa: E402

    OUT_DEFAULT = DIR_RAW / "recollect_badge_titles.txt"

    def badge_title_rows(records: list[dict]) -> list[tuple[str, str]]:
        """title 이 배지 라벨인 (millie_id, source), millie_id 사전순. source 없으면 'recollect'."""
        return sorted((r["millie_id"], r.get("source") or "recollect") for r in records if is_badge_title(r.get("title")))

    def main() -> None:
        ap = argparse.ArgumentParser(description="배지 제목 재수집 대상 목록")
        ap.add_argument("--jsonl", type=Path, default=DIR_RAW / "millie_pages.jsonl")
        ap.add_argument("--out", type=Path, default=OUT_DEFAULT)
        args = ap.parse_args()
        records, _ = read_records(args.jsonl)
        rows = badge_title_rows(records)
        args.out.parent.mkdir(parents=True, exist_ok=True)
        args.out.write_text("".join(f"{m}\t{s}\n" for m, s in rows), encoding="utf-8")
        print(f"recollect targets={len(rows)} / valid={len(records)} → {args.out}")

    if __name__ == "__main__":
        main()
    ```
    (`read_records` 는 `build_millie_catalog` 를 import 하므로 pandas·`millie_difficulty` 도 로드된다 — dev 스크립트라 허용.)

    **2-d. GREEN — `scripts/collect_millie.py`(ruff format 후 순증 ≤30줄 — 시그니처 래핑·add_argument 3줄·`_read_recollect` 10줄 포함, 새 함수 1개, `only=None` 이면 기존 경로와 바이트 동일한 동작):**
    - `load_done()` 아래에 새 함수:
    ```python
    def _read_recollect(path: Path) -> dict[str, str]:
        """--recollect 파일(`millie_id[\\tsource]` 한 줄씩) → {millie_id: source}. 빈 줄·# 주석 무시."""
        out: dict[str, str] = {}
        for line in path.read_text(encoding="utf-8").splitlines():
            parts = line.strip().split("\t")
            if parts[0] and not parts[0].startswith("#"):
                out[parts[0]] = parts[1] if len(parts) > 1 and parts[1] else "recollect"
        return out
    ```
    - `collect` 시그니처 `def collect(limit: int | None, expand: bool, shard: tuple[int, int] | None = None, only: dict[str, str] | None = None) -> None:`(100자 넘으면 줄바꿈), docstring 끝에 1줄 `only={millie_id: source}: 그 id 만 다시 렌더한다 — sitemap 미조회·멱등 skip 우회(03-UAT Test 9 재수집). 예절(DELAY_S·UA·4xx)은 동일 루프.` 진입부 5줄을 다음으로 **치환**(추가 아님):
    ```python
    expand = expand and not only  # only 모드는 확장 금지(main 뿐 아니라 함수 자체가 보장)
    sitemap_ids = [] if only else discover_from_sitemap(log)
    done = set() if only else load_done()  # only: 재수집이 목적 — 기존 줄을 skip 하지 않는다(append → read_records 마지막 줄 우선)
    if only:
        log(f"recollect 모드: {len(only)}건 대상(멱등 skip 우회·확장 없음)")
    else:
        log(f"체크포인트: 기존 {len(done)}건 수집됨 → skip")
    seen = set(sitemap_ids) | set(only or ())
    source = dict.fromkeys(sitemap_ids, "sitemap") | dict(only or {})
    frontier: deque[str] = deque(i for i in (only or sitemap_ids) if i not in done)
    ```
    `if DISCOVERED.exists():` → `if DISCOVERED.exists() and not only:`. 렌더 루프(retry·4xx streak·JSONL append·`source.get(mid, "sitemap")`·DELAY_S)·`by_source`·SUMMARY 로그 무변경 — `source[mid]` 가 only 의 원래 출처(`best:…`)라 `by_source` 집계도 그대로 맞는다.
    - `main()`: `ap.add_argument("--recollect", type=Path, default=None, help="millie_id[\\tsource] 목록 — 그 id 만 재렌더해 JSONL 에 append(멱등 skip 우회, 확장 없음)")` · `only = _read_recollect(args.recollect) if args.recollect else None` · `collect(limit=args.limit, expand=not args.no_expand and only is None, shard=shard, only=only)`(**only 모드는 expand 강제 False** — 확장이 켜지면 seen 이 only 뿐이라 BEST 링크 전체가 프론티어로 들어가 재수집이 전량 수집으로 번진다).
    - 모듈 docstring 실행 예시에 1줄: `uv run --with playwright python scripts/collect_millie.py --recollect data/raw/recollect_badge_titles.txt --shard 0/2`.
    `git diff --numstat scripts/collect_millie.py` → 추가 − 삭제 ≤ 20. `uv run --with playwright python scripts/collect_millie.py --help | grep -c recollect` ≥ 1(네트워크 0).

    **2-e. GREEN 확인:** `uv run ruff format scripts/build_millie_catalog.py scripts/millie_recollect_list.py scripts/collect_millie.py tests/data/test_millie_catalog.py tests/data/test_millie_recollect.py && uv run ruff check <같은 목록>` 클린. `uv run pytest tests/data --no-header 2>&1 | tail -3` → **failed 0**(게이트는 `_n_badge_title,-1` 행을 기록만 하고 통과). `uv run pytest --no-header 2>&1 | tail -2` 전역 failed 0 을 SUMMARY 에. **이 사이 `make millie`·`make millie-build` 를 실행하지 않는다**(구 JSONL 로 재빌드해도 배지 제목이 그대로라 의미 없고, 결정 D-02 가 재빌드 횟수를 제한한다).
  </action>
  <verify>
    <automated>set -o pipefail; cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check scripts/build_millie_catalog.py scripts/millie_recollect_list.py scripts/collect_millie.py tests/data/test_millie_catalog.py tests/data/test_millie_recollect.py && uv run pytest tests/data/test_millie_recollect.py tests/data/test_millie_parse.py --no-header 2>&1 | tail -2 && uv run pytest tests/data/test_millie_catalog.py --no-header --deselect tests/data/test_millie_catalog.py::test_coverage_gate 2>&1 | tail -2 && grep -c "^BADGE_TITLES = " scripts/build_millie_catalog.py && grep -c "_n_badge_title" tests/data/test_millie_catalog.py && grep -c -- "--recollect" scripts/collect_millie.py && [ "$(wc -l < scripts/millie_recollect_list.py)" -le 60 ] && [ "$(git diff --numstat scripts/collect_millie.py | awk '{print $1-$2}')" -le 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - RED 출력(SUMMARY 발췌): `assert None == 1`(카운터) · drift 테스트 AssertionError · recollect 4건 AssertionError(스텁 선행으로 AttributeError·FileNotFoundError 아님), collection error 0
    - GREEN: `uv run pytest tests/data --no-header` → **failed 0**; `uv run pytest tests/data/test_millie_recollect.py --no-header` 4 passed; `grep -c "_n_badge_title" results/millie_coverage.csv` 는 Task 3 전엔 0 이어도 된다(게이트 재실행 시 `-1` 기록)
    - `grep -c "^BADGE_TITLES = frozenset" scripts/build_millie_catalog.py` == 1; `grep -c "def is_badge_title" scripts/build_millie_catalog.py` == 1; `grep -c '"n_badge_title"' scripts/build_millie_catalog.py` == 1; `git diff --numstat scripts/build_millie_catalog.py` 추가 ≤12 · 삭제 0; 합성 카운터 테스트에 title `"종료 D-4"` 레코드 1건을 추가해 `n_badge_title == 2`
    - `grep -c "def _read_recollect" scripts/collect_millie.py` == 1; `grep -c "only: dict\[str, str\] | None = None" scripts/collect_millie.py` == 1; `grep -c "only is None" scripts/collect_millie.py` == 1(expand 강제 False); `git diff --numstat scripts/collect_millie.py` → (추가 − 삭제) ≤ 30; `def render`·렌더 루프(`for attempt in (1, 2)` ~ `time.sleep(DELAY_S)`) diff 0
    - `grep -c "def badge_title_rows" scripts/millie_recollect_list.py` == 1; `grep -c "from build_millie_catalog import" scripts/millie_recollect_list.py` == 1(`is_badge_title, read_records`); `wc -l` ≤ 60
    - `git diff tests/data/test_millie_catalog.py | grep "^-" | grep -vc "^---"` == 0(삭제 0 — 기존 단정·csv 행 무수정); `grep -c "_n_badge_title" tests/data/test_millie_catalog.py` ≥ 1; `grep -c "assert n_badge == 0" tests/data/test_millie_catalog.py` == 0(단언은 Task 3-b0)
    - `git status --short` 에 Makefile·`src/`·`tests/fixtures/millie/<기존 8건>`·`data/id_map.csv` 변경 없음(Task 1 픽스처 2건 신규만)
  </acceptance_criteria>
  <done>게이트가 배지 제목 수를 `_n_badge_title` 행으로 기록하고(단언은 Task 3-b0), 재수집 대상 목록 스크립트와 `--recollect` 옵션이 네트워크 없이 테스트됐다. 전역 스위트 green. `collect()` 기본 경로 무변경. 커밋 없음.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>CHECKPOINT: 배치 중단·최종 스냅샷 확정 승인(결정 D-03) — Task 3 진입 사람 게이트</name>
  <what-built>Task 1·2 완료: 파서 배지 스킵(7종 + `종료 D-N`) · 게이트 `_n_badge_title` 기록 · `--recollect` 옵션 · 대상 목록 스크립트. 전역 스위트 green.</what-built>
  <what-needed>사용자 결정 1건: 밀리 수집 배치가 자연 종료됐으면 "종료 확인", 아직 돌면(Day 2 09-06 오전) "중단·최종 스냅샷 확정" 승인. 승인 문구를 받은 뒤에만 Advisor 가 `pkill -f collect_millie` → Task 3. 예상 재수집 N건(현 JSONL 기준 `millie_recollect_list.py` 출력)·소요(N×7.5s/2)를 함께 제시한다.</what-needed>
  <resume-signal>사용자가 "배치 종료" 또는 "중단 승인" 이라고 말하면 Task 3 진행. `/gsd-execute-phase 3 --gaps-only` 서브에이전트는 여기서 멈추고 Advisor(이 세션)가 Task 3 을 직접 수행한다(lane = Advisor).</resume-signal>
  <files>(없음 — 파일 변경 0, 사용자 결정만)</files>
  <action>`pgrep -f "[p]ython scripts/collect_millie.py" | wc -l` 과 `wc -l data/raw/millie_pages.jsonl`, `uv run python scripts/millie_recollect_list.py`(대상 N·예상 소요 N×7.5s/2)를 출력해 사용자에게 제시하고 **한글로** 승인 여부를 묻는다(AskUserQuestion: "배치 중단·최종 스냅샷 확정" / "자연 종료까지 대기"). 승인 전 어떤 프로세스도 죽이지 않고 어떤 파일도 쓰지 않는다.</action>
  <verify>
    <automated>set -o pipefail; cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && pgrep -f "[p]ython scripts/collect_millie.py" | wc -l && wc -l data/raw/millie_pages.jsonl</automated>
  </verify>
  <done>사용자 승인 문구(또는 배치 자연 종료 확인)가 SUMMARY 에 시각과 함께 기록됨. 승인 없으면 여기서 멈춘다.</done>
</task>

<task type="auto">
  <name>Task 3: [사람 게이트 뒤 · Advisor] 배치 종료 → 게이트 단언 활성화(RED 확인) → 재수집 대상 목록 → `--recollect` 2샤드 → `make millie` 1회(= 03-06 Task 4 최종 스냅샷) → 게이트·level 0 검증 → 수치 교체·기록</name>
  <files>data/raw/recollect_badge_titles.txt, data/raw/millie_pages.jsonl, data/processed/books_kr.parquet, data/processed/item_edges_kr.parquet, data/processed/popularity_kr.parquet, data/id_map.csv, artifacts/serving/books_kr.json, artifacts/serving/item_edges_kr.json, artifacts/serving/popularity_kr.json, artifacts/serving/content_vectors_kr.npz, demo/fallback/popular.json, results/millie_coverage.csv, results/millie_edges_gate.json, report/draft.md, ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md, ../.assets/개발일지/2026-09-06_Day2_<주제>.md</files>
  <read_first>
    - .planning/phases/03-millie-catalog/03-06-PLAN.md Task 2(2-b·2-c·2-d·2-e 명령) · Task 4(4-a~4-e — 이 태스크가 그 절차를 **흡수**한다; 03-06 Task 4 를 따로 실행하지 않는다)
    - .planning/phases/03-millie-catalog/03-06-SUMMARY.md CHECKPOINT 절 · 03-CONTEXT.md D-01·D-02·D-03
    - Task 1·2 SUMMARY 발췌(RED/GREEN·순증 줄 수) — 이 태스크 시작 전 Task 1·2 가 GREEN(게이트 1건 제외)인지 확인
    - ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md §2·§3·§4 · ../.assets/개발일지/README.md 6요소 · `grep -h '^### D' ../.assets/개발일지/*.md | tail -3`
  </read_first>
  <action>
    **3-a. 진입 조건(사람 게이트 — 결정 D-03):** `pgrep -f "[p]ython scripts/collect_millie.py"` 가 비었거나(배치 자연 종료, 감시 루프 `PASS2_DONE`), Day 2(2026-09-06) 오전이면 `## CHECKPOINT REACHED` 로 사용자에게 **"배치 중단 · 최종 스냅샷 확정 · 배지 제목 재수집(≈N건, 2샤드 ≈N×7.5s/2)"** 승인을 받은 뒤 `pkill -f collect_millie`(caffeinate 래퍼 포함) → `pgrep` 빈 출력 확인 → `tail -1 data/raw/millie_pages.jsonl | uv run python -c "import json,sys; json.loads(sys.stdin.read())"`(마지막 줄 완결; 미완결이면 `read_records` 가 badline 으로 세므로 그대로 둔다). 승인 전에는 3-b 이하를 시작하지 않는다. **배치와 재수집을 동시에 돌리지 않는다**(총 요청률 2샤드 상한).

    **3-b0. 게이트 단언 활성화(Advisor, tests/data/test_millie_catalog.py 5줄):** `test_coverage_gate` 의 기존 `assert titleless <= 0.005` **뒤**에
    ```python
    n_badge = report.get("n_badge_title", -1)  # -1 = 카운터 없는 구 report → 재빌드 필요
    assert n_badge == 0, (
        f"배지 title {n_badge}건 — 파서 수정 후 해당 URL 재수집·make millie 재빌드 "
        "(결정 D-06 '파서 미스 비율 ≤0.5% · 초과 시 파서 수정·해당 URL 재수집'"
        "(.planning/phases/03-millie-catalog/03-CONTEXT.md) · 03-UAT Test 9)"
    )
    ```
    → `uv run pytest tests/data/test_millie_catalog.py::test_coverage_gate --no-header 2>&1 | tail -5` 가 **RED(`배지 title -1건` 또는 `<n>건`)** 인 것을 확인해 SUMMARY 에 발췌(현 스냅샷에서 정직한 실패 1회). 이 RED 는 3-d 재빌드로 GREEN 이 되어야 한다. 기존 단정 무수정.

    **3-b. 대상 목록:** `uv run python scripts/millie_recollect_list.py` → `recollect targets=<N> / valid=<V>`. `wc -l data/raw/recollect_badge_titles.txt` == N. `cp data/raw/recollect_badge_titles.txt "$SCRATCH/recollect_targets.txt"`(`SCRATCH=/private/tmp/claude-501/-Users-shinwonchul-Documents----------/bafe3926-70ba-4589-b7e1-d30e43022306/scratchpad`). 예상 소요 = N × 7.5s / 2 / 60 분(실측 09-05: 815건 → ≈51분). N·예상 시간을 SUMMARY 에.

    **3-c. 재수집 2샤드(caffeinate, 백그라운드):**
    ```bash
    cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec"
    wc -l data/raw/millie_pages.jsonl > "$SCRATCH/jsonl_lines.before"
    caffeinate -i -s uv run --with playwright python scripts/collect_millie.py --recollect data/raw/recollect_badge_titles.txt --shard 0/2 > data/raw/recollect_shard0.log 2>&1 &
    caffeinate -i -s uv run --with playwright python scripts/collect_millie.py --recollect data/raw/recollect_badge_titles.txt --shard 1/2 > data/raw/recollect_shard1.log 2>&1 &
    ```
    종료 대기(감시: `pgrep -f "[p]ython scripts/collect_millie.py" | wc -l` 이 0). 두 로그의 `SUMMARY` 줄(`수집 <n0>`·`<n1>`)과 `STOP_4xx`·`FAIL` 줄을 SUMMARY 에 붙인다. `STOP_4xx` 가 나오면 즉시 중단·보고(4xx 3연속 = 예절 규칙). `n0 + n1 + FAIL 수 == N` 확인. JSONL 줄 수 증가분 == n0 + n1(기존 줄 삭제 0 — `head -n $(cat "$SCRATCH/jsonl_lines.before" | awk '{print $1}') data/raw/millie_pages.jsonl | md5` 가 재수집 전 파일 md5 와 같음).
    **재수집 결과 사전 점검(재빌드 전):**
    ```bash
    uv run python -c "
    import sys; sys.path.insert(0,'scripts')
    from pathlib import Path; from build_millie_catalog import read_records, BADGE_TITLES
    v, m = read_records(Path('data/raw/millie_pages.jsonl'))
    bad = [r for r in v if r['title'] in BADGE_TITLES]
    print('valid', len(v), 'badge_title_remaining', len(bad), m)
    import collections; print(collections.Counter(r['title'] for r in bad).most_common(10))
    print([ (r['millie_id'], r['subtitle']) for r in bad[:10] ])"
    ```
    `badge_title_remaining` 이 0 이 아니면: FAIL 로 빠진 id 만 다시 목록으로 만들어(`millie_recollect_list.py` 재실행 → 남은 id = 재렌더 실패분) **1회** 재시도(2샤드). 그래도 남으면 그 수치·millie_id·subtitle 을 SUMMARY 에 기록하고 3-d 로 진행한다(게이트가 fail 로 드러낸다 — 숨기지 않는다). 3-d 에서 게이트가 fail 하면 `## CHECKPOINT REACHED`(checkpoint:decision — 사용자 선택: ① 잔여 레이아웃 조사 후 라벨 추가·재수집 ② 수치 기록 후 진행) 로 멈춘다.

    **3-d. 최종 스냅샷 재빌드(03-06 Task 4-b~4-c 그대로):** 프론티어 기록(`discovered_urls.txt` 줄 수 − JSONL 고유 millie_id 수) → `cp data/id_map.csv "$SCRATCH/id_map.final_before.csv"` → `time make millie 2>&1 | tee "$SCRATCH/make_millie_final.log" | tail -40`. `millie-build` 단계의 `pytest tests/data/test_millie_catalog.py` 가 **강화 게이트 포함** 통과해야 체인이 이어진다 — `_n_badge_title` 이 0 이 아니면 여기서 멈춘다: 재시도 루프 없이 `results/millie_coverage.csv` 의 `_n_badge_title` 값·잔여 제목 상위 10(3-c 점검 출력)을 SUMMARY 에 적고 `## CHECKPOINT REACHED`(사용자 결정: 잔여 레이아웃 조사 vs 수치 기록 후 진행). 통과하면 03-06 Task 2-c 검증 전부(`find -size +50M` 0 · description/curator_note/seg_dist 0 · `git diff --numstat data/id_map.csv` 삭제 0 · `diff <(head -n $(wc -l < "$SCRATCH/id_map.final_before.csv") data/id_map.csv) "$SCRATCH/id_map.final_before.csv"` 빈 출력 · 계약 9키 · popular.json RecommendOut · npz (N,128)) → 2-d 전역 게이트(`uv run ruff format --check . && uv run ruff check .` · `uv run pytest --no-header` failed 0 · **skipped 0** · passed ≥ 228 + Task 1·2 신규(≥16) · `make smoke` PASS).

    **3-e. level 0 실측 — 배지 제목 0 단언 추가(03-06 2-e 확장):**
    ```bash
    uv run uvicorn millie_rec.app.server:app --port 8012 --log-level warning & echo $! > "$SCRATCH/uv.pid"; sleep 4
    curl -s "localhost:8012/api/recommend?seeds=1,2,3&k=5" | tee "$SCRATCH/level0_final.json" | uv run python -c "
    import json,sys,re; d=json.load(sys.stdin); it=d['rows'][0]['items']
    B={'읽던 지점 그대로 이어듣기','도슨트북','무료','오브제북','웹소설','웹툰','오디오웹소설'}
    print(d['fallback_level'], d['model_version'], [i['book_id'] for i in d['items']], d['rows'][0]['row_id'], len(it))
    print([i['title'] for i in it]); assert all(re.search(r'[가-힣]', i['title'] or '') for i in it)
    assert not (B & {i['title'] for i in it}), 'badge title in level 0'; assert not {1,2,3} & {i['book_id'] for i in d['items']}"
    curl -s "localhost:8012/api/recommend" | uv run python -c "import json,sys; d=json.load(sys.stdin); print(d['fallback_level'], d['model_version'], len(d['items']), len(d['rows'][0]['items']))"
    kill $(cat "$SCRATCH/uv.pid")
    ```
    또 `uv run python -c "import json; b=json.load(open('artifacts/serving/books_kr.json')); B={…7종…}; print('badge in export', sum(r['title'] in B for r in b))"` → 0. 진단(단언 아님, Advisor 검토용): `uv run python -c "import pandas as pd; df=pd.read_parquet('data/processed/books_kr.parquet'); s=df[df.title.str.len()<=6].title.value_counts(); print(s[s>=3].head(20).to_string())"` — 출력에 `종료 D-4`(09-05 실측 9건, subtitle 이 실제 제목: 야간비행·톰 소여의 모험 …) 같은 **7종 밖 배지성 제목**이 보이면 수치와 함께 SUMMARY 에 "Advisor 결정 필요(라벨 추가는 실측 근거 + 사용자 확인 후, 이 플랜 범위 밖)" 로 보고만 한다.

    **3-f. 수치 교체·기록(03-06 Task 4-d·4-e 그대로 + 이 갭):** (Advisor 전용 문서 1줄 추가: `../.claude/rules/data.md` "파서 픽스처 8건" → "10건(배지 헤더 2건 포함)".) `report/draft.md` P3 문장의 `[숫자는 Day 2 오전 최종 스냅샷 재빌드 후 교체]` 마커 제거·`<N>`·커버리지·동일 카테고리 비율을 `results/millie_coverage.csv`·`results/millie_edges_gate.json` 값으로(복사만). 트래킹 03 §2 에 `최종 스냅샷 <날짜 시각> · 고유 <N> · 미수집 프론티어 <F> · 배지 제목 재수집 <N_re>건 → _n_badge_title 0` 1행, §3 로그에 `| 09-06 <hh:mm> | 배지 제목 재수집·최종 재빌드 | 03-08-PLAN Task 3 | … |` 1행, §5 예절 체크에 재수집 요청 수·2.5초·2샤드 1줄. 개발일지: `grep -h '^### D' ../.assets/개발일지/*.md | tail -3`(실측 09-05 밤 마지막 D66 → 신규 **D67**), 실행일 파일(09-06 이면 README 규칙대로 `2026-09-06_Day2_<주제>.md` 신설) `## 의사결정` 에 1건(6요소·12줄 이내):
    `### D67. 카탈로그 title 배지 오염 8.4% — 파서 _NAV_NOISE 7종 확장 + 해당 URL 재수집(D-06 절차) + 게이트 _n_badge_title 단언  [축 ② 활용 데이터 · 적재 계획 02 §7 / .planning/phases/03-millie-catalog/03-UAT.md Test 9 · 03-08-PLAN.md]  (확정 — Advisor 검증 09-05 · 사용자 승인 09-06)`
    - 문제: 8,708권 중 728권(재수집 시점 N건)의 title 이 밀리 기능 배지 라벨('읽던 지점 그대로 이어듣기' 444·'도슨트북' 116·'무료' 64 …) — level 0 상위 5 에 2권 노출. 커버리지 게이트는 존재율만 봐 통과.
    - 동기: 데모·앵커 이웃(TF-IDF 입력)·본인 5권이 title 위에 선다. Phase 4·6 전에 닫아야 한다.
    - 선택지: ① 파서 수정 + 해당 URL 재수집(채택) ② 배지 행을 카탈로그에서 필터(N권 손실·D-05 위반 — 버림) ③ subtitle 을 title 로 승격(subtitle 도 배지인 행 다수 — 버림) ④ 라벨 정규식 추측 확장(근거 없는 상수 — 버림).
    - 근거: 실페이지 헤더 1회 렌더(book 213·1550) · parquet 실측 subtitle 분포(도슨트북 116건 중 배지 설명 59) · `read_records` 마지막 줄 우선 dedup 이 append 재수집을 그대로 채택.
    - 장애: (재수집 FAIL·STOP_4xx·잔여 배지 제목 수 — 없으면 "없음").
    - 결과: `millie_parse._NAV_NOISE` +7 · 픽스처 2건 · `BADGE_TITLES`·`_n_badge_title` 게이트 · `collect_millie.py --recollect`(only 모드) · `millie_recollect_list.py` · 최종 스냅샷 <N>권(`_n_badge_title` 0). 잃은 것: 재수집 N건 ≈<분>·요청 N회 추가(예절 규칙 준수). 되돌리는 조건: 밀리 레이아웃이 또 바뀌어 게이트가 다시 fail 하면 같은 절차(픽스처 추가 → 라벨 → 재수집).
    D66 '결과' 요소에 "최종 스냅샷 <N>권(배지 제목 재수집 후) · 재빌드 후 id_map 삭제 0 · 이후 `make millie` 재실행 금지(Day 3 모델 freeze 전 마지막 스냅샷, 결정 D-02 'PDF 수치 = 마지막 재빌드 스냅샷'(.planning/phases/03-millie-catalog/03-CONTEXT.md))" 1줄 추가.

    **3-g. SUMMARY:** 3-a 승인 문구·pkill 시각 · 3-b N·예상/실측 소요 · 3-c 두 SUMMARY 줄·FAIL/STOP 수·JSONL 접두 md5 동일 · 사전 점검 출력 · `make millie` tail · 2-c/2-d 출력 · curl 3줄(배지 0) · 진단 출력(7종 밖 후보 유무) · 문서 diff 요약 · `git status --short` · "커밋하지 않는다 — 커밋 대상: Task 1·2 코드·픽스처·테스트 + artifacts/serving 4파일·demo/fallback/popular.json·results 2파일·data/id_map.csv(사용자 승인 후)" · **03-06 Task 4 는 이 태스크로 완료됨(Advisor 가 STATE.md·03-06 SUMMARY status 갱신)**.
  </action>
  <verify>
    <automated>set -o pipefail; cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && [ -z "$(pgrep -f '[p]ython scripts/collect_millie.py')" ] && test -s data/raw/recollect_badge_titles.txt && grep -qE '^_n_badge_title,0\r?$' results/millie_coverage.csv && [ "$(grep -c '최종 스냅샷으로 교체\|최종 스냅샷 재빌드 후 교체' report/draft.md)" = "0" ] && grep -c "최종 스냅샷" "../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md" && [ "$(git diff --numstat data/id_map.csv | cut -f2)" = "0" ] && uv run pytest --no-header 2>&1 | tail -2 && make smoke</automated>
  </verify>
  <acceptance_criteria>
    - `pgrep -f "[p]ython scripts/collect_millie.py"` 빈 출력(배치 종료 또는 사용자 승인 후 중단); SUMMARY 에 승인 문구·시각
    - `data/raw/recollect_badge_titles.txt` 줄 수 N == 목록 스크립트 출력 `targets=N`; 재수집 로그 2개의 `SUMMARY` 줄 수집 합 + FAIL == N; `STOP_4xx` 0; 재수집 전 JSONL 접두 md5 동일(기존 줄 삭제 0)
    - `results/millie_coverage.csv` 에 `_n_badge_title,0` 행 · `title,1.0000` · `_n_records,<N_final>`; `make millie` 종료 코드 0(체인 어디서도 멈추지 않음); `uv run pytest --no-header` failed 0 · skipped 0 · passed ≥ 244; `make smoke` → `smoke: PASS`; ruff 클린
    - curl level 0: `0 pop_v1 [...] trending 5`, 제목 5개 한글 · 배지 라벨 0 · `{1,2,3}` 제외; `artifacts/serving/books_kr.json` 배지 제목 0건; `find artifacts/serving -size +50M` 빈 출력; description/curator_note/seg_dist 0건; `id_map.final_before.csv` 가 새 `data/id_map.csv` 의 접두(diff 빈 출력)
    - `report/draft.md` 교체 마커 0건·P3 수치 == `results/` 값; 트래킹 03 §2 `최종 스냅샷` 행(미수집 프론티어·재수집 건수 포함) · §3 로그 1행; `grep -c "^### D67\." ../.assets/개발일지/*.md` 합 == 1(6요소 6줄 존재, 12줄 이내, 번호 단독 참조 0); D66 결과 요소에 "재실행 금지" 문구
    - 잔여 배지 제목이 0 이 아닌 경우: `pytest.skip`·단언 완화 0, `_n_badge_title,<n>` 값과 잔여 제목 상위 10 이 SUMMARY 에 있고 `## CHECKPOINT REACHED` 로 끝남(이 경우 위 "failed 0" 대신 failed 1 = test_coverage_gate 를 정직하게 기록)
    - `git log --oneline | head -1` 불변(커밋 0)
  </acceptance_criteria>
  <done>배지 제목 N건이 재수집으로 실제 제목이 되고, 최종 스냅샷 1회 재빌드(03-06 Task 4 흡수)가 강화 게이트(`_n_badge_title 0`)·전역 스위트·smoke·level 0(배지 0)을 통과하며 PDF·트래킹·개발일지 수치가 같은 `results/` 값을 가리킨다. 이후 `make millie` 금지 기록. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `collect_millie.py --recollect` → millie.co.kr | 외부 사이트에 추가 요청 N건. 예절 규칙(2.5초·UA·4xx 중단·2샤드 상한)이 경계 |
| 재수집 append → `data/raw/millie_pages.jsonl` | 배치와 동시에 쓰면 줄이 섞인다. 배치 종료 뒤에만 |
| `tests/fixtures/millie/badge_*.txt`(repo 커밋) | 실페이지 텍스트의 예외적 보관 — 리뷰·필명·큐레이터 실명이 들어가면 규칙 위반 |
| 카탈로그 title → `/api/recommend`·데모·TF-IDF | 오염된 title 이 화면·이웃·PDF 5권 케이스로 번진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-29 | Denial of Service | 재수집이 사이트 요청률을 올림 | mitigate | `collect()` 같은 루프 사용 — 프로세스당 `DELAY_S=2.5`, 샤드 ≤2(결정 D65), `MAX_4XX_STREAK=3` 즉시 중단, 식별 UA; 배치와 동시 실행 금지(3-a `pgrep` 조건) |
| T-03-30 | Tampering | JSONL 동시 append(배치 ↔ 재수집) | mitigate | 3-a 에서 배치 프로세스 0 확인 후에만 3-c; 재수집 전 줄 수·접두 md5 기록 → 기존 줄 삭제 0 검증 |
| T-03-31 | Tampering | `--recollect` 에 `--no-expand` 를 잊으면 BEST 링크 전체가 프론티어로 들어가 전량 재수집 | mitigate | `main()` 에서 `expand = not args.no_expand and only is None` 강제; 테스트 `test_collect_accepts_only_keyword` + SUMMARY 로그 `recollect 모드 … 확장 없음` |
| T-03-32 | Information Disclosure | 픽스처에 리뷰 본문·필명 유입 | mitigate | 1-a 제외 목록 + `grep` 0 검증(`팔로우|좋아요|별점을 남겨주세요|저자 소개`, `_\d{4,}`) + 기존 `test_no_review_or_nickname_leakage` 가 새 픽스처에도 파라미터화 |
| T-03-33 | Tampering | 배지 제목 행을 필터로 지워 N권을 조용히 잃음 | mitigate | 빌더는 카운터만(`test_raw_coverage_counts_badge_titles_but_keeps_rows` 가 행 존속 단언); 복구는 재수집만 |
| T-03-34 | Repudiation | 게이트를 `pytest.skip`·임계 완화로 통과시킴 | mitigate | 단언은 `== 0` 고정, csv 에 `_n_badge_title` 실측 기록; 잔여가 있으면 CHECKPOINT 로 사용자 보고(3-d) |
| T-03-35 | Tampering | `data/id_map.csv` 재빌드 훼손 | mitigate | 재수집은 같은 millie_id(신규 id 0), 빌드 전 사본 접두 diff + `git diff --numstat` 삭제 0(03-06 T-03-26 동일) |
| T-03-36 | Spoofing | 추측 라벨 추가로 실제 제목('무료'·'웹툰' 같은 책)을 오인 | accept | 실측 7종만, 카탈로그에 그 제목의 실제 책 0건(09-05 parquet 실측); 7종 밖 후보 `종료 D-N`(실측 9건, subtitle 이 실제 제목)은 Advisor 결정(09-05 밤)으로 정규식 `^종료 D-\d+$` 1개만 포함 — 그 외 추측 확장 0 |
</threat_model>

<verification>
**Task 1·2(지금, Worker):** `uv run pytest tests/data/test_millie_parse.py tests/data/test_millie_recollect.py --no-header` 전부 passed · `uv run pytest tests/data --no-header` → **failed 0**(게이트는 `_n_badge_title` 기록만, 단언은 Task 3-b0) · 자기 파일 ruff 클린 · 전역 `uv run pytest --no-header` failed 0(Advisor 가 wave 5 Task 2 종료 시 1회 + `make smoke` PASS).

**Task 3(사람 게이트 뒤, Advisor):** 재수집 N건 완료 → `make millie` 1회 통과(강화 게이트 포함) → `uv run pytest --no-header` failed 0·skipped 0 → `make smoke` PASS → level 0 제목에 배지 0 → draft/트래킹/개발일지 수치 일치. 잔여 배지 제목 ≠ 0 이면 CHECKPOINT(숨기지 않음).

**이 플랜이 03-06 Task 4 를 흡수한다:** 03-06 SUMMARY 의 `status: partial` 은 Task 3 완료 후 Advisor 가 갱신. Phase 3 `/gsd-verify-work 3` 는 Task 3 뒤에 다시 돈다(UAT Test 9·12 재검증).
</verification>

<success_criteria>
- 배지 헤더 픽스처 2건이 기존 파서 테스트 8종 + 신규 테스트를 통과하고 `_NAV_NOISE` 가 실측 7종을 포함한다(추측 확장 0)
- 커버리지 게이트가 `_n_badge_title` 을 기록·0 단언하며, 재수집 전에는 정직하게 실패한다
- `--recollect`·목록 스크립트가 네트워크 없이 테스트됐고 `collect()` 기본 경로는 무변경이다
- 배치 종료·승인 뒤 재수집 → 최종 스냅샷 1회 → 게이트·스위트·smoke·level 0(배지 0) 통과, PDF·트래킹·개발일지 수치가 `results/` 와 일치, 이후 재빌드 금지 기록
- 커밋 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-08-SUMMARY.md` — Task 1·2 RED/GREEN 발췌(AssertionError 줄) · 순증 줄 수 3파일 · 픽스처 크기·누출 grep 0 · Task 3 승인 문구·재수집 N·소요·로그 SUMMARY 줄·잔여 배지 수·`make millie` tail·검증 출력·curl 3줄·진단 출력(7종 밖 후보) · 문서 diff 요약 · `git status --short` · "커밋하지 않는다 — 커밋 대상 목록(사용자 승인 후)" · "03-06 Task 4 완료(이 플랜으로) — STATE.md 갱신은 Advisor".
</output>
