---
phase: 03-millie-catalog
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/data/test_millie_edges.py              # 실데이터 게이트가 results/millie_edges_gate.json 을 쓰도록(기록 추가 — 단언 불변)
  - data/processed/item_edges_kr.parquet         # gitignore — 첫 스냅샷 이웃
  - results/millie_edges_gate.json               # 커밋 대상 — 이웃 게이트 3 실측 정본
  - scripts/build_millie_edges.py                # 게이트 ③ 미달 시에만(D-10: description 가중 상향만) — 통과하면 무변경
autonomous: true
requirements: [DATA-05]
lane: Data-B (scripts/ Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - scripts/build_millie_catalog.py
  - scripts/millie_difficulty.py
  - scripts/export_millie_serving.py
  - scripts/millie_parse.py
  - scripts/collect_millie.py
  - data/id_map.csv
  - data/processed/books_kr.parquet
  - tests/data/test_millie_catalog.py
  - tests/data/test_millie_difficulty.py
  - tests/fixtures/millie/**
  - src/millie_rec/**
  - tests/test_architecture.py
  - tests/conftest.py
  - Makefile
  - pyproject.toml
  - demo/**
  - ../.claude/**
  - CLAUDE.md

must_haves:
  truths:
    - "data/processed/item_edges_kr.parquet 가 첫 스냅샷 books_kr.parquet 전 도서에 대해 title+description+curator_note 문자 2~4gram TF-IDF cosine top-20(content_sim) + category_best(0.2) 이웃을 갖는다 — tags 는 TF-IDF 입력에 없다(D-08)"
    - "이웃 게이트 3 이 실데이터에서 통과한다: self-edge 0 · 전 도서 이웃 ≥5 · content_sim top-20 동일 카테고리 비율 평균 ≤0.70 — 게이트 ③ 미달 시 유일한 조치는 description 가중 상향(DESC_REPEAT), 그래도 미달이면 수치 기록·보고(D-10)"
    - "results/millie_edges_gate.json 에 n_books · n_edges · sources(content_sim/category_best 건수) · self_edges · min_degree · same_category_share · generated_at 가 있다 — PDF·트래킹 03 §2 의 이웃 수치는 이 파일에서만 복사한다"
  artifacts:
    - path: "results/millie_edges_gate.json"
      provides: "이웃 게이트 실측(첫 스냅샷)"
      contains: "\"same_category_share\""
    - path: "tests/data/test_millie_edges.py"
      provides: "test_neighbour_quality_gate 가 json 기록 후 단언(기존 단언 불변)"
      contains: "millie_edges_gate.json"
  key_links:
    - from: "scripts/build_millie_edges.py::build"
      to: "data/processed/books_kr.parquet (Plan 01 산출)"
      via: "pd.read_parquet(--books) · read_best_links(--jsonl) millie_id 조인"
      pattern: "read_best_links"
    - from: "tests/data/test_millie_edges.py::test_neighbour_quality_gate"
      to: "results/millie_edges_gate.json"
      via: "json.dumps 기록 → 게이트 3 단언"
      pattern: "GATE_JSON"
---

<objective>
Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md "Phase 3: 밀리 카탈로그 빌드") Success Criteria 2 의 이웃 절반(DATA-05): Plan 01 이 만든 첫 스냅샷 `books_kr.parquet` 로 `scripts/build_millie_edges.py` 를 **실행**해 `item_edges_kr.parquet` 를 만들고 이웃 게이트 3 을 실측한다. 코드 변경은 (1) 게이트 테스트가 수치를 `results/millie_edges_gate.json` 에 남기게 하는 기록 추가, (2) 게이트 ③(동일 카테고리 ≤70%) **미달 시에만** D-10 의 유일 조치(description 가중 상향) — 두 가지뿐이다.

Purpose: PDF P3 "이웃은 제목·소개 문자 2~4gram TF-IDF cosine, 협업 필터링 수치와 섞지 않는다" 문장의 실측 근거(동일 카테고리 비율·엣지 수)와 Plan 04 export 의 입력. 결정 D-08 'tags 는 TF-IDF 기본 제외'(.planning/phases/03-millie-catalog/03-CONTEXT.md) · D-09 'description 결측은 category_best 보험 + top_up, cosine 임계 없음' · D-10 '③ 미달 조치는 description 가중 상향만' · D-11 'TF-IDF 유지·BM25 기각' 이행.
Output: `item_edges_kr.parquet`(gitignore) + `results/millie_edges_gate.json` + 게이트 테스트 기록 3줄. 커밋 0.

**하지 않는 것:** BM25·문장 임베딩(D-11) · cosine 최소 임계·동일 카테고리 상한 후처리(D-09·D-10) · SVD 벡터(Plan 05) · 파일 분리(152줄은 D-16 범위 안 — 기록 추가는 테스트 파일에만).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-PATTERNS.md
@.planning/phases/03-millie-catalog/03-01-SUMMARY.md
@scripts/build_millie_edges.py
@tests/data/test_millie_edges.py
@../.assets/설계서/데이터 소스/02_밀리_데이터_적재_계획.md
@../.claude/rules/data.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**이웃 규칙(`../.claude/rules/data.md` Track B 이웃 절):** "정본은 `content_sim` = `title+description+curator_note` 문자 2~4gram TF-IDF cosine top-20(`tags`는 **기본 제외** — 어휘 ~30토큰이 동일 카테고리 이웃을 밀어 게이트 ③에 불리, `--with-tags`는 실험 옵션. Phase 3 CONTEXT D-08). `category_best`는 이웃 ≥5 보험(저가중)." / "품질 게이트 3(`tests/data/test_millie_edges.py`, 실패 = test fail): self-edge 0 · 전 도서 이웃 ≥5 · top-20 동일 카테고리 비율 ≤70%(미달 시 `description` 가중 상향만, 그래도 미달이면 수치 기록·사용자 보고 — `tags`는 이미 제외)."

**간결함(`../.claude/rules/simplicity.md`):** "근거 없는 상수 금지" — cosine 임계를 두지 않는다(D-09). 새 리랭킹 코드 금지(D-10). 실측 하나가 나오면 즉시 기록.

**TDD(`../.claude/rules/python-tdd.md`):** 실데이터 게이트의 `pytest.skip` 은 산출물 부재 조건이라 예외. "기존 테스트의 단정을 고치는 것" 금지 — 이 플랜의 테스트 변경은 **기록 추가만**(기존 단언 4개 그대로). `uv run pytest <대상> --no-header -s`(출력 확인 시 `-s`).

**숫자 정본(../CLAUDE.md §2-6):** 숫자 = `results/`. 트래킹 03 §2·PDF 는 이 json 에서 복사(Plan 06 Advisor).
</project_rules_inline>

<interfaces>
From scripts/build_millie_edges.py (현재 152줄, 변경 후보 지점만):
```python
TOP_N = 20; MIN_NEIGHBOURS = 5; CATEGORY_BEST_WEIGHT = 0.2; NGRAM_RANGE = (2, 4)
def build_texts(books: pd.DataFrame, with_tags: bool = False) -> list[str]:
    cols = ["title", "description", "curator_note"] + (["tags"] if with_tags else [])
    return [" ".join(p for p in (_text(row[c]) for c in cols) if p) for row in books[cols].to_dict("records")]
def similarity(texts) -> np.ndarray:   # TfidfVectorizer(analyzer="char_wb", ngram_range=NGRAM_RANGE, min_df=1) → cosine_similarity → 대각 -1
def build(books, best_links=None, with_tags=False) -> pd.DataFrame   # EDGE_COLUMNS = src_book_id dst_book_id weight source
main: --books DIR_PROCESSED/"books_kr.parquet" · --out DIR_PROCESSED/"item_edges_kr.parquet" · --jsonl DIR_RAW/"millie_pages.jsonl" · --with-tags
```
From tests/data/test_millie_edges.py: `MAX_SAME_CATEGORY_SHARE = 0.70` · `_degree(edges)` · `_same_category_share(edges, books)`(content_sim top-20 안 동일 카테고리 비율 평균, 분야 = `categories[0]`) · `test_neighbour_quality_gate` L138-169(skip 조건 → print share → 단언 4개: self-edge 0 · dst/src ⊂ known · orphans 없음 · thin 없음 · share ≤ 0.70).

메모리(03-PATTERNS): `cosine_similarity` dense float64 — 5,5k² ≈ 240MB, 9천 권 ≈ 650MB. 이번 스냅샷은 허용 범위. 9천 권 초과 재빌드에서 MemoryError 가 나면 chunked top-k 로 바꾸는 것은 별도 브리프(이 플랜 범위 밖 — SUMMARY 에 메모).

D-10 허용 조치의 구체 형태(③ 미달 시에만): 모듈 상단 `DESC_REPEAT = 2  # D-10: 게이트 ③ 미달 시 description 가중 상향(title 반복 축소). 통과하면 1` 을 두고 `build_texts` 에서 `description` 을 `DESC_REPEAT` 번 join. 기본값은 실측 결과로 정한다(통과하면 이 상수를 만들지 않는다).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 게이트 테스트에 results/millie_edges_gate.json 기록 추가(단언 불변) → 합성 테스트 회귀 확인</name>
  <files>tests/data/test_millie_edges.py</files>
  <read_first>
    - tests/data/test_millie_edges.py L138-169 (`test_neighbour_quality_gate` 전문)
    - tests/data/test_millie_catalog.py L283-303 (`test_coverage_gate` 의 results 기록 형식 — 같은 자리·같은 순서: 기록 먼저, 단언 뒤)
    - ../.claude/rules/python-tdd.md "금지" 절
  </read_first>
  <action>
    `tests/data/test_millie_edges.py` 상단 상수에 `GATE_JSON = ROOT / "results" / "millie_edges_gate.json"` 추가(`import json` · `from datetime import UTC, datetime` 추가). `test_neighbour_quality_gate` 에서 `share` 계산 직후, **기존 단언들 앞에** 기록 블록을 넣는다(단언이 실패해도 수치가 남아야 D-10 보고가 가능하다):
    ```python
    GATE_JSON.parent.mkdir(parents=True, exist_ok=True)
    GATE_JSON.write_text(json.dumps({
        "generated_at": datetime.now(UTC).isoformat(timespec="seconds"),
        "n_books": len(known),
        "n_edges": int(len(real_edges)),
        "sources": {k: int(v) for k, v in real_edges["source"].value_counts().to_dict().items()},
        "self_edges": int((real_edges["src_book_id"] == real_edges["dst_book_id"]).sum()),
        "min_degree": int(degree.min()) if len(degree) else 0,
        "n_orphans": len(orphans),
        "same_category_share": round(float(share), 4),
        "max_same_category_share": MAX_SAME_CATEGORY_SHARE,
    }, ensure_ascii=False, indent=2), encoding="utf-8")
    ```
    (`orphans` 계산 줄을 기록 블록 앞으로 올린다 — 값만 재배치, 단언 문장 4개는 글자 그대로 유지.) 모듈 docstring 세 번째 줄에 `실측 수치는 results/millie_edges_gate.json 에 남긴다(숫자 정본).` 1줄 추가.
    확인: `uv run pytest tests/data/test_millie_edges.py --no-header` → 10 passed, 1 skipped(아직 item_edges 없음 — Task 2 전). `uv run ruff format tests/data/test_millie_edges.py && uv run ruff check tests/data/test_millie_edges.py`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check tests/data/test_millie_edges.py && grep -c "millie_edges_gate.json" tests/data/test_millie_edges.py && uv run pytest tests/data/test_millie_edges.py --no-header 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GATE_JSON" tests/data/test_millie_edges.py` ≥ 3; `grep -c "same_category_share" tests/data/test_millie_edges.py` ≥ 1
    - 기존 단언 4문장 불변: `grep -c "assert share <= MAX_SAME_CATEGORY_SHARE" tests/data/test_millie_edges.py` == 1 · `grep -c "assert thin.empty" ` == 1 · `grep -c "assert not orphans" ` == 1
    - `git diff --numstat tests/data/test_millie_edges.py` 삭제 ≤ 3(줄 재배치만)
    - `uv run pytest tests/data/test_millie_edges.py --no-header` → `10 passed, 1 skipped`(Task 2 전) · ruff 클린
  </acceptance_criteria>
  <done>게이트 테스트가 수치를 results 에 남기고 단언은 그대로다. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 2: build_millie_edges 실행 → 게이트 3 실측 → (③ 미달 시에만 D-10 조치) → results/millie_edges_gate.json</name>
  <files>data/processed/item_edges_kr.parquet, results/millie_edges_gate.json, scripts/build_millie_edges.py (조건부)</files>
  <read_first>
    - .planning/phases/03-millie-catalog/03-01-SUMMARY.md (books_kr.parquet 행 수·스냅샷 시각)
    - scripts/build_millie_edges.py L43-56 (`build_texts`·`similarity` — 조건부 변경 지점)
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-08·D-09·D-10·D-11
    - ../.assets/설계서/데이터 소스/02_밀리_데이터_적재_계획.md §6 이웃(게이트 3·행 소스 분리)
  </read_first>
  <action>
    **2-a. 사전:** `test -f data/processed/books_kr.parquet`(Plan 01 산출 — 없으면 이 플랜은 시작할 수 없다, `## CHECKPOINT REACHED` 로 보고). `uv run python -c "import pandas as pd; df=pd.read_parquet('data/processed/books_kr.parquet'); print(len(df), df['description'].isna().mean().round(3), df['curator_note'].notna().mean().round(3))"` → 행 수·description 결측률(≈0.026)·curator_note 보유율(≈0.004) 을 SUMMARY 에.

    **2-b. 빌드:** `/usr/bin/time -l uv run python scripts/build_millie_edges.py 2>&1 | tail -25`(macOS `-l` 이 "maximum resident set size" 바이트를 찍는다 — SUMMARY 에 peak RSS 와 경과 시간을 기록. architect 추정: `_content_pairs`·`_top_up` 의 `np.argsort` 2회 + float64 sim 으로 5.5k² 에서 ≈640MB, 9천 권 ≈1.9GB. **이번 플랜에서 코드는 바꾸지 않는다** — MemoryError 또는 RSS > 2GB 면 중단·보고하고 `sim.astype(np.float32)` + `order` 1회 계산 같은 최소 변경은 Plan 06 Task 4 최종 재빌드 전 별도 브리프) → `edges=<E> {'content_sim': <c>, 'category_best': <b>} → data/processed/item_edges_kr.parquet`. 기대 E ≈ N×20 + category_best 수(≈ 110k~130k), 소요 1~3분(dense 5.5k² cosine). `MemoryError` 면 중단·보고(9천 권 초과 시 chunked top-k 는 별도 브리프 — 이번 스냅샷에서는 발생하지 않아야 한다).

    **2-c. 게이트 실측:** `uv run pytest tests/data/test_millie_edges.py::test_neighbour_quality_gate --no-header -s 2>&1 | tail -15` → 출력 `top-20 동일 카테고리 비율 평균 <s> (기준 ≤0.7)`. `cat results/millie_edges_gate.json`. 세 경우:
    - **통과(passed):** `scripts/build_millie_edges.py` **무변경**. 2-e 로.
    - **③ 만 미달(share > 0.70, 나머지 통과):** D-10 유일 조치 — `scripts/build_millie_edges.py` 상단에 `DESC_REPEAT = 2  # D-10: 게이트 ③ 미달 시 description 가중 상향(title 반복 축소). 실측 <s_before> → <s_after>` 추가, `build_texts` 를 `parts = [_text(row["title"])] + [_text(row["description"])] * DESC_REPEAT + [_text(row["curator_note"])] + ([_text(row["tags"])] if with_tags else [])` 형태로(빈 문자열 제외 후 공백 join — 기존 `_text` 재사용, 시그니처 불변). `uv run pytest tests/data/test_millie_edges.py --no-header`(합성 10건 통과 유지) → 2-b 재실행 → 2-c 재측정. **그래도 미달이면** 코드는 `DESC_REPEAT = 2` 상태로 두되 추가 조치 없이 두 수치(`s_before`·`s_after`)를 SUMMARY 에 적고 `## CHECKPOINT REACHED`(사용자 보고 — D-10 "동일 카테고리 상한 후처리 같은 새 리랭킹 코드는 만들지 않는다"). 게이트 테스트가 fail 인 상태로 끝나는 것을 숨기지 않는다(`pytest.skip` 금지).
    - **①·② 미달(self-edge >0 또는 이웃 <5):** 코드 결함(`_top_up`·`fill_diagonal`) — 재현 테스트를 먼저 합성 12권에 쓰고 수정(TDD 버그 절차). 발생 확률은 낮다(합성 테스트가 이미 두 게이트를 검사).

    **2-d. 이웃 sanity(SUMMARY 용, 단언 아님):** `uv run python -c "import pandas as pd; e=pd.read_parquet('data/processed/item_edges_kr.parquet'); b=pd.read_parquet('data/processed/books_kr.parquet').set_index('book_id'); src=int(e['src_book_id'].iloc[0]); top=e[e.src_book_id==src].nlargest(5,'weight'); print(b.loc[src,'title']); print(b.loc[top.dst_book_id,'title'].tolist(), top.weight.round(3).tolist())"` — 첫 책의 이웃 5권 제목을 SUMMARY 에(PDF 정성 케이스 미리보기, 숫자는 아님).

    **2-e. 마무리:** `uv run pytest tests/data/test_millie_catalog.py tests/data/test_millie_edges.py --no-header` → failed 0, skipped 0(두 실데이터 게이트가 이제 돈다). 같은 wave 의 Plan 04·05 가 `tests/data/` 에 RED 테스트를 두고 있을 수 있으므로 전체 `tests/data` 는 Advisor wave 종료 기준선에서만. `git status --short` 에 `results/millie_edges_gate.json`(신규)·`tests/data/test_millie_edges.py`(M)·조건부 `scripts/build_millie_edges.py`(M) 만. SUMMARY 에 2-b 출력 원문·2-c 출력·json 전문·조치 여부(무변경 / DESC_REPEAT=2 + before/after)·"커밋하지 않는다".
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test -f data/processed/item_edges_kr.parquet && test -f results/millie_edges_gate.json && uv run python -c "import json; d=json.load(open('results/millie_edges_gate.json')); assert d['self_edges']==0 and d['min_degree']>=5 and d['n_orphans']==0, d; print('gate', d['same_category_share'], d['n_books'], d['n_edges'])" && uv run pytest tests/data/test_millie_edges.py::test_neighbour_quality_gate --no-header 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `data/processed/item_edges_kr.parquet` 존재, 컬럼 `src_book_id dst_book_id weight source`, `source` 값 ⊂ {content_sim, category_best}
    - `results/millie_edges_gate.json` 키 == {generated_at, n_books, n_edges, sources, self_edges, min_degree, n_orphans, same_category_share, max_same_category_share}; `self_edges == 0` · `min_degree ≥ 5` · `n_orphans == 0` · `n_books` == Plan 01 `_n_records`
    - `uv run pytest tests/data/test_millie_edges.py::test_neighbour_quality_gate --no-header` → `1 passed` (또는 ③ 만 미달 시 `1 failed` + SUMMARY 에 before/after 수치 + `## CHECKPOINT REACHED`)
    - 게이트 통과 시 `git diff --stat scripts/build_millie_edges.py` → 출력 없음; 조치 시 `grep -c "DESC_REPEAT = 2" scripts/build_millie_edges.py` == 1 이고 `wc -l scripts/build_millie_edges.py` ≤ 160(순증 ≤8)
    - `uv run pytest tests/data/test_millie_catalog.py tests/data/test_millie_edges.py --no-header` → failed 0(③ 미달 CHECKPOINT 경우 제외), `git log --oneline | head -1` 불변
  </acceptance_criteria>
  <done>첫 스냅샷 이웃이 만들어지고 게이트 3 수치가 `results/millie_edges_gate.json` 에 있다. 통과면 코드 무변경, ③ 미달이면 D-10 조치 1회 후 결과 보고. Plan 04·06 의 입력 준비. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `books_kr.parquet`(description·curator_note 포함) → TF-IDF | 밀리 저작 텍스트가 코드로 들어오는 지점 — 여기서만 쓰고 밖으로 내보내지 않는다 |
| `millie_pages.jsonl` `best_links` → category_best 엣지 | 카탈로그 밖 id 링크가 섞일 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-10 | Information Disclosure | description·curator_note | accept(이 플랜) | parquet(gitignore) 안에서 TF-IDF 입력으로만 소비. `item_edges_kr.parquet` 에는 id·weight·source 만 — export 미포함 단언은 Plan 04·06 |
| T-03-11 | Tampering | `_category_pairs` 카탈로그 밖 `best_links` | mitigate | 기존 코드 `id_of.get(linked)` None → 버림(테스트 `test_category_best_uses_best_links_and_drops_unknown_targets`) — 게이트 `dst ⊂ known` 이 실데이터에서 재확인 |
| T-03-12 | Denial of Service | dense 유사도 행렬 메모리 | accept | 이번 스냅샷 ≈240MB. 9천 권 초과 재빌드에서 MemoryError 시 chunked top-k 별도 브리프(SUMMARY 메모) |
| T-03-13 | Repudiation | 게이트 수치 손기록 | mitigate | 테스트가 `results/millie_edges_gate.json` 을 직접 쓴다 — 트래킹·PDF 는 복사만 |
</threat_model>

<verification>
**이 플랜(Worker)이 직접 확인하는 것:** `uv run ruff format tests/data/test_millie_edges.py [scripts/build_millie_edges.py] && uv run ruff check <같은 파일>` 클린 · `uv run pytest tests/data/test_millie_catalog.py tests/data/test_millie_edges.py --no-header` failed 0·skipped 0 · `results/millie_edges_gate.json` 값 조건 · 2-b 의 peak RSS 기록.

**공통 기준선(Advisor 가 wave 2 종료 시 1회 — 형제 플랜 03-04·03-05 와 경합 방지):** `uv run ruff format --check . && uv run ruff check .` · `uv run pytest --no-header`(failed 0) · `make smoke`(서버 무변경 → `smoke: PASS`).
</verification>

<success_criteria>
- `item_edges_kr.parquet` 존재, 게이트 3 통과(또는 ③ 만 D-10 조치 후 수치 보고), `results/millie_edges_gate.json` 존재
- `build_millie_edges.py` 는 통과 시 무변경, 미달 시 `DESC_REPEAT` 1개 상수 + `build_texts` 1함수만 변경
- 커밋 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-03-SUMMARY.md` — 빌드 출력 원문 · 게이트 출력 · json 전문 · 조치 여부와 before/after · 첫 책 이웃 5권 제목(정성 미리보기) · 변경 파일 목록 · "커밋하지 않는다".
</output>
