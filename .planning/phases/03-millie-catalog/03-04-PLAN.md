---
phase: 03-millie-catalog
plan: 04
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]   # 03-02 = conftest `millie_serving_sample` fixture(1-c 스키마 드리프트 단언에 사용)
files_modified:
  - scripts/build_millie_popularity.py           # NEW — popularity_kr.parquet(all 세그먼트 Must), ≤150줄
  - scripts/export_millie_serving.py             # BOOK_FIELDS 28개 + popularity_kr.json (≤150줄 유지)
  - tests/data/test_millie_popularity.py         # NEW
  - tests/data/test_millie_export.py             # NEW
autonomous: true
requirements: [DATA-07]
lane: Data-B (scripts/ Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - scripts/build_millie_catalog.py
  - scripts/millie_difficulty.py
  - scripts/build_millie_edges.py
  - scripts/millie_parse.py
  - scripts/collect_millie.py
  - data/id_map.csv
  - data/processed/**                            # 실 export 실행은 Plan 06 `make millie`
  - artifacts/**
  - demo/**
  - tests/data/test_millie_catalog.py
  - tests/data/test_millie_edges.py
  - tests/data/test_millie_difficulty.py
  - tests/fixtures/millie/**
  - src/millie_rec/**
  - tests/test_architecture.py
  - tests/conftest.py
  - Makefile
  - pyproject.toml
  - ../.claude/**
  - CLAUDE.md

must_haves:
  truths:
    - "scripts/build_millie_popularity.py 가 books_kr.parquet 에서 popularity_kr.parquet(컬럼 book_id segment score rank)을 만들고, segment='all' 행의 rank 는 books_kr 의 pop_rank 와 동일·score 는 shelf_count(결측 0.0) — 인기 순위 정본은 1곳(pop_rank), 재계산 없음(D-15)"
    - "export_millie_serving.py 의 books_kr.json 행 키가 정확히 28개(계약 9 + subcategories tags publisher subtitle pub_date book_format formats pop_rank millie_label review_count shelf_count completion_prob category_avg_prob expected_min category_avg_min resid_z len_z difficulty difficulty_source top_segment)이고 파일 텍스트에 description·curator_note·seg_dist·millie_id 문자열이 0건이다"
    - "export 가 popularity_kr.json(행 리스트 {book_id, segment, score, rank})을 같은 out_dir 에 추가로 쓰고 세 파일 각각 <50MB 를 단언한다"
    - "difficulty 결측(NaN)은 json 에서 null 로 나간다(_clean) — DATA-04 의 None 이 서빙 산출물까지 보존"
  artifacts:
    - path: "scripts/build_millie_popularity.py"
      provides: "build(books) -> DataFrame(POP_COLUMNS) · main --books --out"
      exports: ["build", "POP_COLUMNS", "SEGMENT_ALL"]
      max_lines: 150
    - path: "scripts/export_millie_serving.py"
      provides: "BOOK_FIELDS(28) · popularity_payload · export(books, edges, popularity, out_dir) 3파일"
      contains: "popularity_kr.json"
      max_lines: 150
    - path: "tests/data/test_millie_export.py"
      provides: "3파일 존재·<50MB · 28키 · 제외 문자열 0건 · NaN→null · popularity 행 형태"
      min_lines: 50
  key_links:
    - from: "scripts/build_millie_popularity.py::build"
      to: "books_kr.parquet pop_rank·shelf_count"
      via: "rank = pop_rank, score = shelf_count.fillna(0)"
      pattern: "pop_rank"
    - from: "scripts/export_millie_serving.py::export"
      to: "artifacts/serving/popularity_kr.json"
      via: "_write(out_dir / 'popularity_kr.json', popularity_payload(pd.read_parquet(pop_path)))"
      pattern: "popularity_kr.json"
---

<objective>
Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md "Phase 3: 밀리 카탈로그 빌드") Success Criteria 4 의 json 절반(DATA-07): (1) `scripts/build_millie_popularity.py`(신규) 가 `popularity_kr.parquet` 의 `all` 세그먼트를 만들고 (2) `scripts/export_millie_serving.py` 가 `books_kr.json` 필드를 `Catalog.meta()` 노출 필드(28개)로 갱신하고 `popularity_kr.json` 을 추가로 쓴다. TDD 는 합성 픽스처 체인(`sample_records.jsonl` → `build_millie_catalog.build` → `build_millie_edges.build` → `build_millie_popularity.build` → `export`) 으로 tmp 에서 한다 — **실 `artifacts/serving/` 은 쓰지 않는다**(Plan 06 `make millie` 가 1회 실행).

Purpose: 서버가 기동 시 읽는 산출물의 형태 정본. 결정 D-15 'popularity_kr 는 all 세그먼트만 Must, 스키마 {book_id, segment, score, rank} 지금 고정'(.planning/phases/03-millie-catalog/03-CONTEXT.md) · D-16 '세그먼트 인기 → scripts/build_millie_popularity.py 새 파일' · Claude's Discretion "export 세부(BOOK_FIELDS = meta 필드, description 0건 grep 단언을 tests/data 에)" 이행. 연령×성별 12세그먼트는 Plan 07(DATA-08 Should) 이 같은 스크립트·같은 파일에 증분.
Output: 스크립트 2(신규 1·수정 1) + 테스트 2. 커밋 0.

**하지 않는 것:** 세그먼트 인기(Plan 07) · SVD npz·popular.json(Plan 05 — export 가 150줄을 넘지 않게 분리, D-16) · Makefile 타깃(Advisor, Plan 06) · 실데이터 export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-PATTERNS.md
@.planning/phases/03-millie-catalog/03-01-SUMMARY.md
@scripts/export_millie_serving.py
@scripts/build_millie_catalog.py
@scripts/build_millie_edges.py
@tests/data/test_millie_edges.py
@tests/app/test_export.py
@../.claude/rules/data.md
@../.claude/rules/serving.md
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**산출물 소유권(`../.claude/rules/architecture.md`):** "`artifacts/serving/` | app(`export`) + `scripts/export_millie_serving.py` | serving은 기동 시 읽기만. Dockerfile `COPY` 대상. 파일당 <50MB, `description` 미포함."

**매니페스트(`../.claude/rules/serving.md`):** "`books_kr.json`(계약 컬럼 + 밀리 컬럼, **`description`·`curator_note` 미포함**) · `item_edges_kr.json` · `content_vectors_kr.npz`(MMR·ILD) · `popularity_kr.json`(all + 세그먼트) · `eval_table.json` + `demo/fallback/popular.json`. `difficulty`는 `books_kr.json` 컬럼(별도 `difficulty_prior.json` 없음)."

**데이터(`../.claude/rules/data.md`):** "**텍스트 2개(`description` ≤400자, `curator_note` ≤100자)는 TF-IDF 입력 전용.** parquet에는 있지만 `artifacts/serving/*`·API·화면에 내보내지 않는다." / "`ratings_count`(= review_count)와 `tags`는 호환 전용… 인기 순위·bestseller 배지·후보 정렬 근거는 `pop_rank`(= `shelf_count` 순위)."

**간결함(`../.claude/rules/simplicity.md`):** 새 `scripts/` 파일 ≤150줄, 기존 export 도 ≤150줄 유지(넘으면 관심사 2개 → Plan 05 가 이미 벡터·fallback 을 분리했다). "설정은 모듈 상단 대문자 상수." 중복 정의 허용("5일 프로젝트에서는 중복 < 결합", architecture.md shared 금지).

**TDD(`../.claude/rules/python-tdd.md`):** Red = AssertionError, `uv run pytest <대상> --no-header`. scripts 테스트는 `importlib` 로더(`test_millie_edges.py` L27-33) + 픽스처 체인. 스크립트가 다른 스크립트를 import 하면 의존 스크립트를 먼저 `_load`(build_millie_catalog 는 자체 `sys.path.insert` 로 `millie_difficulty` 를 찾는다 — Plan 01).
</project_rules_inline>

<interfaces>
From scripts/export_millie_serving.py (현재 102줄 — 변경 지점):
```python
MAX_BYTES = 50 * 1024 * 1024
BOOK_FIELDS = ("book_id","title","authors","image_url","average_rating","ratings_count","original_publication_year",
    "categories","subcategories","book_format","tags","pop_rank","completion_prob","category_avg_prob","expected_min",
    "category_avg_min","millie_label","difficulty_source","shelf_count","review_count","publisher")   # 21개 → 28개로
def _clean(value) -> object        # NA/NaN → None, ndarray → list, numpy 스칼라 → 파이썬
def books_payload(books) -> list[dict]      # books[list(BOOK_FIELDS)].sort_values("book_id")
def edges_payload(edges) -> dict[str, list[list]]
def _write(path, payload) -> int            # json.dumps(ensure_ascii=False) + size < MAX_BYTES 단언
def export(books_path, edges_path, out_dir) -> dict[str, int]
main: --books --edges --out(DIR_ARTIFACTS / "serving")
```
**목표 `BOOK_FIELDS`(28, 이 순서 그대로 — Plan 02 `CatalogKR` fixture·`meta()` 노출 필드와 동일):**
`book_id title authors image_url average_rating ratings_count original_publication_year categories subcategories tags publisher subtitle pub_date book_format formats pop_rank millie_label review_count shelf_count completion_prob category_avg_prob expected_min category_avg_min resid_z len_z difficulty difficulty_source top_segment`.
**제외(변경 없음):** `millie_id rating_observed description curator_note seg_dist isbn13 pages_diag collected_at source`.

From scripts/build_millie_catalog.py (Plan 01 후): `pop_rank` = shelf_count 내림차순, 결측 마지막(`_pop_key`), 1..N 연속 · `shelf_count` Int64 nullable · 픽스처 12권에서 `FIRST_BOOK`(0f1e…6001, shelf 9,814) = pop_rank 1, `NO_SHELF_BOOK`(92a3…000a) = pop_rank 12 · `NO_PROB_BOOK`(6f70…0007) difficulty_source category_prior → difficulty NaN.

`popularity_kr` 행 스키마(D-15 verbatim): `{"book_id": 812, "segment": "all", "score": 9814.0, "rank": 1}` — 세그먼트 확장 시 `"segment": "40대 여성"` 같은 밀리 표기(Plan 07).

픽스처 체인(tests/data/test_millie_edges.py L38-48 verbatim):
```python
@pytest.fixture(scope="module")
def books(tmp_path_factory) -> pd.DataFrame:
    out = tmp_path_factory.mktemp("processed")
    _load("build_millie_catalog").build(FIXTURE, out, raw_dir=out)
    return pd.read_parquet(out / "books_kr.parquet")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): test_millie_popularity 5건 + test_millie_export 8건 → 스텁·현 export 위에서 AssertionError</name>
  <files>tests/data/test_millie_popularity.py, tests/data/test_millie_export.py, scripts/build_millie_popularity.py (스텁)</files>
  <read_first>
    - tests/data/test_millie_edges.py L1-48 (`_load`·픽스처 체인 형식) · tests/data/test_millie_catalog.py L60-80 (픽스처 id 상수)
    - scripts/export_millie_serving.py 전문 · scripts/build_millie_catalog.py `build_frame` `_pop_key`
    - tests/app/test_export.py (json 산출물 키 단정 형식)
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-15 · "Specific Ideas — popularity_kr.json 행 예시"
  </read_first>
  <action>
    **1-a. 스텁 `scripts/build_millie_popularity.py`:** docstring `"""books_kr.parquet → popularity_kr.parquet (DATA-07 Must: segment='all' · DATA-08 Should: 연령×성별 — 적재 계획 02 §6 행 소스 분리)."""`, 상수 `POP_COLUMNS = ("book_id", "segment", "score", "rank")` · `SEGMENT_ALL = "all"` · `SEG_DIST_COL = "seg_dist"  # Plan 07 세그먼트 확장 재료(json 문자열)`, `def build(books: pd.DataFrame) -> pd.DataFrame: return pd.DataFrame(columns=list(POP_COLUMNS))`(빈 프레임 스텁), `main()` argparse `--books`(DIR_PROCESSED/"books_kr.parquet") `--out`(DIR_PROCESSED/"popularity_kr.parquet") + `if __name__ == "__main__": main()`. import 는 `argparse Path pandas millie_rec.contracts.DIR_PROCESSED` 만.

    **1-b. `tests/data/test_millie_popularity.py`** (docstring `"""popularity_kr — 계약(컬럼·segment) · 정확성(rank == pop_rank · score == shelf_count) · 안전성(결측 shelf 0.0·연속 rank)."""`). `_load` 복사; module fixture `books`(위 체인) · `pop_mod = _load("build_millie_popularity")` · `pop = pop_mod.build(books)`. 5건:
    **범위 규칙(D-15 — `segment` 필드는 지금 고정, 연령×성별 행은 Plan 07 이 같은 프레임에 증분):** 아래 단정은 전부 `all_rows = pop[pop["segment"] == "all"]` 위에서 쓴다. 그래야 Plan 07 이 세그먼트 행을 붙여도 이 파일의 단정이 한 줄도 바뀌지 않는다(plan-checker 지적 — 전 프레임 단정은 Plan 07 을 must_not_touch 위반으로 몰아간다).
    - `test_popularity_columns_and_all_segment(pop)` — `list(pop.columns) == ["book_id","segment","score","rank"]`, `"all" in set(pop["segment"])`, `len(all_rows) == 12`, `all_rows["book_id"].is_unique`
    - `test_rank_equals_books_pop_rank(pop, books)` — `all_rows.set_index("book_id")["rank"].to_dict() == books.set_index("book_id")["pop_rank"].astype(int).to_dict()`
    - `test_rank_is_contiguous_from_one(pop)` — `sorted(all_rows["rank"]) == list(range(1, 13))`
    - `test_score_is_shelf_count_desc_with_missing_zero(pop, books)` — all_rows 에서 rank 1 행의 `score == 9814.0`; rank 12 행(NO_SHELF_BOOK)의 `score == 0.0`; `all_rows.sort_values("rank")["score"].is_monotonic_decreasing`; `pop["score"].dtype.kind == "f"`
    - `test_main_writes_parquet(tmp_path, pop_mod, books, monkeypatch)` — books 를 `tmp/"b.parquet"` 로 저장, `monkeypatch.setattr("sys.argv", ["x", "--books", str(tmp/"b.parquet"), "--out", str(tmp/"p.parquet")])`, `pop_mod.main()` → `pd.read_parquet(tmp/"p.parquet")` 컬럼 4개 · `segment == "all"` 행 12개

    **1-c. `tests/data/test_millie_export.py`** (docstring `"""export_millie_serving — 계약(3파일·28키·<50MB) · 정확성(NaN→null·popularity 행) · 안전성(description·curator_note·seg_dist·millie_id 0건)."""`). module fixture `exported(tmp_path_factory)`: 체인 → `books_kr.parquet`·`item_edges_kr.parquet`(`_load("build_millie_edges").build(books, edges_mod.read_best_links(FIXTURE))` 저장)·`popularity_kr.parquet`(`_load("build_millie_popularity").build(books)` 저장) → `exp = _load("export_millie_serving")`; `sizes = exp.export(books_p, edges_p, pop_p, out_dir)` → 반환 `(out_dir, sizes)`. **주의 시그니처:** 현 `export(books_path, edges_path, out_dir)` 3인자 → 목표 4인자 `export(books_path, edges_path, popularity_path, out_dir)`. RED 단계에서 현 코드는 `TypeError` 를 낸다 — 그것은 Red 가 아니므로 fixture 안에서 `inspect.signature(exp.export).parameters` 길이가 3이면 `pytest.fail("export 는 popularity_path 를 받아야 한다(DATA-07)")` 로 **명시적 실패**를 만든다(`pytest.fail` 은 `Failed:` 로 Red 인정). `EXPECTED_BOOK_KEYS` 28개를 **손으로** 리스트에 적는다(스크립트 상수 import 금지 관례). 8건:
    - `test_three_files_exist_under_size_cap(exported)` — `books_kr.json`·`item_edges_kr.json`·`popularity_kr.json` 존재, 각 `stat().st_size < 50*1024*1024`, `sizes` 키 집합이 세 이름
    - `test_books_json_rows_have_exactly_meta_fields(exported)` — 모든 행 `set(row) == set(EXPECTED_BOOK_KEYS)` 그리고 `list(row) == EXPECTED_BOOK_KEYS`(순서), 12행, `book_id` 오름차순
    - `test_books_json_excludes_author_text_and_raw_ids(exported)` — `raw = (out/"books_kr.json").read_text("utf-8")`; `raw.count('"description"') == 0` · `'"curator_note"'` 0 · `'"seg_dist"'` 0 · `'"millie_id"'` 0 · `'"rating_observed"'` 0; 그리고 픽스처 FIRST_BOOK 의 description 첫 20자(`"편의점 야간 아르바이트를"`)가 raw 에 없음(값 자체도 없음)
    - `test_nan_difficulty_becomes_null(exported)` — `difficulty_source == "category_prior"` 인 행이 ≥1 이고 그 행들 전부 `row["difficulty"] is None and row["resid_z"] == 0.0`; `millie_index` 행은 `0 < difficulty < 1`
    - `test_edges_json_unchanged_shape(exported)` — `{"src": [[dst, weight, source], …]}`, 첫 키의 첫 원소 길이 3, weight 내림차순
    - `test_popularity_json_rows(exported)` — `all_rows = [r for r in rows if r["segment"] == "all"]` 12행, 각 `set(row) == {"book_id","segment","score","rank"}`, all_rows 의 `rank` 1..12, `score` float (전 행 수는 단정하지 않는다 — Plan 07 증분 대비)
    - `test_books_json_keys_match_serving_sample_fixture(exported, millie_serving_sample)` — **스키마 드리프트 가드(architect 지적):** Plan 02 conftest fixture `millie_serving_sample` 가 만든 `books_kr.json` 첫 행 키 집합 == `set(EXPECTED_BOOK_KEYS)` == 실제 export 첫 행 키 집합. 28키 사본 3벌(conftest fixture · 이 테스트의 `EXPECTED_BOOK_KEYS` · 스크립트 `BOOK_FIELDS`)이 서로 어긋나면 여기서 잡힌다. fixture 반환 형태(경로 or dict)는 `tests/conftest.py` 를 읽고 맞춘다.
    - `test_book_format_values_and_contract_nine(exported)` — 모든 행 `row["book_format"] in {"전자책","오디오북","챗북"}`; 계약 9개 이름(`book_id title authors image_url average_rating ratings_count original_publication_year categories tags`) 이 첫 행 키에 전부 있음(`/contract-sync` 카탈로그 항목 5 와 같은 검사)

    **1-d. RED:** `uv run pytest tests/data/test_millie_popularity.py tests/data/test_millie_export.py --no-header 2>&1 | tail -40` → popularity 5 failed(빈 프레임 → AssertionError), export 8 failed 또는 error(fixture 의 `pytest.fail` — 출력에 `Failed: export 는 popularity_path` 문구). `ImportError`·`SyntaxError` 0. 출력 SUMMARY 발췌.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test -f scripts/build_millie_popularity.py && uv run pytest tests/data/test_millie_popularity.py tests/data/test_millie_export.py --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/data/test_millie_popularity.py` == 5; `grep -c "def test_" tests/data/test_millie_export.py` == 7
    - `grep -c '"top_segment"' tests/data/test_millie_export.py` ≥ 1 그리고 `grep -c '"description"' tests/data/test_millie_export.py` ≥ 1(제외 단언)
    - RED 출력에 `AssertionError`/`E  assert`/`Failed:` 가 있고 `ImportError`·`ModuleNotFoundError`·`SyntaxError`·`NameError` 없음
    - `scripts/build_millie_popularity.py` 에 `POP_COLUMNS = ("book_id", "segment", "score", "rank")`·`SEGMENT_ALL = "all"` 존재
    - `git diff --stat -- scripts/build_millie_catalog.py scripts/build_millie_edges.py tests/fixtures src artifacts demo` → 출력 없음
  </acceptance_criteria>
  <done>12건 RED(AssertionError·Failed). 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): build_millie_popularity.build 구현 + export BOOK_FIELDS 28 + popularity_kr.json → 13 passed, 두 파일 ≤150줄</name>
  <files>scripts/build_millie_popularity.py, scripts/export_millie_serving.py</files>
  <read_first>
    - scripts/build_millie_popularity.py (스텁) · scripts/export_millie_serving.py (전문)
    - tests/data/test_millie_popularity.py · tests/data/test_millie_export.py (단정이 계약)
    - scripts/build_millie_catalog.py `_pop_key`(L196-201) — 재계산하지 않고 pop_rank 를 옮긴다
  </read_first>
  <action>
    **2-a. `build_millie_popularity.py::build`:**
    ```python
    def build(books: pd.DataFrame) -> pd.DataFrame:
        """segment='all' 행: rank = books_kr.pop_rank(정본 1곳, 재계산 없음), score = shelf_count(결측 0.0)."""
        shelf = pd.to_numeric(books["shelf_count"], errors="coerce").astype(float).fillna(0.0)
        rows = pd.DataFrame({
            "book_id": books["book_id"].astype("int64"),
            "segment": SEGMENT_ALL,
            "score": shelf,
            "rank": books["pop_rank"].astype("int64"),
        })
        return rows.sort_values(["segment", "rank"]).reset_index(drop=True)[list(POP_COLUMNS)]
    ```
    `main`: 읽기 → `build` → `args.out.parent.mkdir(parents=True, exist_ok=True)` → `to_parquet(index=False)` → `print(f"popularity rows={len(df)} segments={df['segment'].nunique()} → {args.out}")`. (Plan 07 이 `build` 에 세그먼트 행을 이어 붙일 자리 — 이 플랜에서는 `# Plan 07(DATA-08): seg_dist 세그먼트 행을 여기서 concat` 주석 1줄만.)

    **2-b. `export_millie_serving.py`:**
    - `BOOK_FIELDS` 를 `<interfaces>` 의 28개 순서로 교체(한 튜플, 한 줄에 4~5개씩). 바로 위 주석 `# = data/catalog_kr.py meta() 노출 필드. description·curator_note·seg_dist·millie_id 는 넣지 않는다(data.md)`.
    - `def popularity_payload(pop: pd.DataFrame) -> list[dict]: return [{k: _clean(v) for k, v in row.items()} for row in pop.sort_values(["segment", "rank"]).to_dict("records")]`
    - `export(books_path: Path, edges_path: Path, popularity_path: Path, out_dir: Path) -> dict[str, int]` — 기존 두 항목 + `"popularity_kr.json": _write(out_dir / "popularity_kr.json", popularity_payload(pd.read_parquet(popularity_path)))`.
    - `main`: `ap.add_argument("--popularity", type=Path, default=DIR_PROCESSED / "popularity_kr.parquet")` 추가, `export(args.books, args.edges, args.popularity, args.out)`.
    - 모듈 docstring 첫 줄을 `"""processed parquet → artifacts/serving/{books_kr,item_edges_kr,popularity_kr}.json (DATA-07, 적재 계획 §3·§7). 벡터 npz 는 export_millie_vectors.py, demo/fallback/popular.json 은 export_millie_fallback.py(Plan 05)."""` 로.
    - 줄 수 확인 `wc -l scripts/export_millie_serving.py` ≤ 150(예상 ≈120).

    **2-c. GREEN 확인:** `uv run pytest tests/data/test_millie_popularity.py tests/data/test_millie_export.py --no-header` → `13 passed`. `uv run ruff format scripts/build_millie_popularity.py scripts/export_millie_serving.py tests/data/test_millie_popularity.py tests/data/test_millie_export.py && uv run ruff check <같은 4파일>`. 회귀 확인은 **자기 테스트 2파일만**(위 명령) — 같은 wave 의 Plan 03·05 가 `tests/data/` 에서 RED 중일 수 있어 전체 `tests/data` 는 Advisor wave 종료 기준선(Plan 06 verification)에서만 돌린다. 테스트 수는 popularity 5 + export 8 = `13 passed`.

    **2-d. 실 실행 금지:** `uv run python scripts/export_millie_serving.py` 를 **실행하지 않는다** — `artifacts/serving/` 은 Plan 06 `make millie` 가 1회 갱신(형제 플랜 03-05 와 같은 디렉터리 경합·Makefile 체인 검증을 위해).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check scripts/build_millie_popularity.py scripts/export_millie_serving.py tests/data/test_millie_popularity.py tests/data/test_millie_export.py && uv run pytest tests/data/test_millie_popularity.py tests/data/test_millie_export.py --no-header 2>&1 | tail -3 && [ "$(wc -l < scripts/export_millie_serving.py)" -le 150 ] && [ "$(wc -l < scripts/build_millie_popularity.py)" -le 150 ] && git diff --quiet --stat -- artifacts demo && echo "artifacts untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/data/test_millie_popularity.py tests/data/test_millie_export.py --no-header` → `13 passed`
    - `wc -l` export ≤ 150 · popularity ≤ 150
    - `grep -c '"top_segment",' scripts/export_millie_serving.py` == 1; `grep -c '"description"\|"curator_note"\|"seg_dist"\|"millie_id"' scripts/export_millie_serving.py` == 0(필드 튜플에 없음 — docstring/주석의 백틱 없는 언급은 허용되므로 따옴표 포함 문자열로 검사)
    - `grep -c "popularity_kr.json" scripts/export_millie_serving.py` ≥ 2; `grep -c "def popularity_payload" scripts/export_millie_serving.py` == 1
    - `grep -c "millie_rec.serving\|millie_rec.app\|millie_rec.data" scripts/build_millie_popularity.py scripts/export_millie_serving.py` → 0 0
    - `git status --short artifacts demo` → 출력 없음(실 export 미실행); `git log --oneline | head -1` 불변
  </acceptance_criteria>
  <done>popularity_kr(all) 빌더와 3파일 export 가 합성 체인으로 증명됨. 실 산출물은 Plan 06. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `books_kr.parquet` → `artifacts/serving/books_kr.json` | 저작 텍스트가 커밋·배포 산출물로 새는 유일한 경로 |
| `artifacts/serving/*.json` → Dockerfile COPY → Railway | 파일 크기 상한(50MB) 넘으면 GitHub·빌드 실패 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-14 | Information Disclosure | `books_payload` | mitigate | `BOOK_FIELDS` 화이트리스트(28) — `description`·`curator_note`·`seg_dist`·`millie_id` 제외. 테스트가 raw 텍스트에서 키와 값(description 첫 20자) 0건 단언 |
| T-03-15 | Denial of Service | `_write` 크기 | mitigate | `assert size < MAX_BYTES` 유지, 세 파일 각각 검사(테스트) |
| T-03-16 | Tampering | `popularity_kr` 순위 조작(재계산 불일치) | mitigate | rank 를 `pop_rank` 에서 복사만 — 테스트 `test_rank_equals_books_pop_rank` |
| T-03-17 | Information Disclosure | 세그먼트 분포(`seg_dist`) 원본 | accept | 원본 json 문자열은 export 하지 않는다. Plan 07 이 파생 순위만 `popularity_kr` 행으로 내보낸다 |
</threat_model>

<verification>
**이 플랜(Worker)이 직접 확인하는 것:** `uv run ruff format <자기 4파일> && uv run ruff check <자기 4파일>` 클린 · `uv run pytest tests/data/test_millie_popularity.py tests/data/test_millie_export.py --no-header` failed 0(전체 `tests/data` 는 Advisor wave 종료 기준선) · 실 `artifacts/`·`demo/` 무변경.

**공통 기준선(Advisor 가 wave 2 종료 시 1회 — 03-03·03-05 와 경합 방지):** `uv run ruff format --check . && uv run ruff check .` · `uv run pytest --no-header`(failed 0) · `make smoke`(`smoke: PASS`).
</verification>

<success_criteria>
- `popularity_kr.parquet` 빌더(all)와 export 3파일이 합성 체인으로 13 passed
- `books_kr.json` 키 28개 정확, 제외 문자열 0건, NaN→null
- 두 스크립트 ≤150줄, 실 산출물 무변경, 커밋 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-04-SUMMARY.md` — RED 발췌 · GREEN 요약 · `BOOK_FIELDS` 28개 최종 목록 · `export` 새 시그니처(Plan 06 Makefile `millie-export` 가 호출하는 CLI 인자: `--books --edges --popularity --out`) · "커밋하지 않는다".
</output>
