---
phase: 03-millie-catalog
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - src/millie_rec/app/pipeline.py               # Advisor 전용 — CatalogPopPipeline · load_catalog · build_pipelines(catalog=) 하위 호환
  - src/millie_rec/app/server.py                 # Advisor 전용 — 카탈로그 조건부 주입(시그니처 불변)
  - tests/app/test_pipeline.py                   # 카탈로그 분기 3건 추가(기존 4건 무수정)
  - tests/app/test_server_catalog.py             # NEW — 서버 주입(fixture 20권)
  - tests/serving/test_smoke.py                  # 배선 2줄: 0-인자 lambda → `lambda **_:` + load_catalog 패치(단정 불변)
  - Makefile                                     # Advisor 전용 — millie-popularity 신설 · millie-export 3스크립트 · millie 체인
  - data/processed/popularity_kr.parquet         # gitignore — make millie 산출
  - data/processed/books_kr.parquet              # 재빌드(멱등)
  - data/processed/item_edges_kr.parquet         # 재빌드(멱등)
  - data/id_map.csv                              # 재빌드 시 append-only(신규 유효분만)
  - artifacts/serving/books_kr.json              # 커밋 대상(승인 후)
  - artifacts/serving/item_edges_kr.json
  - artifacts/serving/popularity_kr.json
  - artifacts/serving/content_vectors_kr.npz
  - demo/fallback/popular.json                   # 소유권 예외(architecture.md 산출물 표) — export 가 갱신
  - results/millie_coverage.csv                  # 게이트 재실행으로 갱신
  - results/millie_edges_gate.json
  - report/draft.md                              # P3 실측 1줄
  - ../.claude/rules/data.md                     # 조건부(3-d) — D-08·D-14 문구는 이미 반영돼 있어 현재 no-op(grep 확인만)
  - ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md   # §1 단계 4~7 · §2 수치 · §3 로그
  - ../.assets/개발일지/2026-09-05_Day1_GSD_초기화와_계약_freeze.md  # (또는 실행일 파일) D 항목 1건 신규 + 기존 2건 확인
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07]
lane: 조립(Advisor 직접) — GSD 실행자 대행 → Advisor 가 diff·숫자·테스트로 검증. 선례 = Phase 2 'Track A 정량 평가 기반'(.planning/phases/02-track-a/02-06-PLAN.md) 의 실측 플랜
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md)). artifacts/serving/·results/·id_map·popular.json 은 커밋 대상이지만 커밋은 사용자 승인 후 Advisor 가 한다
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/**                    # schemas.py 포함 — HTTP 계약 변경 0
  - src/millie_rec/data/**                       # Plan 02 결과를 소비만
  - src/millie_rec/retrieval/**
  - scripts/**                                   # Plan 01·03·04·05 결과를 Makefile 로 호출만
  - tests/data/**
  - tests/conftest.py
  - tests/test_architecture.py
  - tests/fixtures/millie/**
  - demo/js/**                                   # inspector "static_popular" 는 Phase 6
  - demo/scripts/**
  - demo/mock/**
  - pyproject.toml
  - Dockerfile

must_haves:
  truths:
    - "artifacts/serving/books_kr.json 이 있으면 app/server.py 가 CatalogKR 을 catalog=·neighbors=·book_stats=·fallback=GlobalPopularFallback(catalog) 으로 주입하고 pipelines['pop'] 이 Track B 인기(pop_rank 순, user.seen 제외)가 된다; 없거나 손상이면 로그 후 Phase 2 동작 그대로 기동한다 — D-13"
    - "서버는 카탈로그가 있을 때 Goodbooks artifacts/popularity.json 을 로드하지 않는다(평가 전용); build_pipelines(artifact=POP_ARTIFACT, *, catalog=None) 은 catalog=None 이면 기존 Goodbooks 로직 그대로(하위 호환 — 기존 test_pipeline 4건 무수정 통과)"
    - "GET /api/recommend?seeds=1,2,3&k=5 가 fallback_level 0 · model_version pop_v1 · rows[0].row_id trending · items 5개(book_id ∉ {1,2,3}) · rows[0].items[*].title 이 한국어 밀리 책 제목을 내고, seeds 없는 요청은 level 3 이지만 trending 행이 카탈로그 인기로 채워진다"
    - "make millie 가 build→edges→popularity→export(3스크립트) 를 순차 실행해 artifacts/serving/ 4파일 + demo/fallback/popular.json 을 만들고, 파일당 <50MB · description/curator_note 0건 · RecommendOut.model_validate(popular.json) 통과 · data/id_map.csv 삭제 0 이다"
    - "uv run pytest --no-header 전체 통과(skipped 0 — 실데이터 게이트 2개가 돈다) · ruff 클린 · make smoke PASS · /contract-sync 카탈로그 항목 통과"
    - "report/draft.md 3-3 절에 Phase 3 실측 1줄(N권·커버리지·동일 카테고리 비율·SVD 128, '숫자는 최종 스냅샷으로 교체' 표시), 트래킹 03 §1 단계 4~6 ✅·§2 수치, 개발일지 D 항목이 기록됐다"
  artifacts:
    - path: "src/millie_rec/app/pipeline.py"
      provides: "CatalogPopPipeline(name='pop') · load_catalog(serving_dir) -> CatalogKR | None · build_pipelines(artifact, *, catalog=None)"
      exports: ["CatalogPopPipeline", "load_catalog", "build_pipelines", "fit_pipelines", "PopPipeline"]
      max_lines: 150
    - path: "src/millie_rec/app/server.py"
      provides: "catalog = load_catalog() → create_app(... catalog=catalog, neighbors=catalog, book_stats=catalog)"
      contains: "load_catalog()"
      max_lines: 40
    - path: "tests/app/test_server_catalog.py"
      provides: "서버 모듈 재import + load_catalog 패치 → level 0 pop_v1 · 익명 level 3 인기 채움"
      min_lines: 40
    - path: "Makefile"
      provides: "millie-popularity 타깃 · millie-export 3스크립트 · millie = build→edges→popularity→export"
      contains: "millie-popularity"
    - path: "artifacts/serving/content_vectors_kr.npz"
      provides: "SVD 128 다양성 벡터(Phase 4 hybrid_div 입력)"
    - path: "demo/fallback/popular.json"
      provides: "RecommendOut level 3 정적 fallback"
      contains: "\"fallback_v1\""
    - path: "report/draft.md"
      provides: "P3 실측 1줄"
      contains: "Phase 3 '밀리 카탈로그 빌드'"
  key_links:
    - from: "src/millie_rec/app/server.py"
      to: "src/millie_rec/app/pipeline.py::load_catalog"
      via: "기동 1회 호출 — 테스트는 millie_rec.app.pipeline.load_catalog 를 monkeypatch"
      pattern: "load_catalog\\(\\)"
    - from: "src/millie_rec/app/pipeline.py::CatalogPopPipeline.recommend"
      to: "millie_rec.data.CatalogKR.popular / meta"
      via: "popular(n=k+len(seen)) → seen 제외 → meta 조인(title·authors·image_url·book_format·difficulty)"
      pattern: "self.catalog.popular\\("
    - from: "Makefile millie-export"
      to: "scripts/export_millie_serving.py · export_millie_vectors.py · export_millie_fallback.py"
      via: "&& 체인"
      pattern: "export_millie_fallback.py"
    - from: "artifacts/serving/books_kr.json"
      to: "GET /api/recommend level 0"
      via: "load_catalog → CatalogKR → CatalogPopPipeline"
      pattern: "\"fallback_level\":0"
---

<objective>
Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md "Phase 3: 밀리 카탈로그 빌드") 의 **조립·실측·기록** — Advisor 전용 파일만 만진다: (1) `app/pipeline.py`·`app/server.py` 에 카탈로그 조건부 주입과 Track B `pop`(D-13) (2) Makefile 에 `millie-popularity`·3스크립트 `millie-export`·`millie` 체인 (3) `make millie` 실측 → `artifacts/serving/` 4파일 + `demo/fallback/popular.json` + `results/` 갱신 → 전체 스위트·ruff·`make smoke`·level 0 curl·`/contract-sync` 카탈로그 항목 (4) `report/draft.md` P3 1줄 · 트래킹 03 · 개발일지 · `../.claude/rules/data.md` 문구 확인.

Purpose: ROADMAP Success Criteria 4("서버에 주입하면 `/api/recommend` 가 밀리 책으로 응답")·5(전체 스위트·smoke·contract-sync) 를 **응답과 파일로** 증명하고, Phase 2 Deferred '서버의 Goodbooks pop 배선 교체'(.planning/phases/02-track-a/02-CONTEXT.md) 를 이행한다. 결정 D-13 '서버 pop 을 Track B 인기로 교체, Goodbooks 아티팩트는 서버에서 로드하지 않음'(.planning/phases/03-millie-catalog/03-CONTEXT.md) · D-01·D-02(스냅샷·숫자 정본) · D-08·D-14(rules 문구) 이행.
Output: `app/` 2파일 + 테스트 3파일 + Makefile + 실측 산출물 + 문서 3곳. 커밋 0(커밋 대상 파일은 사용자 승인 후 Advisor).

**중단 규칙(실행자):** `make millie` 어느 단계가 실패하면 재시도 루프 없이 오류 전문·단계·경과를 SUMMARY 에 적고 `## CHECKPOINT REACHED`. `git diff --numstat data/id_map.csv` 삭제가 0 이 아니면 `git checkout -- data/id_map.csv` 후 중단. 게이트(커버리지·이웃) fail 은 숨기지 않는다.

**하지 않는 것:** `serving/**` 변경(HTTP 계약 0 변경 — `create_app` 시그니처 그대로 인자만 채움, Phase 1 D-09) · compose 5행·level 1/2·state(Phase 5) · `demo/js` 수정(Phase 6 — `inspector.js` `"static_popular"` 라벨은 Plan 05 인계 노트) · 새 의존성 · 파서·스크립트 코드 변경 · 배치 중단(D-03 은 Day 2 오전 Advisor 별도 결정).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-PATTERNS.md
@.planning/phases/03-millie-catalog/03-01-SUMMARY.md
@.planning/phases/03-millie-catalog/03-02-SUMMARY.md
@.planning/phases/03-millie-catalog/03-03-SUMMARY.md
@.planning/phases/03-millie-catalog/03-04-SUMMARY.md
@.planning/phases/03-millie-catalog/03-05-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/app/pipeline.py
@src/millie_rec/app/server.py
@src/millie_rec/serving/api.py
@src/millie_rec/serving/fallback.py
@src/millie_rec/data/__init__.py
@tests/app/test_pipeline.py
@tests/serving/test_smoke.py
@tests/conftest.py
@Makefile
@report/draft.md
@../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md
@../.assets/개발일지/README.md
@../.claude/rules/architecture.md
@../.claude/rules/local-run.md
@../.claude/rules/serving.md
@../.claude/rules/data.md
@../.claude/rules/report.md
@../.claude/rules/references.md
@../.claude/rules/python-tdd.md
@../.claude/skills/contract-sync/SKILL.md

<project_rules_inline>
**Advisor 전용(`../.claude/rules/architecture.md`):** "`contracts.py` `serving/schemas.py` `app/` `pyproject.toml` `Makefile` `Dockerfile` `.claude/**` 는 Advisor 전용. Worker 가 필요하다고 판단하면 수정하지 말고 보고한다." / "`app`은 다른 슬라이스의 **공개 이름만** import한다: `from millie_rec.retrieval import ItemKNNRetriever` ✅ / `from millie_rec.retrieval.itemknn import ...` ❌" → `from millie_rec.data import BOOKS_KR_JSON, DIR_SERVING, CatalogKR`(Plan 02 가 노출). "`tests/test_architecture.py`가 위 규칙을 정적으로 검사한다."

**로컬 기동(`../.claude/rules/local-run.md`):** "**아티팩트·DB·네트워크 없이 기동한다.** `artifacts/serving/`이 비어 있으면 빈 `trending` 행 + level 3." / "**모든 브리프의 완료 기준에 로컬 스모크를 넣는다:** `uv run pytest --no-header` + `make smoke`." / "`app/server.py`: `create_app(...)` 호출 + `StaticFiles(directory="demo", html=True)`를 마지막에 `/`로 마운트."

**서빙(`../.claude/rules/serving.md`):** "`create_app(pipelines, fallback, *, catalog=None, db, neighbors=None, book_stats=None, state=None)` — `fallback` 뒤는 keyword-only, `db`만 필수. `app/server.py`는 주입·StaticFiles·uvicorn 진입점만." / "artifacts는 기동 시 1회 로드." — `data/__init__` 이 goodbooks(pandas) 를 끌어와 서버 기동 시 pandas 가 1회 import 된다(03-PATTERNS `data/__init__.py` 절) — 요청 경로 아님, 허용. SUMMARY 에 기동 시간 실측 1줄.

**TDD(`../.claude/rules/python-tdd.md`):** "`app/` 조립 · `contracts.py` | Advisor 직접. 아키텍처 테스트 + 계약 테스트로 대체" — 그래도 `tests/app` 3건은 **먼저 써서 AssertionError 를 본 뒤** 구현한다. 금지 "기존 테스트의 단정을 고치는 것" → `test_smoke.py` 변경은 monkeypatch **배선 2줄**(lambda 인자·load_catalog 패치)만, 단정 문장은 글자 그대로. `tests/app/test_pipeline.py` 기존 4건 무수정(하위 호환이 그 증거).

**숫자 정본(../CLAUDE.md §2-6·`../.claude/rules/report.md`):** PDF·트래킹 숫자는 `results/millie_coverage.csv`·`results/millie_edges_gate.json` 에서 복사만. D-02: 최종 수치는 Day 2 오전 마지막 재빌드 스냅샷 — draft 에 "숫자는 최종 스냅샷으로 교체" 표시.

**참조 표기(`../.claude/rules/references.md`):** 개발일지·트래킹·draft 에 `D-13` 같은 번호 단독 금지 — `D-13 '서버 pop 을 Track B 인기로 교체'(.planning/phases/03-millie-catalog/03-CONTEXT.md)` 형식. 개발일지 새 항목 전 `grep -h '^### D' ../.assets/개발일지/*.md | tail -3`.

**개발일지 6요소(`../.assets/개발일지/README.md`):** 문제 · 동기 · 선택지(버린 것 포함) · 근거(사실 ≤3) · 장애 · 결과(반영 위치·잃은 것·되돌리는 조건). 항목당 12줄 이내, 제목 `### D<n>. <제목>  [축 N · main §X / 구체화 Y]  (확정 — …)`.
</project_rules_inline>

<interfaces>
**현재 app/pipeline.py(55줄):** `POP = VARIANTS[0]` · `class PopPipeline(retriever)` · `fit_pipelines(train)`(make eval 용, 불변) · `build_pipelines(artifact: Path = POP_ARTIFACT) -> dict[str, Pipeline]`(없으면 `{}` + `log.info`, 손상 `(OSError, ValueError, KeyError, TypeError)` → `log.exception` + `{}`).
**현재 app/server.py(18줄):** `app = create_app(pipelines=build_pipelines(), fallback=GlobalPopularFallback(None), db=Database(resolve_db_path()))` → `app.mount("/", StaticFiles(directory=ROOT / "demo", html=True), name="demo")` **반드시 마지막**.
**기존 테스트 제약(그대로 통과해야 함):**
- `tests/app/test_pipeline.py` 4건: `build_pipelines(tmp/"none.json") == {}` · 손상 2케이스 `{}` + ERROR 로그 · `build_pipelines(path)` 로 Goodbooks `popularity.json` 로드 → `{"pop"}`, seeds 제외·position·score · `fit_pipelines(interactions)`.
- `tests/serving/test_smoke.py` L164-198 2건: `monkeypatch.setattr("millie_rec.app.pipeline.build_pipelines", lambda: {})` / `lambda: {"pop": _FakePop()}` → `sys.modules.pop("millie_rec.app.server")` → `importlib.import_module` → 단정 `fallback_level == 3` / `== 0`·`model_version == "pop_v1"`·`items == [4,5,6,7,8]`·익명 3. **server.py 가 `build_pipelines(catalog=catalog)` 로 호출하면 0-인자 lambda 는 TypeError** → 두 lambda 를 `lambda **_: {...}` 로 바꾸고 `monkeypatch.setattr("millie_rec.app.pipeline.load_catalog", lambda: None)` 한 줄을 각각 추가(머신에 실 artifacts 가 있어도 결과가 같아야 한다 — 테스트 주석 "아티팩트 유무(머신 상태)에 의존하지 않도록" 그대로).
**Plan 02 공개 표면(`millie_rec.data`):** `CatalogKR(books, edges)` · `CatalogKR.load(serving_dir: Path = DIR_SERVING)`(books_kr.json 없으면 `FileNotFoundError`, 손상 `ValueError`) · `meta(ids) -> list[dict]`(28키, description 없음) · `popular(categories=(), n=50)`(eligible·pop_rank 순) · `eligible` · `neighbors` · `stats` · `user_level -> None` · 상수 `DIR_SERVING`(= `artifacts/serving`) · `BOOKS_KR_JSON`. fixture `millie_serving_sample`(tests/conftest.py, 세션, Path): book_id 1..20, pop_rank == id, 비자격 18·19·20, 제목 `밀리 표본 도서 {i}`.
**Plan 04·05 CLI(SUMMARY 인계):** `scripts/build_millie_popularity.py --books --out` · `scripts/export_millie_serving.py --books --edges --popularity --out` · `scripts/export_millie_vectors.py --books --out` · `scripts/export_millie_fallback.py --books --out` — 기본값이 전부 정본 경로라 Makefile 은 인자 없이 호출한다.
**serving/fallback.py 관례(복제):** `ids = [b for b in catalog.popular(n=k + len(user.seen)) if b not in user.seen][:k]` → `ScoredItem(score=float(len(ids)-i), source="popularity", position=i, source_channels=("popularity",))`. `GlobalPopularFallback.name == "fallback"` 이라 재사용 대신 `app` 에 `name="pop"` 래퍼(Claude's Discretion "Track B pop Pipeline 구현 위치" → app glue).
**api.py:** `K_DEFAULT, K_MAX = 40, 100` — k 상한은 쿼리에서 이미 막힌다. level 0 응답: `items` = `Recommendation(book_id, score)` 평탄화(title 없음), `rows[0].items` = ScoredItem 직렬화(title 등 카드 필드 포함) — "밀리 책 제목" 확인은 `rows[0].items[*].title`.
**Makefile 현재:** `millie-build`(L13-14 `build_millie_catalog.py && uv run pytest tests/data -q`) · `millie-edges`(L16-17) · `millie-export`(L19-20 `export_millie_serving.py` 하나) · `millie: millie-build millie-edges millie-export`(L22) · `smoke`(8010 포트, 3개 curl → `smoke: PASS`). `.PHONY` L2.
**draft.md 3-3 절(L158~):** `## 3-3. 실서비스 ↔ 5일 데모 대응` 표 4행 뒤 `---` — 표 아래·`---` 위에 bullet 1개 추가.
**개발일지 현황(실측 2026-09-05):** 마지막 `### D63`(BM25 기각·150줄 예외). **`### D62` 가 2건 중복**(Phase 3 착수 결정 7건 / PDF 원고 형식) — 중복 해소는 README 예외 규정상 Advisor 결정이라 이 플랜은 **보고만** 한다. CONTEXT "개발일지 D 항목 2건 예정(① 착수 결정 ② 150줄 예외+BM25)" 은 이미 D62·D63 으로 작성돼 있음 → 이 플랜은 **실행·실측 결과 D 항목 1건(D66)** 만 새로 쓴다.
**`../.claude/rules/data.md` 현황(실측):** L36 `tags`는 **기본 제외** … Phase 3 CONTEXT D-08 · 표지 `*.millie.co.kr` … Phase 3 CONTEXT D-14 — **이미 갱신됨**. 이 플랜은 grep 으로 확인만(0건이면 그때 편집).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/pipeline.py·app/server.py 카탈로그 주입(D-13) — 테스트 3건 RED → 구현 → smoke 배선 2줄 → tests/app·tests/serving 전부 GREEN</name>
  <files>src/millie_rec/app/pipeline.py, src/millie_rec/app/server.py, tests/app/test_pipeline.py, tests/app/test_server_catalog.py, tests/serving/test_smoke.py</files>
  <read_first>
    - src/millie_rec/app/pipeline.py · src/millie_rec/app/server.py (전문)
    - tests/app/test_pipeline.py (기존 4건 — 무수정) · tests/serving/test_smoke.py L164-198 (배선 변경 2곳)
    - src/millie_rec/serving/fallback.py L21-34 (recommend 관례) · src/millie_rec/serving/api.py L81-135 (`create_app` 시그니처·level 0 경로)
    - .planning/phases/03-millie-catalog/03-02-SUMMARY.md (공개 이름·fixture 규칙) · tests/conftest.py `millie_serving_sample`
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-13 · "Specific Ideas — 카탈로그 주입 후 기대"
    - ../.claude/rules/architecture.md import 규칙 · ../.claude/rules/local-run.md
  </read_first>
  <behavior>
    - `build_pipelines(tmp/"none.json")` → `{}` · Goodbooks 아티팩트 로드 → `{"pop"}` (기존 4건 그대로)
    - `build_pipelines(tmp/"broken.json", catalog=_FakeCatalog())` → `{"pop"}` 이고 ERROR 로그 0건(손상 아티팩트를 **열지 않는다**); `recommend(UserState(None, explicit_seeds=(1,)), 2)` → book_id `[2, 3]` · position `[0, 1]` · score `[2.0, 1.0]` · source `"popularity"` · source_channels `("popularity",)` · `title == "책2"`(meta 조인)
    - `load_catalog(tmp_path)` → `None`(books_kr.json 없음, INFO 로그); `"not json"` 쓰면 `None` + ERROR 로그 1건; `load_catalog(millie_serving_sample)` → `CatalogKR` 이고 `popular(n=3) == [1, 2, 3]`
    - 서버 모듈: `load_catalog` 를 fixture 로더로 패치해 재import → `GET /api/recommend?seeds=1,2,3&k=5` → `fallback_level 0` · `model_version "pop_v1"` · `rows[0].row_id "trending"` · `[items.book_id] == [4,5,6,7,8]` · `rows[0].items[0].title == "밀리 표본 도서 4"`; `GET /api/recommend`(익명) → `fallback_level 3` · `model_version "fallback_v1"` · `items == []` · `rows[0].items` 의 book_id 앞 5개 `[1,2,3,4,5]` 이고 18·19·20 없음(카탈로그 주입된 GlobalPopularFallback)
  </behavior>
  <action>
    **1-a. RED — 테스트 먼저.**
    `tests/app/test_pipeline.py` 에 3건 추가(기존 4건 한 글자도 바꾸지 않는다): `_FakeCatalog` 클래스(`meta(ids) -> [{"book_id": b, "title": f"책{b}", "authors": "a", "image_url": "https://img.millie.co.kr/x.jpg", "book_format": "전자책", "difficulty": 0.4} for b in ids]`, `popular(categories=(), n=50) -> list(range(1, 11))[:n]`, `eligible(ids) -> list(ids)`) · `test_build_pipelines_with_catalog_uses_track_b_pop_and_skips_artifact(tmp_path, caplog)`(broken 아티팩트 경로 + `catalog=`; ERROR 로그 0; recommend 단정 위 behavior) · `test_load_catalog_none_when_missing_or_corrupt(tmp_path, caplog)` · `test_load_catalog_reads_serving_dir(millie_serving_sample)`. import 에 `CatalogPopPipeline, load_catalog` 추가(스텁 전이라 ImportError 가 나면 **먼저 스텁 이름을 만든다** — collection error 는 Red 가 아니다).
    `tests/app/test_server_catalog.py`(신규, docstring `"""app/server.py 카탈로그 주입(D-13) — 계약(level 0 pop_v1) · 정확성(seen 제외·pop_rank 순·meta 조인) · 안전성(익명 level 3 인기 채움). 실 artifacts/serving 은 읽지 않는다."""`): `tests/serving/test_smoke.py` L184-198 을 복제해 `monkeypatch.setenv(ENV_DATA_DIR, str(tmp_path))` · `monkeypatch.setattr("millie_rec.app.pipeline.load_catalog", lambda: CatalogKR.load(millie_serving_sample))` · `sys.modules.pop("millie_rec.app.server", None)` · `importlib.import_module("millie_rec.app.server")` · `with TestClient(server.app) as c:` → 위 behavior 단정 전부(테스트 2건: `test_server_injects_catalog_pop_level0_with_korean_titles` · `test_server_anonymous_is_level3_but_trending_filled_from_catalog`).
    스텁: `app/pipeline.py` 에 `class CatalogPopPipeline: name = POP; def __init__(self, catalog): ...; def recommend(self, user, k): return []` · `def load_catalog(serving_dir=DIR_SERVING): return None` · `build_pipelines(artifact=POP_ARTIFACT, *, catalog=None)`(catalog 무시). `uv run pytest tests/app --no-header 2>&1 | tail -20` → 신규 5건 failed **AssertionError**, 기존 4건 passed. 출력 SUMMARY 발췌.

    **1-b. GREEN — `src/millie_rec/app/pipeline.py`**(≤150줄, 예상 ≈100):
    ```python
    from millie_rec.contracts import VARIANTS, Catalog, Pipeline, ScoredItem, UserState
    from millie_rec.data import BOOKS_KR_JSON, DIR_SERVING, CatalogKR
    from millie_rec.retrieval import POP_ARTIFACT, PopularityRetriever

    SOURCE_POPULARITY = "popularity"  # serving/fallback.py 와 같은 값 — serving 공개 표면에 없어 중복 정의

    class CatalogPopPipeline:
        """D-13: Track B 인기(pop_rank 순, seen 제외) + meta 조인. GlobalPopularFallback 과 같은 점수 관례, name 만 'pop'."""
        name = POP
        def __init__(self, catalog: Catalog) -> None:
            self.catalog = catalog
        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            ids = [b for b in self.catalog.popular(n=k + len(user.seen)) if b not in user.seen][:k]
            meta = {int(m["book_id"]): m for m in self.catalog.meta(ids)}
            return [
                ScoredItem(book_id=b, score=float(len(ids) - i), source=SOURCE_POPULARITY, position=i,
                           source_channels=(SOURCE_POPULARITY,), title=meta.get(b, {}).get("title"),
                           authors=meta.get(b, {}).get("authors"), image_url=meta.get(b, {}).get("image_url"),
                           book_format=meta.get(b, {}).get("book_format"), difficulty=meta.get(b, {}).get("difficulty"))
                for i, b in enumerate(ids)
            ]

    def load_catalog(serving_dir: Path = DIR_SERVING) -> CatalogKR | None:
        """D-13: books_kr.json 있으면 CatalogKR, 없으면 None(Phase 2 동작), 손상이면 로그 후 None — 기동을 막지 않는다."""
        if not (serving_dir / BOOKS_KR_JSON).exists():
            log.info("no catalog at %s — serving without Track B catalog", serving_dir)
            return None
        try:
            return CatalogKR.load(serving_dir)
        except (OSError, ValueError, KeyError, TypeError):
            log.exception("catalog unreadable at %s — serving without catalog", serving_dir)
            return None

    def build_pipelines(artifact: Path = POP_ARTIFACT, *, catalog: Catalog | None = None) -> dict[str, Pipeline]:
        """서버 기동. 카탈로그가 있으면 Track B pop(D-13, Goodbooks 아티팩트는 열지 않는다), 없으면 Phase 2 D-10 로직."""
        if catalog is not None:
            return {POP: CatalogPopPipeline(catalog)}
        ...(기존 본문 그대로)
    ```
    `meta.get(b, {})` 반복은 `m = meta.get(b, {})` 지역 변수로 정리(≤150줄 유지). `fit_pipelines`·`PopPipeline` 불변.

    **1-c. `src/millie_rec/app/server.py`:**
    ```python
    from millie_rec.app.pipeline import build_pipelines, load_catalog
    ...
    # Phase 3 D-13: artifacts/serving/books_kr.json 있으면 CatalogKR 을 catalog·neighbors·book_stats·fallback 에 주입,
    # 없거나 손상이면 None → Phase 2 동작. 시그니처 불변(Phase 1 D-09) — 인자만 채운다. Phase 5 → state.
    catalog = load_catalog()
    app = create_app(
        pipelines=build_pipelines(catalog=catalog),
        fallback=GlobalPopularFallback(catalog),
        catalog=catalog,
        db=Database(resolve_db_path()),
        neighbors=catalog,
        book_stats=catalog,
    )
    app.mount("/", StaticFiles(directory=ROOT / "demo", html=True), name="demo")  # 반드시 마지막
    ```
    (`CatalogKR` 하나가 세 Protocol 을 모두 만족 — structural typing. `catalog` 가 None 이면 셋 다 None.)

    **1-d. `tests/serving/test_smoke.py` 배선 2곳(단정 불변):** L170 `lambda: {}` → `lambda **_: {}`, L189 `lambda: {"pop": _FakePop()}` → `lambda **_: {"pop": _FakePop()}`; 두 테스트 각각 그 줄 바로 위에 `monkeypatch.setattr("millie_rec.app.pipeline.load_catalog", lambda: None)  # Phase 3: 실 artifacts 유무와 무관` 추가. `git diff --numstat tests/serving/test_smoke.py` → 추가 4·삭제 2 이내, `assert` 줄 diff 0.

    **1-e. GREEN 확인:** `uv run pytest tests/app tests/serving tests/test_architecture.py --no-header` → 전부 passed(신규 5 + 기존). `uv run ruff format src/millie_rec/app tests/app tests/serving/test_smoke.py && uv run ruff check src/millie_rec/app tests/app tests/serving/test_smoke.py`. `wc -l src/millie_rec/app/pipeline.py` ≤ 150.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check src/millie_rec/app tests/app tests/serving/test_smoke.py && uv run pytest tests/app tests/serving tests/test_architecture.py --no-header 2>&1 | tail -3 && [ "$(wc -l < src/millie_rec/app/pipeline.py)" -le 150 ] && grep -c "load_catalog()" src/millie_rec/app/server.py</automated>
  </verify>
  <acceptance_criteria>
    - RED 출력(SUMMARY 발췌)에 신규 5건 `AssertionError`, `ImportError` 없음; GREEN: `uv run pytest tests/app tests/serving tests/test_architecture.py --no-header` failed 0
    - `grep -c "class CatalogPopPipeline" src/millie_rec/app/pipeline.py` == 1; `grep -c "def load_catalog" ` == 1; `grep -c "catalog: Catalog | None = None" ` == 1; `grep -c "from millie_rec.data import" src/millie_rec/app/pipeline.py` == 1 이고 `grep -c "millie_rec.data\." src/millie_rec/app/pipeline.py` == 0(공개 표면만)
    - `grep -c "neighbors=catalog" src/millie_rec/app/server.py` == 1; `grep -c "book_stats=catalog" ` == 1; `grep -c "GlobalPopularFallback(catalog)" ` == 1; `app.mount` 줄이 파일의 마지막 문장
    - `git diff tests/app/test_pipeline.py | grep "^-" | grep -c "assert"` == 0(기존 단정 삭제 0); `git diff tests/serving/test_smoke.py | grep "^-" | grep -vc "lambda"` == 1(헤더 줄만 — 삭제된 줄은 lambda 2줄뿐)
    - `grep -c "def test_" tests/app/test_server_catalog.py` == 2; `grep -c "millie_serving_sample" tests/app/test_server_catalog.py` ≥ 2
    - `wc -l src/millie_rec/app/pipeline.py` ≤ 150 · `src/millie_rec/app/server.py` ≤ 40
  </acceptance_criteria>
  <done>서버가 카탈로그 유무에 따라 Track B pop / Phase 2 동작을 선택하고, 20권 fixture 로 level 0(pop_v1·한국어 제목)·익명 level 3 인기 채움이 증명됨. 기존 테스트 단정 무수정. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 2: Makefile 타깃 → make millie 실측 → 산출물 검증 → 전역 게이트(pytest·ruff·smoke·curl level 0)·/contract-sync 카탈로그 항목</name>
  <files>Makefile, data/processed/popularity_kr.parquet, data/processed/books_kr.parquet, data/processed/item_edges_kr.parquet, data/id_map.csv, artifacts/serving/books_kr.json, artifacts/serving/item_edges_kr.json, artifacts/serving/popularity_kr.json, artifacts/serving/content_vectors_kr.npz, demo/fallback/popular.json, results/millie_coverage.csv, results/millie_edges_gate.json</files>
  <read_first>
    - Makefile L1-22 (`.PHONY`·millie 타깃 4개)
    - .planning/phases/03-millie-catalog/03-04-SUMMARY.md · 03-05-SUMMARY.md (CLI 인자 인계) · 03-01-SUMMARY.md · 03-03-SUMMARY.md (첫 스냅샷 수치 — 재빌드 후 비교)
    - ../.claude/skills/contract-sync/SKILL.md process 5(Track B 카탈로그 정합 항목)
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-01·D-02·D-17 · "Specific Ideas — 카탈로그 주입 후 GET /api/recommend 기대"
  </read_first>
  <action>
    **2-a. Makefile(Advisor 전용):** `.PHONY` 에 `millie-popularity` 추가. 타깃:
    ```make
    millie-popularity: ## books_kr.parquet → popularity_kr.parquet (all 세그먼트 Must · 연령×성별 Should, D-15)
    	uv run python scripts/build_millie_popularity.py

    millie-export: ## artifacts/serving/{books_kr,item_edges_kr,popularity_kr}.json + content_vectors_kr.npz + demo/fallback/popular.json (description 미포함)
    	uv run python scripts/export_millie_serving.py && uv run python scripts/export_millie_vectors.py && uv run python scripts/export_millie_fallback.py

    millie: millie-build millie-edges millie-popularity millie-export  ## 수집 이후 전체 재빌드
    ```
    **게이트 실행 순서 수정(plan-checker·architect BLOCKER, 2026-09-05):** 현 `millie-build` 는 books 재빌드 직후 `uv run pytest tests/data -q` 전체를 돌리는데, 그 안의 `tests/data/test_millie_edges.py::test_neighbour_quality_gate` 는 `books_kr.parquet` 의 모든 `book_id` 가 `item_edges_kr.parquet` 에 이웃을 갖는다고 단언한다(L163 `assert not orphans`). 배치가 계속 돌아 N 이 Plan 03 시점보다 크면(D-01 정상 상황) stale edges 에 새 책이 없어 단언이 깨지고 `&&` 체인이 `millie-edges` 전에 멈춘다. 따라서 게이트를 산출물 뒤로 옮긴다:
    ```make
    millie-build: ## JSONL → books_kr.parquet + id_map.csv(append-only) + 커버리지 게이트(이웃 게이트는 millie-edges 뒤)
    	uv run python scripts/build_millie_catalog.py && uv run pytest tests/data/test_millie_catalog.py --no-header

    millie-edges: ## content_sim + category_best → item_edges_kr.parquet + 이웃 게이트 3
    	uv run python scripts/build_millie_edges.py && uv run pytest tests/data/test_millie_edges.py --no-header
    ```
    `pytest tests/data -q` 문자열은 Makefile 에서 사라진다. 탭 들여쓰기 확인.

    **2-b. 실측 `make millie`:** `cp data/id_map.csv "$SCRATCH/id_map.before.csv"`(`SCRATCH=/private/tmp/claude-501/-Users-shinwonchul-Documents----------/0de83255-33bf-42e3-ac30-5617663e6d70/scratchpad`, `/tmp` 금지) → `time make millie 2>&1 | tee "$SCRATCH/make_millie.log" | tail -30`. 기대 순서: books=N → `test_millie_catalog.py` 통과(커버리지 게이트) → edges=E → `test_millie_edges.py` 통과(이웃 게이트 3) → popularity rows=N segments=1 → 3 json 크기 → npz 크기 dim=128 → popular.json. 어느 단계든 실패 → 중단 규칙. 배치가 계속 돌고 있어 N 이 Plan 01 시점보다 커질 수 있다(D-01 — 정상).

    **2-c. 산출물 검증(전부 SUMMARY 에 원문):**
    ```bash
    ls -la artifacts/serving/ demo/fallback/popular.json
    find artifacts/serving -size +50M | wc -l                       # 0
    grep -o '"description"' artifacts/serving/books_kr.json | wc -l  # 0
    grep -o '"curator_note"' artifacts/serving/books_kr.json | wc -l # 0
    grep -o '"seg_dist"' artifacts/serving/books_kr.json | wc -l     # 0
    git diff --numstat data/id_map.csv                               # 삭제(2열) 0
    diff <(head -n "$(wc -l < "$SCRATCH/id_map.before.csv")" data/id_map.csv) "$SCRATCH/id_map.before.csv"   # 빈 출력 — before 사본 **전체**(HEAD 1,066행 + Plan 01 이 추가한 미커밋 행)가 새 파일의 접두로 불변. git numstat 은 HEAD 기준이라 미커밋 행 변동을 못 잡는다(architect 지적)
    cat results/millie_coverage.csv; cat results/millie_edges_gate.json
    uv run python -c "
    import json, numpy as np
    from millie_rec.serving.schemas import RecommendOut
    from millie_rec.contracts import MODEL_VERSION_FALLBACK, FALLBACK_GLOBAL_POP
    b = json.load(open('artifacts/serving/books_kr.json'))
    need = {'book_id','title','authors','image_url','average_rating','ratings_count','original_publication_year','categories','tags'}
    assert need <= set(b[0]), need - set(b[0]); assert len(set(b[0])) == 28, len(b[0])
    assert set(r['book_format'] for r in b) <= {'전자책','오디오북','챗북'}
    p = json.load(open('demo/fallback/popular.json')); out = RecommendOut.model_validate(p)
    assert out.fallback_level == FALLBACK_GLOBAL_POP and out.model_version == MODEL_VERSION_FALLBACK and out.rows[0].row_id == 'trending' and len(out.rows[0].items) == 40
    z = np.load('artifacts/serving/content_vectors_kr.npz'); assert z['vectors'].shape == (len(b), 128) and z['vectors'].dtype == np.float32
    pop = json.load(open('artifacts/serving/popularity_kr.json')); assert set(pop[0]) == {'book_id','segment','score','rank'} and {r['segment'] for r in pop} == {'all'}
    print('artifacts ok', len(b), z['vectors'].shape, len(pop))
    "
    ```
    `import millie_rec.serving.schemas` 는 검증 스크립트(테스트 성격)에서만 — 스크립트 코드가 아니다.

    **2-d. 전역 게이트(wave 3 는 단일 플랜이라 여기서 직접):** `uv run ruff format --check . && uv run ruff check .` 클린 · `uv run pytest --no-header 2>&1 | tail -3` → failed 0, **skipped 0**(커버리지·이웃 게이트가 실산출물로 돈다; 기준선 171 passed → 이제 ≥171 + Plan 01 9 + Plan 02 15 + Plan 03 0 + Plan 04 12 + Plan 05 12 + Plan 06 5 = ≥224) · `make smoke` → `smoke: PASS`.

    **2-e. level 0 실측(별도 포트):**
    ```bash
    uv run uvicorn millie_rec.app.server:app --port 8012 --log-level warning & echo $! > "$SCRATCH/uv.pid"; sleep 4
    curl -s "localhost:8012/api/recommend?seeds=1,2,3&k=5" | tee "$SCRATCH/level0.json" | uv run python -c "
    import json,sys,re; d=json.load(sys.stdin); it=d['rows'][0]['items']
    print(d['fallback_level'], d['model_version'], [i['book_id'] for i in d['items']], d['rows'][0]['row_id'], len(it))
    print([i['title'] for i in it][:5]); assert all(re.search(r'[가-힣]', i['title'] or '') for i in it), 'title not Korean'
    assert not {1,2,3} & {i['book_id'] for i in d['items']}"
    curl -s "localhost:8012/api/recommend" | uv run python -c "import json,sys; d=json.load(sys.stdin); print(d['fallback_level'], d['model_version'], len(d['items']), len(d['rows'][0]['items']), d['rows'][0]['items'][0]['book_id'])"
    kill $(cat "$SCRATCH/uv.pid")
    ```
    기대 첫 줄 `0 pop_v1 [<5개, 1·2·3 아님>] trending 5`, 둘째 줄 한국어 제목 5개, 셋째 줄 `3 fallback_v1 0 40 <pop_rank 1 인 book_id>`. 기동 시간(uvicorn 로그 첫 줄까지) 을 SUMMARY 에(pandas 1회 import 포함, 참고값).

    **2-f. `/contract-sync` 카탈로그 항목:** 스킬을 실행하거나 process 5 를 인라인으로 — 계약 9개 이름 존재(2-c 에서 확인) · description·curator_note 없음 · `book_format` 값 · `data/id_map.csv` 삭제 0 · `demo/fallback/popular.json` `RecommendOut` 통과 · `source_channels` 에 `itemknn` 없음(`grep -c itemknn demo/fallback/popular.json` == 0). 결과 SUMMARY 에.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && grep -c "millie-popularity" Makefile && test -f artifacts/serving/books_kr.json && test -f artifacts/serving/item_edges_kr.json && test -f artifacts/serving/popularity_kr.json && test -f artifacts/serving/content_vectors_kr.npz && [ "$(find artifacts/serving -size +50M | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -o '"description"' artifacts/serving/books_kr.json | wc -l | tr -d ' ')" = "0" ] && [ "$(git diff --numstat data/id_map.csv | cut -f2)" = "0" ] && uv run python -c "import json; from millie_rec.serving.schemas import RecommendOut; RecommendOut.model_validate(json.load(open('demo/fallback/popular.json'))); print('popular.json ok')" && uv run ruff format --check . && uv run ruff check . && uv run pytest --no-header 2>&1 | tail -2 && make smoke</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^millie-popularity:" Makefile` == 1; `grep -c "export_millie_vectors.py" Makefile` == 1; `grep -c "export_millie_fallback.py" Makefile` == 1; `grep -c "^millie: millie-build millie-edges millie-popularity millie-export" Makefile` == 1; `grep -c "pytest tests/data/test_millie_catalog.py" Makefile` == 1; `grep -c "pytest tests/data/test_millie_edges.py" Makefile` == 1; `grep -c "pytest tests/data -q" Makefile` == 0
    - `make millie` 종료 코드 0; `artifacts/serving/` 에 4파일 + `eval_table.json`, `find artifacts/serving -size +50M` 빈 출력
    - `grep -o '"description"\|"curator_note"\|"seg_dist"' artifacts/serving/books_kr.json | wc -l` == 0; 2-c 검증 스크립트가 `artifacts ok <N> (<N>, 128) <N>` 출력
    - `git diff --numstat data/id_map.csv` 삭제 == 0; `results/millie_coverage.csv` 에 `title,1.0000`·`_n_records,<N>`·`_n_skipped_titleless,` 존재; `results/millie_edges_gate.json` `self_edges 0 · min_degree ≥5 · same_category_share ≤ 0.70`
    - `uv run ruff format --check . && uv run ruff check .` 클린; `uv run pytest --no-header` → failed 0 · skipped 0 · passed ≥ 224
    - `make smoke` 마지막 줄 `smoke: PASS`; curl 첫 줄 `0 pop_v1 [` 로 시작하고 `trending 5` 로 끝남, 제목 5개 전부 한글 포함, 셋째 줄 `3 fallback_v1 0 40`
    - `grep -c itemknn demo/fallback/popular.json` == 0; `grep -c '"fallback_v1"' demo/fallback/popular.json` == 1
    - `git status --short` 에 `artifacts/serving/*`·`demo/fallback/popular.json`·`results/*`·`data/id_map.csv`·`Makefile`·`src/millie_rec/app/*`·`tests/app/*`·`tests/serving/test_smoke.py` 가 보이고 `data/processed` 는 없음; `git log --oneline | head -1` 불변
  </acceptance_criteria>
  <done>`make millie` 한 명령이 4+1 산출물을 만들고, 전체 스위트(게이트 포함)·ruff·smoke·level 0 curl(한국어 제목)·contract-sync 카탈로그 항목이 통과. ROADMAP Success Criteria 4·5 충족. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 기록 — report/draft.md P3 1줄 · 트래킹 03 §1·§2·§3 · 개발일지 D 항목(실행 결과) · rules 문구 2곳 확인</name>
  <files>report/draft.md, ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md, ../.assets/개발일지/2026-09-05_Day1_GSD_초기화와_계약_freeze.md</files>
  <read_first>
    - report/draft.md L158-168 (`## 3-3. 실서비스 ↔ 5일 데모 대응` 표와 `---`)
    - ../.claude/rules/report.md (P3 정의·문체·"숫자는 results 에서만")
    - results/millie_coverage.csv · results/millie_edges_gate.json (Task 2 결과 — 복사할 숫자)
    - ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md 전문(§1 표 행 4~7 · §2 표 · §3 로그 형식)
    - ../.assets/개발일지/README.md "작성 규칙"·"항목 템플릿" · `grep -h '^### D' ../.assets/개발일지/*.md | tail -3`
    - ../.claude/rules/data.md L36·표지 문구(D-08·D-14 반영 확인용) · ../.claude/rules/references.md
  </read_first>
  <action>
    **3-a. `report/draft.md` — 3-3 절 표 아래(`---` 위)에 bullet 1개.** 숫자는 `results/millie_coverage.csv`(`_n_records`·`image_url`·`completion_prob` 행)·`results/millie_edges_gate.json`(`same_category_share`) 에서 **그대로 복사**(`<N>`·`<img>`·`<prob>`·`<share>` 자리만 채움, 백분율 변환은 `×100` 소수 1자리):
    ```
    - (Day 1 실측, Phase 3 '밀리 카탈로그 빌드') 데모 카탈로그 = 밀리의서재 공개 도서 페이지 <N>권(2026-09-05 수집, 비로그인 화면의 수치·메타·표지 URL만, 리뷰 텍스트 미저장, 책 소개는 TF-IDF 입력 전용·미노출). 커버리지 title 100%·표지 <img>%·완독지수 <prob>%(`results/millie_coverage.csv`, 결측 완독지수 책은 `difficulty=None` 으로 가드 모집단에서 제외). 앵커 이웃은 제목·소개 문자 2~4gram TF-IDF cosine top-20(content_sim, 동일 카테고리 비율 <share>, `results/millie_edges_gate.json`), 다양성 벡터는 같은 TF-IDF 의 SVD 128 — 협업 필터링(Item-KNN) 수치는 Goodbooks(Track A)에서만 측정하며 두 트랙의 숫자를 섞지 않는다. 서버는 `artifacts/serving/books_kr.json` 이 있으면 카탈로그를 주입해 `/api/recommend` 가 밀리 책으로 응답한다. [숫자는 Day 2 오전 최종 스냅샷 재빌드 후 교체]
    ```
    `git diff --numstat report/draft.md` → `1\t0`. 다른 절 불변.

    **3-b. 트래킹 03:** §1 표 행 4·5·6 을 `✅` + 시각 + 수치(`_n_records`·`_categories_ge_20` / `n_edges`·`same_category_share` / `ls -la artifacts/serving` 크기 4개 + popular.json), 행 7 을 `▶ 진행`(이 문서·개발일지 갱신 중 → 3-c 후 `✅`). §2 표의 `—` 셀 채움: 수집 성공/실패(`n_success`·`n_skipped_status`·`n_skipped_empty`·`n_skipped_titleless`), 보유율 7개, 카테고리 종수·20권 이상, 이웃 게이트 3 수치, export 크기. §3 로그에 1행: `| 09-05 <hh:mm> | 첫 스냅샷 빌드(배치 진행 중) | D-01 '지금 있는 권수로 첫 빌드'(.planning/phases/03-millie-catalog/03-CONTEXT.md) | 게이트 통과 <N>권. 최종 수치는 Day 2 오전 재빌드 후 교체(D-02) |`. Plan 03 이 D-10 조치를 했으면 그 행도 추가(before/after).

    **3-c. 개발일지:** `grep -h '^### D' ../.assets/개발일지/*.md | tail -3` 로 마지막 번호 확인(실측 2026-09-05 저녁: D63, 단 `### D62` 2건 중복). 실행일 파일(오늘이면 `2026-09-05_Day1_GSD_초기화와_계약_freeze.md`, 09-06 이면 README 규칙대로 `2026-09-06_Day2_<주제>.md` 신설) `## 의사결정` 마지막 항목 아래에 **1건**(6요소·12줄 이내):
    `### D66. Phase 3 '밀리 카탈로그 빌드' 실행·실측 — 첫 스냅샷 <N>권 게이트 통과 · 서버 pop = CatalogPopPipeline(카탈로그 meta 조인, name 'pop') · popular.json 을 RecommendOut level 3 형태로 교체 · smoke 테스트 배선 2건(load_catalog 패치)  [축 ② 활용 데이터 · 적재 계획 02 §7 / .planning/phases/03-millie-catalog/03-0{1..6}-SUMMARY.md]  (확정 — Advisor 실측 09-05)`
    - 문제: 밀리 카탈로그를 서버에 올리려면 Goodbooks 임시 배선(Phase 2 결정 '아티팩트 있으면 로드'(.planning/phases/02-track-a/02-CONTEXT.md D-10))을 바꿔야 했고, 기존 smoke 테스트가 0-인자 `build_pipelines` 를 패치하고 있었다.
    - 동기: Day 2 최소 종료 조건(게이트 2개 통과)·데모가 한국 책으로 응답.
    - 선택지: ① `build_pipelines(catalog=)` 하위 호환 + `load_catalog` 별도 함수(채택) ② server.py 안에서 직접 로드(테스트 패치 불가) ③ `GlobalPopularFallback` 재사용(name 이 'fallback' 이라 model_version 이 틀림 — 버림). popular.json: ① RecommendOut 형태(채택) ② v1 mock 유지(41건 검증 실패 — 버림).
    - 근거: `test_smoke.py` L170·L189 0-인자 lambda 실측 · `RecommendOut.model_validate` 41 errors 실측(03-PATTERNS) · 커버리지·이웃 게이트 수치(`results/`).
    - 장애: (Plan 01~05 SUMMARY 의 CHECKPOINT/조치 여부 — 없으면 "없음").
    - 결과: `app/pipeline.py`·`app/server.py`·Makefile 3타깃·`artifacts/serving/` 4파일·`demo/fallback/popular.json`. 잃은 것: 서버 기동 시 pandas 1회 import(`data/__init__`). 되돌리는 조건: `books_kr.json` 삭제 → Phase 2 동작. `inspector.js` `static_popular` 라벨은 Phase 6.
    기존 D62(Phase 3 착수 7건)·D63(BM25·150줄) 존재를 `grep -c "^### D62\|^### D63"` 로 확인만(CONTEXT 예정 2건은 이미 기록됨). **D62 중복 번호는 SUMMARY 에 "Advisor 결정 필요(README 중복 해소 예외)" 로 보고만** — 이 플랜에서 재번호하지 않는다.

    **3-d. `../.claude/rules/data.md` 문구 2곳 확인(편집은 0건이면):** `grep -c "Phase 3 CONTEXT D-08" ../.claude/rules/data.md` ≥ 1(tags 기본 제외) · `grep -c '\*.millie.co.kr' ../.claude/rules/data.md` ≥ 1 · `grep -c "img.millie.co.kr. 핫링크만" ../.claude/rules/data.md` == 0. 셋 다 만족하면 "이미 반영(09-05)" 로 SUMMARY 에 적고 끝. 하나라도 어긋나면 해당 문장을 CONTEXT D-08/D-14 문구로 교체(Advisor 전용 파일).

    **3-e. SUMMARY 준비:** Task 1 RED/GREEN 발췌 · `make millie` 로그 tail · 2-c 검증 출력 · pytest 요약 줄(passed 수·skipped 0) · ruff · `make smoke` 마지막 줄 · curl 3줄 · contract-sync 결과 · 문서 3곳 diff 요약 · D62 중복 정정 완료(D64) 확인 · Phase 6 인계(inspector 라벨·latency_ms 막대·**`make mock` 금지**: `demo/scripts/make_mock.py` 가 구형 v1 `popular.json` 을 재생성해 Plan 05 산출물을 덮어쓴다 — Phase 6 '데모 재구성'이 make_mock 을 고치기 전까지 실행하지 않는다) · Phase 4 인계(`VectorsKR`·`CatalogKR.neighbors`·`stats` 주입 준비, `hybrid_div` 는 `create_app` 인자에 없어 Phase 4 가 `app/` 에서 소비) · `git status --short` · "커밋하지 않는다 — 커밋 대상: artifacts/serving 4파일·demo/fallback/popular.json·results 2파일·data/id_map.csv(사용자 승인 후)".
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && grep -c "Phase 3 '밀리 카탈로그 빌드'" report/draft.md && grep -c "최종 스냅샷" report/draft.md && [ "$(git diff --numstat report/draft.md | cut -f1,2)" = "$(printf '1\t0')" ] && grep -c "^### D66" ../.assets/개발일지/*.md | grep -v ":0" && grep -c "Phase 3 CONTEXT D-08" ../.claude/rules/data.md && grep -c '\*.millie.co.kr' ../.claude/rules/data.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "(Day 1 실측, Phase 3 '밀리 카탈로그 빌드')" report/draft.md` == 1; `grep -c "최종 스냅샷" report/draft.md` ≥ 1; `grep -c "SVD 128" report/draft.md` ≥ 1; `git diff --numstat report/draft.md` == `1\t0`; draft 의 `<N>권` 숫자 == `results/millie_coverage.csv` `_n_records` 값
    - 트래킹 03 §1 표 행 4·5·6·7 상태 셀이 `✅`(`grep -c "| ✅ |" ../.assets/설계서/데이터\ 소스/03_밀리_데이터_적재_트래킹.md` ≥ 7); §2 표에 `—` 셀이 "수집 성공 / 실패"·"보유율"·"카테고리 종수"·"이웃 게이트"·"export 크기" 행에서 사라짐; §3 로그에 `첫 스냅샷 빌드` 행 존재
    - `grep -c "^### D66\." ../.assets/개발일지/*.md` 합 == 1; 항목에 `문제`·`동기`·`선택지`·`근거`·`장애`·`결과` 6요소 줄 존재, 12줄 이내; 본문에 `D-13` 단독 참조 0(`이름 + 경로` 형식)
    - `grep -c "Phase 3 CONTEXT D-08" ../.claude/rules/data.md` ≥ 1 · `grep -c '\*.millie.co.kr' ../.claude/rules/data.md` ≥ 1 · `grep -c "img.millie.co.kr. 핫링크만" ../.claude/rules/data.md` == 0(이미 반영이면 diff 0)
    - `git log --oneline | head -1` 불변(커밋 0)
  </acceptance_criteria>
  <done>P3 실측 1줄·트래킹 03 단계 4~7·개발일지 D66 가 기록되고 rules 문구 2곳이 확인됐다. D62 중복은 보고만. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 4: 최종 스냅샷 재빌드 체크포인트 — 배치 종료(또는 Day 2 오전 중단) 후 `make millie` 1회 → 수치 교체</name>
  <files>data/processed/books_kr.parquet, data/processed/item_edges_kr.parquet, data/processed/popularity_kr.parquet, data/id_map.csv, results/millie_coverage.csv, results/millie_edges_gate.json, artifacts/serving/books_kr.json, artifacts/serving/item_edges_kr.json, artifacts/serving/popularity_kr.json, artifacts/serving/content_vectors_kr.npz, demo/fallback/popular.json, report/draft.md, ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md, ../.assets/개발일지/2026-09-05_Day1_GSD_초기화와_계약_freeze.md</files>
  <read_first>
    - 이 플랜 Task 2 (2-b·2-c·2-d 명령을 그대로 재사용)
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-01(배치 종료 후 재빌드 1회)·D-02(PDF 수치 = 마지막 스냅샷)·D-03(Day 2 09-06 오전에도 돌면 그 시점 중단, 미수집 프론티어 수 기록)
    - ../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md §2 (수치 표) · §4 (재실행 방법)
    - report/draft.md P3 문장(Task 3-a 가 남긴 `[숫자는 최종 스냅샷으로 교체]` 마커)
  </read_first>
  <action>
    **4-a. 진입 조건(사람 게이트):** `pgrep -fl collect_millie` 가 비었거나(배치 자연 종료), Day 2(2026-09-06) 오전이면 `## CHECKPOINT REACHED` 로 사용자에게 "배치 중단·최종 스냅샷 확정" 승인을 받은 뒤 `pkill -f collect_millie` (D-03 — 결정 '상한 없음'(트래킹 03 헤더)에 시간 상한을 보완, 번복 아님). 승인 전에는 이 태스크를 시작하지 않는다.
    **4-b. 프론티어 기록:** `data/raw/discovered_urls.txt` 총 줄 수 − `millie_pages.jsonl` 고유 `millie_id` 수 = 미수집 프론티어. 트래킹 03 §2 표에 `최종 스냅샷 <날짜 시각> · 고유 <N> · 미수집 프론티어 <F>` 1행 추가.
    **4-c. 재빌드·재검증:** Task 2-b 와 동일하게 `cp data/id_map.csv "$SCRATCH/id_map.final_before.csv"` → `time make millie` → 2-c 검증 전부(before 사본을 `id_map.final_before.csv` 로 바꿔 접두 불변 diff) → 2-d 전역 게이트(pytest·ruff·smoke·curl level 0) 재실행. 실패 시 중단 규칙 그대로.
    **4-d. 수치 교체(D-02):** `results/millie_coverage.csv` 의 `_n_records`·`image_url`·`completion_prob`·`seg_dist` 와 `results/millie_edges_gate.json` 의 동일 카테고리 비율을 읽어 `report/draft.md` P3 문장의 `[숫자는 최종 스냅샷으로 교체]` 마커와 `<N>` 자리를 실제 값으로 바꾸고 마커를 제거. 트래킹 03 §2 에 같은 수치. 개발일지 D66 '결과' 요소에 "최종 스냅샷 N권·재빌드 후 id_map 삭제 0" 1줄 추가(새 D 항목 아님).
    **4-e. Day 3 freeze 규칙 기록:** 개발일지 D66 결과 줄에 "이후 `make millie` 재실행 금지(Day 3 모델 freeze 전 마지막 스냅샷, D-02)" 명시. SUMMARY 에 4-b 수치·4-c 출력 tail·4-d diff 를 적는다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && [ -z "$(pgrep -f collect_millie)" ] && grep -c "최종 스냅샷" "../.assets/설계서/데이터 소스/03_밀리_데이터_적재_트래킹.md" && [ "$(grep -c '최종 스냅샷으로 교체' report/draft.md)" = "0" ] && diff <(head -n "$(wc -l < "$SCRATCH/id_map.final_before.csv")" data/id_map.csv) "$SCRATCH/id_map.final_before.csv" && make smoke</automated>
  </verify>
  <acceptance_criteria>
    - `pgrep -f collect_millie` 빈 출력(배치 종료 또는 사용자 승인 후 중단)
    - 트래킹 03 §2 에 "최종 스냅샷" 행 1개 이상, 미수집 프론티어 수 포함
    - `report/draft.md` 에 `최종 스냅샷으로 교체` 마커 0건, P3 문장의 N·커버리지 수치가 `results/millie_coverage.csv` 값과 일치
    - `id_map.final_before.csv` 전체가 새 `data/id_map.csv` 의 접두(diff 빈 출력) · Task 2-c·2-d 검증 전부 재통과 · `make smoke` PASS
  </acceptance_criteria>
  <done>최종 스냅샷 1회 재빌드로 PDF·트래킹·개발일지 수치가 같은 `results/` 값을 가리키고, 이후 재빌드 금지가 기록됨(D-01·D-02·D-03 이행). 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `artifacts/serving/*.json` → `load_catalog` → 서버 기동 | 커밋·배포되는 파일이 기동 경로에서 파싱된다. 손상 = 가용성 |
| `CatalogPopPipeline` → `/api/recommend` 응답 | 도서 메타(title·authors·image_url)가 외부로 나가는 통로 |
| `demo/fallback/popular.json` → 브라우저 | 정적 공개 파일 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-23 | Denial of Service | `load_catalog` 손상·부재 | mitigate | `(OSError, ValueError, KeyError, TypeError)` → `log.exception` + None → Phase 2 동작으로 기동(테스트 `test_load_catalog_none_when_missing_or_corrupt`); `local-run.md` "아티팩트 없이 기동" 유지 |
| T-03-24 | Information Disclosure | 응답 meta 조인 | mitigate | `CatalogKR.meta()` 가 `EXCLUDED_META_KEYS` 를 제거(Plan 02) + export 화이트리스트(Plan 04) — 이 플랜은 `grep -o '"description"' books_kr.json` 0건·curl 응답 확인으로 이중 검증 |
| T-03-25 | Tampering | 외부 호스트 표지 삽입 | mitigate | `popular()`·popular.json 40권이 `is_eligible`(`.millie.co.kr` 접미·`adult-cover` 제외) 통과분만 |
| T-03-26 | Tampering | `data/id_map.csv` 재빌드 훼손 | mitigate | `git diff --numstat` 삭제 0 + 빌드 전 사본 diff, 실패 시 `git checkout` 복원·중단 |
| T-03-27 | Repudiation | PDF·트래킹 숫자 손기록 | mitigate | `results/millie_coverage.csv`·`results/millie_edges_gate.json` 에서 복사만, draft 에 "최종 스냅샷으로 교체" 표시(D-02) |
| T-03-28 | Elevation of Privilege | HTTP 계약·인증 표면 | accept | 이 플랜은 `serving/**` 무변경(엔드포인트·스키마 0 변경). 인증은 프로젝트 범위 밖(../CLAUDE.md §4 "안 씀") |
</threat_model>

<verification>
**최종 스냅샷(Task 4):** 배치 종료 또는 Day 2 오전 사용자 승인 중단 후 Task 2-b~2-d 를 1회 재실행하고 draft·트래킹 수치를 교체한다. Task 1~3 이 끝났어도 Task 4 전까지 이 플랜은 "부분 완료"다.

**wave 3 전역 게이트(이 플랜이 직접, Task 2-d):** `uv run ruff format --check . && uv run ruff check .` 클린 · `uv run pytest --no-header` failed 0·skipped 0·passed ≥224 · `make smoke` → `smoke: PASS` · curl level 0(`0 pop_v1 … trending 5`, 한국어 제목) · `/contract-sync` 카탈로그 항목 통과.

**wave 1·2 게이트(Advisor 가 이 플랜 시작 전 각 1회):** wave 1 종료(01·02 SUMMARY) — ruff 클린 · pytest failed 0(skipped 1 = 이웃 게이트) · smoke PASS(level 3) / wave 2 종료(03·04·05 SUMMARY) — ruff 클린 · pytest failed 0·skipped 0 · smoke PASS(level 3, 아직 카탈로그 미주입). 실패 시 소유 플랜(`files_modified`)에 수정 브리프 재위임.
</verification>

<success_criteria>
- `app/pipeline.py`·`app/server.py` 가 D-13 을 구현하고 기존 테스트 단정 무수정으로 전체 통과
- `make millie` 1명령으로 4+1 산출물, 파일당 <50MB, description 0건, popular.json RecommendOut 통과, id_map 삭제 0
- `/api/recommend?seeds=1,2,3&k=5` → level 0 · pop_v1 · 한국어 제목; 익명 → level 3 인기 채움
- draft P3 1줄·트래킹 03·개발일지 D66·rules 확인. 커밋 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-06-SUMMARY.md` — Task 1 RED/GREEN 발췌 · `make millie` 로그 tail · 산출물 검증 출력 · pytest/ruff/smoke 요약 · curl 3줄 · contract-sync 결과 · 문서 diff 요약 · **D62 중복은 09-05 밤 Advisor 가 D64 로 정정 완료(보고만)** · Phase 4·6 인계 노트 · 커밋 대상 파일 목록(사용자 승인 대기) · "커밋하지 않는다".
</output>
