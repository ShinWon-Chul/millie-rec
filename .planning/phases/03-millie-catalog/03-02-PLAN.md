---
phase: 03-millie-catalog
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/data/catalog_kr.py            # NEW — Catalog·Neighbors·BookStatsSource 한 클래스, ≤150줄
  - src/millie_rec/data/vectors_kr.py            # NEW — ItemVectors(npz), ≤60줄
  - src/millie_rec/data/__init__.py              # CatalogKR·VectorsKR·DIR_SERVING·BOOKS_KR_JSON 공개
  - tests/conftest.py                            # millie_serving_sample 세션 fixture(20권 합성 서빙 산출물 생성기)
  - tests/data/test_catalog_kr.py                # NEW
  - tests/data/test_vectors_kr.py                # NEW
autonomous: true
requirements: [DATA-04, DATA-06]
lane: Model (data 슬라이스 Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - scripts/**
  - data/id_map.csv
  - data/processed/**
  - artifacts/**
  - tests/data/test_millie_catalog.py
  - tests/data/test_millie_edges.py
  - tests/data/test_millie_parse.py
  - tests/fixtures/millie/**
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/app/**
  - src/millie_rec/data/goodbooks.py
  - src/millie_rec/data/load.py
  - src/millie_rec/data/split.py
  - src/millie_rec/data/onboarding.py
  - src/millie_rec/data/labels.py
  - tests/test_architecture.py
  - Makefile
  - pyproject.toml
  - demo/**
  - ../.claude/**
  - CLAUDE.md

must_haves:
  truths:
    - "CatalogKR.load(serving_dir) 가 books_kr.json(필수)·item_edges_kr.json(있으면) 을 stdlib json 으로 1회 읽고 contracts.Catalog(meta·popular·eligible)·Neighbors(neighbors)·BookStatsSource(stats·user_level) 를 한 객체로 만족한다 — pandas import 0"
    - "meta() 반환 dict 에 description·curator_note·seg_dist 키가 없다(입력 json 에 있어도 제거) — DATA-06"
    - "eligible() = title 있음 ∧ image_url 호스트가 .millie.co.kr 로 끝남(img.·image.·cover.) ∧ URL 에 adult-cover 없음; popular(categories, n) 은 eligible 필터 후 pop_rank 오름차순 상위 n, categories 가 있으면 categories 교집합 — D-14"
    - "stats() 는 BookStats(difficulty=행 difficulty(None 허용), completion_rate=completion_prob/100, source=difficulty_source, completion_prob·category_avg_prob·expected_min·category_avg_min·resid_z·len_z 그대로, rating_mean=average_rating) 이고 user_level() 은 None — DATA-04 결측 = difficulty None 이 계약 DTO 까지 보존"
    - "VectorsKR.load(npz) 는 vectors(book_ids) 가 (len, dim) float dense·행 L2≈1·입력 순서 보존·미지 id 0 벡터 — Phase 4 hybrid_div 가 서버에서 주입받을 ItemVectors"
    - "tests/test_architecture.py 3 passed — data 슬라이스는 contracts 와 자기 내부만 import"
  artifacts:
    - path: "src/millie_rec/data/catalog_kr.py"
      provides: "CatalogKR(books, edges) + load() · is_eligible(book) · DIR_SERVING·BOOKS_KR_JSON·EDGES_KR_JSON 상수"
      exports: ["CatalogKR", "is_eligible", "DIR_SERVING", "BOOKS_KR_JSON", "EDGES_KR_JSON", "COVER_HOST_SUFFIX", "ADULT_COVER_MARK", "EXCLUDED_META_KEYS"]
      max_lines: 150
    - path: "src/millie_rec/data/vectors_kr.py"
      provides: "VectorsKR(book_ids, vectors) + load(path) · VECTORS_KR_NPZ"
      exports: ["VectorsKR", "VECTORS_KR_NPZ"]
      max_lines: 150
    - path: "src/millie_rec/data/__init__.py"
      provides: "공개 표면"
      contains: "\"CatalogKR\""
    - path: "tests/conftest.py"
      provides: "millie_serving_sample(tmp_path_factory) -> Path — 20권 books_kr.json·item_edges_kr.json·popularity_kr.json·content_vectors_kr.npz 생성"
      contains: "def millie_serving_sample"
    - path: "tests/data/test_catalog_kr.py"
      provides: "계약 3종 · 정확성(popular 순서·eligible 3규칙·neighbors 정렬·stats 매핑·meta 제외키) · 안전성(미지 id·엣지 파일 부재·손상)"
      min_lines: 80
  key_links:
    - from: "src/millie_rec/data/catalog_kr.py"
      to: "src/millie_rec/contracts.py"
      via: "BookStats DTO · DIR_ARTIFACTS · UserState 만 import"
      pattern: "from millie_rec.contracts import"
    - from: "src/millie_rec/data/catalog_kr.py::CatalogKR.popular"
      to: "src/millie_rec/data/catalog_kr.py::is_eligible"
      via: "__init__ 에서 eligible ids 를 pop_rank 순으로 1회 정렬해 보관(요청마다 재계산 금지)"
      pattern: "_pop_order"
    - from: "tests/conftest.py::millie_serving_sample"
      to: "tests/data/test_catalog_kr.py · tests/app/test_server_catalog.py(Plan 06)"
      via: "세션 fixture Path — 실 artifacts/serving/ 는 어떤 테스트도 읽지 않는다"
      pattern: "millie_serving_sample"
---

<objective>
Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md "Phase 3: 밀리 카탈로그 빌드") Success Criteria 3 — `data/catalog_kr.py` 어댑터를 TDD 한 사이클로 만든다: `artifacts/serving/books_kr.json`·`item_edges_kr.json` 을 기동 1회 로드해 `contracts.Catalog`·`Neighbors`·`BookStatsSource` 를 한 클래스(`CatalogKR`)로 구현하고, `content_vectors_kr.npz` 를 읽는 `ItemVectors` 구현(`VectorsKR`)을 별도 파일에 둔다. 실 아티팩트 대신 **`tests/conftest.py` 의 생성기 fixture(20권)** 로 검증한다 — 그래서 이 플랜은 wave 1 에서 Plan 01(빌더)과 병렬이다(파일 교집합 0, 데이터 의존 0).

Purpose: 서버 주입(Plan 06, D-13)의 재료. DATA-06 "description·curator_note 미노출" 을 어댑터 층에서도 강제(`EXCLUDED_META_KEYS`)하고, DATA-04 "결측 difficulty=None(가드 모집단 제외)" 이 `BookStats` DTO 까지 그대로 흐르게 한다. 결정 D-14 'Catalog.eligible() = title ∧ *.millie.co.kr ∧ adult-cover 아님'(.planning/phases/03-millie-catalog/03-CONTEXT.md) · Claude's Discretion "catalog_kr.py 위치·구성" 이행.
Output: `src` 2파일 + `data/__init__.py` 노출 + conftest fixture + 테스트 2파일. 커밋 0.

**하지 않는 것:** `popularity_kr.json` 소비(세그먼트 인기는 Phase 5 `state.py`·level 2 몫 — fixture 는 만들어 두되 어댑터는 읽지 않는다) · 서버 주입(`app/**` Advisor 전용, Plan 06) · `user_level` 계산(Phase 4 가드 몫, `None` 고정) · `retrieval/content.py` 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/data/__init__.py
@src/millie_rec/retrieval/popularity.py
@src/millie_rec/retrieval/content.py
@src/millie_rec/serving/fallback.py
@tests/conftest.py
@tests/retrieval/test_content.py
@tests/test_architecture.py
@../.claude/rules/architecture.md
@../.claude/rules/serving.md
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md
@../.claude/rules/data.md

<project_rules_inline>
**star 의존(`../.claude/rules/architecture.md`):** "슬라이스(`app` 제외)는 `millie_rec.contracts`와 **자기 슬라이스 내부**만 import한다. 다른 슬라이스 import 금지." / "`tests/test_architecture.py`가 위 규칙을 정적으로 검사한다. 실패하면 규칙을 어긴 것이다. **테스트를 고치지 않는다.**" → `catalog_kr.py`·`vectors_kr.py` 는 `millie_rec.contracts` + `millie_rec.data.*` 만. `millie_rec.serving`·`millie_rec.retrieval`·`millie_rec.app` 한 줄이면 실패.

**서빙 경로(`../.claude/rules/serving.md`):** "artifacts는 기동 시 1회 로드(`npz`+`json`, **서빙 경로에서 pandas import 금지** — 메모리 0.3GB대). 요청마다 fit·재계산 금지." → 두 파일은 `json`·`numpy`·`urllib.parse` 만. `__init__` 에서 인덱스·인기 순서를 1회 계산.

**간결함(`../.claude/rules/simplicity.md`):** "파일 150줄 이하(`src/` 파이썬). 넘으면 관심사가 2개다." / "클래스는 `contracts.py`의 Protocol을 만족시킬 때만." / "설정은 모듈 상단 대문자 상수." / "추상화는 두 번째 사용에서 — 베이스 클래스·레지스트리·팩토리 금지."

**데이터(`../.claude/rules/data.md`):** "표지는 `*.millie.co.kr` **핫링크만**(실측 호스트 `img.`·`image.`·`cover.`). 성인 표지 플레이스홀더(`adult-cover-*.webp`)·title 없는 책은 `Catalog.eligible()`에서 제외(Phase 3 CONTEXT D-14)." / "**텍스트 2개(`description`, `curator_note`)는 TF-IDF 입력 전용.** `artifacts/serving/*`·API·화면에 내보내지 않는다."

**TDD(`../.claude/rules/python-tdd.md`):** Red 는 `AssertionError` 로 확인, `uv run pytest <대상> --no-header`(CLI `-q` 금지). 슬라이스마다 계약·정확성·안전성 3종. "mock/픽스처 JSON 을 손으로 쓰지 않는다"(`simplicity.md` 바이브 코딩 고유) → 20권 fixture 는 conftest 생성기 함수로 만든다.

**python.md:** "완료 전 `uv run ruff format . && uv run ruff check . && uv run pytest --no-header`" — 이 플랜은 자기 파일만 format/check(형제 플랜 경합 방지). "경로는 `contracts.py`의 `DIR_*` 상수에서 시작한다."
</project_rules_inline>

<interfaces>
<!-- 만족해야 할 Protocol (contracts.py L310-338 verbatim) -->
```python
class ItemVectors(Protocol):
    def vectors(self, book_ids: Sequence[int]) -> "np.ndarray": ...
class Catalog(Protocol):
    def meta(self, book_ids: Sequence[int]) -> list[dict]: ...
    def popular(self, categories: Sequence[str] = (), n: int = 50) -> list[int]: ...
    def eligible(self, book_ids: Sequence[int]) -> list[int]: ...  # main §7 노출 자격 게이트
class Neighbors(Protocol):
    def neighbors(self, book_id: int, n: int = 20) -> list[tuple[int, float]]: ...
class BookStatsSource(Protocol):
    def stats(self, book_ids: Sequence[int]) -> list[BookStats]: ...
    def user_level(self, user: UserState) -> float | None: ...
```
`BookStats` 필드(contracts.py L168-192): `book_id difficulty completion_rate early_dropoff_rate rating_mean rating_var n_events source completion_prob category_avg_prob expected_min category_avg_min resid_z len_z` — `source` 허용값 `STATS_SOURCES = ("millie_index","category_prior","behavior","prior")`. `completion_rate` 주석 "밀리 P_완독(0~1)" vs `completion_prob` "%".

소비자 호출 형태(serving/fallback.py L24, 불변): `catalog.popular(n=k + len(user.seen))` → seen 제외 → `[:k]`.

`books_kr.json` 행 키(Plan 04 export `BOOK_FIELDS` 와 동일 — 이 플랜의 fixture 도 같은 28키를 쓴다): `book_id title authors image_url average_rating ratings_count original_publication_year categories subcategories tags publisher subtitle pub_date book_format formats pop_rank millie_label review_count shelf_count completion_prob category_avg_prob expected_min category_avg_min resid_z len_z difficulty difficulty_source top_segment`. `item_edges_kr.json` 형태: `{"<src_book_id 문자열>": [[dst_book_id, weight, source], …]}` weight 내림차순(export 가 정렬). `content_vectors_kr.npz`: `book_ids` int64[N] · `vectors` float32[N, 128] 행 L2≈1.

Analog(그대로 복제): `retrieval/popularity.py` L22-58 `load` classmethod + json · `retrieval/content.py` L20-42 `_index` dict + `vectors()` 0-벡터 · `app/export.py` L14-16 `SERVING_SUBDIR = "serving"; DIR_SERVING = DIR_ARTIFACTS / SERVING_SUBDIR`(data 는 app 을 import 못 하므로 **중복 정의**).

실측 호스트 분포(2026-09-05): `img.millie.co.kr` 5,057 · `image.millie.co.kr` 339 · `cover.millie.co.kr` 91 · None 3 · `d1miajbjsyro89.cloudfront.net` 2(`adult-cover-*.webp`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): conftest 20권 서빙 fixture 생성기 + test_catalog_kr 11건 + test_vectors_kr 4건 → 스텁 위에서 AssertionError</name>
  <files>tests/conftest.py, tests/data/test_catalog_kr.py, tests/data/test_vectors_kr.py, src/millie_rec/data/catalog_kr.py (스텁), src/millie_rec/data/vectors_kr.py (스텁), src/millie_rec/data/__init__.py</files>
  <read_first>
    - tests/conftest.py (현재 — session fixture 형식 `interactions`·`item_vectors`)
    - tests/retrieval/test_content.py (4단정: shape·L2·순서·미지 id — vectors_kr 테스트가 복제)
    - src/millie_rec/retrieval/content.py · src/millie_rec/retrieval/popularity.py (Protocol 구현 형태)
    - src/millie_rec/contracts.py L168-192 BookStats · L310-338 Protocol
    - src/millie_rec/data/__init__.py (알파벳 정렬 `__all__` — 상수 → 클래스 → 함수 순)
    - ../.claude/rules/python-tdd.md (Red = AssertionError)
  </read_first>
  <action>
    **1-a. 스텁 2파일 + 공개 표면(collection error 방지).**
    `src/millie_rec/data/catalog_kr.py`: docstring `"""Track B 데모 카탈로그 어댑터 — artifacts/serving/*.json 기동 1회 로드 (DATA-06, 적재 계획 02 §3·§7). Catalog·Neighbors·BookStatsSource 를 한 클래스가 만족한다. pandas 없음(serving.md)."""`, 상수 `SERVING_SUBDIR = "serving"` · `DIR_SERVING = DIR_ARTIFACTS / SERVING_SUBDIR  # app/export.py 와 중복 정의 — data 는 app 을 import 못 한다` · `BOOKS_KR_JSON = "books_kr.json"` · `EDGES_KR_JSON = "item_edges_kr.json"` · `COVER_HOST_SUFFIX = ".millie.co.kr"  # D-14: img.·image.·cover. 3종 실측 → endswith` · `ADULT_COVER_MARK = "adult-cover"` · `EXCLUDED_META_KEYS = ("description", "curator_note", "seg_dist")  # DATA-06 — export 도 빼지만 어댑터가 한 번 더 막는다` · `POP_RANK_COL = "pop_rank"`; `def is_eligible(book: dict) -> bool: return False`(스텁); `class CatalogKR` 에 시그니처만(`__init__(self, books: Sequence[dict], edges: dict[str, list[list]] | None = None)`, `load(cls, serving_dir: Path = DIR_SERVING)`, `meta`→`[]`, `popular`→`[]`, `eligible`→`[]`, `neighbors`→`[]`, `stats`→`[]`, `user_level`→`None`).
    `src/millie_rec/data/vectors_kr.py`: `VECTORS_KR_NPZ = "content_vectors_kr.npz"`; `class VectorsKR` `__init__(self, book_ids: np.ndarray, vectors: np.ndarray)`, `load(cls, path: Path = DIR_SERVING / VECTORS_KR_NPZ)`(`from millie_rec.data.catalog_kr import DIR_SERVING` — 같은 슬라이스), `dim` property → 0, `vectors(book_ids)` → `np.zeros((len(book_ids), 0))`.
    `src/millie_rec/data/__init__.py`: `from millie_rec.data.catalog_kr import (BOOKS_KR_JSON, DIR_SERVING, EDGES_KR_JSON, CatalogKR, is_eligible)` · `from millie_rec.data.vectors_kr import VECTORS_KR_NPZ, VectorsKR` 추가, `__all__` 에 7개 이름을 기존 정렬 규칙(대문자 상수 → 클래스 → 함수, 각 알파벳)대로 삽입.
    `uv run pytest tests/test_architecture.py --no-header` → `3 passed`(스텁 import 경계 확인 — 여기서 실패하면 import 를 고친다).

    **1-b. `tests/conftest.py` 에 세션 fixture 추가**(기존 두 fixture 아래, `json`·`Path` import 추가):
    ```python
    @pytest.fixture(scope="session")
    def millie_serving_sample(tmp_path_factory) -> Path:
        """20권 합성 서빙 산출물(books_kr.json·item_edges_kr.json·popularity_kr.json·content_vectors_kr.npz).

        book_id 1..20, pop_rank == book_id. 자격 없는 3권: 18(호스트 cdn.example.com) · 19(title None) · 20(adult-cover).
        홀수 id ≥15 는 difficulty_source=category_prior(difficulty None). 실 artifacts/serving 은 어떤 테스트도 읽지 않는다.
        """
    ```
    구현: `rng = np.random.default_rng(SEED)`; 카테고리 순환 `("소설", "에세이", "경제경영", "인문")`; 호스트 순환 `("img.millie.co.kr", "image.millie.co.kr", "cover.millie.co.kr")`; book_format 순환 `("전자책", "오디오북", "챗북")`; 행 = 28키 전부(위 `<interfaces>` 목록): `title=f"밀리 표본 도서 {i}"`(19 는 `None`), `authors=f"저자 {i}"`, `image_url=f"https://{host}/cover/{i}.jpg"`(18 은 `https://cdn.example.com/{i}.jpg`, 20 은 `https://d1miajbjsyro89.cloudfront.net/adult-cover-a.webp`), `average_rating=round(3.5 + i % 5 * 0.2, 1)`, `ratings_count=i*3`, `original_publication_year=2000+i`, `categories=[cat]`, `subcategories=[]`, `tags=f"{cat} 전자책"`, `publisher="표본 출판사"`, `subtitle=None`, `pub_date=f"20{i:02d}.01.01"`, `formats=[book_format]`, `pop_rank=i`, `millie_label=None`, `review_count=i*3`, `shelf_count=10_000 - i*100`, `completion_prob=50+i`, `category_avg_prob=60`, `expected_min=100+i*10`, `category_avg_min=200`, measured(i 짝수 또는 i<15): `resid_z=round((i%7-3)*0.5, 3)`, `len_z=round((i%5-2)*0.4, 3)`, `difficulty=round(1/(1+np.exp(resid_z)), 4)`, `difficulty_source="millie_index"`; 그 외(홀수 ≥15): `resid_z=0.0`, `len_z=...`, `difficulty=None`, `difficulty_source="category_prior"`; `top_segment="40대 여성"`. 엣지: src i 마다 dst = `[(i + d - 1) % 20 + 1 for d in range(1, 6)]`(자기 제외 5개) weight `0.9, 0.8, 0.7, 0.6, 0.2` source 앞 4 `content_sim` 마지막 `category_best`. popularity: `[{"book_id": i, "segment": "all", "score": float(10_000 - i*100), "rank": i}]`. npz: `vectors = rng.random((20, 8)).astype(np.float32)`; 행 L2 정규화; `np.savez(dir/"content_vectors_kr.npz", book_ids=np.arange(1, 21, dtype=np.int64), vectors=vectors)`. `json.dumps(..., ensure_ascii=False)` 로 `dir/"books_kr.json"`(리스트) · `dir/"item_edges_kr.json"`(키는 **문자열** `str(i)`) · `dir/"popularity_kr.json"`. 반환 `dir`.

    **1-c. `tests/data/test_catalog_kr.py`** (docstring `"""data/catalog_kr.py — 계약(Protocol 3종 형태) · 정확성(popular·eligible·neighbors·stats·meta 제외키) · 안전성(미지 id·파일 부재·손상)."""`; import `from millie_rec.data import CatalogKR, is_eligible, DIR_SERVING, BOOKS_KR_JSON` + `from millie_rec.contracts import BookStats, UserState`). fixture `cat = CatalogKR.load(millie_serving_sample)`. 11건:
    - 계약: `test_catalog_kr_satisfies_three_protocol_shapes(cat)` — `meta([1,2])` 는 `list[dict]` 길이 2 · `popular(n=3)` 는 `list[int]` · `eligible([1,18])` 는 `list[int]` · `neighbors(1, n=3)` 는 길이 3 의 `(int, float)` 튜플 리스트 · `stats([1])` 는 `[BookStats]` · `user_level(UserState(None)) is None`.
    - 정확성: `test_popular_is_pop_rank_order_of_eligible_only(cat)` — `popular(n=20) == [1..17]`(18·19·20 없음), `popular(n=5) == [1,2,3,4,5]`; `test_popular_with_categories_filters_by_intersection(cat)` — `popular(categories=["소설"], n=50)` 의 모든 id 가 `meta` 상 categories 에 "소설" 포함이고 pop_rank 오름차순, `popular(categories=["없는분야"]) == []`; `test_eligible_rules_title_host_suffix_adult_cover(cat)` — `eligible([1,17,18,19,20]) == [1,17]`; `is_eligible({"title":"x","image_url":"https://img.millie.co.kr/a.jpg"})` True · `{"title":"x","image_url":"https://millie.co.kr.evil.com/a.jpg"}` False(접미 `.millie.co.kr` 은 hostname 끝에서만) · `{"title":"x","image_url":None}` False · `{"title":"","image_url":"https://cover.millie.co.kr/a.jpg"}` False · `{"title":"x","image_url":"https://image.millie.co.kr/adult-cover-b.webp"}` False; `test_neighbors_weight_desc_and_n_cap(cat)` — `neighbors(1, n=3) == [(2, 0.9), (3, 0.8), (4, 0.7)]`, `len(neighbors(1)) == 5`, `neighbors(999) == []`; `test_stats_maps_book_stats_and_keeps_none_difficulty(cat)` — `s = stats([2, 15])`; `s[0].book_id == 2 and s[0].source == "millie_index" and s[0].completion_rate == pytest.approx(0.52) and s[0].completion_prob == 52 and s[0].difficulty == pytest.approx(1/(1+np.exp(s[0].resid_z)))`; `s[1].source == "category_prior" and s[1].difficulty is None and s[1].resid_z == 0.0`; `s[0].rating_mean == pytest.approx(3.9)`(i=2 → 3.5+0.4); `test_meta_returns_rows_in_order_and_drops_excluded_keys()` — `CatalogKR([{"book_id": 7, "title": "t", "image_url": "https://img.millie.co.kr/7.jpg", "pop_rank": 1, "categories": ["소설"], "description": "밀리 저작", "curator_note": "x", "seg_dist": "{}"}]).meta([7])[0]` 에 `description`·`curator_note`·`seg_dist` 키 없음, `title`·`pop_rank` 있음; `meta([2, 1])` 이 `[행2, 행1]` 순서.
    - 안전성: `test_unknown_ids_are_skipped_not_raised(cat)` — `meta([999]) == []`, `eligible([999]) == []`, `stats([999]) == []`; `test_load_without_edges_file_gives_empty_neighbors(tmp_path, millie_serving_sample)` — books_kr.json 만 복사한 tmp 로 `load` → `neighbors(1) == []`, `popular(n=1) == [1]`; `test_load_missing_books_raises_file_not_found(tmp_path)` — `pytest.raises(FileNotFoundError)`(부재 처리는 app 로더 몫); `test_load_corrupt_books_raises_value_error(tmp_path)` — `"not json"` → `pytest.raises(ValueError)`(`json.JSONDecodeError` 는 ValueError 하위).
    - `test_dir_serving_constant_points_to_artifacts_serving()` — `DIR_SERVING.name == "serving" and DIR_SERVING.parent.name == "artifacts" and BOOKS_KR_JSON == "books_kr.json"`.

    **1-d. `tests/data/test_vectors_kr.py`** (4건, `tests/retrieval/test_content.py` 단정 복제): fixture `vec = VectorsKR.load(millie_serving_sample / "content_vectors_kr.npz")`. `test_vectors_shape_and_dim` — `vectors([1,2,3]).shape == (3, 8)`, `vec.dim == 8`; `test_rows_are_l2_normalized` — `np.linalg.norm(vectors([1..20]), axis=1) == approx(1, abs=1e-5)`; `test_order_preserved_and_unknown_is_zero` — `v = vectors([3, 999, 1])`; `v[1] == 0` 전부, `np.allclose(v[0], vectors([3])[0])`, `np.allclose(v[2], vectors([1])[0])`; `test_load_from_npz_written_with_savez(tmp_path)` — `np.savez(tmp/"v.npz", book_ids=np.array([10,20,30]), vectors=np.eye(3, dtype=np.float32))` → `VectorsKR.load(tmp/"v.npz").vectors([20])[0].tolist() == [0.0, 1.0, 0.0]`.

    **1-e. RED:** `uv run pytest tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py --no-header 2>&1 | tail -40` → 15 failed 전부 `AssertionError`(스텁이 빈 값·False·zeros 를 주므로) 또는 `Failed: DID NOT RAISE`(load 스텁이 파일을 읽지 않으면 raises 테스트 2건) — 둘 다 Red. `ImportError`·`TypeError` 가 보이면 스텁 시그니처를 고친다(테스트 아님). `uv run pytest tests/test_architecture.py --no-header` → 3 passed.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/test_architecture.py --no-header && uv run pytest tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/data/test_catalog_kr.py` == 11; `grep -c "def test_" tests/data/test_vectors_kr.py` == 4; `grep -c "def millie_serving_sample" tests/conftest.py` == 1
    - RED 출력에 `AssertionError`/`E  assert`/`DID NOT RAISE` 만 있고 `ImportError`·`ModuleNotFoundError`·`SyntaxError`·`NameError`·`TypeError` 없음(SUMMARY 발췌)
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(retrieval\|evaluation\|serving\|app\)" src/millie_rec/data/catalog_kr.py src/millie_rec/data/vectors_kr.py` → 출력 없음; `grep -c "import pandas\|from pandas" src/millie_rec/data/catalog_kr.py src/millie_rec/data/vectors_kr.py` → 0 0
    - `grep -c '"CatalogKR"\|"VectorsKR"\|"DIR_SERVING"\|"BOOKS_KR_JSON"' src/millie_rec/data/__init__.py` == 4
    - `git diff --stat -- scripts data tests/data/test_millie_catalog.py tests/data/test_millie_edges.py src/millie_rec/serving src/millie_rec/app` → 출력 없음
  </acceptance_criteria>
  <done>15건이 단정 실패로 RED, 아키텍처 3 passed, 20권 fixture 생성기가 conftest 에 있다. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): CatalogKR·VectorsKR 구현 → 15 passed · 아키텍처 3 passed · 두 파일 ≤150줄</name>
  <files>src/millie_rec/data/catalog_kr.py, src/millie_rec/data/vectors_kr.py</files>
  <read_first>
    - src/millie_rec/data/catalog_kr.py · vectors_kr.py (Task 1 스텁)
    - tests/data/test_catalog_kr.py · tests/data/test_vectors_kr.py (단정이 계약)
    - src/millie_rec/retrieval/content.py L20-42 (`vectors()` 0-벡터 패턴 — dense 라 `.toarray()` 없음)
    - src/millie_rec/contracts.py L168-192 BookStats
  </read_first>
  <action>
    **2-a. `catalog_kr.py`** (예상 ≈110줄):
    ```python
    import json
    from collections.abc import Sequence
    from pathlib import Path
    from urllib.parse import urlsplit

    from millie_rec.contracts import DIR_ARTIFACTS, BookStats, UserState
    # (상수는 Task 1 그대로)

    def is_eligible(book: dict) -> bool:
        """D-14: title 있음 ∧ 표지 호스트 *.millie.co.kr ∧ 성인 플레이스홀더 아님. 평가·인기·난이도 결측은 자격에 넣지 않는다."""
        url = book.get("image_url") or ""
        host = urlsplit(url).hostname or ""
        return bool(book.get("title")) and host.endswith(COVER_HOST_SUFFIX) and ADULT_COVER_MARK not in url

    class CatalogKR:
        """contracts.Catalog + Neighbors + BookStatsSource. 인덱스·인기 순서는 __init__ 에서 1회."""

        def __init__(self, books: Sequence[dict], edges: dict[str, list[list]] | None = None) -> None:
            self._by_id: dict[int, dict] = {
                int(b["book_id"]): {k: v for k, v in b.items() if k not in EXCLUDED_META_KEYS} for b in books
            }
            self._edges = edges or {}
            ok = [b for b in self._by_id.values() if is_eligible(b)]
            self._pop_order: list[int] = [
                int(b["book_id"]) for b in sorted(ok, key=lambda b: (b.get(POP_RANK_COL) is None, b.get(POP_RANK_COL) or 0, int(b["book_id"])))
            ]

        @classmethod
        def load(cls, serving_dir: Path = DIR_SERVING) -> "CatalogKR":
            """books_kr.json 필수(없으면 FileNotFoundError — 부재 처리는 app 로더 몫), 엣지 파일은 선택."""
            books = json.loads((serving_dir / BOOKS_KR_JSON).read_text(encoding="utf-8"))
            edges_path = serving_dir / EDGES_KR_JSON
            edges = json.loads(edges_path.read_text(encoding="utf-8")) if edges_path.exists() else {}
            return cls(books, edges)

        def meta(self, book_ids): return [self._by_id[int(b)] for b in book_ids if int(b) in self._by_id]
        def popular(self, categories=(), n=50):
            if not categories:
                return self._pop_order[:n]
            wanted = set(categories)
            return [b for b in self._pop_order if wanted & set(self._by_id[b].get("categories") or [])][:n]
        def eligible(self, book_ids): return [int(b) for b in book_ids if int(b) in self._by_id and is_eligible(self._by_id[int(b)])]
        def neighbors(self, book_id, n=20): return [(int(d), float(w)) for d, w, _src in self._edges.get(str(int(book_id)), [])[:n]]
        def stats(self, book_ids):
            out = []
            for b in book_ids:
                row = self._by_id.get(int(b))
                if row is None: continue
                prob = row.get("completion_prob")
                out.append(BookStats(book_id=int(b), difficulty=row.get("difficulty"),
                    completion_rate=(prob / 100.0) if prob is not None else None,
                    rating_mean=row.get("average_rating"), source=row.get("difficulty_source") or "prior",
                    completion_prob=prob, category_avg_prob=row.get("category_avg_prob"),
                    expected_min=row.get("expected_min"), category_avg_min=row.get("category_avg_min"),
                    resid_z=row.get("resid_z"), len_z=row.get("len_z")))
            return out
        def user_level(self, user: UserState) -> float | None:
            return None  # Phase 4 가드 몫 — 완독 이력 평균은 state.py 이후
    ```
    타입 힌트는 공개 메서드 시그니처에 Protocol 그대로(`Sequence[int]` 등). `pop_rank` None 은 마지막(정렬 키 첫 원소). 엣지 키는 json 문자열이라 `str(int(book_id))`.

    **2-b. `vectors_kr.py`** (≈40줄): `contracts.ItemVectors` 구현.
    ```python
    class VectorsKR:
        """L2 정규화 dense 벡터(npz). 미지 id 는 0 벡터(ILD 에서 거리 1) — retrieval/content.py 와 같은 규약."""
        def __init__(self, book_ids: np.ndarray, vectors: np.ndarray) -> None:
            self._matrix = np.asarray(vectors, dtype=float)
            self._index = {int(b): i for i, b in enumerate(np.asarray(book_ids).tolist())}
        @classmethod
        def load(cls, path: Path = DIR_SERVING / VECTORS_KR_NPZ) -> "VectorsKR":
            with np.load(path) as z:
                return cls(z["book_ids"], z["vectors"])
        @property
        def dim(self) -> int: return int(self._matrix.shape[1])
        def vectors(self, book_ids: Sequence[int]) -> np.ndarray:
            out = np.zeros((len(book_ids), self.dim), dtype=float)
            rows = [(i, self._index[int(b)]) for i, b in enumerate(book_ids) if int(b) in self._index]
            if rows:
                dst, src = zip(*rows, strict=True)
                out[list(dst)] = self._matrix[list(src)]
            return out
    ```

    **2-c. GREEN 확인:** `uv run pytest tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py tests/test_architecture.py --no-header` → `18 passed`. `uv run ruff format src/millie_rec/data/catalog_kr.py src/millie_rec/data/vectors_kr.py src/millie_rec/data/__init__.py tests/conftest.py tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py && uv run ruff check <같은 6파일>`. `wc -l src/millie_rec/data/catalog_kr.py src/millie_rec/data/vectors_kr.py` 둘 다 ≤150(catalog_kr 이 150 을 넘으면 `Neighbors` 구현만 `data/neighbors_kr.py` 로 빼고 `__init__`·테스트를 맞춘다 — 그 경우 SUMMARY 에 파일명 명시).

    **2-d. 회귀:** `uv run pytest tests/data tests/serving tests/app --no-header` → failed 0(`data/__init__` 변경이 goodbooks·load 공개 이름을 깨지 않았는지).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check src/millie_rec/data/catalog_kr.py src/millie_rec/data/vectors_kr.py src/millie_rec/data/__init__.py tests/conftest.py tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py && uv run pytest tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py tests/test_architecture.py --no-header 2>&1 | tail -3 && [ "$(wc -l < src/millie_rec/data/catalog_kr.py)" -le 150 ] && [ "$(wc -l < src/millie_rec/data/vectors_kr.py)" -le 150 ]</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py tests/test_architecture.py --no-header` → `18 passed`
    - `wc -l` catalog_kr.py ≤ 150 · vectors_kr.py ≤ 150
    - `grep -c "def meta\|def popular\|def eligible\|def neighbors\|def stats\|def user_level" src/millie_rec/data/catalog_kr.py` == 6; `grep -c "EXCLUDED_META_KEYS" src/millie_rec/data/catalog_kr.py` ≥ 2; `grep -c 'endswith(COVER_HOST_SUFFIX)' src/millie_rec/data/catalog_kr.py` == 1
    - `grep -c "import pandas\|from pandas\|sklearn" src/millie_rec/data/catalog_kr.py src/millie_rec/data/vectors_kr.py` → 0 0
    - `uv run pytest tests/data tests/serving tests/app --no-header` → failed 0
    - `uv run ruff check <6파일>` → `All checks passed`
  </acceptance_criteria>
  <done>어댑터 두 클래스가 Protocol 3+1 종을 만족하고 20권 fixture 로 계약·정확성·안전성이 증명됨. Plan 06 이 `from millie_rec.data import CatalogKR, DIR_SERVING, BOOKS_KR_JSON` 으로 주입할 준비 완료. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `artifacts/serving/*.json` → `CatalogKR.load` | 커밋된 산출물이지만 손상·부분 쓰기 가능. 기동 경로에서 읽힌다 |
| `CatalogKR.meta()` → `/api/recommend` 응답·화면 | 도서 메타가 외부로 나가는 통로. 밀리 저작 텍스트·표지 외부 호스트가 여기서 걸러진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-06 | Information Disclosure | `CatalogKR.meta()` | mitigate | `EXCLUDED_META_KEYS=("description","curator_note","seg_dist")` 를 `__init__` 에서 제거 — export 가 실수로 넣어도 API 로 나가지 않음(테스트 `test_meta_returns_rows_in_order_and_drops_excluded_keys`) |
| T-03-07 | Tampering | `image_url` 핫링크(외부 리소스 삽입) | mitigate | `is_eligible`: `urlsplit().hostname.endswith(".millie.co.kr")` + `adult-cover` 제외 — `millie.co.kr.evil.com` 같은 접미 위장은 hostname 끝 검사로 차단(테스트에 케이스) |
| T-03-08 | Denial of Service | `CatalogKR.load` 손상 json | transfer | 어댑터는 `ValueError`/`FileNotFoundError` 를 그대로 올리고, 기동 계속 여부는 app 로더(Plan 06 `load_catalog`) 가 `(OSError, ValueError, KeyError, TypeError)` 로 받아 None 처리 |
| T-03-09 | Denial of Service | 요청마다 정렬·재계산 | mitigate | `_pop_order`·`_by_id`·`_index` 를 `__init__` 1회 계산(serving.md) |
</threat_model>

<verification>
**이 플랜(Worker)이 직접 확인하는 것:** `uv run ruff format <자기 6파일> && uv run ruff check <자기 6파일>` 클린 · `uv run pytest tests/data/test_catalog_kr.py tests/data/test_vectors_kr.py tests/test_architecture.py --no-header` 18 passed · `uv run pytest tests/data tests/serving tests/app --no-header` failed 0.

**공통 기준선(Advisor 가 wave 1 종료 시 1회):** `uv run ruff format --check . && uv run ruff check .` · `uv run pytest --no-header`(failed 0) · `make smoke`(서버 무변경 → `smoke: PASS`, level 3).
</verification>

<success_criteria>
- `CatalogKR` 이 `Catalog`·`Neighbors`·`BookStatsSource` 를, `VectorsKR` 이 `ItemVectors` 를 만족하고 pandas 없이 json/npz 를 읽는다
- `meta()` 에 description·curator_note·seg_dist 없음, `eligible()` 이 D-14 3규칙, `stats()` 가 결측 difficulty None 보존
- `tests/test_architecture.py` 3 passed, 두 파일 ≤150줄, 커밋 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-02-SUMMARY.md` — RED 발췌 · GREEN 요약 줄 · 두 파일 줄 수 · `data/__init__.py` 에 추가된 이름 · fixture 자격 없는 3권 id(18·19·20)와 category_prior 규칙(Plan 06 이 서버 주입 테스트에서 재사용) · "커밋하지 않는다".
</output>
