---
phase: 03-millie-catalog
plan: 07
type: tdd
wave: 4
depends_on: ["03-04", "03-06"]
files_modified:
  - scripts/build_millie_popularity.py           # 연령×성별 12세그먼트 행 concat(같은 스크립트·같은 파일, D-15)
  - tests/data/test_millie_popularity.py         # 세그먼트 5건 추가(기존 5건 무수정)
  - data/processed/popularity_kr.parquet         # gitignore — make millie-popularity 재실행
  - artifacts/serving/popularity_kr.json         # make millie-export 재실행(커밋 대상, 승인 후)
autonomous: true
requirements: [DATA-08]
lane: Data-B (scripts/ Worker) — Should 꼬리. 시간 부족 시 이 플랜만 버린다(ROADMAP "Should 꼬리" · 적재 계획 02 §8 버리는 순서 마지막 = popularity_kr)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - scripts/build_millie_catalog.py
  - scripts/millie_difficulty.py
  - scripts/build_millie_edges.py
  - scripts/export_millie_serving.py
  - scripts/export_millie_vectors.py
  - scripts/export_millie_fallback.py
  - scripts/millie_parse.py
  - scripts/collect_millie.py
  - data/id_map.csv
  - data/processed/books_kr.parquet
  - data/processed/item_edges_kr.parquet
  - artifacts/serving/books_kr.json
  - artifacts/serving/item_edges_kr.json
  - artifacts/serving/content_vectors_kr.npz
  - demo/**
  - tests/data/test_millie_catalog.py
  - tests/data/test_millie_edges.py
  - tests/data/test_millie_export.py
  - tests/fixtures/millie/**
  - src/millie_rec/**
  - tests/test_architecture.py
  - tests/conftest.py
  - Makefile
  - pyproject.toml
  - ../.claude/**
  - CLAUDE.md

must_haves:
  truths:
    - "popularity_kr.parquet/json 에 segment='all' 행(불변) 외에 seg_dist 가 있는 책마다 연령 6 × 성별 2 = 최대 12 세그먼트 행이 같은 4컬럼 {book_id, segment, score, rank} 로 추가되고, score = shelf_count × seg_dist[age][gender], rank 는 세그먼트 안에서 score 내림차순 1..n 연속이다 — D-15 '같은 스크립트·같은 파일에 증분'"
    - "segment 라벨은 밀리 표기 그대로('40대 여성' — top_segment 와 같은 형식): 연령 키(10대…60대~) + 공백 + 성별(남→남성, 여→여성). 변환 테이블은 성별 2항목 상수뿐이다"
    - "seg_dist 가 없는 책(≈16%)은 세그먼트 행에 나오지 않는다(all 행에는 있다); 손계산 케이스에서 어떤 세그먼트의 rank 1 이 all 의 rank 1 과 다르다 — fallback level 2 응답이 전역 인기와 달라질 데이터 전제"
    - "results/*·books_kr.json·popular.json 은 변하지 않는다(popularity_kr 만 재export). fallback level 2 응답 검증은 Phase 5 state.py 이후라 이 플랜의 완료 기준이 아니다(D-15)"
  artifacts:
    - path: "scripts/build_millie_popularity.py"
      provides: "build(books) = all 행 + segment_rows(books) concat · GENDER_LABEL · SEGMENT_SEP"
      exports: ["build", "segment_rows", "POP_COLUMNS", "SEGMENT_ALL", "GENDER_LABEL"]
      max_lines: 150
    - path: "tests/data/test_millie_popularity.py"
      provides: "기존 5 + 세그먼트 5(라벨·score 식·rank 연속·seg_dist 결측 제외·all 과 순위 상이)"
      min_lines: 80
    - path: "artifacts/serving/popularity_kr.json"
      provides: "all + 12 세그먼트 행"
      contains: "\"40대 여성\""
  key_links:
    - from: "scripts/build_millie_popularity.py::segment_rows"
      to: "books_kr.parquet seg_dist(json 문자열)·shelf_count·top_segment 표기"
      via: "json.loads(seg_dist) → {age: {gender: share}} → score = shelf × share"
      pattern: "json.loads"
    - from: "artifacts/serving/popularity_kr.json"
      to: "Phase 5 serving/fallback.py level 2 (catalog.popular(snapshot.categories)·세그먼트)"
      via: "segment 라벨 == 온보딩 연령·성별 표기(Phase 5 가 소비)"
      pattern: "\"segment\""
---

<objective>
Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md "Phase 3: 밀리 카탈로그 빌드") **Should 꼬리 DATA-08**: `scripts/build_millie_popularity.py` 에 연령×성별 12세그먼트 인기 행을 증분한다(`shelf_count × seg_dist[age][gender]`, 라벨은 밀리 표기 그대로). 같은 스크립트·같은 파일·같은 4컬럼 스키마(D-15). Plan 06 이 만든 Makefile 타깃으로 `popularity_kr` 만 재빌드·재export 한다.

Purpose: Phase 5 '서빙 Must 완성' 의 fallback level 2(세그먼트 인기)와 `similar_readers` 행 소스 분리(적재 계획 02 §6 "또래 독자들이 많이 담은 책")의 데이터 전제. 결정 D-15 'popularity_kr 는 all 만 Must, 12세그먼트는 Should 꼬리로 같은 파일 증분'(.planning/phases/03-millie-catalog/03-CONTEXT.md) · Specific Ideas "`segment: "40대 여성"` 같은 밀리 표기 그대로(변환 테이블 만들지 않음)" 이행.
Output: 스크립트 증분(≤150줄 유지) + 테스트 5건 + `popularity_kr.parquet/json` 재생성. 커밋 0.

**버리는 조건:** Day 2 시간이 부족하면 이 플랜만 생략 — Must(DATA-01~07)는 Plan 01~06 으로 완결. **완료 기준에 넣지 않는 것:** fallback level 2 응답이 전역 인기와 다름(Phase 5 `state.py`·`fallback.py` level 2 이후 검증, D-15) · 온보딩 화면의 연령·성별 입력(Phase 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-04-SUMMARY.md
@.planning/phases/03-millie-catalog/03-06-SUMMARY.md
@scripts/build_millie_popularity.py
@tests/data/test_millie_popularity.py
@tests/fixtures/millie/sample_records.jsonl
@Makefile
@../.assets/설계서/데이터 소스/02_밀리_데이터_적재_계획.md
@../.claude/rules/data.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**데이터(`../.claude/rules/data.md`):** "행 소스 분리: `anchor_<seed>`·`after_completion` = content_sim / `similar_readers` = 세그먼트 인기(`shelf_count × seg_dist[top_segment]`, Should) 또는 content_sim — 제목을 소스에 맞춘다('결이 비슷한 책' / '또래 독자들이 많이 담은 책'). **'비슷한 취향 독자' 문구 금지**(유저 유사도 없음)." / 커버리지 게이트 `seg_dist ≥70%`(실측 ≈84% — 미달이면 §7 표 "popularity_kr·similar_readers 소스 분리 폐기").

**간결함(`../.claude/rules/simplicity.md`):** "설정은 모듈 상단 대문자 상수." / 파일 ≤150줄(현 popularity 스크립트 ≈60줄 + 증분 ≈35줄). 변환 테이블 금지(CONTEXT) — 성별 표기 2항목(`남→남성`, `여→여성`)은 `top_segment` 와 같은 형식을 만들기 위한 최소 상수이며 카테고리 매핑표가 아니다(SUMMARY 에 이 판단 1줄).

**TDD(`../.claude/rules/python-tdd.md`):** Red = AssertionError, `uv run pytest <대상> --no-header`. 기존 5건 무수정(all 행 불변이 그 증거).

**최소 변경(../CLAUDE.md §2-9):** `build()` 의 반환 형태·컬럼·all 행 값 불변. 새 함수 `segment_rows(books)` 1개 + concat 1줄.
</project_rules_inline>

<interfaces>
From scripts/build_millie_popularity.py (Plan 04 결과): `POP_COLUMNS = ("book_id","segment","score","rank")` · `SEGMENT_ALL = "all"` · `SEG_DIST_COL = "seg_dist"` · `build(books) -> DataFrame`(all 행: rank = pop_rank, score = shelf_count.fillna(0)) · `main --books --out`.

`seg_dist` 형태(books_kr.parquet 에 **json 문자열**, `build_millie_catalog._row` `json.dumps(seg, ensure_ascii=False)`; 결측은 None): `{"10대": {"남": 1.4, "여": 1.0}, "20대": {...}, "30대": {...}, "40대": {"남": 0.18, "여": 0.31}, "50대": {...}, "60대~": {...}}` — 값은 비율(%) 이며 단위는 순위에 무관. 연령 키 6종(`10대 20대 30대 40대 50대 60대~`), 성별 키 `남`·`여`. `top_segment` 표기 예 `"40대 여성"`. 픽스처 12권: `FIRST_BOOK`(shelf 9,814) seg_dist `{"10대":{"남":0.02,"여":0.05},"40대":{"남":0.18,"여":0.31}}`(연령 2종만) · `8192a3b4c5d60009` seg_dist 결측 · `NO_SHELF_BOOK` seg_dist 있음·shelf 결측.

Makefile(Plan 06 후): `millie-popularity`(`build_millie_popularity.py`) · `millie-export`(serving → vectors → fallback). Worker 는 타깃을 **실행**만 한다(편집 금지).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): 세그먼트 인기 테스트 5건(손계산 + 픽스처) → AssertionError</name>
  <files>tests/data/test_millie_popularity.py</files>
  <read_first>
    - tests/data/test_millie_popularity.py (Plan 04 — 기존 5건·fixture 체인 그대로 재사용)
    - scripts/build_millie_popularity.py (현재 `build`)
    - tests/fixtures/millie/sample_records.jsonl 의 `seg_dist`·`top_segment` 값(FIRST_BOOK·NO_SHELF_BOOK·결측 책)
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-15 · "Specific Ideas — popularity_kr.json 행 예시"
  </read_first>
  <action>
    기존 5건 아래에 추가. 손계산 프레임 헬퍼 `_seg_frame()`: 컬럼 `book_id shelf_count pop_rank seg_dist`, 행 3개 —
    ```
    A: book_id 1, shelf 1000, pop_rank 1, seg_dist '{"10대":{"남":10,"여":10},"40대":{"남":5,"여":75}}'
    B: book_id 2, shelf  900, pop_rank 2, seg_dist '{"10대":{"남":50,"여":40},"40대":{"남":5,"여":5}}'
    C: book_id 3, shelf  800, pop_rank 3, seg_dist None
    ```
    (all 순위 A>B>C; "10대 남성" 은 B(45,000) > A(10,000); "40대 여성" 은 A(75,000) > B(4,500).) 5건:
    - `test_segment_rows_labels_are_millie_style(pop_mod)` — `seg = pop_mod.segment_rows(_seg_frame())`; `set(seg["segment"]) == {"10대 남성","10대 여성","40대 남성","40대 여성"}`(라벨 = 연령 키 + " " + 남성/여성); 컬럼 == `["book_id","segment","score","rank"]`
    - `test_segment_score_is_shelf_times_share(pop_mod)` — `("10대 남성", book 2)` score == 900×50 == 45000.0; `("40대 여성", book 1)` score == 1000×75 == 75000.0; dtype float
    - `test_segment_rank_is_contiguous_and_can_differ_from_all(pop_mod)` — "10대 남성" rank 1 == book 2 · rank 2 == book 1(all 은 book 1 이 1위 — DATA-08 "전역 인기와 다름" 의 데이터 전제); "40대 여성" rank 1 == book 1; 각 세그먼트 `sorted(rank) == [1, 2]`
    - `test_books_without_seg_dist_only_in_all(pop_mod)` — `full = pop_mod.build(_seg_frame())`; `set(full[full.segment=="all"].book_id) == {1,2,3}`; `3 not in set(full[full.segment!="all"].book_id)`; all 행은 Plan 04 결과와 동일(`rank == pop_rank`, `score == shelf`)
    - `test_fixture_build_has_all_plus_segments(pop, books)` — `pop`(픽스처 12권 build 결과, module fixture 재사용): `"all" in set(pop.segment)`; 세그먼트 라벨이 전부 정규식 `^(10대|20대|30대|40대|50대|60대~) (남성|여성)$` 매치; `FIRST_BOOK`(book_id 1) 의 `"40대 여성"` 행 score == 9814×0.31 ≈ 3042.34(`pytest.approx`); seg_dist 결측 책(millie_id `8192a3b4c5d60009`)의 book_id 가 세그먼트 행에 없음; `NO_SHELF_BOOK` 은 seg_dist 있어도 shelf 결측 → score 0.0 이고 그 세그먼트의 마지막 rank
    **주의:** 기존 `test_popularity_columns_and_all_segment` 가 `set(pop["segment"]) == {"all"}`·`len(pop) == 12` 를 단정한다 — 세그먼트 행이 붙으면 이 두 단정이 깨진다. 이것은 **의도된 동작 변경(D-15 증분)** 이므로 그 두 단정만 `set(pop[pop.segment == "all"]["segment"]) == {"all"}`·`(pop.segment == "all").sum() == 12` 로 **범위를 좁혀** 고친다(all 행 검사 의미 유지 — 삭제 아님). 나머지 4건은 all 행만 보므로 무수정(`test_rank_equals_books_pop_rank` 등이 `pop` 전체를 쓰면 `pop[pop.segment=="all"]` 로 필터하는 한 줄만).
    RED: `uv run pytest tests/data/test_millie_popularity.py --no-header 2>&1 | tail -20` → 신규 5건 failed(`AttributeError: segment_rows` 가 먼저 나면 스텁 `def segment_rows(books): return pd.DataFrame(columns=list(POP_COLUMNS))` 를 스크립트에 추가해 **AssertionError** 로 바꾼다), 기존 5건 passed.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/data/test_millie_popularity.py --no-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/data/test_millie_popularity.py` == 10
    - RED 출력에 신규 5건 `AssertionError`/`E  assert`, `ImportError`·`AttributeError`·`SyntaxError` 없음; 기존 5건 passed
    - `grep -c '"40대 여성"' tests/data/test_millie_popularity.py` ≥ 1; `grep -c "segment_rows" tests/data/test_millie_popularity.py` ≥ 2
    - `git diff tests/data/test_millie_popularity.py | grep "^-" | grep -c "assert"` == 0 (Plan 04 단정은 처음부터 `segment == "all"` 행 범위로 작성돼 이 플랜은 순수 증분 — 단정 삭제·축소 없음)
  </acceptance_criteria>
  <done>세그먼트 5건 RED. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): segment_rows 구현 + build concat → 10 passed → make millie-popularity && make millie-export 재실행 → popularity_kr.json 세그먼트 확인</name>
  <files>scripts/build_millie_popularity.py, data/processed/popularity_kr.parquet, artifacts/serving/popularity_kr.json</files>
  <read_first>
    - scripts/build_millie_popularity.py · tests/data/test_millie_popularity.py (Task 1 — 단정이 계약)
    - Makefile `millie-popularity`·`millie-export`(Plan 06 결과)
    - .planning/phases/03-millie-catalog/03-06-SUMMARY.md (실측 산출물 크기·N)
  </read_first>
  <action>
    **2-a. 스크립트 증분(≤150줄):** `import json` 추가(Plan 04 스텁은 `argparse`·`Path`·`pandas` 만 import — `seg_dist` 는 json 문자열 컬럼). 상수 `GENDER_LABEL = {"남": "남성", "여": "여성"}  # top_segment 표기("40대 여성")와 같은 형식. 카테고리 매핑표가 아니다(CONTEXT "변환 테이블 없음")` · `SEGMENT_SEP = " "`.
    ```python
    def segment_rows(books: pd.DataFrame) -> pd.DataFrame:
        """연령×성별 세그먼트 인기(D-15 Should): score = shelf_count × seg_dist[age][gender], 세그먼트 안 rank. seg_dist 없는 책은 제외."""
        shelf = pd.to_numeric(books["shelf_count"], errors="coerce").astype(float).fillna(0.0)
        rows: list[dict] = []
        for book_id, sc, raw in zip(books["book_id"].astype("int64"), shelf, books[SEG_DIST_COL], strict=True):
            if not isinstance(raw, str) or not raw:
                continue
            for age, by_gender in json.loads(raw).items():
                for gender, share in (by_gender or {}).items():
                    label = f"{age}{SEGMENT_SEP}{GENDER_LABEL.get(gender, gender)}"
                    rows.append({"book_id": int(book_id), "segment": label, "score": float(sc) * float(share or 0.0)})
        if not rows:
            return pd.DataFrame(columns=list(POP_COLUMNS))
        seg = pd.DataFrame(rows).sort_values(["segment", "score", "book_id"], ascending=[True, False, True])
        seg["rank"] = seg.groupby("segment").cumcount() + 1
        return seg.reset_index(drop=True)[list(POP_COLUMNS)]
    ```
    `build()`: 기존 all 프레임을 `all_rows` 로 두고 `return pd.concat([all_rows, segment_rows(books)], ignore_index=True)[list(POP_COLUMNS)]`(all 행이 먼저, 그 값 불변). Plan 04 의 `# Plan 07(DATA-08)` 주석 줄은 이 호출로 교체. `main` 출력의 `segments=` 가 13 이 되는지 확인(all + 12).

    **2-b. GREEN:** `uv run pytest tests/data/test_millie_popularity.py --no-header` → `10 passed`. `uv run pytest tests/data/test_millie_popularity.py tests/data/test_millie_export.py --no-header` failed 0 — Plan 04 의 popularity·export 단정은 처음부터 `segment == "all"` 행 범위라 세그먼트 행 증분으로 깨지지 않는다. `tests/data/test_millie_export.py` 는 must_not_touch 그대로(예외 없음). 깨진다면 그것은 Plan 04 가 범위 규칙을 어긴 것이므로 수정하지 말고 `## CHECKPOINT REACHED` 로 보고한다. `uv run ruff format scripts/build_millie_popularity.py tests/data/test_millie_popularity.py && uv run ruff check <같은 파일>`. `wc -l scripts/build_millie_popularity.py` ≤ 150.

    **2-c. 재빌드·재export(Makefile 타깃 실행만):** `make millie-popularity && make millie-export` → `popularity rows=<N + Σ세그먼트> segments=13`. 검증:
    ```bash
    uv run python -c "
    import json, collections
    p = json.load(open('artifacts/serving/popularity_kr.json'))
    segs = collections.Counter(r['segment'] for r in p)
    assert segs['all'] >= 5000 and len(segs) == 13, segs
    labels = set(segs) - {'all'}
    assert labels == {f'{a} {g}' for a in ('10대','20대','30대','40대','50대','60대~') for g in ('남성','여성')}, labels
    for s in labels:
        ranks = sorted(r['rank'] for r in p if r['segment'] == s); assert ranks == list(range(1, len(ranks)+1)), s
    top = {s: min((r for r in p if r['segment']==s), key=lambda r: r['rank'])['book_id'] for s in segs}
    print('segments ok', {s: n for s, n in segs.items()}, 'distinct top-1 across segments:', len(set(top.values())))
    "
    find artifacts/serving -size +50M | wc -l    # 0
    git status --short artifacts results demo   # popularity_kr.json 만 M (books_kr.json·item_edges·npz·popular.json·results 무변경)
    ```
    `distinct top-1` 이 1 이면(전 세그먼트 1위가 같은 책) 데이터가 세그먼트별로 갈리지 않는다는 뜻 — 단언은 아니지만 SUMMARY 에 기록(Phase 5 level 2 검증 때 참고). `uv run pytest --no-header` 전체 failed 0 · `make smoke` PASS(서버는 popularity_kr.json 을 아직 읽지 않으므로 응답 불변).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check scripts/build_millie_popularity.py tests/data/test_millie_popularity.py && uv run pytest tests/data/test_millie_popularity.py --no-header 2>&1 | tail -2 && [ "$(wc -l < scripts/build_millie_popularity.py)" -le 150 ] && uv run python -c "import json,collections; p=json.load(open('artifacts/serving/popularity_kr.json')); s=collections.Counter(r['segment'] for r in p); assert len(s)==13 and '40대 여성' in s, s; print('segments', len(s))" && [ -z "$(git status --short artifacts/serving/books_kr.json artifacts/serving/item_edges_kr.json artifacts/serving/content_vectors_kr.npz demo results)" ] && uv run pytest --no-header 2>&1 | tail -2 && make smoke</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/data/test_millie_popularity.py --no-header` → `10 passed`; `uv run pytest --no-header` failed 0; `make smoke` → `smoke: PASS`
    - `wc -l scripts/build_millie_popularity.py` ≤ 150; `grep -c "def segment_rows" scripts/build_millie_popularity.py` == 1; `grep -c 'GENDER_LABEL = {"남": "남성", "여": "여성"}' scripts/build_millie_popularity.py` == 1
    - `artifacts/serving/popularity_kr.json` 세그먼트 종류 13(all + 12), 라벨 정규식 `^(10대|20대|30대|40대|50대|60대~) (남성|여성)$`, 세그먼트별 rank 1..n 연속, 크기 <50MB
    - `git status --short` 에서 `artifacts/serving/books_kr.json`·`item_edges_kr.json`·`content_vectors_kr.npz`·`demo/fallback/popular.json`·`results/*` 는 이 플랜으로 변하지 않음(Plan 06 상태 그대로), `popularity_kr.json`·`scripts/build_millie_popularity.py`·`tests/data/test_millie_popularity.py` 만 변경
    - `git log --oneline | head -1` 불변
  </acceptance_criteria>
  <done>popularity_kr 이 all + 12세그먼트를 담고 다른 산출물은 불변. fallback level 2 응답 검증은 Phase 5 몫으로 SUMMARY 에 인계. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `seg_dist`(밀리 집계 비율) → `popularity_kr.json` | 원본 분포는 내보내지 않고 파생 순위만 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-29 | Information Disclosure | `seg_dist` 원본 비율 | mitigate | 행에는 `score`(shelf×share)·`rank` 만 — 원본 json 문자열·`share` 값 자체는 미포함(export 화이트리스트 Plan 04 + 이 행 스키마 4컬럼) |
| T-03-30 | Tampering | 잘못된 seg_dist 문자열(json 손상) | mitigate | `json.loads` 실패는 예외로 드러난다(빌드 단계에서 fail — 조용히 넘기지 않음); `share` None 은 0.0 |
| T-03-31 | Denial of Service | 행 수 증가(N×12) | accept | 5,5k×13 ≈ 72k 행 json ≈ 5MB — 50MB 상한 단언 유지 |
| T-03-32 | Repudiation | "level 2 가 전역 인기와 다르다" 를 이 플랜이 주장 | accept | 데이터 전제(세그먼트별 top-1 상이)만 기록, 응답 검증은 Phase 5 — 완료 기준에 넣지 않는다(D-15) |
</threat_model>

<verification>
**이 플랜(Worker)이 직접 확인하는 것:** `uv run ruff format <자기 파일> && uv run ruff check <자기 파일>` 클린 · `uv run pytest --no-header` failed 0(wave 4 단일 플랜이라 전역 실행 허용) · `make smoke` PASS · `popularity_kr.json` 세그먼트 13종·다른 산출물 무변경.

**공통 기준선:** `uv run pytest --no-header` + `uv run ruff check .` + `make smoke` — 이 플랜은 마지막 플랜이므로 직접 실행한다.
</verification>

<success_criteria>
- `popularity_kr` 에 all + 12세그먼트 행, 라벨 밀리 표기, 세그먼트 내 rank 연속, 손계산 케이스에서 세그먼트 1위 ≠ all 1위
- 기존 all 행·다른 산출물·results 불변, 스크립트 ≤150줄, 커밋 0
- level 2 응답 검증은 Phase 5 인계(완료 기준 아님)
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-07-SUMMARY.md` — RED/GREEN 발췌 · 세그먼트 행 수·13종 라벨 · `distinct top-1` 수 · export 테스트 필터 축소 여부(했으면 2줄 diff) · Phase 5 인계(level 2 는 `segment` 라벨로 조회; 온보딩 연령·성별 입력은 Phase 6 화면) · "커밋하지 않는다".
</output>
