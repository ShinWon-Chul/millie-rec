---
phase: 03-millie-catalog
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/millie_difficulty.py                 # NEW — 순수 함수, ≤150줄
  - scripts/build_millie_catalog.py              # 유효 레코드 필터·카운터·3컬럼·D-17 id 부여(260줄 예외 유지, 순증 ≤25줄)
  - tests/data/test_millie_difficulty.py         # NEW
  - tests/data/test_millie_catalog.py            # EXPECTED_COLUMNS 3컬럼 · 카운터 테스트 · 게이트 단언 강화
  - data/id_map.csv                              # append-only(신규 유효분만). 커밋 대상(승인 후)
  - data/processed/books_kr.parquet              # gitignore — 첫 스냅샷 빌드
  - data/processed/millie_raw_coverage.json      # gitignore
  - results/millie_coverage.csv                  # 커밋 대상 — 게이트 테스트가 쓴다
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
lane: Data-B (scripts/ Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (Phase 2 선례 결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - scripts/build_millie_edges.py
  - scripts/export_millie_serving.py
  - scripts/millie_parse.py
  - scripts/collect_millie.py
  - tests/fixtures/millie/**
  - tests/data/test_millie_edges.py
  - tests/data/test_millie_parse.py
  - src/millie_rec/**
  - tests/test_architecture.py
  - tests/conftest.py
  - Makefile
  - pyproject.toml
  - demo/**
  - ../.claude/**
  - CLAUDE.md

must_haves:
  truths:
    - "scripts/build_millie_catalog.py 가 status 2xx ∧ title 있음 레코드만 books_kr.parquet 행·id 부여·게이트 분모로 삼고, title 없는 성공 페이지는 n_skipped_empty(title·category·shelf_count 전무) / n_skipped_titleless(title 없음 ∧ shelf_count 있음) 카운터로만 millie_raw_coverage.json 에 남는다"
    - "books_kr.parquet 컬럼은 계약 9개(book_id title authors image_url average_rating ratings_count original_publication_year categories tags)를 문자 그대로 갖고, resid_z·len_z·difficulty 3컬럼이 difficulty_source 바로 앞에 있으며 book_format ∈ {전자책, 오디오북, 챗북}"
    - "difficulty = 1/(1+exp(resid_z)) 이고 difficulty_source == 'category_prior' 행은 resid_z == 0.0 · difficulty 결측(NaN) · len_z 는 expected_min 이 있으면 계산된다 — len_z 는 difficulty 에 들어가지 않는다"
    - "data/id_map.csv 는 기존 1,066행 불변(git diff --numstat 삭제 0)이고 신규 book_id 는 유효 레코드에만 N+1 부터 discovered_urls.txt 순서로 붙는다"
    - "실데이터 게이트 test_coverage_gate 가 통과하고 results/millie_coverage.csv 에 title,1.0000 · _n_records · _category_distinct · _categories_ge_20 · _n_skipped_empty · _n_skipped_titleless · _n_success 행이 있으며 n_skipped_titleless / n_success ≤ 0.005 이다"
  artifacts:
    - path: "scripts/millie_difficulty.py"
      provides: "add_difficulty(books: pd.DataFrame) -> pd.DataFrame — resid_z·len_z·difficulty 3컬럼"
      exports: ["add_difficulty", "N_QUANTILES", "MIN_CATEGORY_SAMPLE", "MIN_Z_SAMPLE", "NEW_COLUMNS"]
      max_lines: 150
    - path: "scripts/build_millie_catalog.py"
      provides: "유효 레코드 필터 read_records · 카운터 coverage · MAX_TITLELESS_RATIO · add_difficulty 호출"
      contains: "MAX_TITLELESS_RATIO = 0.005"
    - path: "tests/data/test_millie_difficulty.py"
      provides: "손계산 케이스 6건(부호·σ 항등·결측·소표본·len_z 값·컬럼 불변)"
      min_lines: 60
    - path: "results/millie_coverage.csv"
      provides: "커버리지 게이트 실측(첫 스냅샷)"
      contains: "_n_skipped_titleless"
  key_links:
    - from: "scripts/build_millie_catalog.py"
      to: "scripts/millie_difficulty.py"
      via: "sys.path.insert(scripts 디렉터리) 후 import millie_difficulty — build_frame 끝에서 add_difficulty(df)"
      pattern: "millie_difficulty\\.add_difficulty\\("
    - from: "scripts/build_millie_catalog.py::build"
      to: "scripts/build_millie_catalog.py::assign_ids"
      via: "유효 레코드의 millie_id 만 jsonl_ids 로 넘기고 discovered_urls 목록을 그 집합으로 필터(D-17)"
      pattern: "discovered_urls"
    - from: "tests/data/test_millie_catalog.py::test_coverage_gate"
      to: "results/millie_coverage.csv"
      via: "csv.writer 행 추가 + titleless 비율 단언"
      pattern: "_n_skipped_titleless"
---

<objective>
Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md "Phase 3: 밀리 카탈로그 빌드")의 카탈로그 빌더를 TDD 한 사이클로 마감한다: (1) 완독지수 파생 난이도 순수 함수 `scripts/millie_difficulty.py`(신규) (2) `scripts/build_millie_catalog.py` 의 유효 레코드 정의·카운터·`resid_z len_z difficulty` 3컬럼·id 부여 범위 (3) 지금 있는 스냅샷(고유 ≈5,49x권, 배치 진행 중)으로 **첫 실빌드** → `data/id_map.csv` append → 커버리지 게이트 실측 → `results/millie_coverage.csv`.

Purpose: ROADMAP Success Criteria 1(커버리지 게이트 + DATA-01·02·03)과 3 의 난이도 절반(DATA-04)을 파일과 테스트로 증명한다. PDF P3 "★난이도 = 밀리 완독지수에서 분야·길이 조건부 잔차로 파생" 문장의 코드 증거. 결정 D-01 '지금 있는 권수로 첫 빌드, 배치 종료 후 make millie 재빌드'(.planning/phases/03-millie-catalog/03-CONTEXT.md) · D-05 '유효 레코드 = status 200 ∧ title 있음' · D-06 'titleless ≤0.5% 단언' · D-16 '새 로직은 새 파일' · D-17 'id 부여는 유효 레코드에만'(같은 파일) 을 이행한다.
Output: 위 2개 스크립트 + 테스트 2파일 + 첫 스냅샷 산출물 3개 + `results/millie_coverage.csv`. 커밋 0.

**하지 않는 것:** 파서 수정(title 미스 1건은 D-05 대로 카운터만) · 기존 3파일 150줄 분리(D-16) · 이웃·export·어댑터(형제 플랜) · `tests/fixtures/millie/sample_records.jsonl` 수정(카운터 테스트는 tmp jsonl 을 합성한다).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@.planning/phases/03-millie-catalog/03-PATTERNS.md
@src/millie_rec/contracts.py
@scripts/build_millie_catalog.py
@tests/data/test_millie_catalog.py
@tests/fixtures/millie/sample_records.jsonl
@../.assets/설계서/데이터 소스/02_밀리_데이터_적재_계획.md
@../.claude/rules/data.md
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**실행 인터프리터(`../.claude/rules/python-tdd.md`):** "`uv run pytest <대상> --no-header` — `-q` 는 pyproject addopts 에 이미 있다. CLI 에 `-q` 를 또 붙이면 `-qq` 가 되어 'N passed' 요약 줄이 사라진다." / "**Red는 `AssertionError`(의도한 단정 실패) 메시지로 확인**한다. `ImportError`·`SyntaxError`·collection error는 Red가 아니다." / 금지: "테스트를 통과시키려고 `test_architecture.py`나 기존 테스트의 단정을 고치는 것." — 이 플랜에서 기존 테스트 수정은 **컬럼 확장(EXPECTED_COLUMNS 3개 삽입)과 게이트 강화(단언 추가)** 두 방향만 허용한다(결정 D-06(.planning/phases/03-millie-catalog/03-CONTEXT.md) "테스트 수정은 게이트 강화 방향만").

**데이터 규칙(`../.claude/rules/data.md`):** "`book_id`는 계약대로 **int**. 밀리 16자 hex id → `data/id_map.csv`(`millie_id,book_id,first_seen_at`) 사전순 1회 부여, 보충분은 N+1부터 **append-only**. 재수집·재빌드에도 기존 행 변동 0(diff 검증). `shelf_count` 순 id 부여 금지." / 난이도: "파생 = `resid_z = z_분야(P_완독 − E[P_완독 | 분야, expected_min 분위])`, `len_z = z_분야(expected_min)`. 합성 `difficulty = σ(−resid_z)`. `len_z`는 합성에 넣지 않는다('긴 책 = 어려운 책' 회귀 방지). 결측: `completion_prob := category_avg_prob`, `resid_z := 0`, `difficulty_source := "category_prior"`, **`difficulty := None`**." / 커버리지 게이트: "`title` 100% · `image_url` ≥99% · `categories` ≥95% · `completion_prob` ≥70% · `formats` ≥90% · `seg_dist` ≥70% · 카테고리 종수 ≥8·20권 이상 분야 ≥6. 미달 시 조치는 적재 계획 §7 표."

**간결함(`../.claude/rules/simplicity.md`):** "설정은 모듈 상단 대문자 상수." / "함수 우선. 클래스는 `contracts.py`의 Protocol을 만족시킬 때만." / 새 `scripts/` 파일 ≤150줄. `build_millie_catalog.py` 는 D-16 예외(이미 260줄) — **순증 25줄 이내**, 요청하지 않은 정리 금지(../CLAUDE.md §2-9 최소 변경).

**python.md:** "완료 전 순서대로: `uv run ruff format . && uv run ruff check . && uv run pytest --no-header`." — 이 플랜은 **자기 파일만** format/check 한다(형제 플랜 파일과 경합 방지, Phase 2 wave 게이트 선례). "경로는 `contracts.py`의 `DIR_*` 상수에서 시작한다."

**scripts ↔ 슬라이스 경계(03-PATTERNS.md §0):** `scripts/*.py` 는 `millie_rec.contracts` 만 import 한다. `tests/test_architecture.py` 검사 대상은 아니지만 관례를 유지한다.
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 값. 재탐색하지 않는다. -->

From scripts/build_millie_catalog.py (현재):
```python
COLUMNS = tuple("book_id millie_id title subtitle authors publisher pub_date original_publication_year "
    "categories subcategories book_format formats image_url average_rating rating_observed "
    "ratings_count review_count shelf_count pop_rank tags description curator_note "
    "completion_prob category_avg_prob expected_min category_avg_min millie_label "
    "difficulty_source seg_dist top_segment isbn13 pages_diag collected_at source".split())
INT_COLS = (... "completion_prob category_avg_prob expected_min category_avg_min")   # Int64 nullable
MIN_BOOKS_PER_CATEGORY = 20
def _present(value) -> bool          # None·""·[]·{} 결측, 0 은 값
def _is_collected(rec) -> bool       # status None/"ok"/2xx
def read_records(jsonl) -> tuple[list[dict], dict]   # meta = {"n_lines", "n_skipped_status"}
def assign_ids(path, raw_dir, jsonl_ids) -> dict[str, int]   # order = sorted(catalog_urls) + discovered_urls(파일 순) + sorted(jsonl_ids)
def _row(rec, book_id) -> dict       # completion_prob 결측 → category_avg_prob, difficulty_source 판정
def build_frame(records, ids) -> pd.DataFrame   # pop_rank 부여 → DataFrame(columns=COLUMNS) → INT_COLS Int64
def coverage(records, meta) -> dict  # n_records n_lines n_skipped_status fields categories category_distinct categories_ge_min min_books_per_category
def build(jsonl, out_dir, raw_dir=None, id_map=None) -> pd.DataFrame
```

From tests/data/test_millie_catalog.py (현재): `_load(name)` importlib 로더(L83-89, `sys.modules[name] = module`) · `FIXTURE` 12권(14줄, 실패 1·재수집 중복 1) · `NO_PROB_BOOK="6f708192a3b40007"`(completion_prob 결측, category_avg_prob 49) · `FIRST_BOOK="0f1e2d3c4b5a6001"`(prob 78, expected_min 214, 소설) · `CHATBOOK="1a2b3c4d5e6f7002"` · `FAILED_BOOK="deadbeefdeadbeef"` · `test_raw_coverage_json_is_written` 이 `n_records==12 · n_lines==14 · n_skipped_status==1 · fields.title==1.0 · category_distinct==10` 단정 · `test_coverage_gate` L283-312 (산출물 없으면 skip, csv 행 형식 `writer.writerow(["_n_records", report["n_records"]])`).

원본 JSONL 레코드 키(실수집 = 픽스처 동일): `millie_id status title subtitle authors publisher category pub_date average_rating shelf_count review_count seg_dist top_segment completion_prob category_avg_prob expected_min category_avg_min millie_label curator_note description image_url formats best_links best_titles best_category collected_at source url`. `completion_prob`·`expected_min` 은 **int 또는 None**(둘은 함께 결측, 실측 769/5,492).

실측(2026-09-05 저녁, 배치 진행 중 — 숫자는 스냅샷마다 커진다): 고유 5,492 · title 없음 6(껍데기 5 = title·category·shelf_count 전무, 성인 플레이스홀더 `adult-cover` 2 포함 · 파서 미스 1 = 외국어 shelf 2,038) · 카테고리 27종 · 20권 이상 21종 · description 없음 136 · completion_prob None 769(≈14%). `data/id_map.csv` 1,067줄(헤더 포함 = 1,066행).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): 난이도 손계산 테스트 6건 + 카탈로그 유효 레코드·카운터·3컬럼·id 범위 테스트 → 전부 AssertionError</name>
  <files>tests/data/test_millie_difficulty.py, tests/data/test_millie_catalog.py, scripts/millie_difficulty.py (스텁만)</files>
  <read_first>
    - tests/data/test_millie_catalog.py (전문 — `_load`·픽스처 상수·EXPECTED_COLUMNS·`built` fixture·`test_coverage_gate` 형식을 그대로 따른다)
    - scripts/build_millie_catalog.py (전문 — 현재 `read_records`·`assign_ids`·`coverage` 형태)
    - .planning/phases/03-millie-catalog/03-CONTEXT.md "Claude's Discretion — 난이도 파생 세부" · D-05 · D-06 · D-17
    - .planning/phases/03-millie-catalog/03-PATTERNS.md "`scripts/millie_difficulty.py`" 절(scripts 간 import 함정 포함) · "`scripts/build_millie_catalog.py` (MODIFY)" 절 변경점 1~4
    - ../.claude/rules/python-tdd.md (Red = AssertionError · `-q` 중복 금지)
  </read_first>
  <action>
    **1-a. 스텁 `scripts/millie_difficulty.py`** — 테스트가 `_load("millie_difficulty")` 로 로드할 수 있어야 하므로 먼저 만든다(collection error 는 Red 가 아니다). 내용: 모듈 docstring 1줄(`"""completion_prob·expected_min → resid_z·len_z·difficulty (DATA-04, 적재 계획 02 §4 C-2)."""`) + 상수 `N_QUANTILES = 4` · `MIN_CATEGORY_SAMPLE = 20` · `MIN_Z_SAMPLE = 5` · `CATEGORY_COL = "categories"` · `SOURCE_MEASURED = "millie_index"` · `NEW_COLUMNS = ("resid_z", "len_z", "difficulty")` + `def add_difficulty(books: "pd.DataFrame") -> "pd.DataFrame": return books.copy()`(스텁 — 컬럼을 추가하지 않으므로 단정이 실패한다).

    **1-b. `tests/data/test_millie_difficulty.py`** (신규, 헤더 docstring `"""난이도 파생 — 계약(3컬럼·입력 불변) · 정확성(손계산 부호·σ 항등·len_z 값) · 안전성(결측·소표본)."""`). `_load` 는 `test_millie_catalog.py` L83-89 를 그대로 복사(`sys.modules[name] = module` 포함). module-scope fixture `diff_mod = _load("millie_difficulty")`. 손계산 프레임 헬퍼 `_frame() -> pd.DataFrame` — 컬럼 `book_id categories completion_prob expected_min difficulty_source`, 행 13개:
    ```
    소설 8권(measured):   book_id 1..8, categories ["소설"], expected_min 10,20,30,40,50,60,70,80,
                         completion_prob 80,60,70,50,60,40,50,30, difficulty_source "millie_index"
    에세이 3권(measured): book_id 9,10,11, ["에세이"], expected_min 30,60,90, completion_prob 55,50,45, "millie_index"
    결측 1권:            book_id 12, ["에세이"], completion_prob 50(=분야 평균 대체값), expected_min 120, "category_prior"
    결측+길이 결측 1권:  book_id 13, ["소설"], completion_prob 61, expected_min None, "category_prior"
    ```
    `completion_prob`·`expected_min` 은 `pd.array(..., dtype="Int64")` 로 만들어 실데이터 dtype(Int64 nullable)과 같게 한다. 테스트 6건(이름 그대로):
    - `test_add_difficulty_appends_three_float_columns_and_keeps_input(diff_mod)` — `out = diff_mod.add_difficulty(inp)`; `set(out.columns) == set(inp.columns) | {"resid_z", "len_z", "difficulty"}`; `len(out) == 13`; `pd.testing.assert_frame_equal(out[inp.columns], inp)`(입력 컬럼 불변); 새 3컬럼 dtype 이 float(`out["difficulty"].dtype.kind == "f"`).
    - `test_resid_z_sign_follows_within_cell_residual(diff_mod, monkeypatch)` — `monkeypatch.setattr(diff_mod, "MIN_CATEGORY_SAMPLE", 4)`(소설 8권이 분야 내 4분위를 쓰게); `out = add_difficulty(_frame()).set_index("book_id")`; 분위 셀 (1,2)(3,4)(5,6)(7,8) 마다 앞 책이 +10, 뒤 책이 −10 잔차 → `out.loc[1,"resid_z"] > 0 > out.loc[2,"resid_z"]`, `out.loc[1,"resid_z"] == pytest.approx(-out.loc[2,"resid_z"])`, 같은 관계를 (3,4)·(5,6)·(7,8)에도; `out.loc[1,"difficulty"] < 0.5 < out.loc[2,"difficulty"]`.
    - `test_difficulty_is_sigmoid_of_negative_resid_z(diff_mod)` — measured 11행 전부 `difficulty == pytest.approx(1/(1+np.exp(resid_z)))`; 결측 2행은 `difficulty` 가 `isna`.
    - `test_missing_completion_prob_gives_zero_resid_and_none_difficulty(diff_mod)` — book 12·13: `resid_z == 0.0` · `pd.isna(difficulty)`; book 12 `np.isfinite(len_z)`; book 13 `pd.isna(len_z)`.
    - `test_small_category_uses_global_bins_and_stays_finite(diff_mod)` — 기본 상수(MIN_CATEGORY_SAMPLE=20 → 두 분야 모두 전역 분위): measured 11행 `resid_z` 전부 `np.isfinite`; 에세이 3권(<MIN_Z_SAMPLE) `resid_z` 가 전부 0 은 아님(`(out.loc[[9,10,11],"resid_z"] != 0).any()`) — 전역 분위 셀 평균 대비 잔차라 0 이 아니다.
    - `test_len_z_is_within_category_zscore_of_expected_min(diff_mod)` — 소설 8권(n=8 ≥ MIN_Z_SAMPLE) `expected_min` 10..80 → 평균 45, 표본표준편차(ddof=1) √600 = 24.4949 → `out.loc[1,"len_z"] == pytest.approx(-35/24.494897, abs=1e-3)`, `out.loc[8,"len_z"] == pytest.approx(35/24.494897, abs=1e-3)`; book 13(expected_min 결측)은 len_z 계산에서 빠져야 위 값이 맞다.

    **1-c. `tests/data/test_millie_catalog.py` 수정(허용 방향만):**
    - `EXPECTED_COLUMNS` 에 `"resid_z", "len_z", "difficulty"` 를 `"difficulty_source"` **바로 앞**에 삽입(계약 컬럼 확장 — `../.claude/rules/data.md` 밀리 고유 컬럼 목록에 이미 있는 이름).
    - `test_raw_coverage_json_is_written` 에 단정 3줄 추가: `report.get("n_success") == 12` · `report.get("n_skipped_empty") == 0` · `report.get("n_skipped_titleless") == 0`(**`.get()` 을 써서 키 부재가 KeyError 아닌 AssertionError 로 떨어지게 한다**).
    - 신규 `test_derived_difficulty_columns_in_built_frame(built)` — `_row(df, NO_PROB_BOOK)`: `resid_z == 0.0` · `pd.isna(difficulty)` · `difficulty_source == "category_prior"`; `_row(df, FIRST_BOOK)`: `0.0 < difficulty < 1.0` · `pd.notna(resid_z)` · `pd.notna(len_z)`; `df["difficulty"].dropna().between(0, 1).all()`.
    - 신규 `test_valid_record_filter_counts_empty_and_titleless(tmp_path, catalog)` — tmp jsonl = `FIXTURE` 14줄 + 2줄: 껍데기 `{"millie_id":"ffffffff00000001","status":200,"image_url":"https://d1miajbjsyro89.cloudfront.net/adult-cover-a.webp","collected_at":"2026-09-05T02:00:00+00:00","source":"discovered","url":"https://www.millie.co.kr/v3/bookDetail/ffffffff00000001"}` · 파서 미스 `{"millie_id":"ffffffff00000002","status":200,"category":"외국어","shelf_count":2038,"image_url":"https://img.millie.co.kr/x.jpg","collected_at":"2026-09-05T02:00:00+00:00","source":"discovered","url":"..."}`; `raw/discovered_urls.txt` 에 두 id 를 적는다; `catalog.build(jsonl, out, raw_dir=raw)` → `len(df) == 12`; report: `n_records == 12` · `n_success == 14` · `n_skipped_empty == 1` · `n_skipped_titleless == 1` · `titleless_ratio == pytest.approx(1/14)` · `n_lines == 16`; `id_map.csv` 에 두 id **없음**(D-17 — discovered 목록에 있어도 유효 레코드가 아니면 id 를 받지 않는다). 새 키는 전부 `.get()`.
    - 신규 `test_discovered_id_without_valid_record_gets_no_id(tmp_path, catalog)` — `discovered_urls.txt` = `CHATBOOK` + `"ffffffff00000003"`(레코드 없음) → id_map 에 CHATBOOK 있고 `ffffffff00000003` 없음. (기존 `test_book_ids_are_lexicographic_over_catalog_urls` 의 "sitemap 항목은 실패여도 id" 규칙은 그대로 — `catalog_urls.txt` 는 필터하지 않는다.)
    - `test_coverage_gate` 확장(L301-303 형식 그대로 아래에 3행): `writer.writerow(["_n_skipped_empty", report.get("n_skipped_empty", 0)])` · `["_n_skipped_titleless", report.get("n_skipped_titleless", 0)]` · `["_n_success", report.get("n_success", report["n_records"])]`; 단언 추가 `assert report.get("n_skipped_titleless", 0) / max(report.get("n_success", 1), 1) <= 0.005, "파서 미스 비율 초과 — 파서 수정·해당 URL 재수집(D-06)"`(상수는 손으로 `0.005`, L23 관례). skip 조건·기존 단언 불변.

    **1-d. RED 확인:** `uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py --no-header 2>&1 | tail -40`. 기대: 난이도 6건 failed(`AssertionError`/`KeyError: 'resid_z'` — **KeyError 가 보이면 그 단정 앞에 `assert "resid_z" in out.columns` 를 두어 AssertionError 가 먼저 나게 한다**), 카탈로그 신규·수정 4건 failed(AssertionError), 기존 나머지 passed, 게이트 1 skipped. `ImportError`·`ModuleNotFoundError`·`SyntaxError` 0. 출력을 SUMMARY 에 발췌.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test -f scripts/millie_difficulty.py && uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/data/test_millie_difficulty.py` == 6; `grep -c "def test_" tests/data/test_millie_catalog.py` == 18(기존 15 + 3)
    - `grep -n '"resid_z",' tests/data/test_millie_catalog.py` 가 `"difficulty_source",` 줄보다 앞에 있고 `"len_z"`·`"difficulty"` 가 그 사이에 있다
    - `grep -c "_n_skipped_titleless" tests/data/test_millie_catalog.py` ≥ 1; `grep -c "0.005" tests/data/test_millie_catalog.py` ≥ 1
    - RED 출력에 `AssertionError` 또는 `E  assert` 가 있고 `ImportError`·`ModuleNotFoundError`·`SyntaxError`·`NameError` 가 없다(SUMMARY 발췌)
    - `scripts/millie_difficulty.py` 에 `N_QUANTILES = 4`·`MIN_CATEGORY_SAMPLE = 20`·`MIN_Z_SAMPLE = 5`·`def add_difficulty(` 문자열 존재
    - `git diff --stat -- tests/fixtures scripts/build_millie_edges.py scripts/export_millie_serving.py src` → 출력 없음
  </acceptance_criteria>
  <done>난이도 6건 + 카탈로그 신규·확장 4건이 단정 실패로 RED. 픽스처 원본 불변. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): millie_difficulty.add_difficulty 구현 + build_millie_catalog 유효 레코드·카운터·3컬럼·D-17 → 24 passed(게이트 skip)</name>
  <files>scripts/millie_difficulty.py, scripts/build_millie_catalog.py</files>
  <read_first>
    - scripts/millie_difficulty.py (Task 1 스텁)
    - scripts/build_millie_catalog.py L86-99 `read_records` · L114-146 `assign_ids` · L193-208 `build_frame` · L211-227 `coverage` · L230-245 `build`
    - tests/data/test_millie_difficulty.py · tests/data/test_millie_catalog.py (Task 1 결과 — 단정이 계약이다)
    - ../.claude/rules/data.md "Track B 난이도" 절 · ../.claude/rules/simplicity.md "코드 규칙"
  </read_first>
  <action>
    **2-a. `scripts/millie_difficulty.py`** (≤150줄, pandas·numpy 만, `millie_rec` import 불필요). 상수는 Task 1 스텁 그대로 + `NO_CATEGORY = "_none"`(빈 categories) · `GLOBAL_KEY = "_global"`(소표본 분야의 셀 키) · `NO_LEN_BIN = -1`(expected_min 결측 셀). 함수 4개:
    ```python
    def _category(value: object) -> str | None:
        """categories 리스트의 첫 원소 = 분야(test_millie_edges._same_category_share 와 같은 규칙)."""
        seq = list(value) if isinstance(value, list | tuple | np.ndarray) else []
        return str(seq[0]) if seq else None

    def _bins(x: pd.Series, q: int = N_QUANTILES) -> pd.Series:
        """분위 라벨 0..q-1. rank(method='first') 로 동률을 깨 구간을 항상 만든다. 표본 < q 면 전부 0."""
        if x.notna().sum() < q:
            return pd.Series(0, index=x.index, dtype="int64")
        return pd.qcut(x.rank(method="first"), q, labels=False).astype("int64")

    def _zscore(x: pd.Series, group: pd.Series, min_n: int = MIN_Z_SAMPLE) -> pd.Series:
        """분야 내 z. 분야 표본 < min_n 또는 std 가 0/NaN 이면 분야 평균 − 전역 std(ddof=1)."""
        g = x.groupby(group)
        mean, std, n = g.transform("mean"), g.transform("std"), g.transform("count")
        std = std.where((n >= min_n) & (std > 0), x.std())
        z = (x - mean) / std
        return z.where(std > 0, 0.0)

    def add_difficulty(books: pd.DataFrame) -> pd.DataFrame:
        """resid_z·len_z·difficulty 3컬럼 추가. 입력은 build_frame 결과(completion_prob 결측은 이미 분야 평균으로 채워짐)."""
        df = books.copy()
        cat = df[CATEGORY_COL].map(_category).fillna(NO_CATEGORY)
        prob = pd.to_numeric(df["completion_prob"], errors="coerce").astype(float)
        emin = pd.to_numeric(df["expected_min"], errors="coerce").astype(float)
        measured = (df["difficulty_source"] == SOURCE_MEASURED) & prob.notna()
        # ① expected_min 분위 — 분야 표본(measured) ≥ MIN_CATEGORY_SAMPLE 이면 분야 내, 아니면 전역 분위 + 전역 셀
        m = measured & emin.notna()
        big = cat[m].map(cat[m].value_counts()) >= MIN_CATEGORY_SAMPLE
        cat_bins = emin[m].groupby(cat[m]).transform(lambda s: _bins(s))
        global_bins = _bins(emin[m])
        bins = pd.Series(NO_LEN_BIN, index=df.index, dtype="int64")
        bins[m] = cat_bins.where(big, global_bins)
        cell = pd.Series(NO_CATEGORY, index=df.index, dtype="object")
        cell[measured] = cat[measured]
        cell[m] = cat[m].where(big, GLOBAL_KEY)
        # ② E[P_완독 | 셀, 분위] = 셀 평균 → 잔차
        expected = prob[measured].groupby([cell[measured], bins[measured]]).transform("mean")
        resid = prob[measured] - expected
        # ③ 분야 내 z (resid 는 measured 만, len_z 는 expected_min 이 있는 전 행)
        resid_z = pd.Series(0.0, index=df.index)
        resid_z[measured] = _zscore(resid, cat[measured])
        len_z = pd.Series(np.nan, index=df.index)
        has_len = emin.notna()
        len_z[has_len] = _zscore(emin[has_len], cat[has_len])
        difficulty = pd.Series(np.nan, index=df.index)
        difficulty[measured] = 1.0 / (1.0 + np.exp(resid_z[measured]))  # = σ(−resid_z). len_z 는 넣지 않는다
        df["resid_z"], df["len_z"], df["difficulty"] = resid_z, len_z, difficulty
        return df
    ```
    소표본 분야(측정 표본 <20)의 셀 = `("_global", 전역 분위)` 이므로 그 분야 책의 기대값은 **전 카탈로그 같은 길이 분위의 평균**이다(분야 자체 셀을 쓰면 3권 분야는 잔차가 전부 0 이 되어 σ(0)=0.5 로 뭉친다 — 그것을 피하는 Claude's Discretion 선택). measured 이면서 expected_min 결측인 행은 `(분야, -1)` 셀. 이 규칙 3줄을 모듈 docstring 아래 `#` 주석으로 남긴다(왜만).

    **2-b. `scripts/build_millie_catalog.py`** — 순증 ≤25줄, 기존 함수 시그니처 불변:
    - 상단 import 뒤(`from millie_rec.contracts import ...` 다음):
      ```python
      SCRIPTS_DIR = Path(__file__).resolve().parent
      if str(SCRIPTS_DIR) not in sys.path:  # scripts/ 는 패키지가 아니다 — 테스트의 importlib 로드에서도 찾게
          sys.path.insert(0, str(SCRIPTS_DIR))
      import millie_difficulty  # noqa: E402
      ```
      (`import sys` 를 stdlib 블록에 추가.) `MAX_TITLELESS_RATIO = 0.005  # D-06: 파서 미스 비율 상한 — 단언은 tests/data 게이트가 한다` 를 `MIN_BOOKS_PER_CATEGORY` 아래에.
    - `COLUMNS`: `"difficulty_source"` 앞에 `resid_z len_z difficulty` 삽입(문자열 한 줄 안에서 `"... millie_label resid_z len_z difficulty difficulty_source seg_dist ..."`).
    - `read_records`: docstring 두 번째 문장을 `"유효 = status 2xx ∧ title 있음(D-05). title 없는 성공 페이지는 카운터로만 남긴다 — 껍데기(title·category·shelf_count 전무) / 파서 미스(shelf_count 있음)."` 로 교체. 구현: `json.loads` 를 `try/except json.JSONDecodeError` 로 감싸 실패 줄은 `n_skipped_badline` 카운트(배치가 append 중인 파일의 미완성 마지막 줄 대비 — 배치와 동시에 빌드하는 D-01 의 안전장치). dedup 뒤 세 갈래:
      ```python
      valid, n_empty, n_titleless = [], 0, 0
      for rec in kept.values():
          if _present(rec.get("title")):
              valid.append(rec)
          elif not _present(rec.get("category")) and rec.get("shelf_count") is None:
              n_empty += 1
          else:
              n_titleless += 1
      return valid, {"n_lines": len(lines), "n_skipped_status": skipped, "n_skipped_badline": bad,
                     "n_success": len(kept), "n_skipped_empty": n_empty, "n_skipped_titleless": n_titleless}
      ```
      (`n_lines` 는 비어 있지 않은 줄 수 — 기존 의미 유지, badline 포함.)
    - `assign_ids`: `order` 의 discovered 항목을 `valid = set(jsonl_ids)` 로 필터 — `+ [m for m in _read_id_list(raw_dir / "discovered_urls.txt") if m in valid]`. `catalog_urls` 는 그대로(sitemap 항목은 실패여도 id — 기존 테스트 L136). 주석 1줄 `# D-17: 신규 id 는 유효 레코드에만. 기존 행은 절대 바꾸지 않는다(append-only)`.
    - `build_frame`: `return df` 직전에 `df = millie_difficulty.add_difficulty(df)`.
    - `coverage`: dict 에 `"n_success": meta["n_success"]`, `"n_skipped_empty": meta["n_skipped_empty"]`, `"n_skipped_titleless": meta["n_skipped_titleless"]`, `"n_skipped_badline": meta.get("n_skipped_badline", 0)`, `"titleless_ratio": (meta["n_skipped_titleless"] / meta["n_success"]) if meta["n_success"] else 0.0`, `"max_titleless_ratio": MAX_TITLELESS_RATIO` 추가. 기존 키 유지.
    - `build`: 변경 없음(`records` 가 이미 유효분만이라 `assign_ids(..., [r["millie_id"] for r in records])` 가 D-17 을 만족). `main` 출력에 `skipped empty={} titleless={}` 를 덧붙인다.

    **2-c. GREEN 확인:** `uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py tests/data/test_millie_edges.py --no-header` → 난이도 6 + 카탈로그 17 + 이웃 10 passed, 2 skipped(실데이터 게이트 2개 — 이 시점엔 산출물 없음). `test_millie_edges.py` 는 `build_millie_catalog.build` 를 쓰므로 회귀 확인용. 자기 파일만 정리: `uv run ruff format scripts/millie_difficulty.py scripts/build_millie_catalog.py tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py && uv run ruff check scripts/millie_difficulty.py scripts/build_millie_catalog.py tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py`. `wc -l scripts/millie_difficulty.py` ≤ 150.

    **2-d. REFACTOR(선택, 이 두 파일 안에서만):** 이름·주석 정리만. 통과 재확인.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check scripts/millie_difficulty.py scripts/build_millie_catalog.py tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py && uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py tests/data/test_millie_edges.py --no-header 2>&1 | tail -5 && [ "$(wc -l < scripts/millie_difficulty.py)" -le 150 ]</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py tests/data/test_millie_edges.py --no-header` 요약 `33 passed, 2 skipped`(failed 0)
    - `wc -l scripts/millie_difficulty.py` ≤ 150; `git diff --numstat scripts/build_millie_catalog.py` 의 추가 ≤ 35·삭제 ≤ 10
    - `grep -c "MAX_TITLELESS_RATIO = 0.005" scripts/build_millie_catalog.py` == 1; `grep -c "millie_difficulty.add_difficulty(df)" scripts/build_millie_catalog.py` == 1; `grep -c "n_skipped_titleless" scripts/build_millie_catalog.py` ≥ 2
    - `grep -o "resid_z len_z difficulty difficulty_source" scripts/build_millie_catalog.py | wc -l` == 1(컬럼 순서)
    - `grep -c "millie_rec\." scripts/millie_difficulty.py` == 0(순수 pandas 함수); `grep -c "millie_rec.serving\|millie_rec.app\|millie_rec.data" scripts/build_millie_catalog.py` == 0
    - `uv run ruff check <위 4파일>` → `All checks passed`
  </acceptance_criteria>
  <done>난이도 3컬럼이 계약 순서로 parquet 에 들어가고, 유효 레코드·카운터·D-17 id 범위가 합성 테스트로 증명됨. 실데이터 게이트는 아직 skip. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 3 (실측): 첫 스냅샷 빌드 → id_map append-only 검증 → 커버리지 게이트 → results/millie_coverage.csv</name>
  <files>data/id_map.csv, data/processed/books_kr.parquet, data/processed/millie_raw_coverage.json, results/millie_coverage.csv</files>
  <read_first>
    - .planning/phases/03-millie-catalog/03-CONTEXT.md D-01·D-02·D-04·D-06 · "Specific Ideas — millie_coverage.csv 기대 행"
    - ../.assets/설계서/데이터 소스/02_밀리_데이터_적재_계획.md §7 커버리지 게이트 표(항목별 미달 시 조치)
    - tests/data/test_millie_catalog.py::test_coverage_gate (Task 1 결과)
    - ../.claude/rules/data.md "수집·빌드 스크립트" 절(커버리지 게이트 · id_map 커밋 대상)
  </read_first>
  <action>
    **3-a. 사전:** `wc -l data/id_map.csv`(기대 1067) · `wc -l data/raw/millie_pages.jsonl`(≥5,400, 배치 진행 중이면 더 큼) · `cp data/id_map.csv "$SCRATCH/id_map.before.csv"`(`SCRATCH=/private/tmp/claude-501/-Users-shinwonchul-Documents----------/0de83255-33bf-42e3-ac30-5617663e6d70/scratchpad`, `/tmp` 금지). 배치(`collect_millie.py`)는 **멈추지 않는다**(D-01 — 배치는 계속 돈다; D-03 의 중단은 Day 2 오전 Advisor 몫).

    **3-b. 빌드:** `time uv run python scripts/build_millie_catalog.py` → 출력 `books=<N> → data/processed/books_kr.parquet · id_map → data/id_map.csv skipped empty=<e> titleless=<t>`. 기대 N ≈ 고유 수 − 6 이상(5,48x+), e ≈ 5, t ≈ 1(스냅샷에 따라 커질 수 있다). 30초 넘게 걸리면 시간을 SUMMARY 에.

    **3-c. id_map append-only(D-17·성공 기준 2 절반):**
    ```bash
    git diff --numstat data/id_map.csv          # "<추가>\t0\tdata/id_map.csv" — 삭제 0 이어야 한다
    diff <(head -1067 data/id_map.csv) "$SCRATCH/id_map.before.csv"   # 빈 출력 = 기존 1,066행 + 헤더 바이트 동일
    uv run python -c "import pandas as pd; m=pd.read_csv('data/id_map.csv', dtype={'millie_id':'str'}); assert m['book_id'].is_unique and m['millie_id'].is_unique and list(m['book_id'])==list(range(1,len(m)+1)); print('id_map ok', len(m))"
    ```
    신규 행 수 = 유효 레코드 중 기존 id_map 에 없던 수(≈4,4xx). 삭제가 0 이 아니면 **즉시 중단**하고 `git checkout -- data/id_map.csv` 로 되돌린 뒤 원인(assign_ids 변경)을 SUMMARY 에 적고 `## CHECKPOINT REACHED`.

    **3-d. 게이트:** `uv run pytest tests/data/test_millie_catalog.py::test_coverage_gate --no-header -s` → passed + 출력 두 줄(`average_rating 보유율 …`, `카테고리 N종 · 20권 이상 M종`). `cat results/millie_coverage.csv` — 기대 행: `title,1.0000` · `image_url,≥0.999` · `categories,≥0.999` · `completion_prob,≈0.86` · `formats,≈0.98` · `seg_dist,≈0.83` · `_n_records,<N>` · `_category_distinct,27` · `_categories_ge_20,≥21` · `_n_skipped_empty,≈5` · `_n_skipped_titleless,≈1` · `_n_success,<N+e+t>`. **미달 항목이 있으면** 적재 계획 02 §7 표의 조치를 그대로 적용하는 것이 D-04 이지만 코드 변경은 이 플랜 범위가 아니다 — 수치·해당 조치 문장을 SUMMARY 에 적고 `## CHECKPOINT REACHED`(Advisor 가 개발일지 D 항목으로 기록). titleless 비율 >0.5% 도 같은 절차(D-06: 파서 수정은 그때 별도 브리프).

    **3-e. 마무리:** `uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py --no-header`(이웃 게이트 파일은 제외 — item_edges 없음; 같은 wave 의 Plan 02 가 `tests/conftest.py` 를 편집 중일 수 있어 `tests/data` 전체는 Advisor wave 종료 기준선에서만) → failed 0. `git status --short` 에 `data/id_map.csv`(M)·`results/millie_coverage.csv`(?? 또는 M) 만 데이터 변경으로 보이고 `data/processed/*` 는 없음(gitignore). SUMMARY 에 3-b 출력 원문·3-c 세 줄·3-d csv 전문·`git status --short`·"커밋하지 않는다".
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test -f data/processed/books_kr.parquet && test -f results/millie_coverage.csv && grep -q "^title,1.0000" results/millie_coverage.csv && grep -q "^_n_skipped_titleless," results/millie_coverage.csv && [ "$(git diff --numstat data/id_map.csv | cut -f2)" = "0" ] && uv run pytest tests/data/test_millie_catalog.py::test_coverage_gate --no-header 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `data/processed/books_kr.parquet`·`millie_raw_coverage.json` 존재; `uv run python -c "import pandas as pd; df=pd.read_parquet('data/processed/books_kr.parquet'); print(len(df), list(df.columns)[:9], df['book_format'].unique().tolist(), df['difficulty'].notna().mean().round(3))"` → 행 ≥ 5,000 · 앞 9컬럼 `['book_id','millie_id','title','subtitle','authors','publisher','pub_date','original_publication_year','categories']` · book_format 값 ⊂ {전자책, 오디오북, 챗북} · difficulty 보유율 ≈ 0.86
    - `git diff --numstat data/id_map.csv` 두 번째 필드(삭제) == 0, 첫 필드(추가) ≥ 4,000; `head -1067 data/id_map.csv` 가 빌드 전 파일과 바이트 동일
    - `results/millie_coverage.csv` 에 `title,1.0000`·`_n_records,`·`_category_distinct,`·`_categories_ge_20,`·`_n_skipped_empty,`·`_n_skipped_titleless,`·`_n_success,` 7행 존재; `_category_distinct` ≥ 8·`_categories_ge_20` ≥ 6
    - `uv run pytest tests/data/test_millie_catalog.py::test_coverage_gate --no-header` → `1 passed`
    - `uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py --no-header` → failed 0
    - `git status --short | grep -c "data/processed"` == 0(gitignore); `git log --oneline | head -1` 불변(커밋 0)
  </acceptance_criteria>
  <done>첫 스냅샷 books_kr.parquet 이 게이트를 통과했고 results/millie_coverage.csv 가 있으며 id_map 은 append 만 됐다. 형제 플랜(이웃 실측·export)의 입력이 준비됨. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `data/raw/millie_pages.jsonl` → 빌더 | 외부 사이트에서 수집한 반구조화 텍스트가 코드로 들어오는 유일한 지점(배치가 동시에 append 중) |
| 빌더 → `data/id_map.csv` | 커밋되는 영구 매핑. 손상되면 전 페이즈의 book_id 가 흔들린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Tampering | `assign_ids` → `data/id_map.csv` | mitigate | 기존 행 무변경을 `git diff --numstat` 삭제 0 + 빌드 전 사본 `diff` 로 단언(Task 3-c). 실패 시 `git checkout` 복원·중단 |
| T-03-02 | Denial of Service | `read_records` json.loads (append 중 미완성 줄) | mitigate | `JSONDecodeError` 줄을 `n_skipped_badline` 으로 세고 계속 진행 — 배치와 동시 빌드에서도 크래시 없음 |
| T-03-03 | Information Disclosure | `description`·`curator_note` (밀리 저작 텍스트) | accept(이 플랜) | parquet 는 gitignore·TF-IDF 입력 전용(`data.md`). 서빙 산출물 미포함은 Plan 04·06 의 grep 0건 단언이 담당 |
| T-03-04 | Information Disclosure | 리뷰 텍스트·필명 | accept | 파서가 저장하지 않으며 이 플랜은 파서를 건드리지 않는다(회귀 표면 0) |
| T-03-05 | Tampering | 성인 표지 플레이스홀더·title 없는 껍데기 레코드 | mitigate | D-05 유효 레코드 필터로 카탈로그·id·게이트 분모에서 제외, 건수만 카운터로 기록 |
</threat_model>

<verification>
**이 플랜(Worker)이 직접 확인하는 것:** `uv run ruff format <자기 4파일> && uv run ruff check <자기 4파일>` 클린 · `uv run pytest tests/data/test_millie_difficulty.py tests/data/test_millie_catalog.py --no-header` failed 0(전체 `tests/data` 는 Advisor wave 종료 기준선 — 이웃 게이트 1 skipped 예상) · Task 3 실측 항목 전부.

**공통 기준선(Advisor 가 wave 1 종료 시 1회 — 형제 플랜 03-02 와 포트 8010·전역 ruff 경합 방지):** `uv run ruff format --check . && uv run ruff check .` · `uv run pytest --no-header`(기대 failed 0, skipped 1 = 이웃 게이트) · `make smoke`(서버 코드 무변경이라 level 3 그대로 `smoke: PASS`).
</verification>

<success_criteria>
- `scripts/millie_difficulty.py` ≤150줄, 손계산 6건 통과, `build_millie_catalog.py` 가 3컬럼·카운터·D-17 을 만족하며 기존 15 테스트 무수정 통과(컬럼 리스트 확장·단언 추가만)
- 첫 스냅샷 `books_kr.parquet` 존재, `results/millie_coverage.csv` 에 신설 3행 포함, `test_coverage_gate` passed
- `data/id_map.csv` 삭제 0·기존 1,066행 바이트 동일·신규 book_id 연속
- 커밋 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-millie-catalog/03-01-SUMMARY.md` — RED 출력 발췌(AssertionError) · GREEN 요약 줄 · 실빌드 출력 원문 · `git diff --numstat data/id_map.csv` · `results/millie_coverage.csv` 전문 · 변경 파일 목록 · 미달 항목이 있었으면 §7 조치 문장 · "커밋하지 않는다".
</output>
