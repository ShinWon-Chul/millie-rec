---
phase: 01-local-serving-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/millie_rec/app/server.py
  - tests/serving/test_smoke.py
autonomous: true
requirements: [SKEL-01, SKEL-02, SKEL-05]
lane: 조립(Advisor) — app/server.py + make smoke
no_commit: true   # 실행자는 git commit 을 하지 않는다 — ../CLAUDE.md §6 "커밋/푸시는 항상 사용자 승인 후", 사용자 지시 2026-09-05 "아직 커밋 진행하지 않습니다"

must_haves:
  truths:
    - "`make serve` 를 실행하면 아티팩트·DB·네트워크 없이 서버가 뜨고 http://localhost:8000/health 가 200 HealthOut 을 준다"
    - "http://localhost:8000/ 이 demo/index.html 을 같은 origin 에서 서빙한다(StaticFiles, html=True) — /health·/api/recommend 는 StaticFiles 에 가려지지 않는다"
    - "`make smoke` 가 8010 포트로 서버를 띄워 /health · / · /api/recommend 3개를 확인하고 마지막 줄에 `smoke: PASS` 를 출력한다"
    - "서버 기동만으로 contracts.DIR_DATA_LOCAL/millie.db(data/local/millie.db) 가 자동 생성되고 gitignore 대상이다"
    - "`uv run pytest --no-header` 전체가 통과한다(기준선 93 + Plan 01 의 10 + 이 플랜의 1 = 104 passed, 2 skipped, failed 0)"
  artifacts:
    - path: "src/millie_rec/app/server.py"
      provides: "uvicorn 진입점 `app` — create_app 주입 + demo/ StaticFiles 마운트(마지막). CORS 없음"
      contains: "StaticFiles(directory=ROOT / \"demo\", html=True)"
      max_lines: 30
    - path: "tests/serving/test_smoke.py"
      provides: "server 모듈 import 테스트 1건(GET / 200 + /health 200, DATA_DIR=tmp)"
      contains: "def test_server_module_serves_demo_index_and_keeps_api_routes"
    - path: "data/local/millie.db"
      provides: "make smoke 로 자동 생성된 SQLite(gitignore). 존재 = SKEL-04 실기동 증거"
  key_links:
    - from: "src/millie_rec/app/server.py"
      to: "src/millie_rec/serving/__init__.py"
      via: "공개 표면 2단 import — from millie_rec.serving import Database, GlobalPopularFallback, create_app, resolve_db_path"
      pattern: "from millie_rec\\.serving import"
    - from: "Makefile serve/smoke"
      to: "src/millie_rec/app/server.py"
      via: "uvicorn millie_rec.app.server:app (이미 Makefile L31·L34 에 고정, 변경 없음)"
      pattern: "^app = create_app\\("
    - from: "src/millie_rec/app/server.py"
      to: "demo/index.html"
      via: "app.mount('/', StaticFiles(directory=ROOT / 'demo', html=True)) — 라우트 등록 뒤 마지막"
      pattern: "app\\.mount\\(\"/\""
---

<objective>
Plan 01 이 만든 serving 공개 표면을 `app/server.py` 한 파일로 조립해 실제 프로세스로 띄운다: `uvicorn millie_rec.app.server:app`(Makefile `serve`·`smoke`, Dockerfile CMD 가 이미 가리키는 이름) 이 `create_app(pipelines={}, fallback=GlobalPopularFallback(None), db=Database(resolve_db_path()))` 위에 `demo/` 를 `StaticFiles(html=True)` 로 `/` 에 **마지막** 마운트한다. 그 뒤 `make smoke` 3점 PASS 와 전체 스위트 통과, 사이드이펙트 0 을 확인해 Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md) 을 닫는다.

Purpose: `../.claude/rules/local-run.md` "Day 1 종료부터 서버가 뜬다" 의 실현. 이후 모든 페이즈의 공통 완료 기준(`uv run pytest --no-header` + `make smoke`)이 이 플랜에서 처음 성립한다. 같은 origin 서빙(CORS 없음)이 "프론트+API+SQLite 컨테이너 1개" 설계의 코드 증거(아키텍처 01 §9-3 `app/server.py`).
Output: `src/millie_rec/app/server.py`(≤30줄) + `tests/serving/test_smoke.py` 에 테스트 1건 추가 + `make smoke` PASS 로그. 커밋 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-serving-skeleton/01-CONTEXT.md
@.planning/phases/01-local-serving-skeleton/01-PATTERNS.md
@.planning/phases/01-local-serving-skeleton/01-01-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/__init__.py
@src/millie_rec/app/__init__.py
@tests/serving/test_smoke.py
@tests/test_architecture.py
@Makefile
@Dockerfile
@../.claude/rules/local-run.md
@../.claude/rules/architecture.md
@../.claude/rules/python-tdd.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**app/ 의 import 규칙(`../.claude/rules/architecture.md`, `tests/test_architecture.py` L41-42):** `app` 은 다른 슬라이스의 **공개 표면**(`millie_rec.serving`, 2단 경로)만 import 한다. `from millie_rec.serving.api import create_app` 처럼 3단 경로는 위반. 누구도 `millie_rec.app` 을 import 하지 않는다(테스트는 예외 — `tests/` 는 무엇이든 import 가능). `app/` 에 비즈니스 규칙을 쓰지 않는다(조립만).

**TDD 적용 범위(`../.claude/rules/python-tdd.md` "적용 범위" 표):** `app/` 조립·`contracts.py` 는 "Advisor 직접. 아키텍처 테스트 + 계약 테스트로 대체" — 이 플랜은 `type: execute`. 그래도 테스트 1건을 같은 태스크에서 추가해 `GET /` 200 과 마운트 순서를 고정한다. 실행은 오직 `uv run pytest <대상> --no-header`. `-q` 는 pyproject `addopts` 에 이미 있다 — CLI 에 `-q` 를 붙이지 않는다(`-qq` 가 되어 `N passed` 요약 줄이 사라짐, 2026-09-05 실측). 기존 테스트·`tests/test_architecture.py` 수정 금지.

**로컬 기동(`../.claude/rules/local-run.md`):** `app/server.py` = `create_app(...)` 호출 + `StaticFiles(directory="demo", html=True)` 를 마지막에 `/` 로 마운트. 아티팩트·DB·네트워크 없이 기동. `make smoke` 는 8010 포트에서 `/health`·`/`·`/api/recommend` 3개 확인 후 종료(Makefile L33-40, 변경 없음 — CONTEXT D-12 "3점 유지").

**간결함·최소 변경(`../.claude/rules/simplicity.md`, 사용자 제약 3·4):** CORS 미들웨어 없음(CONTEXT D-11 — 같은 origin 이 설계), 라우트 추가 없음, 환경변수 해석은 `db.py` 의 `resolve_db_path()` 만(server.py 에서 `os.environ` 을 읽지 않는다), 1줄 docstring, 파일 ≤150줄(예상 ≤15줄).

**변경 금지 파일(사용자 제약 1):** `demo/**`(구 `api.js` 가 `hf.space` 를 가리켜 `?source=api` 에서 fallback 배너가 뜨는 것이 **정상** — Phase 6 '데모 재구성' 몫), `src/millie_rec/serving/schemas*.py`, Plan 01 이 만든 `serving/*.py`(이 플랜은 수정하지 않는다), `tests/test_architecture.py`, `tests/serving/test_schemas.py`, `Makefile`, `pyproject.toml`, `Dockerfile`, 다른 슬라이스.

**커밋 금지:** `git add`·`git commit` 을 실행하지 않는다. 작업 트리에 남긴다. 커밋은 Advisor 가 `/codex:review --scope working-tree`(`contracts.py` 가 Plan 01 에서 바뀌었으므로 필수, `../.claude/rules/codex-review.md` 표 1행) 후 사용자 승인으로 실행한다. 커밋 직전 `git ls-files | grep -iE "pdf|png|assets"` 빈 결과 확인은 CONTEXT D-14 — 이 플랜에서는 **점검만** 한다.
</project_rules_inline>

<interfaces>
<!-- Plan 01 이 만든 공개 표면. 실행자는 이 이름만 쓴다. -->

From src/millie_rec/serving/__init__.py (Plan 01 Task 3 결과):
```python
from millie_rec.serving.api import create_app
from millie_rec.serving.db import Database, resolve_db_path
from millie_rec.serving.fallback import GlobalPopularFallback
from millie_rec.serving.schemas import API_VERSION, EventIn, RecommendOut
__all__ = ["API_VERSION", "Database", "EventIn", "GlobalPopularFallback", "RecommendOut", "create_app", "resolve_db_path"]
```

From src/millie_rec/serving/api.py:
```python
def create_app(pipelines: dict[str, Pipeline], fallback: Pipeline, *, catalog: Catalog | None = None,
               db: Database, neighbors: Neighbors | None = None,
               book_stats: BookStatsSource | None = None, state: object | None = None) -> FastAPI: ...
# fallback 뒤는 keyword-only, db 는 필수(아키텍트 리뷰 W-1) — server.py 호출은 키워드 형태만
```

From src/millie_rec/serving/db.py:
```python
def resolve_db_path() -> Path: ...        # $DATA_DIR/millie.db 또는 contracts.DIR_DATA_LOCAL / millie.db
class Database:
    def __init__(self, path: Path) -> None: ...   # parent mkdir(parents=True, exist_ok=True)
```

From src/millie_rec/serving/fallback.py:
```python
class GlobalPopularFallback:
    name = "fallback"
    def __init__(self, catalog: Catalog | None) -> None: ...
```

From src/millie_rec/contracts.py:
```python
ROOT = Path(__file__).resolve().parents[2]   # 로컬 millie-rec/, Docker /app (COPY demo ./demo → /app/demo 와 일치)
ENV_DATA_DIR = "DATA_DIR"
DB_FILENAME = "millie.db"
```

From Makefile (변경 없음, 참조만):
```make
serve:  uv run uvicorn millie_rec.app.server:app --port 8000
smoke:  uv run uvicorn millie_rec.app.server:app --port 8010 --log-level warning & … curl /health · / · /api/recommend?seeds=1,2,3&k=5 … echo "smoke: PASS"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/server.py 조립(create_app + StaticFiles 마지막 마운트) + server 모듈 테스트 1건</name>
  <files>src/millie_rec/app/server.py, tests/serving/test_smoke.py</files>
  <read_first>
    - .planning/phases/01-local-serving-skeleton/01-CONTEXT.md D-09(호출 형태)·D-11(server.py 구성·CORS 없음)·D-13(별도 1건 `GET /` 200 테스트, 실제 `data/local/millie.db` 는 테스트가 건드리지 않음)
    - .planning/phases/01-local-serving-skeleton/01-PATTERNS.md "app/server.py" 절 (골격·`/` 마운트 순서) 및 "test_smoke.py" 절 말미 **함정**(server import 시 `DATA_DIR` 을 tmp 로 먼저 설정, 모듈 캐시 `sys.modules.pop`)
    - src/millie_rec/serving/__init__.py (Plan 01 결과 — export 이름 7개 확인)
    - src/millie_rec/app/__init__.py (docstring 관례 1줄)
    - tests/serving/test_smoke.py (Plan 01 결과 — 마지막에 1건 추가, 기존 10건·import·fixture 는 그대로)
    - tests/test_architecture.py L37-42 (app 은 2단 경로만)
    - Makefile L30-40, Dockerfile L13 (진입점 이름 `millie_rec.app.server:app` 고정)
    - demo/index.html 존재 확인만(`ls -la demo/index.html`, 읽기·수정 금지)
  </read_first>
  <behavior>
    - `monkeypatch.setenv(ENV_DATA_DIR, str(tmp_path))` 후 `millie_rec.app.server` 를 import 하면 `app` 객체가 만들어지고 `tmp_path / "millie.db"` 가 생긴다(실제 `data/local/` 미접촉)
    - `with TestClient(app) as c:` — `c.get("/")` → 200, 본문에 `<html` 포함(demo/index.html)
    - 같은 클라이언트에서 `c.get("/health")` → 200, `c.get("/docs")` → 200 (StaticFiles 가 `/health`·`/docs` 를 가리지 않음 = 마운트가 마지막; `/docs` 는 PDF P2 아키텍처 페이지 캡처 대상)
    - `c.get("/api/recommend", params={"seeds": "1,2,3", "k": 5})` → 200, `fallback_level == 3`
  </behavior>
  <action>
    **1-a. `src/millie_rec/app/server.py` 를 다음 내용으로 생성**(CONTEXT D-09·D-11; PATTERNS "app/server.py" 골격 그대로. `os.environ` 을 읽지 않는다 — 경로 해석은 `resolve_db_path()`):
    ```python
    """uvicorn 진입점: create_app 주입 + demo/ StaticFiles(마지막). CORS 없음(아키텍처 01 §9-3)."""

    from fastapi.staticfiles import StaticFiles

    from millie_rec.contracts import ROOT
    from millie_rec.serving import Database, GlobalPopularFallback, create_app, resolve_db_path

    # Phase 2 → pipelines["pop"], Phase 3 → catalog, Phase 5 → neighbors·book_stats·state.
    # 이 호출의 인자만 채운다 — 시그니처 불변(CONTEXT D-09).
    app = create_app(pipelines={}, fallback=GlobalPopularFallback(None), db=Database(resolve_db_path()))
    # 반드시 마지막 — 앞에 두면 /health·/api/* 가 StaticFiles 에 가려진다
    app.mount("/", StaticFiles(directory=ROOT / "demo", html=True), name="demo")
    ```
    금지: `CORSMiddleware`, 추가 라우트, `os.environ.get`, `millie_rec.serving.api` 같은 3단 import, 비즈니스 규칙.

    **1-b. `tests/serving/test_smoke.py` 끝에 다음 테스트 1건을 추가**(`/docs` 단정 포함 — FastAPI 자동 문서는 PDF P2 캡처 대상(../CLAUDE.md §4 "자동 문서(`/docs` 캡처 → P2)")이므로 StaticFiles 마운트가 이를 가리면 안 된다, 아키텍트 리뷰 W-3. 기존 10건·import·fixture 불변. 상단 import 에 `import importlib`·`import sys` 를 stdlib 그룹에 추가한다 — ruff `I` 정렬 유지):
    ```python
    # ── 조립(app/server.py) ──────────────────────────────────────────────────
    def test_server_module_serves_demo_index_and_keeps_api_routes(tmp_path: Path, monkeypatch):
        """millie_rec.app.server:app — StaticFiles 가 마지막에 마운트되어 / 는 demo, /health·/docs·/api 는 그대로."""
        # 실제 data/local/millie.db 를 건드리지 않는다(D-13)
        monkeypatch.setenv(ENV_DATA_DIR, str(tmp_path))
        # 모듈 캐시 — 이 테스트가 유일한 import 지점이지만 재실행에도 안전하게
        sys.modules.pop("millie_rec.app.server", None)
        server = importlib.import_module("millie_rec.app.server")
        with TestClient(server.app) as c:
            root = c.get("/")
            assert root.status_code == 200 and "<html" in root.text.lower()
            assert c.get("/health").status_code == 200
            assert c.get("/docs").status_code == 200  # PDF P2 캡처 대상 — 마운트 순서로 보장
            rec = c.get("/api/recommend", params={"seeds": "1,2,3", "k": 5})
        assert rec.status_code == 200 and rec.json()["fallback_level"] == FALLBACK_GLOBAL_POP
        assert (tmp_path / DB_FILENAME).exists()
    ```

    **1-c. 확인:** `uv run pytest tests/serving/test_smoke.py --no-header` → **11 passed**. `uv run pytest tests/test_architecture.py --no-header` → 3 passed(`app` 이 2단 경로만 import). `uv run ruff format src/millie_rec/app tests/serving && uv run ruff check src tests` 클린. `wc -l src/millie_rec/app/server.py` ≤ 30. `ls data/local/ 2>/dev/null` — 테스트만으로는 `data/local/` 이 **생기지 않아야** 한다(env 를 tmp 로 돌렸으므로; 이미 다른 이유로 존재하면 무시). **커밋하지 않는다.**
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_smoke.py tests/test_architecture.py --no-header && uv run ruff check src tests && wc -l src/millie_rec/app/server.py</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/millie_rec/app/server.py`; `grep -n '^app = create_app(pipelines={}, fallback=GlobalPopularFallback(None), db=Database(resolve_db_path()))' src/millie_rec/app/server.py` 1행; `grep -n 'app.mount("/", StaticFiles(directory=ROOT / "demo", html=True)' src/millie_rec/app/server.py` 1행이며 파일에서 `app = create_app` 줄보다 **뒤**에 있다
    - (주의: `grep -c … == 0` 류 기준은 grep 이 exit 1 을 내므로 `&&` 체인에 넣지 않고 개별 실행) `grep -n "from millie_rec.serving import" src/millie_rec/app/server.py` 1행, `grep -c "millie_rec.serving\." src/millie_rec/app/server.py` == 0 (3단 경로 없음), `grep -c "CORSMiddleware\|os.environ\|@app\." src/millie_rec/app/server.py` == 0
    - `wc -l src/millie_rec/app/server.py` ≤ 30
    - `grep -c "def test_" tests/serving/test_smoke.py` == 11, `grep -n "def test_server_module_serves_demo_index_and_keeps_api_routes" tests/serving/test_smoke.py` 1행, `grep -n "monkeypatch.setenv(ENV_DATA_DIR" tests/serving/test_smoke.py` 1행, `grep -n 'assert c.get("/docs").status_code == 200' tests/serving/test_smoke.py` 1행(W-3)
    - `uv run pytest tests/serving/test_smoke.py --no-header` → `11 passed`; `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `uv run ruff check src tests` → `All checks passed!`
  </acceptance_criteria>
  <done>`millie_rec.app.server:app` 이 import 가능하고, `/` 가 demo 를, `/health`·`/api/recommend` 가 API 를 준다(마운트 순서 테스트로 고정). 11 passed. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 2: make smoke 3점 PASS · 전체 스위트 · 사이드이펙트 감사 (Phase 1 종료 판정)</name>
  <files>data/local/millie.db (make smoke 가 자동 생성, gitignore — 코드 변경 없음)</files>
  <read_first>
    - Makefile L30-40 (`serve`·`smoke` 원문 — 8010 포트·3 curl·`smoke: PASS`/`FAIL`·`.uvicorn.pid`)
    - ../.claude/rules/local-run.md ("모든 브리프의 완료 기준 = pytest 전체 + `make smoke`" — pytest 는 `uv run pytest --no-header`, 규칙 원문의 `-q` 는 addopts 와 중복; "아티팩트·DB·네트워크 없이 기동")
    - .planning/ROADMAP.md "Phase 1: 로컬 서빙 스켈레톤" Success Criteria 1~4
    - .planning/phases/01-local-serving-skeleton/01-CONTEXT.md D-12(3점 유지)·D-14(커밋 전 점검 — 이 플랜은 점검만)
    - .gitignore (`data/local/`·`*.db` 제외 확인)
  </read_first>
  <action>
    코드 변경 없음. 실측·감사만. 모든 명령은 `/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec` 에서.

    **2-a. 전체 스위트:** `uv run pytest --no-header 2>&1 | tail -3` → **104 passed, 2 skipped**(기준선 93 + Plan 01 10 + Plan 02 1, failed 0; 기준선은 HEAD 8e5172b 실측). `uv run ruff format --check src tests && uv run ruff check src tests` 클린.

    **2-a′. 환경 전제:** `test -z "$DATA_DIR" && echo "DATA_DIR unset — 기본 경로 data/local/millie.db" || echo "DATA_DIR=$DATA_DIR 설정됨 — 2-c 의 기대 경로를 $DATA_DIR/millie.db 로 읽는다"`. 셸에 `DATA_DIR` 가 있으면 `make smoke` 는 그 경로에 DB 를 만든다(`resolve_db_path()`).

    **2-b. 포트 선점 확인 후 `make smoke`:** `lsof -iTCP:8010 -sTCP:LISTEN -n -P || true` 가 비어 있어야 한다(점유 중이면 그 PID 를 SUMMARY 에 적고 `kill` 하지 말고 재시도 대기 — 다른 세션의 프로세스일 수 있다). 그 뒤 `make smoke 2>&1 | tee /private/tmp/claude-501/-Users-shinwonchul-Documents----------/63b477b3-ee16-49e1-b9a5-3e3e73654e1a/scratchpad/smoke.log`. 기대 출력 4줄: `smoke: /health 200` · `smoke: / (demo static) 200` · `smoke: /api/recommend 200` · `smoke: PASS`, exit 0. 3초 sleep 안에 uvicorn 이 안 뜨면(첫 import 가 느린 머신) 한 번 재실행 — Makefile 은 수정하지 않는다. 종료 후 `lsof -iTCP:8010 -sTCP:LISTEN -n -P || true` 빈 출력(pid 정리 확인), `test ! -f .uvicorn.pid`.

    **2-c. SKEL-04 실기동 증거:** `ls -la data/local/millie.db` 존재(`make smoke` 가 `resolve_db_path()` 기본 경로에 생성). `uv run python -c "import sqlite3; con=sqlite3.connect('data/local/millie.db'); print(sorted(r[0] for r in con.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'\"))); print(con.execute('PRAGMA journal_mode').fetchone())"` → 7개 테이블 이름(`book_stats candidate_sets events preference_snapshots ratings recommendations users`) + `('wal',)`. `git check-ignore data/local/millie.db` 가 경로를 출력(커밋 대상 아님). `git status --short | grep -c "data/local"` == 0.

    **2-d. 실응답 형태 스냅샷(SUMMARY 용, PDF 숫자 아님):** 서버를 잠깐 띄워 두 응답을 받는다 —
    `uv run uvicorn millie_rec.app.server:app --port 8011 --log-level warning & echo $! > /private/tmp/claude-501/-Users-shinwonchul-Documents----------/63b477b3-ee16-49e1-b9a5-3e3e73654e1a/scratchpad/uv.pid; sleep 3; curl -fsS localhost:8011/health; echo; curl -fsS "localhost:8011/api/recommend?seeds=1,2,3&k=5"; echo; kill $(cat /private/tmp/claude-501/-Users-shinwonchul-Documents----------/63b477b3-ee16-49e1-b9a5-3e3e73654e1a/scratchpad/uv.pid)`.
    기대: `/health` JSON 에 `"status":"ok"`, `"api_version":"v2"`, `"model_version":null`, `"db_ok":true`, `db_row_count` 에 7키 전부 0; `/api/recommend` JSON 에 `"model_version":"fallback_v1"`, `"fallback_level":3`, `"forced":false`, `"items":[]`, `rows[0].row_id == "trending"`, `"recommendation_id":"rec_……"`(10자). CONTEXT `<specifics>` 의 기대 응답 예시와 필드 집합이 같아야 한다(`latency_ms` 값은 다를 수 있음 — 참고값).

    **2-e. 사이드이펙트 감사(사용자 제약 1, Phase 1 전체):** `git status --short` 의 코드 변경이 정확히 — `M src/millie_rec/contracts.py`(+1줄), `M src/millie_rec/serving/__init__.py`, `?? src/millie_rec/serving/schema.sql`, `?? src/millie_rec/serving/db.py`, `?? src/millie_rec/serving/fallback.py`, `?? src/millie_rec/serving/api.py`, `?? src/millie_rec/app/server.py`, `?? tests/serving/test_smoke.py` — 이 8개뿐이어야 한다(`.planning/**` 은 문서, 무관). `git diff --stat -- demo src/millie_rec/serving/schemas.py src/millie_rec/serving/schemas_should.py tests/test_architecture.py tests/serving/test_schemas.py Makefile pyproject.toml Dockerfile uv.lock` **빈 출력**. `git diff --numstat src/millie_rec/contracts.py` 가 `1	0`(추가 1·삭제 0). `find src/millie_rec -type d \( -name domain -o -name application -o -name infrastructure -o -name api -o -name shared -o -name utils -o -name common -o -name helpers \)` 빈 출력(계층 폴더 0 — 사용자 제약 5). `wc -l src/millie_rec/serving/db.py src/millie_rec/serving/fallback.py src/millie_rec/serving/api.py src/millie_rec/app/server.py` 전부 ≤150. `git ls-files | grep -iE "pdf|png|assets" || echo none` → `none`(D-14 점검). `git log -1 --format=%h` 가 플랜 시작 전과 동일(커밋 0).

    **2-f. SUMMARY 에 남길 것:** `make smoke` 4줄 로그, 전체 pytest 요약 줄, `/health`·`/api/recommend` 실응답(축약), `git status --short` 8줄, `wc -l` 표, "커밋 없음 — 다음은 Advisor: `/codex:review --scope working-tree`(contracts 변경 필수) → 사용자 승인 → 로컬 커밋 1개(push 없음, D-14)". 브라우저 확인(`make serve` → `http://localhost:8000/?source=api` 에서 구 데모가 fallback 배너와 함께 뜨면 정상, CONTEXT specifics)은 **Advisor/사용자 몫**으로 SUMMARY 에 안내만 적고 실행자는 하지 않는다. **커밋하지 않는다.**
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest --no-header 2>&1 | tail -1 && make smoke 2>&1 | tail -4 && test -f data/local/millie.db && git check-ignore -q data/local/millie.db && [ -z "$(git diff --stat -- demo src/millie_rec/serving/schemas.py src/millie_rec/serving/schemas_should.py tests/test_architecture.py tests/serving/test_schemas.py Makefile pyproject.toml Dockerfile)" ] && echo "side-effects: none"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --no-header` 마지막 줄 `104 passed, 2 skipped` (기준선 93 + 11, failed 0)
    - `make smoke` 출력에 `smoke: /health 200`·`smoke: / (demo static) 200`·`smoke: /api/recommend 200`·`smoke: PASS` 4줄 전부, exit 0, 종료 후 `.uvicorn.pid` 없음
    - `data/local/millie.db` 존재, sqlite_master 테이블 7개, `PRAGMA journal_mode` == `wal`, `git check-ignore data/local/millie.db` 출력 있음
    - `curl localhost:<port>/health` JSON 에 `"model_version":null`·`"db_ok":true`, `db_row_count` 키 7개; `/api/recommend?seeds=1,2,3&k=5` JSON 에 `"model_version":"fallback_v1"`·`"fallback_level":3`·`"row_id":"trending"`
    - `git status --short -- src tests data` 의 변경이 위 8경로만(코드 범위로 한정 — `CLAUDE.md`·`.claude/**`·`.planning/**`·`.gitignore` 등 문서·설정 변경은 Advisor 세션 몫이라 감사 대상 아님), `git diff --stat -- demo … Dockerfile uv.lock` 빈 출력, `git diff --numstat src/millie_rec/contracts.py` == `1	0`
    - 계층 폴더 find 결과 빈 출력; 신규 `.py` 4개 `wc -l` ≤ 150
    - `git log -1 --format=%h` 플랜 전후 동일(실행자 커밋 0)
  </acceptance_criteria>
  <done>ROADMAP Phase 1 Success Criteria 1~4 가 실측으로 성립: `make serve` 진입점 기동·`/health` 200·`/` 데모 같은 origin·`/api/recommend` level 3 200·`data/local/millie.db` Must 4(+Should 3) 테이블·`uv run pytest --no-header` + `make smoke` PASS. 허용 경로 밖 변경 0, 커밋 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `StaticFiles(demo/)` | 비신뢰 경로 요청이 파일 시스템 서빙에 닿는 경계 |
| 브라우저 → `/health`·`/api/*` | Plan 01 의 API 경계(마운트 순서로 도달 보장) |
| `make smoke`/`make serve` → `data/local/millie.db` | 로컬 파일 생성(운영자 실행, 사용자 입력 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure (path traversal) | `app/server.py` `StaticFiles(directory=ROOT/"demo")` | accept | Starlette `StaticFiles` 는 요청 경로를 `directory` 안으로 정규화하고 밖이면 404 — 표준 동작에 의존. `demo/` 는 공개 정적 자산만(비밀·`.env`·DB 없음, `.gitignore` 로 `data/local/` 분리) |
| T-01-08 | Denial of Service (라우트 가림) | `/` 마운트 순서 | mitigate | 마운트를 라우트 등록 뒤 마지막에 두고 `test_server_module_serves_demo_index_and_keeps_api_routes` 가 `/health`·`/api/recommend` 200 을 같은 앱에서 단정 |
| T-01-09 | Tampering | `data/local/millie.db` 생성 경로 | accept | 경로는 `resolve_db_path()`(운영자 env 또는 `contracts.DIR_DATA_LOCAL` 고정). HTTP 입력이 경로에 닿지 않음. gitignore 로 커밋 유출 방지 |
| T-01-10 | Repudiation | 요청 로깅 없음 | accept | Phase 1 은 로그 테이블에 쓰지 않는다(D-06). 추천 노출 로그 4필드는 Phase 5 '서빙 Must 완성'(SERV-06 노출 로그, .planning/REQUIREMENTS.md) |
| T-01-11 | Spoofing | 인증 없음·CORS 없음 | accept | 같은 origin 단일 컨테이너 설계(아키텍처 01 §9-3, CONTEXT D-11). 교차 origin 은 브라우저 기본 정책이 차단. 인증은 범위 밖(REQUIREMENTS Out of Scope) |
</threat_model>

<verification>
- `uv run pytest --no-header` → 104 passed, 2 skipped (기준선 93 + 11)
- `uv run pytest tests/test_architecture.py --no-header` → 3 passed (`app` 은 `millie_rec.serving` 2단 경로만)
- `make smoke` → `smoke: PASS` (3점: `/health`·`/`·`/api/recommend`)
- `data/local/millie.db` 자동 생성, 테이블 7개, WAL, gitignore
- `git diff --stat` 에 변경 금지 파일 0, `contracts.py` 는 +1줄만, 계층 폴더 0, 신규 `.py` ≤150줄
- 실행자 커밋 0건
</verification>

<success_criteria>
- **SKEL-01** `make serve` 진입점(`millie_rec.app.server:app`) 이 아티팩트·DB·네트워크 없이 뜨고 `GET /health` 200 `HealthOut`(`model_version` null)
- **SKEL-02** `GET /` 가 `demo/` 정적 데모를 같은 origin 에서 서빙(StaticFiles `html=True`, 마지막 마운트)
- **SKEL-05** `make smoke` 가 3개를 확인하고 `smoke: PASS` — 이후 모든 페이즈의 공통 완료 기준 성립
- (SKEL-03·SKEL-04 는 Plan 01 에서 테스트로, 이 플랜에서 `make smoke`·`data/local/millie.db` 실기동으로 재확인)
- CONTEXT D-09(호출 형태)·D-11(CORS 없음·마운트 마지막)·D-13(server 테스트 1건, 실제 DB 미접촉)·D-14(커밋 전 점검만) 반영
- 사용자 제약 1~5 전부 감사 항목으로 확인됨
</success_criteria>

<output>
완료 후 `.planning/phases/01-local-serving-skeleton/01-02-SUMMARY.md` 를 만든다. 포함: `make smoke` 4줄 로그 · 전체 pytest 요약 · `/health`·`/api/recommend` 실응답 축약 · `git status --short` 8경로 · `wc -l` 표 · 사이드이펙트 감사 결과(빈 diff 명령 3개) · "커밋 없음. 다음(Advisor): `/codex:review --scope working-tree` → 사용자 승인 → 로컬 커밋 1개, push 없음(CONTEXT D-14); 브라우저 확인 `make serve` → `http://localhost:8000/?source=api`(구 데모 + fallback 배너 = 정상)" 안내.
</output>
