---
phase: 01-local-serving-skeleton
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schema.sql
  - src/millie_rec/serving/db.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/api.py
  - src/millie_rec/serving/__init__.py
  - tests/serving/test_smoke.py
autonomous: true
requirements: [SKEL-01, SKEL-03, SKEL-04]
lane: Serving (Worker) + contracts 상수 1줄
no_commit: true   # 실행자는 git commit 을 하지 않는다 — ../CLAUDE.md §6 "커밋/푸시는 항상 사용자 승인 후", 사용자 지시 2026-09-05 "아직 커밋 진행하지 않습니다"
tdd_gate_evidence: pytest-output   # RED/GREEN 증거는 커밋이 아니라 `uv run pytest` 출력(SUMMARY.md 에 붙임)

must_haves:
  truths:
    - "파이프라인·카탈로그·아티팩트가 없어도 create_app(pipelines={}, fallback=GlobalPopularFallback(None), db=Database(tmp)) 로 만든 앱의 GET /health 가 200 과 HealthOut(status=ok, db_ok=true, model_version=null) 을 준다"
    - "GET /api/recommend?seeds=1,2,3 이 200 과 RecommendOut(fallback_level=3, model_version=fallback_v1, rows=[trending 1행, items 빈 배열], recommendation_id=rec_<6hex>) 을 준다"
    - "fallback Pipeline 이 예외를 던져도 /api/recommend 는 500 이 아니라 200 level 3 이다 (추천 API 장애 ≠ 메인 장애)"
    - "잘못된 seeds(정수 아님)·model(VARIANTS 밖)·k(1..100 밖) 는 422 다"
    - "Database(path).apply_schema() 후 Must 4테이블(users·preference_snapshots·events·recommendations) 이 존재하고 /health.db_row_count 에 그 4키가 0 으로 들어 있다"
    - "DATA_DIR 환경변수가 있으면 $DATA_DIR/millie.db, 없으면 contracts.DIR_DATA_LOCAL/millie.db 로 경로가 해석된다(해석은 db.py 만)"
  artifacts:
    - path: "src/millie_rec/contracts.py"
      provides: "level 3 응답의 model_version 상수"
      contains: "MODEL_VERSION_FALLBACK = \"fallback_v1\""
    - path: "src/millie_rec/serving/schema.sql"
      provides: "SQLite DDL Must 4 + Should 3, CREATE TABLE IF NOT EXISTS"
      contains: "CREATE TABLE IF NOT EXISTS recommendations"
    - path: "src/millie_rec/serving/db.py"
      provides: "Database(thread-local 연결·PRAGMA·apply_schema·table_names·row_counts·ok) + resolve_db_path"
      exports: ["Database", "resolve_db_path"]
      max_lines: 150
    - path: "src/millie_rec/serving/fallback.py"
      provides: "contracts.Pipeline 구현 GlobalPopularFallback + trending_row"
      exports: ["GlobalPopularFallback", "trending_row"]
      max_lines: 150
    - path: "src/millie_rec/serving/api.py"
      provides: "create_app · lifespan(apply_schema) · GET /health · GET /api/recommend(level 3)"
      exports: ["create_app"]
      max_lines: 150
    - path: "src/millie_rec/serving/__init__.py"
      provides: "공개 표면 __all__ 에 create_app·Database·GlobalPopularFallback·resolve_db_path"
      contains: "\"create_app\""
    - path: "tests/serving/test_smoke.py"
      provides: "계약·정확성·안전성 3종 스모크 테스트(10건)"
      min_lines: 60
  key_links:
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/db.py"
      via: "lifespan 에서 db.apply_schema(), /health 에서 db.ok()·db.row_counts()"
      pattern: "db\\.apply_schema\\(\\)"
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/fallback.py"
      via: "fallback.recommend(user, k) 를 try/except 로 감싸 trending_row 로 Row 구성"
      pattern: "trending_row\\("
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/schemas.py"
      via: "RecommendOut.from_contract(resp, forced=...) 로만 직렬화(로직 중복 금지)"
      pattern: "RecommendOut\\.from_contract"
    - from: "src/millie_rec/serving/db.py"
      to: "src/millie_rec/contracts.py"
      via: "os.environ.get(ENV_DATA_DIR) → DIR_DATA_LOCAL / DB_FILENAME"
      pattern: "os\\.environ\\.get\\(ENV_DATA_DIR\\)"
---

<objective>
Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md "Phase 1: 로컬 서빙 스켈레톤")의 Serving 레인 몫을 TDD 한 사이클(RED → GREEN → REFACTOR)로 만든다: `serving/schema.sql`·`db.py`·`fallback.py`·`api.py` 4개 파일과 `tests/serving/test_smoke.py`. 파이프라인·카탈로그·아티팩트·네트워크가 전혀 없는 상태에서 `create_app(...)` 이 `GET /health` 200 과 `GET /api/recommend` 200(`fallback_level=3`) 을 주고, SQLite 스키마(Must 4 + Should 3)가 `tmp_path` DB 에 적용됨을 테스트로 고정한다.

Purpose: 이후 모든 페이즈가 "떠 있는 서버에 붙어" 검증되는 walking skeleton(`../.claude/rules/local-run.md`)의 코드 본체. "추천 API 장애 ≠ 메인 장애"(수용 기준 '파이프라인 예외 → 인기 row 200'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표))의 최소 코드 증거를 테스트 이름으로 남긴다(PDF P5 문장 근거).
Output: 위 5개 신규 파일 + `contracts.py` 상수 1줄 + `serving/__init__.py` `__all__` 확장. 커밋 없음(작업 트리에 남김).

한 기능(feature) = **서빙 스켈레톤 수직 슬라이스**: `create_app` 이 SQLite 스키마 위에서 `/health` 와 level 3 `/api/recommend` 를 낸다. 테스트 파일 1개(`test_smoke.py`)가 이 기능의 행동 전부를 정의한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-serving-skeleton/01-CONTEXT.md
@.planning/phases/01-local-serving-skeleton/01-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/__init__.py
@tests/serving/test_schemas.py
@tests/test_architecture.py
@../.claude/rules/serving.md
@../.claude/rules/python-tdd.md
@../.claude/rules/architecture.md
@../.claude/rules/simplicity.md
@../.claude/rules/python.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 테스트는 오직 `uv run pytest <대상> --no-header`. `-q` 는 pyproject `addopts` 에 이미 있다 — CLI 에 `-q` 를 붙이지 않는다(`-qq` 가 되어 `N passed` 요약 줄이 사라짐, 2026-09-05 실측). 맨 `pytest`·`python -m pytest`·`python3` 금지(이 머신의 맨 pytest 는 Python 3.9 → collection error). **RED 는 출력에 `AssertionError` 가 있을 때만 인정.** `ImportError`·`ModuleNotFoundError`·`SyntaxError`·collection error 는 RED 가 아니다. `pytest.skip` 금지. `tests/test_architecture.py`·기존 테스트 수정 금지.

**star 의존(`../.claude/rules/architecture.md`, `tests/test_architecture.py`):** `serving/*.py` 는 `millie_rec.contracts` 와 `millie_rec.serving.<module>` 만 import. 다른 슬라이스·`millie_rec.app` import 금지. `api.py` 는 `millie_rec.serving`(패키지) 가 아니라 `millie_rec.serving.db`·`.fallback`·`.schemas` **모듈 경로**로 import 한다(`__init__` 이 `api` 를 import 하므로 패키지 import 는 순환).

**간결함(`../.claude/rules/simplicity.md`, 사용자 제약 3·4):** 파일 ≤150줄(예산: `schema.sql` ≈45~60 · `db.py` ≈70 · `api.py` ≈90~110 · `fallback.py` ≈60). 1줄 한국어 docstring + 설계서 § 참조. 설정은 모듈 상단 대문자 상수. 주석은 "왜"만. 로깅은 `logging.getLogger(__name__)`. 예외 계층·데코레이터·레지스트리·베이스 클래스 금지. 클래스는 Protocol 만족(`GlobalPopularFallback`) 또는 주입 대상(`Database`)일 때만. 이름은 `contracts.py` 상수만(`FALLBACK_GLOBAL_POP`·`MODEL_VERSION_FALLBACK`·`VARIANTS`·`ENV_DATA_DIR`·`DIR_DATA_LOCAL`·`DB_FILENAME`). 요청받지 않은 리팩토링·기존 코드 변경 금지. Phase 1 은 **DB 에 행을 쓰지 않는다**(CONTEXT D-06), 인덱스 없음, fallback level 1·2 없음(Phase 5), CORS 없음.

**변경 금지 파일(사용자 제약 1):** `demo/**`, `src/millie_rec/serving/schemas.py`, `schemas_should.py`, 다른 슬라이스(`data`·`retrieval`·`ranking`·`reranking`·`evaluation`), `tests/test_architecture.py`, `tests/serving/test_schemas.py`, `Makefile`, `pyproject.toml`, `Dockerfile`. 의존성 추가 없음(`fastapi`·`uvicorn`·dev `httpx` 이미 있음).

**커밋 금지:** 이 플랜의 실행자는 `git add`·`git commit` 을 실행하지 않는다. GSD 기본 원자 커밋과 tdd.md 의 `test(...)`/`feat(...)` 게이트 커밋을 **모두 생략**하고, 대신 RED·GREEN 각각의 `uv run pytest` 출력(요약 줄 + AssertionError 1건 발췌)을 SUMMARY.md `## TDD Gate Compliance` 절에 붙인다. 커밋은 사용자 승인 후 Advisor 가 한다(`contracts.py` 변경이므로 그 전에 `/codex:review --scope working-tree` 필수 — `../.claude/rules/codex-review.md` 표 1행; 실행자 몫 아님).
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 재탐색 불필요. -->

From src/millie_rec/contracts.py (변경 금지 부분):
```python
VARIANTS = ("pop", "cf", "hybrid", "hybrid_div")
MODEL_VERSION_SUFFIX = "_v1"  # ← 이 줄 바로 아래에 MODEL_VERSION_FALLBACK 을 추가한다(Task 1)
ROW_IDS = ("after_completion", "continue_reading", "persona_shelf", "similar_readers", "trending", "fresh_picks", "light_start")
ROW_PURPOSES = ("resume", "discover", "fallback", "explore")
BUDGET_MS = 200
FALLBACK_PERSONALIZED, FALLBACK_CACHE, FALLBACK_SEGMENT_POP, FALLBACK_GLOBAL_POP = 0, 1, 2, 3
DIR_DATA_LOCAL = ROOT / "data" / "local"
ENV_DATA_DIR = "DATA_DIR"
DB_FILENAME = "millie.db"

@dataclass(frozen=True, slots=True)
class UserState:
    user_id: int | None
    explicit_seeds: tuple[int, ...] = ()
    history: tuple[int, ...] = ()
    session: tuple[int, ...] = ()
    context: dict[str, str] = field(default_factory=dict)
    @property
    def seen(self) -> frozenset[int]: ...   # explicit_seeds ∪ history ∪ session

@dataclass(frozen=True, slots=True)
class ScoredItem:
    book_id: int; score: float; source: str | None = None; reason: str | None = None
    title: str | None = None; authors: str | None = None; image_url: str | None = None
    position: int | None = None; source_channels: tuple[str, ...] = ()
    badge: Badge | None = None; book_format: str | None = None; difficulty: float | None = None

@dataclass(frozen=True, slots=True)
class Row:
    row_id: str; title: str; purpose: str
    items: tuple[ScoredItem, ...] = (); subtitle: str | None = None
    channel_mix: dict[str, int] = field(default_factory=dict)

@dataclass(frozen=True, slots=True)
class RecommendResponse:
    items: tuple[Recommendation, ...]; model_version: str; fallback_level: int; latency_ms: float
    recommendation_id: str | None = None; preference_snapshot_id: str | None = None
    rows: tuple[Row, ...] = (); latency_breakdown: dict[str, float] = field(default_factory=dict)
    user_state_weights: dict[str, float] = field(default_factory=dict)
    user_key: str | None = None; cell: str | None = None; dedup_removed: int = 0; context: str | None = None

class Catalog(Protocol):
    def meta(self, book_ids: Sequence[int]) -> list[dict]: ...
    def popular(self, categories: Sequence[str] = (), n: int = 50) -> list[int]: ...
    def eligible(self, book_ids: Sequence[int]) -> list[int]: ...

class Neighbors(Protocol):
    def neighbors(self, book_id: int, n: int = 20) -> list[tuple[int, float]]: ...

class BookStatsSource(Protocol):
    def stats(self, book_ids: Sequence[int]) -> list[BookStats]: ...
    def user_level(self, user: UserState) -> float | None: ...

class Pipeline(Protocol):
    name: str
    def recommend(self, user: UserState, k: int) -> list[ScoredItem]: ...
```

From src/millie_rec/serving/schemas.py (변경 금지, 호출만):
```python
API_VERSION = "v2"

class HealthOut(_Strict):          # extra="forbid"
    status: str
    api_version: str = API_VERSION
    model_version: str | None = None
    artifacts_loaded_at: str | None = None
    db_ok: bool
    db_row_count: dict[str, int] = {}
    nearline_last_run: str | None = None
    uptime_s: float

class RecommendOut(_Strict):
    ...  # recommendation_id, model_version, fallback_level(0..3), latency_ms, rows, items, forced, context, user_state_weights, latency_breakdown ...
    @classmethod
    def from_contract(cls, r: RecommendResponse, *, forced: bool = False) -> "RecommendOut": ...
```
`row_id="trending"` ∈ `ROW_IDS`, `purpose="fallback"` ∈ `ROW_PURPOSES` 이므로 `RowOut` 검증기를 통과한다.
</interfaces>
</context>

<feature>
  <name>서빙 스켈레톤 — create_app 이 SQLite 스키마 위에서 /health 와 level 3 /api/recommend 를 낸다</name>
  <files>
    src/millie_rec/contracts.py (상수 1줄 추가), src/millie_rec/serving/schema.sql, src/millie_rec/serving/db.py,
    src/millie_rec/serving/fallback.py, src/millie_rec/serving/api.py, src/millie_rec/serving/__init__.py,
    tests/serving/test_smoke.py
  </files>
  <behavior>
    앱 = `create_app(pipelines={}, fallback=GlobalPopularFallback(None), db=Database(tmp_path / "millie.db"))`, `with TestClient(app) as c:` (lifespan 이 돌아야 스키마가 적용된다).
    - `GET /health` → 200; `HealthOut.model_validate(json)` 성공; `status == "ok"`, `db_ok is True`, `model_version is None`, `artifacts_loaded_at is None`; `db_row_count` 에 `users`·`preference_snapshots`·`events`·`recommendations` 키가 각각 0.
    - `Database(tmp/"millie.db").apply_schema()` → `table_names()` ⊇ Must 4.
    - `monkeypatch.setenv("DATA_DIR", tmp)` → `resolve_db_path() == tmp / "millie.db"`; `delenv` → `== DIR_DATA_LOCAL / DB_FILENAME`.
    - `GET /api/recommend?seeds=1,2,3&k=5` → 200; `RecommendOut.model_validate(json)` 성공; `fallback_level == 3`, `model_version == "fallback_v1"`, `forced is False`, `items == []`, `rows` 길이 1, `rows[0].row_id == "trending"`, `rows[0].title == "지금 많이 읽는 책"`, `rows[0].purpose == "fallback"`, `rows[0].items == []`, `recommendation_id` 가 `rec_` + 6자, `user_state_weights == {"alpha":0.0,"beta":0.0,"gamma":0.0}`, `"total" in latency_breakdown`.
    - `GET /api/recommend?seeds=1,x` → 422 · `?model=foo` → 422 · `?k=101` → 422 · `?k=0` → 422.
    - `GET /api/recommend?model=pop&seeds=1` → 200, `forced is True`(파이프라인 미주입이라 여전히 level 3).
    - fallback 에 `recommend` 가 항상 `RuntimeError` 를 던지는 가짜 Pipeline 을 주입 → 200, `fallback_level == 3`, `rows[0].items == []`.
    - `GlobalPopularFallback(catalog)` 에 `popular(n) → [1,2,3,4,5]` 인 가짜 Catalog 주입, `UserState(user_id=None, explicit_seeds=(1,))`, k=10 → `[b.book_id for b in items] == [2,3,4,5]`, 전 항목 `source == "popularity"`, `source_channels == ("popularity",)`, `position` 이 0,1,2,3.
  </behavior>
  <implementation>Task 2 의 action 에 4개 파일의 코드 전문이 있다. 그대로 쓴다.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): contracts 상수 + 인터페이스 스텁 4파일 + test_smoke.py 작성 → 전 테스트 AssertionError 확인</name>
  <files>src/millie_rec/contracts.py, src/millie_rec/serving/db.py, src/millie_rec/serving/fallback.py, src/millie_rec/serving/api.py, src/millie_rec/serving/schema.sql, tests/serving/test_smoke.py</files>
  <read_first>
    - .planning/phases/01-local-serving-skeleton/01-CONTEXT.md (D-01~D-13 전부, 특히 D-01 상수·D-02 rows·D-03 rec_id·D-04 wire 값·D-08 경로·D-09 시그니처·D-12/D-13 테스트 형태)
    - .planning/phases/01-local-serving-skeleton/01-PATTERNS.md "tests/serving/test_smoke.py" 절 (테스트 골격·lifespan 함정·`with TestClient`)
    - src/millie_rec/contracts.py L60-62 (`VARIANTS`·`MODEL_VERSION_SUFFIX` — 상수 추가 위치), L84-97, L121-134 `UserState`, L217-269 DTO, L315-345 Protocol
    - src/millie_rec/serving/schemas.py L56-64 `HealthOut`, L164-187 `RecommendOut`
    - tests/serving/test_schemas.py (같은 디렉터리의 import·단정 관례)
    - tests/test_architecture.py L27-43 (import 위반 3종 — 스텁도 이 규칙을 지킨다)
    - ../.claude/rules/python-tdd.md (RED = AssertionError 만)
  </read_first>
  <action>
    **왜 스텁인가:** 모듈이 없으면 테스트가 `ModuleNotFoundError` 로 죽어 RED 가 아니다(`python-tdd.md`). 시그니처만 있고 값은 틀린 스텁을 먼저 두어 **모든 테스트가 `AssertionError` 로 실패**하는 정직한 RED 를 만든다(PATTERNS.md "정직한 Red"). 스텁 본문은 빈 값 반환만 — 로직 0.

    **1-a. `src/millie_rec/contracts.py` — 상수 1줄 추가(CONTEXT D-01).** L62 `MODEL_VERSION_SUFFIX = "_v1"  # ...` 바로 아래에 다음 한 줄을 추가한다. 다른 줄은 건드리지 않는다(삭제·개명 금지, 함수 정의 금지 — `tests/test_architecture.py::test_contracts_has_no_logic`).
    ```python
    MODEL_VERSION_FALLBACK = "fallback_v1"  # level 3 응답의 model_version. VARIANTS 이름을 빌리지 않는다(숫자 불혼합)
    ```

    **1-b. `src/millie_rec/serving/schema.sql` — 빈 파일이 아닌 주석 1줄만** (`-- serving/schema.sql — SQLite DDL (아키텍처 01 §3-4 데이터 저장). Task 2 에서 채운다.`). executescript 가 빈 스크립트도 허용하므로 스텁 `apply_schema` 는 아무 테이블도 만들지 않는다 → 테이블 단정이 AssertionError 로 실패한다.

    **1-c. `src/millie_rec/serving/db.py` 스텁:**
    ```python
    """SQLite 연결: thread-local · PRAGMA · schema.sql 적용 · 경로 해석 (아키텍처 01 §3-4)."""

    import sqlite3
    from pathlib import Path


    def resolve_db_path() -> Path:
        """$DATA_DIR/millie.db 가 있으면 그것, 없으면 contracts.DIR_DATA_LOCAL / millie.db."""
        return Path()


    class Database:
        """threading.local 연결 보유. 핸들러는 스레드풀에서 돌므로 스레드마다 연결 1개."""

        def __init__(self, path: Path) -> None:
            self.path = path

        def connect(self) -> sqlite3.Connection:
            raise NotImplementedError

        def apply_schema(self) -> None:
            return None

        def table_names(self) -> list[str]:
            return []

        def row_counts(self) -> dict[str, int]:
            return {}

        def ok(self) -> bool:
            return False
    ```

    **1-d. `src/millie_rec/serving/fallback.py` 스텁:**
    ```python
    """fallback cascade — Phase 1 은 level 3 만. level 1·2 는 Phase 5 증분(아키텍처 01 §3-11)."""

    from collections.abc import Sequence

    from millie_rec.contracts import Catalog, Row, ScoredItem, UserState

    TRENDING_ROW_ID = "trending"
    TRENDING_TITLE = "지금 많이 읽는 책"
    TRENDING_PURPOSE = "fallback"
    SOURCE_POPULARITY = "popularity"


    class GlobalPopularFallback:
        """contracts.Pipeline 구현. catalog 없으면 빈 목록(level 3, 스켈레톤)."""

        name = "fallback"

        def __init__(self, catalog: Catalog | None) -> None:
            self.catalog = catalog

        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            return []


    def trending_row(items: Sequence[ScoredItem]) -> Row:
        """D-02: level 3 응답의 유일한 행. Must 5행 뼈대는 Phase 5 compose.py."""
        return Row(row_id=TRENDING_ROW_ID, title=TRENDING_TITLE, purpose=TRENDING_PURPOSE)
    ```

    **1-e. `src/millie_rec/serving/api.py` 스텁** — 시그니처는 CONTEXT D-09(`../.claude/rules/serving.md` "구조" 절)의 인자 이름·순서 그대로, 단 `fallback` 뒤는 keyword-only(`*`)이고 `db` 는 필수(아키텍트 리뷰 W-1 채택). 라우트 0개:
    ```python
    """FastAPI create_app · lifespan · GET /health · GET /api/recommend (백엔드 서빙 01 §0·§1·§5)."""

    from fastapi import FastAPI

    from millie_rec.contracts import BookStatsSource, Catalog, Neighbors, Pipeline
    from millie_rec.serving.db import Database


    def create_app(
        pipelines: dict[str, Pipeline],
        fallback: Pipeline,
        *,
        catalog: Catalog | None = None,
        db: Database,
        neighbors: Neighbors | None = None,
        book_stats: BookStatsSource | None = None,
        state: object | None = None,
    ) -> FastAPI:
        """주입만 받는다. 비즈니스 규칙은 슬라이스 함수에."""
        return FastAPI()
    ```

    **1-f. `tests/serving/test_smoke.py` — 아래 10개 테스트를 그대로 작성한다.** 모든 HTTP 테스트는 **첫 단정이 `status_code`** 여야 한다(404 JSON 에 키가 없어 `KeyError` 가 먼저 나면 RED 가 아니다).
    ```python
    """서빙 스켈레톤 스모크 — 계약(스키마 파싱) · 정확성(level 3·Must 4테이블) · 안전성(예외→200·422).

    Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md). serving 레인 단독 조립(CONTEXT D-13).
    app/ 은 import 하지 않는다.
    """

    from collections.abc import Sequence
    from pathlib import Path

    import pytest
    from fastapi.testclient import TestClient

    from millie_rec.contracts import (
        DB_FILENAME,
        DIR_DATA_LOCAL,
        ENV_DATA_DIR,
        FALLBACK_GLOBAL_POP,
        MODEL_VERSION_FALLBACK,
        ScoredItem,
        UserState,
    )
    from millie_rec.serving.api import create_app
    from millie_rec.serving.db import Database, resolve_db_path
    from millie_rec.serving.fallback import GlobalPopularFallback
    from millie_rec.serving.schemas import HealthOut, RecommendOut

    MUST_TABLES = ("users", "preference_snapshots", "events", "recommendations")
    ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}


    class _Boom:
        """recommend 가 항상 예외 — '추천 API 장애 ≠ 메인 장애' 증거용 가짜 Pipeline."""

        name = "boom"

        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            raise RuntimeError("boom")


    class _FakeCatalog:
        """popular() 만 쓰는 가짜 Catalog. meta·eligible 은 Phase 1 fallback 이 호출하지 않는다."""

        def meta(self, book_ids: Sequence[int]) -> list[dict]:
            return []

        def popular(self, categories: Sequence[str] = (), n: int = 50) -> list[int]:
            return [1, 2, 3, 4, 5][:n]

        def eligible(self, book_ids: Sequence[int]) -> list[int]:
            return list(book_ids)


    def _app(tmp_path: Path, fallback=None):
        return create_app(
            pipelines={},
            fallback=fallback or GlobalPopularFallback(None),
            db=Database(tmp_path / DB_FILENAME),
        )


    @pytest.fixture
    def client(tmp_path: Path):
        with TestClient(_app(tmp_path)) as c:  # with 블록이어야 lifespan(스키마 적용)이 돈다
            yield c


    # ── 계약 ────────────────────────────────────────────────────────────────
    def test_health_returns_200_and_parses_health_out(client):
        r = client.get("/health")
        assert r.status_code == 200
        out = HealthOut.model_validate(r.json())
        assert out.status == "ok" and out.db_ok is True
        assert out.model_version is None and out.artifacts_loaded_at is None


    def test_recommend_without_pipeline_parses_recommend_out_level3(client):
        r = client.get("/api/recommend", params={"seeds": "1,2,3", "k": 5})
        assert r.status_code == 200
        out = RecommendOut.model_validate(r.json())
        assert out.fallback_level == FALLBACK_GLOBAL_POP
        assert out.model_version == MODEL_VERSION_FALLBACK
        assert out.forced is False and out.items == []


    # ── 정확성 ──────────────────────────────────────────────────────────────
    def test_recommend_level3_has_single_trending_row_and_rec_id(client):
        r = client.get("/api/recommend", params={"seeds": "1,2,3"})
        assert r.status_code == 200
        d = r.json()
        assert len(d["rows"]) == 1
        row = d["rows"][0]
        assert row["row_id"] == "trending" and row["title"] == "지금 많이 읽는 책"
        assert row["purpose"] == "fallback" and row["items"] == []
        assert d["recommendation_id"].startswith("rec_") and len(d["recommendation_id"]) == 10
        assert d["user_state_weights"] == ZERO_WEIGHTS and "total" in d["latency_breakdown"]


    def test_recommend_with_model_query_sets_forced_true(client):
        r = client.get("/api/recommend", params={"model": "pop", "seeds": "1"})
        assert r.status_code == 200
        assert r.json()["forced"] is True and r.json()["fallback_level"] == FALLBACK_GLOBAL_POP


    def test_health_db_row_count_has_must_tables_at_zero(client):
        r = client.get("/health")
        assert r.status_code == 200
        counts = r.json()["db_row_count"]
        assert all(counts.get(t) == 0 for t in MUST_TABLES), counts


    def test_database_apply_schema_creates_must_tables(tmp_path: Path):
        db = Database(tmp_path / DB_FILENAME)
        db.apply_schema()
        assert set(MUST_TABLES) <= set(db.table_names())
        assert db.ok() is True


    def test_resolve_db_path_prefers_env_then_local_default(tmp_path: Path, monkeypatch):
        monkeypatch.setenv(ENV_DATA_DIR, str(tmp_path))
        assert resolve_db_path() == tmp_path / DB_FILENAME
        monkeypatch.delenv(ENV_DATA_DIR)
        assert resolve_db_path() == DIR_DATA_LOCAL / DB_FILENAME


    def test_fallback_with_catalog_excludes_seeds_and_marks_popularity():
        items = GlobalPopularFallback(_FakeCatalog()).recommend(
            UserState(user_id=None, explicit_seeds=(1,)), k=10
        )
        assert [i.book_id for i in items] == [2, 3, 4, 5]
        assert all(i.source == "popularity" and i.source_channels == ("popularity",) for i in items)
        assert [i.position for i in items] == [0, 1, 2, 3]


    # ── 안전성 ──────────────────────────────────────────────────────────────
    def test_recommend_invalid_query_returns_422(client):
        assert client.get("/api/recommend", params={"seeds": "1,x"}).status_code == 422
        assert client.get("/api/recommend", params={"model": "foo"}).status_code == 422
        assert client.get("/api/recommend", params={"k": 101}).status_code == 422
        assert client.get("/api/recommend", params={"k": 0}).status_code == 422


    def test_recommend_when_fallback_raises_still_200_level3(tmp_path: Path):
        with TestClient(_app(tmp_path, fallback=_Boom())) as c:
            r = c.get("/api/recommend", params={"seeds": "1"})
        assert r.status_code == 200
        assert r.json()["fallback_level"] == FALLBACK_GLOBAL_POP and r.json()["rows"][0]["items"] == []
    ```

    **1-g. RED 확인.** `cd` 없이 절대 경로에서 실행:
    `uv run python --version` (3.11.x 확인) → `uv run pytest tests/serving/test_smoke.py --no-header 2>&1 | tail -40`.
    기대: **10 failed**, 실패 사유가 전부 **단정 실패**(스텁이 404·빈 목록·`Path()`·`False` 를 돌려주므로). pytest 의 assertion rewriting 때문에 출력은 `AssertionError` 문자열 또는 `E assert 404 == 200` 같은 재작성 표현으로 나온다 — **둘 다 RED 다**(실측: 10건 중 3건만 `AssertionError` 문자열, 6건은 `E assert …`). `ImportError`/`ModuleNotFoundError`/`NotImplementedError`/`KeyError`/collection error 가 보이면 스텁 또는 테스트 순서를 고친다(테스트의 단정 의도는 바꾸지 않는다). **통과하는 테스트가 1개라도 있으면 중단**하고 원인을 SUMMARY 에 적는다(tdd.md fail-fast "Unexpected GREEN").
    또한 `uv run pytest tests/test_architecture.py --no-header` 가 스텁 상태에서도 **통과**해야 한다(import 규칙 위반 없음).
    출력의 요약 줄(`10 failed in …s`)과 AssertionError 1건을 SUMMARY 용으로 보관한다. **커밋하지 않는다.**
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_smoke.py --no-header 2>&1 | grep -E "AssertionError|FAILED|passed|failed" | head -20; uv run pytest tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'MODEL_VERSION_FALLBACK = "fallback_v1"' src/millie_rec/contracts.py` 가 1행 출력(L63 근처), `git diff --stat src/millie_rec/contracts.py` 가 `1 +` (추가 1줄만)
    - `uv run pytest tests/serving/test_smoke.py --no-header` 출력에 `10 failed` 와 `AssertionError` 가 있고, `passed` 수가 0, `ImportError`·`ModuleNotFoundError`·`NotImplementedError`·`KeyError` 가 없다
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - 파일 존재: `src/millie_rec/serving/{schema.sql,db.py,fallback.py,api.py}`, `tests/serving/test_smoke.py`; `grep -c "def test_" tests/serving/test_smoke.py` == 10
    - `grep -n "from millie_rec\." src/millie_rec/serving/api.py src/millie_rec/serving/db.py src/millie_rec/serving/fallback.py` 의 대상이 `millie_rec.contracts` 또는 `millie_rec.serving.<module>` 만
  </acceptance_criteria>
  <done>10개 테스트가 전부 단정 실패(`AssertionError` 또는 `E assert …`)로 끝나는 RED 상태. 아키텍처 테스트 통과. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): schema.sql DDL · db.py · fallback.py · api.py 구현 → test_smoke.py 10건 통과</name>
  <files>src/millie_rec/serving/schema.sql, src/millie_rec/serving/db.py, src/millie_rec/serving/fallback.py, src/millie_rec/serving/api.py</files>
  <read_first>
    - .planning/phases/01-local-serving-skeleton/01-PATTERNS.md 의 "schema.sql"·"db.py"·"fallback.py"·"api.py" 절 (골격 코드·lifespan 함정·`Annotated[..., Query()]` 로 ruff B008 회피)
    - .planning/phases/01-local-serving-skeleton/01-CONTEXT.md D-02(rows)·D-03(rec_id)·D-04(wire 값)·D-05(DDL 7테이블)·D-06(쓰기 금지)·D-07(db_row_count 전 테이블)·D-08(PRAGMA·경로)·D-10(GlobalPopularFallback)
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md §3-4 데이터 저장(DDL 원문)·§3-11 fallback cascade·§9-3 serving 파일 분할
    - ../.assets/설계서/백엔드 서빙/01_백엔드_서버_구성.md §0 공통 규약(ID `rec_<6hex>`·422·`k ≤ 100`)·§1 `/health`·§5 `/api/recommend`
    - src/millie_rec/serving/schemas.py L56-64·L164-187 (호출 대상 시그니처)
    - Task 1 에서 만든 tests/serving/test_smoke.py (통과시킬 단정)
  </read_first>
  <action>
    통과시키는 **최소 코드**만. 미래 요구(level 1·2, 행 쓰기, `backup()`, 인덱스, 5행 compose, level 0 분기 본문) 선제 구현 금지. 아래 코드를 그대로 쓴다.

    **2-a. `src/millie_rec/serving/schema.sql`** — 아키텍처 01 §3-4 데이터 저장 DDL 을 `CREATE TABLE IF NOT EXISTS` 로 옮긴다(CONTEXT D-05: Must 4 + Should 3 전부). 타입 규칙: 설계서 표기 `INT` → INTEGER, `REAL` → REAL, `JSON` → TEXT(주석 `-- JSON`), 무표기 → TEXT, 단 contracts DTO 에서 `int` 인 `book_id`·`position`·`stars`·`fallback_level`·`impressions`·`reader_opens`·`qualified_reads`·`completions`·`n_events` → INTEGER. **제약은 PRIMARY KEY 만**(NOT NULL·DEFAULT 를 넣지 않는다 — 익명 level 3 추천 로그의 `user_key` null 등 Phase 5 쓰기 규칙을 여기서 선결하지 않기 위함). 인덱스 없음.
    ```sql
    -- serving/schema.sql — SQLite DDL (아키텍처 01 §3-4 데이터 저장). CREATE TABLE IF NOT EXISTS 만. 인덱스는 Phase 5.
    -- 컬럼 이름은 §3-4 표기 그대로(contracts.Event · Rating · BookStats 필드명과 일치). JSON 컬럼은 TEXT.

    -- Must 4
    CREATE TABLE IF NOT EXISTS users (
        user_key TEXT PRIMARY KEY, created_at TEXT, consent INTEGER, cell TEXT, is_new INTEGER
    );
    CREATE TABLE IF NOT EXISTS preference_snapshots (
        snapshot_id TEXT PRIMARY KEY, user_key TEXT, created_at TEXT, reading_time TEXT,
        categories TEXT, criterion TEXT, subcategories TEXT, seeds TEXT, persona TEXT  -- JSON: categories subcategories seeds persona
    );
    CREATE TABLE IF NOT EXISTS events (
        event_id TEXT PRIMARY KEY, user_key TEXT, book_id INTEGER, event_type TEXT, ts TEXT,
        surface TEXT, row_id TEXT, position INTEGER, selected INTEGER,
        recommendation_id TEXT, model_version TEXT, preference_snapshot_id TEXT, candidate_set_id TEXT,
        payload TEXT, quality_flag TEXT  -- JSON: payload
    );
    CREATE TABLE IF NOT EXISTS recommendations (
        recommendation_id TEXT PRIMARY KEY, user_key TEXT, snapshot_id TEXT, model_version TEXT, cell TEXT,
        forced INTEGER, fallback_level INTEGER, latency_total_ms REAL,
        latency_breakdown TEXT, weights TEXT, rows TEXT, ts TEXT  -- JSON: latency_breakdown weights rows
    );

    -- Should 3
    CREATE TABLE IF NOT EXISTS ratings (
        rating_id TEXT PRIMARY KEY, user_key TEXT, book_id INTEGER, stars INTEGER, ts TEXT, recommendation_id TEXT
    );
    CREATE TABLE IF NOT EXISTS candidate_sets (
        candidate_set_id TEXT PRIMARY KEY, user_key TEXT, ts TEXT, book_ids TEXT, survey_variant TEXT  -- JSON: book_ids
    );
    CREATE TABLE IF NOT EXISTS book_stats (
        book_id INTEGER PRIMARY KEY, impressions INTEGER, reader_opens INTEGER, qualified_reads INTEGER,
        completions INTEGER, rating_mean REAL, rating_var REAL, difficulty REAL, n_events INTEGER,
        source TEXT, updated_at TEXT
    );
    ```

    **2-b. `src/millie_rec/serving/db.py`** (CONTEXT D-08; 환경변수 해석·`mkdir` 은 이 파일만):
    ```python
    """SQLite 연결: thread-local · PRAGMA · schema.sql 적용 · 경로 해석 (아키텍처 01 §3-4)."""

    import os
    import sqlite3
    import threading
    from pathlib import Path

    from millie_rec.contracts import DB_FILENAME, DIR_DATA_LOCAL, ENV_DATA_DIR

    SCHEMA_PATH = Path(__file__).parent / "schema.sql"
    # journal_mode 만 파일에 남고 나머지 둘은 연결 단위 — 연결마다 3개 전부 실행한다
    PRAGMAS = ("PRAGMA journal_mode=WAL", "PRAGMA busy_timeout=5000", "PRAGMA synchronous=NORMAL")
    SQL_TABLES = (
        "SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%' ORDER BY name"
    )


    def resolve_db_path() -> Path:
        """$DATA_DIR/millie.db 가 있으면 그것, 없으면 contracts.DIR_DATA_LOCAL / millie.db."""
        env = os.environ.get(ENV_DATA_DIR)
        return (Path(env) if env else DIR_DATA_LOCAL) / DB_FILENAME


    class Database:
        """threading.local 연결 보유. 핸들러는 스레드풀에서 돌므로 스레드마다 연결 1개."""

        def __init__(self, path: Path) -> None:
            self.path = path
            self._local = threading.local()
            self.path.parent.mkdir(parents=True, exist_ok=True)

        def connect(self) -> sqlite3.Connection:
            con = getattr(self._local, "con", None)
            if con is None:
                con = sqlite3.connect(self.path)
                for pragma in PRAGMAS:
                    con.execute(pragma)
                self._local.con = con
            return con

        def apply_schema(self) -> None:
            """schema.sql 을 executescript 로 적용(IF NOT EXISTS → 멱등). 행은 쓰지 않는다(D-06)."""
            self.connect().executescript(SCHEMA_PATH.read_text(encoding="utf-8"))

        def table_names(self) -> list[str]:
            return [r[0] for r in self.connect().execute(SQL_TABLES).fetchall()]

        def row_counts(self) -> dict[str, int]:
            """D-07: 스키마의 모든 테이블 COUNT(*), 0 건도 키 포함."""
            con = self.connect()
            # 테이블 이름은 sqlite_master 에서 온 값만 — 사용자 입력이 아니라 f-string 이 안전하다
            return {
                t: con.execute(f"SELECT COUNT(*) FROM {t}").fetchone()[0] for t in self.table_names()
            }

        def ok(self) -> bool:
            try:
                return self.connect().execute("SELECT 1").fetchone()[0] == 1
            except sqlite3.Error:
                return False
    ```

    **2-c. `src/millie_rec/serving/fallback.py`** (CONTEXT D-02·D-10; 시드 제외는 `UserState.seen` 계약 사용):
    ```python
    """fallback cascade — Phase 1 은 level 3 만. level 1·2 는 Phase 5 증분(아키텍처 01 §3-11)."""

    from collections.abc import Sequence

    from millie_rec.contracts import Catalog, Row, ScoredItem, UserState

    TRENDING_ROW_ID = "trending"  # contracts.ROW_IDS 안
    TRENDING_TITLE = "지금 많이 읽는 책"  # 백엔드 01 §5 응답 예시. Phase 5 compose.py 가 재사용
    TRENDING_PURPOSE = "fallback"  # contracts.ROW_PURPOSES 안
    SOURCE_POPULARITY = "popularity"  # contracts.Candidate.source 허용값


    class GlobalPopularFallback:
        """contracts.Pipeline 구현. catalog 없으면 빈 목록(level 3, 스켈레톤)."""

        name = "fallback"

        def __init__(self, catalog: Catalog | None) -> None:
            self.catalog = catalog

        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            if self.catalog is None:
                return []
            ids = [b for b in self.catalog.popular(n=k + len(user.seen)) if b not in user.seen][:k]
            return [
                ScoredItem(
                    book_id=b,
                    score=float(len(ids) - i),
                    source=SOURCE_POPULARITY,
                    position=i,
                    source_channels=(SOURCE_POPULARITY,),
                )
                for i, b in enumerate(ids)
            ]


    def trending_row(items: Sequence[ScoredItem]) -> Row:
        """D-02: level 3 응답의 유일한 행. Must 5행 뼈대는 Phase 5 compose.py."""
        return Row(
            row_id=TRENDING_ROW_ID,
            title=TRENDING_TITLE,
            purpose=TRENDING_PURPOSE,
            items=tuple(items),
            channel_mix={SOURCE_POPULARITY: len(items)} if items else {},
        )
    ```

    **2-d. `src/millie_rec/serving/api.py`** (CONTEXT D-03·D-04·D-09·D-13; 백엔드 01 §0 422 규약·`k ≤ 100`; 핸들러는 전부 동기 `def`):
    ```python
    """FastAPI create_app · lifespan · GET /health · GET /api/recommend (백엔드 서빙 01 §0·§1·§5)."""

    import logging
    import uuid
    from collections.abc import AsyncIterator
    from contextlib import asynccontextmanager
    from time import perf_counter
    from typing import Annotated

    from fastapi import FastAPI, HTTPException, Query

    from millie_rec.contracts import (
        FALLBACK_GLOBAL_POP,
        MODEL_VERSION_FALLBACK,
        VARIANTS,
        BookStatsSource,
        Catalog,
        Neighbors,
        Pipeline,
        RecommendResponse,
        UserState,
    )
    from millie_rec.serving.db import Database
    from millie_rec.serving.fallback import trending_row
    from millie_rec.serving.schemas import API_VERSION, HealthOut, RecommendOut

    log = logging.getLogger(__name__)
    K_DEFAULT, K_MAX = 40, 100  # 백엔드 01 §5 "k 기본 40" · §0 "k ≤ 100"
    ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}  # §5 "익명은 3, 가중치 전부 0"


    def _new_rec_id() -> str:
        return "rec_" + uuid.uuid4().hex[:6]  # 백엔드 01 §0 ID 형식 rec_<6hex> (D-03)


    def _422(param: str, msg: str) -> HTTPException:
        return HTTPException(422, detail=[{"loc": ["query", param], "msg": msg, "type": "value_error"}])


    def _parse_seeds(raw: str | None) -> tuple[int, ...]:
        """'1,2,3' → (1, 2, 3). 정수가 아니면 FastAPI 규약대로 422."""
        if not raw:
            return ()
        try:
            return tuple(int(s) for s in raw.split(",") if s.strip())
        except ValueError as e:
            raise _422("seeds", "seeds must be comma-separated integers") from e


    def _fallback_response(
        fallback: Pipeline,
        user: UserState,
        k: int,
        t0: float,
        context: str | None,
        *,
        level: int = FALLBACK_GLOBAL_POP,
    ) -> RecommendResponse:
        """전역 인기 fallback. 예외가 나도 빈 trending 행으로 200 — 추천 API 장애 ≠ 메인 장애(D-13)."""
        try:
            items = fallback.recommend(user, k)
        except Exception:
            log.exception("fallback pipeline failed; serving empty trending row")
            items = []
        ms = (perf_counter() - t0) * 1000
        return RecommendResponse(
            items=(),  # 행 평탄화 상위 k 는 Phase 5 compose.py
            model_version=MODEL_VERSION_FALLBACK,
            fallback_level=level,
            latency_ms=ms,
            recommendation_id=_new_rec_id(),
            rows=(trending_row(items),),
            latency_breakdown={"total": ms},
            user_state_weights=dict(ZERO_WEIGHTS),
            context=context,
        )


    def create_app(
        pipelines: dict[str, Pipeline],
        fallback: Pipeline,
        *,
        catalog: Catalog | None = None,
        db: Database,
        neighbors: Neighbors | None = None,
        book_stats: BookStatsSource | None = None,
        state: object | None = None,
    ) -> FastAPI:
        """주입만 받는다. 비즈니스 규칙은 슬라이스 함수에. db 는 keyword-only 필수(D-09 + 리뷰 W-1)."""
        started = {"t": perf_counter()}

        @asynccontextmanager
        async def lifespan(_: FastAPI) -> AsyncIterator[None]:
            db.apply_schema()  # D-06: 행은 쓰지 않는다. 스키마·PRAGMA 만
            started["t"] = perf_counter()
            yield

        app = FastAPI(title="millie-rec", version=API_VERSION, lifespan=lifespan)

        @app.get("/health", response_model=HealthOut)
        def health() -> HealthOut:  # 동기 def — 스레드풀 (serving.md 이벤트 루프 규칙)
            ok = db.ok()
            return HealthOut(
                status="ok",
                db_ok=ok,
                db_row_count=db.row_counts() if ok else {},
                uptime_s=perf_counter() - started["t"],
            )

        @app.get("/api/recommend", response_model=RecommendOut)
        def recommend(
            user_key: Annotated[str | None, Query()] = None,
            snapshot_id: Annotated[str | None, Query()] = None,
            model: Annotated[str | None, Query()] = None,
            k: Annotated[int, Query(ge=1, le=K_MAX)] = K_DEFAULT,
            context: Annotated[str | None, Query()] = None,
            seeds: Annotated[str | None, Query()] = None,
        ) -> RecommendOut:
            t0 = perf_counter()
            if model is not None and model not in VARIANTS:
                raise _422("model", f"model must be one of {list(VARIANTS)}")
            user = UserState(user_id=None, explicit_seeds=_parse_seeds(seeds))
            # Phase 2 가 pipelines["pop"] 을 주입하면 여기에 level 0 분기를 추가한다
            # user_key·snapshot_id 는 Phase 5(state.py) 몫 — Phase 1 은 쿼리 계약만 받는다
            resp = _fallback_response(fallback, user, k, t0, context)
            return RecommendOut.from_contract(resp, forced=model is not None)

        return app
    ```
    주의: `noqa: BLE001` 을 넣지 않는다(ruff 선택 규칙 `E F I UP B` 에 BLE 없음). `Annotated[..., Query(...)]` 형식으로 B008 회피. `catalog`·`neighbors`·`book_stats`·`state`·`pipelines` 는 시그니처 유지용 인자(D-09) — `None` 분기나 스텁 로직을 만들지 않는다.

    **2-e. GREEN 확인.** `uv run pytest tests/serving/test_smoke.py --no-header` → **10 passed**. 실패하면 구현을 고친다(테스트 단정을 고치지 않는다). 이어서 `uv run pytest tests/test_architecture.py --no-header` → 3 passed. 요약 줄을 SUMMARY 용으로 보관. **커밋하지 않는다.**
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_smoke.py tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/serving/test_smoke.py --no-header` 출력이 `10 passed`
    - `uv run pytest tests/test_architecture.py --no-header` 출력이 `3 passed`
    - `grep -c "^CREATE TABLE IF NOT EXISTS" src/millie_rec/serving/schema.sql` == 7 (행 시작 앵커 — 헤더 주석의 같은 문구는 제외), 테이블 7개 각각 `grep -c "^CREATE TABLE IF NOT EXISTS <name> (" src/millie_rec/serving/schema.sql` == 1(`<name>` ∈ `users preference_snapshots events recommendations ratings candidate_sets book_stats`; 테이블 이름 단독 grep 은 `book_stats.n_events` 때문에 `events` 가 2행이므로 쓰지 않는다 — **`n_events` 컬럼을 지우지 말 것**, `contracts.BookStats.n_events` 대응 컬럼), `grep -ci "CREATE INDEX" src/millie_rec/serving/schema.sql` == 0, `grep -c "NOT NULL" src/millie_rec/serving/schema.sql` == 0
    - `grep -n "PRAGMA journal_mode=WAL" src/millie_rec/serving/db.py`·`grep -n "busy_timeout=5000"`·`grep -n "synchronous=NORMAL"`·`grep -n "threading.local()"`·`grep -n "os.environ.get(ENV_DATA_DIR)"`·`grep -n "mkdir(parents=True, exist_ok=True)"` 각 1행 이상
    - `grep -n "class GlobalPopularFallback" src/millie_rec/serving/fallback.py`·`grep -n 'name = "fallback"'`·`grep -n "def trending_row"` 각 1행
    - `grep -n "def create_app" src/millie_rec/serving/api.py`·`grep -c "^    \*,$" src/millie_rec/serving/api.py` == 2(`create_app`·`_fallback_response` 두 시그니처의 keyword-only 경계 `*,` — 시그니처가 여러 줄이라 `fallback: Pipeline, *,` 단일행 grep은 매치되지 않는다, W-1)·`grep -n "^    db: Database,$" src/millie_rec/serving/api.py`(`db` 필수, `None` 기본값 없음)·`grep -n "def _fallback_response" src/millie_rec/serving/api.py`·`grep -n "level: int = FALLBACK_GLOBAL_POP" src/millie_rec/serving/api.py`(W-2)·`grep -n '"/health"'`·`grep -n '"/api/recommend"'`·`grep -n "RecommendOut.from_contract"`·`grep -n "db.apply_schema()"`·`grep -n "le=K_MAX"`·`grep -n "model not in VARIANTS"` 각 1행; `grep -c "async def" src/millie_rec/serving/api.py` == 1 (lifespan 만), `grep -n "INSERT" src/millie_rec/serving/*.py` 0행 (D-06)
    - `grep -rn "millie_rec.app\|millie_rec.data\|millie_rec.retrieval\|millie_rec.ranking\|millie_rec.reranking\|millie_rec.evaluation" src/millie_rec/serving/` 0행
  </acceptance_criteria>
  <done>test_smoke.py 10건 GREEN, 아키텍처 테스트 통과, DDL 7테이블·PRAGMA 3종·422 검증·예외 → 200 이 코드에 존재. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR + 공개 표면 배선): serving/__init__.py __all__ 확장 · ruff · 줄수 예산 · 전체 스위트 재통과</name>
  <files>src/millie_rec/serving/__init__.py, src/millie_rec/serving/db.py, src/millie_rec/serving/fallback.py, src/millie_rec/serving/api.py, tests/serving/test_smoke.py</files>
  <read_first>
    - src/millie_rec/serving/__init__.py (현재 원문 5줄 — `__all__` 3개)
    - .planning/phases/01-local-serving-skeleton/01-PATTERNS.md "serving/__init__.py" 절 (수정 형태·순환 import 주의)
    - tests/test_architecture.py L41-42 (`app` 은 `millie_rec.serving` 2단 경로만 → 공개 표면에 이름이 있어야 Plan 02 의 server.py 가 import 가능)
    - ../.claude/rules/python.md ("완료 전 순서대로: ruff format → ruff check → pytest"; pytest 는 `uv run pytest --no-header` — 규칙 원문의 `-q` 는 addopts 와 중복)
    - ../.claude/rules/simplicity.md (Refactor 범위 = 이번 플랜이 건드린 파일만, 파일 ≤150줄)
  </read_first>
  <action>
    **3-a. `src/millie_rec/serving/__init__.py` 를 다음으로 교체**(기존 3개 이름 유지 + 4개 추가. Plan 02 의 `app/server.py` 가 `from millie_rec.serving import Database, GlobalPopularFallback, create_app, resolve_db_path` 로만 접근한다):
    ```python
    """serving 슬라이스 공개 표면. 다른 슬라이스는 여기 있는 이름만 import 할 수 있다."""

    from millie_rec.serving.api import create_app
    from millie_rec.serving.db import Database, resolve_db_path
    from millie_rec.serving.fallback import GlobalPopularFallback
    from millie_rec.serving.schemas import API_VERSION, EventIn, RecommendOut

    __all__ = [
        "API_VERSION",
        "Database",
        "EventIn",
        "GlobalPopularFallback",
        "RecommendOut",
        "create_app",
        "resolve_db_path",
    ]
    ```
    순환 확인: `uv run python -c "import millie_rec.serving as s; print(sorted(s.__all__))"` 가 7개 이름을 출력해야 한다(`api.py` 가 패키지 대신 모듈 경로를 import 하므로 순환 없음).

    **3-b. 정리(이번 플랜이 만든 파일 안에서만):** `uv run ruff format src/millie_rec/serving tests/serving src/millie_rec/contracts.py && uv run ruff check src tests`. 경고가 나오면 그 파일에서만 고친다(`schemas*.py`·기존 테스트·다른 슬라이스는 건드리지 않는다). 줄수: `wc -l src/millie_rec/serving/db.py src/millie_rec/serving/fallback.py src/millie_rec/serving/api.py src/millie_rec/serving/schema.sql tests/serving/test_smoke.py` — `.py` 전부 ≤150(예산 `db.py` ≈70 · `fallback.py` ≈60 · `api.py` ≈110). 150 을 넘는 파일이 있으면 관심사가 2개다 — 분리하지 말고 중복 주석·빈 줄을 줄여 맞춘다(이번 페이즈 파일 목록은 고정).
    Refactor 는 **행동 변경 없음**: 상수 이름·함수 시그니처·응답 값은 그대로. `docstring` 은 1줄(필요 시 "왜" 1줄 추가) 유지.

    **3-c. 전체 재통과:** `uv run pytest --no-header` → 기준선 93 passed + 10 = **103 passed, 2 skipped**(기존 테스트 실패 0; 기준선은 HEAD 8e5172b 실측 `93 passed, 2 skipped`). `uv run pytest tests/test_architecture.py --no-header` → 3 passed.

    **3-d. 사이드이펙트 감사(사용자 제약 1):** `git status --short` 에 나오는 코드 변경이 정확히 다음만인지 확인 — `M src/millie_rec/contracts.py`, `M src/millie_rec/serving/__init__.py`, `?? src/millie_rec/serving/schema.sql`, `?? src/millie_rec/serving/db.py`, `?? src/millie_rec/serving/fallback.py`, `?? src/millie_rec/serving/api.py`, `?? tests/serving/test_smoke.py`(`.planning/**` 변경은 무관). `git diff --stat -- demo src/millie_rec/serving/schemas.py src/millie_rec/serving/schemas_should.py tests/test_architecture.py tests/serving/test_schemas.py Makefile pyproject.toml Dockerfile` 이 **빈 출력**이어야 한다. `data/local/` 이 생겼다면 `git check-ignore data/local` 이 경로를 출력(gitignore 대상, 정상). **커밋하지 않는다.**

    **3-e. SUMMARY.md 작성 시** `## TDD Gate Compliance` 절에 "커밋 게이트는 프로젝트 규칙(사용자 승인 전 커밋 금지)으로 생략, 대체 증거:" + Task 1 의 RED 요약 줄(`10 failed`, AssertionError 발췌 1건) + Task 2 의 GREEN 요약 줄(`10 passed`) + Task 3 의 전체 요약 줄(`103 passed, 2 skipped`)을 붙인다. 이것이 tdd.md 게이트 검증의 대체물이다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff check src tests && uv run ruff format --check src/millie_rec/serving tests/serving src/millie_rec/contracts.py && uv run pytest --no-header 2>&1 | tail -2 && wc -l src/millie_rec/serving/db.py src/millie_rec/serving/fallback.py src/millie_rec/serving/api.py && uv run python -c "import millie_rec.serving as s; assert {'create_app','Database','GlobalPopularFallback','resolve_db_path'} <= set(s.__all__); print('exports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --no-header` 마지막 줄이 `103 passed, 2 skipped` (기준선 93 + 신규 10, failed 0)
    - `uv run ruff check src tests` 출력에 `All checks passed!`; `uv run ruff format --check src/millie_rec/serving tests/serving src/millie_rec/contracts.py` 가 "already formatted"(exit 0)
    - `wc -l` 결과: `db.py` ≤ 150, `fallback.py` ≤ 150, `api.py` ≤ 150 (권장 ≤ 110)
    - `grep -c '"' src/millie_rec/serving/__init__.py` 기준 `__all__` 에 `"create_app"`·`"Database"`·`"GlobalPopularFallback"`·`"resolve_db_path"`·`"API_VERSION"`·`"EventIn"`·`"RecommendOut"` 7개 문자열이 전부 있다
    - `git diff --stat -- demo src/millie_rec/serving/schemas.py src/millie_rec/serving/schemas_should.py tests/test_architecture.py tests/serving/test_schemas.py Makefile pyproject.toml Dockerfile` 가 빈 출력
    - `git log -1 --format=%H` 가 플랜 시작 시점(`8e5172b…` 또는 그 이후 사용자 커밋)과 같다 — 실행자가 커밋을 만들지 않았다
  </acceptance_criteria>
  <done>공개 표면 7개 이름 export, ruff 클린, 파일 ≤150줄, 전체 103 passed / 2 skipped, 허용 경로 밖 변경 0, 커밋 0. Plan 02 가 `from millie_rec.serving import …` 로 조립할 수 있는 상태.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → `/api/recommend` 쿼리 | 비신뢰 입력(`seeds`·`k`·`model`·`context`·`user_key`·`snapshot_id`) |
| 클라이언트 → `/health` | 비신뢰 요청, 응답에 DB 테이블 행 수 노출 |
| 운영자 환경변수 `DATA_DIR` → `db.py` | 신뢰 입력(Railway Variables / 로컬 셸). 사용자 제어 아님 |
| `fallback` Pipeline → `api.py` | 주입된 코드의 예외가 요청 핸들러로 전파되는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `api.py` `_parse_seeds`·`k`·`model` | mitigate | `seeds` 는 `int()` 파싱 실패 → 422, `k` 는 `Query(ge=1, le=100)`, `model` 은 `VARIANTS` 밖이면 422 (백엔드 01 §0 오류 규약·`k ≤ 100`). 테스트 `test_recommend_invalid_query_returns_422` |
| T-01-02 | Denial of Service | `api.py` `_fallback_response` | mitigate | 파이프라인 예외를 `except Exception` 으로 흡수해 200 level 3 — 500 재시도 폭주 방지. `k ≤ 100` 으로 응답 크기 상한. 테스트 `test_recommend_when_fallback_raises_still_200_level3` |
| T-01-03 | Information Disclosure | `/health.db_row_count` | accept | 테이블 이름·행 수만 노출(PII 없음). 배포 02 §3-2 볼륨 영구성 확인에 필요한 관측값. `model_version`·`artifacts_loaded_at` 은 null |
| T-01-04 | Tampering (SQL injection) | `db.py` `row_counts` f-string | accept | 테이블 이름은 `sqlite_master` 조회값만 사용, 요청 입력이 SQL 에 닿는 경로 없음(Phase 1 은 쓰기 0). 주석으로 "왜" 기록 |
| T-01-05 | Elevation of Privilege | `db.py` `resolve_db_path`·`mkdir(parents=True)` | accept | 경로는 운영자 환경변수 또는 `contracts.DIR_DATA_LOCAL` 고정. 요청이 경로에 영향 못 줌. Docker 는 root 프로세스가 `/data` 볼륨 소유(Dockerfile 주석, 설계 결정) |
| T-01-06 | Spoofing | 전체 API(인증 없음) | accept | 공개 데모·익명 사용자·PII 무수집이 설계(REQUIREMENTS "Out of Scope" 인증 제외). Phase 5 `/metrics` 만 Bearer |
</threat_model>

<verification>
- `uv run pytest tests/serving/test_smoke.py --no-header` → 10 passed (RED 단계에서는 10 failed, 전부 단정 실패 — `AssertionError` 문자열 또는 `E assert …`)
- `uv run pytest --no-header` → 103 passed, 2 skipped (기준선 93 + 10)
- `uv run pytest tests/test_architecture.py --no-header` → 3 passed (star 의존·계층 폴더 없음·contracts 무로직)
- `uv run ruff check src tests` 클린, `.py` 신규 파일 ≤150줄
- `git diff --stat` 에 `demo/`·`schemas*.py`·`Makefile`·`pyproject.toml`·`Dockerfile`·`tests/test_architecture.py` 없음
- 실행자 커밋 0건 (`git log` 불변)
</verification>

<success_criteria>
- **SKEL-01** `/health` 200 · `HealthOut` 파싱 · `model_version` null (서버 기동 `make serve` 확인은 Plan 02)
- **SKEL-03** 파이프라인 미주입 `GET /api/recommend?seeds=1,2,3` → 200 `fallback_level=3` `RecommendOut` 스키마, 예외 Pipeline 주입에도 200 — 수용 기준 '파이프라인 예외 → 인기 row 200'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)의 최소 형태
- **SKEL-04** `Database(path).apply_schema()` 로 Must 4테이블 생성(Should 3 포함 DDL) · `/health.db_row_count` 에 Must 4 키 · 경로 해석은 `db.py` 만(`DATA_DIR` → `$DATA_DIR/millie.db`, 없으면 `contracts.DIR_DATA_LOCAL/millie.db`)
- CONTEXT 결정 D-01(상수)·D-02(trending 1행)·D-03(rec_id)·D-04(wire 값)·D-05(DDL 7)·D-06(쓰기 0)·D-07(전 테이블 COUNT)·D-08(PRAGMA·경로)·D-09(시그니처 — `db` 는 keyword-only 필수(아키텍트 리뷰 W-1 채택) — serving.md 의 위치 인자 표기와 다르며 Advisor 가 개발일지에 기록한다)·D-10(GlobalPopularFallback)·D-12·D-13(테스트 형태) 가 코드·테스트에 존재
- 사용자 제약 5: 계층 폴더 0, serving 은 `contracts` + 자기 슬라이스만 import, 공개 표면 `__all__` 로만 노출
</success_criteria>

<output>
완료 후 `.planning/phases/01-local-serving-skeleton/01-01-SUMMARY.md` 를 만든다. 포함: RED(어떤 단정이 왜 실패했나 — AssertionError 발췌) · GREEN(무엇을 구현해 통과했나) · REFACTOR(`__init__` 배선·ruff·줄수) · `## TDD Gate Compliance`(커밋 게이트 생략 사유 = 프로젝트 커밋 정책, 대체 증거 = pytest 요약 줄 3개) · 변경 파일 목록(`git status --short`) · `wc -l` 결과 · "커밋 없음 — Advisor 가 `/codex:review --scope working-tree` 후 사용자 승인으로 커밋" 한 줄.
</output>
