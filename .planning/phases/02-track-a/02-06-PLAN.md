---
phase: 02-track-a
plan: 06
type: execute
wave: 3
depends_on: ["02-04", "02-05"]
files_modified:
  - data/raw/goodbooks/ratings.csv          # 다운로드(gitignore)
  - data/raw/goodbooks/books.csv
  - data/raw/goodbooks/book_tags.csv
  - data/raw/goodbooks/tags.csv
  - data/processed/interactions.parquet     # gitignore
  - data/processed/books.parquet
  - results/latest.csv                      # 커밋 대상(승인 후)
  - results/latest_states.csv
  - results/eval_<YYYYMMDD_HHMM>.json
  - artifacts/serving/eval_table.json       # 커밋 대상(승인 후)
  - artifacts/popularity.json               # gitignore
  - report/draft.md                         # P4 실측 1줄
  - src/millie_rec/data/goodbooks.py        # NOISE_TAGS 최대 1회 보충(선택)
  - src/millie_rec/evaluation/figures.py    # EVAL-07 Should — 마지막 태스크, 생략 가능
  - src/millie_rec/evaluation/__init__.py   # plot_eval_bar 노출(EVAL-07 시)
  - tests/evaluation/test_figures.py        # EVAL-07 시
  - report/figures/eval_bar.png             # EVAL-07 시(커밋 대상 — .gitignore !report/figures/*.png)
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-05, EVAL-06, EVAL-07]
lane: Advisor 실측(GSD 실행자 대행 → Advisor 가 diff·숫자·테스트로 검증) — 네트워크는 Task 1 만. 선례 = Phase 1 '로컬 서빙 스켈레톤'(.planning/phases/01-local-serving-skeleton/01-02-SUMMARY.md) 의 app/server.py 실행 방식, 오케스트레이터 결정 W3
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md)). results/·eval_table.json 은 커밋 대상 파일이지만 커밋은 사용자 승인 후 Advisor 가 한다
network: "Task 1 `make data` 만 — GitHub raw HTTPS ≈69MB+, 멱등. 실패 시 재시도 루프 없이 중단·보고"
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/**
  - src/millie_rec/app/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/data/load.py
  - src/millie_rec/data/split.py
  - src/millie_rec/data/onboarding.py
  - src/millie_rec/data/labels.py
  - scripts/**
  - data/id_map.csv
  - tests/test_architecture.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "results/latest.csv 가 존재하고 헤더 `variant,recall@20,ndcg@10,ild@10,n_users,split_mode,model_version` 아래 `pop` 행 1개(n_users=2000, split_mode=holdout, model_version=pop_v1) 만 있다 — PDF 비교표의 유일한 숫자 출처"
    - "results/latest_states.csv 에 `pop,n0,…,2000` 과 `pop,n20,…,<n≥20 적격 수>` 두 행이 있고 n20 의 n_users(1..2000)가 별도로 기록된다(모집단은 다를 수 있다)"
    - "results/eval_<ts>.json 에 dataset=goodbooks-10k · split_mode=holdout · test_frac=0.2 · seed=42 · n_users{n0,n20} · n_excluded · k_recall=20 · k_rank=10 · n_onboard_seeds=5 · k_history=20 · min_user_interactions=5 · min_item_interactions=5 · git_sha · elapsed_s · created_at 가 있다"
    - "artifacts/serving/eval_table.json 이 {rows:[pop 행], meta:{…}} 이고 states 표는 없다(D-09)"
    - "artifacts/popularity.json 이 있는 상태에서 uv run pytest --no-header 전체 통과, make smoke PASS, 그리고 GET /api/recommend?seeds=1,2,3&k=5 가 fallback_level 0 · model_version pop_v1 · items 5개(book_id ∉ {1,2,3}) 를 낸다; seeds 없는 요청은 level 3"
    - "report/draft.md P4 에 실측 숫자 + PDF 각주 문장 1줄이 추가됐다(숫자는 latest.csv 에서 복사)"
    - "(Should) report/figures/eval_bar.png 가 results/latest.csv 에서 생성됐다 — 없으면 PDF 는 표만"
  artifacts:
    - path: "results/latest.csv"
      provides: "pop 1행 비교표"
      contains: ",2000,holdout,pop_v1"
    - path: "results/latest_states.csv"
      provides: "n0·n20 두 행"
      contains: "pop,n20,"
    - path: "artifacts/serving/eval_table.json"
      provides: "쇼케이스 입력(Phase 5 GET /api/showcase)"
      contains: "\"rows\""
    - path: "artifacts/popularity.json"
      provides: "서버 level 0 주입 입력(D-10)"
      contains: "\"name\": \"pop\""
    - path: "report/draft.md"
      provides: "P4 실측 1줄 + 각주"
      contains: "split_mode=holdout"
    - path: "src/millie_rec/evaluation/figures.py"
      provides: "(Should) plot_eval_bar(latest_csv, out) -> Path"
      exports: ["plot_eval_bar"]
      max_lines: 150
  key_links:
    - from: "Makefile eval"
      to: "results/latest.csv"
      via: "uv run python -m millie_rec.app.cli eval --variant all"
      pattern: "pop,"
    - from: "artifacts/popularity.json"
      to: "GET /api/recommend"
      via: "app/server.py build_pipelines() → pipelines['pop'] → level 0"
      pattern: "\"fallback_level\":0"
    - from: "results/latest.csv"
      to: "report/draft.md P4"
      via: "숫자 복사(손으로 계산·수정 없음) + 각주 문장"
      pattern: "Recall@20"
---

<objective>
Phase 2 'Track A 정량 평가 기반'(.planning/ROADMAP.md "Phase 2: Track A 정량 평가 기반")의 **실측**: `make data`(유일한 네트워크 단계, 멱등) → `make eval` → `results/` 세 파일·`artifacts/serving/eval_table.json`·`artifacts/popularity.json` 생성 → `make smoke` 가 level 0 을 보는지 확인 → `report/draft.md` P4 에 실측 1줄 → (Should) `evaluation/figures.py` 로 `report/figures/eval_bar.png`.

Purpose: ROADMAP Success Criteria 1·4·5 를 **파일과 응답으로** 증명한다. Day 1 최소 종료 조건 "`pop` Recall@20 실측 1개"(../CLAUDE.md §5) 가 여기서 채워진다. 결정 'D-17 완료 형태'(.planning/phases/02-track-a/02-CONTEXT.md): Advisor 가 `make data && make eval` 실측 → `latest.csv` `pop` 1행·`latest_states.csv` 2행·`eval_<ts>.json`·`eval_table.json` 존재 → `draft.md` 에 실측 문장 1줄.
Output: 데이터·결과·아티팩트 파일 + `draft.md` 1줄 + (Should) `figures.py`·png. 코드 변경은 `figures.py`(신규)와 필요 시 `goodbooks.py` `NOISE_TAGS` 보충 1회만. 커밋 0(`results/`·`eval_table.json`·png 는 커밋 대상이지만 사용자 승인 후 Advisor 가 커밋).

**중단 규칙(실행자):** Task 1 다운로드 실패(HTTP 오류·디스크 부족·크기 0) → **재시도 루프 없이** 오류 전문·경과 시간·받은 파일 크기를 SUMMARY 에 적고 `## CHECKPOINT REACHED` 로 멈춘다. Task 2 에서 Recall==1.0·ILD==0.0·NDCG>Recall 같은 이상치가 보이면 누수·버그 의심을 먼저 적고 멈춘다(`../.claude/skills/eval-run/SKILL.md` output 규칙). Task 4(EVAL-07)는 시간 부족 시 **이 태스크만 생략** — PDF 는 표만(ROADMAP "Should 꼬리").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-track-a/02-CONTEXT.md
@.planning/phases/02-track-a/02-PATTERNS.md
@.planning/phases/02-track-a/02-01-SUMMARY.md
@.planning/phases/02-track-a/02-02-SUMMARY.md
@.planning/phases/02-track-a/02-03-SUMMARY.md
@.planning/phases/02-track-a/02-04-SUMMARY.md
@.planning/phases/02-track-a/02-05-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/data/goodbooks.py
@src/millie_rec/evaluation/__init__.py
@src/millie_rec/evaluation/report.py
@src/millie_rec/app/cli.py
@report/draft.md
@Makefile
@.gitignore
@../.claude/rules/report.md
@../.claude/rules/evaluation.md
@../.claude/rules/local-run.md
@../.claude/rules/data.md
@../.claude/rules/python-tdd.md
@../.claude/skills/eval-run/SKILL.md

<project_rules_inline>
**숫자 정본(`../.claude/rules/evaluation.md`·`report.md`):** PDF 숫자는 `results/latest.csv` 에서만 복사한다. 손으로 계산·반올림·수정하지 않는다. 숫자가 바뀌면 원인은 재실행이어야 한다. 결과 파일은 커밋 대상(커밋은 사용자 승인 후).

**데이터 2트랙 불혼합(../CLAUDE.md §2-8):** 이 플랜의 숫자는 전부 Track A(Goodbooks-10k). `draft.md` 문장에 밀리 카탈로그 수치를 섞지 않는다. 각주에 데이터셋·라이선스·`split_mode`·N 유저·K·필터 상수를 병기한다.

**네트워크:** `make data` 한 번만. 테스트(`uv run pytest`)는 네트워크를 쓰지 않는다. 다운로드 실패 시 재시도 루프 금지 — 중단·보고.

**draft.md(`../.claude/rules/report.md`):** P4 절에 bullet 1개 추가(기존 Phase 1 bullet 아래). 한 문단 4문장 이내, 영어 용어 첫 등장에 `Term(한글)`. 해석 문장 템플릿은 4행이 다 나온 뒤(Phase 4) — Phase 2 는 실측 문장 + 각주만. `pages`·읽기 속도 언급 금지.

**코드 변경 범위:** `figures.py` 신규(evaluation 슬라이스, ≤150줄, matplotlib `Agg`), `evaluation/__init__.py` 에 `plot_eval_bar` 추가, `tests/evaluation/test_figures.py`(tmp csv → png). `goodbooks.py` 는 `NOISE_TAGS` 에 태그 추가만(최대 1회, 근거를 SUMMARY 에). 그 외 `src/` 변경 금지. 테스트 먼저(Red = AssertionError).

**커밋 금지:** `git add`·`git commit` 실행 안 함. 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다. 커밋 전 점검은 Advisor 몫(`git ls-files | grep -iE "pdf|png|assets"` — `report/figures/*.png` 는 `.gitignore` 예외로 허용).

**완료 기준(D-17):** `uv run ruff format . && uv run ruff check .` 클린 · `uv run pytest --no-header` 전체 통과(아티팩트 있는 상태에서 — Plan 05 가 smoke 단정을 조건부로 만들어 두었다) · `make smoke` PASS + `fallback_level` 0 실측.
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. -->

From Makefile:
```
data:  uv run python -m millie_rec.app.cli data
eval:  uv run python -m millie_rec.app.cli eval --variant all
smoke: uvicorn … --port 8010 … curl "localhost:8010/api/recommend?seeds=1,2,3&k=5" … "smoke: PASS"
```

From src/millie_rec/contracts.py:
```python
DIR_RESULTS = ROOT / "results"; DIR_FIGURES = ROOT / "report" / "figures"; VARIANTS = ("pop", "cf", "hybrid", "hybrid_div")
```

From src/millie_rec/evaluation/report.py (Plan 03):
```python
LATEST_FILE = "latest.csv"; COL_RECALL = "recall@20"; COL_NDCG = "ndcg@10"; COL_ILD = "ild@10"
LATEST_COLUMNS = ("variant", "recall@20", "ndcg@10", "ild@10", "n_users", "split_mode", "model_version")
```

Goodbooks-10k 기대 규모(다운로드 후 확인): `ratings.csv` ≈ 5,976,479행 · 유저 53,424 · 도서 10,000 · `books.csv` 10,000행 · `tags.csv` 34,252행 · `book_tags.csv` ≈ 999,912행. 최소 필터(유저≥5·아이템≥5) 후에도 유저 5만 명대 — `N_TEST_USERS=2000` 표본은 충분하다.

PDF 각주 문장(CONTEXT "Specific Ideas" — 그대로 쓴다):
> "Goodbooks-10k(CC BY-SA 4.0), 유저 ≥5·아이템 ≥5 필터, 유저별 random holdout 20%(`split_mode=holdout`, 타임스탬프 없음), 테스트 유저 2,000명(seed 42), 온보딩 시뮬레이션 = train 긍정 중 무작위 5권을 seeds 로 노출, n≥k 표는 seeds 외 이력 ≥20인 유저."

`report/draft.md` P4 현재 마지막 bullet(그 아래에 추가):
```
- (Day 1 실측, Phase 1 '로컬 서빙 스켈레톤') "추천 API 장애 ≠ 메인 장애"의 코드 증거: …
```
</interfaces>
</context>

<wave_gate>
## Wave 게이트 — 오케스트레이터(Advisor) 체크리스트 (checker B3, 각 wave 종료 후 1회)

플랜 01~05 는 자기 경로(`tests/<slice>`·`ruff … <own files>`)만 검증하고 **전역 게이트를 돌리지 않는다** — 병렬 형제 플랜의 RED 파일·미포맷 파일·고정 포트 8010/`.uvicorn.pid` 와 경합하기 때문. 전역 게이트는 Advisor 가 다음 시점에 **1회씩** 실행한다:

| 시점 | 명령(모두 `/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec` 에서) | 기대 |
|---|---|---|
| wave 1 종료(01·02·03 SUMMARY 존재) | `uv run ruff format --check . && uv run ruff check .` · `uv run pytest --no-header` · `make smoke` | ruff 클린 · `N passed, 2 skipped`(N ≥ 104 + 20 + 11 + 18 = 153), failed 0 · `smoke: PASS`(level 3 — 서버 코드 무변경) |
| wave 2 종료(04·05 SUMMARY 존재) | 같은 3개 + `uv run pytest tests/serving/test_smoke.py --no-header` | N ≥ 153 + 7 + 8 + 1 = 169 · test_smoke `12 passed`(02-05 의 새 조립 테스트가 02-04 분기 위에서 통과) · `smoke: PASS`(아티팩트 없음 → level 3) |
| Plan 06 안 | Task 3 이 직접 실행(wave 3 은 단일 플랜이라 경합 없음) | 아래 Task 3 |

게이트 실패 시: 실패한 파일의 소유 플랜(파일 경로 → `files_modified`)에 수정 브리프를 재위임하고, 해당 플랜의 자기 경로 acceptance 부터 다시 확인한다. `make smoke` 실패 시 `lsof -i :8010`·`rm -f .uvicorn.pid` 로 잔존 프로세스를 정리한 뒤 1회 재실행.
</wave_gate>

<tasks>

<task type="auto">
  <name>Task 1: make data — Goodbooks-10k 멱등 다운로드 + parquet 캐시 (유일한 네트워크 단계) + NOISE_TAGS 점검</name>
  <files>data/raw/goodbooks/ratings.csv, data/raw/goodbooks/books.csv, data/raw/goodbooks/book_tags.csv, data/raw/goodbooks/tags.csv, data/processed/interactions.parquet, data/processed/books.parquet, src/millie_rec/data/goodbooks.py (NOISE_TAGS 보충 시에만)</files>
  <read_first>
    - src/millie_rec/data/goodbooks.py (Plan 01 결과 — URL 상수·`NOISE_TAGS`·`TOP_TAGS`·멱등 검사)
    - .planning/phases/02-track-a/02-01-SUMMARY.md (빈 리스트 컬럼 처리 방식 등)
    - .planning/phases/02-track-a/02-CONTEXT.md D-15("정확한 목록은 Worker 가 태그 빈도 상위를 보고 확정, 상수로 고정"), "Specific Ideas — Goodbooks 사실"
    - .gitignore L1-6(`data/raw/*`·`data/processed/*` 무시 확인)
    - ../.claude/rules/data.md "Track A 규칙"
  </read_first>
  <action>
    **1-a. 사전 점검:** `df -h .` 여유 ≥ 1GB. `ls -la data/raw/goodbooks/ 2>/dev/null`(없어야 정상). `uv run python --version` → 3.11.x.

    **1-b. 다운로드+빌드:** `time make data` (≈1~3분). 성공 출력에 `raw=4 files` 와 두 parquet 이름. 실패하면 **재시도 루프 없이** 중단: 오류 전문·경과·`ls -la data/raw/goodbooks/` 를 SUMMARY 에 적고 `## CHECKPOINT REACHED`(사유: 네트워크/디스크) 로 보고.

    **1-c. 규모 확인(형태 sanity):**
    ```bash
    ls -la data/raw/goodbooks/            # ratings.csv ≈ 69MB, 4파일 크기 > 0
    uv run python -c "
    from millie_rec.data import load_interactions, load_books
    i = load_interactions(); b = load_books()
    print(len(i), i['user_id'].nunique(), i['book_id'].nunique(), i['ts'].isna().all(), i['event'].unique(), i['rating'].min(), i['rating'].max())
    print(len(b), list(b.columns))
    print((b['tags'].str.len() == 0).sum(), 'books with empty tags')
    "
    ```
    기대: 행 ≈ 5,976,479 · 유저 ≈ 53,424 · 도서 10,000 · `ts` 전부 결측 `True` · `event == ['rating']` · rating 1.0~5.0 / books 10,000행, 컬럼에 `tags` 포함 / 빈 tags 책 수는 소수. 크게 다르면(예 행 수 절반) 다운로드가 잘렸을 가능성 — `.part` 잔존 확인 후 해당 파일만 삭제하고 `make data` 1회 재실행(부분 파일 정리이지 재시도 루프가 아니다; 그래도 실패면 중단).

    **1-d. NOISE_TAGS 점검(D-15, 최대 1회):**
    ```bash
    uv run python -c "
    from collections import Counter
    from millie_rec.data import load_books
    c = Counter(t for s in load_books()['tags'] for t in s.split())
    print(c.most_common(40))
    "
    ```
    상위 40 안에 **내용과 무관한 서가 관리·형식·연도 태그**(예 `owned-books`·`my-books`·`e-books`·`read-2016`·`books-i-have`·`shelfari-favorites`·`to-read-fiction` 류)가 `NOISE_TAGS` 에 없으면 `src/millie_rec/data/goodbooks.py` 의 `NOISE_TAGS` 에 **그 태그들만 추가**(장르·주제 태그 `fiction`·`fantasy`·`classics`·`young-adult`·`non-fiction`·`romance`·`mystery`·`historical-fiction`·`science-fiction` 등은 절대 넣지 않는다 — ILD 의 신호다). 추가했으면 `uv run python -m millie_rec.app.cli data` 재실행(다운로드는 멱등으로 건너뛰고 parquet 만 재생성) 후 1-c 마지막 줄 재확인. 추가한 태그와 근거(상위 빈도 출력 발췌)를 SUMMARY 에 적는다. 추가할 것이 없으면 "보충 없음". `uv run pytest tests/data --no-header` 는 여전히 통과해야 한다(테스트의 잡음 태그 `to-read` 유지).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && ls -la data/raw/goodbooks/ratings.csv data/raw/goodbooks/books.csv data/raw/goodbooks/book_tags.csv data/raw/goodbooks/tags.csv data/processed/interactions.parquet data/processed/books.parquet && uv run python -c "from millie_rec.data import load_interactions, load_books; i=load_interactions(); b=load_books(); assert len(i) > 5_000_000 and i['ts'].isna().all() and len(b) == 10_000 and 'tags' in b.columns; print('data ok', len(i), len(b))" && uv run pytest tests/data --no-header</automated>
  </verify>
  <acceptance_criteria>
    - 4개 raw CSV 존재, 각 크기 > 0, `ratings.csv` > 60,000,000 바이트(`stat -f %z data/raw/goodbooks/ratings.csv`); `ls data/raw/goodbooks/*.part 2>/dev/null` 빈 결과
    - `data/processed/interactions.parquet`·`books.parquet` 존재; 위 `uv run python -c` 가 `data ok <행수> 10000` 출력(행수 > 5,000,000)
    - `git status --short | grep -c "data/raw\|data/processed"` == 0(gitignore 확인)
    - `make data` 두 번째 실행이 `downloaded` 로그 없이 끝난다(멱등)
    - `NOISE_TAGS` 를 보충했다면 `git diff --numstat src/millie_rec/data/goodbooks.py` 의 삭제 수 ≤ 3, 추가 태그 전부 소문자·장르 아님; 보충하지 않았다면 diff 없음
    - `uv run pytest tests/data --no-header` 통과(failed 0)
  </acceptance_criteria>
  <done>Goodbooks-10k 가 `data/raw/goodbooks/` 에 있고 두 parquet 이 계약 컬럼으로 캐시됐다. 재실행은 네트워크 없이 끝난다. 커밋 없음(gitignore).</done>
</task>

<task type="auto">
  <name>Task 2: make eval — pop 실측 → results/ 세 파일 + eval_table.json + popularity.json 검증 + 재현성</name>
  <files>results/latest.csv, results/latest_states.csv, results/eval_<YYYYMMDD_HHMM>.json, artifacts/serving/eval_table.json, artifacts/popularity.json</files>
  <read_first>
    - .planning/phases/02-track-a/02-CONTEXT.md D-01(n_users=2000·n_excluded), D-04(K_HISTORY=20·n20 모집단 별도), D-06~D-09(파일 형태), "Specific Ideas — latest.csv·latest_states.csv 기대 형태", "★시간 가변 가중치 증거의 해석 방향"
    - src/millie_rec/app/cli.py (Plan 05 결과 — 출력 형식·`--variant all`)
    - ../.claude/skills/eval-run/SKILL.md process 3~6(5분 상한·이상치 규칙)
    - ../.claude/rules/evaluation.md "결과 파일"·"누수 방지"
  </read_first>
  <action>
    **2-a. 실행:** 먼저 `SCRATCH=/private/tmp/claude-501/-Users-shinwonchul-Documents----------/90da5b8a-79fa-4a89-946f-e6b077928b9c/scratchpad; mkdir -p "$SCRATCH"` 로 스크래치패드를 정의한다(`/tmp` 금지, W4). `time make eval` (예상 3~8분(노트북): parquet 6M행 로드 + 최소 필터 반복 + holdout groupby + 2,000 유저 × 2 상태 pop + TF-IDF fit). `/eval-run` 스킬의 "5분 초과면 표본 축소 제안" 은 Phase 4 이후 몫이며 **Phase 2 에서는 초과 시 SUMMARY 에 실측 시간을 기록하고 계속한다(중단 아님, 표본 축소는 D-01 위반)**. stdout 의 `pop: recall@20=… ndcg@10=… ild@10=… n_users=2000 | n20: n_users=… recall=…` 줄과 마지막 요약 줄을 SUMMARY 에 그대로 붙인다.

    **2-b. 파일 검증:**
    ```bash
    cat results/latest.csv                      # 2줄: 헤더 + pop 행
    cat results/latest_states.csv               # 3줄: 헤더 + pop,n0 + pop,n20
    ls results/eval_*.json | wc -l              # 1
    uv run python -c "
    import json, glob
    d = json.load(open(sorted(glob.glob('results/eval_*.json'))[-1]))
    need = {'dataset','split_mode','test_frac','seed','n_users','n_excluded','k_recall','k_rank','n_onboard_seeds','k_history','min_user_interactions','min_item_interactions','git_sha','elapsed_s','created_at','rows','states'}
    assert need <= set(d), need - set(d)
    assert d['dataset']=='goodbooks-10k' and d['split_mode']=='holdout' and d['test_frac']==0.2 and d['seed']==42
    assert d['n_users']['n0']==2000 and 0 < d['n_users']['n20'] <= 2000, d['n_users']  # 전원이 이력 ≥20 일 수도 있다(B2)
    assert d['k_recall']==20 and d['k_rank']==10 and d['n_onboard_seeds']==5 and d['k_history']==20
    assert d['min_user_interactions']==5 and d['min_item_interactions']==5 and len(d['rows'])==1 and len(d['states'])==2
    print('json ok', d['n_users'], 'excluded', d['n_excluded'], 'sha', d['git_sha'], 'elapsed', d['elapsed_s'])
    t = json.load(open('artifacts/serving/eval_table.json'))
    assert set(t)=={'rows','meta'} and t['rows'][0]['variant']=='pop' and t['meta']['n_users']==2000 and t['meta']['split_mode']=='holdout'
    p = json.load(open('artifacts/popularity.json'))
    assert p['name']=='pop' and 0 < len(p['items']) <= 1000
    print('artifacts ok', len(p['items']))
    "
    ```
    **2-c. 숫자 sanity(누수·버그 의심 규칙 — checker B2 보정):** 이상치는 **`recall@20 == 1.0`·`recall@20 > 1.0`·`ndcg@10 > 1.0`·`ild@10 == 0.0`** 네 가지만이다 — 보이면 누수·버그 의심을 SUMMARY 에 먼저 적고 **멈춘다**(`## CHECKPOINT REACHED`). `ndcg@10 > recall@20` 은 **이상치가 아니다**: 이 정의(NDCG@10 의 IDCG = min(|R_u|, 10) vs Recall@20 의 분모 |R_u|, Goodbooks 에서 |R_u| 는 평균 십수 개)에서는 pop 에서 NDCG 가 Recall 보다 큰 것이 기대값이다. n0 와 n20 의 pop 지표는 **다를 수 있다** — n0 은 `seen` 이 seeds 5권뿐이라 pop 상위에 이미 읽은 train 책이 남고, n20 은 train 이력 전체를 제외하므로 n20 Recall 이 유의하게 높게 나오는 것이 정상이며 모집단(`n_users`)도 다르다. 두 값과 두 `n_users` 를 그대로 기록한다(해석은 Task 3 의 조건문). `n20` 의 `n_users` 가 0 이면 Goodbooks 유저 이력 분포(`interactions.groupby('user_id').size().describe()`)를 SUMMARY 에 적고 멈춘다 — 상수 `K_HISTORY` 는 바꾸지 않는다(D-04 는 사용자 결정).

    **2-d. 재현성:** `cp results/latest.csv "$SCRATCH/latest_first.csv"`(2-a 에서 정의한 스크래치패드 경로, `/tmp` 금지) → `make eval` 재실행 → `diff "$SCRATCH/latest_first.csv" results/latest.csv` 빈 출력(바이트 동일). json 은 `created_at`·`elapsed_s`·파일 이름이 달라지므로 두 번째 실행의 json(최신 이름)을 **삭제**해 `results/eval_*.json` 이 첫 실행 1개만 남게 한다. 다르면 seed 고정 누락 — 중단·보고.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && head -1 results/latest.csv | grep -qx "variant,recall@20,ndcg@10,ild@10,n_users,split_mode,model_version" && [ "$(grep -c '^pop,' results/latest.csv)" = "1" ] && grep -q ",2000,holdout,pop_v1$" results/latest.csv && grep -q "^pop,n0," results/latest_states.csv && grep -q "^pop,n20," results/latest_states.csv && [ "$(ls results/eval_*.json | wc -l | tr -d ' ')" = "1" ] && test -f artifacts/serving/eval_table.json && test -f artifacts/popularity.json && echo "results ok" && cat results/latest.csv results/latest_states.csv</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 results/latest.csv` == `variant,recall@20,ndcg@10,ild@10,n_users,split_mode,model_version`; `wc -l results/latest.csv` == 2; `grep -c "^pop," results/latest.csv` == 1; 행이 `,2000,holdout,pop_v1` 로 끝난다
    - `wc -l results/latest_states.csv` == 3; `grep -c "^pop,n0,.*,2000$" results/latest_states.csv` == 1; `grep -c "^pop,n20," results/latest_states.csv` == 1 이고 그 행의 `n_users` 는 1 이상 2000 이하(2,000명 전원이 seeds 외 이력 ≥20 일 수 있다 — 두 `n_users` 가 같아도 이상치 아님)
    - `ls results/eval_*.json | wc -l` == 1; 위 python 검증이 `json ok …` 와 `artifacts ok …` 를 출력
    - `pop` 행 세 지표가 `(0, 1)` 개구간(`awk -F, 'NR==2{print ($2>0 && $2<1 && $3>0 && $3<1 && $4>0 && $4<1)}' results/latest.csv` → `1`)
    - 재현성: 두 번째 `make eval` 후 `latest.csv` 바이트 동일(`diff` 빈 출력), json 1개 유지
    - `git status --short` 에 `results/latest.csv`·`results/latest_states.csv`·`results/eval_*.json`·`artifacts/serving/eval_table.json` 이 **나타난다**(커밋 대상, 미커밋) 그리고 `artifacts/popularity.json` 은 나타나지 않는다(gitignore)
  </acceptance_criteria>
  <done>`pop` 1행 실측이 `results/` 에 있고 재실행이 같은 숫자를 낸다. 두 상태 표·json 메타·쇼케이스 입력·서버 아티팩트가 D-06~D-10 형태로 존재. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 전체 스위트 + make smoke(level 0 실측) + report/draft.md P4 실측 1줄</name>
  <files>report/draft.md</files>
  <read_first>
    - report/draft.md 전문(P4 절 위치·기존 Phase 1 bullet 형식)
    - ../.claude/rules/report.md(P4 정의·각주 규칙·"숫자는 latest.csv 에서만"·문체)
    - results/latest.csv · results/latest_states.csv (Task 2 결과 — 복사할 숫자)
    - Makefile L33-40(`smoke`)
    - tests/serving/test_smoke.py L152-166(Plan 05 가 조건부로 만든 서버 단정)
  </read_first>
  <action>
    **3-a. 전체 스위트(아티팩트 있는 상태 — wave 3 은 단일 플랜이라 전역 게이트를 여기서 직접 돈다):** `uv run pytest --no-header 2>&1 | tail -3` → `failed 0`(서버 조립 테스트 2건은 Plan 05 가 `build_pipelines` 를 monkeypatch 해 아티팩트 유무와 무관 — B4). `uv run ruff format --check . && uv run ruff check .` 클린.

    **3-b. make smoke + level 0 실측:** `make smoke` → `smoke: PASS`. 이어서 실측(별도 포트; `SCRATCH` 는 Task 2 에서 정의 — 새 셸이면 다시 정의; 시스템 `python3` 금지 → `uv run python`, W4):
    ```bash
    SCRATCH=/private/tmp/claude-501/-Users-shinwonchul-Documents----------/90da5b8a-79fa-4a89-946f-e6b077928b9c/scratchpad; mkdir -p "$SCRATCH"
    uv run uvicorn millie_rec.app.server:app --port 8012 --log-level warning & echo $! > "$SCRATCH/uv.pid"; sleep 3
    curl -s "localhost:8012/api/recommend?seeds=1,2,3&k=5" | tee "$SCRATCH/level0.json" | uv run python -c "import json,sys; d=json.load(sys.stdin); print(d['fallback_level'], d['model_version'], [i['book_id'] for i in d['items']], d['rows'][0]['row_id'], len(d['rows'][0]['items']))"
    curl -s "localhost:8012/api/recommend" | uv run python -c "import json,sys; d=json.load(sys.stdin); print(d['fallback_level'], d['model_version'], d['items'])"
    curl -s localhost:8012/health | uv run python -c "import json,sys; d=json.load(sys.stdin); print(d['status'], d['db_ok'], d['model_version'])"
    kill $(cat "$SCRATCH/uv.pid")
    ```
    기대: 첫 줄 `0 pop_v1 [<5개 id, 1·2·3 아님>] trending 5` · 둘째 줄 `3 fallback_v1 []`(익명 D-13) · 셋째 줄 `ok True None`(`/health.model_version` 채우기는 Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md) — Plan 04 결정). 이 세 줄을 SUMMARY 에 붙인다.

    **3-c. `report/draft.md` P4 수정 2건.**
    ① 기존 P4 소제목 아래 괄호 줄(`report/draft.md` L30 `(Day 1~4: 3지표 ↔ 3단계 그림, temporal split, A/B 지표 위계, …)`) 의 **`temporal split` 을 `random holdout(\`split_mode=holdout\`)` 으로 고친다**(W5 — Goodbooks 에 타임스탬프가 없어 temporal 이 아니며 거짓 표기 금지, D-02). 그 줄의 다른 단어는 그대로.
    ② 기존 Phase 1 bullet 바로 아래에 bullet 1개 추가. 숫자는 `results/latest.csv`·`latest_states.csv` 값을 **그대로 복사** — `<recall>`·`<ndcg>`·`<ild>`·`<n20>`·`<recall_n20>`·`<ndcg_n20>` 자리만 채운다. **결론을 미리 쓰지 않는다** — 두 상태의 차이는 측정 뒤 조건문으로 해석한다(W5):
    ```
    - (Day 1 실측, Phase 2 'Track A 정량 평가 기반') Popularity(전역 인기) 기준선 1행: Recall@20 = <recall> · NDCG@10 = <ndcg> · ILD@10 = <ild> (n=0 온보딩 시뮬레이션, `results/latest.csv`). 같은 pop 모델을 n≥20 이력 상태(n_users=<n20>)에서 재측정하면 Recall@20 = <recall_n20> · NDCG@10 = <ndcg_n20> (`results/latest_states.csv`). 두 값의 차이는 모델이 아니라 **추천 제외 범위의 차이**에서 온다 — n=0 은 `UserState.seen` 이 seeds 5권뿐이어서 pop 상위에 이미 읽은 train 책이 남고, n≥20 은 train 이력 전체를 제외한다(모집단도 다르다). 따라서 pop 의 두 값은 ★시간 가변 가중치의 **바닥선**이고, Phase 4 의 `hybrid` 는 여기에 β(행동 이력 가중치) 변화가 더해져 n≥k 에서 pop 대비 격차가 벌어져야 증거가 된다. 각주: Goodbooks-10k(CC BY-SA 4.0), 유저 ≥5·아이템 ≥5 필터, 유저별 random holdout 20%(`split_mode=holdout`, 타임스탬프 없음), 테스트 유저 2,000명(seed 42), 온보딩 시뮬레이션 = train 긍정(rating≥4) 중 무작위 5권을 seeds 로 노출, n≥k 표는 seeds 외 이력 ≥20인 유저, 정답 = test 긍정(rating≥4), 학습(pop 카운트) = train 전체 / 추천 제외 = 해당 상태의 `UserState.seen`(n0: seeds 5권, n20: seeds + train 이력).
    ```
    다른 절·체크리스트 표는 건드리지 않는다. `git diff --numstat report/draft.md` 가 `2\t1`(추가 2줄, 삭제 1줄 — ① 의 괄호 줄 교체 + ② 의 bullet).

    **3-d. SUMMARY 준비:** Task 1~3 의 실측 출력 원문(`make data` 시간·행 수, `make eval` 시간·stdout, 세 curl 줄, pytest 요약 줄, `make smoke` 마지막 줄), n0 vs n20 두 값과 두 `n_users`(해석은 조건문 그대로), `git status --short`, "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다".
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check . && uv run ruff check . && uv run pytest --no-header 2>&1 | tail -3 && make smoke && grep -c "Phase 2 'Track A 정량 평가 기반'" report/draft.md && grep -c "split_mode=holdout" report/draft.md && [ "$(grep -c "temporal split" report/draft.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --no-header` 요약에 `failed` 없음(아티팩트 존재 상태)
    - `uv run ruff format --check .` · `uv run ruff check .` 클린
    - `make smoke` 마지막 줄 `smoke: PASS`
    - 실측 curl 첫 줄이 `0 pop_v1 [` 로 시작하고 `trending 5` 로 끝난다; id 목록에 1·2·3 없음; 둘째 줄 `3 fallback_v1 []`(SUMMARY 에 원문)
    - `grep -c "(Day 1 실측, Phase 2 'Track A 정량 평가 기반')" report/draft.md` == 1; `grep -c "split_mode=holdout" report/draft.md` ≥ 2(괄호 줄 + bullet); `grep -c "temporal split" report/draft.md` == 0(W5); `grep -c "CC BY-SA 4.0" report/draft.md` ≥ 1; `grep -c "results/latest_states.csv" report/draft.md` ≥ 1; `grep -c "추천 제외 범위의 차이" report/draft.md` == 1; `grep -c "거의 같다" report/draft.md` == 0(측정 전 결론 금지)
    - draft.md 의 Recall@20 숫자 문자열이 `results/latest.csv` 2행 2열과 동일(`awk -F, 'NR==2{print $2}' results/latest.csv` 값이 draft.md 에 `Recall@20 = <값>` 으로 존재)
    - `git diff --numstat report/draft.md` == `2\t1`
    - `git log --oneline | head -1` 불변(커밋 0)
  </acceptance_criteria>
  <done>아티팩트가 있는 상태에서 전체 스위트·`make smoke` 통과(wave 3 전역 게이트), `/api/recommend` level 0 실측 3줄 확보, P4 괄호 줄 정정 + 실측 bullet 1줄(조건문 해석 + 각주). 커밋 없음.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (Should · EVAL-07 · 시간 부족 시 이 태스크만 생략): evaluation/figures.py → report/figures/eval_bar.png</name>
  <files>src/millie_rec/evaluation/figures.py, src/millie_rec/evaluation/__init__.py, tests/evaluation/test_figures.py, report/figures/eval_bar.png</files>
  <read_first>
    - .planning/ROADMAP.md "Phase 2" "Should 꼬리"(그림 없으면 PDF 는 표만) · .planning/REQUIREMENTS.md EVAL-07
    - .planning/phases/02-track-a/02-PATTERNS.md "evaluation/figures.py"(`matplotlib.use("Agg")`·`DIR_FIGURES`·gitignore 예외)
    - src/millie_rec/evaluation/report.py(`LATEST_FILE`·`LATEST_COLUMNS`·`COL_*` — 컬럼 이름을 재정의하지 않고 import)
    - src/millie_rec/evaluation/__init__.py(Plan 03 결과 — `__all__` 12개에 1개 추가)
    - .gitignore L20-21(`*.pdf` 무시, `!report/figures/*.png` 예외)
    - ../.claude/rules/report.md "그림 ③ 비교 막대그래프(evaluation 생성)"
  </read_first>
  <behavior>
    - `plot_eval_bar(latest_csv=tmp / "latest.csv", out=tmp / "eval_bar.png")` — tmp csv 는 헤더 + `pop,0.100,0.200,0.300,2,holdout,pop_v1` + `cf,0.150,0.250,0.350,2,holdout,cf_v1` → 반환 `Path == out`, `out.exists()`, `out.stat().st_size > 1000`, PNG 시그니처(`out.read_bytes()[:8] == b"\x89PNG\r\n\x1a\n"`)
    - 1행(pop 만)짜리 csv 로도 예외 없이 그린다
    - 기본 인자: `latest_csv=DIR_RESULTS / LATEST_FILE`, `out=DIR_FIGURES / "eval_bar.png"`(테스트는 기본 인자를 호출하지 않는다 — 실제 `report/figures/` 는 4-c 에서만)
  </behavior>
  <action>
    **4-a. RED — `tests/evaluation/test_figures.py`** 2건(`test_plot_eval_bar_writes_png_from_latest_csv(tmp_path)`, `test_plot_eval_bar_handles_single_row(tmp_path)`; 헤더 docstring `"""EVAL-07 비교 막대그래프 — 계약(경로 반환·PNG 시그니처) · 안전성(1행)."""`). 스텁 `figures.py`(`plot_eval_bar` 가 아무것도 쓰지 않고 `out` 반환) 로 `uv run pytest tests/evaluation/test_figures.py --no-header` → 2 failed, `AssertionError`(`out.exists()` 가 False).

    **4-b. GREEN — `src/millie_rec/evaluation/figures.py`**:
    ```python
    """EVAL-07 비교 막대그래프 — results/latest.csv → report/figures/eval_bar.png (evaluation.md 결과 파일). PDF 그림 ③."""

    from pathlib import Path

    import matplotlib

    matplotlib.use("Agg")  # 헤드리스 — 서버·CI 에서 디스플레이 없음. pyplot import 보다 먼저
    import matplotlib.pyplot as plt  # noqa: E402
    import pandas as pd

    from millie_rec.contracts import DIR_FIGURES, DIR_RESULTS
    from millie_rec.evaluation.report import COL_ILD, COL_NDCG, COL_RECALL, LATEST_FILE

    FIGURE_NAME = "eval_bar.png"
    METRICS = (COL_RECALL, COL_NDCG, COL_ILD)
    FIGSIZE = (7.0, 3.2)  # PDF 한 단 폭
    DPI = 200


    def plot_eval_bar(latest_csv: Path = DIR_RESULTS / LATEST_FILE, out: Path = DIR_FIGURES / FIGURE_NAME) -> Path:
        """variant 별 3지표 묶음 막대. 숫자는 latest.csv 그대로(재계산 없음)."""
        df = pd.read_csv(latest_csv)
        fig, ax = plt.subplots(figsize=FIGSIZE)
        df.set_index("variant")[list(METRICS)].plot.bar(ax=ax, rot=0, width=0.8)
        ax.set_ylim(0, 1)
        ax.set_ylabel("score")
        ax.set_title(f"Track A (Goodbooks-10k, {df['split_mode'].iloc[0]}, n_users={int(df['n_users'].iloc[0])})")
        ax.legend(loc="upper right", frameon=False)
        for c in ax.containers:
            ax.bar_label(c, fmt="%.3f", fontsize=7)
        fig.tight_layout()
        out.parent.mkdir(parents=True, exist_ok=True)
        fig.savefig(out, dpi=DPI)
        plt.close(fig)
        return out
    ```
    `evaluation/__init__.py` 에 `from millie_rec.evaluation.figures import plot_eval_bar` + `__all__` 에 `"plot_eval_bar"`(알파벳 위치) 추가 → 13개. `uv run pytest tests/evaluation --no-header` → Plan 03 의 18 + 2 = 20 passed. `uv run pytest tests/test_architecture.py --no-header` → 3 passed.

    **4-c. 실제 그림:** `uv run python -c "from millie_rec.evaluation import plot_eval_bar; print(plot_eval_bar())"` → `…/report/figures/eval_bar.png`. `ls -la report/figures/eval_bar.png` 크기 > 10KB. `git status --short` 에 `report/figures/eval_bar.png` 가 나타난다(`.gitignore` 예외 `!report/figures/*.png`). 그림을 열어 보는 것은 사람(Advisor) 몫 — 실행자는 파일 존재·크기·PNG 시그니처만.

    **4-d. 정리:** `uv run ruff format src/millie_rec/evaluation tests/evaluation && uv run ruff check src tests`. `uv run pytest --no-header 2>&1 | tail -3` → failed 0. 생략했다면 SUMMARY 에 "EVAL-07 생략 — PDF 는 표만(ROADMAP Should 꼬리)" 한 줄.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/evaluation tests/test_architecture.py --no-header && uv run ruff check src tests && test -f report/figures/eval_bar.png && uv run python -c "import sys; b=open('report/figures/eval_bar.png','rb').read(8); sys.exit(0 if b==b'\x89PNG\r\n\x1a\n' else 1)" && ls -la report/figures/eval_bar.png</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/evaluation/test_figures.py --no-header` → `2 passed`(RED 단계 `2 failed` + `AssertionError`)
    - `wc -l src/millie_rec/evaluation/figures.py` ≤ 150; `grep -n 'matplotlib.use("Agg")' src/millie_rec/evaluation/figures.py` 1행이며 `import matplotlib.pyplot` 줄보다 앞; `grep -n "from millie_rec.evaluation.report import" src/millie_rec/evaluation/figures.py` 1행(컬럼 상수 재정의 없음)
    - `grep -c '"plot_eval_bar"' src/millie_rec/evaluation/__init__.py` == 1; `uv run python -c "import millie_rec.evaluation as e; print(len(e.__all__))"` → `13`
    - `test -f report/figures/eval_bar.png`, 크기 > 10,000 바이트, 첫 8바이트 PNG 시그니처
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`; `uv run ruff check src tests` 클린
    - 생략 시: 위 항목 대신 SUMMARY 에 "EVAL-07 생략" 문장 1건과 `ls report/figures/` == `.gitkeep`
  </acceptance_criteria>
  <done>`results/latest.csv` 에서 `report/figures/eval_bar.png` 가 생성되고 테스트로 고정됐다(또는 명시적으로 생략됨). 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw(HTTPS) → `data/raw/goodbooks/` | 외부 네트워크 입력 ≈70MB(Plan 01 코드, 이 플랜이 실제 실행) |
| `results/`·`artifacts/serving/` → git(커밋 대상) | PDF 숫자·서빙 입력이 저장소에 들어간다 |
| `artifacts/popularity.json` → 서버 기동 | Task 3 에서 실제로 level 0 경로가 열린다 |
| `report/draft.md` | 사람이 읽는 원고 — 숫자 전사 오류 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-30 | Tampering (integrity) | `make data` 다운로드 | mitigate | HTTPS 상수 URL(Plan 01), `.part`→`replace`, 완료 후 규모 sanity(행 ≈5.98M·도서 10,000·`ts` 전부 결측)를 수용 기준으로 확인. 잘린 파일은 삭제 후 1회 재실행(루프 금지) |
| T-02-31 | Denial of Service | 재다운로드·장시간 eval | mitigate | 멱등 검사로 2회차 네트워크 0(수용 기준 "두 번째 `make data` 는 `downloaded` 로그 없음"). eval 5분 상한은 기록만 — 표본 축소로 D-01 을 어기지 않는다 |
| T-02-32 | Repudiation | 숫자 전사(`draft.md`) | mitigate | 숫자는 `latest.csv` 에서 문자열 그대로 복사, 수용 기준이 `awk` 로 csv 값과 draft.md 문자열 일치를 검사. json 에 `git_sha`·`seed`·`created_at` 동봉, 재현성 `diff` 로 확인 |
| T-02-33 | Information Disclosure | 커밋 대상 `results/`·`eval_table.json`·png | accept | 지표·variant·sha·태그 통계만, PII 없음. Goodbooks CC BY-SA 4.0 귀속(README). `data/raw`·`data/processed`·`popularity.json` 은 gitignore(대용량·파생물) |
| T-02-34 | Elevation of Privilege | 로컬 서버 포트 8010/8012 | accept | 로컬호스트 바인딩, 실측 후 `kill`. `.uvicorn.pid`·스크래치패드 pid 파일 정리 |
| T-02-35 | Information Disclosure | level 0 실제 응답 | accept | Goodbooks book_id·score 만. `model_version` 공개는 설계(인스펙터) |
| T-02-36 | Tampering | `NOISE_TAGS` 보충으로 ILD 조작 | mitigate | 보충은 "내용 무관 태그" 만·최대 1회·근거(상위 빈도 출력) SUMMARY 기록·장르 태그 금지 목록 명시. `tests/data` 통과 유지 |
</threat_model>

<verification>
- `data/raw/goodbooks/` 4파일 + `data/processed/` 2 parquet 존재, 두 번째 `make data` 네트워크 0
- `results/latest.csv` 2줄(`pop`, `n_users=2000`, `holdout`, `pop_v1`) · `latest_states.csv` 3줄(`n0`·`n20`) · `eval_*.json` 1개(D-08 키) · `artifacts/serving/eval_table.json`(rows+meta) · `artifacts/popularity.json`
- 재실행 `latest.csv` 바이트 동일
- `uv run pytest --no-header` failed 0(아티팩트 있는 상태) · `uv run ruff format --check src tests && uv run ruff check src tests` 클린
- `make smoke` PASS; 실측 `GET /api/recommend?seeds=1,2,3&k=5` → `fallback_level 0`·`pop_v1`·items 5(1·2·3 아님); 익명 → level 3
- `report/draft.md` P4 괄호 줄 `temporal split` → `random holdout(split_mode=holdout)` 정정 + bullet 1줄(조건문 해석 + 각주), 숫자 == csv
- (Should) `report/figures/eval_bar.png` PNG, `tests/evaluation/test_figures.py` 2 passed — 또는 명시적 생략
- 커밋 0; `git status --short` 에 `results/*`·`artifacts/serving/eval_table.json`·`report/draft.md`(·png) 는 나타나고 `data/raw`·`data/processed`·`artifacts/popularity.json` 은 나타나지 않는다
</verification>

<success_criteria>
- **EVAL-01** `make data` 실측: Goodbooks-10k 멱등 다운로드 + `interactions.parquet`·`books.parquet`
- **EVAL-02** 실측 json `split_mode=holdout`(ts 없음 → 거짓 temporal 표기 없음)
- **EVAL-05** `results/latest.csv`·`eval_<ts>.json`(`split_mode`·`n_users`·`seed`·`git_sha`) 에 `pop` 1행 — ROADMAP Success Criterion 1
- **EVAL-06** `results/latest_states.csv` `n0`·`n20` 두 행, 별도 `n_users` — ROADMAP Success Criterion 4
- ROADMAP Success Criterion 5: `pop` 주입 후 `/api/recommend` `fallback_level=0` 실측 + `uv run pytest`·`make smoke` PASS
- **EVAL-07**(Should) `report/figures/eval_bar.png` — 또는 명시적 생략(PDF 는 표만)
- Day 1 최소 종료 조건 "`pop` Recall@20 실측 1개"(../CLAUDE.md §5) 달성; `report/draft.md` P4 에 실측 1줄(D-17)
- CONTEXT D-01·D-04·D-06~D-10·D-15(NOISE_TAGS 확정)·D-17 이 파일·응답으로 증명됨
</success_criteria>

<output>
완료 후 `.planning/phases/02-track-a/02-06-SUMMARY.md` 를 만든다. 포함: `make data` 시간·크기·행 수 · `NOISE_TAGS` 보충 여부와 근거 · `make eval` stdout 원문과 `latest.csv`·`latest_states.csv` 전문 · json 메타 요약(`n_users`·`n_excluded`·`git_sha`·`elapsed_s`) · 재현성 `diff` 결과 · 실측 curl 3줄 · pytest 요약 줄·`make smoke` 마지막 줄 · `draft.md` 추가 bullet 원문 · EVAL-07 수행/생략 · 이상치 판단(없으면 "이상치 없음") · 변경 파일 목록(`git status --short`, 커밋 대상 vs gitignore 구분) · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다. `results/`·`eval_table.json`·png 커밋은 사용자 승인 후 Advisor" 한 줄.
</output>
