---
phase: 02-track-a
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/millie_rec/app/pipeline.py
  - src/millie_rec/app/cli.py
  - src/millie_rec/app/export.py         # B5: eval_table.json writer 분리(artifacts/serving/ 소유 = app export)
  - src/millie_rec/app/server.py
  - tests/app/test_pipeline.py
  - tests/app/test_cli.py
  - tests/app/test_export.py
  - tests/serving/test_smoke.py   # 소유자 = 02-05. monkeypatch 로 결정론적 level 3/level 0 단정(Task 3, B4)
autonomous: true
requirements: [EVAL-01, EVAL-05, EVAL-06]
lane: 조립(Advisor 영역 — GSD 실행자가 대행, Advisor 가 diff·테스트로 검증). 선례 = Phase 1 '로컬 서빙 스켈레톤'(.planning/phases/01-local-serving-skeleton/01-02-SUMMARY.md) 의 app/server.py(실행자 작성 → Advisor diff·테스트 검증), 오케스트레이터 결정 W3
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/**        # Plan 04 가 병렬로 수정 중 — 이 플랜은 serving 을 import 만
  - src/millie_rec/data/**
  - src/millie_rec/retrieval/**
  - src/millie_rec/evaluation/**
  - tests/test_architecture.py
  - Makefile
  - pyproject.toml
  - results/**                        # 테스트는 tmp_path 만. 실제 실행은 Plan 06
  - artifacts/**

must_haves:
  truths:
    - "build_pipelines() 는 artifacts/popularity.json 이 있으면 {'pop': PopPipeline} 을, 없거나 손상됐으면 {} 를 돌려주며 어느 경우에도 예외로 기동을 막지 않는다(D-10, local-run.md)"
    - "PopPipeline.recommend(user, k) 는 PopularityRetriever.retrieve 의 Candidate 를 ScoredItem(source='popularity', position=i, source_channels=('popularity',)) 으로 바꿔 돌려준다 — 비즈니스 규칙 0"
    - "python -m millie_rec.app.cli eval --variant all 이 등록 variant × 두 상태(n0·n20)를 평가해 results/latest.csv(pop 1행)·latest_states.csv(pop×2행)·eval_<ts>.json 을 쓰고, artifacts/serving/eval_table.json({rows, meta}) 과 artifacts/popularity.json 을 만든다 — 소형 합성 parquet 으로 네트워크 없이 검증"
    - "python -m millie_rec.app.cli data 는 download_goodbooks → build_goodbooks 를 순서대로 호출한다(유일한 네트워크 진입점)"
    - "app/server.py 는 pipelines=build_pipelines() 한 인자만 바뀌고 create_app 시그니처·StaticFiles 마지막 마운트는 그대로다"
  artifacts:
    - path: "src/millie_rec/app/pipeline.py"
      provides: "PopPipeline(contracts.Pipeline 구현 glue) · fit_pipelines(train) · build_pipelines(artifact)"
      exports: ["PopPipeline", "fit_pipelines", "build_pipelines"]
      max_lines: 150
    - path: "src/millie_rec/app/cli.py"
      provides: "argparse 서브커맨드 data · eval --variant all|pop|cf|hybrid|hybrid_div (+ --processed --out --artifacts --raw 경로 옵션, 기본 DIR_*)"
      exports: ["main"]
      max_lines: 150
    - path: "src/millie_rec/app/export.py"
      provides: "write_eval_table(results_dir, meta, n_users, serving_dir) — latest.csv 행 복사 → artifacts/serving/eval_table.json(D-09). app 소유"
      exports: ["write_eval_table", "EVAL_TABLE", "SERVING_SUBDIR"]
      max_lines: 150
    - path: "src/millie_rec/app/server.py"
      provides: "pipelines=build_pipelines() 주입"
      contains: "pipelines=build_pipelines()"
      max_lines: 30
    - path: "tests/app/test_pipeline.py"
      provides: "아티팩트 없음/손상 → {} · 정상 로드 → pop level 0 동작 · fit_pipelines 키"
      min_lines: 30
    - path: "tests/app/test_cli.py"
      provides: "합성 parquet e2e(latest.csv 헤더·pop 행·states 2행·json·eval_table.json·popularity.json) · data 서브커맨드 호출 순서 · 미등록 variant 종료"
      min_lines: 40
    - path: "tests/app/test_export.py"
      provides: "write_eval_table — rows 복사·meta 키·states 미포함(tmp_path)"
      min_lines: 15
  key_links:
    - from: "src/millie_rec/app/server.py"
      to: "src/millie_rec/app/pipeline.py"
      via: "create_app(pipelines=build_pipelines(), …)"
      pattern: "pipelines=build_pipelines\\(\\)"
    - from: "src/millie_rec/app/pipeline.py"
      to: "src/millie_rec/retrieval/__init__.py"
      via: "공개 표면 2단 import 만"
      pattern: "from millie_rec.retrieval import POP_ARTIFACT, PopularityRetriever"
    - from: "src/millie_rec/app/cli.py"
      to: "src/millie_rec/evaluation/__init__.py"
      via: "evaluate ×(variant × 상태) → write_results → latest.csv 를 읽어 eval_table.json 복사(D-09)"
      pattern: "write_results\\("
    - from: "src/millie_rec/app/export.py"
      to: "artifacts/serving/eval_table.json"
      via: "csv.DictReader(latest.csv) → {\"rows\": …, \"meta\": …}; cli.py 는 write_eval_table 호출만"
      pattern: "def write_eval_table"
---

<objective>
Phase 2 'Track A 정량 평가 기반'(.planning/ROADMAP.md "Phase 2: Track A 정량 평가 기반")의 조립(`app/`) 몫: wave 1 의 세 슬라이스 공개 표면을 이어 `make data`·`make eval` 이 실제로 동작하는 CLI(`app/cli.py`), `pop` variant dict 와 서버용 아티팩트 로더(`app/pipeline.py`), 그리고 `app/server.py` 의 `pipelines={}` 한 자리를 `build_pipelines()` 로 바꾼다. `artifacts/serving/eval_table.json` writer 는 `app/export.py`(CLAUDE.md §3 트리의 `app/export`, `artifacts/serving/` 소유 = app export) 에 둔다 — `cli.py` 가 155줄로 150 을 넘기 때문(checker B5).

Purpose: EVAL-01(`make data`)·EVAL-05(`make eval` → `results/`)·EVAL-06(두 상태 표) 의 **실행 진입점**과, ROADMAP Success Criterion 5(아티팩트 있으면 `/api/recommend` level 0)의 주입 배선(결정 'D-10 아티팩트 있으면 로드'(.planning/phases/02-track-a/02-CONTEXT.md)). `Makefile` 의 `data`·`eval` 타깃은 이미 `python -m millie_rec.app.cli …` 를 가리키므로 새 타깃은 없다.
Output: `app/pipeline.py`·`app/cli.py`·`app/export.py` 신규, `app/server.py` 1행 + import 1행, `tests/app/` 3파일(8건), `tests/serving/test_smoke.py` 에 `build_pipelines` monkeypatch 1줄 + level 0 조립 테스트 1건 추가(기존 단정 무변경). 실데이터·네트워크·`results/`·`artifacts/` 미접촉(전부 tmp_path). 커밋 0.

**planner 결정 — Pipeline 어댑터 위치 = `app/pipeline.py`:** `Candidate → ScoredItem` 변환은 "retriever 단독 variant 를 Pipeline 으로 보이게 하는" 조립 glue 이고 비즈니스 규칙이 없다. retrieval 에 두면 retrieval 이 `Pipeline` 의미(ScoredItem·position)를 알게 되고, Phase 4 가 `cf`·`hybrid`·`hybrid_div` 를 만드는 자리도 이 dict(아키텍처 01 §3-3 "variant dict") 이므로 한 파일에 모은다.

**planner 결정(checker B4 반영) — `tests/serving/test_smoke.py` 의 소유자는 이 플랜(02-05)이고, 서버 조립 테스트는 머신 상태(아티팩트 유무)에 의존하지 않게 `build_pipelines` 를 `monkeypatch` 로 대체한다:** 기존 `test_server_module_serves_demo_index_and_keeps_api_routes` 는 `monkeypatch.setattr("millie_rec.app.pipeline.build_pipelines", lambda: {})` 한 줄을 import 전에 추가해 **기존 `fallback_level == 3` 단정을 그대로** 결정론적으로 통과시키고, 새 테스트 `test_server_module_injects_pop_pipeline_when_build_pipelines_returns_pop` 이 `build_pipelines` 를 `{"pop": _FakePop()}` 으로 대체해 level 0·`pop_v1` 을 단정한다. `artifacts/popularity.json` 이 있든 없든 12건 전부 통과. 02-04 는 이 파일을 건드리지 않는다(같은 wave 2 — 소유 충돌 방지).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-track-a/02-CONTEXT.md
@.planning/phases/02-track-a/02-PATTERNS.md
@.planning/phases/02-track-a/02-01-SUMMARY.md
@.planning/phases/02-track-a/02-02-SUMMARY.md
@.planning/phases/02-track-a/02-03-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/app/server.py
@src/millie_rec/app/__init__.py
@src/millie_rec/data/__init__.py
@src/millie_rec/retrieval/__init__.py
@src/millie_rec/evaluation/__init__.py
@tests/conftest.py
@tests/serving/test_smoke.py
@tests/test_architecture.py
@Makefile
@../.claude/rules/architecture.md
@../.claude/rules/simplicity.md
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/local-run.md

<project_rules_inline>
**실행 명령:** 오직 `uv run …`. 테스트 `uv run pytest <대상> --no-header`. 맨 `pytest`·`python3` 금지. 테스트 먼저(각 태스크 안에서 RED → GREEN, Red 는 `AssertionError`), `pytest.skip` 금지.

**star 의존(`tests/test_architecture.py` L41-42):** `app/*.py` 는 다른 슬라이스의 **공개 표면 2단 경로**만 import — `from millie_rec.retrieval import PopularityRetriever` ✅ / `from millie_rec.retrieval.popularity import …` ❌. `app` 내부끼리(`millie_rec.app.pipeline`)는 자유. 누구도 `millie_rec.app` 을 import 하지 않는다(테스트 제외).

**app 은 조립만(`../.claude/rules/architecture.md` "Service / Application Layer"):** 비즈니스 규칙을 `app/` 에 쓰지 않는다 — 슬라이스 공개 함수를 순서대로 부르고 결과를 넘긴다. `argparse` 는 `app/cli.py` 하나. 설정 프레임워크·환경변수 해석 금지(경로는 `DIR_*` 기본값 + CLI 인자).

**산출물 소유권:** `artifacts/serving/eval_table.json` 은 **app 이 쓴다**(D-09; `latest.csv` 행을 그대로 복사). `artifacts/popularity.json` 은 retrieval 의 `save()` 를 **호출만**(경로 인자로 `--artifacts` 반영). `results/` 는 `write_results(out_dir=…)` 호출만. `data/processed` 읽기는 `load_*` 공개 함수만.

**서버 기동 원칙(`../.claude/rules/local-run.md`):** `build_pipelines()` 는 import 시점 부작용 없이 파일 존재 검사 + 작은 json 로드만. parquet·pandas·sklearn 을 서버 경로에서 로드하지 않는다(아키텍처 01 §8 "서빙 pandas 미로드"). 아티팩트가 없거나 깨져도 `{}` 로 기동(level 3).

**간결함:** 파일 ≤150줄(`server.py` ≤30). 함수 우선 — 클래스는 `PopPipeline`(`contracts.Pipeline` 만족)만. `logging` 기본. 예외 계층 없음.

**변경 금지:** `contracts.py`·`serving/**`(Plan 04 가 병렬 수정 중 — 충돌 방지)·`data/**`·`retrieval/**`·`evaluation/**`·`Makefile`·`pyproject.toml`. `tests/serving/test_smoke.py` 는 objective 에 적은 단정 1건만.

**커밋 금지:** `git add`·`git commit` 실행 안 함. 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다.

**완료 기준(D-17, wave 병렬 보정 — checker B3):** 플랜 안에서는 **자기 경로만** — `uv run ruff format --check src/millie_rec/app tests/app tests/serving/test_smoke.py && uv run ruff check src/millie_rec/app tests/app tests/serving/test_smoke.py` 클린 · `uv run pytest tests/app tests/serving/test_smoke.py tests/test_architecture.py --no-header` failed 0. 전체 트리 `uv run pytest --no-header`·`ruff check .`·`make smoke`(아티팩트 없으면 level 3) 는 **wave 2(04·05) 종료 후 Advisor 가 1회** 실행(02-06-PLAN.md `<wave_gate>`). docstring·주석·문자열은 의미 변경 없이 100자 이내로 줄바꿈해도 된다(ruff E501).
</project_rules_inline>

<interfaces>
<!-- wave 1 이 만든 공개 표면. SUMMARY 01~03 과 실제 __init__.py 로 이름을 재확인한다(아래는 계획값). -->

From src/millie_rec/data/__init__.py (Plan 01):
```python
BOOKS_FILE = "books.parquet"; INTERACTIONS_FILE = "interactions.parquet"; RAW_SUBDIR = "goodbooks"
K_HISTORY = 20; N_TEST_USERS = 2000; STATE_N0 = "n0"; STATE_NK = "n20"; TEST_FRAC = 0.2
MIN_USER_INTERACTIONS = 5; MIN_ITEM_INTERACTIONS = 5
def download_goodbooks(raw_dir: Path = DIR_RAW / "goodbooks") -> list[Path]
def build_goodbooks(raw_dir: Path = ..., out_dir: Path = DIR_PROCESSED) -> tuple[Path, Path]
def load_interactions(path: Path = DIR_PROCESSED / INTERACTIONS_FILE) -> pd.DataFrame
def load_books(path: Path = DIR_PROCESSED / BOOKS_FILE) -> pd.DataFrame
def filter_min_interactions(df, *, min_user=5, min_item=5) -> pd.DataFrame
@dataclass(frozen=True) class Split: train; test; split_mode
def split(df, *, test_frac=TEST_FRAC, seed=SEED) -> Split
def select_test_users(train, test, *, n=N_TEST_USERS, n_seeds=N_ONBOARD_SEEDS, seed=SEED) -> tuple[tuple[int, ...], int]   # (users, n_excluded)
@dataclass(frozen=True, slots=True) class OnboardingStates: n0: tuple[UserState, ...]; n_k: tuple[UserState, ...]
def mask_onboarding(train, users, *, n=N_ONBOARD_SEEDS, k_history=K_HISTORY, seed=SEED) -> OnboardingStates
def relevant_sets(test) -> dict[int, frozenset[int]]
```

From src/millie_rec/retrieval/__init__.py (Plan 02):
```python
POP_ARTIFACT = DIR_ARTIFACTS / "popularity.json"; TOP_N = 1000
class PopularityRetriever:  # name="pop"; fit(train) -> self; retrieve(user, k) -> list[Candidate]; save(path=POP_ARTIFACT, *, top_n=TOP_N) -> Path; @classmethod load(path=POP_ARTIFACT)
class ContentVectors:  # __init__(books: pd.DataFrame, text_col="tags"); vectors(book_ids) -> np.ndarray
```

From src/millie_rec/evaluation/__init__.py (Plan 03):
```python
LATEST_FILE = "latest.csv"; STATES_FILE = "latest_states.csv"
EvalUser = tuple[UserState, frozenset[int]]
def evaluate(pipeline: Pipeline, users: Sequence[EvalUser], vectors: ItemVectors, *, k_recall=K_RECALL, k_rank=K_RANK) -> EvalResult
@dataclass(frozen=True, slots=True) class RunMeta: split_mode; test_frac; n_excluded; n_onboard_seeds; k_history; min_user_interactions; min_item_interactions; elapsed_s; dataset="goodbooks-10k"; seed=SEED; git_sha=None
def write_results(rows: Sequence[EvalResult], states: Sequence[tuple[str, EvalResult]], meta: RunMeta, out_dir: Path = DIR_RESULTS) -> Path   # json 경로
def git_sha() -> str | None
```

From src/millie_rec/contracts.py:
```python
DIR_RAW, DIR_PROCESSED, DIR_ARTIFACTS, DIR_RESULTS; VARIANTS = ("pop", "cf", "hybrid", "hybrid_div"); K_RECALL = 20; K_RANK = 10; N_ONBOARD_SEEDS = 5
class Pipeline(Protocol): name: str; def recommend(self, user: UserState, k: int) -> list[ScoredItem]
@dataclass(frozen=True, slots=True) class ScoredItem: book_id; score; source=None; …; position=None; source_channels=()
@dataclass(frozen=True, slots=True) class Candidate: book_id; source; score
```

From src/millie_rec/app/server.py (현재 전문 12줄 — L10 만 바뀐다):
```python
app = create_app(pipelines={}, fallback=GlobalPopularFallback(None), db=Database(resolve_db_path()))
```

From Makefile (변경 금지 — 이미 이 진입점을 가리킨다):
```
data:  uv run python -m millie_rec.app.cli data
eval:  uv run python -m millie_rec.app.cli eval --variant all
```

`artifacts/serving/eval_table.json` 형태(D-09, Phase 5 `GET /api/showcase` 입력):
```json
{"rows": [<latest.csv 행 dict 그대로 — 값은 csv 문자열>], "meta": {"dataset": "goodbooks-10k", "split_mode": "holdout", "n_users": 2000, "k_recall": 20, "k_rank": 10, "seed": 42, "git_sha": "abc1234", "created_at": "2026-…"}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/pipeline.py — PopPipeline · fit_pipelines · build_pipelines (+ tests/app/test_pipeline.py 4건, 테스트 먼저)</name>
  <files>src/millie_rec/app/pipeline.py, tests/app/test_pipeline.py</files>
  <read_first>
    - .planning/phases/02-track-a/02-CONTEXT.md — D-10(아티팩트 있으면 로드·없으면 {}·기동 시 parquet 금지), D-11(dict 키 = VARIANTS 이름), "Claude's Discretion — Pipeline 어댑터 위치"
    - .planning/phases/02-track-a/02-PATTERNS.md "app/pipeline.py" 절(형태·import 허용 형태)
    - .planning/phases/02-track-a/02-02-SUMMARY.md + src/millie_rec/retrieval/__init__.py(실제 export 이름 확인 — `POP_ARTIFACT`·`PopularityRetriever`)
    - src/millie_rec/retrieval/popularity.py(`retrieve`·`save`·`load` 시그니처 — 읽기만)
    - src/millie_rec/serving/fallback.py L25-34(`ScoredItem` 생성 형태 모사 — 읽기만)
    - src/millie_rec/contracts.py L211-232(`Candidate`·`ScoredItem`), L341-346(`Pipeline`)
    - tests/test_architecture.py L41-42(app 은 2단 경로만)
    - tests/conftest.py(`interactions` fixture)
  </read_first>
  <behavior>
    - `build_pipelines(tmp_path / "none.json") == {}`(파일 없음, 예외 없음)
    - `p.write_text("not json")` → `build_pipelines(p) == {}`; `p.write_text('{"name": "pop"}')`(items 없음) → `{}`; 둘 다 `caplog` 에 `ERROR` 레벨 기록 1건 이상
    - `PopularityRetriever([(1, 3.0), (2, 2.0), (3, 1.0)]).save(p)` 후 `pipes = build_pipelines(p)`: `set(pipes) == {"pop"}`, `pipes["pop"].name == "pop"`, `items = pipes["pop"].recommend(UserState(user_id=None, explicit_seeds=(1,)), 2)` → `[i.book_id for i in items] == [2, 3]`, `[i.position for i in items] == [0, 1]`, 전부 `source == "popularity"`·`source_channels == ("popularity",)`·`isinstance(i, ScoredItem)`, 점수 `[2.0, 1.0]`
    - `fit_pipelines(interactions)` → `set(...) == {"pop"}`, 키 전부 `in VARIANTS`, `recommend(UserState(None), 5)` 길이 5
  </behavior>
  <action>
    **1-a. RED — `tests/app/test_pipeline.py` 먼저**(디렉터리 `tests/app/` 신설, `__init__.py` 불필요). 헤더 docstring `"""app/pipeline.py — 계약(Pipeline·ScoredItem) · 정확성(Candidate→ScoredItem·seen 제외) · 안전성(아티팩트 없음·손상 → {} 기동)."""`. import: `from millie_rec.app.pipeline import build_pipelines, fit_pipelines`, `from millie_rec.retrieval import PopularityRetriever`, `from millie_rec.contracts import VARIANTS, ScoredItem, UserState`. 4건: `test_build_pipelines_empty_when_artifact_missing`, `test_build_pipelines_empty_and_logged_when_artifact_malformed(tmp_path, caplog)`, `test_build_pipelines_loads_pop_and_recommend_excludes_seen(tmp_path)`, `test_fit_pipelines_keys_are_variants_and_recommend_returns_k(interactions)`. RED 확인: 모듈이 없으면 `ModuleNotFoundError` 라 RED 가 아니므로 **먼저 스텁** `app/pipeline.py`(시그니처만: `build_pipelines` 는 `{"stub": None}`, `fit_pipelines` 는 `{}`, `PopPipeline.recommend` 는 `[]`) 를 두고 `uv run pytest tests/app/test_pipeline.py --no-header` → 4 failed, 전부 `AssertionError`.

    **1-b. GREEN — `src/millie_rec/app/pipeline.py`** 전문:
    ```python
    """variant 조립 — dict 하나(아키텍처 01 §3-3).

    Phase 2 는 pop 만. Phase 4 가 cf·hybrid·hybrid_div 를 같은 dict 에 추가한다.
    """

    import logging
    from pathlib import Path
    from typing import TYPE_CHECKING

    from millie_rec.contracts import VARIANTS, Pipeline, ScoredItem, UserState
    from millie_rec.retrieval import POP_ARTIFACT, PopularityRetriever

    if TYPE_CHECKING:
        import pandas as pd

    log = logging.getLogger(__name__)
    POP = VARIANTS[0]  # "pop" — 이름의 정본은 contracts.VARIANTS


    class PopPipeline:
        """contracts.Pipeline 구현 — retriever 단독 variant 의 glue(Candidate → ScoredItem)."""

        name = POP

        def __init__(self, retriever: PopularityRetriever) -> None:
            self.retriever = retriever

        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            return [
                ScoredItem(
                    book_id=c.book_id, score=c.score, source=c.source, position=i, source_channels=(c.source,)
                )
                for i, c in enumerate(self.retriever.retrieve(user, k))
            ]


    def fit_pipelines(train: "pd.DataFrame") -> dict[str, Pipeline]:
        """make eval 용: train 으로 새로 fit. 키는 contracts.VARIANTS 이름 그대로."""
        return {POP: PopPipeline(PopularityRetriever().fit(train))}


    def build_pipelines(artifact: Path = POP_ARTIFACT) -> dict[str, Pipeline]:
        """서버 기동(D-10): 아티팩트 있으면 pop, 없거나 손상되면 {} — 기동을 막지 않는다."""
        if not artifact.exists():
            log.info("no pop artifact at %s — serving level 3 only", artifact)
            return {}
        try:
            return {POP: PopPipeline(PopularityRetriever.load(artifact))}
        except (OSError, ValueError, KeyError, TypeError):  # json 손상·키 누락·형 불일치
            log.exception("pop artifact unreadable at %s — serving level 3 only", artifact)
            return {}
    ```
    (`json.JSONDecodeError` 는 `ValueError` 하위. `PopularityRetriever.load` 가 `int()`/`float()` 로 강제 변환하므로 형 불일치는 `ValueError`/`TypeError`.)

    **1-c. GREEN 확인:** `uv run pytest tests/app/test_pipeline.py --no-header` → 4 passed. `uv run pytest tests/test_architecture.py --no-header` → 3 passed(app 이 `millie_rec.retrieval` 2단 경로만).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/app/test_pipeline.py tests/test_architecture.py --no-header && wc -l src/millie_rec/app/pipeline.py && grep -c "millie_rec\.retrieval\." src/millie_rec/app/pipeline.py || true</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/app/test_pipeline.py --no-header` → `4 passed`(RED 단계 출력에 `4 failed` + `AssertionError`)
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `wc -l src/millie_rec/app/pipeline.py` ≤ 150
    - `grep -n "^from millie_rec.retrieval import POP_ARTIFACT, PopularityRetriever" src/millie_rec/app/pipeline.py` 1행; `grep -c "millie_rec\.retrieval\." src/millie_rec/app/pipeline.py` == 0(3단 경로 없음)
    - `grep -n "^def build_pipelines(artifact: Path = POP_ARTIFACT) -> dict\[str, Pipeline\]" src/millie_rec/app/pipeline.py` 1행; `grep -n "^def fit_pipelines(train" src/millie_rec/app/pipeline.py` 1행; `grep -n "class PopPipeline" src/millie_rec/app/pipeline.py` 1행
    - `grep -n "if not artifact.exists()" src/millie_rec/app/pipeline.py` 1행; `grep -n "log.exception" src/millie_rec/app/pipeline.py` 1행
    - `grep -c "pandas\|read_parquet\|sklearn" src/millie_rec/app/pipeline.py` == 1(TYPE_CHECKING 블록의 `import pandas as pd` 만)
    - `awk 'length > 100' src/millie_rec/app/pipeline.py tests/app/test_pipeline.py | wc -l` == 0(E501)
  </acceptance_criteria>
  <done>`build_pipelines` 가 없음/손상/정상 세 경로에서 기동을 막지 않고, `PopPipeline` 이 seen 제외 `ScoredItem` 을 낸다. 4 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app/export.py(eval_table.json writer) + app/cli.py — data · eval 서브커맨드 (+ tests/app/test_export.py 1건·test_cli.py 3건, 합성 parquet e2e, 테스트 먼저)</name>
  <files>src/millie_rec/app/export.py, src/millie_rec/app/cli.py, tests/app/test_export.py, tests/app/test_cli.py</files>
  <read_first>
    - .planning/phases/02-track-a/02-CONTEXT.md — D-01(2,000명·n_excluded), D-04(두 상태), D-06~D-09(세 결과 파일 + eval_table.json 형태·app 소유), "Claude's Discretion — CLI·git sha·아티팩트", "Integration Points — app/cli.py 흐름"
    - .planning/phases/02-track-a/02-PATTERNS.md "app/cli.py" 절(argparse analog·eval 흐름·`fit_pipelines` 필요성)
    - .planning/phases/02-track-a/02-01-SUMMARY.md · 02-03-SUMMARY.md + `src/millie_rec/data/__init__.py`·`src/millie_rec/evaluation/__init__.py`(실제 export 이름 확인)
    - src/millie_rec/evaluation/report.py(`RunMeta` 필드·`write_results` 반환값 — 읽기만)
    - src/millie_rec/data/onboarding.py(`OnboardingStates.n0`/`.n_k` 속성 이름 — 읽기만)
    - scripts/build_millie_catalog.py L248-260(argparse 관례 — 읽기만)
    - Makefile L7-8, L24-25(호출 형태 고정: `python -m millie_rec.app.cli data` / `… eval --variant all`)
  </read_first>
  <behavior>
    - 합성 데이터: `rng = np.random.default_rng(SEED)`; 유저 30명 × 아이템 40개 중 무작위 15개(중복 없음), `rating = rng.choice([4.0, 5.0], size)`, `ts = None`, `event = "rating"` → `tmp/processed/interactions.parquet`; books 40행 `{book_id: 1..40, title: f"B{i}", authors: "A", image_url: "", average_rating: 4.0, ratings_count: 10, original_publication_year: 2000, categories: [] , subcategories: [], tags: <"fantasy magic"|"history war"|"romance drama"|"science space" 순환>}` → `tmp/processed/books.parquet`
    - `main(["eval", "--variant", "all", "--processed", str(proc), "--out", str(out), "--artifacts", str(art)])` 실행 후:
      - `(out / "latest.csv").read_text().splitlines()[0] == "variant,recall@20,ndcg@10,ild@10,n_users,split_mode,model_version"`; 2줄째 `startswith("pop,")`, `endswith(",30,holdout,pop_v1")`(30 유저 전부 적격: train 12 긍정 ≥5, test 3 긍정 ≥1); recall 값 `0.0 < x <= 1.0`
      - `(out / "latest_states.csv")` 3줄: 헤더, `pop,n0,…,30`, `pop,n20,0.000,0.000,0.000,0`(seeds 외 이력 12−5=7 < 20 → n20 모집단 0 — 표에 `n_users=0` 으로 남는다; 예외 없음)
      - `sorted(out.glob("eval_*.json"))` 길이 1; json `split_mode == "holdout"`, `n_users == {"n0": 30, "n20": 0}`, `n_excluded == 0`, `k_history == 20`, `test_frac == 0.2`, `min_user_interactions == 5`, `"git_sha" in d`
      - `(art / "serving" / "eval_table.json")` 존재; `d["rows"][0]["variant"] == "pop"`, `d["meta"]["n_users"] == 30`, `d["meta"]["split_mode"] == "holdout"`, `set(d["meta"]) >= {"dataset", "split_mode", "n_users", "k_recall", "k_rank", "seed", "git_sha", "created_at"}`, `"states" not in d`(D-09)
      - `(art / "popularity.json")` 존재, `json.loads(...)["name"] == "pop"`
    - `main(["eval", "--variant", "cf", "--processed", …])` → `pytest.raises(SystemExit)`(미등록 variant — Phase 4 전)
    - `main(["data", "--raw", str(tmp / "raw"), "--processed", str(tmp / "p")])` 는 monkeypatch 된 `millie_rec.app.cli.download_goodbooks`·`build_goodbooks` 를 그 순서로 각 1회 호출(`calls == ["download", "build"]`), 인자 경로가 그대로 전달 — 네트워크 0
  </behavior>
  <action>
    **2-a. RED — `tests/app/test_export.py` 와 `tests/app/test_cli.py` 먼저.** `test_export.py` 1건 `test_write_eval_table_copies_latest_rows_and_meta_without_states(tmp_path)`: tmp `results/latest.csv` 에 헤더 + `pop,0.100,0.200,0.300,2,holdout,pop_v1` 를 쓰고 `meta = RunMeta(split_mode="holdout", test_frac=0.2, n_excluded=0, n_onboard_seeds=5, k_history=20, min_user_interactions=5, min_item_interactions=5, elapsed_s=0.0, git_sha="abc1234")`; `out = write_eval_table(results_dir, meta, 2, serving_dir=tmp / "serving")` → **첫 단정 `out.exists()`**, `d = json.loads(out.read_text())`, `set(d) == {"rows", "meta"}`, `d["rows"] == [{"variant": "pop", "recall@20": "0.100", "ndcg@10": "0.200", "ild@10": "0.300", "n_users": "2", "split_mode": "holdout", "model_version": "pop_v1"}]`(csv 문자열 그대로), `set(d["meta"]) == {"dataset", "split_mode", "n_users", "k_recall", "k_rank", "seed", "git_sha", "created_at"}`, `d["meta"]["n_users"] == 2`. 스텁 `app/export.py`(시그니처만, 아무것도 쓰지 않고 `serving_dir / "eval_table.json"` 반환) → AssertionError.
    `tests/app/test_cli.py`: 헤더 docstring `"""app/cli.py — 합성 parquet e2e(make eval 의 파일 계약) · data 서브커맨드 호출 순서 · 미등록 variant 종료. 네트워크·실데이터 0."""`. 헬퍼 `_write_processed(proc: Path)` 가 behavior 의 두 parquet 을 쓴다(컬럼은 `contracts.COL_*`, ts 컬럼은 `pd.Series([None] * n, dtype="object")`). 3건: `test_eval_end_to_end_on_synthetic_parquet(tmp_path)`, `test_eval_unregistered_variant_exits(tmp_path)`, `test_data_calls_download_then_build(monkeypatch, tmp_path)`. 스텁 `app/cli.py`(`main(argv=None)` 이 `parse_args` 만 하고 아무것도 안 씀; `download_goodbooks`·`build_goodbooks` 를 모듈 이름으로 import 해 두어 monkeypatch 대상이 존재하게) 를 두고 `uv run pytest tests/app/test_cli.py --no-header` → 3 failed, `AssertionError`/`DID NOT RAISE`(e2e 는 `latest.csv` 부재 → **첫 단정을 `(out / "latest.csv").exists()` 로** 두어 FileNotFoundError 가 아닌 AssertionError 로).

    **2-b. GREEN ① — `src/millie_rec/app/export.py`** 전문(B5 — `artifacts/serving/` 소유 = app export; star 의존: `contracts` + `millie_rec.evaluation` 공개 이름만):
    ```python
    """artifacts/serving/ export — app 소유(architecture.md 산출물 표). Phase 2 는 eval_table.json 만(D-09)."""

    import csv
    import json
    from datetime import UTC, datetime
    from pathlib import Path

    from millie_rec.contracts import DIR_ARTIFACTS, K_RANK, K_RECALL
    from millie_rec.evaluation import LATEST_FILE, RunMeta

    EVAL_TABLE = "eval_table.json"  # Phase 5 GET /api/showcase 입력(D-09)
    SERVING_SUBDIR = "serving"
    DIR_SERVING = DIR_ARTIFACTS / SERVING_SUBDIR  # Dockerfile COPY 대상, 커밋 대상


    def write_eval_table(
        results_dir: Path, meta: RunMeta, n_users: int, serving_dir: Path = DIR_SERVING
    ) -> Path:
        """latest.csv 행을 그대로 복사(재계산 없음). states 표는 넣지 않는다 — PDF P3 전용(D-09)."""
        with (results_dir / LATEST_FILE).open(encoding="utf-8", newline="") as f:
            rows = list(csv.DictReader(f))
        payload = {
            "rows": rows,
            "meta": {
                "dataset": meta.dataset,
                "split_mode": meta.split_mode,
                "n_users": n_users,
                "k_recall": K_RECALL,
                "k_rank": K_RANK,
                "seed": meta.seed,
                "git_sha": meta.git_sha,
                "created_at": datetime.now(UTC).isoformat(timespec="seconds"),
            },
        }
        serving_dir.mkdir(parents=True, exist_ok=True)
        out = serving_dir / EVAL_TABLE
        out.write_text(json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8")
        return out
    ```
    (≈40줄.)

    **2-b. GREEN ② — `src/millie_rec/app/cli.py`** 전문(ruff 가 import 를 정렬·감쌈; export 분리 후 **≈125줄** — checker 실측 155줄에서 `_eval_table` 16줄 + `csv`·`json`·`datetime` import 3줄 + `EVAL_TABLE`·`SERVING_SUBDIR` 2줄 감소, print/RunMeta 줄바꿈으로 +6):
    ```python
    """CLI 진입점 — argparse 하나(simplicity.md).

    data: Goodbooks 다운로드 → parquet · eval: 등록 variant × 두 상태 → results/ + eval_table.json (아키텍처 01 §3-6).
    """

    import argparse
    import logging
    from pathlib import Path
    from time import perf_counter

    from millie_rec.app.export import SERVING_SUBDIR, write_eval_table
    from millie_rec.app.pipeline import fit_pipelines
    from millie_rec.contracts import DIR_ARTIFACTS, DIR_PROCESSED, DIR_RAW, DIR_RESULTS, K_RANK, K_RECALL, N_ONBOARD_SEEDS, VARIANTS
    from millie_rec.data import (
        BOOKS_FILE, INTERACTIONS_FILE, K_HISTORY, MIN_ITEM_INTERACTIONS, MIN_USER_INTERACTIONS, RAW_SUBDIR, STATE_N0, STATE_NK, TEST_FRAC,
        build_goodbooks, download_goodbooks, filter_min_interactions, load_books, load_interactions, mask_onboarding,
        relevant_sets, select_test_users, split,
    )
    from millie_rec.evaluation import RunMeta, evaluate, git_sha, write_results
    from millie_rec.retrieval import POP_ARTIFACT, ContentVectors

    log = logging.getLogger(__name__)
    ALL = "all"


    def cmd_data(args: argparse.Namespace) -> None:
        """유일한 네트워크 진입점(make data). 멱등."""
        raw = download_goodbooks(args.raw)
        ip, bp = build_goodbooks(args.raw, args.processed)
        print(f"raw={len(raw)} files @ {args.raw} → {ip.name} · {bp.name} @ {args.processed}")


    def cmd_eval(args: argparse.Namespace) -> None:
        """load → filter → split → 표본 → 두 상태 → 등록 variant 평가 → results/ → eval_table.json → pop 아티팩트."""
        t0 = perf_counter()
        inter = filter_min_interactions(load_interactions(args.processed / INTERACTIONS_FILE))
        books = load_books(args.processed / BOOKS_FILE)
        sp = split(inter)
        users, n_excluded = select_test_users(sp.train, sp.test)
        states = mask_onboarding(sp.train, users)
        rel = relevant_sets(sp.test)
        n0 = [(u, rel[u.user_id]) for u in states.n0]
        nk = [(u, rel[u.user_id]) for u in states.n_k]
        vectors = ContentVectors(books)
        pipes = fit_pipelines(sp.train)
        wanted = list(pipes) if args.variant == ALL else [args.variant]
        missing = [v for v in wanted if v not in pipes]
        if missing:
            raise SystemExit(f"variant {missing} 미등록 — 등록된 variant: {list(pipes)} (Phase 4 에서 추가)")
        rows, state_rows = [], []
        for name in VARIANTS:
            if name not in wanted:
                continue
            r0 = evaluate(pipes[name], n0, vectors)
            rk = evaluate(pipes[name], nk, vectors)
            rows.append(r0)
            state_rows += [(STATE_N0, r0), (STATE_NK, rk)]
            print(
                f"{name}: recall@{K_RECALL}={r0.recall:.3f} ndcg@{K_RANK}={r0.ndcg:.3f} "
                f"ild@{K_RANK}={r0.ild:.3f} n_users={r0.n_users} | "
                f"{STATE_NK}: n_users={rk.n_users} recall={rk.recall:.3f}"
            )
        meta = RunMeta(
            split_mode=sp.split_mode,
            test_frac=TEST_FRAC,
            n_excluded=n_excluded,
            n_onboard_seeds=N_ONBOARD_SEEDS,
            k_history=K_HISTORY,
            min_user_interactions=MIN_USER_INTERACTIONS,
            min_item_interactions=MIN_ITEM_INTERACTIONS,
            elapsed_s=round(perf_counter() - t0, 1),
            git_sha=git_sha(),
        )
        jp = write_results(rows, state_rows, meta, out_dir=args.out)
        tp = write_eval_table(args.out, meta, len(n0), serving_dir=args.artifacts / SERVING_SUBDIR)
        pop = pipes.get(VARIANTS[0])
        ap = pop.retriever.save(args.artifacts / POP_ARTIFACT.name) if pop is not None else None
        print(f"split_mode={sp.split_mode} n_users={len(n0)}/{len(nk)} n_excluded={n_excluded}")
        print(f"→ {jp.name} · {tp} · {ap}")


    def main(argv: list[str] | None = None) -> None:
        ap = argparse.ArgumentParser(prog="millie_rec.app.cli", description="Track A 데이터·평가 진입점(make data · make eval)")
        sub = ap.add_subparsers(dest="cmd", required=True)
        d = sub.add_parser("data", help="Goodbooks-10k 멱등 다운로드 + parquet 캐시")
        d.add_argument("--raw", type=Path, default=DIR_RAW / RAW_SUBDIR)
        d.add_argument("--processed", type=Path, default=DIR_PROCESSED)
        d.set_defaults(func=cmd_data)
        e = sub.add_parser("eval", help="등록 variant × 두 상태 평가 → results/")
        e.add_argument("--variant", choices=[ALL, *VARIANTS], default=ALL)
        e.add_argument("--processed", type=Path, default=DIR_PROCESSED)
        e.add_argument("--out", type=Path, default=DIR_RESULTS)
        e.add_argument("--artifacts", type=Path, default=DIR_ARTIFACTS)
        e.set_defaults(func=cmd_eval)
        args = ap.parse_args(argv)
        logging.basicConfig(level=logging.INFO, format="%(levelname)s %(name)s: %(message)s")
        args.func(args)


    if __name__ == "__main__":
        main()
    ```
    주의: `--raw` 기본값은 `DIR_RAW / RAW_SUBDIR`(Plan 01 이 `RAW_SUBDIR="goodbooks"` 를 data 공개 표면에 노출) — `"goodbooks"` 리터럴을 cli 에 다시 쓰지 않는다. 만약 실제 `data/__init__.py` 에 `RAW_SUBDIR` 가 없으면 이 플랜에서 `data/` 를 고치지 말고 `--raw` 기본값을 `None` 으로 두고 `None` 이면 `download_goodbooks()`·`build_goodbooks()` 를 인자 없이 호출한다(기본 인자에 맡김). 어느 쪽을 택했는지 SUMMARY 에 적는다. `args.processed / INTERACTIONS_FILE` 처럼 파일 이름은 data 공개 상수만 쓴다.

    **2-c. GREEN 확인:** `uv run pytest tests/app --no-header` → 8 passed(Task 1 의 4 + export 1 + cli 3). `wc -l src/millie_rec/app/cli.py src/millie_rec/app/export.py` 각 ≤150(cli ≈125·export ≈40 — 150 을 넘으면 print 요약 줄을 1개로 합치는 것 외의 삭제는 하지 않는다). e2e 는 30초 안에 끝나야 한다(`--durations=3` 로 확인). `uv run pytest tests/test_architecture.py --no-header` → 3 passed. `ls results/ artifacts/` 가 플랜 시작 전과 동일(테스트는 tmp_path 만).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/app tests/test_architecture.py --no-header --durations=3 && wc -l src/millie_rec/app/cli.py src/millie_rec/app/export.py && ls results/ artifacts/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/app/test_cli.py --no-header` → `3 passed`; `uv run pytest tests/app/test_export.py --no-header` → `1 passed`(RED 단계 각 `3 failed`·`1 failed`, `AssertionError`/`DID NOT RAISE`)
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`; `grep -c "millie_rec\.\(data\|retrieval\|evaluation\|serving\)\." src/millie_rec/app/cli.py` == 0(3단 경로 없음)
    - `wc -l src/millie_rec/app/cli.py` ≤ 150; `wc -l src/millie_rec/app/export.py` ≤ 150; `awk 'length > 100' src/millie_rec/app/cli.py src/millie_rec/app/export.py tests/app/test_cli.py tests/app/test_export.py | wc -l` == 0(E501)
    - `grep -n 'sub.add_parser("data"' src/millie_rec/app/cli.py` 1행; `grep -n 'sub.add_parser("eval"' src/millie_rec/app/cli.py` 1행; `grep -n 'choices=\[ALL, \*VARIANTS\]' src/millie_rec/app/cli.py` 1행; `grep -n 'if __name__ == "__main__":' src/millie_rec/app/cli.py` 1행
    - `grep -n '^EVAL_TABLE = "eval_table.json"' src/millie_rec/app/export.py` 1행; `grep -q "def write_eval_table" src/millie_rec/app/export.py`; `grep -c '"states"' src/millie_rec/app/export.py` == 0(D-09: states 표 미포함); `grep -n "csv.DictReader" src/millie_rec/app/export.py` 1행; `grep -c "^import csv\|^import json\|csv\.\|json\." src/millie_rec/app/cli.py` == 0(writer 는 export.py 만 — docstring 의 `eval_table.json` 언급은 허용); `grep -n "^from millie_rec.app.export import SERVING_SUBDIR, write_eval_table" src/millie_rec/app/cli.py` 1행
    - `grep -rn "millie_rec\.\(data\|retrieval\|evaluation\|serving\)\." src/millie_rec/app/export.py` → 출력 없음(공개 표면 2단만)
    - `grep -n "evaluate(pipes\[name\], n0, vectors)" src/millie_rec/app/cli.py` 1행; `grep -n "evaluate(pipes\[name\], nk, vectors)" src/millie_rec/app/cli.py` 1행(두 상태 D-04·D-07)
    - `grep -n "retriever.save(" src/millie_rec/app/cli.py` 1행(`artifacts/` 쓰기는 retrieval 의 save 호출만)
    - `grep -c "import argparse" src/millie_rec/app/*.py` 합 == 1(cli.py 만)
    - `ls results/` → `.gitkeep` 만; `ls artifacts/` → `.gitkeep serving` 만(테스트는 tmp_path)
  </acceptance_criteria>
  <done>`make eval` 의 파일 계약(latest.csv·latest_states.csv·eval_<ts>.json·eval_table.json·popularity.json)이 합성 parquet 으로 네트워크 없이 증명됐다. `tests/app` 8 passed, cli.py·export.py ≤150. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 3: app/server.py 주입 1행 + test_smoke 서버 조립 테스트 monkeypatch(결정론) + level 0 조립 테스트 1건 + 자기 경로 ruff·pytest</name>
  <files>src/millie_rec/app/server.py, tests/serving/test_smoke.py</files>
  <read_first>
    - src/millie_rec/app/server.py 전문(12줄 — L6 import 뒤에 1행, L10 인자 1개만 바뀐다)
    - tests/serving/test_smoke.py L15-27(import 블록), L33-39(`_Boom` — `_FakePop` 을 같은 형태로 추가), L152-166(`test_server_module_serves_demo_index_and_keeps_api_routes` — monkeypatch 1줄 추가 대상, 단정은 무변경)
    - .planning/phases/01-local-serving-skeleton/01-CONTEXT.md D-09(시그니처 불변)·D-11(StaticFiles 마지막)
    - .planning/phases/02-track-a/02-PATTERNS.md "app/server.py (MODIFY)" 절(import 시점 부작용 금지 — 테스트가 `sys.modules.pop` 후 재import)
    - Makefile L30-40(`serve`·`smoke`) · ../.claude/rules/local-run.md
  </read_first>
  <action>
    **3-a. `src/millie_rec/app/server.py`** — 두 줄만 바꾼다. import 블록에 `from millie_rec.app.pipeline import build_pipelines` 를 추가(ruff `I`: `millie_rec.app.pipeline` 이 `millie_rec.contracts` 보다 앞), L10 을 다음으로:
    ```python
    app = create_app(pipelines=build_pipelines(), fallback=GlobalPopularFallback(None), db=Database(resolve_db_path()))
    ```
    주석 L8 은 `# Phase 2 ✅ pipelines=build_pipelines()(아티팩트 있으면 pop, D-10), Phase 3 → catalog, Phase 5 → neighbors·book_stats·state.` 로 갱신. 다른 줄 무변경(StaticFiles 마운트가 마지막). `os.environ`·CORS·라우트 추가 없음.

    **3-b. `tests/serving/test_smoke.py`** — objective 의 planner 결정(B4)대로 **결정론적**으로 만든다. 기존 단정은 한 글자도 바꾸지 않는다(삭제 0줄).
    ① import 블록 `from millie_rec.contracts import (…)` 에 `FALLBACK_PERSONALIZED`·`MODEL_VERSION_SUFFIX` 를 알파벳 위치에 추가.
    ② `_Boom` 아래에 가짜 pop 을 추가(02-04 `test_recommend_level0.py` 의 `_FakePop` 과 같은 형태 — 파일이 달라 중복 정의 허용):
    ```python
    class _FakePop:
        """seen 을 뺀 1..39 상위 k — build_pipelines 대체용(D-10 배선 검증)."""

        name = "pop"

        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            ids = [b for b in range(1, 40) if b not in user.seen][:k]
            return [ScoredItem(book_id=b, score=float(40 - b), source="popularity") for b in ids]
    ```
    ③ 기존 `test_server_module_serves_demo_index_and_keeps_api_routes` 의 `sys.modules.pop(...)` **바로 앞**에 한 줄 추가 — `server.py` 가 import 시점에 `build_pipelines()` 를 호출하므로 모듈 재import 전에 대체해야 한다:
    ```python
        # Phase 2 D-10: 아티팩트 유무(머신 상태)에 의존하지 않도록 build_pipelines 를 대체 — level 3 경로
        monkeypatch.setattr("millie_rec.app.pipeline.build_pipelines", lambda: {})
    ```
    기존 단정 `rec.json()["fallback_level"] == FALLBACK_GLOBAL_POP` 그대로.
    ④ 그 아래에 새 테스트 1건 추가:
    ```python
    def test_server_module_injects_pop_pipeline_when_build_pipelines_returns_pop(
        tmp_path: Path, monkeypatch
    ):
        """app.server:app — build_pipelines() 가 pop 을 주면 /api/recommend 는 level 0(D-10 배선)."""
        monkeypatch.setenv(ENV_DATA_DIR, str(tmp_path))
        monkeypatch.setattr("millie_rec.app.pipeline.build_pipelines", lambda: {"pop": _FakePop()})
        sys.modules.pop("millie_rec.app.server", None)
        server = importlib.import_module("millie_rec.app.server")
        with TestClient(server.app) as c:
            rec = c.get("/api/recommend", params={"seeds": "1,2,3", "k": 5})
            anon = c.get("/api/recommend")
        assert rec.status_code == 200 and rec.json()["fallback_level"] == FALLBACK_PERSONALIZED
        assert rec.json()["model_version"] == "pop" + MODEL_VERSION_SUFFIX
        assert [i["book_id"] for i in rec.json()["items"]] == [4, 5, 6, 7, 8]
        assert anon.status_code == 200 and anon.json()["fallback_level"] == FALLBACK_GLOBAL_POP  # D-13
    ```
    (level 0 분기 자체는 02-04 가 만든다 — 02-04 가 아직 안 끝났으면 이 새 테스트는 `assert 3 == 0` 으로 실패한다. 그 경우 SUMMARY 에 "02-04 대기" 로 적고 wave 2 게이트에서 재확인. 02-04 가 끝났으면 즉시 통과.)
    다른 10건·fixture·`_app`·`_Boom`·`_FakeCatalog` 무변경. `git diff --numstat tests/serving/test_smoke.py` 의 **삭제 수 == 0**.

    **3-c. 검증:** `uv run pytest tests/serving/test_smoke.py --no-header` → 12 passed(02-04 완료 시; 미완료면 11 passed + 새 테스트 1 failed 를 SUMMARY 에 기록). 두 결과 모두 `artifacts/popularity.json` 유무와 무관해야 한다. `uv run pytest tests/test_architecture.py --no-header` → 3 passed(`server.py` 는 `millie_rec.app.pipeline`(app 내부)·`millie_rec.serving`(2단)만). 기동 무부작용 확인: `uv run python -c "import sys, millie_rec.app.server; print('pandas' in sys.modules, 'sklearn' in sys.modules)"` → `False False`(아티팩트 없을 때; `retrieval.__init__` 이 `content.py` 를 import 하지만 sklearn 은 메서드 안에서만 import 되고 pandas 는 TYPE_CHECKING — Plan 02 REFACTOR 가 이를 보장. `True` 가 나오면 Plan 02 결과를 확인하고 SUMMARY 에 기록, 이 플랜에서는 고치지 않는다).

    **3-d. 정리(자기 경로만 — checker B3):** `uv run ruff format src/millie_rec/app tests/app tests/serving/test_smoke.py && uv run ruff check src/millie_rec/app tests/app tests/serving/test_smoke.py`. `uv run pytest tests/app tests/serving/test_smoke.py tests/test_architecture.py --no-header 2>&1 | tail -3` → `failed 0`(02-04 미완료 시 새 테스트 1건만 예외). **전역 게이트는 이 플랜에서 돌리지 않는다:** 전체 트리 `uv run pytest --no-header`·`uv run ruff check .`·`make smoke`(고정 포트 8010 + 공유 `.uvicorn.pid`) 는 wave 2 의 두 플랜(04·05)이 모두 끝난 뒤 Advisor 가 1회 실행한다(02-06-PLAN.md `<wave_gate>`) — 02-04 가 `serving/api.py` 를 병렬로 바꾸는 중이라 이 시점의 전역 결과는 신뢰할 수 없다.

    **3-e. SUMMARY 준비:** `git diff --stat -- src/millie_rec/app tests/app tests/serving/test_smoke.py`, 각 태스크 RED/GREEN 요약 줄, `wc -l`, `git diff --numstat tests/serving/test_smoke.py`, planner 결정 3개(어댑터 위치·export.py 분리·smoke 조립 테스트 monkeypatch 결정론화) 근거, `--raw` 기본값 처리 방식, "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다".
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/app tests/app tests/serving/test_smoke.py && uv run ruff check src/millie_rec/app tests/app tests/serving/test_smoke.py && uv run pytest tests/app tests/serving/test_smoke.py tests/test_architecture.py --no-header 2>&1 | tail -3 && uv run python -c "import sys, millie_rec.app.server; print('pandas' in sys.modules, 'sklearn' in sys.modules)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^app = create_app(pipelines=build_pipelines(), fallback=GlobalPopularFallback(None), db=Database(resolve_db_path()))" src/millie_rec/app/server.py` 1행; `grep -n "^from millie_rec.app.pipeline import build_pipelines" src/millie_rec/app/server.py` 1행; `grep -n 'app.mount("/", StaticFiles(directory=ROOT / "demo", html=True)' src/millie_rec/app/server.py` 1행이며 `app = create_app` 줄보다 뒤
    - `wc -l src/millie_rec/app/server.py` ≤ 30; `grep -c "CORSMiddleware\|os.environ\|@app\.\|millie_rec.serving\." src/millie_rec/app/server.py` == 0
    - `grep -c 'monkeypatch.setattr("millie_rec.app.pipeline.build_pipelines"' tests/serving/test_smoke.py` == 2; `grep -c "POP_ARTIFACT.exists()" tests/serving/test_smoke.py` == 0(머신 상태 의존 없음); `grep -c "def test_" tests/serving/test_smoke.py` == 12; `grep -n "def test_server_module_injects_pop_pipeline_when_build_pipelines_returns_pop" tests/serving/test_smoke.py` 1행; `git diff --numstat tests/serving/test_smoke.py` 의 **삭제 수 == 0**
    - `uv run pytest tests/serving/test_smoke.py --no-header` → `12 passed`(02-04 완료 시; 미완료 시 `11 passed, 1 failed` 이고 실패는 새 테스트 1건만 — SUMMARY 기록); `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `uv run ruff format --check src/millie_rec/app tests/app tests/serving/test_smoke.py` · `uv run ruff check src/millie_rec/app tests/app tests/serving/test_smoke.py` 클린; `awk 'length > 100' src/millie_rec/app/*.py tests/app/*.py tests/serving/test_smoke.py | wc -l` == 0
    - `uv run pytest tests/app tests/serving/test_smoke.py tests/test_architecture.py --no-header` → `failed 0`(위 예외 1건 외)
    - (전역 게이트 — 이 플랜의 acceptance 아님) 전체 `uv run pytest --no-header`·`ruff check .`·`make smoke` 는 wave 2 종료 후 Advisor 1회(02-06-PLAN.md `<wave_gate>`)
    - `git diff --stat -- src/millie_rec/contracts.py Makefile pyproject.toml src/millie_rec/data src/millie_rec/retrieval src/millie_rec/evaluation` → 출력 없음(이 플랜 범위 밖; `serving/` 은 02-04 가 병렬로 바꾸므로 보지 않는다)
    - `git log --oneline | head -1` 불변(커밋 0)
  </acceptance_criteria>
  <done>서버가 `build_pipelines()` 결과에 따라 level 0/3 을 자동 선택하는 배선이 붙었고, 두 경로가 아티팩트 유무와 무관한 monkeypatch 테스트로 고정됐다. 자기 경로 ruff·pytest 통과. 전역 게이트는 wave 2 종료 후 Advisor. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `artifacts/popularity.json`(디스크) → `build_pipelines` → 서버 기동 | 손상·부재 가능한 파일을 기동 경로에서 읽는다 |
| CLI 인자(`--raw`·`--processed`·`--out`·`--artifacts`) → 파일 쓰기 | 운영자(개발자) 입력. 요청 입력 아님 |
| `make data` → GitHub raw(HTTPS) | 외부 네트워크(Plan 01 `download_goodbooks` 가 구현, cli 는 호출만) |
| `cli eval` → `results/`·`artifacts/serving/`·`artifacts/` | PDF 숫자·서빙 입력 파일 쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-24 | Denial of Service | `build_pipelines` 기동 실패 | mitigate | 부재 → `{}` + `log.info`; `OSError`/`ValueError`(JSONDecodeError 포함)/`KeyError`/`TypeError` → `log.exception` + `{}`. 서버는 항상 뜬다(level 3). 테스트 `test_build_pipelines_empty_and_logged_when_artifact_malformed` |
| T-02-25 | Tampering | 아티팩트 내용 → 추천 결과 | accept | 로컬/컨테이너 파일 시스템 소유자만 쓸 수 있다. `json.loads` 만(코드 실행 없음), 값은 `int/float` 강제. 요청 입력이 경로·내용에 닿지 않음 |
| T-02-26 | Elevation of Privilege | CLI 경로 인자로 임의 위치 쓰기 | accept | 개발자 로컬 도구(`make data`/`make eval`), 기본값은 `contracts.DIR_*`. 서버 프로세스는 cli 를 실행하지 않는다 |
| T-02-27 | Repudiation | `eval_table.json` 이 `latest.csv` 와 어긋남 | mitigate | `latest.csv` 를 `csv.DictReader` 로 **읽어 복사**(재계산·수기 없음), `meta.git_sha`·`created_at` 동봉. 테스트 `test_eval_end_to_end_on_synthetic_parquet` 가 rows[0].variant 일치 단정 |
| T-02-28 | Information Disclosure | `eval_table.json`(커밋·서빙 대상) | accept | 지표 숫자·variant·sha 만. PII 없음. Goodbooks CC BY-SA 4.0 귀속은 README |
| T-02-29 | Tampering | `make data` 네트워크 | transfer | Plan 01 `download_goodbooks`(HTTPS 상수·멱등·`.part`→`replace`) 가 담당. cli 는 인자 전달만 |
</threat_model>

<verification>
- `uv run pytest tests/app --no-header` → 8 passed
- `uv run pytest tests/serving/test_smoke.py --no-header` → 12 passed(monkeypatch 1줄 + 새 테스트 1건, 기존 단정 삭제 0)
- `uv run pytest tests/test_architecture.py --no-header` → 3 passed
- `uv run ruff format --check src/millie_rec/app tests/app tests/serving/test_smoke.py && uv run ruff check src/millie_rec/app tests/app tests/serving/test_smoke.py` 클린
- `wc -l src/millie_rec/app/pipeline.py src/millie_rec/app/cli.py src/millie_rec/app/export.py` ≤150, `server.py` ≤30
- `uv run python -c "import sys, millie_rec.app.server; print('pandas' in sys.modules, 'sklearn' in sys.modules)"` → `False False`
- (wave 2 종료 후 Advisor 1회) `uv run pytest --no-header` → failed 0 · `uv run ruff check .` · `make smoke` → `smoke: PASS`
- `results/`·`artifacts/` 미접촉(테스트는 tmp_path), 커밋 0
</verification>

<success_criteria>
- **EVAL-01** 진입점: `python -m millie_rec.app.cli data`(= `make data`) 가 `download_goodbooks → build_goodbooks` 를 부른다(실측 다운로드는 Plan 06)
- **EVAL-05**·**EVAL-06** 진입점: `python -m millie_rec.app.cli eval --variant all`(= `make eval`) 이 등록 variant × 두 상태를 평가해 `latest.csv`·`latest_states.csv`·`eval_<ts>.json`·`eval_table.json`·`popularity.json` 을 만든다 — 합성 parquet e2e 테스트로 파일 계약 고정
- ROADMAP Success Criterion 5 의 배선: `server.py` 가 `build_pipelines()` 를 주입, 아티팩트 없으면 Phase 1 과 동일 level 3(`local-run.md` 유지)
- CONTEXT 결정 D-09(eval_table.json 형태·states 미포함·app 소유)·D-10(아티팩트 있으면 로드·없으면 {}·parquet 미로드)·D-11(dict 키 = VARIANTS)·"Claude's Discretion — CLI·어댑터 위치·git sha" 가 코드·테스트에 존재
- planner 결정 3개(어댑터 = `app/pipeline.py`, `eval_table.json` writer = `app/export.py`(B5), smoke 서버 조립 테스트 monkeypatch 결정론화(B4)) 가 SUMMARY 에 근거와 함께 기록
</success_criteria>

<output>
완료 후 `.planning/phases/02-track-a/02-05-SUMMARY.md` 를 만든다. 포함: 태스크별 RED/GREEN 요약 줄(AssertionError 발췌) · 파일별 요지·줄 수 · `git diff --numstat tests/serving/test_smoke.py` · `False False` 출력 · planner 결정 3개와 근거 · "`eval_table.json.rows` 값은 csv 문자열(Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md) `/api/showcase` 스키마가 float 을 기대하면 그때 변환)" · `--raw` 기본값 처리 방식 · 변경 파일 목록(`git diff --stat -- src/millie_rec/app tests/app tests/serving/test_smoke.py` + `git status --short -- src/millie_rec/app tests/app`) · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
