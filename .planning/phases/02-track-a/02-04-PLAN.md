---
phase: 02-track-a
plan: 04
type: tdd
wave: 2
depends_on: ["02-01", "02-02", "02-03"]   # 파일 충돌은 없다(serving 만). wave 2 인 이유: CONTEXT "Worker 분할" 순서 유지 + 전체 스위트 기준선을 wave 1 결과 위에서 확인
files_modified:
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/api.py
  - tests/serving/test_recommend_level0.py
autonomous: true
requirements: [EVAL-05]   # ROADMAP Phase 2 Success Criterion 5 — "pop 파이프라인 주입 후 /api/recommend 가 fallback_level=0"
lane: Serving (Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/fallback.py
  - src/millie_rec/serving/db.py
  - src/millie_rec/app/**
  - tests/serving/test_smoke.py
  - tests/test_architecture.py
  - Makefile

must_haves:
  truths:
    - "pipelines={'pop': FakePop} 주입 + GET /api/recommend?seeds=1,2,3&k=5 → 200, fallback_level=0, model_version='pop_v1', rows[0].row_id='trending', items 5개(book_id ∉ {1,2,3}), rows[0].items 와 items 의 book_id 순서가 같다, user_state_weights 전부 0"
    - "seeds 가 없는 익명 요청은 pipelines 가 있어도 level 3(model_version=fallback_v1, items=[]) 이다(D-13)"
    - "model 쿼리가 없으면 등록된 것 중 VARIANTS 순서상 마지막이 기본 variant 다(D-11); model 이 VARIANTS 에는 있지만 미등록이면 422 도 KeyError 도 아닌 기본 variant 로 200"
    - "파이프라인이 예외를 던지면 500 이 아니라 level 3 200 이며 응답 본문에 예외 문자열이 없다"
    - "pipelines={} 인 기존 동작(level 3, /health model_version=null) 은 그대로다 — tests/serving/test_smoke.py 의 serving 단독 10건(`-k 'not server_module'`) 통과. 서버 조립 테스트 1건과 그 파일의 소유자는 02-05(B4)"
  artifacts:
    - path: "src/millie_rec/serving/compose.py"
      provides: "build_response(items, *, model_version, level, k, t0, context) — trending 1행 + level 0 items 평탄화 · default_variant(D-11) · new_rec_id · ZERO_WEIGHTS. serving/__init__ 에 미노출"
      exports: ["build_response", "default_variant", "new_rec_id", "ZERO_WEIGHTS"]
      max_lines: 150
    - path: "src/millie_rec/serving/api.py"
      provides: "recommend 핸들러 level 0 분기(_personalized_response; default_variant 는 compose.py) — create_app·_fallback_response 시그니처 불변"
      exports: ["create_app"]
      max_lines: 150
    - path: "tests/serving/test_recommend_level0.py"
      provides: "계약·정확성·안전성 7건(가짜 Pipeline)"
      min_lines: 60
  key_links:
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/compose.py"
      via: "level 0·level 3 모두 build_response 로만 RecommendResponse 를 만든다(이중 구현 금지)"
      pattern: "from millie_rec.serving.compose import build_response"
    - from: "src/millie_rec/serving/api.py"
      to: "pipelines[name].recommend"
      via: "seeds 있고 등록 variant 있을 때만 호출, try/except 로 level 3 강하"
      pattern: "_personalized_response\\("
    - from: "src/millie_rec/serving/compose.py"
      to: "src/millie_rec/serving/fallback.py"
      via: "trending_row(items) 재사용(D-12) — 새 행 상수 없음"
      pattern: "from millie_rec.serving.fallback import trending_row"
---

<objective>
Phase 2 'Track A 정량 평가 기반'(.planning/ROADMAP.md "Phase 2: Track A 정량 평가 기반")의 Serving 몫: `GET /api/recommend` 에 **level 0 분기**를 TDD 한 사이클로 붙인다. `seeds` 가 있고 `pipelines` 에 등록 variant 가 있으면 그 파이프라인을 호출해 `fallback_level=0`·`model_version=f"{variant}{MODEL_VERSION_SUFFIX}"`·`trending` 1행 + `items` 상위 k 평탄화로 응답한다(결정 'D-11 기본 variant'·'D-12 level 0 응답 형태'·'D-13 익명은 level 3'(.planning/phases/02-track-a/02-CONTEXT.md)).

Purpose: ROADMAP Success Criterion 5 — Plan 05 가 `artifacts/popularity.json` 을 로드해 `pipelines={"pop": …}` 를 주입하면 `make smoke` 가 `fallback_level: 0` 을 본다. "추천 API 장애 ≠ 메인 장애"(Phase 1 '로컬 서빙 스켈레톤'(.planning/phases/01-local-serving-skeleton/01-CONTEXT.md D-13))는 level 0 경로에서도 유지: 파이프라인 예외 → level 3 200.
Output: 신규 `serving/compose.py`(응답 조립 — Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md)이 Must 5행을 같은 파일에 증분) + `serving/api.py` 수정(≤150줄 유지) + `tests/serving/test_recommend_level0.py` 7건. 커밋 0.

**planner 결정 — 왜 `compose.py` 를 지금 만드는가:** `api.py` 는 128줄이다. level 0 분기(≈10줄)·기본 variant 헬퍼(≈5줄)·개인화 응답 헬퍼(≈12줄)를 넣으면 `RecommendResponse` 조립을 두 번 쓰게 되어 160줄을 넘는다(파일 ≤150줄, `../.claude/rules/simplicity.md`). `RecommendResponse` 조립을 `compose.py::build_response` 하나로 빼면 level 3 도 같은 함수를 쓰고 `api.py` 는 ≈140줄로 남는다. 아키텍처 01 §9-3 serving 파일 목록에 `compose.py` 가 있고, Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md) 이 `fallback.py` 를 level 3 만으로 만들고 "level 1·2 는 Phase 5 증분" 이라 한 것과 같은 방식이다. **오케스트레이터 승인 조건(W2) 2개:** ① Phase 1 헬퍼 `_fallback_response(…, *, level=FALLBACK_GLOBAL_POP)` 의 시그니처는 **그대로 유지**한다(최소 변경 원칙 — kwarg 제거 없음) ② `build_response`·`default_variant` 는 `serving/__init__.py` `__all__` 에 **노출하지 않는다**(serving 내부 모듈). Must 5행·행 순서·`compose.py` 의 정본은 Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md) 에 남는다(D-12 "행 순서 정본은 Phase 5 compose.py") — Phase 2 는 그 파일에 응답 조립 함수 2개를 먼저 두는 것뿐이다.

**planner 결정 — `/health.model_version`·`artifacts_loaded_at` 은 Phase 2 에서 채우지 않는다(Phase 5 로):** 스키마 optional 이라 계약 영향 0. `api.py` 150줄 예산이 빡빡하고, Phase 5 가 `nearline_last_run` 으로 `/health` 를 어차피 다시 연다. 기존 테스트 `test_health_returns_200_and_parses_health_out` 의 `model_version is None` 단정도 그대로 유효.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-track-a/02-CONTEXT.md
@.planning/phases/02-track-a/02-PATTERNS.md
@.planning/phases/01-local-serving-skeleton/01-CONTEXT.md
@src/millie_rec/contracts.py
@src/millie_rec/serving/api.py
@src/millie_rec/serving/fallback.py
@src/millie_rec/serving/schemas.py
@src/millie_rec/serving/__init__.py
@tests/serving/test_smoke.py
@tests/test_architecture.py
@../.claude/rules/serving.md
@../.claude/rules/python-tdd.md
@../.claude/rules/architecture.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 오직 `uv run pytest <대상> --no-header`. 맨 `pytest`·`python3` 금지. **RED 는 `AssertionError`/`E  assert` 만 인정** — 이 플랜은 `api.py` 가 이미 있으므로 스텁 없이도 새 테스트가 `fallback_level 3 != 0` 류의 AssertionError 로 떨어진다(정직한 RED). `pytest.skip` 금지. **`tests/serving/test_smoke.py` 는 이 플랜이 수정하지 않는다(소유자 02-05, B4). serving 단독 10건(`-k "not server_module"`)이 그대로 통과해야 한다**(pipelines={} 동작 불변).

**star 의존(`tests/test_architecture.py` L37-42):** `serving/*.py` 는 `millie_rec.contracts` 와 `millie_rec.serving.<module>`(모듈 경로 — `millie_rec.serving` 패키지 import 는 순환) 만. `data`·`retrieval`·`evaluation`·`app` import 금지 — 파이프라인 객체는 `create_app(pipelines=…)` 로 주입받는다.

**계약 freeze:** `contracts.py`·`serving/schemas.py` 무변경. `create_app(pipelines, fallback, *, catalog, db, neighbors, book_stats, state)` 시그니처 불변(Phase 1 D-09). 직렬화는 `RecommendOut.from_contract` 만(로직 중복 금지). `fallback.py`(`trending_row`·`GlobalPopularFallback`) 무변경 — 재사용만.

**간결함(`../.claude/rules/simplicity.md`):** `api.py` ≤150줄(현재 128). 헬퍼는 함수. 예외 계층 없음, `log.exception` + 안전 기본값(level 3). 요청받지 않은 리팩토링 금지 — 이 플랜이 `_fallback_response` 를 건드리는 유일한 이유는 `RecommendResponse` 조립을 `build_response` 로 공유하기 위함(줄 수 예산).

**보안(`../.claude/rules/serving.md`, ASVS L1):** 파이프라인 예외는 `except Exception` 으로 잡아 level 3 200 — 스택 트레이스·예외 메시지를 응답에 넣지 않는다(`log.exception` 만). `model` 은 기존대로 `VARIANTS` 밖이면 422; `VARIANTS` 안이지만 미등록이면 `KeyError` 없이 기본 variant. `seeds` 파싱은 기존 `_parse_seeds`(422) 그대로.

**커밋 금지:** `git add`·`git commit` 실행 안 함. RED·GREEN pytest 출력을 SUMMARY `## TDD Gate Compliance` 에 붙인다. 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다. Codex 리뷰: `contracts.py`·`schemas.py` 무변경이라 필수 아님(`../.claude/rules/codex-review.md` 표) — Advisor 판단.

**완료 기준(D-17, wave 병렬 보정 — checker B3):** 플랜 안에서는 **자기 경로만** — `uv run ruff format --check src/millie_rec/serving tests/serving && uv run ruff check src/millie_rec/serving tests/serving` 클린 · `uv run pytest tests/serving -k "not server_module" tests/test_architecture.py --no-header` failed 0(서버 조립 테스트 `test_server_module_serves_demo_index_and_keeps_api_routes` 는 `app/` 과 `test_smoke.py` 를 수정하는 02-05 소유 — B4). 전체 트리 `uv run pytest --no-header`·`ruff check .`·`make smoke` 는 **wave 2(04·05) 종료 후 Advisor 가 1회** 실행(02-06-PLAN.md `<wave_gate>`). 아티팩트가 아직 없으면 level 3 — level 0 실측은 Plan 06. **docstring·주석·문자열은 의미 변경 없이 100자 이내로 줄바꿈해도 된다(ruff E501)** — 아래 "주석·docstring 삭제 금지" 는 삭제 금지일 뿐 줄바꿈은 허용.
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 재탐색 불필요. -->

From src/millie_rec/contracts.py:
```python
VARIANTS = ("pop", "cf", "hybrid", "hybrid_div")
MODEL_VERSION_SUFFIX = "_v1"
MODEL_VERSION_FALLBACK = "fallback_v1"
FALLBACK_PERSONALIZED, FALLBACK_CACHE, FALLBACK_SEGMENT_POP, FALLBACK_GLOBAL_POP = 0, 1, 2, 3

@dataclass(frozen=True, slots=True)
class Recommendation:
    book_id: int
    score: float
    title: str | None = None
    reason: str | None = None

@dataclass(frozen=True, slots=True)
class RecommendResponse:
    items: tuple[Recommendation, ...]
    model_version: str
    fallback_level: int
    latency_ms: float
    recommendation_id: str | None = None
    preference_snapshot_id: str | None = None
    rows: tuple[Row, ...] = ()
    latency_breakdown: dict[str, float] = field(default_factory=dict)
    user_state_weights: dict[str, float] = field(default_factory=dict)
    user_key: str | None = None; cell: str | None = None; dedup_removed: int = 0; context: str | None = None

class Pipeline(Protocol):
    name: str
    def recommend(self, user: UserState, k: int) -> list[ScoredItem]: ...
```

From src/millie_rec/serving/fallback.py (무변경, 재사용):
```python
SOURCE_POPULARITY = "popularity"
def trending_row(items: Sequence[ScoredItem]) -> Row:   # row_id="trending", title="지금 많이 읽는 책", purpose="fallback", channel_mix={"popularity": n}
```

From src/millie_rec/serving/schemas.py L164-187 (무변경):
```python
class RecommendOut(_Strict):
    ... recommendation_id, model_version, fallback_level(0..3), latency_ms, rows, items, forced, context, user_state_weights, latency_breakdown ...
    @classmethod
    def from_contract(cls, r: RecommendResponse, *, forced: bool = False) -> "RecommendOut": ...  # asdict 그대로
```

From src/millie_rec/serving/api.py (현재 전문은 파일에서 읽는다 — L28-29 상수, L32-33 `_new_rec_id`, L50-76 `_fallback_response`, L110-126 `recommend` 핸들러가 변경 대상)

From tests/serving/test_smoke.py L33-39, L55-60 (복사해 쓸 패턴):
```python
class _Boom:
    name = "boom"
    def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
        raise RuntimeError("boom")

def _app(tmp_path: Path, fallback=None):
    return create_app(pipelines={}, fallback=fallback or GlobalPopularFallback(None), db=Database(tmp_path / DB_FILENAME))
```

level 0 기대 응답(CONTEXT "Specific Ideas"):
```json
{"recommendation_id":"rec_…","model_version":"pop_v1","fallback_level":0,"forced":false,
 "latency_ms":1.2,"latency_breakdown":{"total":1.2},"user_state_weights":{"alpha":0.0,"beta":0.0,"gamma":0.0},
 "items":[{"book_id":…,"score":…}],
 "rows":[{"row_id":"trending","title":"지금 많이 읽는 책","purpose":"fallback","items":[…5개, book_id ∉ {1,2,3}]}]}
```
</interfaces>
</context>

<feature>
  <name>GET /api/recommend level 0 분기 — seeds + 등록 variant 면 파이프라인 호출, 예외·익명은 level 3</name>
  <files>src/millie_rec/serving/compose.py, src/millie_rec/serving/api.py, tests/serving/test_recommend_level0.py</files>
  <behavior>
    앱 = `create_app(pipelines={"pop": _FakePop()}, fallback=GlobalPopularFallback(None), db=Database(tmp_path / DB_FILENAME))`, `with TestClient(app) as c:`. `_FakePop.name = "pop"`, `recommend(user, k)` → `[b for b in range(1, 40) if b not in user.seen][:k]` 를 `ScoredItem(book_id=b, score=float(40 - b), source="popularity", position=i, source_channels=("popularity",))` 로.
    - `GET /api/recommend?seeds=1,2,3&k=5` → 200; `RecommendOut.model_validate(json)` 성공; `fallback_level == 0`; `model_version == "pop_v1"`; `forced is False`; `len(items) == 5`; `[i["book_id"] for i in items] == [4, 5, 6, 7, 8]`; `rows` 길이 1; `rows[0]["row_id"] == "trending"`, `["title"] == "지금 많이 읽는 책"`, `["purpose"] == "fallback"`; `[i["book_id"] for i in rows[0]["items"]] == [4, 5, 6, 7, 8]`; `rows[0]["channel_mix"] == {"popularity": 5}`; `user_state_weights == {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}`; `recommendation_id` 가 `rec_` + 6자; `"total" in latency_breakdown`
    - items 점수 비증가: `scores == sorted(scores, reverse=True)`
    - 기본 variant(D-11): `pipelines={"pop": _FakePop(), "cf": _FakeNamed("cf")}` + `?seeds=1` → `model_version == "cf_v1"`(VARIANTS 순서상 마지막 등록), `forced is False`; `?seeds=1&model=pop` → `"pop_v1"`, `forced is True`
    - 미등록 유효 model(D-11 fallback): `pipelines={"pop": _FakePop()}` + `?seeds=1&model=cf` → 200, `model_version == "pop_v1"`, `forced is True`, `fallback_level == 0`(KeyError 없음)
    - 익명(D-13): `pipelines={"pop": _FakePop()}` + `GET /api/recommend`(seeds 없음) → 200, `fallback_level == 3`, `model_version == "fallback_v1"`, `items == []`
    - 예외(Phase 1 D-13 유지): `pipelines={"pop": _Boom()}` + `?seeds=1` → 200, `fallback_level == 3`, `rows[0]["items"] == []`, `"boom" not in r.text and "Traceback" not in r.text`
    - 회귀: `pipelines={}` + `?seeds=1,2,3&k=5` → `fallback_level == 3`(Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md) 과 동일)
  </behavior>
  <implementation>Task 2 action 에 `compose.py` 전문과 `api.py` 변경 후 전문이 있다. 그대로 쓴다.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): tests/serving/test_recommend_level0.py 7건 작성 → 기존 api.py 에서 AssertionError 확인</name>
  <files>tests/serving/test_recommend_level0.py</files>
  <read_first>
    - .planning/phases/02-track-a/02-CONTEXT.md — D-11(기본 variant), D-12(level 0 응답 형태·trending 재사용·weights 0), D-13(익명 level 3·예외 → level 3), D-16(serving 필수 테스트), "Specific Ideas — level 0 기대 응답"
    - .planning/phases/02-track-a/02-PATTERNS.md — "serving/api.py (MODIFY)" 절(현재 핸들러·분기 형태), "tests/serving/test_recommend_level0.py" 절(exact analog·단정 목록·헤더 형식)
    - tests/serving/test_smoke.py 전문(`_Boom`·`_app`·`client` fixture·3종 구분 주석·`with TestClient` 함정)
    - src/millie_rec/serving/api.py L110-126(현재 핸들러 — RED 의 실패 지점)
    - src/millie_rec/contracts.py L60-63, L87(상수), L218-232(`ScoredItem`), L341-346(`Pipeline`)
    - ../.claude/rules/python-tdd.md
  </read_first>
  <action>
    **1-a. `tests/serving/test_recommend_level0.py`** 를 작성한다(기존 `test_smoke.py` 는 열어 보기만 — 수정 금지):
    ```python
    """/api/recommend level 0 — 계약(RecommendOut·pop_v1) · 정확성(seeds 제외·기본 variant D-11) · 안전성(익명 level 3 D-13·예외 → level 3).

    Phase 2 'Track A 정량 평가 기반'(.planning/ROADMAP.md). 가짜 Pipeline 주입 — retrieval·data 를 import 하지 않는다.
    """

    from pathlib import Path

    from fastapi.testclient import TestClient

    from millie_rec.contracts import (
        DB_FILENAME,
        FALLBACK_GLOBAL_POP,
        FALLBACK_PERSONALIZED,
        MODEL_VERSION_FALLBACK,
        MODEL_VERSION_SUFFIX,
        ScoredItem,
        UserState,
    )
    from millie_rec.serving.api import create_app
    from millie_rec.serving.db import Database
    from millie_rec.serving.fallback import GlobalPopularFallback
    from millie_rec.serving.schemas import RecommendOut

    ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}


    class _FakePop:
        """seen 을 뺀 1..39 상위 k — Plan 05 PopPipeline 의 형태 모사."""

        name = "pop"

        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            ids = [b for b in range(1, 40) if b not in user.seen][:k]
            return [
                ScoredItem(book_id=b, score=float(40 - b), source="popularity", position=i, source_channels=("popularity",))
                for i, b in enumerate(ids)
            ]


    class _FakeNamed(_FakePop):
        """name 만 다른 가짜 — 기본 variant(D-11) 검증용."""

        def __init__(self, name: str) -> None:
            self.name = name


    class _Boom:
        name = "pop"

        def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
            raise RuntimeError("boom")


    def _app(tmp_path: Path, pipelines: dict | None = None):
        return create_app(
            pipelines=pipelines if pipelines is not None else {"pop": _FakePop()},
            fallback=GlobalPopularFallback(None),
            db=Database(tmp_path / DB_FILENAME),
        )
    ```
    테스트 7건(모든 HTTP 테스트의 **첫 단정은 `status_code`**):
    - `# ── 계약 ──` `test_level0_with_pop_pipeline_returns_personalized_recommend_out(tmp_path)` — behavior 첫 항목 전부(`RecommendOut.model_validate`, `fallback_level == FALLBACK_PERSONALIZED`, `model_version == "pop" + MODEL_VERSION_SUFFIX`, `forced is False`, items 5개 `[4,5,6,7,8]`, rows 1개 trending, `channel_mix == {"popularity": 5}`, weights, rec_id 형식, `"total" in latency_breakdown`)
    - `# ── 정확성 ──` `test_level0_items_exclude_seeds_and_are_score_sorted(tmp_path)` — `?seeds=4,5&k=3` → items `[1, 2, 3]`, 어느 것도 {4,5} 아님, 점수 비증가, `rows[0]["items"]` 의 book_id 순서 == `items` 순서
    - `test_default_variant_is_last_registered_in_variants_order(tmp_path)` — `{"pop": _FakePop(), "cf": _FakeNamed("cf")}`; `?seeds=1` → `"cf" + MODEL_VERSION_SUFFIX`, forced False; `?seeds=1&model=pop` → `"pop_v1"`, forced True
    - `test_registered_only_pop_but_model_cf_uses_default_without_error(tmp_path)` — `?seeds=1&model=cf` → 200, `model_version == "pop_v1"`, `forced is True`, `fallback_level == FALLBACK_PERSONALIZED`
    - `# ── 안전성 ──` `test_anonymous_request_without_seeds_stays_level3(tmp_path)` — `GET /api/recommend` → 200, `fallback_level == FALLBACK_GLOBAL_POP`, `model_version == MODEL_VERSION_FALLBACK`, `items == []`
    - `test_pipeline_exception_degrades_to_level3_200_without_leaking_error(tmp_path)` — `{"pop": _Boom()}` `?seeds=1` → 200, `fallback_level == FALLBACK_GLOBAL_POP`, `rows[0]["items"] == []`, `"boom" not in r.text`, `"Traceback" not in r.text`
    - `test_empty_pipelines_regression_stays_level3(tmp_path)` — `pipelines={}` `?seeds=1,2,3&k=5` → `fallback_level == FALLBACK_GLOBAL_POP`, `model_version == MODEL_VERSION_FALLBACK`

    **1-b. RED 확인:** `uv run pytest tests/serving/test_recommend_level0.py --no-header 2>&1 | tail -30` → **4 failed, 3 passed** 가 정직한 RED 다: level 0 을 요구하는 4건(계약·seeds 제외·기본 variant·미등록 model)은 `assert 3 == 0` 류 AssertionError 로 실패하고, 익명·예외·빈 pipelines 3건은 **현재 코드가 이미 level 3 이므로 통과**한다(회귀 방지용 — 예상된 GREEN, SUMMARY 에 "의도된 사전 통과 3건" 으로 적는다). `ImportError`·`KeyError`·`TypeError` 가 있으면 테스트를 고친다(구현이 아니라 — 이 단계는 구현 전).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving/test_recommend_level0.py --no-header 2>&1 | tail -25; uv run pytest tests/serving/test_smoke.py -k "not server_module" --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/serving/test_recommend_level0.py`; `grep -c "def test_" tests/serving/test_recommend_level0.py` == 7
    - RED 출력: `4 failed, 3 passed`, 실패 사유에 `AssertionError`/`E  assert` 만(`KeyError`·`TypeError`·`ImportError` 없음)
    - `uv run pytest tests/serving/test_smoke.py -k "not server_module" --no-header` → `10 passed`(serving 단독 10건; 서버 조립 1건·파일 소유자는 02-05 — 이 플랜은 `test_smoke.py` 를 수정하지 않는다)
    - `grep -c "millie_rec.retrieval\|millie_rec.data\|millie_rec.evaluation\|millie_rec.app" tests/serving/test_recommend_level0.py` == 0
    - `git diff --stat src/millie_rec/serving/` → 출력 없음(이 태스크는 테스트만)
  </acceptance_criteria>
  <done>7건 중 level 0 을 요구하는 4건이 AssertionError 로 RED, 회귀 3건 통과, test_smoke serving 단독 10건 통과. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): serving/compose.py 신설 + api.py level 0 분기 → 7 passed + test_smoke serving 단독 10건</name>
  <files>src/millie_rec/serving/compose.py, src/millie_rec/serving/api.py</files>
  <read_first>
    - src/millie_rec/serving/api.py 전문(변경 대상 — 현재 128줄)
    - src/millie_rec/serving/fallback.py L37-45(`trending_row`)
    - tests/serving/test_recommend_level0.py(Task 1 결과) · tests/serving/test_smoke.py L70-103(수정 금지 — 소유자 02-05. 통과해야 하는 단정: `items == []` level 3, rec_id 길이 10, weights 0)
    - .planning/phases/02-track-a/02-PATTERNS.md "serving/api.py (MODIFY)" 절 분기 형태
    - .planning/phases/01-local-serving-skeleton/01-CONTEXT.md D-02(trending 행)·D-03(rec_id)·D-04(wire 값)·D-09(시그니처)
  </read_first>
  <action>
    **2-a. `src/millie_rec/serving/compose.py` 신설** (전문):
    ```python
    """응답 조립 — Phase 2 는 trending 1행 + level 0 items 평탄화(CONTEXT D-12).

    Must 5행·행 순서 정본은 Phase 5 가 이 파일에 증분(아키텍처 01 §3-3). __init__ 에 노출하지 않는다.
    """

    import uuid
    from collections.abc import Sequence
    from time import perf_counter

    from millie_rec.contracts import (
        FALLBACK_PERSONALIZED,
        VARIANTS,
        Pipeline,
        Recommendation,
        RecommendResponse,
        ScoredItem,
    )
    from millie_rec.serving.fallback import trending_row

    # 백엔드 01 §5 "익명은 3, 가중치 전부 0". blend 는 Phase 4·상태는 Phase 5
    ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}


    def default_variant(pipelines: dict[str, Pipeline]) -> str | None:
        """Phase 2 D-11: 등록된 것 중 VARIANTS 순서상 마지막. 셀 배정(A/B)은 Phase 5 몫."""
        registered = [v for v in VARIANTS if v in pipelines]
        return registered[-1] if registered else None


    def new_rec_id() -> str:
        return "rec_" + uuid.uuid4().hex[:6]  # 백엔드 01 §0 ID 형식 rec_<6hex> (Phase 1 D-03)


    def build_response(
        items: Sequence[ScoredItem],
        *,
        model_version: str,
        level: int,
        k: int,
        t0: float,
        context: str | None,
    ) -> RecommendResponse:
        """level 0 만 items 를 상위 k 로 평탄화한다. level 3 의 items 는 Phase 1 그대로 빈 튜플."""
        ms = (perf_counter() - t0) * 1000
        flat = (
            tuple(Recommendation(book_id=i.book_id, score=i.score) for i in items[:k])
            if level == FALLBACK_PERSONALIZED
            else ()
        )
        return RecommendResponse(
            items=flat,
            model_version=model_version,
            fallback_level=level,
            latency_ms=ms,
            recommendation_id=new_rec_id(),
            rows=(trending_row(items),),
            latency_breakdown={"total": ms},
            user_state_weights=dict(ZERO_WEIGHTS),
            context=context,
        )
    ```

    **2-b. `src/millie_rec/serving/api.py` 변경** — 아래가 변경 후 **전문**이다(현재 파일과 diff 를 비교해 의도치 않은 변경이 없는지 확인: `_422`·`_parse_seeds`·`create_app` 시그니처·lifespan·`/health` 본문·쿼리 인자 이름·422 규칙은 그대로):
    ```python
    """FastAPI create_app · lifespan · GET /health · GET /api/recommend (백엔드 서빙 01 §0·§1·§5)."""

    import logging
    from collections.abc import AsyncIterator
    from contextlib import asynccontextmanager
    from time import perf_counter
    from typing import Annotated

    from fastapi import FastAPI, HTTPException, Query

    from millie_rec.contracts import (
        FALLBACK_GLOBAL_POP,
        FALLBACK_PERSONALIZED,
        MODEL_VERSION_FALLBACK,
        MODEL_VERSION_SUFFIX,
        VARIANTS,
        BookStatsSource,
        Catalog,
        Neighbors,
        Pipeline,
        RecommendResponse,
        UserState,
    )
    from millie_rec.serving.compose import build_response, default_variant
    from millie_rec.serving.db import Database
    from millie_rec.serving.schemas import API_VERSION, HealthOut, RecommendOut

    log = logging.getLogger(__name__)
    K_DEFAULT, K_MAX = 40, 100  # 백엔드 01 §5 "k 기본 40" · §0 "k ≤ 100"


    def _422(param: str, msg: str) -> HTTPException:
        return HTTPException(422, detail=[{"loc": ["query", param], "msg": msg, "type": "value_error"}])


    def _parse_seeds(raw: str | None) -> tuple[int, ...]:
        """'1,2,3' → (1, 2, 3). 정수가 아니면 FastAPI 규약대로 422."""
        if not raw:
            return ()
        try:
            return tuple(int(s) for s in raw.split(",") if s.strip())
        except ValueError as e:
            raise _422("seeds", "seeds must be comma-separated integers") from e


    def _fallback_response(
        fallback: Pipeline,
        user: UserState,
        k: int,
        t0: float,
        context: str | None,
        *,
        level: int = FALLBACK_GLOBAL_POP,
    ) -> RecommendResponse:
        """전역 인기 fallback. 예외가 나도 빈 trending 행으로 200 — 추천 API 장애 ≠ 메인 장애(D-13)."""
        try:
            items = fallback.recommend(user, k)
        except Exception:
            log.exception("fallback pipeline failed; serving empty trending row")
            items = []
        return build_response(
            items, model_version=MODEL_VERSION_FALLBACK, level=level, k=k, t0=t0, context=context
        )


    def _personalized_response(
        pipe: Pipeline, fallback: Pipeline, user: UserState, k: int, t0: float, context: str | None
    ) -> RecommendResponse:
        """level 0. 파이프라인 예외는 level 3 로 강하 — 500 도 예외 문자열 노출도 없다(Phase 2 D-13)."""
        try:
            items = pipe.recommend(user, k)
        except Exception:
            log.exception("pipeline %s failed; falling back to level 3", pipe.name)
            return _fallback_response(fallback, user, k, t0, context)
        version = f"{pipe.name}{MODEL_VERSION_SUFFIX}"
        return build_response(
            items, model_version=version, level=FALLBACK_PERSONALIZED, k=k, t0=t0, context=context
        )


    def create_app(
        pipelines: dict[str, Pipeline],
        fallback: Pipeline,
        *,
        catalog: Catalog | None = None,
        db: Database,
        neighbors: Neighbors | None = None,
        book_stats: BookStatsSource | None = None,
        state: object | None = None,
    ) -> FastAPI:
        """주입만 받는다. 비즈니스 규칙은 슬라이스 함수에. db 는 keyword-only 필수(D-09 + 리뷰 W-1)."""
        started = {"t": perf_counter()}
        default = default_variant(pipelines)  # D-11. compose.py 에 두어 api.py ≤150줄 유지

        @asynccontextmanager
        async def lifespan(_: FastAPI) -> AsyncIterator[None]:
            db.apply_schema()  # D-06: 행은 쓰지 않는다. 스키마·PRAGMA 만
            started["t"] = perf_counter()
            yield

        app = FastAPI(title="millie-rec", version=API_VERSION, lifespan=lifespan)

        @app.get("/health", response_model=HealthOut)
        def health() -> HealthOut:  # 동기 def — 스레드풀 (serving.md 이벤트 루프 규칙)
            ok = db.ok()
            return HealthOut(
                status="ok",
                db_ok=ok,
                db_row_count=db.row_counts() if ok else {},
                uptime_s=perf_counter() - started["t"],
            )

        @app.get("/api/recommend", response_model=RecommendOut)
        def recommend(
            user_key: Annotated[str | None, Query()] = None,
            snapshot_id: Annotated[str | None, Query()] = None,
            model: Annotated[str | None, Query()] = None,
            k: Annotated[int, Query(ge=1, le=K_MAX)] = K_DEFAULT,
            context: Annotated[str | None, Query()] = None,
            seeds: Annotated[str | None, Query()] = None,
        ) -> RecommendOut:
            t0 = perf_counter()
            if model is not None and model not in VARIANTS:
                raise _422("model", f"model must be one of {list(VARIANTS)}")
            user = UserState(user_id=None, explicit_seeds=_parse_seeds(seeds))
            # D-13 seeds 있을 때만 level 0(익명은 3) · D-11 미등록 model 은 기본 variant
            # user_key·snapshot_id 는 Phase 5 state.py 몫 — 쿼리 계약만 받는다
            name = model if model in pipelines else default
            if user.explicit_seeds and name:
                resp = _personalized_response(pipelines[name], fallback, user, k, t0, context)
            else:
                resp = _fallback_response(fallback, user, k, t0, context)
            return RecommendOut.from_contract(resp, forced=model is not None)

        return app
    ```
    변경 요지: ① `uuid`·`ZERO_WEIGHTS`·`_new_rec_id`·`RecommendResponse(...)` 조립이 `compose.py` 로 이동(`api.py` 에서 삭제) ② **`_fallback_response` 시그니처(`*, level=FALLBACK_GLOBAL_POP` 포함)는 Phase 1 그대로 유지**(W2 승인 조건 ①) — 본문만 `build_response(..., level=level, ...)` 호출로 ③ `default_variant` 는 `compose.py` 에(줄 예산), `_personalized_response` 는 `api.py` 에 신설 ④ 핸들러 분기 ⑤ `/health` 본문 무변경(`model_version` 채우기는 Phase 5 '서빙 Must 완성'(.planning/ROADMAP.md) — objective 참조). 예상 줄 수: `api.py` ≈145, `compose.py` ≈60.
    **150줄 초과 시:** `_personalized_response` 도 `compose.py` 로 옮겨 `personalized_response(pipe, fallback_items_fn, …)` 형태로 두지 말고 — 대신 `_fallback_response` 의 docstring 을 1줄로 줄이고 빈 줄만 정리한다. 주석·docstring **삭제**는 하지 않는다(100자 줄바꿈은 허용).

    **2-c. GREEN 확인:** `uv run pytest tests/serving -k "not server_module" --no-header` → **test_recommend_level0 7 passed + test_smoke serving 단독 10 passed + test_schemas 기존 전부**(`failed 0`; 서버 조립 1건은 02-05 소유라 제외). `uv run pytest tests/test_architecture.py --no-header` → 3 passed(`compose.py` 는 `contracts`·`serving.fallback` 만 import).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving tests/test_architecture.py -k "not server_module" --no-header && wc -l src/millie_rec/serving/api.py src/millie_rec/serving/compose.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/serving/test_recommend_level0.py --no-header` → `7 passed`; `uv run pytest tests/serving/test_smoke.py -k "not server_module" --no-header` → `10 passed`(이 플랜은 `test_smoke.py` 를 수정하지 않는다 — 소유자 02-05, B4)
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `wc -l src/millie_rec/serving/api.py` ≤ 150; `wc -l src/millie_rec/serving/compose.py` ≤ 150
    - `grep -n "^from millie_rec.serving.compose import build_response, default_variant" src/millie_rec/serving/api.py` 1행; `grep -c "RecommendResponse(" src/millie_rec/serving/api.py` == 0(조립은 compose 만); `grep -c "RecommendResponse(" src/millie_rec/serving/compose.py` == 1
    - `grep -n "def default_variant(pipelines: dict\[str, Pipeline\]) -> str | None" src/millie_rec/serving/compose.py` 1행; `grep -n "registered\[-1\] if registered else None" src/millie_rec/serving/compose.py` 1행; `grep -n "level: int = FALLBACK_GLOBAL_POP," src/millie_rec/serving/api.py` 1행(W2 ① 시그니처 유지)
    - `awk 'length > 100' src/millie_rec/serving/api.py src/millie_rec/serving/compose.py tests/serving/test_recommend_level0.py | wc -l` == 0(E501)
    - `grep -n "if user.explicit_seeds and name:" src/millie_rec/serving/api.py` 1행(D-13); `grep -n "name = model if model in pipelines else default" src/millie_rec/serving/api.py` 1행(D-11)
    - `grep -c "except Exception:" src/millie_rec/serving/api.py` == 2(fallback + personalized); `grep -c "log.exception" src/millie_rec/serving/api.py` == 2
    - `grep -n "^def create_app(" -A 9 src/millie_rec/serving/api.py` 의 인자 이름·순서가 `pipelines, fallback, *, catalog, db, neighbors, book_stats, state` 그대로
    - `grep -n "from millie_rec.serving.fallback import trending_row" src/millie_rec/serving/compose.py` 1행; `grep -n 'ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}' src/millie_rec/serving/compose.py` 1행; `grep -c "uuid" src/millie_rec/serving/api.py` == 0
    - `grep -rn "millie_rec\.\(data\|retrieval\|evaluation\|app\)" src/millie_rec/serving/` → 출력 없음
    - `git diff --stat src/millie_rec/contracts.py src/millie_rec/serving/schemas.py src/millie_rec/serving/fallback.py src/millie_rec/serving/db.py` → 출력 없음
  </acceptance_criteria>
  <done>level 0 7건 + 기존 serving 테스트 전부 GREEN, `api.py`·`compose.py` ≤150줄, `create_app` 시그니처·`/health`·422 규칙·level 3 형태 불변. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR): 자기 경로 ruff + tests/serving 재통과 (전역 게이트는 wave 2 종료 후 Advisor)</name>
  <files>src/millie_rec/serving/compose.py, src/millie_rec/serving/api.py</files>
  <read_first>
    - Makefile L33-40 (`smoke` — 8010 포트, `seeds=1,2,3&k=5`)
    - ../.claude/rules/local-run.md ("아티팩트 없이 기동")
    - src/millie_rec/serving/__init__.py (공개 표면 — `compose` 이름을 노출할지: **노출하지 않는다**. `build_response` 는 serving 내부용이고 app 은 `create_app` 만 쓴다)
  </read_first>
  <action>
    **3-a. 정리(이 플랜이 건드린 파일 안에서만):** `uv run ruff format src/millie_rec/serving/compose.py src/millie_rec/serving/api.py tests/serving/test_recommend_level0.py && uv run ruff check src/millie_rec/serving tests/serving`. `serving/__init__.py` 는 건드리지 않는다(`compose` 는 내부 모듈 — `build_response`·`default_variant` 미노출, W2 ②).

    **3-b. 자기 경로 재통과:** `uv run pytest tests/serving tests/test_architecture.py -k "not server_module" --no-header` → `failed 0`.

    **3-c. 전역 게이트는 이 플랜에서 돌리지 않는다(checker B3):** 전체 트리 `uv run pytest --no-header`·`uv run ruff check .`·`make smoke`(고정 포트 8010 + 공유 `.uvicorn.pid`) 와 임시 uvicorn 기동은 wave 2 의 두 플랜(04·05)이 **모두 끝난 뒤 Advisor 가 1회** 실행한다(02-06-PLAN.md `<wave_gate>`) — 02-05 가 `app/server.py`·`test_smoke.py` 를 병렬로 바꾸는 중이라 이 시점의 전역 결과는 신뢰할 수 없다. level 3 유지는 `test_empty_pipelines_regression_stays_level3`·`test_anonymous_request_without_seeds_stays_level3` 가 이미 단정한다.

    **3-d. SUMMARY 준비:** `git diff --stat -- src/millie_rec/serving tests/serving`, RED(`4 failed, 3 passed` + AssertionError 발췌)/GREEN 요약 줄, `wc -l`, "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다", compose.py 신설 근거(objective 의 planner 결정 2개) 1줄.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/serving tests/serving && uv run ruff check src/millie_rec/serving tests/serving && uv run pytest tests/serving tests/test_architecture.py -k "not server_module" --no-header 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff format --check src/millie_rec/serving tests/serving` · `uv run ruff check src/millie_rec/serving tests/serving` 클린
    - `uv run pytest tests/serving tests/test_architecture.py -k "not server_module" --no-header` 요약에 `failed` 없음
    - `git diff --stat -- src/millie_rec/serving/__init__.py` → 출력 없음(이 플랜은 `__init__.py` 를 건드리지 않는다; `test_smoke.py` 는 02-05 소유라 wave 2 동시 실행 중 diff 로 판정하지 않는다)
    - (전역 게이트 — 이 플랜의 acceptance 아님) 전체 `uv run pytest --no-header`·`ruff check .`·`make smoke` 는 wave 2 종료 후 Advisor 1회(02-06-PLAN.md `<wave_gate>`)
    - `git log --oneline | head -1` 불변(커밋 0)
  </acceptance_criteria>
  <done>자기 경로 ruff 클린, `tests/serving`(서버 조립 1건 제외) + 아키텍처 통과. 전역 게이트는 wave 2 종료 후 Advisor. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → `/api/recommend` 쿼리 | 비신뢰 입력 `seeds`·`k`·`model`·`context`(Phase 1 검증 유지) |
| 주입된 `pipelines[name]` → 핸들러 | 모델 코드의 예외·느린 응답·seen 미제외 가능 |
| `pipelines` dict 키 → `VARIANTS` | 조립(app)이 넣은 이름. 요청 `model` 과 대조 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Denial of Service / Information Disclosure | `_personalized_response` | mitigate | 파이프라인 예외 `except Exception` → `log.exception` + level 3 200. 응답에 예외 문자열·스택 없음(테스트 `..._without_leaking_error` 가 `"boom" not in r.text`·`"Traceback" not in r.text` 단정). `k ≤ 100` 응답 크기 상한 유지 |
| T-02-19 | Tampering | `model` 쿼리 → `pipelines[name]` | mitigate | `model ∉ VARIANTS` → 422(기존). `model ∈ VARIANTS` 이지만 미등록 → `name = default`(D-11) 로 `KeyError` 경로 제거(테스트 `..._uses_default_without_error`). `pipelines` 가 비면 `name=None` → level 3 |
| T-02-20 | Tampering | `seeds` → `UserState.explicit_seeds` → 파이프라인 | accept | 정수 CSV 파싱·422 는 Phase 1 그대로(`_parse_seeds`). seeds 개수 상한은 두지 않는다 — pop retrieve 는 O(k+|seen|), 쿼리 길이는 서버(uvicorn) 기본 헤더 한도가 막는다. Phase 5 `user_key` 경로에서 재검토 |
| T-02-21 | Information Disclosure | level 0 응답 `items`·`rows` | accept | Goodbooks book_id·score 만(PII 없음). `model_version` 은 variant 이름 — 설계상 공개(인스펙터 라디오) |
| T-02-22 | Spoofing | 인증 없음 | accept | 공개 데모·익명(REQUIREMENTS Out of Scope). Phase 5 `/metrics` 만 Bearer |
| T-02-23 | Repudiation | `recommendation_id` | accept | 매 응답 발급(`new_rec_id`), 저장은 Phase 5 `recommendations` 테이블 |
</threat_model>

<verification>
- `uv run pytest tests/serving/test_recommend_level0.py --no-header` → 7 passed(RED 단계 `4 failed, 3 passed`)
- `uv run pytest tests/serving/test_smoke.py -k "not server_module" --no-header` → 10 passed(파일은 02-05 소유 — 이 플랜 무변경)
- (wave 2 종료 후 Advisor 1회) `uv run pytest --no-header` → failed 0 · `uv run ruff check .` · `make smoke` → `smoke: PASS`
- `uv run pytest tests/test_architecture.py --no-header` → 3 passed
- `uv run ruff format --check src/millie_rec/serving tests/serving && uv run ruff check src/millie_rec/serving tests/serving` 클린
- `wc -l src/millie_rec/serving/api.py src/millie_rec/serving/compose.py` 각 ≤150; `awk 'length > 100'` 0줄
- `git diff --stat -- src/millie_rec/contracts.py src/millie_rec/serving/schemas.py src/millie_rec/serving/fallback.py src/millie_rec/serving/db.py Makefile` 에 `contracts.py`·`schemas*.py`·`fallback.py`·`db.py`·`app/`·`Makefile` 없음, 커밋 0
</verification>

<success_criteria>
- ROADMAP Phase 2 Success Criterion 5 의 서빙 몫: 파이프라인 주입 시 `/api/recommend` 가 `fallback_level=0`·`model_version=pop_v1`·`trending` 1행 + `items` — 가짜 Pipeline 테스트로 고정(실측은 Plan 06 `make smoke`)
- CONTEXT 결정 D-11(기본 variant = 등록 중 VARIANTS 마지막, 상수 추가 없음)·D-12(trending 행 재사용, weights 0, `model_version`·`fallback_level`·`items` 만 변경)·D-13(익명 level 3, 예외 → level 3 200)·D-16(serving 필수 테스트 5종) 가 코드·테스트에 존재
- Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md) 계약 유지: `create_app`·`_fallback_response` 시그니처 불변, `RecommendOut.from_contract` 만으로 직렬화, `test_smoke.py` serving 단독 10건 통과(파일 무변경 — 소유자 02-05), `/health` 무변경
- `compose.py` 신설 근거·`/health` 채우기 연기가 SUMMARY 에 기록됨(Advisor 가 개발일지 D 항목으로 옮길 수 있게)
</success_criteria>

<output>
완료 후 `.planning/phases/02-track-a/02-04-SUMMARY.md` 를 만든다. 포함: RED(`4 failed, 3 passed` — AssertionError 발췌 + "의도된 사전 통과 3건" 명시) · GREEN(compose.py 신설·api.py 변경 요지 5개·줄 수) · REFACTOR(ruff·전체 스위트·smoke level) · `## TDD Gate Compliance`(커밋 게이트 생략 사유 = D-18, 대체 증거 = pytest 요약 줄) · planner 결정 2개(compose.py 지금 신설 / `/health` 채우기 Phase 5 연기) 와 그 근거 · 변경 파일 목록(`git status --short`) · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
