---
phase: 02-track-a
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/data/goodbooks.py
  - src/millie_rec/data/load.py
  - src/millie_rec/data/split.py
  - src/millie_rec/data/onboarding.py
  - src/millie_rec/data/labels.py
  - src/millie_rec/data/__init__.py
  - tests/data/test_goodbooks.py
  - tests/data/test_load.py
  - tests/data/test_split.py
  - tests/data/test_onboarding.py
  - tests/data/test_labels.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03]
lane: Model (data 슬라이스 Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다 (결정 'D-18 커밋 없음'(.planning/phases/02-track-a/02-CONTEXT.md))
tdd_gate_evidence: pytest-output
must_not_touch:
  - scripts/**
  - data/id_map.csv
  - tests/data/test_millie_catalog.py
  - tests/data/test_millie_edges.py
  - tests/data/test_millie_parse.py
  - src/millie_rec/contracts.py
  - tests/test_architecture.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "split(df) 는 ts 가 전부 결측이면 split_mode='holdout' 으로 유저별 20% random holdout(seed 42) 을, ts 가 있으면 split_mode='temporal' 로 전역 시점 분할을 반환하며 temporal 은 train.ts.max() < test.ts.min() 을 만족한다"
    - "mask_onboarding(train, users) 는 유저마다 정확히 5개 explicit_seeds 를 주고 그 seeds 는 전부 train 긍정(rating≥4) 안에 있으며, 반환 구조(UserState)에 negative 필드가 없다"
    - "n≥k 상태는 seeds 를 뺀 train 행이 K_HISTORY(20) 이상인 유저만 포함하고 history ∩ explicit_seeds = ∅ 이다"
    - "is_positive 는 rating ≥ 4 만 True 이고 relevant_sets 는 test 의 긍정만 정답 집합으로 만든다"
    - "build_goodbooks(raw_dir, out_dir) 는 interactions.parquet(user_id, book_id, ts=None, event='rating', rating) 과 books.parquet(계약 컬럼 9개 + tags) 을 만들고, book_tags 의 goodreads_book_id 를 book_id 로 매핑하며 NOISE_TAGS 를 제외한다 — 네트워크 없이 tmp CSV 로 검증"
    - "download_goodbooks 는 파일이 이미 있고 크기>0 이면 네트워크를 호출하지 않는다(멱등)"
  artifacts:
    - path: "src/millie_rec/data/goodbooks.py"
      provides: "download_goodbooks(멱등 HTTPS 다운로드) · build_goodbooks(raw CSV → 2 parquet)"
      exports: ["download_goodbooks", "build_goodbooks", "NOISE_TAGS", "TOP_TAGS", "RAW_SUBDIR"]
      max_lines: 150
    - path: "src/millie_rec/data/load.py"
      provides: "load_interactions · load_books · filter_min_interactions(유저≥5·아이템≥5)"
      exports: ["load_interactions", "load_books", "filter_min_interactions", "MIN_USER_INTERACTIONS", "MIN_ITEM_INTERACTIONS", "INTERACTIONS_FILE", "BOOKS_FILE"]
      max_lines: 150
    - path: "src/millie_rec/data/split.py"
      provides: "Split DTO + split() holdout|temporal"
      exports: ["Split", "split", "TEST_FRAC"]
      max_lines: 150
    - path: "src/millie_rec/data/onboarding.py"
      provides: "select_test_users · mask_onboarding → OnboardingStates(n0, n_k)"
      exports: ["OnboardingStates", "select_test_users", "mask_onboarding", "K_HISTORY", "N_TEST_USERS", "STATE_N0", "STATE_NK"]
      max_lines: 150
    - path: "src/millie_rec/data/labels.py"
      provides: "is_positive(rating≥4) · relevant_sets"
      exports: ["is_positive", "relevant_sets", "POSITIVE_MIN_RATING"]
      max_lines: 150
    - path: "src/millie_rec/data/__init__.py"
      provides: "공개 표면 __all__"
      contains: "\"mask_onboarding\""
    - path: "tests/data/test_split.py"
      provides: "temporal 누수 단언 · holdout split_mode · 유저별 비율 · 재현성"
      min_lines: 40
    - path: "tests/data/test_onboarding.py"
      provides: "seeds 5개 ⊂ 긍정 · negative 구조 없음 · n≥k 모집단 · 적격 유저 선택"
      min_lines: 40
  key_links:
    - from: "src/millie_rec/data/onboarding.py"
      to: "src/millie_rec/data/labels.py"
      via: "seeds 후보 = is_positive(train) 행만"
      pattern: "from millie_rec.data.labels import is_positive"
    - from: "src/millie_rec/data/split.py"
      to: "src/millie_rec/contracts.py"
      via: "np.random.default_rng(SEED) · COL_USER · COL_TS"
      pattern: "default_rng\\(seed\\)|default_rng\\(SEED\\)"
    - from: "src/millie_rec/data/goodbooks.py"
      to: "src/millie_rec/data/load.py"
      via: "parquet 파일 이름 상수 공유(INTERACTIONS_FILE·BOOKS_FILE)"
      pattern: "from millie_rec.data.load import"
---

<objective>
Phase 2 'Track A 정량 평가 기반'(.planning/ROADMAP.md "Phase 2: Track A 정량 평가 기반")의 `data` 슬라이스를 TDD 한 사이클(RED → GREEN → REFACTOR)로 만든다: Goodbooks-10k 멱등 다운로드·parquet 캐시(`goodbooks.py`), 읽기·최소 상호작용 필터(`load.py`), holdout|temporal 분할(`split.py`), 온보딩 5권 마스킹과 두 상태(`onboarding.py`), 라벨 `rating≥4`(`labels.py`).

Purpose: PDF 비교표 숫자의 입력 데이터가 되는 코드. 분할이 거짓으로 temporal 이라 기록되지 않고(결정 'D-02 분할'(.planning/phases/02-track-a/02-CONTEXT.md)), 미선택 책이 어느 단계에서도 negative 라벨을 만들지 않음(수용 기준 '미선택 책 ≠ 부정 신호'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표))을 테스트 이름으로 남긴다.
Output: 위 6개 `src` 파일 + `tests/data/` Track A 테스트 5파일(20건). 네트워크 호출 0(테스트), 커밋 0.

한 기능(feature) = **Track A 데이터 준비 수직 슬라이스**: raw CSV → parquet → 필터 → split → 테스트 유저 선택 → 두 UserState 상태 + 정답 집합. `evaluation`·`retrieval` 은 이 출력(`pd.DataFrame`·`UserState`·`frozenset[int]`)만 받는다.

**Track B 불변:** `scripts/`·`data/id_map.csv`·`tests/data/test_millie_*.py` 는 읽지도 고치지도 않는다(데이터 2트랙 불혼합, ../CLAUDE.md §2-8).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-track-a/02-CONTEXT.md
@.planning/phases/02-track-a/02-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/data/__init__.py
@src/millie_rec/serving/__init__.py
@tests/conftest.py
@tests/test_architecture.py
@../.claude/rules/data.md
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/architecture.md
@../.claude/rules/simplicity.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 테스트는 오직 `uv run pytest <대상> --no-header`(`-q` 는 pyproject `addopts` 에 이미 있음 — 붙이면 `-qq` 가 되어 요약 줄이 사라진다). 맨 `pytest`·`python -m pytest`·`python3` 금지(이 머신의 맨 pytest 는 Python 3.9 → collection error). **RED 는 출력에 `AssertionError`(또는 `E  assert`) 가 있을 때만 인정.** `ImportError`·`ModuleNotFoundError`·`SyntaxError` 는 RED 가 아니다 → 시그니처만 있는 스텁을 먼저 둔다. `pytest.skip` 금지. `tests/test_architecture.py`·기존 테스트 수정 금지.

**star 의존(`../.claude/rules/architecture.md`, `tests/test_architecture.py` L37-42):** `data/*.py` 는 `millie_rec.contracts` 와 `millie_rec.data.<module>` 만 import. `retrieval`·`evaluation`·`serving`·`app` import 금지.

**이름·난수·경로(`../.claude/rules/python.md`):** 컬럼은 `contracts.COL_USER`·`COL_ITEM`·`COL_TS`·`COL_EVENT`·`COL_RATING` 상수만(문자열 리터럴 산재 금지). 난수는 `np.random.default_rng(seed)` 하나(seed 기본값 `contracts.SEED`). 경로는 `DIR_RAW`·`DIR_PROCESSED` 에서 시작. 유저×아이템 파이썬 루프 금지 — split 은 groupby 벡터화, 유저 루프는 `mask_onboarding`(테스트 유저 2,000명 대상, 평가용) 만 허용.

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄. 1줄 한국어 docstring + 정본 절 참조. 설정은 모듈 상단 대문자 상수 + `#` 주석에 정본 위치. 클래스는 `frozen dataclass` DTO(`Split`·`OnboardingStates`) 만. 예외 계층·재시도 데코레이터·설정 프레임워크 금지. 로깅은 `logging.getLogger(__name__)`.

**테스트(`../.claude/rules/python-tdd.md` 3종):** 데이터는 `tests/conftest.py::interactions`(유저 20×아이템 50, **ts 있음**) 또는 테스트 안의 소형 inline DataFrame 만. **다운로드·실데이터 parquet 절대 금지.** `urllib.request.urlretrieve` 는 monkeypatch 로 호출되면 실패하게 한다. 기대값은 구현 상수를 import 하지 않고 손으로 적는다(`tests/data/test_millie_catalog.py` L24 관례).

**커밋 금지:** `git add`·`git commit` 을 실행하지 않는다. RED·GREEN 각각의 `uv run pytest` 출력(요약 줄 + AssertionError 1건 발췌)을 SUMMARY.md `## TDD Gate Compliance` 에 붙인다. 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다.

**완료 기준(결정 'D-17'(.planning/phases/02-track-a/02-CONTEXT.md), wave 병렬 보정 — checker B3):** 플랜 안에서는 **자기 경로만** — `uv run ruff format --check src/millie_rec/data tests/data && uv run ruff check src/millie_rec/data tests/data` 클린 · `uv run pytest tests/data tests/test_architecture.py --no-header` failed 0. 전체 트리 `uv run pytest --no-header`(기준선 104 passed / 2 skipped 위로 증가)·`ruff check .`·`make smoke`(Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md) 과 동일 level 3 PASS) 는 **wave 1 종료 후 Advisor 가 1회** 실행한다(02-06-PLAN.md `<wave_gate>`).
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 재탐색 불필요. contracts.py 는 변경 금지. -->

From src/millie_rec/contracts.py:
```python
SEED = 42
ROOT = Path(__file__).resolve().parents[2]
DIR_RAW = ROOT / "data" / "raw"
DIR_PROCESSED = ROOT / "data" / "processed"
COL_USER = "user_id"; COL_ITEM = "book_id"; COL_TS = "ts"; COL_EVENT = "event"; COL_RATING = "rating"
EVENT_TYPES = (..., "rating", ...)   # "rating" 이 허용 이벤트(L46)
N_ONBOARD_SEEDS = 5

@dataclass(frozen=True, slots=True)
class UserState:
    user_id: int | None
    explicit_seeds: tuple[int, ...] = ()
    history: tuple[int, ...] = ()
    session: tuple[int, ...] = ()
    context: dict[str, str] = field(default_factory=dict)
    @property
    def seen(self) -> frozenset[int]: ...  # explicit_seeds ∪ history ∪ session
```

From tests/conftest.py (session fixture, 그대로 사용):
```python
@pytest.fixture(scope="session")
def interactions() -> pd.DataFrame:   # 유저 20 × 아이템 50, ts 오름차순(2020-01-01 + 0..365일), event="completion", rating 1..5 float, (user,item) 중복 제거
```
holdout 분기 테스트는 `df = interactions.copy(); df[COL_TS] = None` 으로 만든다(session fixture 를 in-place 로 바꾸지 않는다).

Goodbooks-10k raw CSV 열(다운로드 전 확인용, CONTEXT "Specific Ideas"):
- `ratings.csv`: `user_id,book_id,rating` (≈5,976,479행, rating 1..5 정수, ts 없음)
- `books.csv`: `book_id,goodreads_book_id,best_book_id,work_id,books_count,isbn,isbn13,authors,original_publication_year,original_title,title,language_code,average_rating,ratings_count,work_ratings_count,work_text_reviews_count,ratings_1..5,image_url,small_image_url`
- `book_tags.csv`: `goodreads_book_id,tag_id,count` (**키가 goodreads_book_id — books.csv 로 book_id 매핑 필수**, count 에 음수 존재 → count>0 만)
- `tags.csv`: `tag_id,tag_name`
</interfaces>
</context>

<feature>
  <name>Track A 데이터 준비 — Goodbooks 멱등 다운로드·parquet → 필터 → holdout|temporal split → 온보딩 5권 마스킹 두 상태 + rating≥4 라벨</name>
  <files>
    src/millie_rec/data/goodbooks.py, src/millie_rec/data/load.py, src/millie_rec/data/split.py,
    src/millie_rec/data/onboarding.py, src/millie_rec/data/labels.py, src/millie_rec/data/__init__.py,
    tests/data/test_goodbooks.py, tests/data/test_load.py, tests/data/test_split.py, tests/data/test_onboarding.py, tests/data/test_labels.py
  </files>
  <behavior>
    **labels.py**
    - `is_positive(pd.DataFrame({COL_RATING: [1.0, 3.0, 4.0, 5.0, 3.9]}))` → `[False, False, True, True, False]` (`POSITIVE_MIN_RATING = 4.0`, `>=`)
    - `relevant_sets(test)` with test rows `(user 1, item 1, r 5)`, `(user 1, item 2, r 3)`, `(user 2, item 3, r 2)` → `{1: frozenset({1})}` (user 2 는 긍정이 없어 키 자체가 없다)

    **split.py**
    - `split(df_no_ts)` (fixture 복사 후 `df[COL_TS] = None`) → `.split_mode == "holdout"`; 각 유저의 `train ∪ test` 행 = 그 유저의 원본 행, 교집합 ∅; 행 수 n≥2 인 유저는 test 행 수 == `max(1, floor(n * 0.2))`; 두 번 호출 결과가 동일(재현성)
    - 손계산: 유저 1 이 아이템 1..10 을 평가한 10행(ts None) → test 정확히 2행, train 8행
    - `split(interactions)` (fixture 그대로, ts 있음) → `.split_mode == "temporal"`, `train[COL_TS].max() < test[COL_TS].min()`, `len(train) + len(test) == len(df)`, 둘 다 비어 있지 않음
    - ts 가 일부만 결측이면 `ValueError`
    - `test_frac` 가 `(0, 1)` 밖이면 `ValueError`

    **onboarding.py** (손계산 train: 유저 1 = 아이템 1..12, rating `[5,5,5,4,4,4,3,2,1,5,5,5]` → 긍정 {1,2,3,4,5,6,10,11,12}; 유저 2 = 아이템 21..26 rating 전부 5)
    - `mask_onboarding(train, (1, 2), k_history=3)` → `.n0` 길이 2, 각 `UserState.explicit_seeds` 길이 5, `set(seeds) ⊂ 긍정`, `history == ()`, `session == ()`
    - `.n_k` 에는 유저 1 만(12−5=7 ≥ 3; 유저 2 는 6−5=1 < 3) → 길이 1, `set(history) == {1..12} − set(seeds)`, `set(history) & set(seeds) == set()`
    - `UserState.__dataclass_fields__.keys() == {"user_id", "explicit_seeds", "history", "session", "context"}` — negative 구조 없음(수용 기준 '미선택 책 ≠ 부정 신호'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표))
    - 같은 입력 두 번 → 같은 seeds(seed 고정)
    - `select_test_users(train, test)` with train: 유저 1 긍정 6개, 유저 2 긍정 6개, 유저 3 긍정 3개 / test: 유저 1 (r 5), 유저 2 (r 2), 유저 3 (r 5) → `((1,), 2)` (유저 2 는 test 긍정 0, 유저 3 은 train 긍정 <5 → 제외 2)
    - `select_test_users(..., n=1)` 적격이 2명 이상이면 정확히 1명, 두 번 호출 동일

    **load.py**
    - `filter_min_interactions(df)`: 유저 1..5 × 아이템 1..5(25행) + 유저 6→아이템 6(1행) + 유저 1→아이템 7(1행) → 결과 25행, 유저 집합 {1..5}, 아이템 집합 {1..5}
    - `load_interactions(path)`·`load_books(path)` 는 parquet 을 그대로 읽는다(컬럼 순서 유지)

    **goodbooks.py** (tmp_path 에 소형 CSV 4개를 써서 검증 — 네트워크 0)
    - `build_goodbooks(raw_dir=tmp/"goodbooks", out_dir=tmp/"processed")` → 두 parquet 경로 반환; `interactions.parquet` 컬럼 == `[user_id, book_id, ts, event, rating]`, `ts` 전부 결측, `event` 전부 `"rating"`, `rating` float
    - `books.parquet` 컬럼 ⊇ `{book_id, title, authors, image_url, average_rating, ratings_count, original_publication_year, categories, subcategories, tags}`; book_id 1(goodreads 100) 의 `tags` 문자열에 `to-read` 없음(NOISE), `fantasy` 있음; count 상위 `TOP_TAGS` 개만
    - `download_goodbooks(raw_dir)` 에서 4파일이 이미 있고 크기>0 이면 `urllib.request.urlretrieve` 를 호출하지 않는다(monkeypatch 로 호출 시 실패)
  </behavior>
  <implementation>Task 2 action 에 파일별 시그니처·상수·알고리즘이 있다. 그대로 쓴다.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): data 슬라이스 스텁 5파일 + Track A 테스트 5파일 작성 → 전 테스트 AssertionError 확인</name>
  <files>src/millie_rec/data/goodbooks.py, src/millie_rec/data/load.py, src/millie_rec/data/split.py, src/millie_rec/data/onboarding.py, src/millie_rec/data/labels.py, tests/data/test_goodbooks.py, tests/data/test_load.py, tests/data/test_split.py, tests/data/test_onboarding.py, tests/data/test_labels.py</files>
  <read_first>
    - .planning/phases/02-track-a/02-CONTEXT.md — D-01(적격·2,000명·제외 수), D-02(holdout 20%·temporal 단언·거짓 표기 금지), D-03(seeds = train 긍정 중 무작위 5권), D-04(두 상태·K_HISTORY=20), D-05(seen = train 전체 · 정답 = test 긍정), D-15(tags 상위 20·NOISE_TAGS), D-16(data 필수 테스트), "Claude's Discretion" 다운로드·event 값
    - .planning/phases/02-track-a/02-PATTERNS.md — "0. 모든 신규 .py 파일 공통", "data/load.py", "data/split.py", "data/onboarding.py", "data/labels.py", "tests/data/*" 절, "Shared Patterns"
    - src/millie_rec/contracts.py L17-35(상수), L55-58(K·N_ONBOARD_SEEDS), L122-135(`UserState`)
    - tests/conftest.py 전문(fixture 형태·ts 존재)
    - tests/test_architecture.py L27-43(import 위반 3종 — 스텁도 지킨다)
    - tests/data/test_millie_parse.py L1-30(같은 디렉터리 헤더·상수 관례만 참고, 수정 금지)
    - ../.claude/rules/data.md "Track A 규칙" 절 · ../.claude/rules/python-tdd.md 전문
  </read_first>
  <action>
    **왜 스텁인가:** 모듈이 없으면 `ModuleNotFoundError` 로 죽어 RED 가 아니다. 시그니처만 있고 값은 틀린 스텁을 두어 **모든 테스트가 `AssertionError` 로 실패**하는 정직한 RED 를 만든다.

    **1-a. 스텁 5파일** — 각 파일: 1줄 docstring, Task 2 와 동일한 import·상수·시그니처, 본문은 빈/틀린 값 반환만.
    - `labels.py`: `POSITIVE_MIN_RATING = 4.0`; `is_positive(df) -> pd.Series` 는 `pd.Series(False, index=df.index)`; `relevant_sets(test) -> dict[int, frozenset[int]]` 는 `{}`.
    - `split.py`: `TEST_FRAC = 0.2`, `SPLIT_HOLDOUT = "holdout"`, `SPLIT_TEMPORAL = "temporal"`; `@dataclass(frozen=True, slots=True) class Split: train: pd.DataFrame; test: pd.DataFrame; split_mode: str`; `split(df, *, test_frac=TEST_FRAC, seed=SEED) -> Split` 는 `Split(df, df.iloc[0:0], "")`.
    - `onboarding.py`: `K_HISTORY = 20`, `N_TEST_USERS = 2000`, `STATE_N0 = "n0"`, `STATE_NK = f"n{K_HISTORY}"`; `@dataclass(frozen=True, slots=True) class OnboardingStates: n0: tuple[UserState, ...]; n_k: tuple[UserState, ...]`; `select_test_users(train, test, *, n=N_TEST_USERS, n_seeds=N_ONBOARD_SEEDS, seed=SEED) -> tuple[tuple[int, ...], int]` 는 `((), 0)`; `mask_onboarding(train, users, *, n=N_ONBOARD_SEEDS, k_history=K_HISTORY, seed=SEED) -> OnboardingStates` 는 `OnboardingStates((), ())`.
    - `load.py`: `INTERACTIONS_FILE = "interactions.parquet"`, `BOOKS_FILE = "books.parquet"`, `MIN_USER_INTERACTIONS = 5`, `MIN_ITEM_INTERACTIONS = 5`; `load_interactions(path=DIR_PROCESSED / INTERACTIONS_FILE) -> pd.DataFrame` 와 `load_books(path=DIR_PROCESSED / BOOKS_FILE)` 는 `pd.read_parquet(path)` (읽기는 스텁에서도 진짜 — 테스트가 tmp parquet 을 쓴다); `filter_min_interactions(df, *, min_user=MIN_USER_INTERACTIONS, min_item=MIN_ITEM_INTERACTIONS) -> pd.DataFrame` 는 `df` 그대로 반환(필터 안 함 → 행 수 단정 실패).
    - `goodbooks.py`: 상수 전부(Task 2-a 참조: `GOODBOOKS_URL_BASE`·`FILES`·`RAW_SUBDIR`·`EVENT_RATING`·`TOP_TAGS`·`NOISE_TAGS`·`BOOK_COLS`); `download_goodbooks(raw_dir=DIR_RAW / RAW_SUBDIR) -> list[Path]` 는 **무조건 `urllib.request.urlretrieve` 를 1회 호출**(멱등 테스트가 AssertionError 로 실패하도록 — monkeypatch 된 함수가 `AssertionError("network")` 를 던진다); `build_goodbooks(raw_dir=..., out_dir=DIR_PROCESSED) -> tuple[Path, Path]` 는 빈 DataFrame 2개를 parquet 으로 써서 경로 반환(컬럼 단정 실패).

    **1-b. `tests/data/test_labels.py`** (2건):
    ```python
    """Track A 라벨 — rating≥4 만 긍정(결정 '착수 전 운영 결정 4건'(../.assets/개발일지/2026-09-04_Day0.5_브레인스토밍과_데모명세.md 항목 D45))."""

    import pandas as pd

    from millie_rec.contracts import COL_ITEM, COL_RATING, COL_USER
    from millie_rec.data.labels import is_positive, relevant_sets


    # ── 정확성 ──────────────────────────────────────────────────────────────
    def test_is_positive_is_rating_ge_4_only():
        df = pd.DataFrame({COL_RATING: [1.0, 3.0, 4.0, 5.0, 3.9]})
        assert is_positive(df).tolist() == [False, False, True, True, False]


    # ── 계약 ────────────────────────────────────────────────────────────────
    def test_relevant_sets_keeps_only_positive_test_items_per_user():
        test = pd.DataFrame({COL_USER: [1, 1, 2], COL_ITEM: [1, 2, 3], COL_RATING: [5.0, 3.0, 2.0]})
        rel = relevant_sets(test)
        assert rel == {1: frozenset({1})}
        assert all(isinstance(v, frozenset) for v in rel.values())
    ```

    **1-c. `tests/data/test_split.py`** (6건) — 헬퍼 `_no_ts(interactions)` 는 `df = interactions.copy(); df[COL_TS] = None; return df`:
    - `test_holdout_when_ts_missing_records_split_mode_holdout` — `split(_no_ts(interactions)).split_mode == "holdout"`
    - `test_holdout_partitions_each_user_without_overlap` — 유저별 `set(train idx) | set(test idx) == 원본`, 교집합 ∅ (원본 index 를 유지하는지에 의존하지 않도록 `(user, item)` 쌍으로 비교)
    - `test_holdout_test_size_per_user_is_max1_floor_frac` — 유저별 n≥2 이면 `len(test_u) == max(1, int(n * 0.2))` (float 오차 회피: `int(np.floor(n * 0.2 + 1e-9))`)
    - `test_holdout_hand_case_10_rows_gives_2_test_rows` — 유저 1, 아이템 1..10, ts None → `len(test) == 2 and len(train) == 8`
    - `test_temporal_split_no_future_leak` — `s = split(interactions)`; `s.split_mode == "temporal"`; `s.train[COL_TS].max() < s.test[COL_TS].min()`; `len(s.train) + len(s.test) == len(interactions)`; `len(s.test) > 0`
    - `test_split_is_reproducible_and_rejects_partial_ts_and_bad_frac` — 두 번 호출 `equals`; ts 절반만 None → `pytest.raises(ValueError)`; `test_frac=1.5` → `ValueError`
    (`pytest.raises` 단정은 스텁에서 `Failed: DID NOT RAISE` 로 실패 — 이것은 pytest 의 `AssertionError` 하위이므로 RED 로 인정한다.)

    **1-d. `tests/data/test_onboarding.py`** (6건) — 모듈 상단에 손계산 train/test 를 만드는 헬퍼 `_train()`·`_test()`:
    ```python
    def _train() -> pd.DataFrame:
        u1 = pd.DataFrame({COL_USER: 1, COL_ITEM: range(1, 13), COL_RATING: [5, 5, 5, 4, 4, 4, 3, 2, 1, 5, 5, 5]})
        u2 = pd.DataFrame({COL_USER: 2, COL_ITEM: range(21, 27), COL_RATING: 5.0})
        u3 = pd.DataFrame({COL_USER: 3, COL_ITEM: range(31, 35), COL_RATING: [5, 5, 5, 1]})
        return pd.concat([u1, u2, u3], ignore_index=True).astype({COL_RATING: float})

    def _test() -> pd.DataFrame:
        return pd.DataFrame({COL_USER: [1, 2, 3], COL_ITEM: [101, 102, 103], COL_RATING: [5.0, 2.0, 5.0]})
    ```
    - `test_mask_onboarding_exposes_exactly_5_seeds_from_train_positives` — `st = mask_onboarding(_train(), (1, 2), k_history=3)`; `len(st.n0) == 2`; 각 u: `len(u.explicit_seeds) == 5`, `u.history == () and u.session == ()`, seeds ⊂ 그 유저의 긍정 집합(유저 1 → {1,2,3,4,5,6,10,11,12}, 유저 2 → {21..26})
    - `test_mask_onboarding_nk_state_requires_k_history_and_excludes_seeds` — `len(st.n_k) == 1 and st.n_k[0].user_id == 1`; `set(st.n_k[0].history) == set(range(1, 13)) - set(st.n_k[0].explicit_seeds)`; `set(history) & set(seeds) == set()`; `st.n_k[0].explicit_seeds == st.n0[0].explicit_seeds`(같은 유저는 두 상태에서 같은 seeds)
    - `test_user_state_has_no_negative_structure` — `set(UserState.__dataclass_fields__) == {"user_id", "explicit_seeds", "history", "session", "context"}`; 그리고 `st.n0[0].context == {}`; 테스트 아이템 101 ∉ `seen`
    - `test_mask_onboarding_is_deterministic` — 두 번 호출 seeds 동일
    - `test_select_test_users_requires_5_train_positives_and_1_test_positive` — `select_test_users(_train(), _test()) == ((1,), 2)`; 그리고 `select_test_users(_train(), _test(), n=1) == ((1,), 2)`
    - `test_select_test_users_samples_n_deterministically` — train 에 유저 4(아이템 41..46 rating 5)·test 유저 4 (r 5) 를 추가 → 적격 2명 → `n=1` 두 번 호출 결과 같고 길이 1, `n_excluded == 2`

    **1-e. `tests/data/test_load.py`** (2건):
    - `test_filter_min_interactions_hand_case` — 위 behavior 의 27행 → 25행, `set(df[COL_USER]) == {1,2,3,4,5}`, `set(df[COL_ITEM]) == {1,2,3,4,5}`
    - `test_load_functions_read_parquet_as_is` — tmp parquet 2개(interactions 5열·books 3열) 써서 `load_interactions(p)`·`load_books(p)` 컬럼 리스트 동일

    **1-f. `tests/data/test_goodbooks.py`** (4건) — 헬퍼 `_write_raw(raw: Path)` 가 4개 CSV 를 쓴다:
    ```
    ratings.csv:   user_id,book_id,rating / 1,1,5 / 1,2,3 / 2,1,4 / 2,3,2 / 1,1,4   ← 마지막 행은 (1,1) 중복(B1 dedupe 검증)
    books.csv:     book_id,goodreads_book_id,authors,original_publication_year,title,average_rating,ratings_count,image_url
                   1,100,"A. Author",2001,"Book One",4.1,1000,https://x/1.jpg
                   2,200,"B. Author",1999,"Book Two",3.9,500,https://x/2.jpg
                   3,300,"C. Author",,"Book Three",4.5,10,https://x/3.jpg
    book_tags.csv: goodreads_book_id,tag_id,count / 100,1,50 / 100,2,40 / 100,3,30 / 100,4,-1 / 200,2,10 / 200,3,5
    tags.csv:      tag_id,tag_name / 1,to-read / 2,fantasy / 3,fiction / 4,classics
    ```
    - `test_build_goodbooks_writes_contract_columns_and_null_ts` — `ip, bp = build_goodbooks(raw_dir=raw, out_dir=out)`; `list(pd.read_parquet(ip).columns) == [COL_USER, COL_ITEM, COL_TS, COL_EVENT, COL_RATING]`; `ts` 전부 `isna()`; `event` 전부 `"rating"`; **4행**(입력 5행 중 (1,1) 중복이 1행으로); rating dtype float
    - `test_build_goodbooks_maps_goodreads_id_and_drops_noise_tags` — `b = pd.read_parquet(bp).set_index(COL_ITEM)`; 컬럼 ⊇ {title, authors, image_url, average_rating, ratings_count, original_publication_year, categories, subcategories, tags}; `b.loc[1, "tags"] == "fantasy fiction"`(to-read 제외, count 음수 classics 제외, count 내림차순); `b.loc[2, "tags"] == "fantasy fiction"`; `b.loc[3, "tags"] == ""`; `len(b) == 3`
    - `test_build_goodbooks_dedupes_repeated_user_book_pairs` — `i = pd.read_parquet(ip)`; `i.duplicated([COL_USER, COL_ITEM]).sum() == 0`; `len(i) == 4`; `float(i[(i[COL_USER] == 1) & (i[COL_ITEM] == 1)][COL_RATING].iloc[0]) == 4.0`(`keep="last"` — 마지막 평점이 남는다; 첫 단정은 `duplicated().sum() == 0` 으로 두어 스텁의 빈 프레임에서도 `len == 4` 가 AssertionError 로 실패)
    - `test_download_is_idempotent_when_files_exist` — `_write_raw(raw)` 후 `monkeypatch.setattr("urllib.request.urlretrieve", lambda *a, **k: (_ for _ in ()).throw(AssertionError("network must not be called")))`; `paths = download_goodbooks(raw_dir=raw)`; `len(paths) == 4 and all(p.exists() for p in paths)`

    **1-g. RED 확인:** `uv run pytest tests/data/test_labels.py tests/data/test_split.py tests/data/test_onboarding.py tests/data/test_load.py tests/data/test_goodbooks.py --no-header 2>&1 | tail -40`. 기대: 전부 failed, 실패 사유는 `AssertionError`/`E  assert`/`Failed: DID NOT RAISE` 만. `ImportError`·`NameError`·`TypeError` 가 보이면 스텁 시그니처를 고친다(테스트를 고치지 않는다). 예상외 통과가 있으면 그 테스트가 무언가를 단정하지 않는 것이므로 단정을 보강한다. `uv run pytest tests/test_architecture.py --no-header` → 3 passed(스텁 import 경계 확인).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/data/test_labels.py tests/data/test_split.py tests/data/test_onboarding.py tests/data/test_load.py tests/data/test_goodbooks.py --no-header 2>&1 | tail -30; uv run pytest tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - 5개 src 파일과 5개 테스트 파일이 존재한다: `ls src/millie_rec/data/{goodbooks,load,split,onboarding,labels}.py tests/data/test_{goodbooks,load,split,onboarding,labels}.py`
    - `grep -c "def test_" tests/data/test_labels.py tests/data/test_split.py tests/data/test_onboarding.py tests/data/test_load.py tests/data/test_goodbooks.py` → 2, 6, 6, 2, 4 (합 20)
    - RED 출력에 `AssertionError` 또는 `E  assert` 또는 `DID NOT RAISE` 가 있고, `ImportError`·`ModuleNotFoundError`·`SyntaxError`·`NameError` 가 없다(출력을 SUMMARY 에 발췌)
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(retrieval\|evaluation\|serving\|app\)" src/millie_rec/data/` → 출력 없음
    - `git diff --stat -- scripts data/id_map.csv tests/data/test_millie_catalog.py tests/data/test_millie_edges.py tests/data/test_millie_parse.py` → 출력 없음(Track B 불변 — 자기 레인 파일만 확인, 형제 플랜 파일은 보지 않는다)
  </acceptance_criteria>
  <done>20개 테스트가 전부 단정 실패(AssertionError 계열)로 RED. 아키텍처 테스트 3 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): goodbooks·load·split·onboarding·labels 구현 → 20 passed</name>
  <files>src/millie_rec/data/goodbooks.py, src/millie_rec/data/load.py, src/millie_rec/data/split.py, src/millie_rec/data/onboarding.py, src/millie_rec/data/labels.py</files>
  <read_first>
    - Task 1 에서 만든 스텁 5파일과 테스트 5파일(단정이 요구하는 정확한 값)
    - .planning/phases/02-track-a/02-PATTERNS.md "data/load.py" Analog A~D(parquet 쓰기·mkdir·컬럼 상수 조립·dtype), "data/split.py"(`default_rng`), "data/onboarding.py"(두 상태 조립 형태)
    - .planning/phases/02-track-a/02-CONTEXT.md D-01~D-05, D-15, "Claude's Discretion — 다운로드 구현·event 값"
    - scripts/build_millie_catalog.py L239-245(parquet 쓰기 관례 — 읽기만, 수정 금지)
  </read_first>
  <action>
    각 파일 ≤150줄. **docstring·주석·문자열은 의미 변경 없이 100자 이내로 줄바꿈해도 된다(ruff E501 — 아래 1줄 docstring 이 100자를 넘으면 두 줄 docstring 으로)**. 모듈 머리는 PATTERNS "0. 공통" 형태(1줄 docstring + 정본 절 참조 → stdlib → 서드파티 → `millie_rec.contracts`/자기 슬라이스).

    **2-a. `src/millie_rec/data/goodbooks.py`** — docstring `"""Goodbooks-10k(Track A) 멱등 다운로드 + raw CSV → parquet 캐시(데이터 소스 01 §4-1, CONTEXT D-15). 네트워크는 여기만."""`
    ```python
    import logging
    import urllib.request
    from pathlib import Path

    import pandas as pd

    from millie_rec.contracts import COL_EVENT, COL_ITEM, COL_RATING, COL_TS, COL_USER, DIR_PROCESSED, DIR_RAW
    from millie_rec.data.load import BOOKS_FILE, INTERACTIONS_FILE

    log = logging.getLogger(__name__)
    GOODBOOKS_URL_BASE = "https://raw.githubusercontent.com/zygmuntz/goodbooks-10k/master/"  # HTTPS 고정. CC BY-SA 4.0
    FILES = ("ratings.csv", "books.csv", "book_tags.csv", "tags.csv")  # to_read.csv 미사용(CONTEXT 재량)
    RAW_SUBDIR = "goodbooks"
    EVENT_RATING = "rating"  # contracts.EVENT_TYPES 안. 데이터 사실 그대로
    TOP_TAGS = 20  # D-15: 책별 count 상위 20 태그
    # D-15: 서가 관리 잡음 태그 — 안 빼면 거의 모든 책이 같은 태그를 공유해 ILD 가 일괄 눌린다.
    # 실데이터 상위 빈도 확인 후 Plan 06 이 최대 1회 보충한다(PDF 각주 대상 아님).
    NOISE_TAGS = frozenset({
        "to-read", "currently-reading", "favorites", "favourites", "owned", "books-i-own", "owned-books", "i-own",
        "own", "kindle", "ebook", "ebooks", "e-book", "audiobook", "audiobooks", "audio", "library", "my-library",
        "my-books", "to-buy", "wish-list", "wishlist", "default", "re-read", "reread", "borrowed", "book-club",
        "all-time-favorites", "books", "series", "dnf", "did-not-finish", "abandoned", "read-in-2014",
        "read-in-2015", "read-in-2016", "read-in-2017", "2014", "2015", "2016", "2017",
    })
    BOOK_COLS = ("book_id", "title", "authors", "image_url", "average_rating", "ratings_count", "original_publication_year")  # books.csv 이름 그대로 = 계약 컬럼
    ```
    함수:
    - `download_goodbooks(raw_dir: Path = DIR_RAW / RAW_SUBDIR) -> list[Path]`: `raw_dir.mkdir(parents=True, exist_ok=True)`; 각 파일에 대해 `dst = raw_dir / name`; **`dst.exists() and dst.stat().st_size > 0` 이면 건너뜀**(멱등); 아니면 `tmp = dst.with_suffix(".part")`, `urllib.request.urlretrieve(GOODBOOKS_URL_BASE + name, tmp)`, `tmp.replace(dst)`, `log.info("downloaded %s (%d bytes)", name, dst.stat().st_size)`. 반환 `[raw_dir / n for n in FILES]`. `shell` 호출 없음, 경로는 `raw_dir` 아래만.
    - `_interactions(raw_dir) -> pd.DataFrame`: `pd.read_csv(raw_dir / "ratings.csv")` → `pd.DataFrame({COL_USER: r["user_id"].astype("int64"), COL_ITEM: r["book_id"].astype("int64"), COL_TS: pd.Series([None] * len(r), dtype="object"), COL_EVENT: EVENT_RATING, COL_RATING: r["rating"].astype(float)})`. 그 다음 **`.drop_duplicates([COL_USER, COL_ITEM], keep="last").reset_index(drop=True)`** — Goodbooks `ratings.csv` 에는 같은 (user, book) 쌍이 여러 번 나온다. dedupe 없이 holdout 을 하면 같은 쌍이 train/test 양쪽에 들어가 `harness.evaluate` 의 `ValueError("leak…")` 로 `make eval` 이 중단된다(checker B1). `keep="last"` 택 — 파일 순서상 뒤의 행을 최신 평점으로 본다. (`ts` 전부 None → `split()` holdout 트리거. parquet 은 null 컬럼을 허용한다.)
    - `_tags_per_book(raw_dir, books: pd.DataFrame) -> pd.Series`(index=book_id, value=str): `bt = read_csv(book_tags.csv)`; `tags = read_csv(tags.csv)`; `bt = bt[bt["count"] > 0]`; `bt = bt.merge(tags, on="tag_id")`; `bt = bt[~bt["tag_name"].isin(NOISE_TAGS)]`; **`goodreads_book_id → book_id` 매핑**: `bt = bt.merge(books[["book_id", "goodreads_book_id"]], on="goodreads_book_id")`; `bt = bt.sort_values(["book_id", "count"], ascending=[True, False])`; `bt = bt.groupby("book_id").head(TOP_TAGS)`; `bt.groupby("book_id")["tag_name"].agg(" ".join)`.
    - `_books(raw_dir) -> pd.DataFrame`: `b = read_csv(books.csv)`; `out = b[list(BOOK_COLS)].copy()`; `out["book_id"] = out["book_id"].astype("int64")`; `out["categories"] = [[] for _ in range(len(out))]`; `out["subcategories"] = [[] for _ in range(len(out))]`(Goodbooks 엔 분류 없음, Track A 미사용 — D-15); `out["tags"] = out["book_id"].map(_tags_per_book(raw_dir, b)).fillna("")`; `out.reset_index(drop=True)`.
    - `build_goodbooks(raw_dir: Path = DIR_RAW / RAW_SUBDIR, out_dir: Path = DIR_PROCESSED) -> tuple[Path, Path]`: `out_dir.mkdir(parents=True, exist_ok=True)`; `ip = out_dir / INTERACTIONS_FILE`; `bp = out_dir / BOOKS_FILE`; `_interactions(raw_dir).to_parquet(ip, index=False)`; `_books(raw_dir).to_parquet(bp, index=False)`; `log.info(...)`; `return ip, bp`.
    주의: `pd.read_csv(books.csv)` 의 `original_publication_year` 결측은 float NaN 그대로(계약은 nullable). 빈 리스트 컬럼은 pyarrow 가 `list<null>` 로 쓴다 — 테스트는 컬럼 존재만 단정한다. 만약 `to_parquet` 이 빈 리스트 컬럼에서 실패하면 `pd.Series([[]] * n, dtype=object)` 대신 `pd.Series([()] * n, dtype=object)` 를 시도하고, 그래도 실패하면 빈 문자열 `""` 로 두고 SUMMARY 에 기록한다(Track B `books_kr` 과의 형태 차이는 Track A 미사용 컬럼이라 허용).

    **2-b. `src/millie_rec/data/load.py`** — docstring `"""Track A parquet 읽기 + 최소 상호작용 필터(유저≥5·아이템≥5 — PDF 각주 상수, data.md Track A 규칙)."""`
    ```python
    INTERACTIONS_FILE = "interactions.parquet"
    BOOKS_FILE = "books.parquet"
    MIN_USER_INTERACTIONS = 5  # D-01 적격 필터. PDF 각주
    MIN_ITEM_INTERACTIONS = 5
    ```
    - `load_interactions(path: Path = DIR_PROCESSED / INTERACTIONS_FILE) -> pd.DataFrame` / `load_books(path: Path = DIR_PROCESSED / BOOKS_FILE) -> pd.DataFrame`: `pd.read_parquet(path)`.
    - `filter_min_interactions(df, *, min_user=MIN_USER_INTERACTIONS, min_item=MIN_ITEM_INTERACTIONS) -> pd.DataFrame`: 안정될 때까지 반복(아이템을 지우면 유저가 기준 아래로 내려갈 수 있다), 각 반복은 `value_counts` 2회 + 불리언 마스크 1회만:
      ```python
      while True:
          n = len(df)
          uc = df[COL_USER].value_counts()
          ic = df[COL_ITEM].value_counts()
          keep = (df[COL_USER].map(uc) >= min_user) & (df[COL_ITEM].map(ic) >= min_item)
          df = df[keep]
          if len(df) == n:
              break
      return df.reset_index(drop=True)
      ```
      벡터화만, 유저 루프 없음. Goodbooks 6M 행에서 반복은 보통 2~3회(각 수 초).

    **2-c. `src/millie_rec/data/labels.py`** — docstring `"""라벨 계층 한 곳 — Goodbooks positive = rating ≥ 4 (결정 '착수 전 운영 결정 4건'(개발일지 2026-09-04 항목 D45)). 미선택 책은 negative 가 아니다."""`
    - `POSITIVE_MIN_RATING = 4.0`
    - `is_positive(df: pd.DataFrame) -> pd.Series`: `return df[COL_RATING] >= POSITIVE_MIN_RATING` (벡터화 bool Series).
    - `relevant_sets(test: pd.DataFrame) -> dict[int, frozenset[int]]`: `pos = test[is_positive(test)]`; `return {int(u): frozenset(int(i) for i in items) for u, items in pos.groupby(COL_USER)[COL_ITEM]}`. 긍정이 없는 유저는 키가 없다(하네스에서 KeyError 가 아니라 `select_test_users` 가 test 긍정 ≥1 을 보장한다).

    **2-d. `src/millie_rec/data/split.py`** — docstring `"""Track A split 함수 하나 — ts 없으면 유저별 random holdout, 있으면 전역 시점 temporal(data.md Track A · CONTEXT D-02). 거짓으로 temporal 이라 쓰지 않는다."""`
    ```python
    TEST_FRAC = 0.2  # D-02. PDF 각주
    SPLIT_HOLDOUT = "holdout"
    SPLIT_TEMPORAL = "temporal"

    @dataclass(frozen=True, slots=True)  # DataFrame 필드 — eq/hash 비교하지 않는다(식별은 split_mode 문자열)
    class Split:
        train: pd.DataFrame
        test: pd.DataFrame
        split_mode: str
    ```
    - `split(df: pd.DataFrame, *, test_frac: float = TEST_FRAC, seed: int = SEED) -> Split`:
      1. `if not 0 < test_frac < 1: raise ValueError(...)`.
      2. `has_ts = COL_TS in df.columns and df[COL_TS].notna().any()`; `if has_ts and df[COL_TS].isna().any(): raise ValueError("ts 가 일부만 결측 — 분할 방식을 정할 수 없다")`.
      3. temporal(`has_ts`): `ts = pd.to_datetime(df[COL_TS])`; `cutoff = ts.quantile(1 - test_frac)`; `train = df[ts <= cutoff]`, `test = df[ts > cutoff]`(경계 동률은 train 으로 → `train.ts.max() <= cutoff < test.ts.min()` 이 성립); `assert train[COL_TS].max() < test[COL_TS].min()`(누수 단언, 규칙 원문); `split_mode = SPLIT_TEMPORAL`.
      4. holdout: `rng = np.random.default_rng(seed)`; `key = rng.random(len(df))`; `order = df.assign(_key=key).sort_values([COL_USER, "_key"], kind="stable")`; `size = order.groupby(COL_USER)[COL_USER].transform("size")`; `rank = order.groupby(COL_USER).cumcount()`; `n_test = np.where(size >= 2, np.maximum(1, np.floor(size * test_frac + 1e-9)), 0).astype(int)`; `is_test = rank.to_numpy() < n_test`; `test = order[is_test].drop(columns="_key")`, `train = order[~is_test].drop(columns="_key")`; `split_mode = SPLIT_HOLDOUT`.
      5. `return Split(train.reset_index(drop=True), test.reset_index(drop=True), split_mode)`.
      유저 루프 없음(groupby 벡터화). 같은 df·seed → 같은 결과(`kind="stable"` + 고정 rng).

    **2-e. `src/millie_rec/data/onboarding.py`** — docstring `"""온보딩 시뮬레이션 — train 긍정 중 seed 고정 무작위 5권만 explicit_seeds, 두 상태 n=0 / n≥k (main §5-2 · CONTEXT D-01·D-03·D-04)."""`
    ```python
    from millie_rec.contracts import COL_ITEM, COL_USER, N_ONBOARD_SEEDS, SEED, UserState
    from millie_rec.data.labels import is_positive

    K_HISTORY = 20  # D-04: seeds 외 train 행 ≥20 인 유저만 n≥k 표. PDF 각주
    N_TEST_USERS = 2000  # D-01: 전 variant·전 상태 공통 표본
    STATE_N0 = "n0"
    STATE_NK = f"n{K_HISTORY}"  # latest_states.csv 의 state 값(D-07)

    @dataclass(frozen=True, slots=True)
    class OnboardingStates:
        """n0 = seeds 만, n_k = seeds + 나머지 train 이력(≥K_HISTORY 유저만). 두 상태의 정답은 같다."""
        n0: tuple[UserState, ...]
        n_k: tuple[UserState, ...]
    ```
    - `select_test_users(train, test, *, n=N_TEST_USERS, n_seeds=N_ONBOARD_SEEDS, seed=SEED) -> tuple[tuple[int, ...], int]`: 벡터 연산으로 적격 마스크를 만든 뒤 `rng.choice` 1회(53k 유저 파이썬 루프 금지 — checker W7):
      ```python
      all_users = pd.Index(np.sort(train[COL_USER].unique()))  # 최소 필터를 통과한 유저 집합
      train_pos = train[is_positive(train)].groupby(COL_USER).size().reindex(all_users, fill_value=0)
      test_pos = test[is_positive(test)].groupby(COL_USER).size().reindex(all_users, fill_value=0)
      eligible = all_users[(train_pos.to_numpy() >= n_seeds) & (test_pos.to_numpy() >= 1)]
      n_excluded = len(all_users) - len(eligible)
      rng = np.random.default_rng(seed)
      size = min(n, len(eligible))
      chosen = np.sort(rng.choice(eligible.to_numpy(), size=size, replace=False)) if size else np.array([], dtype=int)
      return tuple(int(u) for u in chosen), int(n_excluded)
      ```
      (`all_users` 를 정렬해 두면 `rng.choice` 의 결과가 groupby 순서에 의존하지 않는다 — 재현성.)
    - `mask_onboarding(train, users, *, n=N_ONBOARD_SEEDS, k_history=K_HISTORY, seed=SEED) -> OnboardingStates`: **첫 줄 `train = train[train[COL_USER].isin(users)]`**(선필터 — 53k 유저·4.8M 행 전체에 groupby.apply 를 돌리지 않는다, 동작 변화 없음, checker W6); `rng = np.random.default_rng(seed)`; `pos = train[is_positive(train)]`; `items_by_user = train.groupby(COL_USER)[COL_ITEM].apply(lambda s: tuple(int(i) for i in s))`; `pos_by_user = pos.groupby(COL_USER)[COL_ITEM].apply(list)`; `n0, nk = [], []`; `for u in users:`(유저 루프는 여기만 — 평가용 2,000명) `cand = sorted(pos_by_user[u]); seeds = tuple(int(i) for i in sorted(rng.choice(cand, size=n, replace=False)))`; `n0.append(UserState(user_id=int(u), explicit_seeds=seeds))`; `rest = tuple(i for i in items_by_user[u] if i not in set(seeds))`; `if len(rest) >= k_history: nk.append(UserState(user_id=int(u), explicit_seeds=seeds, history=rest))`; `return OnboardingStates(tuple(n0), tuple(nk))`. **negative 구조를 만들지 않는다** — 미선택 긍정·비긍정 어느 것도 별도 필드·컬럼으로 남기지 않는다. `cand` 를 정렬하는 이유: groupby 순서 의존 없이 seed 재현.

    **2-f. GREEN 확인:** `uv run pytest tests/data tests/test_architecture.py --no-header` → Track A 20 passed + Track B 기존(test_millie_*) + 아키텍처 3, `failed 0`. **전체 트리 스위트는 이 플랜에서 돌리지 않는다**(wave 1 형제 플랜의 RED 파일과 경합 — 전역 게이트는 wave 종료 후 Advisor 1회, checker B3). 실패가 남으면 **테스트가 아니라 구현을 고친다**(단, 테스트의 손계산 오류가 확실하면 계산 근거를 SUMMARY 에 적고 고친다).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/data --no-header && uv run pytest tests/test_architecture.py --no-header && wc -l src/millie_rec/data/*.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/data/test_labels.py tests/data/test_split.py tests/data/test_onboarding.py tests/data/test_load.py tests/data/test_goodbooks.py --no-header` → `20 passed`
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -n 'drop_duplicates(\[COL_USER, COL_ITEM\], keep="last")' src/millie_rec/data/goodbooks.py` 1행(B1)
    - `grep -n "train = train\[train\[COL_USER\].isin(users)\]" src/millie_rec/data/onboarding.py` 1행(W6); `grep -c "for u in all_users" src/millie_rec/data/onboarding.py` == 0(W7)
    - `wc -l src/millie_rec/data/goodbooks.py src/millie_rec/data/load.py src/millie_rec/data/split.py src/millie_rec/data/onboarding.py src/millie_rec/data/labels.py` 각 ≤ 150
    - `grep -n 'GOODBOOKS_URL_BASE = "https://raw.githubusercontent.com/zygmuntz/goodbooks-10k/master/"' src/millie_rec/data/goodbooks.py` 1행; `grep -c "shell=True\|subprocess" src/millie_rec/data/goodbooks.py` == 0; `grep -n "st_size > 0" src/millie_rec/data/goodbooks.py` ≥1행(멱등 검사)
    - `grep -n "^TEST_FRAC = 0.2" src/millie_rec/data/split.py`, `grep -n "^K_HISTORY = 20" src/millie_rec/data/onboarding.py`, `grep -n "^N_TEST_USERS = 2000" src/millie_rec/data/onboarding.py`, `grep -n "^MIN_USER_INTERACTIONS = 5" src/millie_rec/data/load.py`, `grep -n "^MIN_ITEM_INTERACTIONS = 5" src/millie_rec/data/load.py`, `grep -n "^TOP_TAGS = 20" src/millie_rec/data/goodbooks.py`, `grep -n "^POSITIVE_MIN_RATING = 4.0" src/millie_rec/data/labels.py` 각 1행
    - `grep -n 'SPLIT_HOLDOUT = "holdout"' src/millie_rec/data/split.py` 1행; `grep -n "max() < " src/millie_rec/data/split.py` ≥1행(temporal 누수 단언)
    - `awk 'length > 100' src/millie_rec/data/*.py tests/data/test_goodbooks.py tests/data/test_load.py tests/data/test_split.py tests/data/test_onboarding.py tests/data/test_labels.py | wc -l` == 0(E501)
    - `grep -c "negative\|neg_" src/millie_rec/data/onboarding.py` == 0 (구조 없음 — 주석의 "negative 가 아니다" 문구는 labels.py docstring 에만)
    - `grep -n "default_rng(seed)" src/millie_rec/data/split.py src/millie_rec/data/onboarding.py` ≥2행; `grep -rn "\"user_id\"\|\"book_id\"\|\"rating\"" src/millie_rec/data/split.py src/millie_rec/data/onboarding.py src/millie_rec/data/labels.py src/millie_rec/data/load.py` → 출력 없음(컬럼 리터럴 금지; goodbooks.py 의 raw CSV 열 이름 리터럴은 원본 스키마라 허용)
  </acceptance_criteria>
  <done>Track A 20건 GREEN, 아키텍처 3 passed, 5파일 각 ≤150줄, 상수 값이 CONTEXT D-01~D-05·D-15 와 문자 단위로 일치. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR): data/__init__.py 공개 표면 + ruff + 전체 스위트 + make smoke</name>
  <files>src/millie_rec/data/__init__.py</files>
  <read_first>
    - src/millie_rec/serving/__init__.py (공개 표면 정확한 형태 — exact analog)
    - src/millie_rec/data/__init__.py (현재 `__all__: list[str] = []`)
    - .planning/phases/02-track-a/02-PATTERNS.md "public surface" 절(노출 이름 목록)
    - Makefile L33-40 (`smoke`)
  </read_first>
  <action>
    **3-a. `src/millie_rec/data/__init__.py`** 를 다음으로 교체(알파벳 정렬 `__all__`; `app/cli.py`(Plan 05)가 `from millie_rec.data import …` 로만 가져오므로 상수도 노출한다 — D-08 json 메타에 `test_frac`·`k_history`·`min_*` 를 기록해야 하고 중복 정의보다 노출이 낫다):
    ```python
    """data 슬라이스 공개 표면. 다른 슬라이스는 여기 있는 이름만 import 할 수 있다."""

    from millie_rec.data.goodbooks import NOISE_TAGS, RAW_SUBDIR, TOP_TAGS, build_goodbooks, download_goodbooks
    from millie_rec.data.labels import POSITIVE_MIN_RATING, is_positive, relevant_sets
    from millie_rec.data.load import (
        BOOKS_FILE,
        INTERACTIONS_FILE,
        MIN_ITEM_INTERACTIONS,
        MIN_USER_INTERACTIONS,
        filter_min_interactions,
        load_books,
        load_interactions,
    )
    from millie_rec.data.onboarding import (
        K_HISTORY,
        N_TEST_USERS,
        STATE_N0,
        STATE_NK,
        OnboardingStates,
        mask_onboarding,
        select_test_users,
    )
    from millie_rec.data.split import TEST_FRAC, Split, split

    __all__ = [
        "BOOKS_FILE",
        "INTERACTIONS_FILE",
        "K_HISTORY",
        "MIN_ITEM_INTERACTIONS",
        "MIN_USER_INTERACTIONS",
        "NOISE_TAGS",
        "N_TEST_USERS",
        "POSITIVE_MIN_RATING",
        "RAW_SUBDIR",
        "STATE_N0",
        "STATE_NK",
        "TEST_FRAC",
        "TOP_TAGS",
        "OnboardingStates",
        "Split",
        "build_goodbooks",
        "download_goodbooks",
        "filter_min_interactions",
        "is_positive",
        "load_books",
        "load_interactions",
        "mask_onboarding",
        "relevant_sets",
        "select_test_users",
        "split",
    ]
    ```
    `uv run python -c "import millie_rec.data as d; print(len(d.__all__))"` → 25 (순환 import 없음 확인 — goodbooks → load 단방향). `INTERACTIONS_FILE`·`BOOKS_FILE`·`RAW_SUBDIR` 를 노출하는 이유: Plan 05 `app/cli.py` 가 `--processed <dir>`·`--raw` 인자 기본값을 리터럴 없이 만들기 위함.

    **3-b. 정리(이 플랜이 만든 파일 안에서만):** `uv run ruff format src/millie_rec/data tests/data && uv run ruff check src/millie_rec/data tests/data`. 줄 수 재확인. 행동 변경 없음 — Refactor 후 `uv run pytest tests/data tests/test_architecture.py --no-header` 재통과.

    **3-c. 전역 게이트는 이 플랜에서 돌리지 않는다(checker B3):** 전체 트리 `uv run pytest --no-header`·`uv run ruff check .`·`make smoke`(고정 포트 8010 + 공유 `.uvicorn.pid`) 는 wave 1 의 세 플랜(01·02·03)이 **모두 끝난 뒤 Advisor(오케스트레이터)가 1회** 실행한다 — 형제 플랜의 RED 상태·미포맷 파일·동시 smoke 와 경합하기 때문. 이 플랜은 서버 코드를 건드리지 않으므로 smoke 는 Phase 1 '로컬 서빙 스켈레톤'(.planning/ROADMAP.md) 과 동일하게 통과해야 한다(level 3). 절차는 02-06-PLAN.md `<wave_gate>` 절.

    **3-d. SUMMARY 준비:** `git diff --stat -- src/millie_rec/data tests/data` 로 변경 파일 목록, RED/GREEN pytest 요약 줄, `wc -l` 결과, "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 문장.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/data tests/data && uv run ruff check src/millie_rec/data tests/data && uv run pytest tests/data tests/test_architecture.py --no-header 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import millie_rec.data as d; print(len(d.__all__))"` → `25`; `grep -c '"mask_onboarding"' src/millie_rec/data/__init__.py` == 1; `grep -c '"split"' src/millie_rec/data/__init__.py` == 1
    - `uv run ruff format --check src/millie_rec/data tests/data` 와 `uv run ruff check src/millie_rec/data tests/data` 모두 클린(`All checks passed!`)
    - `uv run pytest tests/data tests/test_architecture.py --no-header` 요약 줄에 `failed` 없음(Track A 20 + Track B 기존 + 아키텍처 3; skip 2 유지)
    - `git log --oneline | head -1` 이 플랜 시작 전과 동일(커밋 0)
    - `git diff --stat -- scripts data/id_map.csv src/millie_rec/contracts.py Makefile pyproject.toml tests/data/test_millie_catalog.py tests/data/test_millie_edges.py tests/data/test_millie_parse.py` → 출력 없음(자기 레인 불변 파일만 확인)
    - (전역 게이트 — 이 플랜의 acceptance 아님) 전체 `uv run pytest --no-header`·`ruff check .`·`make smoke` 는 wave 1 종료 후 Advisor 가 1회 실행(02-06-PLAN.md `<wave_gate>`)
  </acceptance_criteria>
  <done>공개 표면 25개 이름, 자기 경로 ruff 클린, `tests/data` + 아키텍처 통과. 전역 게이트는 wave 종료 후 Advisor. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw → `goodbooks.py` `download_goodbooks` | 외부 네트워크 입력(CSV 4개). `make data` 에서만 실행, 테스트는 절대 호출 안 함 |
| raw CSV → parquet 빌드 | 데이터 파일 파싱(pandas). 코드 실행 경로 없음 |
| `select_test_users`/`mask_onboarding` → `UserState` | test 라벨이 UserState 로 넘어가는지의 누수 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `download_goodbooks` URL·경로 | mitigate | `GOODBOOKS_URL_BASE` HTTPS 리터럴 상수 + `FILES` 고정 튜플만 결합(사용자 입력 없음). 파일은 `raw_dir/<name>` 아래에만, `.part` 임시 파일 후 `replace`(부분 다운로드가 완성본으로 남지 않음). `shell=True`·`subprocess` 0. 수용 기준 grep 으로 확인 |
| T-02-02 | Denial of Service | `download_goodbooks` 재다운로드 | mitigate | 존재+크기>0 검사로 멱등(≈69MB 반복 다운로드 방지). 재시도 루프 없음 — 실패는 예외로 전파해 사람이 본다 |
| T-02-03 | Tampering (integrity) | raw CSV 위·변조 | accept | 공개 데이터셋(CC BY-SA 4.0)·해시 고정은 5일 범위 밖. 결과 재현은 `results/eval_*.json` 의 행 수·seed·sha 로 사후 확인 |
| T-02-04 | Information Disclosure | 평가 누수(test → UserState) | mitigate | `mask_onboarding` 은 train 만 받는다(test 인자 없음). `select_test_users` 는 test 를 적격 판정에만 쓴다. 테스트 `test_user_state_has_no_negative_structure` 가 test 아이템 ∉ seen 을 단정; 하네스(Plan 03) 가 `seen ∩ R_u == ∅` 를 재단정 |
| T-02-05 | Repudiation | 분할 방식 거짓 표기 | mitigate | `split()` 이 데이터에서 `split_mode` 를 결정하고 temporal 은 `assert train.ts.max() < test.ts.min()`. 사람이 값을 고를 수 없다(인자 없음) |
| T-02-06 | Elevation of Privilege | parquet 쓰기 경로 | accept | `out_dir` 기본 `contracts.DIR_PROCESSED`, `mkdir(parents=True, exist_ok=True)`. CLI(Plan 05)만 호출, 요청 입력이 경로에 닿지 않음 |
</threat_model>

<verification>
- `uv run pytest tests/data tests/test_architecture.py --no-header` → Track A 20 passed + Track B 기존 통과(skip 2 유지) + 아키텍처 3, failed 0
- `uv run ruff format --check src/millie_rec/data tests/data && uv run ruff check src/millie_rec/data tests/data` 클린
- `wc -l src/millie_rec/data/*.py` 전부 ≤150
- (wave 1 종료 후 Advisor 1회) `uv run pytest --no-header` N ≥ 124 passed·2 skipped·failed 0 · `uv run ruff check .` · `make smoke` → `smoke: PASS`
- 네트워크 호출 0: 테스트 실행 중 `data/raw/goodbooks/` 가 생기지 않는다(`ls data/raw/goodbooks 2>/dev/null` 빈 결과)
- 커밋 0: `git log --oneline | head -1` 불변
</verification>

<success_criteria>
- **EVAL-01** 의 코드 몫: `download_goodbooks`(멱등·HTTPS)·`build_goodbooks`(interactions.parquet·books.parquet 계약 컬럼) 존재, tmp CSV 로 검증. 실행(`make data`)은 Plan 06
- **EVAL-02** `split()` — ts 없으면 `split_mode="holdout"` 유저별 20% seed 고정, 있으면 temporal + 누수 단언. fixture 로 두 분기 모두 테스트
- **EVAL-03** `mask_onboarding` — 정확히 5개 seeds ⊂ train 긍정, `UserState` 5필드 외 구조 없음(수용 기준 '미선택 책 ≠ 부정 신호'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)), n≥k 상태는 K_HISTORY 이상 유저만
- CONTEXT 결정 D-01(적격·N_TEST_USERS·n_excluded)·D-02·D-03·D-04·D-05(라벨 rating≥4, seen = train 전체)·D-15(TOP_TAGS·NOISE_TAGS·categories 빈 리스트)·D-16(data 필수 테스트)가 코드·테스트에 존재
- Track B 파일 무변경, `contracts.py` 무변경, 의존성 추가 0, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/02-track-a/02-01-SUMMARY.md` 를 만든다. 포함: RED(어떤 단정이 왜 실패했나 — AssertionError 발췌 3건 이상) · GREEN(파일별 구현 요지, 상수 값) · REFACTOR(`__init__` 25 이름·ruff·`wc -l`) · `## TDD Gate Compliance`(커밋 게이트 생략 사유 = 프로젝트 커밋 정책 D-18, 대체 증거 = pytest 요약 줄 RED/GREEN/전체 3개) · 변경 파일 목록(`git diff --stat -- src/millie_rec/data tests/data` + `git status --short -- src/millie_rec/data tests/data`) · 빈 리스트 컬럼 parquet 처리 결과(2-a 주의 사항 중 무엇을 택했나) · "`../.claude/rules/data.md` 'ts 있으면 시간순 첫 n권' 분기는 결정 'D-03 seeds'(.planning/phases/02-track-a/02-CONTEXT.md) 잠금으로 미구현 — UCSD Poetry temporal 레인 착수 시 재검토" 1줄 · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
