---
phase: 04-freeze
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/millie_rec/serving/api.py
  - src/millie_rec/data/vectors_kr.py
  - src/millie_rec/app/pipeline.py
  - src/millie_rec/app/pipeline_kr.py
  - src/millie_rec/app/cli.py
  - src/millie_rec/app/server.py
  - tests/serving/test_api_weights.py
  - tests/data/test_vectors_kr.py
  - tests/app/test_pipeline.py
  - tests/app/test_cli.py
  - tests/app/test_server_catalog.py
  - tests/app/test_variants.py
autonomous: true
requirements: [REC-01, REC-02, REC-03]
lane: 조립(Advisor 직접 — app/·serving/api.py weights 배선·data/vectors_kr.py 소변경 승인)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05)
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/schemas.py
  - src/millie_rec/serving/schemas_should.py
  - src/millie_rec/serving/compose.py
  - src/millie_rec/serving/state.py
  - src/millie_rec/serving/nearline.py
  - src/millie_rec/retrieval/**
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - scripts/**
  - data/raw/**
  - data/id_map.csv
  - demo/**
  - tests/test_architecture.py
  - tests/serving/test_smoke.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "fit_pipelines(train, vectors) 는 키가 정확히 contracts.VARIANTS 4종인 dict 를 돌려주고, pop 은 PopPipeline(.retriever 보존), cf/hybrid/hybrid_div 는 StagedPipeline(슬롯 retriever → blend_channels → rerankers → 상위 k) 이며 어느 variant 도 user.seen 을 반환하지 않는다(REC-02)"
    - "서버가 Track B 카탈로그 + content_vectors_kr.npz 를 읽으면 GET /api/recommend?model=<v>&seeds=1,2,3 4종이 각각 model_version <v>_v1 level 0 을 주고, model 없이 부르면 hybrid_div_v1 이며, 20권 fixture 에서 pop=[4..13]·cf=[4,5,6,7,8]·hybrid 상위 10 에 7 이 있고 hybrid_div 상위 10 에 7 이 없다(가드) — 4 variant 가 서로 다른 책"
    - "create_app(..., weights=state_weights) 를 주입하면 level 0 응답 user_state_weights 가 seeds 만인 사용자에게 {alpha: 1.0, beta: 0.0, gamma: 0.0}(β=0, REC-03) 이고 익명(level 3)은 0 셋이며, weights 미주입이면 기존처럼 0 셋(하위 호환)"
    - "Track B 4 variant 의 모든 items 는 catalog.eligible 을 통과한 책만(18·19·20 없음) 이고 hybrid_div items[0].title 이 한국어 메타로 채워진다"
    - "make smoke PASS · uv run pytest --no-header 는 기준선(255 passed) 위로 증가하고 test_coverage_gate 1건(재수집 중 의도된 RED) 외 실패 0"
  artifacts:
    - path: "src/millie_rec/app/pipeline.py"
      provides: "POOL 사용 StagedPipeline · PopPipeline · fit_pipelines(train, vectors, *, knn_artifact, weights) · load_catalog · build_pipelines(artifact, *, catalog, weights)"
      exports: ["StagedPipeline", "PopPipeline", "fit_pipelines", "build_pipelines", "load_catalog"]
      max_lines: 150
    - path: "src/millie_rec/app/pipeline_kr.py"
      provides: "CatalogPopPipeline · WithMeta · load_vectors_kr · build_pipelines_kr(catalog, *, weights, vectors)"
      exports: ["CatalogPopPipeline", "WithMeta", "load_vectors_kr", "build_pipelines_kr"]
      max_lines: 150
    - path: "src/millie_rec/serving/api.py"
      provides: "create_app(..., weights: Callable[[UserState], dict[str, float]] | None = None) + dataclasses.replace 로 user_state_weights 채움"
      contains: "weights"
      max_lines: 150
    - path: "src/millie_rec/app/server.py"
      provides: "build_pipelines(catalog=catalog, weights=state_weights) · create_app(..., weights=state_weights)"
      contains: "weights=state_weights"
    - path: "src/millie_rec/data/vectors_kr.py"
      provides: "VectorsKR.book_ids 읽기 전용 프로퍼티(≤3줄, Advisor 승인)"
      contains: "def book_ids"
    - path: "tests/app/test_variants.py"
      provides: "서버 4 variant 상이 · 기본 hybrid_div · user_state_weights β=0 · eligible 필터 · 한국어 meta"
      min_lines: 50
    - path: "tests/serving/test_api_weights.py"
      provides: "create_app(weights=) level 0 채움 · 익명 0 · 미주입 0 · weights 예외 → level 3 200"
      min_lines: 30
  key_links:
    - from: "src/millie_rec/app/pipeline.py"
      to: "src/millie_rec/ranking/__init__.py"
      via: "from millie_rec.ranking import CH_CF, CH_CONTENT, CH_POP, CHANNEL_WEIGHTS, blend_channels"
      pattern: "from millie_rec.ranking import"
    - from: "src/millie_rec/app/pipeline.py"
      to: "src/millie_rec/reranking/__init__.py"
      via: "rerankers=(MMRReranker(vectors), DifficultyGuard(book_stats)) — D-08 순서"
      pattern: "MMRReranker\\("
    - from: "src/millie_rec/app/server.py"
      to: "src/millie_rec/serving/api.py"
      via: "create_app(weights=state_weights) — 표시 혼합비 = 실제 혼합비(D-09)"
      pattern: "weights=state_weights"
    - from: "src/millie_rec/serving/api.py"
      to: "src/millie_rec/serving/compose.py"
      via: "build_response 결과를 dataclasses.replace(resp, user_state_weights=…) 로 덮음(compose.py 무변경)"
      pattern: "replace\\(resp, user_state_weights="
    - from: "src/millie_rec/app/pipeline_kr.py"
      to: "src/millie_rec/data/__init__.py"
      via: "VectorsKR.load · VECTORS_KR_NPZ · CatalogKR(neighbors·stats·eligible·meta)"
      pattern: "VectorsKR"
---

<objective>
Phase 4 '추천 파이프라인과 모델 freeze'(.planning/ROADMAP.md "Phase 4: 추천 파이프라인과 모델 freeze")의 **조립**: wave 1 이 만든 세 슬라이스를 `app/` 이 `contracts.VARIANTS` 4종 dict 로 잇는다 — Track A `fit_pipelines(train, vectors)`(make eval), Track B `build_pipelines(catalog=, weights=)`(서버, `pipeline_kr.py` 분리), `serving/api.py::create_app(weights=)` + `app/server.py` 주입, `data/vectors_kr.py::book_ids` 소변경, 기존 테스트 4건의 의도된 갱신, 신규 테스트(4 variant 상이·`user_state_weights` β=0).

Purpose: ROADMAP Success Criteria 1 후반("서버에서 `model=` 쿼리로 4 variant 가 서로 다른 책") · 3("`user_state_weights` 가 재정규화되어 신규 β=0") 의 코드 증거. **REC-02**(4종 dict 조립·hybrid 가중합) · **REC-03**(응답 β=0 경로) · **REC-01**(세 retriever 가 실제로 조립돼 후보를 낸다). 결정 D-09(`create_app(weights=)`)·D-10(Track B 재료)·D-11(기본 variant 자동 이동·판정 fixture) 의 이행. Plan 04-05 `make eval` 4행 실측의 전제.
Output: `app/` 3파일 수정 + 1 신규, `serving/api.py` ≈8줄, `data/vectors_kr.py` 3줄, 테스트 신규 2 + 갱신 4. 커밋 0.

**Advisor 영역:** `app/`·`serving/api.py` 배선·`data/vectors_kr.py` 는 Advisor 직접 편집 영역(../CLAUDE.md §6). 이 플랜은 Advisor(오케스트레이터) 세션이 실행한다. Worker 위임 시에도 브리프는 이 플랜 본문을 그대로 쓴다.

**건드리지 않는 것:** `serving/compose.py`(Phase 5 가 Day 3 병행 증분 — `user_state_weights` 0 고정 자리는 api.py 에서 `dataclasses.replace` 로 덮는다)·`schemas*.py`(freeze)·`state.py`·`nearline.py`·`tests/serving/test_smoke.py`(L171-173·L193-195 가 `load_catalog()` 0인자·`build_pipelines(**_)` keyword 호출을 전제 — **server.py 는 keyword 인자 추가만**).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-freeze/04-CONTEXT.md
@.planning/phases/04-freeze/04-PATTERNS.md
@.planning/phases/04-freeze/04-01-SUMMARY.md
@.planning/phases/04-freeze/04-02-SUMMARY.md
@.planning/phases/04-freeze/04-03-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/app/pipeline.py
@src/millie_rec/app/cli.py
@src/millie_rec/app/server.py
@src/millie_rec/serving/api.py
@src/millie_rec/serving/compose.py
@src/millie_rec/data/vectors_kr.py
@src/millie_rec/data/catalog_kr.py
@src/millie_rec/retrieval/__init__.py
@src/millie_rec/ranking/__init__.py
@src/millie_rec/reranking/__init__.py
@tests/conftest.py
@tests/app/test_pipeline.py
@tests/app/test_cli.py
@tests/app/test_server_catalog.py
@tests/serving/test_recommend_level0.py
@tests/serving/test_smoke.py
@tests/data/test_vectors_kr.py
@tests/test_architecture.py
@../.claude/rules/architecture.md
@../.claude/rules/serving.md
@../.claude/rules/local-run.md
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/data.md

<project_rules_inline>
**star 의존(`../.claude/rules/architecture.md`, `tests/test_architecture.py` L41-42):** "`app`은 다른 슬라이스의 **공개 이름만** import한다: `from millie_rec.retrieval import ItemKNNRetriever` ✅ / `from millie_rec.retrieval.itemknn import ...` ❌" — `len(parts) > 2` 면 위반. `app` 내부끼리(`millie_rec.app.pipeline_kr`)는 자유. "누구도 `millie_rec.app`을 import하지 않는다." `serving/api.py` 는 `contracts` + `serving` 내부만.

**계약 변경 금지(`../.claude/rules/architecture.md`):** "`contracts.py`… Day 1 종료 시 freeze… 필드 추가는 기본값 있는 optional로만." `create_app` 시그니처 규칙(Phase 1 D-09, `.planning/phases/01-local-serving-skeleton/01-CONTEXT.md`): **기본값 있는 인자 추가만** — `weights: Callable[[UserState], dict[str, float]] | None = None` keyword-only. `../.claude/rules/serving.md` 의 `create_app` 문구 갱신은 Plan 04-06(Advisor 문서).

**serving(`../.claude/rules/serving.md`):** "서빙 점수 함수는 `retrieval/`·`ranking/` 슬라이스 함수를 app이 주입한 그대로(skew 방지)." "artifacts는 기동 시 1회 로드 … 요청마다 fit·재계산 금지." "HTTP 핸들러는 전부 동기 `def`." **서빙 경로에서 pandas import 금지** — `pipeline_kr.py` 는 pandas 를 import 하지 않는다(`VectorsKR`·`CatalogKR` 는 numpy·json 만).

**local-run(`../.claude/rules/local-run.md`):** "아티팩트·DB·네트워크 없이 기동한다." `content_vectors_kr.npz` 가 없거나 손상이면 `{pop: CatalogPopPipeline}` 만 등록하고 기동(`log.info`/`log.exception`), 예외로 죽지 않는다. `make smoke` = 8010 포트 `/health`·`/`·`/api/recommend?seeds=1,2,3&k=5` 3개 200.

**python(`../.claude/rules/python.md`):** 파일 ≤150줄(`app/pipeline.py` 110 → 분리 승인, Advisor 확정 6). 상수는 모듈 상단. `uv run` 만. 완료 순서 `uv run ruff format . && uv run ruff check . && uv run pytest --no-header`.

**기존 테스트 갱신 = 의도된 변경(Advisor 확정 5, D-15 "기존 테스트 단정 수정 금지" 의 명시적 예외):** 이 플랜이 바꾸는 단정은 Task 2·3 action 에 열거된 것만. `tests/test_architecture.py`·`tests/serving/test_smoke.py` 는 고치지 않는다.

**Track B 표기(`../.claude/rules/data.md`):** Track B `cf`·`hybrid` 이웃 채널은 `source="content"`(`NeighborRetriever` 기본값), `itemknn` 금지. 텍스트(`description`) 는 어디에도 싣지 않는다(`CatalogKR` 가 이미 제거).

**완료 기준:** `uv run ruff format . && uv run ruff check . && uv run pytest --no-header`(기준선 255 passed + wave 1 54 + 이 플랜 신규 ≥ 8 → **≥ 317 passed**, 실패는 `test_coverage_gate` 1건만) + `make smoke` PASS. 이 플랜은 wave 2 단독이라 전역 게이트를 직접 돌린다.
</project_rules_inline>

<wave_gate>
## Wave 1 게이트 — 이 플랜 Task 1 시작 전 Advisor 가 1회 실행

wave 1 세 플랜(04-01·02·03)은 자기 경로만 검증했다. 전역 게이트를 여기서 1회 돈다(모두 `/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec` 에서):

| 명령 | 기대 |
|---|---|
| `ls .planning/phases/04-freeze/04-01-SUMMARY.md .planning/phases/04-freeze/04-02-SUMMARY.md .planning/phases/04-freeze/04-03-SUMMARY.md` | 3파일 존재 |
| `uv run ruff format --check . && uv run ruff check .` | 클린 |
| `uv run pytest --no-header` | `309 passed, 1 failed`(255 + retrieval 20 + ranking 18 + reranking 16 = 309; 1 failed = `tests/data/test_millie_catalog.py::test_coverage_gate` 재수집 중 의도) — 다른 failed 가 있으면 소유 플랜에 수정 브리프 |
| `make smoke` | `smoke: PASS`(서버 코드 무변경 — Phase 3 상태) |
| `uv run python -c "import millie_rec.retrieval, millie_rec.ranking, millie_rec.reranking, sys; print('scipy' in sys.modules, 'sklearn' in sys.modules)"` | `False False` |

게이트 실패 시: 실패 파일의 소유 플랜(`files_modified`)에 수정 브리프를 재위임하고 그 플랜의 acceptance 부터 다시 확인한다. `make smoke` 실패 시 `lsof -i :8010`·`rm -f .uvicorn.pid` 로 잔존 프로세스 정리 후 1회 재실행.
</wave_gate>

<interfaces>
<!-- wave 1 이 만든 공개 시그니처(SUMMARY 로 재확인). app 은 이 이름만 import 한다. -->

From src/millie_rec/retrieval/__init__.py (Plan 04-01):
```python
KNN_ARTIFACT: Path; KNN_TOP = 50; POOL = 200; SOURCE_CONTENT = "content"; SOURCE_ITEMKNN = "itemknn"; POP_ARTIFACT; TOP_N = 1000
class ItemKNNRetriever:  # name="cf"; fit(train); retrieve(user,k); neighbors(book_id,n); save/load
    def __init__(self, book_ids=None, nbr_ids=None, nbr_sims=None, *, meta=(0,0,0), weights: WeightFn | None = None)
def load_or_fit_itemknn(train, path: Path | None = None, *, weights=None) -> ItemKNNRetriever
class NeighborRetriever:  # name="content"
    def __init__(self, neighbors: Neighbors, *, source: str = "content", weights=None, n_neighbors: int = 20)
class ContentVectors:  # ItemVectors + CandidateGenerator(name="content", fit no-op, retrieve)
    def __init__(self, books: pd.DataFrame, text_col: str = "tags", *, weights=None)
class VectorRetriever:  # name="content"
    def __init__(self, vectors: ItemVectors, ids: Sequence[int], *, source: str = "content", weights=None)
class PopularityRetriever:  # 기존. __init__(ranked: Sequence[tuple[int, float]] = ())
```

From src/millie_rec/ranking/__init__.py (Plan 04-02):
```python
CH_CF, CH_CONTENT, CH_POP = "cf", "content", "pop"
CHANNEL_WEIGHTS = {"cf": 0.5, "content": 0.3, "pop": 0.2}
def state_weights(user: UserState, *, reset_boost=False, session_active=False) -> dict[str, float]
def blend_channels(user, channels: dict[str, list[Candidate]], weights: dict[str, float] | None = None, *, book_stats: BookStatsSource | None = None) -> list[ScoredItem]
```

From src/millie_rec/reranking/__init__.py (Plan 04-03):
```python
class MMRReranker:  def __init__(self, vectors: ItemVectors, *, lam: float = 0.7, pool: int = 50)
class DifficultyGuard:  def __init__(self, book_stats: BookStatsSource | None = None, *, top_n: int = 10, resid_z_max: float = -1.0, min_completed: int = 3)
# 둘 다 rerank(user, items, k) -> list[ScoredItem]
```

From src/millie_rec/data/__init__.py (기존):
```python
class CatalogKR:  # Catalog + Neighbors + BookStatsSource. load(serving_dir) · meta(ids) · popular(categories=(), n=50) · eligible(ids) · neighbors(book_id, n=20) · stats(ids) · user_level(user) -> None
class VectorsKR:  # ItemVectors. load(path = DIR_SERVING / VECTORS_KR_NPZ) · vectors(ids) · dim ; (이 플랜) book_ids 프로퍼티 추가
BOOKS_KR_JSON = "books_kr.json"; DIR_SERVING; VECTORS_KR_NPZ = "content_vectors_kr.npz"
```

From src/millie_rec/serving/compose.py (읽기만):
```python
ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}
def default_variant(pipelines) -> str | None   # 등록된 것 중 VARIANTS 순서 마지막 → 4종 등록 시 "hybrid_div"
def build_response(items, *, model_version, level, k, t0, context) -> RecommendResponse   # user_state_weights=dict(ZERO_WEIGHTS)
```

From src/millie_rec/serving/api.py L66-90 (현재 — 수정 대상):
```python
def _personalized_response(pipe, fallback, user, k, t0, context) -> RecommendResponse: ...
def create_app(pipelines, fallback, *, catalog=None, db, neighbors=None, book_stats=None, state=None) -> FastAPI:
```

fixture 사실(tests/conftest.py L46-130, `millie_serving_sample` 20권): `pop_rank == book_id`; 자격 없는 3권 = 18(외부 호스트)·19(title None)·20(adult-cover) → `popular()`·`eligible()` 에 없음; 엣지 `i → (i+d−1)%20+1, d=1..5, w=(0.9, 0.8, 0.7, 0.6, 0.2)`; 벡터 `default_rng(SEED)` 8차원 L2 정규화; **`resid_z = (i%7−3)·0.5` → 가드 모집단(millie_index ∧ < −1) = {7, 14}**(1·8·15 는 −1.0 경계 비위반); 홀수 ≥15 는 `difficulty None`·`category_prior`.
손계산(seeds 1,2,3 · state_weights → α=1): cf(엣지) 점수 4: 0.7+0.8+0.9=2.4 · 5: 0.6+0.7+0.8=2.1 · 6: 0.2+0.6+0.7=1.5 · 7: 0.2+0.6=0.8 · 8: 0.2 → **cf top = [4, 5, 6, 7, 8]**(5개뿐). pop(eligible, seen 제외) = **[4, 5, …, 17]**(14개) → k=10 → [4..13]. hybrid 후보 14개 중 7 은 cf 0.27·0.5 + pop 0.77·0.2 ≈ 0.29 + content → 상위 10 안. hybrid_div: MMR → guard(n_completed=0 <3) 가 7 을 상단 10 밖으로.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: serving/api.py create_app(weights=) 배선 + VectorsKR.book_ids + 테스트 2파일</name>
  <files>src/millie_rec/serving/api.py, src/millie_rec/data/vectors_kr.py, tests/serving/test_api_weights.py, tests/data/test_vectors_kr.py</files>
  <read_first>
    - src/millie_rec/serving/api.py 전문(135줄 — `_personalized_response` L66-78 · `create_app` L81-135)
    - src/millie_rec/serving/compose.py L21·L34-60(`ZERO_WEIGHTS`·`build_response` — 읽기만, 수정 금지)
    - tests/serving/test_recommend_level0.py L1-80(`_FakePop`·`_Boom`·`_app` 조립 패턴 — 복제해 새 파일에 쓴다; 이 파일은 수정하지 않는다)
    - tests/serving/test_smoke.py L32(`ZERO_WEIGHTS` 단정 위치) · L109 부근 level 3 `user_state_weights == ZERO_WEIGHTS`
    - src/millie_rec/data/vectors_kr.py 전문(38줄) · tests/data/test_vectors_kr.py 전문(추가할 위치·`DIM = 8` 관례)
    - .planning/phases/04-freeze/04-PATTERNS.md "serving/api.py" 절 · 04-CONTEXT.md D-09
    - ../.claude/rules/serving.md "구조" 절(create_app 시그니처 문장)
  </read_first>
  <action>
    **1-a. 테스트 먼저 — `tests/serving/test_api_weights.py`(신규, 4건).** docstring `"""create_app(weights=) — level 0 user_state_weights 채움(D-09·REC-03) · 익명 0 · 미주입 하위 호환 · weights 예외 → level 3 200."""`. `_FakePop`·`_Boom` 은 `tests/serving/test_recommend_level0.py` L28-56 을 그대로 복제(테스트 간 import 금지 관례). `ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}` 손으로. 헬퍼 `_app(tmp_path, *, weights=None, pipelines=None)` = `create_app(pipelines=pipelines or {"pop": _FakePop()}, fallback=GlobalPopularFallback(None), db=Database(tmp_path / DB_FILENAME), weights=weights)`.
    - `test_level0_user_state_weights_come_from_injected_callable` — `weights=lambda u: {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}`; `with TestClient(_app(...)) as c: r = c.get("/api/recommend", params={"seeds": "1,2,3", "k": 5}).json()`; `r["fallback_level"] == 0`; `r["user_state_weights"] == {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}`
    - `test_callable_receives_user_state_with_seeds` — `seen = []; weights = lambda u: (seen.append(u.explicit_seeds), {"alpha": 0.5, "beta": 0.5, "gamma": 0.0})[1]`; seeds `7,8` → `seen == [(7, 8)]`; 응답 `user_state_weights["beta"] == 0.5`
    - `test_anonymous_and_uninjected_stay_zero` — 같은 weights 로 익명 요청 → level 3, `user_state_weights == ZERO_WEIGHTS`; `weights=None`(기본) 로 seeds 요청 → level 0, `user_state_weights == ZERO_WEIGHTS`(하위 호환 — `tests/serving/test_recommend_level0.py` 가 그대로 통과하는 이유)
    - `test_weights_exception_degrades_to_level3_200` — `weights=lambda u: 1 / 0`; seeds 요청 → `status_code == 200`, `fallback_level == 3`, `model_version == "fallback_v1"`, `user_state_weights == ZERO_WEIGHTS`
    RED 확인: `uv run pytest tests/serving/test_api_weights.py --no-header` → 4건 중 `weights=` 인자가 없어 **`TypeError`** 가 난다 — 이것은 RED 가 아니므로 먼저 1-b 의 시그니처만(본문은 미사용) 추가한 뒤 재실행해 `AssertionError` 3건(첫·둘째·넷째는 weights 미적용으로 실패, 셋째는 통과)을 확인하고 SUMMARY 에 발췌한다.

    **1-b. `src/millie_rec/serving/api.py` 수정(≈8줄, 135 → ≤150).**
    - import: `from collections.abc import AsyncIterator, Callable` · `from dataclasses import replace`.
    - `_personalized_response(pipe, fallback, user, k, t0, context, weights: Callable[[UserState], dict[str, float]] | None = None)` — 본문:
      ```python
      try:
          items = pipe.recommend(user, k)
          shown = weights(user) if weights is not None else None  # D-09: 파이프라인과 같은 함수 → 표시 = 실제
      except Exception:
          log.exception("pipeline %s failed; falling back to level 3", pipe.name)
          return _fallback_response(fallback, user, k, t0, context)
      version = f"{pipe.name}{MODEL_VERSION_SUFFIX}"
      resp = build_response(items, model_version=version, level=FALLBACK_PERSONALIZED, k=k, t0=t0, context=context)
      return replace(resp, user_state_weights=dict(shown)) if shown is not None else resp
      ```
      (`compose.py` 는 건드리지 않는다 — `RecommendResponse` 는 frozen dataclass 라 `replace`. fallback 경로는 0 유지: 백엔드 서빙 01 §5 "익명은 가중치 전부 0".)
    - `create_app(..., state: object | None = None, weights: Callable[[UserState], dict[str, float]] | None = None)` — 마지막 keyword-only 인자로 추가(기본값 있음 = Phase 1 D-09 규칙 안). docstring 한 줄에 "weights: ranking.state_weights 주입(D-09)" 추가. `recommend` 핸들러의 `_personalized_response(pipelines[name], fallback, user, k, t0, context, weights)` 로 전달.
    GREEN: `uv run pytest tests/serving --no-header` → 신규 4 + 기존 전부 passed(`test_recommend_level0.py` 의 level 0 `ZERO_WEIGHTS` 단정이 있다면 `weights=None` 기본값이 보호). `wc -l src/millie_rec/serving/api.py` ≤ 150.

    **1-c. `src/millie_rec/data/vectors_kr.py` — `book_ids` 프로퍼티(Advisor 승인 4, ≤3줄, 기존 시그니처 불변):**
    ```python
        @property
        def book_ids(self) -> list[int]:
            return list(self._index)  # npz 저장 순서. Track B content 채널이 eligible 필터 전 전체 id 열람(D-10)
    ```
    `dim` 프로퍼티 바로 뒤. 테스트 먼저: `tests/data/test_vectors_kr.py` 끝에 `test_book_ids_lists_npz_order(millie_serving_sample)` — `VectorsKR.load(millie_serving_sample / "content_vectors_kr.npz").book_ids == list(range(1, 21))`(fixture L101·L121: `ids = 1..20` 순서 그대로) 추가 → RED(`AttributeError` 가 아닌 RED 를 위해 프로퍼티 스텁 `return []` 먼저) → GREEN.

    **1-d. 자기 경로 확인:** `uv run pytest tests/serving tests/data/test_vectors_kr.py tests/test_architecture.py --no-header` failed 0.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/serving tests/data/test_vectors_kr.py tests/test_architecture.py --no-header 2>&1 | tail -3 && wc -l src/millie_rec/serving/api.py src/millie_rec/data/vectors_kr.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/serving/test_api_weights.py` == 4 · `grep -n "def test_book_ids_lists_npz_order" tests/data/test_vectors_kr.py` 1행
    - `uv run pytest tests/serving tests/data/test_vectors_kr.py tests/test_architecture.py --no-header` failed 0
    - `wc -l src/millie_rec/serving/api.py` ≤ 150 · `wc -l src/millie_rec/data/vectors_kr.py` ≤ 45
    - `grep -n "weights: Callable\[\[UserState\], dict\[str, float\]\] | None = None" src/millie_rec/serving/api.py` ≥ 2행(create_app + _personalized_response)
    - `grep -n "replace(resp, user_state_weights=" src/millie_rec/serving/api.py` 1행 · `grep -n "from dataclasses import replace" src/millie_rec/serving/api.py` 1행
    - `grep -n "def create_app" -A 10 src/millie_rec/serving/api.py | grep -c "state: object | None = None,"` == 1 (기존 인자 순서·이름 불변, weights 는 그 뒤)
    - `git diff --stat -- src/millie_rec/serving/compose.py src/millie_rec/serving/schemas.py src/millie_rec/serving/schemas_should.py src/millie_rec/contracts.py` → 출력 없음
    - `grep -n "def book_ids" src/millie_rec/data/vectors_kr.py` 1행 · `grep -n "def __init__(self, book_ids: np.ndarray, vectors: np.ndarray)" src/millie_rec/data/vectors_kr.py` 1행(시그니처 불변)
    - SUMMARY 에 RED 발췌(AssertionError, `test_api_weights` 3건 + `test_book_ids` 1건)
  </acceptance_criteria>
  <done>create_app(weights=) 가 level 0 응답에 주입 함수 결과를 싣고, 미주입·익명·예외 경로는 0. VectorsKR.book_ids 존재. tests/serving·data 통과, api.py ≤150. 커밋 없음.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app/pipeline.py 4 variant(Track A) + app/pipeline_kr.py(Track B) + cli cmd_eval 확장 + 기존 테스트 갱신</name>
  <files>src/millie_rec/app/pipeline.py, src/millie_rec/app/pipeline_kr.py, src/millie_rec/app/cli.py, tests/app/test_pipeline.py, tests/app/test_cli.py</files>
  <read_first>
    - src/millie_rec/app/pipeline.py 전문(110줄 — `PopPipeline`·`CatalogPopPipeline`·`fit_pipelines`·`load_catalog`·`build_pipelines`)
    - src/millie_rec/app/cli.py L58-104(`cmd_eval` — `vectors = ContentVectors(books)` L69, `fit_pipelines(sp.train)` L70, `missing` L72-74, `pop.retriever.save` L101-102)
    - tests/app/test_pipeline.py 전문(갱신 대상 L51-55·L82-97, `_FakeCatalog` L59-79) · tests/app/test_cli.py 전문(갱신 대상 L76-79·L99-106, 합성 parquet L28-60: 유저 30×아이템 40×15권, ts 없음)
    - .planning/phases/04-freeze/04-01-SUMMARY.md·04-02-SUMMARY.md·04-03-SUMMARY.md "인계용 시그니처" 절
    - .planning/phases/04-freeze/04-PATTERNS.md "app/pipeline.py" 절(StagedPipeline 제안·⚠ 기존 코드 의존·줄 수·Track B 재료)·"기존 테스트와의 충돌" 표
    - .planning/phases/04-freeze/04-CONTEXT.md D-01(variant 정의)·D-04(풀 200)·D-08(순서)·D-10(Track B 재료)·D-11
    - src/millie_rec/data/catalog_kr.py L73-91(`meta`·`popular`·`eligible`·`neighbors`)
  </read_first>
  <action>
    **2-a. 테스트 먼저 — 기존 테스트 갱신(의도된 변경, Advisor 확정 5) + 추가.**
    `tests/app/test_pipeline.py`:
    - import 갱신: `from millie_rec.app.pipeline import StagedPipeline, build_pipelines, fit_pipelines, load_catalog` · `from millie_rec.app.pipeline_kr import CatalogPopPipeline` · `from millie_rec.retrieval import ContentVectors, PopularityRetriever` · `import pandas as pd` · `from millie_rec.contracts import COL_ITEM, VARIANTS, ScoredItem, UserState`.
    - 헬퍼 `_books50()`: `pd.DataFrame({COL_ITEM: list(range(50)), "tags": [("fantasy magic", "history war", "romance drama", "science space")[i % 4] for i in range(50)]})`.
    - L51-55 `test_fit_pipelines_keys_are_variants_and_recommend_returns_k(interactions)` 교체:
      ```python
      pipes = fit_pipelines(interactions, ContentVectors(_books50()))
      assert set(pipes) == set(VARIANTS)
      top3 = tuple(int(b) for b in interactions[COL_ITEM].value_counts().index[:3])  # 공동 소비가 많은 seed — cf 후보가 5개 이상 나오게
      u = UserState(None, explicit_seeds=top3)
      for name in VARIANTS:
          assert pipes[name].name == name
          items = pipes[name].recommend(u, 5)
          assert len(items) == 5 and all(isinstance(i, ScoredItem) for i in items)
          assert not {i.book_id for i in items} & u.seen
          assert [i.position for i in items] == [0, 1, 2, 3, 4]
      assert isinstance(pipes["cf"], StagedPipeline) and isinstance(pipes["hybrid_div"], StagedPipeline)
      assert hasattr(pipes["pop"], "retriever")  # cli.cmd_eval L101-102 가 pop.retriever.save 를 부른다
      ```
    - 추가 `test_fit_pipelines_knn_artifact_written_only_when_path_given(interactions, tmp_path)` — `fit_pipelines(interactions, ContentVectors(_books50()), knn_artifact=tmp_path / "knn.npz")` 후 `(tmp_path / "knn.npz").exists()`; `fit_pipelines(interactions, ContentVectors(_books50()))` 는 `tmp_path` 에 아무것도 안 쓴다(`list(tmp_path.iterdir())` 길이 1 유지)
    - L82-97 `test_build_pipelines_with_catalog_uses_track_b_pop_and_skips_artifact` — `monkeypatch` 인자 추가 후 첫 줄에 `monkeypatch.setattr("millie_rec.app.pipeline_kr.load_vectors_kr", lambda *a, **k: None)`(벡터 없음 → pop 만) — 나머지 단정 전부 그대로(`set(pipes) == {"pop"}`, `CatalogPopPipeline`, ERROR 로그 0, items·meta 조인)
    - L100-114 `load_catalog` 2건 그대로.
    `tests/app/test_cli.py`:
    - L76-79: `assert len(states) == 9`(헤더 + 4 variant × 2 상태); `states[1].startswith("pop,n0,") and states[1].endswith(",30")`; `states[2] == "pop,n20,0.000,0.000,0.000,0"` 유지(정렬: variant 순서 → state 순서); 추가 `assert [s.split(",")[0] for s in states[1:]] == ["pop", "pop", "cf", "cf", "hybrid", "hybrid", "hybrid_div", "hybrid_div"]`
    - L70-74 뒤 추가: `assert len(lines) == 5 and [l.split(",")[0] for l in lines[1:]] == list(VARIANTS)`(`from millie_rec.contracts import VARIANTS` import 추가); `assert lines[3].startswith("hybrid,") and lines[3].endswith(",30,holdout,hybrid_v1")`
    - L95-96 뒤 추가: `assert (art / "item_neighbors.npz").exists()`(knn 캐시가 `--artifacts` 아래에 생김)
    - L99-106 `test_eval_unregistered_variant_exits` → 이름 `test_eval_unknown_variant_name_is_rejected_by_argparse` 로 바꾸고 `"--variant", "nope"` 로 교체(argparse `choices` 가 `SystemExit(2)`; `cf` 는 이제 등록됨)
    RED 확인: `uv run pytest tests/app/test_pipeline.py tests/app/test_cli.py --no-header` → 갱신·추가 단정이 `AssertionError`(`set(pipes) == set(VARIANTS)`, `len(states) == 9` 등) 로 실패. `ImportError`(`StagedPipeline`·`pipeline_kr`) 가 나면 2-b 의 뼈대(클래스·함수 시그니처만) 를 먼저 두고 재실행해 AssertionError 를 확인한다.

    **2-b. `src/millie_rec/app/pipeline.py`(≤150줄) — Track A 조립 + 공통 glue.** docstring 첫 줄 `"""variant 조립 — dict 하나(아키텍처 01 §3-3). Track A fit_pipelines · 서버 build_pipelines(Track B 는 pipeline_kr)."""`.
    ```python
    import logging
    from collections.abc import Callable, Sequence
    from dataclasses import replace
    from pathlib import Path
    from typing import TYPE_CHECKING

    from millie_rec.contracts import (
        VARIANTS, BookStatsSource, CandidateGenerator, Catalog, Pipeline, Reranker, ScoredItem, UserState,
    )
    from millie_rec.data import BOOKS_KR_JSON, DIR_SERVING, CatalogKR
    from millie_rec.ranking import CH_CF, CH_CONTENT, CH_POP, CHANNEL_WEIGHTS, blend_channels
    from millie_rec.reranking import DifficultyGuard, MMRReranker
    from millie_rec.retrieval import (
        POOL, POP_ARTIFACT, ContentVectors, PopularityRetriever, load_or_fit_itemknn,
    )

    if TYPE_CHECKING:
        import pandas as pd

    log = logging.getLogger(__name__)
    POP, CF, HYBRID, HYBRID_DIV = VARIANTS  # 이름의 정본은 contracts.VARIANTS
    SOURCE_POPULARITY = "popularity"  # serving/fallback.py 와 같은 값 — 공개 표면에 없어 중복 정의
    WeightFn = Callable[[UserState], dict[str, float]]
    ```
    - `PopPipeline` 그대로(L25-43).
    - `class StagedPipeline:` docstring `"""contracts.Pipeline — 슬롯별 retriever(풀 POOL) → blend_channels → rerankers(순서대로) → 상위 k. D-01·D-08."""`
      ```python
      def __init__(self, name: str, channels: dict[str, CandidateGenerator], *, weights: dict[str, float] | None = None,
                   rerankers: Sequence[Reranker] = (), book_stats: BookStatsSource | None = None,
                   catalog: Catalog | None = None, pool: int = POOL) -> None:
          self.name = name  # 인스턴스 속성 — Protocol name: str 은 인스턴스로도 만족(test_recommend_level0._FakeNamed 관례)
          self.channels, self.weights, self.rerankers = dict(channels), weights, tuple(rerankers)
          self.book_stats, self.catalog, self.pool = book_stats, catalog, pool

      def recommend(self, user: UserState, k: int) -> list[ScoredItem]:
          cands = {slot: r.retrieve(user, self.pool) for slot, r in self.channels.items()}
          items = blend_channels(user, cands, self.weights, book_stats=self.book_stats)
          if self.catalog is not None:  # main §7 노출 자격 게이트 — 엣지 dst 가 자격 없는 책일 수 있다(Track B)
              ok = set(self.catalog.eligible([i.book_id for i in items]))
              items = [i for i in items if i.book_id in ok]
          last = len(self.rerankers) - 1
          for n_, rr in enumerate(self.rerankers):
              # 마지막 reranker 만 k — 앞 단계(MMR)는 풀 전체(≤ MMR_POOL 50)를 재배열해 돌려준다.
              # k ≤ N(10) 요청에서 가드가 위반 책을 N 밖으로 내리고 다음 책을 끌어올릴 자리를 남기기 위함
              # (MMR 은 items[:pool] 만 보고, 그리디는 prefix-일관이라 상위 k 는 k 로 불렀을 때와 같다).
              items = rr.rerank(user, items, k if n_ == last else len(items))
          return [replace(i, position=n) for n, i in enumerate(items[:k])]
      ```
      **k 흐름(architect 검토 반영):** 채널 풀 `POOL=200` → `blend_channels` 전체 → eligible 필터 → MMR(`rerank(user, items, len(items))` → `items[:50]` 재배열 50개) → guard(`rerank(user, items50, k)` → 위반 책을 N=10 밖으로, k개) → `[:k]`. reranker 가 없으면(cf·hybrid) `blend_channels` 순서 그대로 `[:k]`. 후보가 k 미만이면 있는 만큼만(길이 < k 허용, harness 는 길이를 강제하지 않는다). `tests/app/test_variants.py` 의 hybrid_div 판정은 **k=10 으로** 호출해 가드 대상 책(fixture book 1·8 중 상단 진입분)이 상위 10 에 없음을 단정한다 — k=10 에서도 성립해야 서버 기본 요청에서 가드가 동작한다.
    - `fit_pipelines(train: "pd.DataFrame", vectors: ContentVectors, *, knn_artifact: Path | None = None, weights: WeightFn | None = None) -> dict[str, Pipeline]` — docstring `"""make eval 용. 키 = VARIANTS 4종. vectors 는 content 채널·MMR 양쪽에(TF-IDF 1회, D-04). knn_artifact None 이면 캐시 안 씀(테스트)."""`:
      ```python
      pop = PopularityRetriever().fit(train)
      knn = load_or_fit_itemknn(train, knn_artifact, weights=weights)
      three = {CH_CF: knn, CH_CONTENT: vectors, CH_POP: pop}
      return {
          POP: PopPipeline(pop),
          CF: StagedPipeline(CF, {CH_CF: knn}, weights={CH_CF: 1.0}),
          HYBRID: StagedPipeline(HYBRID, three, weights=dict(CHANNEL_WEIGHTS)),
          HYBRID_DIV: StagedPipeline(HYBRID_DIV, three, weights=dict(CHANNEL_WEIGHTS),
                                     rerankers=(MMRReranker(vectors), DifficultyGuard(None))),  # D-08 rank→MMR→guard, Track A 가드 패스스루
      }
      ```
      (`ContentVectors` 의 성분 가중은 생성자 `weights=` — `cmd_eval` 이 `ContentVectors(books, weights=state_weights)` 로 만든다.)
    - `load_catalog` 그대로.
    - `build_pipelines(artifact: Path = POP_ARTIFACT, *, catalog: Catalog | None = None, weights: WeightFn | None = None) -> dict[str, Pipeline]` — `if catalog is not None: from millie_rec.app.pipeline_kr import build_pipelines_kr; return build_pipelines_kr(catalog, weights=weights)`(함수 안 import 로 순환 회피 — pipeline_kr 이 pipeline 의 `StagedPipeline`·`PopPipeline` 를 import 한다); 나머지(아티팩트 없음 → `{}`, 손상 → 로그 + `{}`, 정상 → `{POP: PopPipeline(...)}`) 그대로. **위치 인자·keyword 이름 불변**(`tests/serving/test_smoke.py` L173·L195 `lambda **_: {}`).
    - `CatalogPopPipeline` 은 이 파일에서 **삭제**하고 `pipeline_kr.py` 로 이동(테스트 import 갱신은 2-a). 줄 수 목표 ≤150 — `wc -l` 로 확인, 넘으면 `StagedPipeline` 을 `app/staged.py` 로(Advisor 확정 6).

    **2-c. `src/millie_rec/app/pipeline_kr.py`(신규, ≤150줄) — Track B 조립(D-10).** docstring `"""Track B(밀리 카탈로그) 4 variant 조립 — cf 채널 = CatalogKR.neighbors 엣지, content 채널 = VectorsKR(SVD 128), pop = pop_rank. 표기는 전부 content(data.md). pandas 없음(서빙 경로)."""`
    ```python
    import logging
    from pathlib import Path

    from millie_rec.app.pipeline import CF, HYBRID, HYBRID_DIV, POP, SOURCE_POPULARITY, PopPipeline, StagedPipeline, WeightFn
    from millie_rec.contracts import Catalog, Pipeline, ScoredItem, UserState
    from millie_rec.data import DIR_SERVING, VECTORS_KR_NPZ, CatalogKR, VectorsKR
    from millie_rec.ranking import CH_CF, CH_CONTENT, CH_POP, CHANNEL_WEIGHTS
    from millie_rec.reranking import DifficultyGuard, MMRReranker
    from millie_rec.retrieval import TOP_N, NeighborRetriever, PopularityRetriever, VectorRetriever

    log = logging.getLogger(__name__)
    ```
    - `CatalogPopPipeline` — 기존 L46-77 그대로 이동(`SOURCE_POPULARITY` 는 pipeline 에서 import).
    - `class WithMeta:` docstring `"""Pipeline 데코레이터 — 결과에 catalog.meta(title·authors·image_url·book_format·difficulty) 조인. name 은 안쪽 그대로."""`; `__init__(self, inner: Pipeline, catalog: Catalog)`: `self.inner, self.catalog, self.name = inner, catalog, inner.name`; `recommend(user, k)`: `items = self.inner.recommend(user, k)`; `meta = {int(m["book_id"]): m for m in self.catalog.meta([i.book_id for i in items])}`; `return [replace(i, title=m.get("title"), authors=m.get("authors"), image_url=m.get("image_url"), book_format=m.get("book_format"), difficulty=i.difficulty if i.difficulty is not None else m.get("difficulty")) for i in items for m in (meta.get(i.book_id, {}),)]`(`from dataclasses import replace`).
    - `def load_vectors_kr(serving_dir: Path = DIR_SERVING) -> VectorsKR | None` — `load_catalog` 패턴: 파일 없음 → `log.info("no vectors at %s — serving without content channel/MMR", path)` + `None`; 손상 `(OSError, ValueError, KeyError)` → `log.exception(...)` + `None`; 정상 → `VectorsKR.load(path)`.
    - `def build_pipelines_kr(catalog: CatalogKR, *, weights: WeightFn | None = None, vectors: VectorsKR | None = None) -> dict[str, Pipeline]` — docstring `"""D-10. vectors None 이면 load_vectors_kr() 시도, 그래도 None 이면 Phase 3 동작(pop 만) — 기동을 막지 않는다."""`:
      ```python
      vectors = vectors if vectors is not None else load_vectors_kr()
      pops = {POP: CatalogPopPipeline(catalog)}
      if vectors is None:
          return pops
      ranked = catalog.popular(n=TOP_N)
      pop_r = PopularityRetriever([(b, float(len(ranked) - i)) for i, b in enumerate(ranked)])  # 기존 클래스 재사용
      nbr = NeighborRetriever(catalog, weights=weights)  # source="content" 기본(data.md)
      vec = VectorRetriever(vectors, catalog.eligible(vectors.book_ids), weights=weights)  # 03-CONTEXT D-14 eligible 선필터
      three = {CH_CF: nbr, CH_CONTENT: vec, CH_POP: pop_r}
      staged = {
          CF: StagedPipeline(CF, {CH_CF: nbr}, weights={CH_CF: 1.0}, catalog=catalog),
          HYBRID: StagedPipeline(HYBRID, three, weights=dict(CHANNEL_WEIGHTS), book_stats=catalog, catalog=catalog),
          HYBRID_DIV: StagedPipeline(HYBRID_DIV, three, weights=dict(CHANNEL_WEIGHTS), book_stats=catalog, catalog=catalog,
                                     rerankers=(MMRReranker(vectors), DifficultyGuard(catalog))),
      }
      return {**pops, **{name: WithMeta(p, catalog) for name, p in staged.items()}}
      ```
      `CatalogKR.user_level` 은 None → gap 항 0(D-06), 가드는 `len(history)` 대리값으로 발동(D-08). `catalog` 인자 타입은 `CatalogKR`(Catalog+Neighbors+BookStatsSource 를 한 객체가 만족) — 가짜 카탈로그(`_FakeCatalog`, neighbors 없음) 로는 `vectors=None` 경로만 온다(테스트 2-a 의 monkeypatch).

    **2-d. `src/millie_rec/app/cli.py` `cmd_eval` 확장(+4줄).** import 에 `from millie_rec.ranking import state_weights` · `from millie_rec.retrieval import KNN_ARTIFACT, POP_ARTIFACT, ContentVectors`. L69-70:
    ```python
    vectors = ContentVectors(books, weights=state_weights)  # content 채널 성분 가중 + ILD 벡터(같은 객체)
    pipes = fit_pipelines(sp.train, vectors, knn_artifact=args.artifacts / KNN_ARTIFACT.name, weights=state_weights)
    ```
    L74 메시지 `"(Phase 4 에서 추가)"` 삭제 → `f"variant {missing} 미등록 — 등록: {list(pipes)}"`. 나머지 불변(`pop.retriever.save` L101-102 는 `PopPipeline` 유지로 동작).

    **2-e. GREEN + 아키텍처:** `uv run pytest tests/app tests/test_architecture.py --no-header` failed 0(`test_server_catalog.py` 는 Task 3 에서 갱신 — 이 시점엔 기본 variant 변경으로 1건 실패할 수 있다: `model_version == "pop_v1"` 단정. **그 1건만** 허용, Task 3 에서 해소). `wc -l src/millie_rec/app/pipeline.py src/millie_rec/app/pipeline_kr.py src/millie_rec/app/cli.py` 각 ≤150. `test_cli` e2e 는 합성 40 아이템에서 ItemKNN fit(top=39)·MMR 이 돌아야 한다 — 실패 시 소유 슬라이스 플랜에 수정 브리프(플랜 04-01/03), app 에서 우회하지 않는다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/app/test_pipeline.py tests/app/test_cli.py tests/test_architecture.py --no-header 2>&1 | tail -3 && wc -l src/millie_rec/app/pipeline.py src/millie_rec/app/pipeline_kr.py src/millie_rec/app/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/app/test_pipeline.py tests/app/test_cli.py tests/test_architecture.py --no-header` failed 0
    - `wc -l src/millie_rec/app/pipeline.py src/millie_rec/app/pipeline_kr.py src/millie_rec/app/cli.py` 각 ≤ 150
    - `grep -n "^POP, CF, HYBRID, HYBRID_DIV = VARIANTS" src/millie_rec/app/pipeline.py` 1행 · `grep -n "class StagedPipeline" src/millie_rec/app/pipeline.py` 1행 · `grep -c "class CatalogPopPipeline" src/millie_rec/app/pipeline.py src/millie_rec/app/pipeline_kr.py` → pipeline.py 0, pipeline_kr.py 1
    - `grep -n "def fit_pipelines(" -A 3 src/millie_rec/app/pipeline.py | grep -c "knn_artifact: Path | None = None"` == 1 · `grep -n "def build_pipelines(" -A 2 src/millie_rec/app/pipeline.py | grep -c "artifact: Path = POP_ARTIFACT"` == 1(위치 인자 불변)
    - `grep -c "k if n_ == last else len(items)" src/millie_rec/app/pipeline.py` == 1(앞 단계 reranker 는 풀 전체, 마지막만 k — architect·checker 블로커 해소)
    - `grep -n "rerankers=(MMRReranker(vectors), DifficultyGuard(None))" src/millie_rec/app/pipeline.py` 1행 · `grep -n "rerankers=(MMRReranker(vectors), DifficultyGuard(catalog))" src/millie_rec/app/pipeline_kr.py` 1행(D-08 순서)
    - `grep -n "NeighborRetriever(catalog, weights=weights)" src/millie_rec/app/pipeline_kr.py` 1행(source 기본 content) · `grep -c "itemknn" src/millie_rec/app/pipeline_kr.py` == 0 · `grep -n "catalog.eligible(vectors.book_ids)" src/millie_rec/app/pipeline_kr.py` 1행
    - `grep -c "^import pandas\|^    import pandas" src/millie_rec/app/pipeline_kr.py` == 0(서빙 경로 pandas 금지)
    - `grep -n "ContentVectors(books, weights=state_weights)" src/millie_rec/app/cli.py` 1행 · `grep -n "knn_artifact=args.artifacts / KNN_ARTIFACT.name" src/millie_rec/app/cli.py` 1행 · `grep -c "Phase 4 에서 추가" src/millie_rec/app/cli.py` == 0
    - `grep -rn "from millie_rec\.\(retrieval\|ranking\|reranking\|data\|serving\|evaluation\)\.[a-z_]* import" src/millie_rec/app/` → 출력 없음(공개 표면만)
    - `grep -n "set(pipes) == set(VARIANTS)" tests/app/test_pipeline.py` 1행 · `grep -n "len(states) == 9" tests/app/test_cli.py` 1행 · `grep -c "test_eval_unregistered_variant_exits" tests/app/test_cli.py` == 0 · `grep -n '"--variant", "nope"' tests/app/test_cli.py` 1행
    - `git diff --stat -- tests/serving/test_smoke.py tests/test_architecture.py src/millie_rec/contracts.py` → 출력 없음
  </acceptance_criteria>
  <done>fit_pipelines 4 variant(Track A), build_pipelines_kr 4 variant(Track B, vectors 없으면 pop 만), cli cmd_eval 이 knn 캐시·weights 를 넘김. 기존 테스트 갱신 4건 + 신규 1건 통과, 아키텍처 통과. 커밋 없음.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: app/server.py weights 주입 + tests/app/test_variants.py(서버 4 variant 상이·기본 hybrid_div·β=0) + test_server_catalog 갱신 + 전역 게이트 + make smoke</name>
  <files>src/millie_rec/app/server.py, tests/app/test_variants.py, tests/app/test_server_catalog.py</files>
  <read_first>
    - src/millie_rec/app/server.py 전문(23줄)
    - tests/app/test_server_catalog.py 전문(`_server` L20-24 재import 패턴 · L34 `pop_v1` 단정 · L36 `[4,5,6,7,8]`)
    - tests/conftest.py L46-130(fixture 사실 — `<interfaces>` 손계산 참조)
    - tests/serving/test_smoke.py L164-204(`load_catalog`·`build_pipelines` monkeypatch 형태 — 불변 확인용)
    - .planning/phases/04-freeze/04-PATTERNS.md "app/server.py" 절 · "tests/app/test_variants.py" 절
    - .planning/phases/04-freeze/04-CONTEXT.md D-09·D-11
    - Makefile L36-43(`smoke`)
  </read_first>
  <action>
    **3-a. 테스트 먼저 — `tests/app/test_variants.py`(신규, 5건).** docstring `"""app/server.py 4 variant 배선(D-10·D-11) — 계약(model_version 4종·기본 hybrid_div) · 정확성(fixture 손계산 pop/cf·가드 7) · 안전성(eligible·β=0). 실 artifacts/serving 은 읽지 않는다."""`. `_server(tmp_path, monkeypatch, serving_dir)` 는 `test_server_catalog.py` L20-24 복제 + 한 줄 추가: `monkeypatch.setattr("millie_rec.app.pipeline_kr.load_vectors_kr", lambda *a, **k: VectorsKR.load(serving_dir / VECTORS_KR_NPZ))`(`from millie_rec.data import VECTORS_KR_NPZ, CatalogKR, VectorsKR`). 헬퍼 `_ids(c, model, k=10)`: `c.get("/api/recommend", params={"seeds": "1,2,3", "k": k, **({"model": model} if model else {})}).json()` 반환.
    - `# ── 계약 ──` `test_four_variants_return_their_model_version_level0(tmp_path, monkeypatch, millie_serving_sample)` — 각 `v in VARIANTS`: `r = _ids(c, v)`; `r["model_version"] == f"{v}_v1"`; `r["fallback_level"] == 0`; `r["forced"] is True`
    - `test_default_variant_is_hybrid_div_when_four_registered` — `model` 없이 → `model_version == "hybrid_div_v1"`, `forced is False`(D-11 자동 이동)
    - `# ── 정확성 ──` `test_variants_return_different_books_on_fixture` — `ids = {v: [i["book_id"] for i in _ids(c, v)["items"]] for v in VARIANTS}`; `ids["pop"] == list(range(4, 14))`(pop_rank 순, seen 1·2·3 제외); `ids["cf"] == [4, 5, 6, 7, 8]`(엣지 손계산 — 이웃이 8 까지만 닿는다); `7 in ids["hybrid"]`; `7 not in ids["hybrid_div"]`(가드: 7 은 resid_z −1.5 millie_index, n_completed=0 → 상단 10 밖); `ids["hybrid"] != ids["hybrid_div"]`; `ids["pop"] != ids["hybrid"]`; `len({tuple(v) for v in ids.values()}) == 4`
      (fixture 는 결정적(`SEED`) 이므로 결과가 실행마다 같다. 만약 이 fixture 에서 `7 not in ids["hybrid"]` 가 관측되면 — MMR 이 아니라 hybrid 는 재순위화가 없어 거의 불가능 — 손계산을 SUMMARY 에 다시 적고 Advisor 에 보고한다. 단정을 완화하지 않는다.)
    - `# ── 안전성 ──` `test_all_variants_only_eligible_books_with_korean_meta` — 4 variant 전부 `not {18, 19, 20} & set(ids)`; `hybrid_div` 응답 `rows[0]["items"][0]["title"].startswith("밀리 표본 도서")`(WithMeta 조인)
    - `test_user_state_weights_beta_zero_for_seeds_only_user` — `_ids(c, "hybrid_div")["user_state_weights"] == {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}`(D-09·REC-03); 익명 `c.get("/api/recommend").json()["user_state_weights"] == {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}`
    RED: `uv run pytest tests/app/test_variants.py --no-header` → server.py 가 weights 를 안 넘겨 `user_state_weights` 단정 실패, `load_vectors_kr` 패치 대상은 Task 2 로 이미 존재하므로 나머지는 통과할 수 있다(4 variant 배선은 Task 2 `build_pipelines_kr` 가 이미 함) — 최소 1건 AssertionError 확인.

    **3-b. `tests/app/test_server_catalog.py` 갱신(의도된 변경 — 기본 variant 가 hybrid_div 로 이동):** L32 `params={"seeds": "1,2,3", "k": 5}` → `params={"seeds": "1,2,3", "k": 5, "model": "pop"}`; 단정 L33-37 그대로(`pop_v1`·`[4,5,6,7,8]`·한국어 제목). `_server` 에 `load_vectors_kr` 패치 한 줄 추가(실 npz 를 읽지 않게 — fixture npz 로). 익명 테스트 L40-51 불변.

    **3-c. `src/millie_rec/app/server.py`(+2줄):** `from millie_rec.ranking import state_weights`; `pipelines=build_pipelines(catalog=catalog, weights=state_weights)`; `create_app(..., book_stats=catalog, weights=state_weights)`. 주석 L9-12 에 "Phase 4 D-09: 같은 `state_weights` 를 파이프라인(성분 가중)과 응답(user_state_weights) 양쪽에 → 표시 혼합비 = 실제 혼합비" 1줄. `load_catalog()` 0인자·`build_pipelines(` keyword 호출 유지.

    **3-d. GREEN + 전역 게이트(wave 2 단독 — 여기서 직접):**
    1. `uv run pytest tests/app tests/serving tests/test_architecture.py --no-header` failed 0.
    2. `uv run ruff format . && uv run ruff check .` 클린.
    3. `uv run pytest --no-header` → **≥ 317 passed, 1 failed**(`test_coverage_gate` 만 — STATE.md Blockers 재수집 중 의도된 RED). 다른 실패 0.
    4. `make smoke` → `smoke: PASS`. 실 `artifacts/serving/` 에 books 9,450 · vectors 8,708 스냅샷이 있으므로 서버는 4 variant 를 등록하고 기본 `hybrid_div` 로 `seeds=1,2,3&k=5` level 0 을 준다(`content_vectors_kr.npz` 8,708 ≠ 9,450 불일치는 미지 id 0 벡터로 무해 — **판정은 최종 `make millie` 뒤 Plan 04-06**). `make smoke` 뒤 `curl -s "localhost:8010/..."` 는 하지 않는다(smoke 가 서버를 내림). 대신 `uv run python -c "from millie_rec.app.pipeline import build_pipelines, load_catalog; from millie_rec.ranking import state_weights; p = build_pipelines(catalog=load_catalog(), weights=state_weights); print(sorted(p))"` → `['cf', 'hybrid', 'hybrid_div', 'pop']`(실 스냅샷 로드 확인, 읽기만).
    5. `uv run python -c "import millie_rec.app.server"` 가 예외 없이 끝난다(모듈 import 부작용 = 카탈로그·벡터 로드 ≈ 20MB).

    **3-e. SUMMARY 준비:** 변경 파일 목록(`git status --short -- src/millie_rec/app src/millie_rec/serving/api.py src/millie_rec/data/vectors_kr.py tests/app tests/serving/test_api_weights.py tests/data/test_vectors_kr.py`), 전역 pytest 요약 줄, `make smoke` 출력, `wc -l` 표, 기존 테스트 갱신 목록 5건(test_pipeline L51-55·L82-97, test_cli L70-79·L99-106, test_server_catalog L32) 과 이유, fixture 손계산(cf=[4,5,6,7,8]·가드 7) 실측 일치 여부, "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check . && uv run ruff check . && uv run pytest --no-header 2>&1 | tail -3 && make smoke</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/app/test_variants.py` == 5
    - `grep -n "weights=state_weights" src/millie_rec/app/server.py` == 2행(build_pipelines·create_app) · `grep -n "from millie_rec.ranking import state_weights" src/millie_rec/app/server.py` 1행 · `grep -n "load_catalog()" src/millie_rec/app/server.py` 1행(0인자 유지) · `grep -n "build_pipelines(catalog=catalog, weights=state_weights)" src/millie_rec/app/server.py` 1행
    - `grep -n '"model": "pop"' tests/app/test_server_catalog.py` 1행
    - `uv run pytest tests/app tests/serving tests/test_architecture.py --no-header` failed 0
    - `uv run ruff format --check . && uv run ruff check .` 클린
    - `uv run pytest --no-header` 요약 줄: `passed` 수 ≥ 317, `failed` 수 == 1 이고 그 1건이 `tests/data/test_millie_catalog.py::test_coverage_gate`(출력에 다른 `FAILED` 줄 없음)
    - `make smoke` 출력에 `smoke: PASS`
    - `uv run python -c "from millie_rec.app.pipeline import build_pipelines, load_catalog; from millie_rec.ranking import state_weights; print(sorted(build_pipelines(catalog=load_catalog(), weights=state_weights)))"` → `['cf', 'hybrid', 'hybrid_div', 'pop']`
    - `git diff --stat -- src/millie_rec/contracts.py src/millie_rec/serving/compose.py src/millie_rec/serving/schemas.py tests/serving/test_smoke.py tests/test_architecture.py Makefile pyproject.toml scripts data/id_map.csv demo` → 출력 없음
    - `git log --oneline | head -1` 불변(커밋 0)
  </acceptance_criteria>
  <done>서버가 4 variant 를 등록하고 기본 hybrid_div, fixture 에서 pop/cf 손계산 일치·가드 7 제외·β=0 응답. 전역 pytest ≥317 passed(coverage_gate 1건만 RED), ruff 클린, smoke PASS. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /api/recommend?model=&seeds=&k=` → 4 variant 파이프라인·`weights(user)` | 유일한 외부 입력. `model` 은 `VARIANTS` 밖 422(api.py L123-124 기존), `seeds` 정수 파싱 422(`_parse_seeds`), `k ≤ K_MAX=100`(Query le) — **재검증만, 변경 없음** |
| `artifacts/serving/content_vectors_kr.npz` → `load_vectors_kr` | 로컬 커밋 아티팩트(`np.load` allow_pickle 기본 False) |
| `state_weights` 출력 → 응답 `user_state_weights` | 표시 값. 파이프라인과 같은 함수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Denial of Service | `/api/recommend` 4 variant 비용 | mitigate | 후보 풀 `POOL=200`×3 슬롯, MMR 풀 50, `k ≤ 100` — 요청당 수십 ms. 기동 시 1회 로드(`VectorRetriever` dense 9.7MB), 요청마다 fit 없음(serving.md). 파이프라인 예외는 `_personalized_response` 가 level 3 200 으로 강하(기존, `test_weights_exception_degrades_to_level3_200` 로 weights 경로도 확인) |
| T-04-20 | Tampering | `weights` 주입 경로 | accept | `create_app(weights=)` 는 프로세스 내부 함수 객체 주입(`app/server.py` 만). 요청 파라미터로 가중치를 받지 않는다. 미주입 기본 None → 0 셋(하위 호환 테스트) |
| T-04-21 | Information Disclosure | 응답 필드 | accept | 응답 형태는 `serving/schemas.py`(freeze) 그대로 — 새 필드 0. `user_state_weights` 는 이미 계약 필드(0 고정이던 값을 채움). `description`·`resid_z` 는 어디에도 싣지 않는다(`CatalogKR.EXCLUDED_META_KEYS`, WithMeta 는 5필드만 조인) |
| T-04-22 | Information Disclosure | 자격 없는 책 노출(엣지 dst) | mitigate | `StagedPipeline(catalog=)` 가 blend 후 `catalog.eligible` 필터(main §7 노출 자격 게이트) — 테스트 `test_all_variants_only_eligible_books…`(18·19·20 없음) |
| T-04-23 | Tampering | 손상 npz → 기동 실패 | mitigate | `load_vectors_kr` 가 `(OSError, ValueError, KeyError)` → `log.exception` + None → pop 만 등록. `allow_pickle` 기본 False 유지(grep 0) |
| T-04-24 | Repudiation | 표시 혼합비 ≠ 실제 혼합비 | mitigate | `server.py` 가 같은 `state_weights` 를 `build_pipelines(weights=)`·`create_app(weights=)` 양쪽에(acceptance grep 2행) |
| T-04-25 | Elevation of Privilege | `model` 파라미터로 미등록 variant 강제 | accept | 미등록 이름은 `default` 로 대체(기존 L128), `VARIANTS` 밖은 422 — 코드 경로 변경 없음 |
</threat_model>

<verification>
- `uv run pytest --no-header` → ≥ 317 passed · failed 1(`test_coverage_gate`, Phase 3 재수집 중 의도) · 다른 실패 0
- `uv run ruff format --check . && uv run ruff check .` 클린
- `make smoke` → `smoke: PASS`(실 스냅샷에서 기본 `hybrid_div` level 0)
- `wc -l` — `app/pipeline.py`·`app/pipeline_kr.py`·`app/cli.py`·`serving/api.py` 각 ≤150
- `uv run pytest tests/test_architecture.py --no-header` → 3 passed(app 은 공개 표면만 import)
- fixture 판정: pop `[4..13]` · cf `[4,5,6,7,8]` · `7 ∈ hybrid` ∧ `7 ∉ hybrid_div` · 4 variant model_version 상이 · 기본 `hybrid_div_v1` · `user_state_weights == {1.0, 0.0, 0.0}`
- 커밋 0
</verification>

<success_criteria>
- **REC-02** `app/pipeline.py` 가 `contracts.VARIANTS` 4종을 dict 로 조립(Track A `fit_pipelines(train, vectors)`), `hybrid` = cf ∪ content ∪ pop 가중합(`CHANNEL_WEIGHTS`), `hybrid_div` = + MMR → guard(D-08)
- **REC-03** 서버 응답 `user_state_weights` 가 `state_weights` 에서 나와 seeds 만인 사용자에게 β=0(`create_app(weights=)`, D-09)
- **REC-01** 세 retriever 가 실제 파이프라인에 조립돼 후보를 낸다(Track A ItemKNN·ContentVectors·Popularity / Track B Neighbor·Vector·Popularity)
- ROADMAP Success Criteria 1 후반 "서버에서 `model=` 쿼리로 4 variant 가 서로 다른 책" — 20권 fixture 손계산 판정 테스트 존재(실 카탈로그 판정은 Plan 04-06, 최종 `make millie` 뒤)
- 결정 D-10(Track B 재료·표기 content)·D-11(기본 hybrid_div 자동, 상수 추가 없음)·03-CONTEXT D-14(`eligible` 필터) 이행
- `contracts.py`·`schemas*.py`·`compose.py` 무변경, 의존성 0, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/04-freeze/04-04-SUMMARY.md` 를 만든다. 포함: wave 1 게이트 결과(요약 줄 `309 passed, 1 failed`·smoke) · Task 별 RED 발췌·GREEN 요약 줄 · 전역 게이트 결과(`≥317 passed, 1 failed`·ruff·smoke PASS) · `wc -l` 표(pipeline 110→N, pipeline_kr N, cli 128→N, api 135→N) · 기존 테스트 갱신 5건 표(파일·줄·이전 단정·이후 단정·이유) · fixture 손계산 vs 실측(pop·cf 목록, 7 의 hybrid/hybrid_div 위치, hybrid 상위 10 실제 목록) · 실 스냅샷 `build_pipelines` 4키 확인 · Plan 04-05 인계: `make eval` 실행 전 `rm -f artifacts/item_neighbors.npz`(캐시 무효화) 필요 여부(첫 실행이라 불필요), `cli demo` 가 쓸 `build_pipelines_kr(catalog, weights=, vectors=)`·`load_vectors_kr(serving_dir)` 시그니처 · 변경 파일 목록 · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
