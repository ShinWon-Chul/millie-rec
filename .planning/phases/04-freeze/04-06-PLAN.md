---
phase: 04-freeze
plan: 06
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - report/demo_5books.md
  - report/draft.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md
  - ../.claude/rules/serving.md
  - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md
autonomous: false
requirements: [REC-07, REC-08]
lane: Advisor + 사용자 게이트(5권 제공 · 최종 카탈로그 스냅샷 확인)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05). 커밋은 Phase 1~4 산출물 일괄로 사용자 승인 후
must_not_touch:
  - src/**                     # freeze 선언 플랜 — 코드 변경 0(PDF 숫자를 깨는 버그 수정도 이 플랜 밖)
  - tests/**
  - scripts/**
  - data/**
  - demo/**
  - Makefile
  - pyproject.toml
  - results/**                 # 숫자는 Plan 04-05 실측 그대로 — 손편집 금지
  - artifacts/**

must_haves:
  truths:
    - "사용자가 제공한 밀리 카탈로그 내 본인 5권으로 make demo SEEDS=… 를 1회 실행해 report/demo_5books.md 가 생기고, 그 안에 seed 별 '을 좋아하셨다면' 표 5개 + hybrid_div 상위 10 표 1개 + '이웃은 콘텍츠 유사도' 병기 + data.md 고정 문장이 있다(REC-07, ROADMAP Success Criteria 4)"
    - "실측 전제 — 재수집 종료 + 최종 make millie 1회 완료(STATE.md Blockers 해소) — 를 사람이 확인한 뒤에만 5권 실측·서버 4 variant 판정을 한다(04-CONTEXT.md domain, 03-CONTEXT.md D-02)"
    - "최종 카탈로그 위에서 GET /api/recommend?model=<v>&seeds=<5권> 4종이 서로 다른 상위 10 을 주고 배지 제목 책이 0권임을 실측해 SUMMARY 에 남긴다(ROADMAP Success Criteria 1 후반, 실 카탈로그 판정)"
    - "freeze 4종(① retrieval/ranking/reranking 상수 ② contracts.VARIANTS 4종 ③ artifacts/serving/* 최종 스냅샷 ④ serving/schemas*.py 응답 형태)이 3곳에 기록된다: .planning/STATE.md Day 3 게이트 ✅ + 'freeze' 문구 · 개발일지 D 항목(6요소, freeze 상수 값 + eval_<ts>.json 파일명 인용) · report/draft.md §4-1 각주 + §5-1 앵커 문단(REC-08, D-14)"
    - ".planning/REQUIREMENTS.md REC-01~REC-08 체크박스가 [x] 이고, ../.claude/rules/serving.md create_app 시그니처에 weights 가 있고, 아키텍처 01 §3-3 단계 규칙 문구가 D-02 결합안(α_n = max(0.2, 0.6·e^{−n/20}), β:γ=3:1, 빈 성분 제거 재정규화)으로 갱신된다"
  artifacts:
    - path: "report/demo_5books.md"
      provides: "본인 5권 앵커 1장(PDF P5 축약 원본)"
      contains: "을 좋아하셨다면"
    - path: ".planning/STATE.md"
      provides: "Day 3 게이트 ✅ + freeze 선언 문구 + Phase 4 완료 위치"
      contains: "freeze"
    - path: "../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md"
      provides: "결정 D 항목(6요소) — Phase 4 실측·freeze 4종·상수 값·eval_<ts>.json·튜닝 이력"
      contains: "freeze"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REC-01~08 [x]"
      contains: "[x] **REC-08**"
  key_links:
    - from: "report/draft.md"
      to: "report/demo_5books.md"
      via: "§5-1 앵커 문단이 5권 표를 축약 인용(숫자 아님 — 제목·이웃 관계)"
      pattern: "demo_5books.md"
    - from: ".planning/STATE.md"
      to: "results/eval_<ts>.json"
      via: "freeze 선언이 채택 실행 파일명을 인용"
      pattern: "eval_[0-9]{8}_[0-9]{4}\\.json"
---

<objective>
Phase 4 '추천 파이프라인과 모델 freeze'(.planning/ROADMAP.md "Phase 4: 추천 파이프라인과 모델 freeze")의 **마무리 게이트**: 사용자가 밀리 카탈로그 안에서 실제 읽은 5권 제목을 주고 최종 카탈로그 스냅샷이 확정된 뒤 ① `make demo SEEDS=…` 1회 → `report/demo_5books.md` ② 최종 카탈로그 위 서버 4 variant 판정 실측 ③ Day 3 종료 **freeze 4종 선언 3곳 기록**(STATE.md · 개발일지 D 항목 · draft) ④ REQUIREMENTS REC-01~08 체크 · 규칙·설계서 문구 갱신(`serving.md` `create_app(weights=)`, 아키텍처 01 §3-3 단계 규칙 → D-02 결합안).

Purpose: **REC-07** 본인 5권 앵커 1장(ROADMAP Success Criteria 4 · 수용 기준 '온보딩 5권 → 앵커 row'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)의 CLI 몫) · **REC-08** Day 3 freeze 선언(ROADMAP Success Criteria 5) — 이후 Phase 5·6 은 상수·variant·응답 필드를 건드리지 않는다. 결정 D-12(5권은 사용자 몫·Advisor 1회 실측)·D-13(출력 위치)·D-14(freeze 4종 3곳) 이행. Deferred "아키텍처 01 §3-3 문구·`serving.md` 시그니처 갱신 → Advisor 후속" 해소.
Output: `report/demo_5books.md` · STATE.md · 개발일지 파일(D 항목 1개, 번호는 실행 시 `grep` 으로 확정) · REQUIREMENTS.md · draft §4-1 각주·§5-1 · 규칙 1파일 · 설계서 1절. **코드 변경 0**. 커밋 0(사용자 승인 후 Phase 1~4 일괄).

**사람 게이트 2개(체크포인트, Task 1):** (a) 본인 5권 제목(→ `cli demo --find` 로 book_id 확정) (b) 재수집 종료 + 최종 `make millie` 1회 완료 확인(Phase 3 몫 — 이 플랜은 `make millie*` 를 실행하지 않는다). 둘 중 하나라도 없으면 Task 2 이하를 시작하지 않는다(D-12 "임시 5권으로 먼저 실측 후 교체 → 채택하지 않음", freeze 이후 재실행 위험).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-freeze/04-CONTEXT.md
@.planning/phases/04-freeze/04-04-SUMMARY.md
@.planning/phases/04-freeze/04-05-SUMMARY.md
@.planning/phases/03-millie-catalog/03-CONTEXT.md
@Makefile
@report/draft.md
@results/latest.csv
@results/latest_states.csv
@../.claude/rules/serving.md
@../.claude/rules/references.md
@../.claude/rules/report.md
@../.claude/rules/data.md
@../.assets/개발일지/README.md
@../.assets/개발일지/2026-09-05_Day1_GSD_초기화와_계약_freeze.md
@../CLAUDE.md

<project_rules_inline>
**개발일지(`../CLAUDE.md` §0-2):** 하루 1파일 `YYYY-MM-DD_DayN_<주제>.md`, 섹션 한 일 / 의사결정 / 고민 포인트 / 미결·다음. **의사결정 항목은 6요소 고정 — 문제 · 동기 · 선택지(2개 이상, 버린 것 포함) · 근거(사실 3개까지) · 장애(없으면 "없음") · 결과(반영 위치 · 잃은 것 · 되돌리는 조건).** 각 요소 1~2줄(결과만 3줄), 항목당 12줄 이내, 제목에 `[축 N · main §X / 구체화 Y]` 태그. **D 번호는 전역 연속·재번호 금지 — 새 항목 전 `grep -h '^### D' ../.assets/개발일지/*.md | tail -3` 로 마지막 번호 확인**(09-04 밤 중복 사고). 숫자는 `results/` 만. 번복 항목은 지우지 않고 새로 쓴다.

**참조 표기(`../.claude/rules/references.md`):** 약호 단독 금지. `결정 D-14(04-CONTEXT.md, freeze 4종·3곳 기록)`, `수용 기준 '평가 4행 + split_mode 출력'(PRD §9 수용 기준 표)`, `Phase 4 '추천 파이프라인과 모델 freeze'(.planning/ROADMAP.md)`. STATE.md·개발일지·draft 전부.

**리포트(`../.claude/rules/report.md`):** 숫자는 `results/` 만. 밀리 저작 텍스트 미노출(`demo_5books.md` 는 제목·저자·분야·난이도 점·근거만 — `cli demo` 가 보장). 5장 = 앵커 1문단·난이도 1문단·데모·로드맵 — `demo_5books.md` 표는 P5 에서 **축약**(제목 5개 + 이웃 예 1~2개), 전문은 첨부하지 않는다.

**데이터 2트랙(`../.claude/rules/data.md`):** "본인 5권 정성 케이스는 밀리 카탈로그(Track B) 안에서 본인이 실제 읽은 한국 책 5권… 앵커 이웃은 content_sim. 출력에 '데모 카탈로그 이웃 = 콘텍츠 유사도' 문구 병기." 5권 실측 숫자(이웃 가중치 등)는 비교표·PDF 숫자 표에 넣지 않는다.

**freeze 이후 규칙(`../.claude/rules/simplicity.md`):** "Day 3 종료 후: 새 모델·후보 통로·feature 금지. 파라미터 튜닝은 Day 4 시작 전까지만." "Day 5: 코드 변경은 PDF 숫자를 깨는 버그 수정만." `make millie` 재실행 금지(03-CONTEXT.md D-02).

**세션 소유(`../CLAUDE.md` §0-2):** 설계서(`../.assets/설계서/**`)·`.claude/**`·`CLAUDE.md`·`PROGRESS.md` 는 단일 Advisor 세션만 편집한다 — 이 플랜은 Advisor 가 실행한다. **다른 세션이 동시에 편집할 수 있으므로 Edit 전 반드시 다시 읽는다.**
</project_rules_inline>

<interfaces>
From Makefile (Plan 04-05 후):
```makefile
demo:   uv run python -m millie_rec.app.cli demo --seeds $(SEEDS) > report/demo_5books.md && echo "→ report/demo_5books.md"
smoke:  8010 포트 /health · / · /api/recommend?seeds=1,2,3&k=5
```
CLI: `uv run python -m millie_rec.app.cli demo --find <제목 일부>` → `| book_id | 제목 | 저자 | 분야 |` 표(eligible 만).

From .planning/STATE.md (갱신 대상 절):
- frontmatter `status:` · `stopped_at:` · `last_updated:` · `progress:`(completed_phases 3 → 4, completed_plans 16 → 22, total_plans 16 → 22)
- `## Current Position` Phase/Plan/Status
- `### Day 게이트` 표 L75: `| 3 | 비교표 4행 실측 + 본인 5권 케이스 · **모델·API 형태 freeze 선언** | ⬜ |` → ✅
- `### Blockers/Concerns` 재수집·최종 스냅샷 항목(Phase 3 가 해소했으면 ~~취소선~~ 상태여야 한다 — 이 플랜의 전제)

From .planning/REQUIREMENTS.md L42-49: `- [ ] **REC-01** …` ~ `- [ ] **REC-08** …` 8줄.

From ../.claude/rules/serving.md "구조" 절 첫 줄(현재): `create_app(pipelines: dict[str, Pipeline], fallback: Pipeline, *, catalog: Catalog | None = None, db: Database, neighbors: Neighbors | None = None, book_stats: BookStatsSource | None = None, state=None) -> FastAPI` — `weights: Callable[[UserState], dict[str, float]] | None = None` 추가(Plan 04-04 코드와 일치).

From 아키텍처 01 §3-3(갱신 대상 문구): "초기값 α₀=0.6/β₀=0.3/γ₀=0.1 · 하한 0.2 · 재정규화" 는 유지, "`reader_open`마다 ±0.05 단계 규칙" 문장을 D-02 결합안으로: "α_n = max(0.2, 0.6·e^{−n/20})(n = 완독/이력 권수, 데모는 history 권수), 나머지를 β:γ = 3:1 로 나눈 뒤 비어 있는 성분을 빼고 합 1 로 재정규화. 재설정 직후 +0.15 · 세션 활동 +0.1 부스트는 state.py(Phase 5)." — 개발일지 결정 'Phase 4 착수 결정 9건'(../.assets/개발일지/2026-09-05_Day1_GSD_초기화와_계약_freeze.md 항목 D68) 장애 ① 해소.

개발일지 마지막 번호(플랜 작성 시점 2026-09-05): **D68**. 실행 시 `grep -h '^### D' ../.assets/개발일지/*.md | tail -3` 로 재확인 후 다음 번호 사용(다른 세션이 D69 를 썼을 수 있다).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (사용자 게이트): 본인 5권 제목 제공 + 최종 카탈로그 스냅샷 확인</name>
  <what-built>Plan 04-01~05 로 4 variant 파이프라인·서버 배선·Track A 4행 실측·`cli demo --find/--seeds`·`make demo` 리다이렉트가 준비됐다. 남은 입력은 사람 몫 둘이다.</what-built>
  <how-to-verify>
    1. **최종 카탈로그 스냅샷 확인(Phase 3 몫):** `pgrep -f collect_millie` 가 빈 출력이고, `.planning/STATE.md` Blockers 의 "재수집 진행 중" 항목이 해소(~~취소선~~ 또는 삭제)되어 있고, 최종 `make millie` 1회가 끝나 `results/millie_coverage.csv` 의 `_n_badge_title` 이 0 이며 `uv run pytest tests/data/test_millie_catalog.py::test_coverage_gate --no-header` 가 passed 인지 확인한다. **아니면 여기서 멈춘다**(이 플랜은 `make millie*` 를 실행하지 않는다 — Phase 3 '밀리 카탈로그 빌드'(.planning/ROADMAP.md) 절차로 돌아간다).
    2. **본인 5권:** 밀리 카탈로그 안에서 실제 읽은 한국 책 5권 제목을 준다. Advisor 가 각 제목으로 `uv run python -m millie_rec.app.cli demo --find "<제목 일부>"` 를 돌려 `book_id` 를 확정하고 사용자에게 `book_id · 제목 · 저자` 5줄을 되돌려 확인받는다(동명 도서·판본 주의). 카탈로그에 없는 책은 다른 책으로 교체 요청.
    3. 확인이 끝나면 `SEEDS=a,b,c,d,e` 형태의 5 정수를 이 플랜의 SUMMARY 초안에 적는다.
  </how-to-verify>
  <resume-signal>"5권 확정: SEEDS=a,b,c,d,e · 최종 스냅샷 확인" 또는 "스냅샷 미완 — 중단"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: make demo SEEDS=… 1회 → report/demo_5books.md 검증 + 최종 카탈로그 서버 4 variant 판정 실측</name>
  <files>report/demo_5books.md</files>
  <read_first>
    - Task 1 결과(SEEDS 5개, 스냅샷 확인 문장)
    - Makefile L30-31(`demo` 리다이렉트) · L36-43(`smoke`)
    - .planning/phases/04-freeze/04-05-SUMMARY.md("실 카탈로그 임의 5권 출력 앞 20줄" — 형태 참고, 숫자·책은 버린다)
    - .planning/phases/04-freeze/04-CONTEXT.md D-10 실측 절(seeds `[1446, 1086, 1378]` 재빌드 전 관측 — 최종 재빌드 후엔 무효), D-13(출력 형태)
    - results/millie_coverage.csv(최종 스냅샷 수치 — 개발일지 인용용)
  </read_first>
  <action>
    **2-a. 5권 실측(1회):** `make demo SEEDS=a,b,c,d,e` → `→ report/demo_5books.md` 출력. 검증(전부 grep):
    - `grep -c "을 좋아하셨다면" report/demo_5books.md` == 5
    - `grep -c "| # | 책 | 저자 | 분야 | 난이도 | 근거 |" report/demo_5books.md` == 6
    - `grep -c "hybrid_div 상위 10" report/demo_5books.md` == 1 · `grep -n "alpha=1.0 beta=0.0 gamma=0.0" report/demo_5books.md` 1행(seeds 만 → α=1, REC-03)
    - `grep -c "이웃은 콘텍츠 유사도(제목·소개 TF-IDF)" report/demo_5books.md` == 5 · `grep -c "데모 카탈로그의 앵커 이웃은 콘텍츠 유사도다" report/demo_5books.md` == 1
    - `grep -c "카탈로그에 없음" report/demo_5books.md` == 0(5권 전부 카탈로그 안)
    - `grep -c "description\|curator_note\|resid_z" report/demo_5books.md` == 0
    - `grep -n "상위 10 중 resid_z<−1(millie_index) 0권" report/demo_5books.md` 1행(가드가 상단 10 을 비웠다)
    - 배지 제목 0: `grep -c "도슨트북\|읽던 지점 그대로\|오브제북\|오디오웹소설" report/demo_5books.md` == 0(재수집 목적 — 03-08 배지 제목 정정 확인)
    **1회만 실행한다.** 결과가 어색해도(이웃이 이상함 등) 재실행·seed 교체를 하지 않고 SUMMARY 에 관찰을 적는다(D-12 "freeze 이후 재실행 위험"). 단 위 grep 중 하나라도 실패하면 그것은 **Plan 04-05 코드 버그**이므로 04-05 수정 브리프로 돌려보내고, 고친 뒤 이 Task 를 처음부터(`make demo` 1회) 다시 한다 — 그 기록을 SUMMARY 에.

    **2-b. 최종 카탈로그 서버 4 variant 판정(ROADMAP Success Criteria 1 후반 실 카탈로그 몫):** 서버를 임시 포트로 띄워 4 variant 를 비교하고 내린다:
    ```bash
    uv run uvicorn millie_rec.app.server:app --port 8011 --log-level warning & echo $! > /tmp/claude-501/uv8011.pid; sleep 4
    for v in pop cf hybrid hybrid_div; do
      curl -fsS "localhost:8011/api/recommend?model=$v&seeds=a,b,c,d,e&k=10" \
        | uv run python -c "import sys,json; r=json.load(sys.stdin); print(r['model_version'], r['fallback_level'], [i['book_id'] for i in r['items']], r['user_state_weights'])"
    done
    curl -fsS "localhost:8011/api/recommend?seeds=a,b,c,d,e&k=10" | uv run python -c "import sys,json; print(json.load(sys.stdin)['model_version'])"
    kill $(cat /tmp/claude-501/uv8011.pid)
    ```
    (스크래치패드 경로는 세션 지정 디렉터리로 바꾼다.) 기록: 4 variant 의 `model_version` 이 `pop_v1 cf_v1 hybrid_v1 hybrid_div_v1`·level 0·`user_state_weights == {1.0, 0.0, 0.0}`, 기본(`model` 없음) = `hybrid_div_v1`, **상위 10 id 리스트 4개가 pairwise 동일하지 않음**(교집합 크기 표 cf∩content 등 — D-10 실측 절 형식: `cf∩pop`·`cf∩hybrid`·`hybrid∩hybrid_div` 크기), 4 리스트 전체 `rows[0].items[*].title` 에 배지 제목 0. 이 표를 SUMMARY 와 개발일지 근거에 넣는다. 서로 다른 책이 아니면(어떤 두 리스트가 완전히 같음) 사실대로 기록하고 사용자 보고 — 코드 변경은 하지 않는다(freeze).

    **2-c. 전체 게이트(코드 무변경 확인 겸):** `uv run pytest --no-header`(이제 `test_coverage_gate` 도 passed — 실패 0) · `make smoke` PASS · `git status --short -- src tests` 가 Plan 04-05 종료 시점과 동일(이 플랜에서 코드 변경 0).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && grep -c "을 좋아하셨다면" report/demo_5books.md && grep -c "| # | 책 | 저자 | 분야 | 난이도 | 근거 |" report/demo_5books.md && grep -c "데모 카탈로그의 앵커 이웃은 콘텍츠 유사도다" report/demo_5books.md && grep -c "description\|curator_note\|resid_z\|카탈로그에 없음" report/demo_5books.md; uv run pytest --no-header 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `ls report/demo_5books.md` 존재 · `grep -c "을 좋아하셨다면" report/demo_5books.md` == 5 · `grep -c "| # | 책 | 저자 | 분야 | 난이도 | 근거 |" report/demo_5books.md` == 6 · `grep -c "hybrid_div 상위 10" report/demo_5books.md` == 1
    - `grep -c "데모 카탈로그의 앵커 이웃은 콘텍츠 유사도다" report/demo_5books.md` == 1 · `grep -c "이웃은 콘텍츠 유사도(제목·소개 TF-IDF)" report/demo_5books.md` == 5
    - `grep -c "description\|curator_note\|resid_z\|카탈로그에 없음\|도슨트북\|읽던 지점 그대로" report/demo_5books.md` == 0
    - `grep -n "alpha=1.0 beta=0.0 gamma=0.0" report/demo_5books.md` 1행 · `grep -n "0권" report/demo_5books.md` ≥ 1행
    - SUMMARY 에 서버 4 variant 판정 표(model_version 4종·level 0·상위 10 리스트 4개·교집합 크기·기본 hybrid_div_v1·배지 제목 0) 존재
    - `uv run pytest --no-header` 요약 줄에 `failed` 없음(최종 스냅샷 후 `test_coverage_gate` 통과 포함) · `make smoke` → `smoke: PASS`
    - `git status --short -- src tests Makefile pyproject.toml` 가 Plan 04-05 SUMMARY 의 변경 파일 목록과 동일(이 플랜 코드 변경 0)
    - `make demo` 실행 횟수 1(SUMMARY 에 명시; 재실행이 있었다면 사유 = 04-05 버그 수정)
  </acceptance_criteria>
  <done>본인 5권 앵커 1장이 `report/demo_5books.md` 에 있고(표 6·병기 문장·고정 문장·배지 0·텍스트 0), 최종 카탈로그 서버 4 variant 가 서로 다른 책을 준다는 실측 표가 SUMMARY 에 있다. 코드 변경 0.</done>
</task>

<task type="auto">
  <name>Task 3: freeze 4종 선언 3곳 기록 + REQUIREMENTS 체크 + serving.md·아키텍처 01 §3-3 문구 갱신</name>
  <files>.planning/STATE.md, ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md, report/draft.md, .planning/REQUIREMENTS.md, ../.claude/rules/serving.md, ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md</files>
  <read_first>
    - .planning/STATE.md 전문(**Edit 직전 다시 읽는다** — 다른 세션 편집 가능)
    - .planning/REQUIREMENTS.md L40-49(REC 8줄 현재 상태)
    - .planning/phases/04-freeze/04-05-SUMMARY.md "채택 상수 표"·"make eval 실행 표"(eval_<ts>.json 파일명·버린 조합) · 04-02-SUMMARY·04-03-SUMMARY "freeze 상수 표"
    - `grep -h '^### D' ../.assets/개발일지/*.md | tail -3`(마지막 D 번호 — 실행 시점 확정) · ../.assets/개발일지/README.md(템플릿) · ../.assets/개발일지/2026-09-05_Day1_GSD_초기화와_계약_freeze.md 항목 D68(형식 아날로그 — 6요소·태그·12줄)
    - ../.claude/rules/serving.md "구조" 절 첫 문장 · 아키텍처 01 §3-3(`grep -n "reader_open\|±0.05\|0.6/0.3/0.1" "<설계서 경로>"` 로 위치)
    - report/draft.md L200 각주 · L229-231(§5-1 앵커 문단) · L236(§5-3 `[Phase 4~8]`)
    - results/latest.csv(freeze 시점 숫자 — 개발일지 근거 3개 중 1)
    - ../.claude/rules/references.md
  </read_first>
  <action>
    **3-a. 개발일지 D 항목(6요소) — 파일 `../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md`**(실행 날짜가 다르면 그 날짜·Day 로; 그날 파일이 이미 있으면 그 파일에 항목만 추가). 번호 = `grep -h '^### D' ../.assets/개발일지/*.md | tail -1` 다음 수(예상 D69). 제목 형식(D68 아날로그): `### D69. Phase 4 '추천 파이프라인과 모델 freeze' 실측·freeze 선언 — 4 variant 비교표 4행(cf {recall}·hybrid {ndcg}·hybrid_div ILD {a}→{b}) · 본인 5권 앵커 1장 · freeze 4종(상수·VARIANTS·스냅샷·응답 형태)  [축 ③ 추천 구조·④ 성능 평가 · main §5-1·§5-2·§5-6·§6-1 / 아키텍처 01 §3-3 / PRD §9 수용 기준 '평가 4행 + split_mode 출력'·'n=0 vs n≥k 지표 분리'·'온보딩 5권 → 앵커 row' / millie-rec/.planning/phases/04-freeze/04-0{1..6}-SUMMARY.md]  (확정 — Advisor 실측 2026-09-0X HH:MM)`. 본문 6요소(각 1~2줄, 결과 3줄, 총 ≤12줄):
    - **문제** — 설계서의 4단계 파이프라인·★가중치·★난이도·★앵커가 코드·실측 없이 주장으로만 있었다.
    - **동기** — Day 3 종료 = 모델·API 형태 freeze(../CLAUDE.md §2-4). 이후 Phase 5·6 이 그 위에서 조립·화면만 하려면 상수·variant·응답 형태가 얼어야 한다.
    - **선택지** — (버림) ALS·RRF·λ 그리드 ILD 최대화·guard 후 MMR·Pipeline Protocol `weights()` 확장·임시 5권 선실측 / (채택) scipy Item-KNN·min-max 가중합·λ 0.7(→0.5 1회 조건)·rank→MMR→guard·`create_app(weights=)`·사용자 5권 후 1회 실측(결정 'Phase 4 착수 결정 9건'(같은 폴더 2026-09-05 파일 항목 D68)).
    - **근거(사실 3)** — ① `results/latest.csv`(파일 `results/eval_<ts>.json` 채택본) 4행: pop/cf/hybrid/hybrid_div 의 Recall@20·NDCG@10·ILD@10 숫자, D-07 게이트 통과 여부 ② `latest_states.csv` n20−n0 격차(hybrid vs pop) ③ 최종 카탈로그 서버 4 variant 교집합 표(Task 2) + `report/demo_5books.md` 표 6개.
    - **장애** — 튜닝 이력(있었다면: "λ 0.7 게이트 미달 → 0.5 1회, 버린 실행 `eval_<ts>.json`" / "W 그리드 (…) 1회") · fixture 가드 모집단 정정(1·8 → 7·14, conftest `resid_z=(i%7−3)·0.5`) · 없으면 "없음".
    - **결과** — 반영 위치: freeze 상수(`retrieval` KNN_TOP=50 POOL=200 · `ranking` ALPHA0/BETA0/GAMMA0=0.6/0.3/0.1 TAU=20 ALPHA_FLOOR=0.2 W_CF/W_CONTENT/W_POP={채택} W_GAP/W_GAP_POS/W_NCOMP_GAP=−0.10/−0.20/−0.01 · `reranking` LAMBDA_MMR={채택} MMR_POOL=50 GUARD_RESID_Z=−1.0 GUARD_MIN_COMPLETED=3) · `contracts.VARIANTS` 4종 · `artifacts/serving/*` 최종 스냅샷(`results/millie_coverage.csv` N권) · `serving/schemas*.py`. 잃은 것: Day 4 이후 파라미터 튜닝·새 통로 불가, ablation A/B/C 는 설계만. 되돌리는 조건: 없음(제출까지 freeze) — PDF 숫자를 깨는 버그만 수정.
    "한 일" 절에 Phase 4 플랜 6개 한 줄씩, "미결·다음" 에 Phase 5 '서빙 Must 완성' 착수(`compose.py` 앵커 행이 `Neighbors` 사용·`state.py` 가 `n_completed`·`user_level` 대리값 교체).

    **3-b. `.planning/STATE.md`(Edit 직전 재읽기):**
    - frontmatter: `status: "Phase 4 완료 — 모델·API 응답 형태 freeze 선언(2026-09-0X). 4 variant 비교표 4행 실측·본인 5권 앵커·서버 4 variant 판정 완료. 이후 새 모델·후보 통로·응답 필드·상수 변경 금지"`, `stopped_at: Phase 4 '추천 파이프라인과 모델 freeze' 완료 · freeze 선언 — 다음 /gsd-verify-work 4 → Phase 5 '서빙 Must 완성'`, `last_updated`, `progress: completed_phases 4 · total_plans 22 · completed_plans 22 · percent 50`.
    - `## Current Position`: `Phase: 04 (추천 파이프라인과 모델 freeze) — COMPLETE · freeze`, `Plan: 6 of 6 complete`, Progress 바 50%.
    - `### Day 게이트` L75: `| 3 | 비교표 4행 실측 ✅(09-0X, latest.csv pop/cf/hybrid/hybrid_div) + 본인 5권 케이스 ✅(report/demo_5books.md) · **모델·API 형태 freeze 선언 ✅(09-0X, 개발일지 D69)** | ✅ |`.
    - `### Decisions` 에 1줄 추가: `- **freeze(2026-09-0X):** retrieval/ranking/reranking 모듈 상단 상수 · contracts.VARIANTS 4종 · artifacts/serving/* 최종 스냅샷 · serving/schemas*.py 응답 형태 — 결정 'Phase 4 실측·freeze 선언'(개발일지 2026-09-06 파일 항목 D69). 채택 실행 results/eval_<ts>.json. Phase 5·6 은 조립·화면만`.
    - `### Blockers/Concerns` 에 1줄: `- **freeze 이후 금지:** make millie* · 새 variant/후보 통로/feature · 상수 변경 · schemas 필드 추가. Phase 5 인계: n_completed·user_level 대리값을 state.py 가 교체(D-02·D-06·D-08), compose.py 앵커 행은 Neighbors 로`.

    **3-c. `.planning/REQUIREMENTS.md` L42-49:** `- [ ] **REC-01**` ~ `- [ ] **REC-08**` 8줄을 `- [x]` 로. 다른 줄 무변경.

    **3-d. `report/draft.md`:**
    - L200 각주 끝에 "모델·API 응답 형태는 Day 3(2026-09-0X) freeze — 이후 숫자 변경은 없다." 1구.
    - §5-1 앵커 문단(L229-231)에 1문장: "본인이 실제 읽은 5권(『…』·『…』·『…』·『…』·『…』)을 시드로 넣은 앵커 5행과 hybrid_div 10권은 `report/demo_5books.md` 에 있고, 그 이웃은 콘텍츠 유사도다(협업 필터링 숫자와 섞지 않는다)." — 제목 5개는 실제 값(밀리 저작 텍스트 아님 — 도서 제목). `[Phase 4~8]`(L236) 의 "본인이 실제 읽은 5권 → 앵커 5행 … `report/demo_5books.md`" 부분은 완료된 셈이지만 마커는 Phase 8(스크린샷·QR) 몫이 남아 **그대로 둔다**.
    - `grep -c '\[Phase 4\]' report/draft.md` == 0 유지(Plan 04-05 결과).

    **3-e. `../.claude/rules/serving.md` "구조" 절 첫 문장:** `create_app(…, book_stats: BookStatsSource | None = None, state=None, weights: Callable[[UserState], dict[str, float]] | None = None) -> FastAPI` 로 갱신 + 괄호 안에 "`weights` = `ranking.state_weights` 주입(Phase 4 D-09) — 표시 혼합비 = 실제 혼합비" 추가. 다른 문구 무변경.

    **3-f. 아키텍처 01 §3-3:** `grep -n "reader_open\|±0.05" "<설계서 경로>"` 로 단계 규칙 문장 위치 확인 → 그 문장을 결합안으로 교체: "가중치 스케줄(Phase 4 D-02 결합안): 초기값 α₀=0.6/β₀=0.3/γ₀=0.1, α_n = max(0.2, 0.6·e^{−n/20})(n = 이력 권수, 데모는 history 권수), 나머지 (1−α) 를 β:γ = 3:1 로 나눈 뒤 비어 있는 성분을 빼고 합 1 로 재정규화 — 신규 사용자는 α=1. 재설정 직후 +0.15·세션 활동 +0.1 부스트는 `state.py`(Phase 5). 코드: `ranking/blend.py::state_weights`." 절 끝에 `(갱신 2026-09-0X — 결정 'Phase 4 실측·freeze 선언'(개발일지 2026-09-06 파일 항목 D69))`. 다른 문장 무변경. **Edit 전 재읽기**(다른 세션 편집 가능).

    **3-g. 검증:** 아래 acceptance grep 전부 + `uv run pytest --no-header` 실패 0(문서만 바꿨으니 변동 없음) + `git status --short -- src tests` 변동 없음.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && grep -c "\[x\] \*\*REC-0[1-8]\*\*" .planning/REQUIREMENTS.md && grep -n "freeze" .planning/STATE.md | head -3 && grep -c "weights: Callable" ../.claude/rules/serving.md && grep -h '^### D' ../.assets/개발일지/*.md | tail -1 && grep -c '\[Phase 4\]' report/draft.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*REC-0[1-8]\*\*" .planning/REQUIREMENTS.md` == 8 · `grep -c "\[ \] \*\*REC-0[1-8]\*\*" .planning/REQUIREMENTS.md` == 0
    - `grep -n "^| 3 |" .planning/STATE.md` 의 행에 `✅` 와 `freeze` 가 있다 · `grep -c "freeze" .planning/STATE.md` ≥ 3 · `grep -n "completed_phases: 4" .planning/STATE.md` 1행 · `grep -nE "eval_[0-9]{8}_[0-9]{4}\.json" .planning/STATE.md` ≥ 1행
    - 개발일지: `grep -h '^### D' ../.assets/개발일지/*.md | tail -1` 이 새 항목(제목에 `freeze` 와 `Phase 4` 포함) · 그 항목 안에 `**문제**`·`**동기**`·`**선택지**`·`**근거**`·`**장애**`·`**결과**` 6 요소가 각 1회 · `grep -c "D69\b\|D70\b" ../.assets/개발일지/2026-09-0*_Day*_Phase4*.md` ≥ 1 · 항목 본문에 `LAMBDA_MMR`·`W_CF`·`ALPHA0`·`KNN_TOP`·`eval_` 문자열 존재 · 같은 번호가 다른 파일에 없다(`grep -l "^### D69\." ../.assets/개발일지/*.md | wc -l` == 1, 번호는 실제 사용 번호로)
    - `grep -n "weights: Callable\[\[UserState\], dict\[str, float\]\] | None = None" ../.claude/rules/serving.md` 1행
    - 아키텍처 01: `grep -c "±0.05" "../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md"` == 0 · `grep -c "e^{−n/20}\|e\^{-n/20}" "<같은 경로>"` ≥ 1 · `grep -c "state_weights" "<같은 경로>"` ≥ 1
    - `grep -n "demo_5books.md" report/draft.md` ≥ 2행(§5-1 신규 + §5-3 기존) · `grep -c '\[Phase 4\]' report/draft.md` == 0 · `grep -n "freeze" report/draft.md` ≥ 1행(§4-1 각주)
    - `git status --short -- src tests Makefile pyproject.toml results artifacts` 가 Plan 04-05 종료 시점과 동일(코드·숫자 변경 0)
    - `uv run pytest --no-header` 실패 0 · `make smoke` → `smoke: PASS`
    - `git log --oneline | head -1` 불변(커밋 0 — 사용자 승인 후 Phase 1~4 일괄)
  </acceptance_criteria>
  <done>freeze 4종이 STATE.md·개발일지 D 항목(6요소)·draft 3곳에 기록되고, REC-01~08 [x], serving.md·아키텍처 01 §3-3 문구가 코드와 일치한다. 코드·숫자 변경 0, 커밋 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력(5권 제목·SEEDS) → `cli demo --find/--seeds` | 개발자 본인의 로컬 CLI 인자. 정수 파싱 실패는 종료 |
| 임시 서버(8011) `curl` 4 variant | 로컬 루프백만, 실행 후 즉시 종료 |
| 문서 편집(STATE·개발일지·규칙·설계서) | 단일 Advisor 세션 소유. 다른 세션과 동시 편집 가능성 → Edit 전 재읽기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-32 | Information Disclosure | `report/demo_5books.md` 내용 | mitigate | 제목·저자·분야·난이도 점·근거만(`cli demo` META_COLS). `description`·`curator_note`·`resid_z` grep 0 acceptance. 도서 제목은 밀리 저작 텍스트가 아니다(data.md) |
| T-04-33 | Information Disclosure | 개인 독서 이력(본인 5권) | accept | 사용자가 제출물에 넣기로 선택한 정성 케이스(구체화 F). 제출 PDF 범위 안 |
| T-04-34 | Repudiation | freeze 이후 숫자 변경 | mitigate | 3곳 기록(STATE·개발일지·draft) + 채택 `eval_<ts>.json` 파일명 인용 + `results/` 손편집 금지 + 이 플랜 `must_not_touch: src/** results/**`(코드·숫자 변경 0 acceptance) |
| T-04-35 | Tampering | 문서 동시 편집 충돌 | mitigate | Edit 직전 재읽기(../CLAUDE.md §0-2), D 번호 `grep` 재확인(중복 방지, 결정 '참조 표기 규칙'(개발일지 2026-09-04 파일 항목 D49)) |
| T-04-36 | Denial of Service | 임시 서버 잔존 프로세스 | mitigate | pid 파일로 `kill`, 포트 8011(smoke 8010 과 분리). 실패 시 `lsof -i :8011` 정리 |
| T-04-37 | Elevation of Privilege | `make millie*` 재실행 유혹 | mitigate | 이 플랜은 실행하지 않는다(Task 1 게이트 = Phase 3 완료 확인만). 03-CONTEXT D-02·STATE Blockers 문구 |
</threat_model>

<verification>
- Task 1 체크포인트: 사용자 5권 SEEDS 확정 + 최종 스냅샷 확인 문장(없으면 중단)
- `report/demo_5books.md`: 표 6·"을 좋아하셨다면" 5·병기 문장 5·고정 문장 1·텍스트/resid_z/배지 제목 0·`alpha=1.0 beta=0.0 gamma=0.0`·가드 0권
- 최종 카탈로그 서버 4 variant: model_version 4종·level 0·기본 hybrid_div_v1·상위 10 pairwise 상이·배지 제목 0 (SUMMARY 표)
- freeze 3곳: STATE.md(Day 3 ✅·freeze·eval_<ts>.json) · 개발일지 D 항목 6요소(상수 값·파일명) · draft(각주·§5-1)
- REQUIREMENTS REC-01~08 [x] · serving.md `weights` · 아키텍처 01 §3-3 `±0.05` 0
- `uv run pytest --no-header` 실패 0 · `make smoke` PASS · 코드·숫자 변경 0 · 커밋 0
</verification>

<success_criteria>
- **REC-07** `cli demo --seeds <본인 5권>` 실측 1회 → `report/demo_5books.md` 앵커 1장, 이웃 = 콘텍츠 유사도 병기(ROADMAP Success Criteria 4 · 수용 기준 '온보딩 5권 → 앵커 row'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표))
- **REC-08** Day 3 freeze 가 `.planning/STATE.md` 와 개발일지에 선언되고 `uv run pytest -q`·`make smoke` PASS(ROADMAP Success Criteria 5) — 결정 D-14(04-CONTEXT.md, freeze 4종·3곳 기록) 이행
- ROADMAP Success Criteria 1 후반(실 카탈로그 4 variant 서로 다른 책) 실측 기록
- 결정 D-12·D-13 이행, Deferred 문서 갱신 2건(`serving.md`·아키텍처 01 §3-3) 해소, REC-01~08 체크
- 코드·`results/`·`artifacts/` 변경 0, `make millie*` 0, 커밋 0(사용자 승인 후 Phase 1~4 일괄)
</success_criteria>

<output>
완료 후 `.planning/phases/04-freeze/04-06-SUMMARY.md` 를 만든다. 포함: Task 1 게이트 결과(SEEDS 5개 + 제목 5개 + 스냅샷 확인 근거: `pgrep` 빈 출력·`_n_badge_title 0`·`test_coverage_gate` passed) · `make demo` 실행 횟수·grep 검증 표 · 서버 4 variant 판정 표(model_version·level·상위 10 리스트 4개·교집합 크기·기본 variant·배지 제목 0) · freeze 선언 3곳 diff 요지(STATE 행·개발일지 D 번호와 제목·draft 문장) · 채택 상수 최종값 표(04-05 인용) · REQUIREMENTS 체크 8줄 · 문서 갱신 2건(serving.md·아키텍처 01 §3-3) 전후 · "코드·숫자 변경 0" 확인(`git status --short -- src tests results artifacts` 비교) · Phase 5 인계 사항(대리값 교체·compose 앵커 행·`make millie` 금지) · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다. 커밋은 사용자 승인 후 Phase 1~4 산출물 일괄(.planning 포함)" 한 줄.
</output>
