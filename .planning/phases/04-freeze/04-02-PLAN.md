---
phase: 04-freeze
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/ranking/blend.py
  - src/millie_rec/ranking/hybrid.py
  - src/millie_rec/ranking/__init__.py
  - tests/ranking/test_blend.py
  - tests/ranking/test_hybrid.py
autonomous: true
requirements: [REC-02, REC-03, REC-04]
lane: Model (ranking 슬라이스 Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/retrieval/**
  - src/millie_rec/reranking/**
  - src/millie_rec/app/**
  - src/millie_rec/serving/**
  - src/millie_rec/data/**
  - scripts/**
  - data/id_map.csv
  - demo/**
  - tests/test_architecture.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "state_weights(user) 는 α_n = max(0.2, 0.6·e^{−n/20})(n = len(user.history)), 나머지를 β:γ = 3:1 로 나눈 뒤 비어 있는 성분을 빼고 합 1 로 재정규화한다 — seeds 만이면 {alpha: 1.0, beta: 0.0, gamma: 0.0}(신규 β=0, REC-03)"
    - "history 20 + session 비어 있음 → α≈0.274, β≈0.726, γ=0.0 · history 20 + session 있음 → α≈0.221, β≈0.584, γ≈0.195 · history 200 + session 있음 → 정확히 {0.2, 0.6, 0.2}(하한) · 익명(전부 비어 있음) → 전부 0.0"
    - "blend_channels(user, channels, weights, book_stats=) 는 채널(cf·content·pop 슬롯)별 min-max 정규화(후보 1개·분산 0 → 1.0, 채널에 없는 후보 → 0) 후 가중합(기본 W_CF=0.5·W_CONTENT=0.3·W_POP=0.2)으로 ScoredItem 을 (−score, book_id) 정렬해 반환하고, source_channels 는 그 책이 들어 있던 채널의 Candidate.source 문자열 튜플이다"
    - "gap 3항(gap = difficulty − user_level · gap⁺ = max(gap, 0) · n_completed×gap, n_completed = len(user.history) 대리값)이 음수 상수 가중치로 점수에 더해지고, book_stats None · user_level() None · difficulty None 세 경로 모두 가중 0 이라 점수가 변하지 않는다(REC-04)"
    - "HybridRanker(contracts.Ranker) 는 flat Candidate 목록을 source → 슬롯 매핑({itemknn: cf, content: content, popularity: pop})으로 묶어 blend_channels 를 부르는 얇은 래퍼이고, blend 는 user.seen 을 절대 반환하지 않는다"
  artifacts:
    - path: "src/millie_rec/ranking/blend.py"
      provides: "state_weights · ALPHA0 BETA0 GAMMA0 TAU ALPHA_FLOOR WEIGHT_KEYS RESET_BOOST SESSION_BOOST"
      exports: ["state_weights", "ALPHA0", "BETA0", "GAMMA0", "TAU", "ALPHA_FLOOR", "WEIGHT_KEYS"]
      max_lines: 150
    - path: "src/millie_rec/ranking/hybrid.py"
      provides: "blend_channels · HybridRanker · CH_CF CH_CONTENT CH_POP · W_CF W_CONTENT W_POP CHANNEL_WEIGHTS · W_GAP W_GAP_POS W_NCOMP_GAP · SLOT_OF_SOURCE"
      exports: ["blend_channels", "HybridRanker", "CH_CF", "CH_CONTENT", "CH_POP", "CHANNEL_WEIGHTS", "W_CF", "W_CONTENT", "W_POP"]
      max_lines: 150
    - path: "src/millie_rec/ranking/__init__.py"
      provides: "공개 표면 __all__ 20개"
      contains: "\"state_weights\""
    - path: "tests/ranking/test_blend.py"
      provides: "재정규화 합 1 · 신규 β=0 · n=20 두 경우 · 하한 0.2 · 익명 0 · 키 = ZERO_WEIGHTS 키"
      min_lines: 40
    - path: "tests/ranking/test_hybrid.py"
      provides: "min-max 손계산 · 가중합 순서 · source_channels · cf 단독 · gap 3항 점수 변화 · None 3경로 가중 0 · seen 미반환 · HybridRanker 슬롯 매핑"
      min_lines: 80
  key_links:
    - from: "src/millie_rec/ranking/hybrid.py"
      to: "src/millie_rec/contracts.py"
      via: "BookStatsSource.stats/user_level Protocol 로 난이도 소비(구현체 import 금지)"
      pattern: "BookStatsSource"
    - from: "src/millie_rec/ranking/blend.py"
      to: "src/millie_rec/serving/compose.py"
      via: "응답 dict 키 alpha·beta·gamma = compose.ZERO_WEIGHTS 키(코드 의존 없음, 값 규약만)"
      pattern: "WEIGHT_KEYS = \\(\"alpha\", \"beta\", \"gamma\"\\)"
---

<objective>
Phase 4 '추천 파이프라인과 모델 freeze'(.planning/ROADMAP.md "Phase 4: 추천 파이프라인과 모델 freeze")의 **랭킹 단계**를 TDD 한 사이클로 만든다: `ranking/blend.py` `state_weights`(★시간 가변 가중치 α/β/γ — 초기값 0.6/0.3/0.1·하한 0.2·τ=20 감쇠·빈 성분 제거 재정규화)와 `ranking/hybrid.py` `blend_channels`(채널 min-max + 고정 가중합 W_CF/W_CONTENT/W_POP + 난이도 gap 3항) + `HybridRanker`(`contracts.Ranker` 래퍼).

Purpose: **REC-03** α/β/γ 재정규화(신규 유저 β=0 — 응답 `user_state_weights` 의 원천, Plan 04-04 가 `create_app(weights=state_weights)` 로 주입) · **REC-02** `hybrid` = cf ∪ content ∪ pop 가중합 · **REC-04**(Should, 결정 D-05(04-CONTEXT.md, Should 범위)에 따라 구현) gap 피처 3항 + 결측 가중 0 → 수용 기준 '난이도 4층 피처·ablation'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)의 "피처 3항이 각각 순위화 입력에 존재" 몫. 결정 D-01(2단 가중치)·D-02(스케줄 결합)·D-03(min-max + 고정 상수)·D-06(gap 피처) 의 코드 증거이며 PDF P3 ★박스 두 개의 근거 코드.
Output: `src` 2파일 신규 + `__init__` 교체 + `tests/ranking/` 2파일. 커밋 0.

**채널 슬롯 원칙(Advisor 확정 3):** 채널 가중치는 `Candidate.source` 문자열이 아니라 **슬롯 `cf`·`content`·`pop`** 에 붙는다. 공개 함수는 슬롯별 후보 dict 를 받는다(Track B 는 cf 슬롯 retriever 가 `source="content"` 를 내므로 app 이 슬롯 dict 를 직접 만든다 — `data.md` 표기 규칙과 D-01 가중치 구조를 동시에 만족). `HybridRanker` 는 flat 후보용 얇은 래퍼.

**형제 플랜 불변:** `retrieval/`·`reranking/`·`app/`·`serving/`·`data/` 는 읽지도 고치지도 않는다(wave 1 병렬).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-freeze/04-CONTEXT.md
@.planning/phases/04-freeze/04-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/ranking/__init__.py
@src/millie_rec/retrieval/__init__.py
@src/millie_rec/evaluation/metrics.py
@tests/conftest.py
@tests/evaluation/test_metrics.py
@tests/evaluation/test_harness.py
@tests/test_architecture.py
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/architecture.md
@../.claude/rules/simplicity.md
@../.claude/rules/data.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** `uv run pytest <대상> --no-header` 만(`-q` 금지 — addopts 에 이미 있음). 맨 `pytest`·`python3` 금지(Python 3.9 → collection error). **RED 는 `AssertionError`/`E  assert` 로만 인정**, `ImportError`·`AttributeError`·`TypeError` 는 RED 가 아니다 → 시그니처만 있는 스텁 먼저. `pytest.skip` 금지. `tests/test_architecture.py`·기존 테스트 수정 금지.

**star 의존(`../.claude/rules/architecture.md`, `tests/test_architecture.py` L37-42):** `ranking/*.py` 는 `millie_rec.contracts` 와 `millie_rec.ranking.<module>` 만 import. **`millie_rec.retrieval` import 금지**(`SOURCE_ITEMKNN` 등은 ranking 에 **중복 정의** — "같은 코드가 3개 슬라이스에서 필요해질 때 Advisor 승인으로 `_shared`", 그 전까지 중복 허용). `ItemVectors`·`BookStatsSource` 는 Protocol 로 받는다(구현체 `CatalogKR` import 금지).

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄. 함수 우선 — 클래스는 `contracts.Ranker` 를 만족시키는 `HybridRanker` 하나. 설정은 모듈 상단 대문자 상수 + `#` 주석에 근거(D-xx). 이 상수들은 **freeze 대상(D-14 ①)** — 이름을 바꾸지 않는다: `ALPHA0=0.6 BETA0=0.3 GAMMA0=0.1 TAU=20 ALPHA_FLOOR=0.2 W_CF=0.5 W_CONTENT=0.3 W_POP=0.2 W_GAP W_GAP_POS W_NCOMP_GAP`. 개발일지 D 항목·PDF 각주가 이 이름을 인용한다.

**python(`../.claude/rules/python.md`):** "같은 입력 → 같은 결과여야 PDF 숫자가 재현된다" → 정렬은 `(-score, book_id)` 로 동률 결정적. 순수 함수, I/O 없음, `logging` 불필요.

**테스트 3종(`../.claude/rules/python-tdd.md`):** ① 계약(dict 키·ScoredItem 타입·position·source_channels) ② 정확성(손계산 — 수식 대입 값) ③ 안전성(seen 미반환·빈 채널·None 3경로). 기대값은 구현 상수를 import 하지 않고 **손으로 적는다**(구현이 상수를 바꾸면 테스트가 잡아야 freeze 가 의미 있다). float 는 `pytest.approx`.

**완료 기준(자기 경로만 — wave 병렬):** `uv run ruff format --check src/millie_rec/ranking tests/ranking && uv run ruff check src/millie_rec/ranking tests/ranking` 클린 · `uv run pytest tests/ranking tests/test_architecture.py --no-header` failed 0. 전체 트리·`make smoke` 는 wave 1 종료 후 Advisor 1회(04-04-PLAN.md `<wave_gate>`).
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. contracts.py 변경 금지. -->

From src/millie_rec/contracts.py (verbatim 발췌):
```python
K_RANK = 10

@dataclass(frozen=True, slots=True)
class UserState:
    user_id: int | None
    explicit_seeds: tuple[int, ...] = ()
    history: tuple[int, ...] = ()
    session: tuple[int, ...] = ()
    context: dict[str, str] = field(default_factory=dict)
    @property
    def seen(self) -> frozenset[int]: ...

@dataclass(frozen=True, slots=True)
class Candidate:
    book_id: int
    source: str  # "popularity" | "itemknn" | "content" | "explicit"
    score: float

@dataclass(frozen=True, slots=True)
class ScoredItem:
    book_id: int
    score: float
    source: str | None = None
    reason: str | None = None
    title: str | None = None
    authors: str | None = None
    image_url: str | None = None
    position: int | None = None
    source_channels: tuple[str, ...] = ()
    badge: Badge | None = None
    book_format: str | None = None
    difficulty: float | None = None

@dataclass(frozen=True, slots=True)
class BookStats:
    book_id: int
    difficulty: float | None = None   # 0~1. None = 결측(가드 모집단 제외·가중 0)
    ...
    source: str = "prior"             # STATS_SOURCES = ("millie_index", "category_prior", "behavior", "prior")
    resid_z: float | None = None

class Ranker(Protocol):
    def rank(self, user: UserState, candidates: list[Candidate]) -> list[ScoredItem]: ...

class BookStatsSource(Protocol):
    def stats(self, book_ids: Sequence[int]) -> list[BookStats]: ...
    def user_level(self, user: UserState) -> float | None: ...
```

From src/millie_rec/serving/compose.py L21 (값 규약만 — import 하지 않는다):
```python
ZERO_WEIGHTS = {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}   # 응답 user_state_weights 키
```

From src/millie_rec/evaluation/metrics.py L11-16 (아날로그 — 상수 기본 인자·경계값·1줄 docstring 순수 함수):
```python
def recall_at_k(ranked: Sequence[int], relevant: Set[int], k: int = K_RECALL) -> float:
    """|L_u[:k] ∩ R_u| / |R_u|. 분모를 min(|R_u|, k) 로 바꾸지 않는다(evaluation.md)."""
    if not relevant:
        return 0.0
```

가짜 Protocol 구현 패턴(tests/evaluation/test_harness.py::_OneHot L30-37, tests/app/test_pipeline.py::_FakeCatalog L59-79) — 상속 없이 메서드만 갖는 클래스.
</interfaces>
</context>

<feature>
  <name>랭킹 단계 — 시간 가변 사용자 상태 가중치 state_weights + 채널 min-max 가중합 blend_channels(gap 3항 포함) + HybridRanker</name>
  <files>
    src/millie_rec/ranking/blend.py, src/millie_rec/ranking/hybrid.py, src/millie_rec/ranking/__init__.py,
    tests/ranking/test_blend.py, tests/ranking/test_hybrid.py
  </files>
  <behavior>
    **blend.py `state_weights(user, *, reset_boost=False, session_active=False) -> dict[str, float]`** (수식: α_n = max(0.2, 0.6·e^{−n/20}), 나머지 (1−α) 를 β:γ = 0.3:0.1 로, 빈 성분 제거 후 합 1 재정규화, `round(…, 3)`)
    - (a) seeds 만 `UserState(None, explicit_seeds=(1, 2, 3, 4, 5))` → `{"alpha": 1.0, "beta": 0.0, "gamma": 0.0}` 정확히(REC-03 신규 β=0)
    - (b) seeds + history 20 + session 비어 있음 → α = 0.220728/(0.220728+0.584454) ≈ **0.274**, β ≈ **0.726**, γ = **0.0**; 합 ≈ 1.0(abs 2e-3)
    - (c) seeds + history 20 + session `(99,)` → α ≈ **0.221**(0.6·e^{−1} = 0.220728), β ≈ **0.584**, γ ≈ **0.195**; 합 ≈ 1.0(abs 2e-3) — CONTEXT specifics 의 "α≈0.22, β≈0.585, γ≈0.195"
    - (d) seeds + history 200 + session `(99,)` → 하한: α = 0.2 정확히, β = 0.6, γ = 0.2(abs 1e-9 — 0.8·0.75, 0.8·0.25 는 정확)
    - (e) 익명 `UserState(None)` → `{"alpha": 0.0, "beta": 0.0, "gamma": 0.0}`(합 0 → 나눗셈 회피, compose.ZERO_WEIGHTS 와 동일)
    - (f) history 만(seeds 없음) 20 + session 없음 → `{"alpha": 0.0, "beta": 1.0, "gamma": 0.0}`
    - 키 순서·집합 == `("alpha", "beta", "gamma")`; `reset_boost=True`·`session_active=True` 를 넘겨도 Phase 4 에서는 결과가 (a)~(f) 와 동일(적용은 Phase 5 `state.py` — D-02)
    - 같은 입력 두 번 → 같은 dict

    **hybrid.py `blend_channels(user, channels: dict[str, list[Candidate]], weights: dict[str, float] | None = None, *, book_stats: BookStatsSource | None = None) -> list[ScoredItem]`** (손계산 채널: `cf: [C(1,"itemknn",4.0), C(2,"itemknn",2.0)]`, `content: [C(2,"content",1.0), C(3,"content",0.5)]`, `pop: [C(3,"popularity",10.0)]`; min-max → cf 1→1.0 2→0.0 · content 2→1.0 3→0.0 · pop 3→1.0(단독); 가중 0.5/0.3/0.2)
    - 기본 weights → book_id 순서 `[1, 2, 3]`, score `[0.5, 0.3, 0.2]`(approx), `position == [0, 1, 2]`, 전부 `ScoredItem`
    - `source_channels`: 1 → `("itemknn",)`, 2 → `("itemknn", "content")`, 3 → `("content", "popularity")`(그 책이 **들어 있던** 채널 — 정규화 점수 0 이어도 포함, 슬롯 순서 cf→content→pop, 중복 제거)
    - `source`: 1 → `"itemknn"`, 2 → `"content"`(기여 0.3 > 0.0), 3 → `"popularity"`(0.2 > 0.0) — 가중 기여 최대 채널의 source, 동률은 앞 슬롯
    - `weights={"cf": 1.0}` (cf variant) → `[1, 2]` score `[1.0, 0.0]`, 3 은 없음(가중 없는 슬롯은 후보에서도 빠진다 — cf variant 의 채널 dict 에는 cf 만 들어오므로 실제로는 `channels={"cf": …}` 로 부른다; 테스트는 두 형태 모두 같은 결과를 단정)
    - min-max 경계: 채널 후보 1개 → 1.0; 두 후보 점수 동일(분산 0) → 둘 다 1.0; 빈 dict `channels={}` → `[]`; 어떤 채널이 `[]` 여도 예외 없음
    - **gap 3항(가짜 `_FakeStats(level=0.5)`: book1 difficulty 0.9(gap 0.4) · book2 0.5(gap 0) · book3 None, 전부 source millie_index; 상수 W_GAP=−0.1, W_GAP_POS=−0.2, W_NCOMP_GAP=−0.01)**: history 비어 있음(n_completed 0) → book1 = 0.5 + (−0.1·0.4) + (−0.2·0.4) = **0.38**, book2 = 0.3(gap 0), book3 = 0.2(결측 → 가중 0) → 순서 여전히 `[1, 2, 3]`, `items[0].score ≈ 0.38`; `items[0].difficulty == 0.9`, `items[2].difficulty is None`
    - history `(7, 8)`(n_completed 2, 7·8 은 후보에 없음) → book1 = 0.38 + (−0.01·2·0.4) = **0.372**
    - None 3경로 → 점수가 기본 케이스 `[0.5, 0.3, 0.2]` 와 동일: ① `book_stats=None` ② `user_level()` 이 None 을 주는 가짜 ③ `difficulty` 전부 None 인 가짜
    - seen 안전성: `UserState(None, explicit_seeds=(2,))` 로 위 채널 → book 2 가 결과에 없다(retriever 가 걸러도 blend 가 한 번 더 막는다)
    - **`HybridRanker().rank(user, flat)`** — `flat = cf + content + pop` 후보를 한 리스트로 → `blend_channels(user, {"cf": …, "content": …, "pop": …})` 와 book_id·score 동일; `HybridRanker(weights={"cf": 1.0}).rank(user, flat)` → `[1, 2]`; 미지 source(`"explicit"`) 후보는 매핑에 없어 무시된다(예외 없음)
  </behavior>
  <implementation>Task 2 action 에 상수·시그니처·알고리즘이 있다. 그대로 쓴다.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): ranking 스텁 2파일 + 테스트 2파일 → 전 테스트 AssertionError</name>
  <files>src/millie_rec/ranking/blend.py, src/millie_rec/ranking/hybrid.py, tests/ranking/test_blend.py, tests/ranking/test_hybrid.py</files>
  <read_first>
    - src/millie_rec/contracts.py L122-135(`UserState`), L211-232(`Candidate`·`ScoredItem`), L168-192(`BookStats`), L298-301(`Ranker`), L334-338(`BookStatsSource`)
    - src/millie_rec/ranking/__init__.py (현재 `__all__: list[str] = []` — Task 3 에서 교체)
    - tests/conftest.py L1-43 (fixture 형태만 — 이 플랜 테스트는 손계산 inline 데이터 위주)
    - tests/evaluation/test_metrics.py (손계산·`pytest.approx` 관례) · tests/evaluation/test_harness.py L30-37(`_OneHot` 가짜 Protocol 패턴)
    - .planning/phases/04-freeze/04-PATTERNS.md "ranking/blend.py" 절(제안 형태 + ⚠ 내부 불일치 해소: 테스트 3건) · "ranking/hybrid.py" 절 Analog A·B · "No Analog" 스니펫 B
    - .planning/phases/04-freeze/04-CONTEXT.md D-01·D-02·D-03·D-06, specifics "`user_state_weights` 기대"
    - ../.claude/rules/python-tdd.md 전문
  </read_first>
  <action>
    **왜 스텁인가:** import 가 죽으면 RED 가 아니다. Task 2 와 같은 상수·시그니처, 값만 틀린 스텁을 두어 모든 테스트가 `AssertionError` 로 실패하게 한다.

    **1-a. `src/millie_rec/ranking/blend.py` 스텁** — 상수 전부(Task 2-a 와 동일 값), `state_weights(user, *, reset_boost=False, session_active=False) -> dict[str, float]` 는 `{"alpha": 0.0, "beta": 0.0, "gamma": 0.0}` 고정 반환(→ (a)~(d)·(f) 실패, (e) 익명 케이스는 우연히 통과할 수 있다 — 그래서 (e) 테스트는 `state_weights(UserState(None, explicit_seeds=(1,)))["alpha"] == 1.0` 도 같은 함수 안에서 단정해 스텁에서 실패하게 한다).
    **1-b. `src/millie_rec/ranking/hybrid.py` 스텁** — 상수 전부(Task 2-b), `blend_channels(user, channels, weights=None, *, book_stats=None) -> list[ScoredItem]` 는 `[]`, `class HybridRanker` 는 `__init__(self, weights=None, *, book_stats=None, slot_of=None)` + `rank(self, user, candidates) -> list[ScoredItem]: return []`.

    **1-c. `tests/ranking/test_blend.py`** (8건). 헤더 docstring `"""ranking/blend.py state_weights — 계약(키) · 정확성(수식 대입 손계산) · 안전성(익명·부스트 인자 무효)."""`, `import pytest`, `from millie_rec.contracts import UserState`, `from millie_rec.ranking.blend import state_weights`. 헬퍼 `_u(n_hist: int, session: bool = False, seeds: bool = True) -> UserState`: `UserState(None, explicit_seeds=(1, 2, 3, 4, 5) if seeds else (), history=tuple(range(100, 100 + n_hist)), session=(99,) if session else ())`.
    - `# ── 계약 ──` `test_keys_are_alpha_beta_gamma_in_order` — `list(state_weights(_u(0))) == ["alpha", "beta", "gamma"]`; 값 전부 `float`
    - `# ── 정확성 ──` `test_new_user_seeds_only_gives_alpha_1_beta_0` — `state_weights(_u(0)) == {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}`(정확 비교 — REC-03)
    - `test_history_20_without_session_renormalizes_over_alpha_beta` — `w = state_weights(_u(20))`; `w["alpha"] == pytest.approx(0.274, abs=2e-3)`; `w["beta"] == pytest.approx(0.726, abs=2e-3)`; `w["gamma"] == 0.0`; `sum(w.values()) == pytest.approx(1.0, abs=2e-3)`
    - `test_history_20_with_session_matches_decay_formula` — `w = state_weights(_u(20, session=True))`; `alpha ≈ 0.221`, `beta ≈ 0.584`, `gamma ≈ 0.195`(abs 2e-3 각각); 합 ≈ 1.0(abs 2e-3)
    - `test_history_200_hits_alpha_floor_exactly` — `state_weights(_u(200, session=True)) == {"alpha": 0.2, "beta": 0.6, "gamma": 0.2}`(round 3 후 정확)
    - `test_history_only_gives_beta_1` — `state_weights(_u(20, seeds=False)) == {"alpha": 0.0, "beta": 1.0, "gamma": 0.0}`
    - `# ── 안전성 ──` `test_anonymous_is_all_zero_and_seeds_user_is_not` — `state_weights(UserState(None)) == {"alpha": 0.0, "beta": 0.0, "gamma": 0.0}`; `state_weights(UserState(None, explicit_seeds=(1,)))["alpha"] == 1.0`
    - `test_boost_flags_accepted_but_not_applied_in_phase4` — `state_weights(_u(20, session=True), reset_boost=True, session_active=True) == state_weights(_u(20, session=True))`; 두 번 호출 동일

    **1-d. `tests/ranking/test_hybrid.py`** (10건). 헤더 docstring `"""ranking/hybrid.py blend_channels·HybridRanker — 계약 · 정확성(min-max 손계산·gap 3항) · 안전성(None 3경로·seen)."""`. import: `pytest`, `from millie_rec.contracts import BookStats, Candidate, ScoredItem, UserState`, `from millie_rec.ranking.hybrid import HybridRanker, blend_channels`. 상단 손계산 재료:
    ```python
    def _channels() -> dict[str, list[Candidate]]:
        """min-max 후: cf 1→1.0 2→0.0 · content 2→1.0 3→0.0 · pop 3→1.0(단독). 가중 0.5/0.3/0.2."""
        return {
            "cf": [Candidate(1, "itemknn", 4.0), Candidate(2, "itemknn", 2.0)],
            "content": [Candidate(2, "content", 1.0), Candidate(3, "content", 0.5)],
            "pop": [Candidate(3, "popularity", 10.0)],
        }

    class _FakeStats:
        """contracts.BookStatsSource 가짜 — level 과 difficulty 표만."""
        def __init__(self, level: float | None, difficulty: dict[int, float | None]) -> None:
            self.level, self.difficulty = level, difficulty
        def stats(self, book_ids):
            return [BookStats(book_id=int(b), difficulty=self.difficulty.get(int(b)), source="millie_index") for b in book_ids]
        def user_level(self, user):
            return self.level

    U = UserState(None, explicit_seeds=(50,))  # 후보와 겹치지 않는 seed
    ```
    - `# ── 계약 ──` `test_blend_returns_scored_items_sorted_with_positions` — `items = blend_channels(U, _channels())`; `[i.book_id for i in items] == [1, 2, 3]`; `[i.score for i in items] == pytest.approx([0.5, 0.3, 0.2])`; `[i.position for i in items] == [0, 1, 2]`; `all(isinstance(i, ScoredItem) for i in items)`
    - `test_source_channels_and_source_follow_membership_and_max_contribution` — `items[0].source_channels == ("itemknn",)`; `items[1].source_channels == ("itemknn", "content")`; `items[2].source_channels == ("content", "popularity")`; `[i.source for i in items] == ["itemknn", "content", "popularity"]`
    - `# ── 정확성 ──` `test_cf_only_weights_drop_other_channels` — `a = blend_channels(U, _channels(), weights={"cf": 1.0})`; `b = blend_channels(U, {"cf": _channels()["cf"]}, weights={"cf": 1.0})`; 둘 다 book_id `[1, 2]`, score `approx([1.0, 0.0])`
    - `test_minmax_edges_single_and_constant_channel` — `blend_channels(U, {"pop": [Candidate(9, "popularity", 3.0)]}, weights={"pop": 1.0})[0].score == pytest.approx(1.0)`; `blend_channels(U, {"cf": [Candidate(1, "itemknn", 2.0), Candidate(2, "itemknn", 2.0)]}, weights={"cf": 1.0})` 의 score `== approx([1.0, 1.0])` 이고 book_id `[1, 2]`(동률 → id 오름차순)
    - `test_gap_features_penalize_harder_books_with_negative_weights` — `stats = _FakeStats(0.5, {1: 0.9, 2: 0.5, 3: None})`; `items = blend_channels(U, _channels(), book_stats=stats)`; `[i.book_id for i in items] == [1, 2, 3]`; `items[0].score == pytest.approx(0.38)`; `items[1].score == pytest.approx(0.3)`; `items[2].score == pytest.approx(0.2)`; `items[0].difficulty == 0.9 and items[2].difficulty is None`
    - `test_n_completed_times_gap_uses_history_length_proxy` — `u = UserState(None, explicit_seeds=(50,), history=(7, 8))`; `blend_channels(u, _channels(), book_stats=_FakeStats(0.5, {1: 0.9, 2: 0.5, 3: None}))[0].score == pytest.approx(0.372)`
    - `# ── 안전성 ──` `test_missing_stats_level_or_difficulty_means_zero_weight` — 기준 `base = [0.5, 0.3, 0.2]`; ① `blend_channels(U, _channels(), book_stats=None)` ② `_FakeStats(None, {1: 0.9, 2: 0.5, 3: 0.1})` ③ `_FakeStats(0.5, {1: None, 2: None, 3: None})` 세 경우 모두 score `approx(base)` 이고 순서 `[1, 2, 3]`
    - `test_seen_candidates_are_never_returned` — `blend_channels(UserState(None, explicit_seeds=(2,)), _channels())` 의 book_id 에 2 없음, 나머지 `[1, 3]`
    - `test_empty_channels_give_empty_list` — `blend_channels(U, {}) == []`; `blend_channels(U, {"cf": [], "content": [], "pop": []}) == []`
    - `test_hybrid_ranker_groups_flat_candidates_by_source_slot` — `flat = [*_channels()["cf"], *_channels()["content"], *_channels()["pop"], Candidate(4, "explicit", 1.0)]`; `r = HybridRanker().rank(U, flat)`; `[i.book_id for i in r] == [1, 2, 3]`(explicit 는 매핑 없어 무시); score `approx([0.5, 0.3, 0.2])`; `HybridRanker(weights={"cf": 1.0}).rank(U, flat)` 의 book_id `[1, 2]`

    **1-e. RED 확인:** `uv run pytest tests/ranking --no-header 2>&1 | tail -40` → 18 failed, 사유 `AssertionError`/`E  assert` 만. `uv run pytest tests/test_architecture.py --no-header` → `3 passed`.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/ranking --no-header 2>&1 | tail -30; uv run pytest tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/millie_rec/ranking/blend.py src/millie_rec/ranking/hybrid.py tests/ranking/test_blend.py tests/ranking/test_hybrid.py` 4파일 존재
    - `grep -c "def test_" tests/ranking/test_blend.py tests/ranking/test_hybrid.py` → 8, 10
    - RED 요약 줄에 신규 테스트의 대다수가 failed(checker 재계산: `test_keys_are_alpha_beta_gamma_in_order`·`test_boost_flags_accepted…`·`test_empty_channels_give_empty_list` 3건은 스텁에서도 통과 → 예상 `15 failed, 3 passed`; 판정 기준은 숫자가 아니라 사유); 출력에 `AssertionError` 또는 `E  assert` 가 있고 `ImportError`·`ModuleNotFoundError`·`AttributeError`·`TypeError`·`SyntaxError` 없음
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(retrieval\|reranking\|data\|evaluation\|serving\|app\)" src/millie_rec/ranking/` → 출력 없음
    - `grep -rn "^from millie_rec\|^import" tests/ranking/test_blend.py tests/ranking/test_hybrid.py | grep -c "ranking\.\(blend\|hybrid\) import"` == 2 (구현 상수는 import 하지 않는다 — `ALPHA0` 등 이름이 테스트에 없음: `grep -c "ALPHA0\|W_CF\|W_GAP" tests/ranking/*.py` == 0)
  </acceptance_criteria>
  <done>18건 전부 AssertionError 계열 RED, 아키텍처 3 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): blend.state_weights + hybrid.blend_channels/HybridRanker 구현 → 18 passed</name>
  <files>src/millie_rec/ranking/blend.py, src/millie_rec/ranking/hybrid.py</files>
  <read_first>
    - Task 1 이 만든 스텁 2파일과 테스트 2파일(단정이 요구하는 정확한 값)
    - .planning/phases/04-freeze/04-PATTERNS.md "ranking/blend.py" 제안 형태(verbatim 수식) · "No Analog" 스니펫 B(`_minmax`) · "ranking/hybrid.py" Analog A(ScoredItem 조립 L57-77)
    - .planning/phases/04-freeze/04-CONTEXT.md D-02(재정규화 규칙이 정본)·D-03·D-06·D-08(`n_completed = len(user.history)` 대리값)
  </read_first>
  <action>
    각 파일 ≤150줄, 줄 ≤100자. 모듈 머리 = 1줄 docstring → stdlib → `millie_rec.contracts`. 서드파티 import 없음(순수 파이썬 산술 — numpy 불필요).

    **2-a. `src/millie_rec/ranking/blend.py`** — docstring `"""★시간 가변 사용자 상태 가중치 α/β/γ — main §5-2 감쇠 + 아키텍처 01 §3-3 초기값·하한·재정규화(D-02)."""`
    ```python
    import math

    from millie_rec.contracts import UserState

    ALPHA0, BETA0, GAMMA0 = 0.6, 0.3, 0.1  # 아키텍처 01 §3-3 초기값. freeze(D-14 ①)
    TAU = 20  # main §5-2 α_n = α₀·e^{−n/τ}. n=20 → α≈0.22
    ALPHA_FLOOR = 0.2  # 아키텍처 01 §3-3 하한 — explicit prior 는 0 이 되지 않는다
    WEIGHT_KEYS = ("alpha", "beta", "gamma")  # 응답 user_state_weights 키 = serving/compose.ZERO_WEIGHTS 키
    RESET_BOOST, SESSION_BOOST = 0.15, 0.10  # D-02: 인자로만 받는다. 적용은 Phase 5 state.py
    ROUND = 3  # 응답 표시 소수 3자리(CONTEXT specifics)


    def state_weights(
        user: UserState, *, reset_boost: bool = False, session_active: bool = False
    ) -> dict[str, float]:
        """α_n = max(0.2, 0.6·e^{−n/20}), 나머지를 β:γ = 3:1 → 비어 있는 성분 제거 → 합 1 재정규화.

        n = len(user.history)(Phase 4 는 이벤트가 없다). reset_boost·session_active 는 Phase 5 가 적용한다.
        """
        n = len(user.history)
        alpha = max(ALPHA_FLOOR, ALPHA0 * math.exp(-n / TAU))
        rest = 1.0 - alpha
        raw = {
            "alpha": alpha,
            "beta": rest * BETA0 / (BETA0 + GAMMA0),
            "gamma": rest * GAMMA0 / (BETA0 + GAMMA0),
        }
        present = {"alpha": bool(user.explicit_seeds), "beta": bool(user.history), "gamma": bool(user.session)}
        kept = {k: v for k, v in raw.items() if present[k]}
        total = sum(kept.values())
        if total <= 0.0:
            return {k: 0.0 for k in WEIGHT_KEYS}  # 익명 — compose.ZERO_WEIGHTS 와 같은 값
        return {k: round(kept.get(k, 0.0) / total, ROUND) for k in WEIGHT_KEYS}
    ```
    `reset_boost`·`session_active` 는 본문에서 참조하지 않는다(ruff 기본 규칙은 미사용 인자를 잡지 않는다; 잡히면 `del reset_boost, session_active` 대신 docstring 유지 + `# noqa: ARG001` 금지 — 규칙 집합을 확인하고 필요하면 `_ = (reset_boost, session_active)` 1줄).
    손계산 검증: n=20 → α=0.220728, rest=0.779272, β_raw=0.584454, γ_raw=0.194818 → session 있으면 (0.221, 0.584, 0.195); session 없으면 kept={α, β} total=0.805182 → (0.274, 0.726, 0.0). n=200 → α=0.2, β=0.6, γ=0.2.

    **2-b. `src/millie_rec/ranking/hybrid.py`** — docstring `"""hybrid 가중합 랭커 — 채널(슬롯) min-max + 고정 상수 가중합(D-03) + ★난이도 gap 3항(D-06). contracts.Ranker 구현."""`
    ```python
    from collections.abc import Sequence
    from dataclasses import replace

    from millie_rec.contracts import BookStatsSource, Candidate, ScoredItem, UserState

    CH_CF, CH_CONTENT, CH_POP = "cf", "content", "pop"  # 채널 슬롯. Candidate.source(표기) 와 다른 층(Advisor 확정 3)
    W_CF, W_CONTENT, W_POP = 0.5, 0.3, 0.2  # D-03 초기값. Day 3 ≤3조합 그리드 1회 후 freeze(D-14 ①)
    CHANNEL_WEIGHTS = {CH_CF: W_CF, CH_CONTENT: W_CONTENT, CH_POP: W_POP}
    SLOT_ORDER = (CH_CF, CH_CONTENT, CH_POP)  # source_channels 순서·동률 우선순위
    # Track A source → 슬롯. Track B 는 app 이 슬롯 dict 를 직접 만든다(cf 슬롯도 source="content")
    SOURCE_ITEMKNN, SOURCE_CONTENT, SOURCE_POPULARITY = "itemknn", "content", "popularity"  # retrieval 과 중복 정의
    SLOT_OF_SOURCE = {SOURCE_ITEMKNN: CH_CF, SOURCE_CONTENT: CH_CONTENT, SOURCE_POPULARITY: CH_POP}
    # D-06 ★난이도 gap 3항 — 음수(어려운 쪽 감점). 결측·level None 은 가중 0. freeze(D-14 ①)
    W_GAP, W_GAP_POS, W_NCOMP_GAP = -0.10, -0.20, -0.01
    ```
    - `def _minmax(cands: Sequence[Candidate]) -> dict[int, float]` — 스니펫 B 그대로: 빈 → `{}`; `span = hi - lo`; `span > 0` 이면 `(score-lo)/span`, 아니면 1.0(후보 1개·분산 0 — Claude's Discretion). 같은 book_id 가 채널 안에 두 번 오면 큰 점수 유지(`max`) — `dict` 조립 전에 `by_id = {}` 로 `max` 처리.
    - `def _gap_bonus(user: UserState, book_ids: Sequence[int], book_stats: BookStatsSource | None) -> tuple[dict[int, float], dict[int, float | None]]` — 반환 `(bonus, difficulty)`. `if book_stats is None: return {}, {}`; `level = book_stats.user_level(user)`; `stats = {s.book_id: s for s in book_stats.stats(list(book_ids))}`; `difficulty = {b: (stats[b].difficulty if b in stats else None) for b in book_ids}`; `if level is None: return {}, difficulty`; `n_completed = len(user.history)  # D-08 대리값 — Phase 5 state.py 가 completion 이벤트 수로 교체`; `bonus = {}`; `for b, d in difficulty.items(): if d is None: continue; gap = d - level; bonus[b] = W_GAP * gap + W_GAP_POS * max(gap, 0.0) + W_NCOMP_GAP * n_completed * gap`; `return bonus, difficulty`.
    - `def blend_channels(user, channels: dict[str, list[Candidate]], weights: dict[str, float] | None = None, *, book_stats: BookStatsSource | None = None) -> list[ScoredItem]` — docstring `"""슬롯별 min-max → Σ w_slot·norm(채널에 없으면 0) + gap 3항 → (−score, book_id) 정렬. seen 은 넣지 않는다."""`:
      ```python
      w = dict(CHANNEL_WEIGHTS if weights is None else weights)
      slots = [s for s in SLOT_ORDER if s in channels and s in w] + [s for s in channels if s in w and s not in SLOT_ORDER]
      norm = {s: _minmax(channels[s]) for s in slots}
      src = {s: {c.book_id: c.source for c in channels[s]} for s in slots}
      books = sorted({b for s in slots for b in norm[s] if b not in user.seen})
      if not books:
          return []
      bonus, difficulty = _gap_bonus(user, books, book_stats)
      scored = []
      for b in books:
          contrib = {s: w[s] * norm[s].get(b, 0.0) for s in slots if b in norm[s]}
          total = sum(contrib.values()) + bonus.get(b, 0.0)
          best = max(contrib, key=lambda s: (contrib[s], -SLOT_ORDER.index(s) if s in SLOT_ORDER else 0))
          chans = tuple(dict.fromkeys(src[s][b] for s in slots if b in norm[s]))  # 순서 유지 중복 제거
          scored.append((total, b, src[best][b], chans, difficulty.get(b)))
      scored.sort(key=lambda t: (-t[0], t[1]))
      return [ScoredItem(book_id=b, score=float(t), source=s, position=i, source_channels=ch, difficulty=d)
              for i, (t, b, s, ch, d) in enumerate(scored)]
      ```
      `best` 동률 규칙: 기여가 같으면 `SLOT_ORDER` 앞 슬롯 — `max(..., key=(contrib, -index))` 가 그 뜻(테스트: book 2 는 content 0.3 > cf 0.0 → content; book 3 은 pop 0.2 > content 0.0 → popularity).
    - `class HybridRanker:` docstring `"""contracts.Ranker — flat Candidate 를 source → 슬롯으로 묶어 blend_channels 를 부르는 얇은 래퍼."""`; `__init__(self, weights: dict[str, float] | None = None, *, book_stats: BookStatsSource | None = None, slot_of: dict[str, str] | None = None)`: 저장(`slot_of` None → `SLOT_OF_SOURCE`); `rank(self, user, candidates)`: `channels: dict[str, list[Candidate]] = {}`; `for c in candidates: slot = self._slot_of.get(c.source); if slot is None: continue; channels.setdefault(slot, []).append(c)`; `return blend_channels(user, channels, self._weights, book_stats=self._book_stats)`.
    (`replace` import 는 필요 없으면 지운다 — ruff F401.)

    **2-c. GREEN 확인:** `uv run pytest tests/ranking tests/test_architecture.py --no-header` → `21 passed`(18 + 3). 실패가 남으면 구현을 고친다. 손계산 검증 포인트: `test_gap_features…` 0.38 = 0.5 − 0.04 − 0.08; `…proxy` 0.372 = 0.38 − 0.008.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/ranking tests/test_architecture.py --no-header 2>&1 | tail -3 && wc -l src/millie_rec/ranking/*.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ranking tests/test_architecture.py --no-header` 요약 줄 `21 passed`, failed 없음
    - `wc -l src/millie_rec/ranking/blend.py src/millie_rec/ranking/hybrid.py` 각 ≤ 150
    - `grep -n "^ALPHA0, BETA0, GAMMA0 = 0.6, 0.3, 0.1" src/millie_rec/ranking/blend.py` 1행 · `grep -n "^TAU = 20" src/millie_rec/ranking/blend.py` 1행 · `grep -n "^ALPHA_FLOOR = 0.2" src/millie_rec/ranking/blend.py` 1행 · `grep -n '^WEIGHT_KEYS = ("alpha", "beta", "gamma")' src/millie_rec/ranking/blend.py` 1행
    - `grep -n "^W_CF, W_CONTENT, W_POP = 0.5, 0.3, 0.2" src/millie_rec/ranking/hybrid.py` 1행 · `grep -n "^W_GAP, W_GAP_POS, W_NCOMP_GAP = -0.10, -0.20, -0.01" src/millie_rec/ranking/hybrid.py` 1행 · `grep -n '^CH_CF, CH_CONTENT, CH_POP = "cf", "content", "pop"' src/millie_rec/ranking/hybrid.py` 1행
    - `grep -c "def state_weights\|def blend_channels\|def _minmax\|def _gap_bonus\|class HybridRanker" src/millie_rec/ranking/*.py` 합 == 5
    - `grep -n "len(user.history)" src/millie_rec/ranking/blend.py src/millie_rec/ranking/hybrid.py` ≥ 2행(n 과 n_completed 대리값) · `grep -n "대리값" src/millie_rec/ranking/hybrid.py` ≥ 1행(D-08 주석)
    - `grep -rn "millie_rec\.\(retrieval\|reranking\|data\|evaluation\|serving\|app\)" src/millie_rec/ranking/` → 출력 없음 · `grep -c "^import numpy\|^import pandas" src/millie_rec/ranking/*.py` == 0
    - `awk 'length > 100' src/millie_rec/ranking/*.py tests/ranking/*.py | wc -l` == 0
  </acceptance_criteria>
  <done>tests/ranking 18건 GREEN, 아키텍처 3 passed, 상수 값이 04-CONTEXT.md D-02·D-03·D-06 과 문자 단위 일치. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR): ranking/__init__.py 공개 표면 20개 + ruff(자기 경로) + 재통과</name>
  <files>src/millie_rec/ranking/__init__.py</files>
  <read_first>
    - src/millie_rec/ranking/__init__.py (현재 `__all__: list[str] = []`)
    - src/millie_rec/retrieval/__init__.py (형태 아날로그: import → `__all__` 알파벳순, 상수 → 클래스 → 함수)
    - Task 2 결과 2파일(노출 이름 확인 — `_minmax`·`_gap_bonus`·`ROUND`·`SLOT_ORDER`·`SOURCE_*` 는 내부)
  </read_first>
  <action>
    **3-a. `src/millie_rec/ranking/__init__.py`** 교체(Plan 04-04 `app/` 가 `from millie_rec.ranking import CH_CF, CH_CONTENT, CH_POP, CHANNEL_WEIGHTS, blend_channels, state_weights` 로 가져온다; 상수는 개발일지 freeze 인용·Plan 04-05 튜닝 시 참조):
    ```python
    """ranking 슬라이스 공개 표면. 다른 슬라이스는 여기 있는 이름만 import 할 수 있다."""

    from millie_rec.ranking.blend import (
        ALPHA0,
        ALPHA_FLOOR,
        BETA0,
        GAMMA0,
        TAU,
        WEIGHT_KEYS,
        state_weights,
    )
    from millie_rec.ranking.hybrid import (
        CH_CF,
        CH_CONTENT,
        CH_POP,
        CHANNEL_WEIGHTS,
        SLOT_OF_SOURCE,
        W_CF,
        W_CONTENT,
        W_GAP,
        W_GAP_POS,
        W_NCOMP_GAP,
        W_POP,
        HybridRanker,
        blend_channels,
    )

    __all__ = [
        "ALPHA0",
        "ALPHA_FLOOR",
        "BETA0",
        "CHANNEL_WEIGHTS",
        "CH_CF",
        "CH_CONTENT",
        "CH_POP",
        "GAMMA0",
        "SLOT_OF_SOURCE",
        "TAU",
        "WEIGHT_KEYS",
        "W_CF",
        "W_CONTENT",
        "W_GAP",
        "W_GAP_POS",
        "W_NCOMP_GAP",
        "W_POP",
        "HybridRanker",
        "blend_channels",
        "state_weights",
    ]
    ```
    → 20개 이름. `uv run python -c "import millie_rec.ranking as r; print(len(r.__all__))"` → `20`.

    **3-b. 정리(이 플랜이 건드린 파일 안에서만):** `uv run ruff format src/millie_rec/ranking tests/ranking && uv run ruff check src/millie_rec/ranking tests/ranking`. 미사용 import 제거. 행동 변경 없음 — `uv run pytest tests/ranking tests/test_architecture.py --no-header` 재통과(21 passed).

    **3-c. 전역 게이트는 이 플랜에서 돌리지 않는다** — wave 1 세 플랜 종료 후 Advisor 1회(04-04-PLAN.md `<wave_gate>`).

    **3-d. SUMMARY 준비:** 변경 파일 목록(`git status --short -- src/millie_rec/ranking tests/ranking`), RED/GREEN 요약 줄 + AssertionError 발췌 3건, `wc -l`, Plan 04-04 인계용 시그니처 2개(`state_weights(user, *, reset_boost=False, session_active=False) -> dict[str, float]` · `blend_channels(user, channels: dict[str, list[Candidate]], weights: dict[str, float] | None = None, *, book_stats: BookStatsSource | None = None) -> list[ScoredItem]`) + 슬롯 상수 값, freeze 상수 표(이름·값 11개), "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/ranking tests/ranking && uv run ruff check src/millie_rec/ranking tests/ranking && uv run pytest tests/ranking tests/test_architecture.py --no-header 2>&1 | tail -3 && uv run python -c "import millie_rec.ranking as r; print(len(r.__all__))"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import millie_rec.ranking as r; print(len(r.__all__))"` → `20`; `grep -c '"state_weights"\|"blend_channels"\|"HybridRanker"\|"CHANNEL_WEIGHTS"\|"CH_CF"' src/millie_rec/ranking/__init__.py` == 5
    - `uv run ruff format --check src/millie_rec/ranking tests/ranking` 와 `uv run ruff check src/millie_rec/ranking tests/ranking` 모두 클린
    - `uv run pytest tests/ranking tests/test_architecture.py --no-header` → `21 passed`, failed 0
    - `uv run python -c "import millie_rec.ranking, sys; print('numpy' in sys.modules)"` → `False`(순수 파이썬 — 서버가 ranking 공개 표면을 import 해도 numpy 를 새로 끌어오지 않음; 이미 로드돼 있으면 True 라도 무관하므로 `-c` 단독 실행으로 확인)
    - `git log --oneline | head -1` 불변(커밋 0)
    - `git status --short -- src/millie_rec/retrieval src/millie_rec/reranking src/millie_rec/app src/millie_rec/serving src/millie_rec/data src/millie_rec/contracts.py Makefile pyproject.toml tests/test_architecture.py` → 출력 없음
  </acceptance_criteria>
  <done>공개 표면 20개, 자기 경로 ruff 클린, tests/ranking 18 + 아키텍처 3 통과. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `UserState` → `state_weights`/`blend_channels` | app/serving 이 만든 정수 튜플. 요청 문자열은 여기 닿지 않는다(`serving/api.py::_parse_seeds` 가 422 로 거른다) |
| `BookStatsSource` → `_gap_bonus` | 카탈로그 어댑터(`CatalogKR`)의 숫자 필드. None 허용 |
| `state_weights` 출력 → HTTP 응답 `user_state_weights` | 사용자에게 표시되는 혼합비. 파이프라인과 같은 함수(D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Denial of Service | `state_weights` `math.exp(-n/TAU)` | accept | `n = len(history)` 는 정수 ≥0, `exp(-n/20)` 는 (0, 1] — 오버플로 없음. 합 0 분기(`total <= 0`) 로 ZeroDivision 방지(테스트 `test_anonymous_is_all_zero…`) |
| T-04-08 | Denial of Service | `blend_channels` 후보 수 | accept | 입력은 app 이 `POOL=200` × 슬롯 3 = ≤600 후보. 파이썬 dict 연산 수천 회/요청. 상한은 app 이 잡는다 |
| T-04-09 | Tampering | `weights` dict 외부 주입 | accept | app 조립 상수(`CHANNEL_WEIGHTS`·`{cf: 1.0}`)만 들어온다. 요청 파라미터로 가중치를 받는 경로 없음(`/api/recommend?model=` 은 variant 이름만) |
| T-04-10 | Information Disclosure | `_gap_bonus` 난이도 → 응답 `difficulty` | accept | `difficulty` 는 이미 `ScoredItem`·`books_kr.json` 공개 필드(0~1 합성값, UI 는 3분위 점만). `resid_z`·`user_level` 은 응답에 넣지 않는다 |
| T-04-11 | Repudiation | 표시 혼합비 ≠ 실제 혼합비 | mitigate | `state_weights` 하나가 retriever 성분 가중(Plan 04-01 Callable)과 응답 표시(Plan 04-04 `create_app(weights=)`) 양쪽에 주입된다 — 이중 구현 없음(D-09) |
| T-04-12 | Information Disclosure | 평가 누수 | mitigate | ranking 은 `UserState`·`Candidate` 만 본다. test 라벨 접근 경로 없음. seen 방어 필터(`test_seen_candidates_are_never_returned`) |
</threat_model>

<verification>
- `uv run pytest tests/ranking tests/test_architecture.py --no-header` → `21 passed`(blend 8 + hybrid 10 + 아키텍처 3), failed 0
- `uv run ruff format --check src/millie_rec/ranking tests/ranking && uv run ruff check src/millie_rec/ranking tests/ranking` 클린
- `wc -l src/millie_rec/ranking/*.py` 전부 ≤150
- 상수 11개 이름·값이 04-CONTEXT.md D-02·D-03·D-06 과 일치(acceptance grep)
- (wave 1 종료 후 Advisor 1회) 전체 `uv run pytest --no-header`·`ruff check .`·`make smoke`
- 커밋 0
</verification>

<success_criteria>
- **REC-03** `ranking/blend.py` 가 α/β/γ 를 초기값(0.6/0.3/0.1)에서 감쇠(τ=20)·하한(0.2)·빈 성분 제거 재정규화 — 신규 유저 `{1.0, 0.0, 0.0}`(β=0), n=20 에서 α≈0.22(session 있음)/0.274(없음) 테스트 존재
- **REC-02** 의 랭커 몫: `blend_channels` 가 cf·content·pop 슬롯 min-max 가중합(W 0.5/0.3/0.2) — cf variant 는 `{cf: 1.0}` 으로 같은 함수
- **REC-04** `hybrid.py` 가 gap·gap⁺·n_completed×gap 3항을 음수 상수로 더하고 None 3경로 가중 0 — 수용 기준 '난이도 4층 피처·ablation'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표)의 피처 존재 몫(ablation 표는 설계만, D-05)
- 결정 D-01(성분 가중과 채널 가중 2단 분리)·D-02·D-03(RRF 아님)·D-06·D-08(`n_completed` 대리값 docstring) 이 코드·테스트에 존재
- `contracts.py`·형제 슬라이스 무변경, 의존성 0, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/04-freeze/04-02-SUMMARY.md` 를 만든다. 포함: RED(AssertionError 발췌 3건 이상, 요약 줄 — 예상 `15 failed, 3 passed`) · GREEN(수식 대입 값 표 — n=0/20/200 × session 유무, min-max 손계산 표) · REFACTOR(`__all__` 20·ruff·`wc -l`) · `## TDD Gate Compliance`(커밋 게이트 생략 사유·대체 증거) · **freeze 상수 표**(`ALPHA0 BETA0 GAMMA0 TAU ALPHA_FLOOR W_CF W_CONTENT W_POP W_GAP W_GAP_POS W_NCOMP_GAP` 이름·값 — 개발일지 D 항목이 인용) · Plan 04-04 인계용 시그니처 2개 · 변경 파일 목록 · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
