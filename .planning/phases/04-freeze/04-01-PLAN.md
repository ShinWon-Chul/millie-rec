---
phase: 04-freeze
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/millie_rec/retrieval/neighbors.py
  - src/millie_rec/retrieval/itemknn.py
  - src/millie_rec/retrieval/content.py
  - src/millie_rec/retrieval/__init__.py
  - tests/retrieval/test_neighbors.py
  - tests/retrieval/test_itemknn.py
  - tests/retrieval/test_content.py
autonomous: true
requirements: [REC-01]
lane: Model (retrieval 슬라이스 Worker)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05, .planning 포함 당분간 커밋 생략)
tdd_gate_evidence: pytest-output
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/ranking/**
  - src/millie_rec/reranking/**
  - src/millie_rec/app/**
  - src/millie_rec/serving/**
  - src/millie_rec/data/**
  - scripts/**
  - data/raw/**
  - data/id_map.csv
  - demo/**
  - tests/test_architecture.py
  - Makefile
  - pyproject.toml

must_haves:
  truths:
    - "ItemKNNRetriever().fit(train) 은 train 1인자만 받아 이진 user×item cosine 으로 아이템당 이웃 ≤50 을 만들고, retrieve(user, k) 는 user.seen 을 제외한 Candidate(source='itemknn') 를 점수 내림차순으로 최대 k 개 반환한다"
    - "ItemKNNRetriever 는 contracts.Neighbors 도 만족한다: neighbors(book_id, n) 이 (book_id, sim) 목록을 sim 내림차순으로 주고, 공동 소비가 없는 아이템은 [] 이다"
    - "save(path) → load(path) 라운드트립 후 retrieve 결과가 동일하고, npz 에 book_ids·nbr_ids·nbr_sims·meta(train 행 수·유저 수·아이템 수) 4 키가 있다"
    - "retrieve_from_neighbors(nbrs, user, k, source=, weights=) 하나가 Track A(ItemKNN 자기 자신)·Track B(CatalogKR.neighbors 를 감싼 NeighborRetriever) 두 경로의 유일한 이웃→후보 구현이며, weights 가 None 이면 seeds·history·session 성분 가중 전부 1.0 이다"
    - "ContentVectors.retrieve(user, k) 는 성분별 TF-IDF 평균 벡터 cosine 상위 k(seen 제외, 점수 0 제외) Candidate(source='content') 를 주고, VectorRetriever(vectors, ids) 는 같은 top-k 함수를 dense 행렬 위에서 쓴다(이중 구현 없음)"
  artifacts:
    - path: "src/millie_rec/retrieval/neighbors.py"
      provides: "SOURCE_CONTENT · DEFAULT_WEIGHTS · component_weights · weighted_items · retrieve_from_neighbors · NeighborRetriever"
      exports: ["SOURCE_CONTENT", "NeighborRetriever", "retrieve_from_neighbors", "component_weights", "weighted_items"]
      max_lines: 150
    - path: "src/millie_rec/retrieval/itemknn.py"
      provides: "ItemKNNRetriever(CandidateGenerator + Neighbors) · KNN_TOP=50 · POOL=200 · KNN_ARTIFACT · load_or_fit_itemknn"
      exports: ["ItemKNNRetriever", "KNN_ARTIFACT", "KNN_TOP", "POOL", "SOURCE_ITEMKNN", "load_or_fit_itemknn"]
      max_lines: 150
    - path: "src/millie_rec/retrieval/content.py"
      provides: "ContentVectors(ItemVectors + CandidateGenerator: name·fit·retrieve 증분) · VectorRetriever · _topk_cosine"
      exports: ["ContentVectors", "VectorRetriever"]
      max_lines: 150
    - path: "src/millie_rec/retrieval/__init__.py"
      provides: "공개 표면 __all__ 13개 이름"
      contains: "\"ItemKNNRetriever\""
    - path: "tests/retrieval/test_itemknn.py"
      provides: "손계산 4아이템 cosine · seen 제외 · fit 1인자 · npz 라운드트립 · 성분 가중 Callable"
      min_lines: 60
    - path: "tests/retrieval/test_neighbors.py"
      provides: "retrieve_from_neighbors 가짜 Neighbors 손계산 · NeighborRetriever source=content · weights None=1.0"
      min_lines: 30
    - path: "tests/retrieval/test_content.py"
      provides: "기존 4건 + retrieve seeds 벡터·seen 제외·미지 seed [] · VectorRetriever 동일 결과"
      min_lines: 70
  key_links:
    - from: "src/millie_rec/retrieval/itemknn.py"
      to: "src/millie_rec/retrieval/neighbors.py"
      via: "retrieve = retrieve_from_neighbors(self, user, k, source=SOURCE_ITEMKNN, weights=self._weights)"
      pattern: "retrieve_from_neighbors\\(self"
    - from: "src/millie_rec/retrieval/content.py"
      to: "src/millie_rec/retrieval/neighbors.py"
      via: "성분 가중 component_weights 공유(seeds α·history β·session γ)"
      pattern: "from millie_rec.retrieval.neighbors import"
    - from: "src/millie_rec/retrieval/itemknn.py"
      to: "src/millie_rec/contracts.py"
      via: "COL_USER·COL_ITEM·DIR_ARTIFACTS·Candidate·UserState 만 import(star 의존)"
      pattern: "from millie_rec.contracts import"
---

<objective>
Phase 4 '추천 파이프라인과 모델 freeze'(.planning/ROADMAP.md "Phase 4: 추천 파이프라인과 모델 freeze")의 **후보 3통로 중 남은 2통로**를 TDD 한 사이클(RED → GREEN → REFACTOR)로 만든다: `retrieval/itemknn.py`(scipy.sparse 이진 user×item cosine, 아이템당 이웃 50, npz 캐시), `retrieval/content.py` `retrieve()` 증분(성분별 TF-IDF 평균 벡터 cosine top-k) + `VectorRetriever`(Track B `VectorsKR` 용 dense 대칭), 그리고 두 트랙이 공유하는 이웃→후보 함수 `retrieval/neighbors.py`.

Purpose: 비교표 `cf`·`hybrid` 행(수용 기준 '평가 4행 + split_mode 출력'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표))의 후보 생성 단계. **REC-01** `PopularityRetriever`(✅)·`ItemKNNRetriever`·`ContentRetriever` 가 `CandidateGenerator` 계약을 만족한다. 결정 D-04(04-CONTEXT.md, 후보 통로 세부)·D-01(04-CONTEXT.md, 2단 가중치 — 성분 가중은 retriever 안에서 α/β/γ 로 합친다)·D-10(04-CONTEXT.md, Track B cf 채널 = `CatalogKR.neighbors`, content 채널 = `VectorsKR`) 의 코드 증거.
Output: `src` 3파일(신규 2 + 증분 1) + `__init__` 갱신 + `tests/retrieval/` 3파일(신규 2 + 확장 1). 커밋 0.

**성분 가중 Callable 주입(Advisor 확정 7):** retrieval 은 ranking 을 import 할 수 없으므로 α/β/γ 는 `weights: Callable[[UserState], dict[str, float]] | None` 로 받는다. `None` 이면 세 성분 전부 1.0. app(Plan 04-04)이 `ranking.state_weights` 함수 객체를 넘긴다.

**형제 플랜 불변:** `ranking/`·`reranking/`·`app/`·`serving/`·`data/` 는 읽지도 고치지도 않는다(wave 1 병렬 — 04-02·04-03 이 같은 시각에 돈다).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-freeze/04-CONTEXT.md
@.planning/phases/04-freeze/04-PATTERNS.md
@src/millie_rec/contracts.py
@src/millie_rec/retrieval/popularity.py
@src/millie_rec/retrieval/content.py
@src/millie_rec/retrieval/__init__.py
@src/millie_rec/data/vectors_kr.py
@src/millie_rec/data/catalog_kr.py
@tests/conftest.py
@tests/retrieval/test_popularity.py
@tests/retrieval/test_content.py
@tests/test_architecture.py
@../.claude/rules/python.md
@../.claude/rules/python-tdd.md
@../.claude/rules/architecture.md
@../.claude/rules/simplicity.md
@../.claude/rules/data.md

<project_rules_inline>
**실행 명령(`../.claude/rules/python-tdd.md`):** 테스트는 오직 `uv run pytest <대상> --no-header`(`-q` 는 pyproject `addopts` 에 이미 있음 — 붙이면 `-qq` 가 되어 요약 줄이 사라진다). 맨 `pytest`·`python -m pytest`·`python3` 금지(이 머신의 맨 pytest 는 Python 3.9 → collection error). **RED 는 출력에 `AssertionError`(또는 `E  assert`) 가 있을 때만 인정.** `ImportError`·`ModuleNotFoundError`·`SyntaxError`·`TypeError` 는 RED 가 아니다 → 시그니처만 있는 스텁을 먼저 둔다. `pytest.skip` 금지. `tests/test_architecture.py`·기존 테스트 단정 수정 금지(이 플랜은 `test_content.py` 에 **추가만** 한다).

**star 의존(`../.claude/rules/architecture.md`, `tests/test_architecture.py` L37-42):** `retrieval/*.py` 는 `millie_rec.contracts` 와 `millie_rec.retrieval.<module>` 만 import. `ranking`·`reranking`·`data`·`evaluation`·`serving`·`app` import 금지. 다른 슬라이스의 결과가 필요하면 `contracts.py` 의 Protocol(`Neighbors`·`ItemVectors`)로 받고 app 이 주입한다. "`artifacts/` | retrieval | `fit`/`load`는 retrieval 안에서만."

**지연 import(04-PATTERNS.md §0):** pandas 는 `TYPE_CHECKING` 아래, **scipy·sklearn 은 함수 안에서 import**(서버가 `millie_rec.retrieval` 공개 표면을 import 해도 로드되지 않게 — `app/server.py` → `app/pipeline.py` → `from millie_rec.retrieval import …` 경로가 실제로 있다). numpy 는 모듈 상단 허용(`content.py` 관례).

**이름·난수·경로(`../.claude/rules/python.md`):** 컬럼은 `contracts.COL_USER`·`COL_ITEM` 상수만. 경로는 `DIR_ARTIFACTS` 에서 시작. "행렬 연산은 numpy/scipy.sparse. 파이썬 루프로 유저×아이템을 돌지 않는다." — Item-KNN fit 은 chunked 행렬곱, retrieve 의 "사용자 아이템 × 이웃 50" 루프는 유저 1명 내부 루프라 허용.

**간결함(`../.claude/rules/simplicity.md`):** 파일 ≤150줄. 1줄 한국어 docstring + 정본 절 참조. 설정은 모듈 상단 대문자 상수 + `#` 주석에 근거(D-xx). "함수 우선. 클래스는 `contracts.py`의 Protocol을 만족시킬 때만." 예외 계층·재시도 데코레이터 금지. "돌아가는 Item-KNN > 완벽한 ALS." 이 상수들은 **freeze 대상(D-14 ①, 04-CONTEXT.md)** — 이름을 바꾸지 않는다: `KNN_TOP=50 POOL=200 CHUNK=500 SOURCE_ITEMKNN="itemknn" SOURCE_CONTENT="content"`.

**표기(`../.claude/rules/data.md`):** "Track B에는 유저 로그가 없다. 데모 variant `cf`·`hybrid`의 이웃 채널은 content_sim이며 `Candidate.source`·`ScoredItem.source_channels`는 **`content`**로 표기한다. `itemknn`이라 쓰지 않는다." → `NeighborRetriever` 기본 `source=SOURCE_CONTENT`, Track A `ItemKNNRetriever` 만 `itemknn`.

**테스트 3종(`../.claude/rules/python-tdd.md`):** ① 계약(Candidate 타입·source·정렬·`name`) ② 정확성(손계산 소형 예제) ③ 안전성(fit 1인자·seen 제외·라운드트립·빈 입력). 데이터는 `tests/conftest.py::interactions`(유저 20×아이템 50) 또는 테스트 안 소형 DataFrame. 기대값은 구현 상수를 import 하지 않고 손으로 적는다.

**완료 기준(자기 경로만 — wave 병렬):** `uv run ruff format --check src/millie_rec/retrieval tests/retrieval && uv run ruff check src/millie_rec/retrieval tests/retrieval` 클린 · `uv run pytest tests/retrieval tests/test_architecture.py --no-header` failed 0. 전체 트리 `uv run pytest --no-header`·`make smoke` 는 **wave 1 종료 후 Advisor 가 1회**(04-04-PLAN.md `<wave_gate>`). 기준선 255 passed / 1 failed(`test_coverage_gate` — 재수집 중 의도된 RED, Phase 3 몫) 은 이 플랜의 실패로 세지 않는다.
</project_rules_inline>

<interfaces>
<!-- 실행자가 그대로 쓰는 계약. 재탐색 불필요. contracts.py 는 변경 금지. -->

From src/millie_rec/contracts.py (verbatim 발췌):
```python
COL_USER = "user_id"; COL_ITEM = "book_id"
DIR_ARTIFACTS = ROOT / "artifacts"
K_RECALL = 20; K_RANK = 10

@dataclass(frozen=True, slots=True)
class UserState:
    user_id: int | None
    explicit_seeds: tuple[int, ...] = ()
    history: tuple[int, ...] = ()
    session: tuple[int, ...] = ()
    context: dict[str, str] = field(default_factory=dict)
    @property
    def seen(self) -> frozenset[int]: ...   # explicit_seeds ∪ history ∪ session

@dataclass(frozen=True, slots=True)
class Candidate:
    book_id: int
    source: str  # "popularity" | "itemknn" | "content" | "explicit"
    score: float

class CandidateGenerator(Protocol):
    name: str
    def fit(self, train: "pd.DataFrame") -> "CandidateGenerator": ...
    def retrieve(self, user: UserState, k: int) -> list[Candidate]: ...

class ItemVectors(Protocol):
    def vectors(self, book_ids: Sequence[int]) -> "np.ndarray": ...

class Neighbors(Protocol):
    def neighbors(self, book_id: int, n: int = 20) -> list[tuple[int, float]]: ...
```

From src/millie_rec/retrieval/popularity.py (아날로그 — 4메서드 형태·seen 제외 루프·`name` 클래스 속성):
```python
class PopularityRetriever:
    name = "pop"
    def __init__(self, ranked: Sequence[tuple[int, float]] = ()) -> None: ...
    def fit(self, train: "pd.DataFrame") -> "PopularityRetriever": ...   # inspect.signature == ["self", "train"]
    def retrieve(self, user: UserState, k: int) -> list[Candidate]: ...  # seen 건너뛰고 k 에서 멈춤
    def save(self, path: Path = POP_ARTIFACT, *, top_n: int = TOP_N) -> Path: ...
    @classmethod
    def load(cls, path: Path = POP_ARTIFACT) -> "PopularityRetriever": ...
```

From src/millie_rec/retrieval/content.py (현재 42줄, 그대로 유지 + 증분):
```python
TAGS_COL = "tags"; TOKEN_PATTERN = r"\S+"
class ContentVectors:
    def __init__(self, books: "pd.DataFrame", text_col: str = TAGS_COL) -> None:
        self._matrix = vec.fit_transform(texts)   # scipy CSR (n_books, dim), 행 L2=1(빈 문서 0)
        self._index = {int(b): i for i, b in enumerate(books[COL_ITEM].tolist())}
    @property
    def dim(self) -> int: ...
    def vectors(self, book_ids: Sequence[int]) -> np.ndarray: ...  # 미지 id → 0 벡터
```

From src/millie_rec/data/catalog_kr.py L89-91 (Track B Neighbors 구현 — 읽기만, Plan 04-04 가 주입):
```python
    def neighbors(self, book_id: int, n: int = 20) -> list[tuple[int, float]]:
        edges = self._edges.get(str(int(book_id)), [])
        return [(int(dst), float(w)) for dst, w, _source in edges[:n]]
```

From src/millie_rec/data/vectors_kr.py L23-26 (npz 로드 아날로그):
```python
        with np.load(path) as z:  # 파일 핸들을 닫는다
            return cls(z["book_ids"], z["vectors"])
```
</interfaces>
</context>

<feature>
  <name>후보 3통로 완성 — Item-KNN(train 이진 cosine, 이웃 50, npz 캐시) + Content retrieve(성분별 TF-IDF 평균 벡터) + 두 트랙 공용 이웃→후보 함수</name>
  <files>
    src/millie_rec/retrieval/neighbors.py, src/millie_rec/retrieval/itemknn.py, src/millie_rec/retrieval/content.py, src/millie_rec/retrieval/__init__.py,
    tests/retrieval/test_neighbors.py, tests/retrieval/test_itemknn.py, tests/retrieval/test_content.py
  </files>
  <behavior>
    **neighbors.py** (가짜 `_Nbrs`: `{1: [(2, 1.0), (3, 0.5)], 2: [(1, 1.0), (3, 0.5)], 3: [(1, 0.5), (2, 0.5)], 4: []}`)
    - `component_weights(UserState(None, explicit_seeds=(1,)), None)` → `{"alpha": 1.0, "beta": 1.0, "gamma": 1.0}`; `weights=lambda u: {"alpha": 0.5, "beta": 0.0, "gamma": 0.0}` 이면 그 dict 그대로
    - `weighted_items(UserState(None, explicit_seeds=(1,), history=(2, 3), session=(4,)), {"alpha": 1.0, "beta": 0.5, "gamma": 0.1})` → `[(1, 1.0), (2, 0.5), (3, 0.5), (4, 0.1)]`
    - `retrieve_from_neighbors(_Nbrs(), UserState(None, explicit_seeds=(1,)), k=5, source="content")` → book_id `[2, 3]`, score `[1.0, 0.5]`, 전부 `Candidate`·`source == "content"`
    - seeds `(1,)` + history `(2,)` (weights None → β=1.0): 3 의 점수 = 0.5 + 0.5 = **1.0**, 1·2 는 seen 이라 없음 → `[(3, 1.0)]`
    - 같은 입력에 `weights=lambda u: {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}` → 3 의 점수 **0.5**(history 성분 0)
    - `k=1` → 길이 1; seeds 만 `(4,)`(이웃 없음) → `[]`; 익명 `UserState(None)` → `[]`
    - `NeighborRetriever(_Nbrs()).retrieve(UserState(None, explicit_seeds=(1,)), 5)` 가 위와 같은 결과이고 `NeighborRetriever.name == "content"`, `NeighborRetriever(_Nbrs(), source="itemknn")` 는 `source == "itemknn"`

    **itemknn.py** (손계산 `_train()`: u1={1,2,3}, u2={1,2}, u3={4}, rating 1~5 혼합 6행 → 아이템 벡터 1:{u1,u2} 2:{u1,u2} 3:{u1} 4:{u3}; cos(1,2)=1.0, cos(1,3)=cos(2,3)=1/√2≈0.7071, 4 는 이웃 없음)
    - `ItemKNNRetriever().fit(_train()).neighbors(1, n=5)` → `[(2, ≈1.0), (3, ≈0.7071)]`(sim 내림차순, 자기 자신 없음); `neighbors(4)` → `[]`; `neighbors(99)`(미지) → `[]`
    - `retrieve(UserState(None, explicit_seeds=(1,)), k=5)` → book_id `[2, 3]`, score `[≈1.0, ≈0.7071]`, `source == "itemknn"`, `Candidate` 타입; `ItemKNNRetriever.name == "cf"`
    - `retrieve(UserState(None, explicit_seeds=(1,), history=(2,)), k=5)` → `[3]` 점수 ≈1.4142(0.7071+0.7071); `weights=lambda u: {"alpha": 1, "beta": 0, "gamma": 0}` 주입 시 ≈0.7071
    - `retrieve(UserState(None, history=(1, 2, 3, 4)), k=5)` → `[]`(전부 seen)
    - `inspect.signature(ItemKNNRetriever.fit).parameters` 키 == `["self", "train"]`
    - `save(tmp/"knn.npz")` 후 `np.load` 키 집합 == `{"book_ids", "nbr_ids", "nbr_sims", "meta"}`, `meta.tolist() == [6, 3, 4]`(행 6·유저 3·아이템 4), `nbr_ids.shape == (4, 50)`, `load(path).retrieve(seeds (1,), 5) == 원본.retrieve(...)`
    - `load_or_fit_itemknn(_train(), tmp/"knn.npz")` 첫 호출은 파일을 만들고, 두 번째 호출은 `fit` 을 부르지 않는다(monkeypatch 로 `ItemKNNRetriever.fit` 이 호출되면 `AssertionError`); train 행을 1개 지운 프레임(meta 불일치)으로 세 번째 호출하면 다시 fit 한다; `path=None` 이면 파일을 쓰지 않는다
    - fixture `interactions`(20×50): `retrieve(UserState(None, explicit_seeds=(0,)), k=20)` 길이 ≤ 20, 점수 내림차순, seen(0) 없음

    **content.py** (기존 `_books()`: 10 "fantasy magic" · 20 "magic romance" · 30 "history war")
    - `ContentVectors(_books()).retrieve(UserState(None, explicit_seeds=(10,)), k=2)` → book_id `[20]`(30 은 직교 → 점수 0 → 제외), `source == "content"`, `0 < score ≤ 1.0`; 10 은 seen 이라 없음
    - `retrieve(UserState(None, explicit_seeds=(99,)), 2)`(미지 seed 만) → `[]`; `retrieve(UserState(None), 2)`(성분 전부 비어 있음) → `[]`
    - `ContentVectors.name == "content"`; `cv.fit(pd.DataFrame()) is cv`(no-op, 하네스 대칭)
    - `ContentVectors(_books(), weights=lambda u: {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}).retrieve(UserState(None, explicit_seeds=(30,), history=(10,)), 2)` → 30 과 10 은 seen, 후보 없음 → `[]`; 같은 weights 로 `explicit_seeds=(10,)`, `history=(30,)` → `[20]`(history 성분 가중 0 이라 30 의 벡터가 q 에 안 들어가고, 20 은 10 과 magic 공유)
    - `VectorRetriever(cv, ids=[10, 20, 30]).retrieve(UserState(None, explicit_seeds=(10,)), 2)` 가 `cv.retrieve(...)` 와 book_id·score 동일(`pytest.approx`); `VectorRetriever.name == "content"`; `VectorRetriever(cv, ids=[])` → `retrieve` 는 `[]`
    - 기존 4건(`test_vectors_*`·`test_unknown_id_*`) 은 **그대로 통과**
  </behavior>
  <implementation>Task 2 action 에 파일별 상수·시그니처·알고리즘이 있다. 그대로 쓴다.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): retrieval 스텁 2파일 + content 스텁 증분 + 테스트 3파일 → 전 신규 테스트 AssertionError</name>
  <files>src/millie_rec/retrieval/neighbors.py, src/millie_rec/retrieval/itemknn.py, src/millie_rec/retrieval/content.py, tests/retrieval/test_neighbors.py, tests/retrieval/test_itemknn.py, tests/retrieval/test_content.py</files>
  <read_first>
    - src/millie_rec/contracts.py L122-135(`UserState.seen`), L211-215(`Candidate`), L289-295(`CandidateGenerator`), L310-313(`ItemVectors`), L324-331(`Neighbors`)
    - src/millie_rec/retrieval/popularity.py 전문(4메서드 형태·`name`·seen 루프·save/load)
    - src/millie_rec/retrieval/content.py 전문(현재 42줄 — `_matrix`·`_index` 를 그대로 쓴다)
    - tests/retrieval/test_popularity.py 전문(`_train()` 손계산·`# ── 계약 ──` 구분 주석·`inspect.signature` L61)
    - tests/retrieval/test_content.py 전문(`_books()` L12-18 — 확장 대상, 기존 4건 수정 금지)
    - tests/conftest.py L21-43(`interactions` 20×50 · `item_vectors`)
    - .planning/phases/04-freeze/04-PATTERNS.md "itemknn.py" 절 Analog A·B·C + "No Analog" 스니펫 A, "content.py" 절
    - .planning/phases/04-freeze/04-CONTEXT.md D-01·D-04·D-10, Claude's Discretion "Item-KNN 세부"·"성분별 점수 계산 방식"·"이웃 npz 캐시 무효화"
    - ../.claude/rules/python-tdd.md 전문 · ../.claude/rules/data.md "두 트랙" 절
  </read_first>
  <action>
    **왜 스텁인가:** 모듈이 없으면 `ModuleNotFoundError`, 메서드가 없으면 `AttributeError` 로 죽어 RED 가 아니다. 시그니처는 Task 2 와 동일하고 값만 틀린 스텁을 두어 **모든 신규 테스트가 `AssertionError` 로 실패**하는 정직한 RED 를 만든다.

    **1-a. `src/millie_rec/retrieval/neighbors.py` 스텁** — 1줄 docstring, Task 2 와 같은 import·상수·시그니처:
    ```python
    SOURCE_CONTENT = "content"
    DEFAULT_WEIGHTS = {"alpha": 1.0, "beta": 1.0, "gamma": 1.0}
    WeightFn = Callable[[UserState], dict[str, float]]
    def component_weights(user, weights) -> dict[str, float]: return {}
    def weighted_items(user, w) -> list[tuple[int, float]]: return []
    def retrieve_from_neighbors(nbrs, user, k, *, source=SOURCE_CONTENT, weights=None, n_neighbors=20) -> list[Candidate]: return []
    class NeighborRetriever:
        name = "neighbor"   # 틀린 값 — 테스트가 "content" 를 단정
        def __init__(self, neighbors, *, source=SOURCE_CONTENT, weights=None) -> None: ...
        def fit(self, train): return self
        def retrieve(self, user, k) -> list[Candidate]: return []
    ```
    **1-b. `src/millie_rec/retrieval/itemknn.py` 스텁** — 상수 `SOURCE_ITEMKNN = "itemknn"`, `KNN_TOP = 50`, `POOL = 200`, `CHUNK = 500`, `ARTIFACT_NAME = "item_neighbors.npz"`, `KNN_ARTIFACT = DIR_ARTIFACTS / ARTIFACT_NAME`; `class ItemKNNRetriever` 에 `name = "cf"`, `__init__(self, book_ids=None, nbr_ids=None, nbr_sims=None, *, meta=None, weights=None)`, `fit(self, train)` 은 `return self`(테이블을 만들지 않음), `neighbors(self, book_id, n=KNN_TOP)` 는 `[]`, `retrieve(self, user, k)` 는 `[]`, `meta` 프로퍼티는 `(0, 0, 0)`, `save(self, path=KNN_ARTIFACT)` 는 빈 npz(`np.savez(path, book_ids=np.zeros(0, dtype=np.int64))`) 후 `path` 반환, `load(cls, path=KNN_ARTIFACT, *, weights=None)` 는 `cls()`; 모듈 함수 `load_or_fit_itemknn(train, path=None, *, weights=None)` 는 `ItemKNNRetriever()`(fit 안 함).
    **1-c. `src/millie_rec/retrieval/content.py` 스텁 증분** — 기존 코드는 **한 글자도 바꾸지 않고** 아래만 추가: `ContentVectors.name = "content"` 클래스 속성, `__init__` 에 keyword-only `weights: WeightFn | None = None` 추가(`self._weights = weights` 저장만 — 기존 위치 인자 `books, text_col` 불변), `fit(self, train): return self`, `retrieve(self, user, k) -> list[Candidate]: return []`; `class VectorRetriever` 에 `name = "vector"`(틀린 값), `__init__(self, vectors, ids, *, source=SOURCE_CONTENT, weights=None)`, `fit` no-op, `retrieve` 는 `[]`. `from millie_rec.retrieval.neighbors import SOURCE_CONTENT, WeightFn, component_weights` 를 import 블록에 둔다(스텁 단계에선 `component_weights` 미사용이라 ruff F401 이 뜬다 — Task 2 에서 쓰이므로 RED 단계에선 무시하고, `ruff check` 는 Task 3 에서 본다).

    **1-d. `tests/retrieval/test_neighbors.py`** (6건) — 상단에 가짜:
    ```python
    class _Nbrs:
        """contracts.Neighbors 가짜 — 상속 없이 메서드만(tests/evaluation/test_harness.py::_OneHot 관례)."""
        TABLE = {1: [(2, 1.0), (3, 0.5)], 2: [(1, 1.0), (3, 0.5)], 3: [(1, 0.5), (2, 0.5)], 4: []}
        def neighbors(self, book_id: int, n: int = 20) -> list[tuple[int, float]]:
            return self.TABLE.get(int(book_id), [])[:n]
    ```
    - `# ── 계약 ──` `test_retrieve_from_neighbors_returns_content_candidates_sorted` — seeds (1,) k=5 → `[c.book_id for c in out] == [2, 3]`, `[c.score for c in out] == [1.0, 0.5]`, `all(isinstance(c, Candidate) and c.source == "content" for c in out)`
    - `test_neighbor_retriever_name_and_source` — `NeighborRetriever.name == "content"`; `NeighborRetriever(_Nbrs()).retrieve(seeds (1,), 5)` 의 book_id `== [2, 3]`; `NeighborRetriever(_Nbrs(), source="itemknn").retrieve(...)[0].source == "itemknn"`
    - `# ── 정확성 ──` `test_history_component_adds_to_score_when_weights_none` — seeds (1,) history (2,) → `[(3, 1.0)]`(`pytest.approx`); 1·2 없음
    - `test_weights_callable_scales_components` — 같은 유저, `weights=lambda u: {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}` → 3 의 score ≈ 0.5; `component_weights(UserState(None), None) == {"alpha": 1.0, "beta": 1.0, "gamma": 1.0}`; `weighted_items(UserState(None, explicit_seeds=(1,), history=(2, 3), session=(4,)), {"alpha": 1.0, "beta": 0.5, "gamma": 0.1}) == [(1, 1.0), (2, 0.5), (3, 0.5), (4, 0.1)]`
    - `# ── 안전성 ──` `test_k_limits_and_no_neighbors_gives_empty` — k=1 → 길이 1; seeds (4,) → `[]`; `UserState(None)` → `[]`
    - `test_seen_never_returned` — `UserState(None, explicit_seeds=(1,), history=(2, 3))` → `[]`(3 이웃은 1·2 뿐이고 전부 seen)

    **1-e. `tests/retrieval/test_itemknn.py`** (8건) — `_train()`:
    ```python
    def _train() -> pd.DataFrame:
        """u1={1,2,3} u2={1,2} u3={4}. cos(1,2)=1, cos(1,3)=cos(2,3)=1/√2, 4 는 이웃 없음. 평점 혼합 = '평점 무관'."""
        return pd.DataFrame(
            {COL_USER: [1, 1, 1, 2, 2, 3], COL_ITEM: [1, 2, 3, 1, 2, 4], COL_RATING: [5.0, 1.0, 3.0, 2.0, 4.0, 5.0]}
        )
    ```
    - `# ── 계약 ──` `test_retrieve_returns_itemknn_candidates_sorted_desc` — `r = ItemKNNRetriever().fit(_train())`; seeds (1,) k=5 → book_id `[2, 3]`, `score == pytest.approx([1.0, 0.7071], abs=1e-3)`, `all(isinstance(c, Candidate) and c.source == "itemknn")`, `ItemKNNRetriever.name == "cf"`
    - `test_neighbors_protocol_sorted_and_self_excluded` — `r.neighbors(1, n=5)` 의 id `[2, 3]`, sim `approx([1.0, 0.7071], abs=1e-3)`; `r.neighbors(4) == []`; `r.neighbors(99) == []`; `r.neighbors(1, n=1)` 길이 1
    - `# ── 정확성 ──` `test_history_component_sums_neighbor_similarity` — seeds (1,) history (2,) → `[3]`, score ≈ 1.4142(abs 1e-3)
    - `test_weights_callable_zeroes_history_component` — 위와 같은 유저, `ItemKNNRetriever(weights=lambda u: {"alpha": 1.0, "beta": 0.0, "gamma": 0.0}).fit(_train())` → 3 의 score ≈ 0.7071
    - `test_retrieve_on_fixture_is_sorted_and_excludes_seen(interactions)` — seeds (0,) k=20 → `len(out) <= 20`, 점수 내림차순, `0 not in ids`
    - `# ── 안전성 ──` `test_fit_takes_only_train_and_seen_items_never_returned` — `list(inspect.signature(ItemKNNRetriever.fit).parameters) == ["self", "train"]`; `r.retrieve(UserState(None, history=(1, 2, 3, 4)), k=5) == []`
    - `test_save_load_roundtrip_keys_and_meta(tmp_path)` — `p = r.save(tmp_path / "knn.npz")`; `with np.load(p) as z: assert set(z.files) == {"book_ids", "nbr_ids", "nbr_sims", "meta"}; assert z["meta"].tolist() == [6, 3, 4]; assert z["nbr_ids"].shape == (4, 50)`; `ItemKNNRetriever.load(p).retrieve(UserState(None, explicit_seeds=(1,)), 5) == r.retrieve(UserState(None, explicit_seeds=(1,)), 5)`
    - `test_load_or_fit_uses_cache_only_when_meta_matches(tmp_path, monkeypatch)` — `p = tmp_path / "knn.npz"`; 1회 `load_or_fit_itemknn(_train(), p)` → `p.exists()`; `monkeypatch.setattr(ItemKNNRetriever, "fit", lambda self, train: (_ for _ in ()).throw(AssertionError("fit must not run")))`; 2회 `load_or_fit_itemknn(_train(), p).retrieve(seeds (1,), 5)` 의 book_id `== [2, 3]`(캐시 로드); `monkeypatch.undo()`; 3회 `load_or_fit_itemknn(_train().iloc[:-1], p)` 는 `meta == (5, 2, 3)`(재fit — 마지막 행 u3·4 가 빠져 행 5·유저 2·아이템 3); `load_or_fit_itemknn(_train(), None)` 뒤 `tmp_path` 에 새 파일 없음
    (스텁에서 `.throw(AssertionError)` 는 호출되지 않으니 이 테스트는 `p.exists()` 단정에서 AssertionError 로 실패한다.)

    **1-f. `tests/retrieval/test_content.py` 확장** (기존 4건 유지 + 6건 추가, `from millie_rec.contracts import COL_ITEM, Candidate, UserState`, `from millie_rec.retrieval.content import ContentVectors, VectorRetriever` 로 import 갱신 — 기존 import 줄을 **확장**만):
    - `# ── retrieve 계약 ──` `test_retrieve_returns_content_candidates_for_seed` — `ContentVectors(_books()).retrieve(UserState(None, explicit_seeds=(10,)), k=2)` → book_id `[20]`, `0.0 < out[0].score <= 1.0 + 1e-9`, `isinstance Candidate`, `source == "content"`, `ContentVectors.name == "content"`
    - `test_fit_is_noop_returning_self` — `cv = ContentVectors(_books()); assert cv.fit(pd.DataFrame()) is cv`
    - `# ── retrieve 정확성 ──` `test_retrieve_weights_zero_history_component` — `cv = ContentVectors(_books(), weights=lambda u: {"alpha": 1.0, "beta": 0.0, "gamma": 0.0})`; `cv.retrieve(UserState(None, explicit_seeds=(10,), history=(30,)), 2)` 의 book_id `== [20]`; 기본 weights(None) 로 같은 유저면 30 의 벡터(history)가 q 에 들어가지만 30 은 seen → 결과는 여전히 `[20]` 이고 **두 점수가 같다**(20 은 history/war 와 직교 → q 의 history 성분이 20 점수에 기여 0) → `pytest.approx` 로 동일 단정
    - `test_vector_retriever_matches_content_retrieve` — `vr = VectorRetriever(cv, ids=[10, 20, 30])`; `a = cv.retrieve(u, 2)`, `b = vr.retrieve(u, 2)`(u = seeds (10,)); `[c.book_id for c in a] == [c.book_id for c in b]`; `[c.score for c in a] == pytest.approx([c.score for c in b])`; `VectorRetriever.name == "content"`
    - `# ── retrieve 안전성 ──` `test_retrieve_unknown_seed_or_empty_user_gives_empty` — seeds (99,) → `[]`; `UserState(None)` → `[]`; `VectorRetriever(cv, ids=[]).retrieve(seeds (10,), 2) == []`
    - `test_retrieve_never_returns_seen` — `cv.retrieve(UserState(None, explicit_seeds=(10,), history=(20,)), 3)` 의 book_id 집합 ∩ {10, 20} == ∅

    **1-g. RED 확인:** `uv run pytest tests/retrieval/test_neighbors.py tests/retrieval/test_itemknn.py tests/retrieval/test_content.py --no-header 2>&1 | tail -40`. 기대: 신규 20건 전부 failed, 사유는 `AssertionError`/`E  assert` 만; 기존 `test_content.py` 4건은 passed. `ImportError`·`AttributeError`·`TypeError` 가 보이면 스텁 시그니처를 고친다(테스트를 고치지 않는다). `uv run pytest tests/test_architecture.py --no-header` → `3 passed`(스텁 import 경계).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/retrieval/test_neighbors.py tests/retrieval/test_itemknn.py tests/retrieval/test_content.py --no-header 2>&1 | tail -30; uv run pytest tests/test_architecture.py --no-header</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/millie_rec/retrieval/neighbors.py src/millie_rec/retrieval/itemknn.py tests/retrieval/test_neighbors.py tests/retrieval/test_itemknn.py` 4파일 존재
    - `grep -c "def test_" tests/retrieval/test_neighbors.py tests/retrieval/test_itemknn.py tests/retrieval/test_content.py` → 6, 8, 10
    - RED 출력에 `AssertionError` 또는 `E  assert` 가 있고 `ImportError`·`ModuleNotFoundError`·`AttributeError`·`TypeError`·`SyntaxError` 가 없다(요약 줄 + 발췌를 SUMMARY 에)
    - RED 요약 줄에 신규 테스트의 **대다수가 failed**(checker 재계산: 스텁 `[]`·`fit(self, train)` 시그니처로 `test_retrieve_unknown_seed_or_empty_user_gives_empty`·`test_fit_takes_only_train_and_seen_items_never_returned` 2건은 스텁에서도 통과 → 예상 `18 failed, 6 passed`; 정확한 숫자보다 "failed 사유가 전부 AssertionError 계열" 이 판정 기준), 기존 content 4건 통과 유지
    - `uv run pytest tests/test_architecture.py --no-header` → `3 passed`
    - `grep -rn "millie_rec\.\(ranking\|reranking\|data\|evaluation\|serving\|app\)" src/millie_rec/retrieval/` → 출력 없음
    - `grep -n 'name = "cf"' src/millie_rec/retrieval/itemknn.py` 1행 · `grep -n '^SOURCE_ITEMKNN = "itemknn"' src/millie_rec/retrieval/itemknn.py` 1행 · `grep -n '^SOURCE_CONTENT = "content"' src/millie_rec/retrieval/neighbors.py` 1행
    - `git diff --stat -- src/millie_rec/ranking src/millie_rec/reranking src/millie_rec/app src/millie_rec/serving src/millie_rec/data src/millie_rec/contracts.py` → 출력 없음
  </acceptance_criteria>
  <done>신규 20건 전부 AssertionError 계열 RED, 기존 content 4건·아키텍처 3건 passed. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): neighbors·itemknn·content retrieve 구현 → tests/retrieval 전부 passed</name>
  <files>src/millie_rec/retrieval/neighbors.py, src/millie_rec/retrieval/itemknn.py, src/millie_rec/retrieval/content.py</files>
  <read_first>
    - Task 1 이 만든 스텁 3파일과 테스트 3파일(단정이 요구하는 정확한 값)
    - .planning/phases/04-freeze/04-PATTERNS.md "No Analog" 스니펫 A(chunked top-k) 전문 · "itemknn.py" Analog B(npz)·C(Neighbors 대칭) · "content.py" 증분 절 · "Shared Patterns" '0-벡터 안전 cosine'·'seen 제외 + 넉넉히 뽑기'
    - src/millie_rec/evaluation/metrics.py L28-38(`ild_at_k` — 0-벡터 안전 정규화 4줄, 읽기만)
    - src/millie_rec/retrieval/content.py L23-30(`_matrix` CSR·`_index`)
  </read_first>
  <action>
    각 파일 ≤150줄, 줄 ≤100자(ruff E501). 모듈 머리 = 1줄 docstring(무엇 — 계약 Protocol 이름(근거 결정)) → stdlib → 서드파티(numpy 만 상단) → `millie_rec.contracts`/자기 슬라이스. **scipy 는 `fit` 안에서 import.**

    **2-a. `src/millie_rec/retrieval/neighbors.py`** — docstring `"""이웃 → 후보 공용 함수 + NeighborRetriever(contracts.CandidateGenerator). Track A ItemKNN·Track B CatalogKR.neighbors 가 같은 함수를 쓴다(D-10, 이중 구현 금지)."""`
    ```python
    from collections.abc import Callable
    from typing import TYPE_CHECKING

    from millie_rec.contracts import Candidate, Neighbors, UserState

    if TYPE_CHECKING:
        import pandas as pd

    SOURCE_CONTENT = "content"  # data.md: Track B 이웃 채널 표기. serving/app 과 중복 정의(중복 < 결합)
    DEFAULT_WEIGHTS = {"alpha": 1.0, "beta": 1.0, "gamma": 1.0}  # weights 미주입 = 성분 가중 전부 1
    N_NEIGHBORS = 20  # 성분 아이템당 읽는 이웃 수 기본값(contracts.Neighbors.n 기본과 동일)
    WeightFn = Callable[[UserState], dict[str, float]]  # app 이 ranking.state_weights 를 넘긴다(D-01·D-09)


    def component_weights(user: UserState, weights: WeightFn | None) -> dict[str, float]:
        """α/β/γ dict. None 이면 DEFAULT_WEIGHTS 복사 — retrieval 은 ranking 을 import 하지 않는다."""
        return dict(weights(user)) if weights is not None else dict(DEFAULT_WEIGHTS)


    def weighted_items(user: UserState, w: dict[str, float]) -> list[tuple[int, float]]:
        """(book_id, α|β|γ) — seeds→alpha, history→beta, session→gamma. 비어 있는 성분은 자연히 없다."""
        return (
            [(int(b), w.get("alpha", 0.0)) for b in user.explicit_seeds]
            + [(int(b), w.get("beta", 0.0)) for b in user.history]
            + [(int(b), w.get("gamma", 0.0)) for b in user.session]
        )


    def retrieve_from_neighbors(
        nbrs: Neighbors, user: UserState, k: int, *,
        source: str = SOURCE_CONTENT, weights: WeightFn | None = None, n_neighbors: int = N_NEIGHBORS,
    ) -> list[Candidate]:
        """Σ_{i∈성분 아이템} w_성분 · sim(i, ·), seen 제외, (−score, book_id) 정렬 상위 k."""
        scores: dict[int, float] = {}
        for b, wc in weighted_items(user, component_weights(user, weights)):
            if wc == 0.0:
                continue
            for nb, sim in nbrs.neighbors(b, n_neighbors):
                if nb in user.seen:
                    continue
                scores[nb] = scores.get(nb, 0.0) + wc * float(sim)
        ranked = sorted(scores.items(), key=lambda t: (-t[1], t[0]))[:k]  # 동률은 id 오름차순(재현성)
        return [Candidate(book_id=int(b), source=source, score=float(s)) for b, s in ranked]


    class NeighborRetriever:
        """Neighbors 어댑터를 감싼 CandidateGenerator. Track B cf 채널(CatalogKR 엣지) 용, source=content."""

        name = SOURCE_CONTENT

        def __init__(self, neighbors: Neighbors, *, source: str = SOURCE_CONTENT,
                     weights: WeightFn | None = None, n_neighbors: int = N_NEIGHBORS) -> None:
            self._nbrs, self._source, self._weights, self._n = neighbors, source, weights, n_neighbors

        def fit(self, train: "pd.DataFrame") -> "NeighborRetriever":
            return self  # 이웃은 주입된 어댑터가 이미 가진다(빌드는 scripts/ 또는 ItemKNN.fit)

        def retrieve(self, user: UserState, k: int) -> list[Candidate]:
            return retrieve_from_neighbors(
                self._nbrs, user, k, source=self._source, weights=self._weights, n_neighbors=self._n
            )
    ```
    `n_neighbors` 는 ItemKNN 이 `KNN_TOP`(50) 으로 넘긴다(자기 테이블 전부). Track B 는 기본 20(엣지 top-20 + category_best).

    **2-b. `src/millie_rec/retrieval/itemknn.py`** — docstring `"""cf 후보 — contracts.CandidateGenerator + Neighbors 구현. train 이진 user×item cosine, 아이템당 이웃 KNN_TOP, npz 캐시(D-04)."""`
    ```python
    import logging
    from pathlib import Path
    from typing import TYPE_CHECKING

    import numpy as np

    from millie_rec.contracts import COL_ITEM, COL_USER, DIR_ARTIFACTS, Candidate, UserState
    from millie_rec.retrieval.neighbors import WeightFn, retrieve_from_neighbors

    if TYPE_CHECKING:
        import pandas as pd

    log = logging.getLogger(__name__)
    SOURCE_ITEMKNN = "itemknn"  # contracts.Candidate.source 허용값. Track A 전용(data.md — Track B 는 content)
    KNN_TOP = 50  # D-04 아이템당 이웃. freeze(D-14 ①)
    POOL = 200  # D-04 채널별 후보 풀. app StagedPipeline 이 retrieve(user, POOL) 로 부른다
    CHUNK = 500  # (CHUNK × n_items) dense 한 장 = 500×10k×8B = 40MB — dense 10k×10k 금지(아키 §8 리스크)
    ARTIFACT_NAME = "item_neighbors.npz"
    KNN_ARTIFACT = DIR_ARTIFACTS / ARTIFACT_NAME  # artifacts/* 는 gitignore(artifacts/serving 만 예외)
    ```
    - `def _meta(train) -> tuple[int, int, int]`: `(len(train), int(train[COL_USER].nunique()), int(train[COL_ITEM].nunique()))` — 캐시 무효화 키(Claude's Discretion: 파일 존재만 보면 필터 상수 변경 후 옛 이웃을 쓰게 되어 재현성이 깨진다).
    - `class ItemKNNRetriever:` docstring `"""이웃 테이블(book_ids · nbr_ids[n,KNN_TOP] · nbr_sims) 을 쥔다. -1/0.0 은 패딩."""`; `name = "cf"`(= `VARIANTS[1]` 값이지만 contracts 를 import 해 VARIANTS 를 끌어오진 않는다 — 문자열 리터럴 + 주석).
      - `__init__(self, book_ids=None, nbr_ids=None, nbr_sims=None, *, meta=(0, 0, 0), weights: WeightFn | None = None)`: `self._ids = np.asarray(book_ids if book_ids is not None else [], dtype=np.int64)`; `self._nbr_ids = np.asarray(nbr_ids if nbr_ids is not None else np.zeros((0, KNN_TOP)), dtype=np.int64)`; `self._nbr_sims = ... float32`; `self._index = {int(b): i for i, b in enumerate(self._ids.tolist())}`; `self._meta = tuple(int(x) for x in meta)`; `self._weights = weights`.
      - `@property def meta(self) -> tuple[int, int, int]: return self._meta`
      - `fit(self, train: "pd.DataFrame") -> "ItemKNNRetriever"` — 스니펫 A 그대로(`import scipy.sparse as sp` 함수 안):
        ```python
        users, u_idx = np.unique(train[COL_USER].to_numpy(), return_inverse=True)
        items, i_idx = np.unique(train[COL_ITEM].to_numpy(), return_inverse=True)
        x = sp.csr_matrix((np.ones(len(train), dtype=np.float32), (u_idx, i_idx)), shape=(len(users), len(items)))
        x.data[:] = 1.0  # 중복 (user,item) 이 합쳐져 2 가 되지 않게 — 이진
        norms = np.sqrt(np.asarray(x.multiply(x).sum(axis=0)).ravel())
        xn = x @ sp.diags(1.0 / np.where(norms == 0, 1.0, norms))  # 열 L2=1
        xnt = xn.T.tocsr()
        n = len(items); top = min(KNN_TOP, n - 1)  # 합성 3~4 아이템 가드(argpartition kth < n)
        nbr_ids = np.full((n, KNN_TOP), -1, dtype=np.int64); nbr_sims = np.zeros((n, KNN_TOP), dtype=np.float32)
        for s in range(0, n, CHUNK):
            block = np.asarray((xnt[s : s + CHUNK] @ xn).todense())  # (chunk, n) cosine
            rows = np.arange(block.shape[0]); block[rows, s + rows] = -1.0  # self 제외
            if top <= 0: break
            idx = np.argpartition(-block, top - 1, axis=1)[:, :top]
            sims = np.take_along_axis(block, idx, axis=1)
            order = np.argsort(-sims, axis=1, kind="stable")
            idx = np.take_along_axis(idx, order, axis=1); sims = np.take_along_axis(sims, order, axis=1)
            keep = sims > 0  # 공동 소비 0 은 이웃이 아니다
            nbr_ids[s : s + CHUNK, :top] = np.where(keep, items[idx], -1)
            nbr_sims[s : s + CHUNK, :top] = np.where(keep, sims, 0.0).astype(np.float32)
        self._ids, self._nbr_ids, self._nbr_sims = items.astype(np.int64), nbr_ids, nbr_sims
        self._index = {int(b): i for i, b in enumerate(items.tolist())}
        self._meta = _meta(train)
        return self
        ```
        (`argpartition` 의 kth 는 `top - 1` — 상위 `top` 개를 앞으로. 손계산 4아이템에선 `top = 3`.)
      - `neighbors(self, book_id: int, n: int = KNN_TOP) -> list[tuple[int, float]]`: `i = self._index.get(int(book_id)); if i is None: return []`; `ids, sims = self._nbr_ids[i], self._nbr_sims[i]`; `m = ids >= 0`; `return [(int(b), float(s)) for b, s in zip(ids[m][:n].tolist(), sims[m][:n].tolist(), strict=True)]`(fit 이 이미 sim 내림차순으로 저장).
      - `retrieve(self, user, k)`: `return retrieve_from_neighbors(self, user, k, source=SOURCE_ITEMKNN, weights=self._weights, n_neighbors=KNN_TOP)`.
      - `save(self, path: Path = KNN_ARTIFACT) -> Path`: `path.parent.mkdir(parents=True, exist_ok=True)`; `np.savez(path, book_ids=self._ids, nbr_ids=self._nbr_ids, nbr_sims=self._nbr_sims, meta=np.asarray(self._meta, dtype=np.int64))`; `return path`.
      - `@classmethod load(cls, path: Path = KNN_ARTIFACT, *, weights: WeightFn | None = None)`: `with np.load(path) as z:`(allow_pickle 기본 False 유지 — 숫자 배열만) `return cls(z["book_ids"], z["nbr_ids"], z["nbr_sims"], meta=tuple(int(x) for x in z["meta"]), weights=weights)`.
    - `def load_or_fit_itemknn(train: "pd.DataFrame", path: Path | None = None, *, weights: WeightFn | None = None) -> ItemKNNRetriever`: docstring `"""캐시 meta(행·유저·아이템 수) 가 train 과 같으면 로드, 아니면 fit 후 저장. path None 이면 저장 안 함."""`; `want = _meta(train)`; `if path is not None and path.exists(): try: r = ItemKNNRetriever.load(path, weights=weights); if r.meta == want: return r; log.info("itemknn cache meta %s != train %s — refit", r.meta, want) except (OSError, ValueError, KeyError): log.exception("itemknn cache unreadable at %s — refit", path)`; `r = ItemKNNRetriever(weights=weights).fit(train)`; `if path is not None: r.save(path)`; `return r`.

    **2-c. `src/millie_rec/retrieval/content.py`** — 기존 42줄은 그대로(docstring 첫 줄만 `"""contracts.ItemVectors + CandidateGenerator — Goodbooks tags TF-IDF 벡터 + 성분별 평균 벡터 cosine 후보(D-04·D-14)."""` 로 갱신 허용, 두 번째 줄 "Phase 4 REC-01 이 증분" 문장은 삭제). 추가:
    ```python
    from millie_rec.retrieval.neighbors import SOURCE_CONTENT, WeightFn, component_weights
    ```
    - 모듈 함수 2개(두 클래스 공유 — 이중 구현 금지):
      ```python
      def _query(vectors: ItemVectors, user: UserState, weights: WeightFn | None) -> np.ndarray:
          """q = Σ_성분 w · mean(성분 아이템 벡터). 미지 id 는 0 벡터라 자연히 빠진다. 성분 전부 비면 0 벡터."""
          w = component_weights(user, weights)
          parts = ((user.explicit_seeds, w.get("alpha", 0.0)), (user.history, w.get("beta", 0.0)), (user.session, w.get("gamma", 0.0)))
          q: np.ndarray | None = None
          for items, wc in parts:
              if not items or wc == 0.0:
                  continue
              v = wc * vectors.vectors(list(items)).mean(axis=0)
              q = v if q is None else q + v
          return q if q is not None else np.zeros(vectors.vectors([]).shape[1], dtype=float)

      def _topk_cosine(scores: np.ndarray, ids: np.ndarray, seen: frozenset[int], k: int, source: str) -> list[Candidate]:
          """점수 > 0 이고 seen 이 아닌 상위 k. argpartition 크기 k + len(seen)(넉넉히 뽑기 관례)."""
          scores = np.asarray(scores, dtype=float).ravel()
          if k <= 0 or scores.size == 0 or not (scores > 0).any():
              return []
          n = min(scores.size, k + len(seen))
          idx = np.argpartition(-scores, n - 1)[:n]
          idx = idx[np.lexsort((ids[idx], -scores[idx]))]  # (−score, id) 결정적 정렬
          out = [Candidate(book_id=int(ids[i]), source=source, score=float(scores[i])) for i in idx if scores[i] > 0 and int(ids[i]) not in seen]
          return out[:k]
      ```
      (`ItemVectors` 는 `from millie_rec.contracts import COL_ITEM, Candidate, ItemVectors, UserState`.)
    - `ContentVectors`: `name = SOURCE_CONTENT`; `__init__(self, books, text_col=TAGS_COL, *, weights: WeightFn | None = None)` — 기존 본문 뒤에 `self._ids = np.asarray(books[COL_ITEM].to_numpy(), dtype=np.int64)`(`_index` 의 역)·`self._weights = weights`; `fit(self, train) -> "ContentVectors": return self`(no-op, 하네스 대칭 — Protocol 은 structural 이라 있어야 app 이 CandidateGenerator 로 본다); `retrieve(self, user, k)`: `q = _query(self, user, self._weights)`; `scores = np.asarray(self._matrix @ q).ravel()`(CSR·dense → dense (n_books,)); `return _topk_cosine(scores, self._ids, user.seen, k, SOURCE_CONTENT)`.
    - `class VectorRetriever:` docstring `"""ItemVectors(Track B VectorsKR) 위의 content 후보 — 생성 시 dense 행렬 1회(9,450×128 ≈ 9.7MB), 요청마다 재계산 없음."""`; `name = SOURCE_CONTENT`; `__init__(self, vectors: ItemVectors, ids: Sequence[int], *, source: str = SOURCE_CONTENT, weights: WeightFn | None = None)`: `self._vectors = vectors; self._ids = np.asarray([int(b) for b in ids], dtype=np.int64); self._matrix = np.asarray(vectors.vectors(list(self._ids)), dtype=float) if len(self._ids) else np.zeros((0, 0)); self._source, self._weights = source, weights`; `fit(self, train): return self`; `retrieve(self, user, k)`: `if self._matrix.size == 0: return []`; `q = _query(self._vectors, user, self._weights)`; `scores = self._matrix @ q`; `return _topk_cosine(scores, self._ids, user.seen, k, self._source)`.
    (`VectorsKR.vectors` 의 행은 L2=1 이고 `ContentVectors._matrix` 도 행 L2=1 이라 `matrix @ q` 가 cosine×|q| — 채널 안 min-max(Plan 04-02)로 정규화되므로 q 를 단위화하지 않는다. 미지 seed 만이면 q=0 → 점수 전부 0 → `[]`.)

    **2-d. GREEN 확인:** `uv run pytest tests/retrieval tests/test_architecture.py --no-header` → test_neighbors 6 + test_itemknn 8 + test_content 10 + test_popularity 7 + 아키텍처 3 = **34 passed**, failed 0. 실패가 남으면 **테스트가 아니라 구현을 고친다**(단, 손계산 오류가 확실하면 근거를 SUMMARY 에 적고 고친다 — 예: cos(1,3) 값). 스니펫 A 의 `todense()` 가 `np.matrix` 를 돌려주면 `np.asarray` 로 감싼 것이 그 이유다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/retrieval tests/test_architecture.py --no-header 2>&1 | tail -5 && wc -l src/millie_rec/retrieval/*.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/retrieval tests/test_architecture.py --no-header` 요약 줄 `34 passed`, `failed` 없음
    - `wc -l src/millie_rec/retrieval/neighbors.py src/millie_rec/retrieval/itemknn.py src/millie_rec/retrieval/content.py` 각 ≤ 150
    - `grep -n "^KNN_TOP = 50$" src/millie_rec/retrieval/itemknn.py` 1행 · `grep -n "^POOL = 200$" src/millie_rec/retrieval/itemknn.py` 1행 · `grep -n "^CHUNK = 500$" src/millie_rec/retrieval/itemknn.py` 1행 · `grep -n 'ARTIFACT_NAME = "item_neighbors.npz"' src/millie_rec/retrieval/itemknn.py` 1행
    - `grep -n "import scipy" src/millie_rec/retrieval/itemknn.py` 가 `def fit` 뒤에 있다(함수 안 import): `awk '/def fit/{f=1} f && /import scipy.sparse as sp/{print NR; exit}' src/millie_rec/retrieval/itemknn.py` 가 숫자 1개 출력
    - `grep -c "retrieve_from_neighbors(self" src/millie_rec/retrieval/itemknn.py` == 1 (retrieve 로직은 neighbors.py 한 곳)
    - `grep -c "def _topk_cosine\|def _query" src/millie_rec/retrieval/content.py` == 2 · `grep -c "_topk_cosine(" src/millie_rec/retrieval/content.py` ≥ 3(정의 1 + 호출 2)
    - `grep -n "allow_pickle" src/millie_rec/retrieval/itemknn.py` → 출력 없음(기본 False 유지, True 로 열지 않는다)
    - `grep -n 'np.savez(path, book_ids=' src/millie_rec/retrieval/itemknn.py` 1행 · `grep -n '"meta"\|meta=np.asarray' src/millie_rec/retrieval/itemknn.py` ≥ 1행
    - `grep -rn "millie_rec\.\(ranking\|reranking\|data\|evaluation\|serving\|app\)" src/millie_rec/retrieval/` → 출력 없음
    - `awk 'length > 100' src/millie_rec/retrieval/*.py tests/retrieval/*.py | wc -l` == 0
    - `grep -n "def __init__(self, books: \"pd.DataFrame\", text_col: str = TAGS_COL" src/millie_rec/retrieval/content.py` 1행(기존 위치 인자 불변, weights 는 keyword-only 추가)
  </acceptance_criteria>
  <done>tests/retrieval 31건 + 아키텍처 3건 GREEN. 상수 값이 04-CONTEXT.md D-04 와 문자 단위 일치(50·200·npz). retrieve 로직 1곳, top-k cosine 1곳. 커밋 없음.</done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR): retrieval/__init__.py 공개 표면 13개 + ruff(자기 경로) + 재통과</name>
  <files>src/millie_rec/retrieval/__init__.py</files>
  <read_first>
    - src/millie_rec/retrieval/__init__.py (현재 4개 이름)
    - src/millie_rec/data/__init__.py L39-72 (`__all__` 정렬 관례: 대문자 상수 → 클래스 → 함수, 각 알파벳순)
    - Task 2 결과 3파일(노출할 이름 확인 — `_query`·`_topk_cosine`·`_meta`·`CHUNK`·`WeightFn` 는 내부라 노출하지 않는다)
  </read_first>
  <action>
    **3-a. `src/millie_rec/retrieval/__init__.py`** 를 다음으로 교체(Plan 04-04 `app/pipeline.py`·`pipeline_kr.py`·`cli.py` 가 `from millie_rec.retrieval import …` 로만 가져온다 — `tests/test_architecture.py` L41-42 가 `millie_rec.retrieval.itemknn` 같은 깊은 import 를 app 에서 금지):
    ```python
    """retrieval 슬라이스 공개 표면. 다른 슬라이스는 여기 있는 이름만 import 할 수 있다."""

    from millie_rec.retrieval.content import ContentVectors, VectorRetriever
    from millie_rec.retrieval.itemknn import (
        KNN_ARTIFACT,
        KNN_TOP,
        POOL,
        SOURCE_ITEMKNN,
        ItemKNNRetriever,
        load_or_fit_itemknn,
    )
    from millie_rec.retrieval.neighbors import SOURCE_CONTENT, NeighborRetriever, retrieve_from_neighbors
    from millie_rec.retrieval.popularity import POP_ARTIFACT, TOP_N, PopularityRetriever

    __all__ = [
        "KNN_ARTIFACT",
        "KNN_TOP",
        "POOL",
        "POP_ARTIFACT",
        "SOURCE_CONTENT",
        "SOURCE_ITEMKNN",
        "TOP_N",
        "ContentVectors",
        "ItemKNNRetriever",
        "NeighborRetriever",
        "PopularityRetriever",
        "VectorRetriever",
        "load_or_fit_itemknn",
        "retrieve_from_neighbors",
    ]
    ```
    → 14개 이름. `uv run python -c "import millie_rec.retrieval as r; print(len(r.__all__))"` → `14`. 순환 import 없음 확인(neighbors ← itemknn, neighbors ← content 단방향).

    **3-b. 정리(이 플랜이 건드린 파일 안에서만 — 최소 변경 원칙):** `uv run ruff format src/millie_rec/retrieval tests/retrieval && uv run ruff check src/millie_rec/retrieval tests/retrieval`. 스텁 단계의 미사용 import 가 남아 있으면 지운다. 행동 변경 없음 — 정리 후 `uv run pytest tests/retrieval tests/test_architecture.py --no-header` 재통과(34 passed). `wc -l` 재확인.

    **3-c. 전역 게이트는 이 플랜에서 돌리지 않는다:** 전체 `uv run pytest --no-header`·`ruff check .`·`make smoke`(고정 포트 8010 + 공유 `.uvicorn.pid`) 는 wave 1 세 플랜(04-01·02·03)이 모두 끝난 뒤 **Advisor 가 1회** 실행(04-04-PLAN.md `<wave_gate>`). 이 플랜은 서버 코드를 건드리지 않으므로 smoke 는 Phase 3 상태와 동일하게 통과해야 한다.

    **3-d. SUMMARY 준비:** `git status --short -- src/millie_rec/retrieval tests/retrieval` 변경 파일 목록, RED/GREEN pytest 요약 줄(AssertionError 발췌 3건 이상), `wc -l`, "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 문장, Plan 04-04 가 쓸 공개 시그니처 5개(`ItemKNNRetriever(weights=)`·`load_or_fit_itemknn(train, path, *, weights)`·`NeighborRetriever(neighbors, *, source, weights)`·`ContentVectors(books, *, weights)`·`VectorRetriever(vectors, ids, *, source, weights)`) 를 그대로 적는다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run ruff format --check src/millie_rec/retrieval tests/retrieval && uv run ruff check src/millie_rec/retrieval tests/retrieval && uv run pytest tests/retrieval tests/test_architecture.py --no-header 2>&1 | tail -3 && uv run python -c "import millie_rec.retrieval as r; print(len(r.__all__))"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import millie_rec.retrieval as r; print(len(r.__all__))"` → `14`; `grep -c '"ItemKNNRetriever"\|"VectorRetriever"\|"NeighborRetriever"\|"load_or_fit_itemknn"\|"POOL"\|"KNN_ARTIFACT"' src/millie_rec/retrieval/__init__.py` == 6
    - `uv run ruff format --check src/millie_rec/retrieval tests/retrieval` 와 `uv run ruff check src/millie_rec/retrieval tests/retrieval` 모두 클린(`All checks passed!`)
    - `uv run pytest tests/retrieval tests/test_architecture.py --no-header` → `34 passed`, failed 0
    - `uv run python -c "import millie_rec.retrieval, sys; print('scipy' in sys.modules, 'sklearn' in sys.modules)"` → `False False`(공개 표면 import 가 scipy·sklearn 을 끌어오지 않는다 — 서버 메모리 규칙)
    - `git log --oneline | head -1` 이 플랜 시작 전과 동일(커밋 0)
    - `git status --short -- src/millie_rec/ranking src/millie_rec/reranking src/millie_rec/app src/millie_rec/serving src/millie_rec/data src/millie_rec/contracts.py Makefile pyproject.toml tests/test_architecture.py` → 출력 없음(형제 플랜·Advisor 영역 불변)
  </acceptance_criteria>
  <done>공개 표면 14개, 자기 경로 ruff 클린, tests/retrieval 31 + 아키텍처 3 통과, scipy/sklearn 지연 import 유지. 전역 게이트는 wave 종료 후 Advisor. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `artifacts/item_neighbors.npz` → `ItemKNNRetriever.load` | 로컬 캐시 파일 읽기(`np.load`). 사용자 입력 아님, `make eval` 이 쓴 파일 |
| `UserState`(seeds·history·session) → `retrieve` | app/serving 이 검증한 정수 튜플. seeds 파싱·k 상한은 `serving/api.py` 가 이미 함(재검증 불필요) |
| Track B 밀리 텍스트 | 이 슬라이스는 `books_kr` description 을 읽지 않는다(`VectorsKR` npz 숫자만) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `ItemKNNRetriever.load` npz | mitigate | `np.load` 기본 `allow_pickle=False` 유지(acceptance grep — 숫자 배열 4키만). 손상·키 누락은 `load_or_fit_itemknn` 이 `(OSError, ValueError, KeyError)` 로 잡아 재fit + `log.exception`. 경로는 `contracts.DIR_ARTIFACTS` 상수 또는 app 이 넘긴 `Path` — 요청 입력이 경로에 닿지 않음 |
| T-04-02 | Denial of Service | `ItemKNNRetriever.fit` 메모리 | mitigate | chunked top-k(`CHUNK=500`, dense 한 장 40MB) — dense 10k×10k 100M 셀 금지(아키텍처 01 §8 리스크 '메모리'). `top = min(KNN_TOP, n-1)` 로 소형 입력 ValueError 방지 |
| T-04-03 | Denial of Service | `retrieve` 비용(k·seen) | accept | `k` 는 api.py `K_MAX=100`·app `POOL=200` 로 상한, `argpartition(k+len(seen))` 은 n 에 클립. 유저 1명 내부 루프(성분 아이템×이웃 50) 는 수천 회 — 평가 2,000명×2상태에서 수 초 |
| T-04-04 | Information Disclosure | 평가 누수(test → retriever) | mitigate | `fit(train)` 1인자(`inspect.signature` 테스트). `retrieve` 는 `UserState` 만 본다. 하네스가 seen 반환을 `ValueError` 로 잡는다(`evaluation/harness.py` L47-52) |
| T-04-05 | Information Disclosure | 밀리 저작 텍스트 | accept | retrieval 은 텍스트를 읽지 않는다(Track A tags TF-IDF, Track B npz 벡터). `VectorRetriever` 출력은 `book_id`·점수만 |
| T-04-06 | Repudiation | 캐시로 인한 숫자 재현 불가 | mitigate | npz `meta`(train 행·유저·아이템 수) 불일치 시 재fit(테스트 `test_load_or_fit_uses_cache_only_when_meta_matches`). 상수 변경(KNN_TOP) 시 캐시는 수동 삭제 — Plan 04-05 실측 절차에 `rm -f artifacts/item_neighbors.npz` 선행 |
</threat_model>

<verification>
- `uv run pytest tests/retrieval tests/test_architecture.py --no-header` → `34 passed`(신규 20 + 기존 content 4 + popularity 7 + 아키텍처 3), failed 0
- `uv run ruff format --check src/millie_rec/retrieval tests/retrieval && uv run ruff check src/millie_rec/retrieval tests/retrieval` 클린
- `wc -l src/millie_rec/retrieval/*.py` 전부 ≤150
- 공개 표면 import 가 scipy·sklearn 을 로드하지 않는다
- (wave 1 종료 후 Advisor 1회) `uv run pytest --no-header` 255 + 20 = 275 passed 이상 · `test_coverage_gate` 1 failed 는 Phase 3 몫 · `make smoke` PASS
- 커밋 0: `git log --oneline | head -1` 불변
</verification>

<success_criteria>
- **REC-01** 후보 3통로: `PopularityRetriever`(✅ 기존)·`ItemKNNRetriever`(scipy.sparse cosine, train 1인자, `Neighbors` 겸용)·`ContentVectors.retrieve`(TF-IDF, `ItemVectors` 겸용) 가 `CandidateGenerator` 를 구조적으로 만족(`name`·`fit`·`retrieve`), 테스트 3종(계약·정확성·안전성) 존재
- 결정 D-04(04-CONTEXT.md, 후보 통로 세부 — 이웃 50·풀 200·npz 캐시·chunked)·D-01(성분 가중 α/β/γ 는 retriever 안, Callable 주입)·D-10(Track B `NeighborRetriever(source=content)`·`VectorRetriever`) 이 코드·테스트에 존재
- `.claude/rules/data.md` 표기 규칙: Track A 만 `itemknn`, `NeighborRetriever`·`VectorRetriever` 기본 `content`
- `contracts.py` 무변경, 의존성 추가 0, 형제 슬라이스 파일 무변경, 커밋 0
</success_criteria>

<output>
완료 후 `.planning/phases/04-freeze/04-01-SUMMARY.md` 를 만든다. 포함: RED(어떤 단정이 왜 실패했나 — AssertionError 발췌 3건 이상, 요약 줄 — 예상 `18 failed, 6 passed`, 판정은 사유가 AssertionError 계열인지) · GREEN(파일별 구현 요지, 상수 값 `KNN_TOP=50 POOL=200 CHUNK=500`, 손계산 cos 값이 맞았는지) · REFACTOR(`__init__` 14 이름·ruff·`wc -l`) · `## TDD Gate Compliance`(커밋 게이트 생략 사유 = 프로젝트 커밋 정책(사용자 지시 2026-09-05), 대체 증거 = pytest 요약 줄 RED/GREEN 2개) · Plan 04-04 인계용 공개 시그니처 5개 · 변경 파일 목록(`git status --short -- src/millie_rec/retrieval tests/retrieval`) · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
