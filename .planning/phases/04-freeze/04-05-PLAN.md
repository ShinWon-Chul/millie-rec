---
phase: 04-freeze
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - src/millie_rec/app/demo_cli.py
  - src/millie_rec/app/cli.py
  - Makefile
  - tests/app/test_demo_cli.py
  - results/latest.csv
  - results/latest_states.csv
  - results/eval_<ts>.json
  - artifacts/serving/eval_table.json
  - artifacts/popularity.json
  - artifacts/item_neighbors.npz
  - report/figures/eval_bar.png
  - report/draft.md
  - src/millie_rec/ranking/hybrid.py      # D-03 그리드 1회 시 W_CF/W_CONTENT/W_POP 줄만(조건부)
  - src/millie_rec/reranking/mmr.py       # D-07 미달 시 LAMBDA_MMR 줄만(조건부)
  - tests/ranking/test_hybrid.py          # 상수 변경 시 손계산 기대값 갱신(조건부)
  - tests/reranking/test_mmr.py           # 상수 변경 시 손계산 기대값 갱신(조건부)
autonomous: true
requirements: [REC-02, REC-05, REC-07]
lane: 조립 + Advisor 실측(app/cli · Makefile · results · report)
no_commit: true   # 커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다(사용자 지시 2026-09-05)
must_not_touch:
  - src/millie_rec/contracts.py
  - src/millie_rec/serving/**
  - src/millie_rec/retrieval/**     # ranking/hybrid.py W_* · reranking/mmr.py LAMBDA_MMR 는 D-03·D-07 튜닝 1회 규칙 안에서 Task 2 만 예외
  - src/millie_rec/data/**
  - src/millie_rec/evaluation/**
  - src/millie_rec/app/pipeline.py
  - src/millie_rec/app/pipeline_kr.py
  - src/millie_rec/app/server.py
  - scripts/**
  - data/raw/**
  - data/id_map.csv
  - demo/**
  - tests/test_architecture.py
  - pyproject.toml
  - report/demo_5books.md          # Plan 04-06(사용자 5권) 몫 — 여기서 만들지 않는다

must_haves:
  truths:
    - "make eval 이 끝나면 results/latest.csv 에 pop·cf·hybrid·hybrid_div 4행이 split_mode=holdout·model_version=<v>_v1 과 함께 있고 latest_states.csv 에 8행(n0/n20 × 4)이 있다(REC-02, 수용 기준 '평가 4행 + split_mode 출력')"
    - "D-07 게이트 ild(hybrid_div) > ild(hybrid) ∧ ndcg(hybrid_div) ≥ 0.8·ndcg(hybrid) 가 latest.csv 숫자로 성립한다 — 미달이면 LAMBDA_MMR=0.5 로 정확히 1회만 재실행하고 버린 λ 는 eval_<ts>.json 에만 남는다(REC-05)"
    - "report/draft.md §4-1 표에 4 variant × 2 상태 8행 실측이 들어가고 evaluation.md 템플릿 해석 문단이 있으며, 파일 안에 문자열 '[Phase 4]' 가 0개다"
    - "cli demo --find <제목 일부> 가 books_kr.json 에서 제목·저자 부분 일치(대소문자·공백 무시) 를 book_id·title·authors·categories 표로 stdout 에 내고, cli demo --seeds a,b,c 가 seed 별 이웃 top-5 표 + hybrid_div 상위 10 표 + α/β/γ + 가드 상태 + data.md 고정 문장을 stdout 마크다운으로만 낸다(report/ 를 열지 않는다, description 미출력)(REC-07 코드 몫)"
    - "make demo SEEDS=… 가 stdout 을 report/demo_5books.md 로 리다이렉트한다(D-13)"
  artifacts:
    - path: "src/millie_rec/app/demo_cli.py"
      provides: "cmd_demo(args) · --find · --seeds 표 렌더 · 난이도 3분위 ●○○ · 고정 문장"
      exports: ["cmd_demo", "add_demo_parser"]
      max_lines: 150
    - path: "results/latest.csv"
      provides: "4행 비교표(PDF 숫자의 유일한 출처)"
      contains: "hybrid_div,"
    - path: "results/latest_states.csv"
      provides: "n0/n20 × 4 variant 8행"
      contains: "hybrid_div,n20,"
    - path: "report/figures/eval_bar.png"
      provides: "4 variant 3지표 막대(PDF 그림 ③)"
    - path: "report/draft.md"
      provides: "§4-1 실측 8행 + 해석 문단 + 측정 설계(마커 제거)"
      contains: "hybrid_div"
    - path: "tests/app/test_demo_cli.py"
      provides: "--find 부분 일치 · --seeds 표 5개+hybrid_div+고정 문장+description 미출력 · 카탈로그 없음 SystemExit"
      min_lines: 40
  key_links:
    - from: "Makefile"
      to: "report/demo_5books.md"
      via: "demo 타깃 셸 리다이렉트(cli 는 report/ 를 열지 않는다 — 소유권 예외 불필요)"
      pattern: "> report/demo_5books.md"
    - from: "src/millie_rec/app/demo_cli.py"
      to: "src/millie_rec/app/pipeline_kr.py"
      via: "build_pipelines_kr(catalog, weights=state_weights, vectors=load_vectors_kr(serving)) → hybrid_div.recommend"
      pattern: "build_pipelines_kr\\("
    - from: "report/draft.md"
      to: "results/latest_states.csv"
      via: "숫자는 이 파일에서만 복사(evaluation.md·report.md)"
      pattern: "latest_states.csv"
---

<objective>
Phase 4 '추천 파이프라인과 모델 freeze'(.planning/ROADMAP.md "Phase 4: 추천 파이프라인과 모델 freeze")의 **Track A 실측과 5권 도우미**: ① `make eval` 4행 → D-07 게이트 → (미달 시) λ=0.5 1회 → (기대 관계 위반 시) W 그리드 ≤3조합 1회 → `results/latest.csv`·`latest_states.csv`·`eval_<ts>.json`·`eval_bar.png` ② `report/draft.md` §4-1 표 8행 + 해석 문단(`[Phase 4]` 마커 0) ③ `cli demo --find`·`--seeds`(`app/demo_cli.py`) + `Makefile demo` 리다이렉트 + 20권 fixture 테스트 + 실 카탈로그 임의 eligible 5권으로 파이프라인 동작 확인(사용자 5권 실측은 Plan 04-06).

Purpose: ROADMAP Success Criteria 1 전반(4행 + `split_mode`)·2(ILD 상승 + 해석 문단)·4 의 코드 몫(`cli demo --seeds` 출력 + 콘텍츠 유사도 병기). **REC-02**(4행)·**REC-05**(`ild(hybrid_div) > ild(hybrid)` 실측)·**REC-07**(demo CLI). 수용 기준 '평가 4행 + split_mode 출력'·'n=0 vs n≥k 지표 분리'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표). PDF P4 표·P5 앵커 표의 원천.
Output: `demo_cli.py` 신규 + `cli.py` 서브파서 + `Makefile` 1줄 + 테스트 1파일 + `results/` 4파일 + 그림 + draft §4-1. 커밋 0.

**Advisor 실측 원칙(D-15):** `make eval` 은 Advisor 가 직접 돌리고 숫자를 읽는다. Worker 보고의 숫자는 증거가 아니다. **튜닝은 각각 정확히 1회**(D-03·D-07) — 두 번째 그리드·두 번째 λ 는 없다. 실행 순서: 기본 상수로 1회 → 게이트·기대 관계 판정 → 필요 시 1회 조정 → 최종 1회. 최종 실행의 파일이 `latest.*` 가 되고, 버린 조합은 `results/eval_<ts>.json` 버전 파일로만 남는다(PDF·`latest.csv` 에 쓰지 않는다).

**하지 않는 것:** `make millie*`(STATE.md Blockers — 재수집 중·최종 스냅샷 대기), `report/demo_5books.md` 생성(사용자 5권 뒤 Plan 04-06), 새 모델·후보 통로·feature, `results/` 손편집.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-freeze/04-CONTEXT.md
@.planning/phases/04-freeze/04-PATTERNS.md
@.planning/phases/04-freeze/04-04-SUMMARY.md
@src/millie_rec/contracts.py
@src/millie_rec/app/cli.py
@src/millie_rec/app/pipeline.py
@src/millie_rec/app/pipeline_kr.py
@src/millie_rec/ranking/hybrid.py
@src/millie_rec/reranking/mmr.py
@src/millie_rec/evaluation/report.py
@src/millie_rec/evaluation/figures.py
@src/millie_rec/data/catalog_kr.py
@Makefile
@results/latest.csv
@results/latest_states.csv
@report/draft.md
@tests/conftest.py
@tests/app/test_cli.py
@../.claude/rules/evaluation.md
@../.claude/rules/report.md
@../.claude/rules/data.md
@../.claude/rules/simplicity.md
@../.claude/rules/python.md
@../.claude/rules/references.md

<project_rules_inline>
**평가(`../.claude/rules/evaluation.md`):** "`contracts.VARIANTS` 순서 그대로: 1. `pop` 2. `cf`(Item-KNN) 3. `hybrid`(cf + content/explicit-seed 병합) 4. `hybrid_div`(+MMR). 다른 이름 금지." "**PDF 숫자는 이 파일(`results/latest.csv`)에서만 복사한다.** 소수 3자리." "결과 파일은 커밋한다. 손으로 편집하지 않는다. 숫자가 바뀌면 원인은 재실행이어야 한다." "비교 막대그래프는 `figures.py`가 matplotlib로 `report/figures/eval_bar.png`에 생성한다." 트랙 분리: "비교표는 Track A(Goodbooks-10k) 전용… 밀리 지표 숫자를 비교표·PDF에 넣지 않는다." 해석 템플릿: "MMR 적용으로 NDCG@10은 x→y로 소폭 하락했지만 ILD@10은 a→b로 개선됐다. 동일 장르 반복을 줄이는 UX 목적상 허용 가능한 trade-off이며 최종 판단은 A/B로 한다."

**리포트(`../.claude/rules/report.md`):** "숫자는 `results/latest.csv`·`latest_states.csv`·`latency.json`에서만." "`[Phase N]` 마커 = 미완 자리. 제출 시 0개." "한 문단 3문장 이내, 표 > 문장." "영어 용어 첫 등장 = `용어(풀네임: 설명)`." "데이터 2트랙 명시… 두 트랙 숫자를 한 표에 섞지 않는다." 밀리 저작 텍스트 미노출.

**데모 고정 문장(`../.claude/rules/data.md`, 그대로 출력):** "데모 카탈로그의 앵커 이웃은 콘텍츠 유사도다. 협업 필터링(Item-KNN)의 Recall·NDCG는 유저 단위 로그가 있는 Goodbooks(Track A)에서만 측정하며 두 트랙의 숫자를 섞지 않는다." 5권: "밀리 카탈로그(Track B) 안에서 본인이 실제 읽은 한국 책 5권… 출력에 '데모 카탈로그 이웃 = 콘텍츠 유사도' 문구 병기." **텍스트 2개(`description`·`curator_note`)는 TF-IDF 입력 전용** — `CatalogKR.meta` 가 이미 제거하지만 표에 싣는 컬럼을 명시적으로 고정한다.

**간결함(`../.claude/rules/simplicity.md`):** "Day 3 종료 후: 새 모델·후보 통로·feature 금지. 파라미터 튜닝은 Day 4 시작 전까지만." "실측 하나가 나오면 그 즉시 `report/draft.md`에 문장으로 옮긴다." 파일 ≤150줄(`cli.py` 128 → demo 는 `demo_cli.py` 로 분리, Advisor 확정 6). CLI 는 `argparse` 하나 — `demo_cli.py` 는 서브파서 등록 함수 + `cmd_demo` 만 노출하고 `cli.py` 가 부른다.

**참조 표기(`../.claude/rules/references.md`):** draft·SUMMARY 에 `D-07` 단독 금지 → `결정 D-07(04-CONTEXT.md, MMR 완료 게이트)` 형식. PRD 는 `수용 기준 '평가 4행 + split_mode 출력'(PRD §9 수용 기준 표)`.

**완료 기준:** `uv run ruff format . && uv run ruff check . && uv run pytest --no-header`(04-04 게이트 수 + 신규 ≥4, `test_coverage_gate` 1건 외 실패 0) + `make smoke` PASS + **Advisor 실측** `make eval` 4행·게이트 통과 + `grep -c '\[Phase 4\]' report/draft.md` == 0.
</project_rules_inline>

<interfaces>
From src/millie_rec/app/cli.py (현재 — L107-124 `main` 서브파서 등록 자리):
```python
    sub = ap.add_subparsers(dest="cmd", required=True)
    d = sub.add_parser("data", ...); ...; d.set_defaults(func=cmd_data)
    e = sub.add_parser("eval", ...); ...; e.set_defaults(func=cmd_eval)
    args = ap.parse_args(argv)
    logging.basicConfig(level=logging.INFO, format="%(levelname)s %(name)s: %(message)s")
    args.func(args)
```

From src/millie_rec/app/pipeline.py·pipeline_kr.py (Plan 04-04):
```python
def load_catalog(serving_dir: Path = DIR_SERVING) -> CatalogKR | None
def load_vectors_kr(serving_dir: Path = DIR_SERVING) -> VectorsKR | None
def build_pipelines_kr(catalog: CatalogKR, *, weights=None, vectors: VectorsKR | None = None) -> dict[str, Pipeline]   # vectors None → pop 만
```

From src/millie_rec/data/catalog_kr.py:
```python
class CatalogKR:
    def meta(self, book_ids) -> list[dict]        # keys: book_id title authors image_url categories difficulty resid_z difficulty_source book_format pop_rank … (description·curator_note·seg_dist 제거)
    def popular(self, categories=(), n=50) -> list[int]   # eligible 만, pop_rank 순
    def neighbors(self, book_id, n=20) -> list[tuple[int, float]]
    def stats(self, book_ids) -> list[BookStats]  # difficulty · resid_z · source
```

From src/millie_rec/ranking (Plan 04-02): `state_weights(user) -> {"alpha","beta","gamma"}` · `W_CF, W_CONTENT, W_POP = 0.5, 0.3, 0.2`(hybrid.py L?, 튜닝 시 이 줄만) · `GUARD_RESID_Z=-1.0 GUARD_MIN_COMPLETED=3`
From src/millie_rec/reranking/mmr.py (Plan 04-03): `LAMBDA_MMR = 0.7`(게이트 미달 시 이 줄만 0.5)

From src/millie_rec/evaluation/report.py: `LATEST_COLUMNS = ("variant","recall@20","ndcg@10","ild@10","n_users","split_mode","model_version")` · `STATES_COLUMNS = ("variant","state","recall@20","ndcg@10","ild@10","n_users")` · `FLOAT_FORMAT="%.3f"` · json `eval_<YYYYMMDD_HHMM>.json`
From src/millie_rec/evaluation/figures.py: `plot_eval_bar(latest_csv=DIR_RESULTS/"latest.csv", out=DIR_FIGURES/"eval_bar.png") -> Path`(공개 표면 `millie_rec.evaluation.plot_eval_bar`)

현재 기준선(results/latest_states.csv, Phase 2): `pop,n0,0.063,0.054,0.764,2000` · `pop,n20,0.080,0.087,0.746,1990`. Phase 4 기대 관계(04-CONTEXT specifics): recall(cf) > recall(pop) · ndcg(hybrid) ≥ ndcg(cf) · ild(hybrid_div) > ild(hybrid) ∧ ndcg(hybrid_div) ≥ 0.8·ndcg(hybrid) · hybrid·cf 의 n20 행이 n0 대비 벌어진다.

draft.md §4-1 현재 표(L186-194) — 6행(pop n0·n20 실측 + cf·hybrid·hybrid_div `[Phase 4]` 3행) · L196 해석 문단 자리 `[Phase 4]` · L198 "측정 설계 `[Phase 4]`" 문단(내용은 이미 D68 반영) · L104 "구현 결정 `[Phase 4]`" · L106 "`[Phase 4]` 협업 필터링·하이브리드 변형" · L233 "구현 결정 `[Phase 4]`"(§5-2). L233 의 `[Phase 3~4] 밀리 표본 산점도` 와 L236 `[Phase 4~8]` 은 **다른 문자열**(grep `\[Phase 4\]` 에 안 걸림) — 그대로 둔다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/demo_cli.py(--find · --seeds stdout 마크다운) + cli.py 서브파서 + Makefile demo 리다이렉트 + fixture 테스트</name>
  <files>src/millie_rec/app/demo_cli.py, src/millie_rec/app/cli.py, Makefile, tests/app/test_demo_cli.py</files>
  <read_first>
    - src/millie_rec/app/cli.py 전문(서브파서 등록 L107-124 · `raise SystemExit(f"...")` L74 관례)
    - src/millie_rec/app/pipeline_kr.py 전문(Plan 04-04 — `load_vectors_kr`·`build_pipelines_kr` 시그니처)
    - src/millie_rec/app/pipeline.py `load_catalog`
    - src/millie_rec/data/catalog_kr.py L73-118(`meta`·`popular`·`neighbors`·`stats` — 표에 쓸 키)
    - src/millie_rec/serving/api.py L36-43(`_parse_seeds` — 같은 로직을 **중복 정의**, app 은 serving 공개 표면에 `_parse_seeds` 가 없다)
    - tests/conftest.py L46-130(20권 fixture — 제목 `밀리 표본 도서 {i}`, 저자 `저자 {i}`, 카테고리 4종 순환, 엣지 `i→i+1..i+5`, 가드 모집단 7·14)
    - tests/app/test_cli.py L1-26(테스트 헤더·`main` import 관례) · tests/app/test_server_catalog.py L20-24(fixture 주입 monkeypatch 관례)
    - Makefile L30-31(`demo` 타깃)
    - .planning/phases/04-freeze/04-CONTEXT.md D-12·D-13, Claude's Discretion "`cli demo` 표의 난이도 3분위·`--find` 매칭 규칙"
    - .planning/phases/04-freeze/04-PATTERNS.md "app/cli.py" 절
  </read_first>
  <action>
    **1-a. 테스트 먼저 — `tests/app/test_demo_cli.py`(신규, 5건, `capsys` + `millie_serving_sample`).** docstring `"""cli demo(REC-07, D-12·D-13) — --find 부분 일치 · --seeds 표(seed 5 + hybrid_div 1 + α/β/γ + 가드 + 고정 문장) · description 미출력 · 카탈로그 없음 SystemExit. 실 artifacts/serving 은 읽지 않는다."""`. `from millie_rec.app.cli import main`. 모든 호출에 `"--serving", str(millie_serving_sample)`.
    - `test_find_matches_title_and_author_ignoring_case_and_spaces` — `main(["demo", "--find", "표본도서 1", "--serving", ...])`(공백 제거·부분 일치 → `밀리 표본 도서 1`·`10`~`17`·`19`(title None 은 제외)); `out = capsys.readouterr().out`; `"| 1 |" in out` 또는 정규식 `^\| 1 \|` 행 존재; `"| 10 |" in out`; `"| 19 |" not in out`(eligible 만 — `popular(n=10**6)` 기반); 헤더 `| book_id | 제목 | 저자 | 분야 |` 포함; `main(["demo", "--find", "저자 4", "--serving", ...])` → `"| 4 |"` 포함
    - `test_seeds_prints_five_anchor_tables_and_hybrid_div_table` — `main(["demo", "--seeds", "1,2,3,4,5", "--serving", ...])`; `out.count("을 좋아하셨다면") == 5`; `"『밀리 표본 도서 1』" in out`; `"hybrid_div" in out`; `"| # | 책 | 저자 | 분야 | 난이도 | 근거 |" in out`; `out.count("| # | 책 | 저자 | 분야 | 난이도 | 근거 |") == 6`(seed 5 + hybrid_div 1); `"alpha" in out and "1.0" in out and "beta" in out`; `"content" in out`(근거 셀)
    - `test_seeds_output_declares_content_neighbors_and_guard_state` — 고정 문장 전문 `"데모 카탈로그의 앵커 이웃은 콘텍츠 유사도다. 협업 필터링(Item-KNN)의 Recall·NDCG는 유저 단위 로그가 있는 Goodbooks(Track A)에서만 측정하며 두 트랙의 숫자를 섞지 않는다."` 가 out 에 있다; `"이웃은 콘텍츠 유사도(제목·소개 TF-IDF)" in out`; `"가드" in out and "n_completed=0" in out`; hybrid_div 표 행에 `| 7 |`·`| 14 |` 가 없다(가드 모집단 — seeds 1..5 가 seen 이라 7·14 는 후보에 남을 수 있지만 상단 10 에서 밀린다). **검사 범위는 hybrid_div 절만**: seed 2~5 앵커 표에는 이웃 `i→i+1..i+5` 규칙으로 "밀리 표본 도서 7" 행이 정당하게 들어가므로 out 전체를 훑으면 구현이 옳아도 실패한다(checker 블로커). 정확히는 `tail = out.split("### hybrid_div 상위")[1]; assert re.search(r"^\| \d+ \| 밀리 표본 도서 (7|14) \|", tail, re.M) is None`. 같은 `tail` 에서 `"resid_z<−1(millie_index) 0권" in tail`.
    - `test_seeds_never_prints_description_or_curator_note` — fixture `_sample_book` 에는 두 키가 없지만 방어: `"description" not in out and "curator_note" not in out`; 또 `"resid_z" not in out`(내부 수치 미노출)
    - `test_seeds_without_catalog_exits(tmp_path)` — `with pytest.raises(SystemExit): main(["demo", "--seeds", "1,2", "--serving", str(tmp_path)])`; `main(["demo", "--find", "x", "--serving", str(tmp_path)])` 도 `SystemExit`; `--seeds` 와 `--find` 둘 다 없으면 `SystemExit`(argparse 그룹 또는 명시 검사)
    RED: `uv run pytest tests/app/test_demo_cli.py --no-header` → `demo` 서브커맨드가 없어 argparse `SystemExit(2)` 로 **5건 중 마지막만 통과하고 나머지는 `SystemExit`** — `SystemExit` 은 AssertionError 가 아니다 → 1-b 의 서브파서 뼈대(`cmd_demo` 가 `print("")` 만) 를 먼저 넣고 재실행해 `AssertionError` 4건을 확인·발췌한다.

    **1-b. `src/millie_rec/app/demo_cli.py`(신규, ≤150줄).** docstring `"""cli demo — 본인 5권 앵커 케이스(REC-07, D-12·D-13). stdout 마크다운만 쓴다. report/ 는 Makefile 리다이렉트 몫."""`
    ```python
    import argparse
    from pathlib import Path

    import numpy as np

    from millie_rec.app.pipeline import load_catalog
    from millie_rec.app.pipeline_kr import build_pipelines_kr, load_vectors_kr
    from millie_rec.contracts import K_RANK, VARIANTS, UserState
    from millie_rec.data import DIR_SERVING, CatalogKR
    from millie_rec.ranking import state_weights

    HYBRID_DIV = VARIANTS[3]
    N_NEIGHBORS = 5  # D-13 seed 별 이웃 top-5
    FIND_LIMIT = 30
    HEADER = "| # | 책 | 저자 | 분야 | 난이도 | 근거 |"  # D-13 헤더
    RULE = "|---|---|---|---|---|---|"
    NEIGHBOR_NOTE = "이웃은 콘텍츠 유사도(제목·소개 TF-IDF)"
    FIXED_SENTENCE = (  # .claude/rules/data.md 고정 문장 — 글자 그대로
        "데모 카탈로그의 앵커 이웃은 콘텍츠 유사도다. 협업 필터링(Item-KNN)의 Recall·NDCG는 "
        "유저 단위 로그가 있는 Goodbooks(Track A)에서만 측정하며 두 트랙의 숫자를 섞지 않는다."
    )
    META_COLS = ("title", "authors", "categories", "difficulty")  # 표에 싣는 메타 — description 등은 절대 아님
    ```
    - `def _norm(s) -> str: return "".join(str(s or "").split()).casefold()` (대소문자·공백 무시 — Claude's Discretion)
    - `def _parse_seeds(raw: str) -> tuple[int, ...]`: `tuple(int(s) for s in raw.split(",") if s.strip())`; 비어 있으면 `SystemExit("--seeds 는 정수 콤마 목록")` (serving/api.py L36-43 과 같은 로직의 중복 정의 — app 은 serving 내부 함수를 import 하지 않는다)
    - `def _all_meta(catalog: CatalogKR) -> list[dict]: return catalog.meta(catalog.popular(n=10**6))` (eligible 전량, pop_rank 순)
    - `def _tertiles(metas) -> tuple[float, float]`: `d = [m["difficulty"] for m in metas if m.get("difficulty") is not None]`; 비어 있으면 `(1/3, 2/3)`; 아니면 `tuple(np.quantile(d, [1/3, 2/3]))`
    - `def _dots(d: float | None, t: tuple[float, float]) -> str`: `None → "—"`; `d < t[0] → "●○○"`, `d < t[1] → "●●○"`, else `"●●●"`(계약 `BookStats.difficulty` 주석의 3분위 표기)
    - `def _row(n: int, m: dict, dots: str, why: str) -> str`: `f"| {n} | {m.get('title') or '(제목 없음)'} | {m.get('authors') or '—'} | {'·'.join(m.get('categories') or []) or '—'} | {dots} | {why} |"`
    - `def cmd_find(catalog: CatalogKR, query: str) -> None`: `q = _norm(query)`; 매치 = `[m for m in _all_meta(catalog) if q in _norm(m.get("title")) or q in _norm(m.get("authors"))][:FIND_LIMIT]`; `print("| book_id | 제목 | 저자 | 분야 |")`, `print("|---|---|---|---|")`, 각 행 `| {book_id} | {title} | {authors} | {categories} |`; 0건이면 `print("(일치 없음)")`.
    - `def cmd_seeds(catalog: CatalogKR, vectors, seeds: tuple[int, ...], k: int) -> None`:
      1. `pipes = build_pipelines_kr(catalog, weights=state_weights, vectors=vectors)`; `if HYBRID_DIV not in pipes: raise SystemExit("content_vectors_kr.npz 없음 — hybrid_div 를 만들 수 없다(make millie 는 freeze 전 최종 1회만)")`.
      2. `metas = _all_meta(catalog)`; `t = _tertiles(metas)`; `by_id = {int(m["book_id"]): m for m in catalog.meta(list(seeds) + [n for s in seeds for n, _ in catalog.neighbors(s, N_NEIGHBORS)])}`.
      3. seed 별: `m = by_id.get(s)`; 없으면 `print(f"### seed {s} — 카탈로그에 없음")` 계속; `print(f"### 『{m['title']}』을 좋아하셨다면 — {NEIGHBOR_NOTE}")`; `print(HEADER); print(RULE)`; `for n, (nb, w) in enumerate(catalog.neighbors(s, N_NEIGHBORS), 1): print(_row(n, by_id.get(nb, {"book_id": nb}), _dots(by_id.get(nb, {}).get("difficulty"), t), f"content {w:.2f}"))`; 빈 줄.
      4. `user = UserState(None, explicit_seeds=seeds)`; `w = state_weights(user)`; `items = pipes[HYBRID_DIV].recommend(user, k)`; `bad = sum(1 for st in catalog.stats([i.book_id for i in items]) if st.source == "millie_index" and st.resid_z is not None and st.resid_z < -1.0)`; `print(f"### hybrid_div 상위 {k} — alpha={w['alpha']} beta={w['beta']} gamma={w['gamma']} · 가드 활성(n_completed={len(user.history)}<3) · 상위 {k} 중 resid_z<−1(millie_index) {bad}권")`; `print(HEADER); print(RULE)`; `for n, i in enumerate(items, 1): print(_row(n, {"title": i.title, "authors": i.authors, "categories": (by_id.get(i.book_id) or {}).get("categories"), "book_id": i.book_id}, _dots(i.difficulty, t), "+".join(i.source_channels)))` — items 의 title·authors 는 WithMeta 가 채운 값, categories 는 `catalog.meta([i.book_id …])` 로 한 번 더 조인(≤k 개).
      5. `print(); print(FIXED_SENTENCE)`.
      (가드 임계 −1.0 은 `reranking` 공개 상수 `GUARD_RESID_Z` 를 import 해 쓴다 — `from millie_rec.reranking import GUARD_RESID_Z, GUARD_MIN_COMPLETED`; 위 `< -1.0`·`<3` 은 그 상수로 대체.)
    - `def cmd_demo(args: argparse.Namespace) -> None`: `catalog = load_catalog(args.serving)`; `if catalog is None: raise SystemExit(f"카탈로그 없음 @ {args.serving} — make millie 후(freeze 전 최종 1회)")`; `if args.find: cmd_find(catalog, args.find)`; `elif args.seeds: cmd_seeds(catalog, load_vectors_kr(args.serving), _parse_seeds(args.seeds), args.k)`; `else: raise SystemExit("--find 또는 --seeds 중 하나")`.
    - `def add_demo_parser(sub) -> None`: `m = sub.add_parser("demo", help="본인 5권 앵커 케이스(stdout 마크다운) · --find 제목/저자 검색")`; `m.add_argument("--seeds", type=str, default=None)`; `m.add_argument("--find", type=str, default=None)`; `m.add_argument("--serving", type=Path, default=DIR_SERVING)`; `m.add_argument("--k", type=int, default=K_RANK)`; `m.set_defaults(func=cmd_demo)`.
    **`print` 만 쓴다. `open`·`Path.write_text` 0건**(D-13 — `report/` 소유는 사람, 저장은 Makefile 리다이렉트).

    **1-c. `src/millie_rec/app/cli.py`(+2줄):** `from millie_rec.app.demo_cli import add_demo_parser`; `main` 의 `e.set_defaults(func=cmd_eval)` 다음 줄에 `add_demo_parser(sub)`. docstring 첫 단락에 "demo: 본인 5권 앵커(stdout)" 추가. 줄 수 ≤150.

    **1-d. `Makefile` L30-31:**
    ```makefile
    demo:         ## 본인 5권 cold-start 정성 케이스 → report/demo_5books.md (D-13). 예: make demo SEEDS=1,2,3,4,5 · 검색: uv run python -m millie_rec.app.cli demo --find 제목
    	uv run python -m millie_rec.app.cli demo --seeds $(SEEDS) > report/demo_5books.md && echo "→ report/demo_5books.md"
    ```
    (`.PHONY` 에 `demo` 이미 있음. 탭 들여쓰기.)

    **1-e. GREEN:** `uv run pytest tests/app/test_demo_cli.py tests/app/test_cli.py tests/test_architecture.py --no-header` failed 0. `wc -l src/millie_rec/app/demo_cli.py src/millie_rec/app/cli.py` ≤150. 실 카탈로그 동작 확인(읽기만, 재수집 전 스냅샷이라 **판정 아님**): `uv run python -m millie_rec.app.cli demo --find 위버멘쉬 | head -5` 가 표를 내고, 그 결과의 book_id 1개 + `uv run python -c "from millie_rec.app.pipeline import load_catalog; print(load_catalog().popular(n=5))"` 의 4개로 `uv run python -m millie_rec.app.cli demo --seeds <5개> | head -40` 이 표 6개·고정 문장을 낸다(SUMMARY 에 앞 20줄 발췌 — **PDF 에 쓰지 않는다**, 사용자 5권 실측은 Plan 04-06).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/app/test_demo_cli.py tests/app/test_cli.py tests/test_architecture.py --no-header 2>&1 | tail -3 && wc -l src/millie_rec/app/demo_cli.py src/millie_rec/app/cli.py && grep -n "demo_5books" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/app/test_demo_cli.py` == 5 · `uv run pytest tests/app/test_demo_cli.py tests/app/test_cli.py tests/test_architecture.py --no-header` failed 0
    - `wc -l src/millie_rec/app/demo_cli.py` ≤ 150 · `wc -l src/millie_rec/app/cli.py` ≤ 150
    - `grep -c "open(\|write_text\|report/" src/millie_rec/app/demo_cli.py` == 0(stdout 만) · `grep -c "print(" src/millie_rec/app/demo_cli.py` ≥ 8
    - `grep -n '"데모 카탈로그의 앵커 이웃은 콘텍츠 유사도다. 협업 필터링(Item-KNN)의 Recall·NDCG는 "' src/millie_rec/app/demo_cli.py` 1행(고정 문장 전반부 글자 그대로) · `grep -n 'HEADER = "| # | 책 | 저자 | 분야 | 난이도 | 근거 |"' src/millie_rec/app/demo_cli.py` 1행
    - `grep -c "description\|curator_note" src/millie_rec/app/demo_cli.py` ≤ 1(META_COLS 주석의 "description 등은 절대 아님" 만) · `grep -n "GUARD_RESID_Z" src/millie_rec/app/demo_cli.py` ≥ 1행
    - `grep -n "add_demo_parser(sub)" src/millie_rec/app/cli.py` 1행 · `grep -n "from millie_rec.app.demo_cli import add_demo_parser" src/millie_rec/app/cli.py` 1행
    - `grep -n "demo --seeds \$(SEEDS) > report/demo_5books.md" Makefile` 1행
    - `grep -rn "from millie_rec\.\(retrieval\|ranking\|reranking\|data\|serving\|evaluation\)\.[a-z_]* import" src/millie_rec/app/demo_cli.py` → 출력 없음(공개 표면만)
    - `ls report/demo_5books.md` → 파일 없음(이 플랜은 만들지 않는다 — Plan 04-06)
  </acceptance_criteria>
  <done>`cli demo --find/--seeds` 가 fixture 에서 표 6개·α/β/γ·가드 상태·고정 문장을 stdout 으로 내고 report/ 를 열지 않는다. Makefile demo 가 리다이렉트. 테스트 5건 통과. 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 2 (Advisor 실측): make eval 4행 → D-07 게이트·기대 관계 판정 → 튜닝 각 ≤1회 → latest.* · eval_bar.png</name>
  <files>results/latest.csv, results/latest_states.csv, results/eval_<ts>.json, artifacts/serving/eval_table.json, artifacts/popularity.json, artifacts/item_neighbors.npz, report/figures/eval_bar.png, src/millie_rec/reranking/mmr.py (λ 1회 예외), src/millie_rec/ranking/hybrid.py (W 1회 예외)</files>
  <read_first>
    - .planning/phases/04-freeze/04-CONTEXT.md D-03(≤3조합 1회)·D-07(게이트·λ 0.5 1회)·specifics "`latest.csv` 기대 형태"
    - results/latest.csv · results/latest_states.csv(Phase 2 기준선 — 덮어써진다)
    - src/millie_rec/evaluation/report.py L17-33(컬럼·포맷) · src/millie_rec/evaluation/figures.py L25-48(`plot_eval_bar`)
    - src/millie_rec/ranking/hybrid.py `W_CF, W_CONTENT, W_POP` 줄 · src/millie_rec/reranking/mmr.py `LAMBDA_MMR` 줄(튜닝 시 이 줄만)
    - Makefile L27-28(`eval`)
    - ../.claude/rules/evaluation.md 전문
  </read_first>
  <action>
    **2-a. 사전:** `ls data/processed/interactions.parquet data/processed/books.parquet`(Phase 2 산출물 존재 — 없으면 `make data`, 네트워크 1회) · `ls artifacts/item_neighbors.npz 2>/dev/null`(첫 실행이라 없어야 정상; 있으면 Plan 04-04 테스트가 아닌 실 데이터 캐시인지 `meta` 확인 — 의심스러우면 `rm -f artifacts/item_neighbors.npz`) · `uv run python --version` 3.11.x · 디스크 여유 ≥1GB.

    **2-b. 1차 실행(기본 상수 W 0.5/0.3/0.2 · λ 0.7):** `time make eval 2>&1 | tee "$SCRATCH/eval_run1.log"`(Item-KNN fit 1~2분 + 4 variant × 2 상태 × 2,000명 — **run_in_background + timeout 600s**, 10분 초과 시 중단하고 `retrieve_from_neighbors` 성분 루프 비용을 SUMMARY 에 보고 — 코드 변경은 소유 플랜 04-01 수정 브리프). 종료 후 `cat results/latest.csv results/latest_states.csv`.

    **2-c. 판정(python 한 줄, 숫자를 눈으로도 읽는다):**
    ```bash
    uv run python - <<'EOF'
    import pandas as pd
    d = pd.read_csv("results/latest.csv").set_index("variant")
    g1 = d.loc["hybrid_div","ild@10"] > d.loc["hybrid","ild@10"]
    g2 = d.loc["hybrid_div","ndcg@10"] >= 0.8 * d.loc["hybrid","ndcg@10"]
    r1 = d.loc["cf","recall@20"] > d.loc["pop","recall@20"]
    r2 = d.loc["hybrid","ndcg@10"] >= d.loc["cf","ndcg@10"]
    print(d[["recall@20","ndcg@10","ild@10"]]); print("D-07 gate:", g1 and g2, "| recall(cf)>pop:", r1, "| ndcg(hybrid)>=cf:", r2)
    EOF
    ```
    - **게이트 통과 ∧ 기대 관계 성립** → 2-e 로.
    - **D-07 게이트 미달**(g1 또는 g2 False) → `src/millie_rec/reranking/mmr.py` 의 `LAMBDA_MMR = 0.7` → `LAMBDA_MMR = 0.5`(그 줄만, 주석에 "D-07 1회 재실행 2026-09-0X: 0.7 은 results/eval_<ts>.json 에 기록" 추가) → `make eval` **1회** 재실행 → 다시 판정. 그래도 미달이면 **더 돌리지 않는다** — 숫자를 그대로 두고 SUMMARY·개발일지에 "게이트 미달, λ 0.5 도 미달, freeze 는 0.5 로" 를 적고 draft 해석 문단은 실제 숫자로 쓴다(사용자 보고).
    - **recall(cf) ≤ recall(pop) 또는 ndcg(hybrid) < ndcg(cf)**(기대 관계 위반) → W 그리드 **≤3조합 1회**: 후보 `(0.6, 0.3, 0.1)`·`(0.4, 0.4, 0.2)`·`(0.5, 0.4, 0.1)` 중 **한 조합만 골라**(직관: cf 가 약하면 첫째, content 가 약하면 셋째) `hybrid.py` `W_CF, W_CONTENT, W_POP` 줄만 바꾸고 `make eval` 1회. 두 결과 중 `ndcg@10(hybrid)` n0 가 큰 쪽을 남긴다(작으면 원래 값으로 되돌리고 **재실행 없이** 이전 `eval_<ts>.json` 을 최종으로 — 단 `latest.*` 는 마지막 실행 것이라, 되돌릴 경우 `make eval` 1회 더 필요 → 그래서 "그리드 = 총 실행 ≤ 2회" 로 계획: 조합 1개만 시도). recall(cf) ≤ pop 은 W 로 못 고친다 — Item-KNN 자체 문제 → 04-01 수정 브리프(예: 이웃 sim 정규화·shrinkage) 후 재실행, SUMMARY 에 기록.
    - λ 변경과 W 변경이 **둘 다** 필요하면 W 먼저(hybrid 가 바뀌면 hybrid_div 게이트 기준도 바뀐다) → 판정 → λ.

    **2-d. 버린 실행 기록:** 각 `make eval` 은 `results/eval_<YYYYMMDD_HHMM>.json` 을 하나씩 남긴다(파일명 = 실행 시각). 최종이 아닌 파일은 **지우지 않고** SUMMARY 표에 `파일명 · 상수(W·λ) · 4행 숫자 · 채택 여부` 로 적는다(D-03 "버린 조합의 숫자는 `eval_<ts>.json` 버전 파일로만"). `latest.csv`·`latest_states.csv`·`eval_table.json` 은 마지막 실행 것 = 채택본.

    **2-e. 그림·아티팩트:** `uv run python -c "from millie_rec.evaluation import plot_eval_bar; print(plot_eval_bar())"` → `report/figures/eval_bar.png` 갱신(4 variant 막대, 파일 크기 > 10KB). `ls -la artifacts/popularity.json artifacts/item_neighbors.npz artifacts/serving/eval_table.json` 존재. `uv run python -c "import json; t = json.load(open('artifacts/serving/eval_table.json')); print([r['variant'] for r in t['rows']])"` → `['pop', 'cf', 'hybrid', 'hybrid_div']`.

    **2-f. 전체 게이트:** `uv run pytest --no-header`(04-04 수 + 5, `test_coverage_gate` 1건만 RED) · `uv run ruff format --check . && uv run ruff check .` · `make smoke` PASS. 상수를 바꿨다면 `tests/ranking`·`tests/reranking` 손계산 테스트가 실패할 수 있다(테스트가 상수를 손으로 적었기 때문 — 이것이 freeze 검증의 의도): 그 경우 **테스트의 기대값을 새 상수로 갱신**하고 SUMMARY 에 "상수 변경에 따른 테스트 기대값 갱신(파일·줄·이전→이후)" 을 적는다(D-15 예외의 유일한 허용 사유).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && cat results/latest.csv && cat results/latest_states.csv && uv run python -c "import pandas as pd; d=pd.read_csv('results/latest.csv').set_index('variant'); print('gate', d.loc['hybrid_div','ild@10']>d.loc['hybrid','ild@10'] and d.loc['hybrid_div','ndcg@10']>=0.8*d.loc['hybrid','ndcg@10'])" && ls -la report/figures/eval_bar.png results/eval_*.json</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l < results/latest.csv` == 5(헤더 + 4) · `cut -d, -f1 results/latest.csv | tail -n +2 | tr '\n' ' '` == `pop cf hybrid hybrid_div ` · `grep -c ",holdout," results/latest.csv` == 4 · `grep -c "_v1$" results/latest.csv` == 4
    - `wc -l < results/latest_states.csv` == 9 · `grep -c "^hybrid_div,n20," results/latest_states.csv` == 1 · `grep -c "^cf,n0," results/latest_states.csv` == 1
    - 판정 스크립트 출력 `gate True`(또는 SUMMARY 에 "λ 0.5 에서도 미달" 사용자 보고 + 개발일지 예정 — 이 경우 acceptance 는 "재실행 총 횟수 ≤ 2" 로 대체하며 `ls results/eval_*.json | wc -l` 이 Phase 2 의 1 + 이번 ≤ 3)
    - `ls results/eval_*.json | wc -l` ≤ 4(Phase 2 1개 + 이번 최대 3회: 기본·W·λ)
    - `stat -f %z report/figures/eval_bar.png` > 10000 이고 mtime 이 이번 실행 이후
    - `uv run python -c "import json; t = json.load(open('artifacts/serving/eval_table.json')); print([r['variant'] for r in t['rows']])"` → `['pop', 'cf', 'hybrid', 'hybrid_div']`
    - `ls artifacts/item_neighbors.npz` 존재 · `git check-ignore artifacts/item_neighbors.npz` 출력 있음(gitignore 확인) · `git check-ignore results/latest.csv` 출력 없음(커밋 대상)
    - `uv run pytest --no-header` 실패는 `test_coverage_gate` 1건만 · `uv run ruff check .` 클린 · `make smoke` → `smoke: PASS`
    - `grep -n "^LAMBDA_MMR = " src/millie_rec/reranking/mmr.py` 와 `grep -n "^W_CF, W_CONTENT, W_POP = " src/millie_rec/ranking/hybrid.py` 의 값이 SUMMARY 의 "채택 상수" 와 일치(freeze 값)
  </acceptance_criteria>
  <done>4행·8행 실측 파일, D-07 게이트 판정(통과 또는 1회 재시도 후 보고), 그림 갱신, 버린 실행은 eval_<ts>.json 으로만. 상수 최종값 확정(= freeze 후보). 커밋 없음.</done>
</task>

<task type="auto">
  <name>Task 3: report/draft.md §4-1 표 8행 + 해석 문단 + [Phase 4] 마커 0</name>
  <files>report/draft.md</files>
  <read_first>
    - report/draft.md L100-108(§2-2 "구현 결정 `[Phase 4]`"·"구현 현황") · L174-200(§4-1 표·해석 자리·측정 설계·각주) · L229-236(§5-1·§5-2·§5-3 마커)
    - results/latest_states.csv(Task 2 결과 — 숫자의 유일한 출처) · results/latest.csv
    - src/millie_rec/ranking/hybrid.py `W_*` 줄 · src/millie_rec/reranking/mmr.py `LAMBDA_MMR` 줄(채택 값 — 측정 설계 문단의 숫자와 일치시킨다)
    - ../.claude/rules/report.md 전문 · ../.claude/rules/evaluation.md "해석 문장" 절 · ../.claude/rules/references.md
  </read_first>
  <action>
    숫자는 **`results/latest_states.csv` 에서 그대로 복사**(소수 3자리, 재계산 없음). 한 문단 3문장 이내.

    **3-a. §4-1 표(L186-194) 교체 — 8행:**
    ```markdown
    **실측 (Phase 4 완료, `results/latest_states.csv` · Goodbooks-10k · holdout)**

    | 모델 | 사용자 상태 | Recall@20 | NDCG@10 | ILD@10 | n |
    |---|---|---:|---:|---:|---:|
    | pop (전역 인기, 기준선) | 온보딩 직후 (n=0) | {pop,n0} | | | 2,000 |
    | pop (전역 인기, 기준선) | 이력 축적 (n≥20) | {pop,n20} | | | {n} |
    | cf (Item-KNN) | 온보딩 직후 (n=0) | … |
    | cf (Item-KNN) | 이력 축적 (n≥20) | … |
    | hybrid (+취향 설정·콘텍츠) | 온보딩 직후 (n=0) | … |
    | hybrid (+취향 설정·콘텍츠) | 이력 축적 (n≥20) | … |
    | hybrid_div (+다양성·난이도 가드) | 온보딩 직후 (n=0) | … |
    | hybrid_div (+다양성·난이도 가드) | 이력 축적 (n≥20) | … |
    ```
    `{…}` 자리에 csv 값. n 은 `n_users`(천 단위 콤마).

    **3-b. L196 해석 문단 — `[Phase 4]` 자리를 evaluation.md 템플릿으로 채운다(숫자 대입, hybrid→hybrid_div n=0 행 기준):**
    "MMR 적용으로 NDCG@10은 {ndcg hybrid n0}→{ndcg hybrid_div n0}로 소폭 하락했지만 ILD@10은 {ild hybrid n0}→{ild hybrid_div n0}로 개선됐다. 동일 장르 반복을 줄이는 UX 목적상 허용 가능한 trade-off이며 최종 판단은 A/B로 한다." + 두 상태 증거 1문장: "hybrid는 n≥20에서 pop 대비 NDCG@10 격차를 {hybrid n20 − pop n20}만큼 벌려(pop {…}→hybrid {…}) 이력 성분 β가 순위화에 들어온다는 §2-2 주장의 실측 증거가 된다."(부호가 반대면 사실대로 적고 "기대와 어긋남 — Day 4 튜닝 없이 freeze, 원인은 개발일지" 로). **α 수치 표기 주의(checker 경고):** 평가 n_k 상태의 `history` 는 seeds 외 train 전체(유저별 20~수백)라 α 는 대부분 하한 `ALPHA_FLOOR=0.2` 근방이다 — draft 에 "n=20 → α≈0.22" 같은 단일 수치를 쓰지 말고 "n≥20(α 하한 0.2 근방, β 가 대부분)" 으로 적는다. **D-05 "설계만" 1구(checker 경고):** 같은 §4-1 각주 또는 §5 난이도 문단 끝에 "난이도 ablation A/B/C(AUC 표)는 사용자별 완독 라벨이 있는 실서비스에서만 가능 — 설계만(ablation 계획)" 1문장을 추가한다(수용 기준 '난이도 4층 피처·ablation'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표) 허용 표기). acceptance: `grep -c "설계만" report/draft.md` ≥ 1 증가. 기존 문장 "pop 기준선의 두 값 차이는 … 바닥선이고, **hybrid가 n≥20에서 pop 대비 격차를 벌려야 증거**가 된다." 는 유지하고 그 뒤 "해석 문단(다양성 재순위화의 NDCG↔ILD 상충) `[Phase 4]`." 를 위 문단으로 교체.

    **3-c. L198 "측정 설계 `[Phase 4]`" → "측정 설계."** 로 마커 제거. 문단 안 숫자(협업 0.5 · 콘텍츠 0.3 · 인기 0.2 · λ=0.7)를 **채택 상수와 일치**시킨다(Task 2 에서 바뀌었으면 갱신하고 "Day 3에 1회만 조정" 문구는 "Day 3에 1회 조정(0.7→0.5)" 식으로 사실대로).

    **3-d. 다른 마커:** L104 "**구현 결정 `[Phase 4]`.**" → "**구현 결정.**" · L106 "`[Phase 4]` 협업 필터링·하이브리드 변형." → "협업 필터링·하이브리드 변형·다양성 재순위화·난이도 가드 완료(Phase 4)." · L233 "**구현 결정 `[Phase 4]`.**" → "**구현 결정.**". **L233 의 `[Phase 3~4] 밀리 표본 산점도` 와 L236 `[Phase 4~8]` 은 그대로**(다른 페이즈 몫, 문자열도 다르다).

    **3-e. 각주(L200)** 에 1구 추가: "Item-KNN 이웃 50 · 후보 풀 채널별 200 · MMR 풀 50" (PDF 각주 상수 — D-14 freeze 대상 인용).

    **3-f. 검증:** `grep -c '\[Phase 4\]' report/draft.md` == 0 · `grep -c '\[Phase 3~4\]\|\[Phase 4~8\]' report/draft.md` == 2(불변) · 표 숫자 8행이 csv 와 문자 단위 일치(`uv run python` 으로 csv 를 읽어 draft 에 각 값 문자열이 있는지 검사) · `make pdf` 는 돌리지 않는다(Day 5).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && grep -c '\[Phase 4\]' report/draft.md; grep -c '\[Phase 3~4\]\|\[Phase 4~8\]' report/draft.md; uv run python -c "import pandas as pd; s=pd.read_csv('results/latest_states.csv'); t=open('report/draft.md',encoding='utf-8').read(); bad=[(r['variant'],r['state']) for _,r in s.iterrows() if not all(f'{r[c]:.3f}' in t for c in ['recall@20','ndcg@10','ild@10'])]; print('rows',len(s),'mismatch',bad)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[Phase 4\]' report/draft.md` == 0
    - `grep -c '\[Phase 3~4\]\|\[Phase 4~8\]' report/draft.md` == 2(다른 페이즈 마커 불변)
    - `grep -c "| hybrid_div (+다양성·난이도 가드) |" report/draft.md` == 2(n0·n20 두 행) · `grep -c "| cf (Item-KNN) |" report/draft.md` == 2 · `grep -c "| hybrid (+취향 설정·콘텍츠) |" report/draft.md` == 2
    - `grep -n "MMR 적용으로 NDCG@10은" report/draft.md` 1행 · `grep -n "최종 판단은 A/B로 한다" report/draft.md` ≥ 1행
    - 표의 각 숫자 셀이 `results/latest_states.csv` 의 값(소수 3자리 문자열)과 일치: `uv run python -c "import pandas as pd,re; s=pd.read_csv('results/latest_states.csv'); t=open('report/draft.md',encoding='utf-8').read(); bad=[(r['variant'],r['state']) for _,r in s.iterrows() if not all(f'{r[c]:.3f}' in t for c in ['recall@20','ndcg@10','ild@10'])]; print(bad)"` → `[]`
    - `grep -n "실측 (Phase 4 완료, \`results/latest_states.csv\`" report/draft.md` 1행 · `grep -c "실측 (Phase 2 완료" report/draft.md` == 0
    - 측정 설계 문단의 λ·W 숫자가 `src/millie_rec/reranking/mmr.py`·`src/millie_rec/ranking/hybrid.py` 상수와 일치(SUMMARY 에 대조 표)
    - `grep -n "이웃 50 · 후보 풀 채널별 200 · MMR 풀 50" report/draft.md` 1행
    - `git diff --stat -- report/demo_5books.md` → 출력 없음(파일 자체 없음)
  </acceptance_criteria>
  <done>draft §4-1 이 8행 실측 + 해석 문단 + 측정 설계(채택 상수) 이고 `[Phase 4]` 마커 0. 숫자는 csv 와 문자 단위 일치. 커밋 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cli demo --find/--seeds` 인자 → `demo_cli.py` | 로컬 stdout 전용 CLI. 사용자 = 개발자 본인. 네트워크·파일 쓰기 없음 |
| `artifacts/serving/books_kr.json` → 표 출력 | 밀리 공개 메타. `description`·`curator_note` 는 `CatalogKR` 가 제거, `META_COLS` 로 표 컬럼 고정 |
| `make eval` → `results/`·`artifacts/` 쓰기 | 로컬 파일. PDF 숫자의 출처 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Information Disclosure | `cli demo` 표에 밀리 저작 텍스트 | mitigate | 표 컬럼 = `title·authors·categories·difficulty` 만(`META_COLS`), `description`·`curator_note`·`resid_z` 미출력(테스트 `test_seeds_never_prints_description_or_curator_note`, acceptance grep ≤1) |
| T-04-27 | Tampering | `--seeds` 파싱 | accept | `int()` 실패 → `ValueError` 로 종료(로컬 CLI, 사용자 본인). `--serving` 경로는 argparse `Path` — `load_catalog` 가 없으면 `SystemExit` |
| T-04-28 | Tampering | `results/` 손편집 | mitigate | evaluation.md "손으로 편집하지 않는다" — 이 플랜은 `make eval` 로만 쓴다. 버린 실행도 `eval_<ts>.json` 으로 남긴다(재현 근거). draft 숫자 = csv 문자 일치 검사(acceptance) |
| T-04-29 | Repudiation | 튜닝 횟수 초과(p-hacking) | mitigate | D-03·D-07 "각 1회" — 실행 총 ≤3회, `ls results/eval_*.json | wc -l ≤ 4` acceptance. 버린 상수·숫자는 SUMMARY 표 + 개발일지(Plan 04-06) |
| T-04-30 | Denial of Service | `make eval` 소요·메모리 | accept | Item-KNN chunked(40MB/장), npz 캐시로 2회차부터 fit 생략. timeout 600s 배경 실행, 초과 시 보고 |
| T-04-31 | Information Disclosure | Track A·B 숫자 혼합 | mitigate | draft 표는 `latest_states.csv`(Track A) 만, `cli demo` 출력은 PDF 표에 넣지 않고 고정 문장으로 트랙 분리 선언 |
</threat_model>

<verification>
- `results/latest.csv` 4행(`pop cf hybrid hybrid_div`, `holdout`, `_v1`) · `latest_states.csv` 8행 · `eval_<ts>.json` 이번 실행 ≤3개 · `eval_bar.png` 갱신
- D-07 게이트 `ild(hybrid_div) > ild(hybrid) ∧ ndcg(hybrid_div) ≥ 0.8·ndcg(hybrid)` True(또는 λ 0.5 1회 후 사용자 보고)
- `report/draft.md`: `[Phase 4]` 0개 · 표 8행 숫자 = csv · 해석 문단 템플릿 · 측정 설계 상수 = 코드 상수
- `cli demo --find`·`--seeds` fixture 테스트 5건 통과, `Makefile demo` 리다이렉트
- `uv run pytest --no-header` 실패는 `test_coverage_gate` 1건만 · `ruff` 클린 · `make smoke` PASS
- 커밋 0 · `make millie*` 실행 0 · `report/demo_5books.md` 미생성
</verification>

<success_criteria>
- **REC-02** 실측: `make eval` 결과 `results/latest.csv` 에 4행이 `split_mode` 와 함께 — 수용 기준 '평가 4행 + split_mode 출력'(../.assets/PRD/PRD_메인_추천_시스템.md §9 수용 기준 표) · `latest_states.csv` n0/n20 8행 — 수용 기준 'n=0 vs n≥k 지표 분리'(같은 표)
- **REC-05** 실측: `ild(hybrid_div) > ild(hybrid)` ∧ NDCG 하락 ≤20%(결정 D-07(04-CONTEXT.md, MMR 완료 게이트)), 해석 문단이 `report/draft.md` §4-1 에 있다(ROADMAP Success Criteria 2)
- **REC-07** 코드 몫: `cli demo --seeds` 가 앵커 표 + hybrid_div 표 + 콘텍츠 유사도 병기 문장을 출력(ROADMAP Success Criteria 4 의 "이웃이 콘텍츠 유사도임을 출력에 병기"), `--find` 도우미(D-12), `make demo` 리다이렉트(D-13). 사용자 5권 실측은 Plan 04-06
- 결정 D-03(그리드 ≤3조합 1회, 버린 숫자는 json 만)·D-07(λ 1회)·D-13 이행. 상수 최종값 = Plan 04-06 freeze 선언 값
- 커밋 0, `make millie*` 0, 의존성 0
</success_criteria>

<output>
완료 후 `.planning/phases/04-freeze/04-05-SUMMARY.md` 를 만든다. 포함: `make eval` 실행 표(회차·시각·`eval_<ts>.json` 파일명·W·λ·4행 숫자·소요 시간·채택 여부) · D-07 게이트·기대 관계 판정 결과(True/False 와 숫자) · 채택 상수 표(`W_CF W_CONTENT W_POP LAMBDA_MMR` 최종값 — Plan 04-06 freeze 선언이 인용) · 상수 변경 시 갱신한 테스트 기대값 목록 · `cli demo` fixture 테스트 RED/GREEN 요약 · 실 카탈로그 임의 5권 `--seeds` 출력 앞 20줄(판정 아님, 재수집 전 스냅샷) · draft §4-1 변경 전후(마커 3곳·표 8행·해석 문단) · 전역 게이트 결과 · 변경 파일 목록(`git status --short -- src/millie_rec/app/demo_cli.py src/millie_rec/app/cli.py Makefile tests/app/test_demo_cli.py results report src/millie_rec/ranking/hybrid.py src/millie_rec/reranking/mmr.py`) · "커밋하지 않는다 — 작업 트리에 남기고 SUMMARY 에 변경 파일 목록을 적는다" 한 줄.
</output>
