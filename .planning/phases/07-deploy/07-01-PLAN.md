---
phase: 07-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/millie_rec/app/server.py
  - tests/app/test_sentry.py          # 신설 1파일만 — 기존 tests/** 는 무수정
  - ../PROGRESS.md                    # 결정 로그 1행(의존성 추가) append 만
# Task 3 = Codex 3분류 → 사용자 커밋 승인 체크포인트
autonomous: false
requirements: [DEPLOY-02, DEPLOY-03]
lane: 조립(Advisor 전용 파일 — pyproject.toml·app/server.py. Worker 위임 시 허용 경로는 src/millie_rec/app/server.py·tests/app/test_sentry.py 2개만)
tdd_gate_evidence: pytest-output
push_policy: "커밋은 사용자 승인 후(Task 3). push 는 이 플랜에서 하지 않는다 — 07-02 Task 1 에서 사용자 승인 후 1회. push = Railway 자동 재배포(07-CONTEXT D-03·D-12)"
must_not_touch:
  - src/millie_rec/serving/**         # Phase 5 검증 완료 — 이 페이즈 must_not_touch
  - src/millie_rec/contracts.py       # 🧊 freeze
  - src/millie_rec/{data,retrieval,ranking,reranking,evaluation}/**
  - demo/**                           # Phase 6 레인이 동시에 쓴다
  - tests/serving/** · tests/demo/** · tests/test_architecture.py · tests/app/* (test_sentry.py 제외)
  - artifacts/serving/**              # 🧊 재빌드 금지 — make millie* 절대 실행 안 함
  - Dockerfile · railway.json · .dockerignore   # D-11 3조건 불변 — 이 플랜은 grep 으로 검사만(Codex 필수 fix 를 사용자가 승인한 경우만 예외)
  - Makefile                          # docker-up·docker-down 타깃이 이미 있다 — 추가 불필요
  - results/**                        # p95 79.5ms 로컬 실측이 정본

must_haves:
  truths:
    - "pyproject.toml dependencies 에 sentry-sdk[fastapi] 1개만 추가되고 prometheus-client 는 없다(07-CONTEXT D-04·D-05). uv.lock 이 갱신돼 Dockerfile 의 uv sync --frozen 이 성립한다"
    - "SENTRY_DSN 환경변수가 없으면 app/server.py 는 sentry_sdk.init 을 호출하지 않는다(네트워크 호출 0) — tests/app/test_sentry.py 가 단정한다. DSN 이 있으면 traces_sample_rate=0 · send_default_pii=False 로 1회 초기화한다(D-04)"
    - "app/server.py 는 50줄 이하이고 create_app(...) 호출·StaticFiles 마지막 마운트 등 기존 흐름은 한 글자도 바뀌지 않는다"
    - "Dockerfile 3조건(uv sync --frozen --no-dev 2회 · USER 지시어 0 · ${PORT:-8000}) 과 railway.json(healthcheckPath /health · numReplicas 1) 이 grep/jq 로 확인된다(D-11)"
    - "로컬 게이트 3종이 통과한다: uv run pytest --no-header failed 0 · make smoke PASS · make docker-up PORT=8001 이 smoke: PASS 를 출력하고 docker restart 뒤 /health.db_row_count 가 유지된다(D-12, 배포 02 §4-2)"
    - "repo 위생: git ls-files | grep -icE 'pdf|png|assets' == 0 · report/figures/ 가 무시된다(D-07·D-08) · find artifacts/serving -size +50M 빈 결과 · uv.lock 추적 중"
    - "Codex 필수 리뷰(Dockerfile·railway.json·app/server.py·pyproject.toml — ../.claude/rules/codex-review.md '배포 직전' 행) 가 실행되고 필수 fix / 토론 / 무시 3분류 ≤10줄로 보고되며, 사용자 승인분만 반영한 뒤 사용자 승인으로 커밋된다(push 없음)"
  artifacts:
    - path: "pyproject.toml"
      provides: "dependencies 에 sentry-sdk[fastapi] 1줄"
      contains: "sentry-sdk[fastapi]"
    - path: "src/millie_rec/app/server.py"
      provides: "init_sentry() — SENTRY_DSN 있을 때만 sentry_sdk.init(traces_sample_rate=0, send_default_pii=False, environment=…)"
      contains: "SENTRY_DSN"
      max_lines: 50
    - path: "tests/app/test_sentry.py"
      provides: "DSN 없음 → init 미호출·is_initialized False / DSN 있음(가짜) → init 1회·옵션 단정(spy — 네트워크 0)"
      contains: "SENTRY_DSN"
    - path: "../PROGRESS.md"
      provides: "결정 로그 1행 — sentry-sdk[fastapi] 추가(D-04), prometheus-client 미추가(D-05)"
      contains: "sentry-sdk"
  key_links:
    - from: "src/millie_rec/app/server.py"
      to: "os.environ SENTRY_DSN"
      via: "init_sentry() 조건 분기 — 없으면 return False(import 도 함수 안에서만)"
      pattern: "SENTRY_DSN"
    - from: "Dockerfile"
      to: "uv.lock"
      via: "uv sync --frozen 2회 — lock 미갱신이면 빌드 시점에만 실패"
      pattern: "uv sync --frozen --no-dev"
    - from: "make docker-up PORT=8001"
      to: "scripts/docker_up.sh"
      via: "이미지 빌드 → named volume /data → 3점 스모크(배포 02 §4-2 를 1명령으로)"
      pattern: "docker_up.sh"
---

<objective>
Phase 7 '배포'(.planning/ROADMAP.md "### Phase 7: 배포") 의 **코드 측 준비**. 이 페이즈에서 유일하게 코드가 바뀌는 플랜이다 — `sentry-sdk[fastapi]` 의존성 1개(07-CONTEXT D-04) + `app/server.py` Sentry 초기화(DSN 없으면 완전 비활성) + 그것을 단정하는 테스트 1파일. 그 뒤 push 선행 조건인 로컬 게이트 3종(pytest·smoke·docker 스모크, D-12)과 Dockerfile 3조건(D-11)·repo 위생(D-07·D-08) 을 기계적으로 확인하고, Codex 필수 리뷰(`../.claude/rules/codex-review.md` — Dockerfile·railway.json·배포 직전) 를 3분류 보고한 뒤 사용자 승인으로 **커밋만** 한다. push 는 07-02 에서.

Purpose: D-01 "스켈레톤을 지금 배포해 빌드·볼륨·도메인 리스크를 Day 4 이전에 드러낸다" 의 전제는 "push 할 코드가 로컬에서 Railway 와 같은 형태(Docker)로 뜬다" 는 사실이다. Sentry 를 스켈레톤에 함께 싣는 이유는 본배포(07-03) 에서 `SENTRY_DSN` 변수만 추가하면 되게 하려는 것(D-02 재배포는 push 한 번). PDF 기여: 축③ "컨테이너 1개가 프론트·API·SQLite 를 한 주소에서"(draft §4-3 배포·관측 행 — 이미 "Sentry(예외) · 가동 모니터" 로 적혀 있어 코드가 그 문장을 참으로 만든다).
Output: 코드 2파일(server.py 수정·test_sentry.py 신설) + pyproject/uv.lock + PROGRESS 1행. 커밋 1(사용자 승인 후). push 0.

**이 플랜을 막는 것:** 없음(wave 1). Docker Desktop 이 꺼져 있으면 Task 2 의 docker 스모크만 막힌다 — `scripts/docker_up.sh` 가 "docker 데몬에 연결할 수 없다" 로 종료하므로 Docker Desktop 실행 후 재시도.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy/07-CONTEXT.md
@.planning/phases/05-must/05-09-SUMMARY.md
@src/millie_rec/app/server.py
@tests/app/test_server_catalog.py
@tests/conftest.py
@pyproject.toml
@Dockerfile
@railway.json
@.dockerignore
@Makefile
@scripts/docker_up.sh
@../PROGRESS.md
@../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md
@../.claude/rules/local-run.md
@../.claude/rules/serving.md
@../.claude/rules/codex-review.md
@../.claude/rules/simplicity.md
@../.claude/rules/references.md

<interfaces>
<!-- 현재 app/server.py (38줄, Advisor 전용). 이 흐름은 한 글자도 바꾸지 않는다 — init_sentry() 정의 + 호출 1줄만 import 블록 뒤·catalog = load_catalog() 앞에 추가한다. -->
From src/millie_rec/app/server.py (현재):
```python
"""uvicorn 진입점: create_app 주입 + demo/ StaticFiles(마지막). CORS 없음(아키텍처 01 §9-3)."""
from fastapi.staticfiles import StaticFiles
from millie_rec.app.pipeline import build_pipelines, load_catalog
from millie_rec.contracts import ROOT
from millie_rec.ranking import state_weights
from millie_rec.serving import (Database, GlobalPopularFallback, ServingBookStats, StateStore, create_app, resolve_db_path)
# … 주석 8줄(Phase 3 D-13 · Phase 4 D-09 · Phase 5 D-07·D-08) …
catalog = load_catalog()
store = StateStore()
stats = ServingBookStats(catalog, store) if catalog is not None else None
app = create_app(pipelines=build_pipelines(catalog=catalog, weights=state_weights, book_stats=stats),
                 fallback=GlobalPopularFallback(catalog), catalog=catalog, db=Database(resolve_db_path()),
                 neighbors=catalog, book_stats=stats, weights=state_weights, state=store)
# 반드시 마지막 — 앞에 두면 /health·/api/* 가 StaticFiles 에 가려진다
app.mount("/", StaticFiles(directory=ROOT / "demo", html=True), name="demo")
```

From tests/app/test_server_catalog.py (server 모듈을 실 아티팩트 없이 import 하는 관례 — 그대로 복제해 쓴다, 3곳째가 아니므로 `_shared` 금지):
```python
def _server(tmp_path: Path, monkeypatch, serving_dir: Path):
    monkeypatch.setenv(ENV_DATA_DIR, str(tmp_path))
    monkeypatch.setattr("millie_rec.app.pipeline.load_catalog", lambda: CatalogKR.load(serving_dir))
    monkeypatch.setattr("millie_rec.app.pipeline_kr.load_vectors_kr",
                        lambda *a, **k: VectorsKR.load(serving_dir / VECTORS_KR_NPZ))
    sys.modules.pop("millie_rec.app.server", None)
    return importlib.import_module("millie_rec.app.server")
```
fixture: `millie_serving_sample`(tests/conftest.py L93, 20권) — `_server(tmp_path, monkeypatch, millie_serving_sample)`.

From sentry-sdk 2.x (ctx7 확인 2026-09-06): `sentry_sdk.init(dsn=..., traces_sample_rate=0, send_default_pii=False, environment=...)` · `sentry_sdk.is_initialized() -> bool`(활성 클라이언트 유무). `dsn=None` 이면 SDK 가 스스로 `os.environ["SENTRY_DSN"]` 을 읽으므로 **우리 쪽에서 DSN 부재 시 init 자체를 부르지 않는다**(D-04 "완전 비활성").

From Makefile (이미 있음 — 추가 금지):
```
docker-up:    PORT=$(or $(PORT),8000) scripts/docker_up.sh   # 빌드 → named volume millie-rec-data → /data → 3점 스모크 → smoke: PASS
docker-down:  scripts/docker_down.sh                          # 컨테이너만 정리(볼륨·이미지 유지). 전부: scripts/docker_down.sh --all
smoke:        8010 포트 기동 → /health · / · /api/recommend?seeds=1,2,3&k=5 → smoke: PASS
```
</interfaces>

<project_rules_inline>
**로컬 기동(`../.claude/rules/local-run.md`):** "아티팩트·DB·네트워크 없이 기동한다. … 외부 호출(Railway·Sentry·Grafana)은 환경변수가 없으면 비활성." · "모든 브리프의 완료 기준에 로컬 스모크를 넣는다: `uv run pytest --no-header` + `make smoke`." · "배포는 로컬 통과 후에만. `/deploy-demo`는 `make smoke`·로컬 docker 스모크(배포 02 §4-2)를 선행 조건으로 한다."

**서빙 배포 절(`../.claude/rules/serving.md` "배포"):** "`Dockerfile`(명령 11줄 + 주석 2줄): `uv sync --frozen --no-dev --no-install-project` → `COPY src demo artifacts/serving` → **두 번째 `uv sync --frozen --no-dev`**(src-layout 설치) → `CMD uv run --no-sync uvicorn … --port ${PORT:-8000} --workers 1`. **`USER` 지시어 금지**(Railway 볼륨 root 소유). `.dockerignore`·`railway.json`(healthcheck `/health`, replica 1) 동반." · "환경변수 `DATA_DIR` `METRICS_TOKEN` `SENTRY_DSN` `FREEZE_BOOK_STATS`는 Railway Variables에만." · 숫자 규칙: "p95는 `bench.py`(로컬) → `results/latency.json`만."

**간결함(`../.claude/rules/simplicity.md`):** "의존성 추가는 Advisor 승인 + `PROGRESS.md` 기록. 이유가 한 줄로 설명돼야 한다." · "파일 150줄 이하" · "설정은 모듈 상단 대문자 상수. 설정 파일·pydantic Settings·환경변수 프레임워크 금지." · "함수 우선." · "**최소 변경**: 언급된 것만, 기존 시그니처·흐름 변경 금지." · 테스트: "`uv run pytest`만(맨 `pytest` = Python 3.9 함정), Red = `AssertionError`."

**Codex(`../.claude/rules/codex-review.md`):** "`Dockerfile` · `railway.json` · 배포 직전(Day 4) → **필수** — `/codex:review --base main --scope branch --background`" · "`contracts.py` · `serving/schemas.py` 변경 → 필수"(이 플랜은 무변경 — 해당 없음) · 표준 흐름: "검증 사다리 통과 → Codex 리뷰 → Advisor 가 지적을 **필수 fix / 토론 / 무시** 3분류로 ≤10줄 보고. 무시 사유 1줄씩 → **사용자가 승인한 항목만** 반영 → 사용자가 '커밋' 승인 → `git commit`. push(= Railway 자동 배포)는 별도 승인." · "필수에 해당하면 안내가 아니라 실행 후 3분류 보고까지가 완료 조건."

**참조 표기(`../.claude/rules/references.md`):** 번호 단독 금지 — `결정 '스켈레톤을 지금 한다'(.planning/phases/07-deploy/07-CONTEXT.md D-01)` · `Phase 7 '배포'(.planning/ROADMAP.md)` 처럼 이름 + 경로.

**아키텍처(`../.claude/rules/architecture.md`):** `app/` 은 Advisor 전용. `app`은 다른 슬라이스의 공개 이름만 import. `tests/`는 무엇이든 import 가능. `tests/test_architecture.py` 를 고치지 않는다.
</project_rules_inline>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sentry 초기화 — 테스트 먼저(Red) → sentry-sdk[fastapi] 의존성 → app/server.py init_sentry() (Green, ≤50줄)</name>
  <files>tests/app/test_sentry.py, pyproject.toml, uv.lock, src/millie_rec/app/server.py, ../PROGRESS.md</files>
  <read_first>
    - src/millie_rec/app/server.py 전체(38줄 — 어디에 무엇을 넣는지: import 블록 뒤, `catalog = load_catalog()` 앞)
    - tests/app/test_server_catalog.py L1-30(`_server` 헬퍼 — 실 아티팩트 없이 server 모듈을 import 하는 관례)
    - .planning/phases/07-deploy/07-CONTEXT.md D-04(Sentry 유지 — 옵션 3개)·D-05(prometheus-client 금지)·"Claude's Discretion"(초기화 위치·environment 값)
    - ../PROGRESS.md "## 결정 로그" 표 마지막 행(형식 `| 날짜 | 결정 | 이유 | 영향 |`)
  </read_first>
  <behavior>
    - Test 1 `test_no_dsn_never_calls_sentry_init`: `monkeypatch.delenv("SENTRY_DSN", raising=False)` + `monkeypatch.setattr("sentry_sdk.init", spy)` 후 `_server(...)` 로 server 모듈을 새로 import → `spy.calls == []` 이고 `sentry_sdk.is_initialized() is False`
    - Test 2 `test_dsn_inits_once_with_no_tracing_no_pii`: `monkeypatch.setenv("SENTRY_DSN", "https://examplePublicKey@o0.ingest.sentry.io/0")` + 같은 spy → import 후 `len(spy.calls) == 1` 이고 kwargs 가 `dsn == 그 값`, `traces_sample_rate == 0`, `send_default_pii is False`, `"environment" in kwargs`(값은 문자열). spy 가 `sentry_sdk.init` 을 대체하므로 실제 초기화·네트워크 0
    - Test 3 `test_server_under_50_lines`: `len(Path(millie_rec.app.server.__file__).read_text().splitlines()) <= 50`
    - Red 확인: `uv run pytest tests/app/test_sentry.py --no-header` 가 `ModuleNotFoundError: sentry_sdk` 또는 `AssertionError` 로 실패해야 한다(둘 다 Red 로 인정 — 의존성이 아직 없다)
  </behavior>
  <action>
    **1-a(Red):** `tests/app/test_sentry.py` 를 위 `<behavior>` 3개로 작성. `_server` 헬퍼는 `tests/app/test_server_catalog.py` 의 6줄을 그대로 복제(2곳째 — `_shared` 금지, simplicity.md "중복 < 결합"). spy 는 `calls: list[dict]` 를 모으는 클로저 1개. `uv run pytest tests/app/test_sentry.py --no-header` → 실패 출력을 SUMMARY 에 verbatim.

    **1-b(의존성, D-04):** `pyproject.toml` `dependencies` 리스트 끝에 `"sentry-sdk[fastapi]>=2.0",` 1줄. `uv lock && uv sync` → `uv.lock` 갱신(Dockerfile `--frozen` 이 이 lock 을 읽는다 — 빠뜨리면 Railway 빌드에서만 실패). `grep -c prometheus pyproject.toml` == 0 유지(D-05). `../PROGRESS.md` 결정 로그 표 끝에 1행 append(Edit 전 재읽기 — 다른 세션이 편집한다): `| 09-06 | **의존성 추가: \`sentry-sdk[fastapi]\` 1개** — \`SENTRY_DSN\` 있을 때만 \`app/server.py\` 가 초기화(traces 0·PII off). \`prometheus-client\`·Grafana 는 폐기 | 결정 'Sentry 유지'·'Grafana 폐기'(.planning/phases/07-deploy/07-CONTEXT.md D-04·D-05) — 결정 'Should 꼬리…'(개발일지 2026-09-06 파일 항목 D76) 부분 번복 | \`pyproject.toml\`·\`uv.lock\`·\`src/millie_rec/app/server.py\`·\`tests/app/test_sentry.py\` |`.

    **1-c(Green):** `src/millie_rec/app/server.py` — import 블록에 `import os` 추가(표준 라이브러리 그룹, ruff I 정렬), 기존 주석 블록 뒤·`catalog = load_catalog()` 앞에 다음을 넣는다(Claude's Discretion 결정: 위치 = create_app 앞 모듈 상단, `environment` = Railway 가 주입하는 `RAILWAY_ENVIRONMENT_NAME` 이 있으면 그 값, 없으면 `"local"`):
    ```python
    def init_sentry() -> bool:
        """SENTRY_DSN 이 있을 때만 초기화 — 없으면 import 도 하지 않아 로컬·테스트는 네트워크 0(07-CONTEXT D-04)."""
        dsn = os.environ.get("SENTRY_DSN")
        if not dsn:
            return False
        import sentry_sdk  # 지연 import: 로컬 기동 경로에서 SDK 를 건드리지 않는다

        sentry_sdk.init(
            dsn=dsn,
            traces_sample_rate=0,  # 예외만, 성능 추적 없음
            send_default_pii=False,  # 가명 user_key 외 개인정보 없음(백엔드 §3-8)
            environment=os.environ.get("RAILWAY_ENVIRONMENT_NAME", "local"),
        )
        return True


    init_sentry()
    ```
    docstring 1줄 갱신: `"""uvicorn 진입점: Sentry(DSN 있을 때만) + create_app 주입 + demo/ StaticFiles(마지막). CORS 없음(아키텍처 01 §9-3)."""`. 기존 주석·`create_app(...)` 호출·`app.mount` 는 무변경. 50줄을 넘으면 함수 안 주석을 줄인다(기존 주석 블록은 건드리지 않는다). `uv run ruff format src/millie_rec/app/server.py tests/app/test_sentry.py && uv run ruff check src/millie_rec/app/server.py tests/app/test_sentry.py` 클린 → `uv run pytest tests/app/test_sentry.py tests/app --no-header` Green.

    **하지 않는 것:** `app/observability.py` 같은 새 파일(D-04 는 server.py 를 지정), `sentry_sdk` 최상단 import(로컬 경로 오염), `integrations=[...]` 명시(sentry-sdk 2.x 는 fastapi 설치 시 자동 활성 — 명시하면 starlette 도 함께 넣어야 해 줄 수만 늘어난다), `prometheus-client`, `Makefile` 타깃.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/app/test_sentry.py tests/app --no-header && test "$(wc -l < src/millie_rec/app/server.py)" -le 50 && grep -c 'sentry-sdk\[fastapi\]' pyproject.toml && test "$(grep -c prometheus pyproject.toml)" -eq 0 && grep -q 'sentry-sdk' uv.lock && grep -c 'sentry-sdk' ../PROGRESS.md && env -u SENTRY_DSN uv run python -c "import millie_rec.app.server, sentry_sdk; assert not sentry_sdk.is_initialized(); print('SENTRY_OFF_WITHOUT_DSN')"</automated>
  </verify>
  <acceptance_criteria>
    - Red 출력(ModuleNotFoundError 또는 AssertionError) 이 SUMMARY 에 verbatim 기록됨
    - `uv run pytest tests/app/test_sentry.py --no-header` → `3 passed`
    - `wc -l src/millie_rec/app/server.py` ≤ 50 · `git diff src/millie_rec/app/server.py` 에 `create_app(`·`app.mount(` 줄 변경 없음(추가만)
    - `grep -c 'sentry-sdk\[fastapi\]' pyproject.toml` == 1 · `grep -c prometheus pyproject.toml` == 0 · `grep -q sentry-sdk uv.lock` 성립
    - `env -u SENTRY_DSN uv run python -c "import millie_rec.app.server, sentry_sdk; assert not sentry_sdk.is_initialized()"` 종료 0(실 아티팩트로 기동해도 Sentry 비활성)
    - `../PROGRESS.md` 결정 로그에 `sentry-sdk` 행 1개(표 형식 유지)
  </acceptance_criteria>
  <done>테스트 3개 Green, server.py ≤50줄, 의존성 1개 + lock 갱신 + PROGRESS 1행. 커밋 없음(Task 3 에서).</done>
</task>

<task type="auto">
  <name>Task 2: push 선행 게이트 — Dockerfile 3조건·railway.json·repo 위생 + 로컬 게이트 3종(pytest·smoke·docker 스모크 + restart 영구성)</name>
  <files></files>
  <read_first>
    - Dockerfile(13줄)·railway.json·.dockerignore — D-11 3조건이 어디 줄인지
    - scripts/docker_up.sh(옵션 PORT·NO_CACHE·WAIT_S, 컨테이너 이름 `millie-rec-local`, 볼륨 `millie-rec-data`)
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §1-3 저장소 상태 · §2-1 주의 3가지 · §4-2 로컬 Docker 스모크 · §7 장애 대응표(빌드 실패 시 원인 매핑)
  </read_first>
  <action>
    아래 표를 순서대로 실행하고 출력 요약 줄을 SUMMARY 에 그대로 적는다. 하나라도 실패하면 Task 3 으로 가지 않는다. `make serve`(8000) 가 떠 있을 수 있으니 docker 는 **8001** 로 띄운다.

    | # | 명령 | 기대 |
    |---|---|---|
    | 1 | `grep -c 'uv sync --frozen --no-dev' Dockerfile` | `2`(D-11 ①) |
    | 2 | `grep -c '^USER ' Dockerfile` | `0`(D-11 ②) |
    | 3 | `grep -c 'PORT:-8000' Dockerfile` | `1`(D-11 ③) |
    | 4 | `jq -e '.deploy.healthcheckPath == "/health" and .deploy.numReplicas == 1 and .build.builder == "DOCKERFILE"' railway.json` | 종료 0 |
    | 5 | `grep -c '^!artifacts/serving' .dockerignore .gitignore` | 각 1 · `test -f artifacts/serving/.gitkeep` |
    | 6 | `git ls-files \| grep -icE "pdf\|png\|assets"` | `0`(DEPLOY-03, D-08 완화 없음) |
    | 7 | `git check-ignore -q report/figures/x.png && echo FIGURES_IGNORED` | `FIGURES_IGNORED`(D-07) |
    | 8 | `find artifacts/serving -size +50M \| wc -l` · `du -sh artifacts/serving` | `0` · ≈30M(D-09) |
    | 9 | `git ls-files uv.lock` | `uv.lock`(추적 중, Task 1 갱신분은 아직 미커밋 — 정상) |
    | 10 | `uv run ruff format --check . && uv run ruff check .` | 클린 |
    | 11 | `uv run pytest --no-header` | failed 0(기준선 = 05-09 SUMMARY 513 + Phase 6 신규 + 이 플랜 3 — 하드코딩하지 않고 요약 줄 verbatim) |
    | 12 | `make smoke` | `smoke: PASS` |
    | 13 | `make docker-up PORT=8001`(빌드 수 분 — `run_in_background` 허용, `WAIT_S=120`) | `smoke: PASS` + `/health` 줄에 `db_ok=True model_version=<freeze 버전>` |
    | 14 | `curl -fsS localhost:8001/health \| jq '{db_ok, model_version, artifacts_loaded_at, db_row_count}'` | `db_ok true`, `model_version` 비-null(아티팩트가 이미지에 실림 — D-09) |
    | 15 | `curl -fsS -X POST localhost:8001/api/preferences -H 'content-type: application/json' -d '{"user_key":"probe-docker-volume","seeds":[1,2,3,4,5]}' \| jq -c '.'` → `N1=$(curl -fsS localhost:8001/health \| jq '.db_row_count.users')` | `N1 ≥ 1` |
    | 16 | `docker restart millie-rec-local && sleep 8 && curl -fsS localhost:8001/health \| jq '.db_row_count.users'` | `== N1`(named volume 영구성 — Railway 볼륨의 로컬 대리 검증. **권한 문제는 여기서 안 잡힌다**(배포 02 §4-2 주의) — 그건 07-02 §3-2 에서) |
    | 17 | `curl -fsS "localhost:8001/api/recommend?model=hybrid_div&seeds=1,2,3,4,5" \| jq '{fallback_level, model_version, rows: [.rows[].row_id]}'` | `fallback_level 0` 이 아니어도 200 이면 통과(seeds cold-start 는 1행 trending 형태 — 결정 'seeds cold-start 는 1행 유지'(개발일지 2026-09-06 파일 항목 D78)) |
    | 18 | `curl -fsS -o /dev/null -w '%{http_code}\n' 'localhost:8001/?source=api'` | `200`(정적 데모 — Phase 6 진행 중이라 화면 완주는 07-03 게이트로 이관, 06-CONTEXT D-09) |
    | 19 | `docker image inspect millie-rec:local --format '{{.Size}}' \| awk '{printf "%.0f MB\n", $1/1e6}'` | 기록(빌드 시간과 함께 — Railway 빌드 예측치) |
    | 20 | `make docker-down` | `removed container millie-rec-local`(볼륨·이미지 유지) |

    실패 시 매핑(배포 02 §7): 13 에서 `ModuleNotFoundError: millie_rec` → 두 번째 uv sync 누락(1 이 2 가 아닐 것) · `unable to open database file` → DATA_DIR/볼륨 · 헬스체크 타임아웃 → PORT · `COPY artifacts/serving` 실패 → gitignore/.gitkeep. `uv.lock` 이 stale 이면 `uv sync --frozen` 이 빌드 단계에서 실패한다 → Task 1-b 재확인. 이 플랜은 Dockerfile 을 고치지 않는다 — 결함이면 SUMMARY 에 원인·기대·실제 3줄 + Task 3 Codex 리뷰의 필수 fix 후보로 올려 사용자 승인 후 Advisor 가 직접 고친다(5줄 이하).
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test "$(grep -c 'uv sync --frozen --no-dev' Dockerfile)" -eq 2 && test "$(grep -c '^USER ' Dockerfile)" -eq 0 && test "$(grep -c 'PORT:-8000' Dockerfile)" -eq 1 && jq -e '.deploy.healthcheckPath == "/health" and .deploy.numReplicas == 1' railway.json >/dev/null && test "$(git ls-files | grep -icE 'pdf|png|assets')" -eq 0 && git check-ignore -q report/figures/x.png && test "$(find artifacts/serving -size +50M | wc -l | tr -d ' ')" -eq 0 && uv run pytest --no-header && make smoke && echo LOCAL_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - 표 1~4: `2`·`0`·`1`·종료 0(D-11 3조건 + railway.json)
    - 표 6·7·8: `0`·`FIGURES_IGNORED`·`0`(DEPLOY-03 게이트, D-07·D-08·D-09)
    - 표 10~12: ruff 클린 · pytest 요약 줄 `failed` 없음 · `smoke: PASS`
    - 표 13·16: docker `smoke: PASS` · restart 후 `db_row_count.users == N1`(SUMMARY 에 N1 과 재시작 후 값, 빌드 시간·이미지 크기 기록)
    - 표 20: 컨테이너 정리됨(`docker ps -a --format '{{.Names}}' | grep -c millie-rec-local` == 0)
  </acceptance_criteria>
  <done>게이트 20행 전부 통과·요약 기록. Dockerfile·railway.json 무변경(`git diff --stat -- Dockerfile railway.json .dockerignore` 빈 출력).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Codex 필수 리뷰(Dockerfile·railway.json·server.py·pyproject) → 3분류 ≤10줄 → 사용자 승인분 반영 → 커밋 승인(push 없음)</name>
  <files></files>
  <read_first>
    - ../.claude/rules/codex-review.md "언제" 표(`Dockerfile`·`railway.json`·배포 직전 = 필수) · "표준 흐름" 1~6
    - .planning/phases/05-must/05-09-SUMMARY.md "Codex 리뷰 3분류" 절(보고 형식 선례)
  </read_first>
  <action>
    **자동 부분(사람 확인 전):** (1) `git status --short` 로 변경 파일이 `pyproject.toml`·`uv.lock`·`src/millie_rec/app/server.py`·`tests/app/test_sentry.py`·`../PROGRESS.md` 와 `.planning/**` 만인지 확인(`demo/fallback/popular.json` 의 M 은 Phase 6 세션 몫 — 이 커밋에 넣지 않는다). (2) `/codex:review --scope working-tree` 실행 — 리뷰 초점을 프롬프트에 명시: "Dockerfile 3조건(두 번째 uv sync·USER 없음·${PORT})·railway.json(healthcheck·replica 1)·app/server.py Sentry 초기화(DSN 없으면 비활성, PII off, traces 0)·pyproject 의존성 1개. Railway 볼륨 root 소유·단일 worker 전제." Dockerfile 자체는 diff 에 없으므로 `--scope working-tree` 가 Dockerfile 을 건너뛰면 `/codex:review --base 20d5fa3 --scope branch --background` 로 한 번 더(브랜치 전체 = Phase 6 커밋 + 이 변경; demo/ 지적은 "Phase 6 레인 — 06-07 인계" 로 분류). (3) 지적을 **필수 fix / 토론 / 무시** 3분류 ≤10줄로 정리(무시 사유 1줄씩). 필수 fix 후보 중 5줄 이하는 Advisor 직접 반영안, 그 이상은 수정 브리프 초안을 함께 제시. (4) 아래 `<how-to-verify>` 를 사용자에게 제시하고 멈춘다.

    **재개 후:** 사용자가 승인한 항목만 반영(최소 변경) → Task 1 `<verify>`·Task 2 표 10~12 재실행 → 사용자 "커밋 승인" 시 `git add pyproject.toml uv.lock src/millie_rec/app/server.py tests/app/test_sentry.py && git commit -m "feat(07-01): sentry-sdk[fastapi] + app/server.py Sentry 초기화(SENTRY_DSN 없으면 비활성) — 스켈레톤 배포 코드 준비"`. `../PROGRESS.md` 는 상위 폴더(다른 repo 아님 — `millie-rec/` 밖) 이므로 커밋 대상이 아니다. `.planning/**` 은 `docs(07-01): plan+summary` 로 별도 커밋(사용자 승인 동일 턴). **push 하지 않는다** — 07-02 Task 1 에서 사용자 승인 후 1회(push = Railway 자동 배포, D-03).
  </action>
  <what-built>
    Advisor 가 자동으로 마친 것: Task 1(Sentry 초기화 + 테스트 3개 Green + 의존성 1개 + PROGRESS 1행) · Task 2(게이트 20행 — Dockerfile 3조건·repo 위생·pytest·smoke·docker 스모크·restart 영구성) · Codex 리뷰 실행 + 3분류 표.
  </what-built>
  <how-to-verify>
    1. Codex 3분류 표(≤10줄)를 읽고 **필수 fix 중 반영할 것을 고른다**(번호로). 토론 항목은 한 줄 답만 주면 된다. Dockerfile 3조건(D-11) 을 바꾸자는 지적은 기본 무시 — 배포 02 §2-1 architect 검토에서 이미 잡힌 결함의 수정분이다.
    2. `git diff src/millie_rec/app/server.py` — 추가된 것이 `import os`·`init_sentry()` 함수·호출 1줄·docstring 1줄뿐인지(기존 `create_app(...)`·`app.mount` 무변경).
    3. `git diff pyproject.toml` — `sentry-sdk[fastapi]` 1줄만.
    4. Task 2 표 13·16 결과(docker `smoke: PASS`, restart 후 `db_row_count.users` 유지)와 이미지 크기·빌드 시간을 본다.
    5. 커밋 승인 여부. (push 는 다음 플랜 07-02 에서 따로 묻는다 — push 하면 Railway 가 없어도 원격 main 이 바뀌고, Railway 연결 뒤에는 자동 배포다.)
  </how-to-verify>
  <resume-signal>"approved: fix 1,3 / 커밋 ok" 형식 — 반영할 필수 fix 번호 + 커밋 승인. 문제가 있으면 항목별로 적는다</resume-signal>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && uv run pytest tests/app --no-header && make smoke && git log --oneline -1 | grep -q "07-01" && git status --short -- pyproject.toml uv.lock src/millie_rec/app/server.py tests/app/test_sentry.py | wc -l | grep -qx 0 && test "$(git rev-list --count origin/main..HEAD)" -ge 1 && echo COMMITTED_NOT_PUSHED</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 Codex 3분류 표(필수 fix / 토론 / 무시, ≤10줄) 와 사용자 승인 번호·반영 파일
    - 승인분 반영 후 `uv run pytest --no-header` failed 0 · `make smoke` PASS 재확인
    - `git log --oneline -1` 에 `07-01` 커밋 · `git rev-list --count origin/main..HEAD ≥ 1`(로컬에만 있음 — **push 0**)
    - `git status --short` 에 코드 4파일 없음(커밋됨) · `demo/fallback/popular.json` 은 이 커밋에 포함되지 않음(`git show --stat HEAD | grep -c demo/` == 0)
  </acceptance_criteria>
  <done>Codex 3분류 보고 완료 · 사용자 승인분 반영 · 커밋 1(push 0). 07-02 착수 조건 충족.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버 프로세스 → Sentry SaaS | 예외 이벤트가 외부로 나간다(DSN 있을 때만). 요청 본문·쿠키·IP 가 실릴 수 있는 경로 |
| pyproject/uv.lock → PyPI | 새 의존성 1개(공급망) |
| 작업 트리 → Codex(외부 모델) | 소스 diff 전송 |
| 로컬 docker 컨테이너 → 호스트 8001 | 로컬 전용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | `init_sentry()` — 예외 이벤트에 PII | mitigate | `send_default_pii=False`(요청 헤더·IP·쿠키 미전송) + 서버는 가명 `user_key` 만 다룬다(백엔드 §3-8). 테스트가 옵션을 단정 |
| T-07-01-02 | Information Disclosure | `SENTRY_DSN` 이 코드·repo 에 들어감 | mitigate | 환경변수만(Railway Variables). `grep -rn "ingest.sentry.io" src/ pyproject.toml` 은 테스트의 가짜 DSN(`o0/0`) 외 0 — Task 3 자동 부분에서 확인 |
| T-07-01-03 | Tampering | 공급망 — sentry-sdk 배포판 | accept | `uv.lock` 해시 고정 + `--frozen` 빌드. 5일 과제 범위에서 추가 검증 없음 |
| T-07-01-04 | Denial of Service | Sentry 전송 실패가 요청 지연에 전파 | accept | SDK 는 백그라운드 전송·`traces_sample_rate=0`. 로컬 bench(p95 79.5ms) 는 DSN 없이 측정된 정본 — Railway 에서 재측정하지 않는다(D-12) |
| T-07-01-05 | Repudiation | 게이트 통과 근거 재현 불가 | mitigate | SUMMARY 에 20행 출력 요약·pytest 요약 줄·이미지 크기·빌드 시간 verbatim |
</threat_model>

<verification>
- `uv run pytest tests/app/test_sentry.py --no-header` 3 passed · `env -u SENTRY_DSN … assert not sentry_sdk.is_initialized()` 종료 0
- `wc -l src/millie_rec/app/server.py` ≤ 50 · `grep -c prometheus pyproject.toml` == 0
- Task 2 표 20행 전부 기대값(SUMMARY 기록)
- Codex 3분류 ≤10줄 + 사용자 승인 번호 · 커밋 1 · `git rev-list --count origin/main..HEAD ≥ 1`(push 0)
</verification>

<success_criteria>
- **DEPLOY-03** 의 저작권 게이트(`git ls-files | grep -icE "pdf|png|assets"` == 0) 통과 확인(UptimeRobot 감시는 07-02)
- **DEPLOY-02** 의 전제 "로컬 `make smoke`·docker 스모크 통과 후 push" 중 로컬 부분 완료 — push 는 07-02
- ROADMAP Success Criteria 4("배포 전 로컬에서 `uv run pytest -q`·`make smoke`·docker 스모크가 모두 PASS") 충족
- 결정 'Sentry 유지'(07-CONTEXT D-04)·'Grafana 폐기'(D-05)·'Dockerfile 3조건 불변'(D-11)·'배포 전 로컬 게이트'(D-12) 이행 · Codex 필수 리뷰 완료(`../.claude/rules/codex-review.md`)
</success_criteria>

<output>
완료 후 `.planning/phases/07-deploy/07-01-SUMMARY.md`: Red 출력 verbatim · `3 passed` · server.py 줄 수 · 게이트 20행 결과표(N1·재시작 후 값·이미지 크기·빌드 시간 포함) · Codex 3분류 표 + 사용자 승인 번호·반영 diff 발췌 · 커밋 sha · "push 0 — 07-02 Task 1 에서 사용자 승인 후" 1줄 · 다음 플랜(07-02) 착수 조건 충족 여부.
</output>
