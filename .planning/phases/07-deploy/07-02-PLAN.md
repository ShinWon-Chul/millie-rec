---
phase: 07-deploy
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/STATE.md                # 배포 URL·리전·다운타임 실측 1~3줄(Current Position·Day 게이트 2)
# autonomous 이유: Task 1·Task 3 = 사용자 대시보드 작업(checkpoint:human-action)
autonomous: false
requirements: [DEPLOY-01, DEPLOY-03]
lane: 조립(Advisor 직접 — push·curl 검증·기록. 코드 0)
tdd_gate_evidence: none               # 코드 변경 없음 — 증거는 curl 출력·대시보드 확인
push_policy: "push 는 Task 1 에서 사용자 승인 후 1회(07-01 커밋), Task 3 에서 다운타임 측정용 빈 커밋 push 1회(사용자 승인 포함). 두 번 다 07-01 Task 2 로컬 게이트(pytest failed 0 · make smoke PASS · docker 스모크) 통과가 선행 조건(07-CONTEXT D-12)"
must_not_touch:
  - src/**                            # 코드 0 — 배포 실패는 원인만 기록, 수정은 07-01 재개(수정 브리프)
  - tests/**
  - demo/**                           # Phase 6 레인
  - artifacts/serving/**              # 🧊
  - Dockerfile · railway.json · .dockerignore · Makefile · pyproject.toml
  - results/**                        # Railway 에서 bench 금지 — p95 79.5ms 로컬 실측이 정본(D-12)
  - ../PROGRESS.md · ../.assets/**    # 기록은 07-04 에서 일괄(이 플랜은 STATE.md 만)

must_haves:
  truths:
    - "origin/main 에 07-01 커밋이 push 되고 Railway 가 Dockerfile 로 빌드해 배포 URL https://<name>.up.railway.app 이 생겼다(D-01 스켈레톤)"
    - "curl $BASE/health 가 200 이고 status ok · db_ok true · model_version 비-null(아티팩트가 이미지에 실림, D-09) · curl -o /dev/null -w %{http_code} $BASE/ 가 200(정적 데모) · $BASE/docs 가 Swagger HTML 이다(배포 02 §3-2)"
    - "Railway 대시보드 Restart 뒤 /health.db_row_count.users 가 재시작 전 값 이상이다 — 볼륨 /data 영구성(D-10, DEPLOY-01 '재배포 후 SQLite 유지')"
    - "재배포(빈 커밋 push) 중 /health 5초 폴링으로 다운 구간(초) 을 1회 실측해 STATE.md 에 적었다(D-14)"
    - "UptimeRobot 모니터가 $BASE/health 를 5분 간격으로 감시하고(DEPLOY-03, D-06) 공개 상태 페이지 URL 이 기록된다"
    - "Sentry 프로젝트(Python/FastAPI) 가 만들어져 DSN 이 비밀번호 관리자에 보관됐다 — Railway Variables 에는 아직 넣지 않는다(Day 4, 07-03)"
    - "STATE.md 에 BASE·리전·볼륨·다운타임·상태 페이지 URL 이 1~3줄로 기록된다(배포 02 §3-4 완료 기준)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Current Position 에 배포 URL 1줄 + Day 게이트 2 행 '배포 URL /health 200·볼륨 영구 확인' ✅ + 다운타임 초"
      contains: "up.railway.app"
  key_links:
    - from: "git push origin main"
      to: "Railway 빌드 → healthcheck /health → 도메인"
      via: "GitHub 앱 연동(§1-0 A) + Deploy from GitHub repo(§1-0 B)"
      pattern: "railway"
    - from: "Railway Variables DATA_DIR=/data + Volume /data"
      to: "serving/db.py resolve_db_path()"
      via: "contracts.ENV_DATA_DIR 해석 — 불일치면 db_ok false 또는 데이터 휘발"
      pattern: "DATA_DIR"
    - from: "UptimeRobot 모니터"
      to: "$BASE/health"
      via: "HTTP(s) 5분 간격"
      pattern: "health"
---

<objective>
Phase 7 '배포'(.planning/ROADMAP.md "### Phase 7: 배포") 의 **① 스켈레톤 배포**. 사용자가 브라우저에서 Railway 프로젝트를 만들고(결제·볼륨·변수·도메인 — 에이전트가 못 하는 일), Advisor 가 push·검증·기록을 맡는다. 배포되는 것은 Phase 5 까지의 서버(Must API 12개 전부) + 07-01 Sentry 초기화(DSN 없어 비활성) + 데모 현재 상태(Phase 6 진행 중 — 화면 완주는 이 플랜의 검증 대상이 아니다, 06-CONTEXT D-09 → 07-03).

Purpose: 결정 '스켈레톤을 지금 한다'(.planning/phases/07-deploy/07-CONTEXT.md D-01) — "빌드·볼륨·도메인 리스크를 Day 4 이전에 드러낸다". 배포 02 §0: "①이 성공하면 ②의 리스크는 코드 문제로 좁혀진다." **DEPLOY-01**(`/health` 200·정적 데모·볼륨 영구) 과 **DEPLOY-03** 의 UptimeRobot 절반이 여기서 끝난다. **D-14** 다운타임 실측 1회는 PDF 3장 "컨테이너 1개" 주장의 운영 현실 숫자다.
Output: 배포 URL 1개 · 검증 curl 출력 · 다운타임 초 · UptimeRobot 상태 페이지 URL · STATE.md 1~3줄. 코드 0. 커밋 = 빈 커밋 1(다운타임 측정용).

**이 플랜을 막는 것:** 07-01 커밋이 로컬에 있어야 한다(`git rev-list --count origin/main..HEAD ≥ 1`). 사용자 몫 A 묶음(Railway 가입 + GitHub 앱 `ShinWon-Chul/millie-rec` 접근 허용, UptimeRobot·Sentry 가입 — 배포 02 §1-0 A) 이 안 됐으면 Task 1 체크리스트 앞에 15분이 더 든다. **90분 규칙(D-13):** Task 1 시작부터 90분 안에 `/health` 200 이 안 나오면 멈추고 배포 02 §9 대체 경로(로컬 uvicorn + 캡처, PDF URL 자리 "로컬 데모, 면접 시 시연") 로 전환을 사용자에게 제안한다 — 배포 실패는 PDF 실패가 아니다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy/07-CONTEXT.md
@.planning/phases/07-deploy/07-01-SUMMARY.md
@railway.json
@../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md
@../.claude/rules/local-run.md
@../.claude/rules/serving.md
@../.claude/rules/references.md
@../.claude/skills/deploy-demo/SKILL.md

<interfaces>
From src/millie_rec/serving/schemas.py (`/health` 응답 — 볼륨 영구성·UptimeRobot 이 같은 엔드포인트):
```python
class HealthOut(_Strict):
    status: str
    api_version: str = API_VERSION
    model_version: str | None = None      # 아티팩트가 이미지에 있으므로 배포본은 비-null
    artifacts_loaded_at: str | None = None
    db_ok: bool
    db_row_count: dict[str, int] = {}     # 예: {"users": 1, "preference_snapshots": 1, "events": 0, "recommendations": 0}
    nearline_last_run: str | None = None
    uptime_s: float
```
`POST /api/preferences` 최소 본문: `{"user_key": "<str>", "seeds": [1,2,3,4,5]}` (`PreferencesRequest` — consent 기본 true) → `users`·`preference_snapshots` 1행씩 증가. 볼륨 영구성의 "표식 행" 으로 쓴다.

From railway.json: `healthcheckPath /health` · `healthcheckTimeout 120` · `restartPolicyType ON_FAILURE`(3회) · `numReplicas 1`. 대시보드에서 다시 설정할 필요 없음(코드로 고정).
</interfaces>

<project_rules_inline>
**로컬 기동(`../.claude/rules/local-run.md`):** "배포는 로컬 통과 후에만. `/deploy-demo`는 `make smoke`·로컬 docker 스모크(배포 02 §4-2)를 선행 조건으로 한다." — 07-01 Task 2 가 그 게이트다. 이 플랜에서 코드가 바뀌지 않았으므로 재실행은 `make smoke` 1회로 충분(Task 1 자동 부분).

**서빙 규칙(`../.claude/rules/serving.md`):** "환경변수 `DATA_DIR` `METRICS_TOKEN` `SENTRY_DSN` `FREEZE_BOOK_STATS`는 Railway Variables에만." · "Day 2 스켈레톤 배포 → Day 4 본배포. 재배포는 정지→시작(다운타임 실측 기록)." · 숫자 규칙: "서버 `latency_ms`는 PDF 숫자가 아니다. p95는 `bench.py`(로컬 uvicorn 1 worker) → `results/latency.json`만." → **Railway 에서 `make bench` 를 돌리지 않는다.**

**커밋·push(`../CLAUDE.md` 상위 §Advisor 경계 · 07-CONTEXT D-03):** "커밋/푸시는 항상 사용자 승인 후 실행." "이후 push = Railway 자동 재배포이므로 push 전 로컬 게이트 통과가 의무."

**참조 표기(`../.claude/rules/references.md`):** STATE.md·SUMMARY 기록도 이름 + 경로. `D-10` 단독 금지 → `결정 '볼륨은 /data 하나'(.planning/phases/07-deploy/07-CONTEXT.md D-10)`.

**Codex(`../.claude/rules/codex-review.md`):** 이 플랜은 코드 변경 0 → 리뷰 없음(07-01 Task 3 에서 완료). `demo/`·문서만 바뀌면 생략.
</project_rules_inline>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: push 승인 → Advisor push → 사용자: Railway 프로젝트 생성(배포 02 §1-0 B 묶음 · §3-1) → 배포 URL 회신</name>
  <files></files>
  <read_first>
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §1-0 A/B · §3-1(1~7) · §7 장애 대응표
    - .planning/phases/07-deploy/07-01-SUMMARY.md(커밋 sha · 이미지 크기·빌드 시간 — Railway 빌드 대기 예측)
  </read_first>
  <action>
    **자동 부분(사용자 확인 전):** (1) `git rev-list --count origin/main..HEAD` ≥ 1 이고 `git log --oneline origin/main..HEAD` 에 07-01 커밋이 보이는지. (2) 로컬 게이트 재확인(코드 무변경이라 축약): `uv run pytest tests/app --no-header && make smoke` → PASS. (3) `git status --short | grep -v '^??' | grep -v '^ M demo/fallback/popular.json'` 이 비어 있는지(커밋되지 않은 코드 변경이 push 에 섞이지 않게; `demo/fallback/popular.json` M 은 Phase 6 세션 몫 — 건드리지 않는다). (4) 아래 `<how-to-verify>` 체크리스트를 사용자에게 제시하고 **"push ok" 를 받은 뒤** `git push origin main` 을 실행한다(push 는 되돌릴 수 없는 공개 행위 + 자동 배포 트리거). push 출력(`20d5fa3..<sha>  main -> main`) 을 기록하고 사용자가 B 묶음을 진행하는 동안 대기한다.

    **재개 후(URL 회신):** `BASE=https://<name>.up.railway.app` 를 SUMMARY·STATE.md 에 적고 Task 2 로.
  </action>
  <what-built>
    Advisor 가 마친 것: 07-01 커밋(sentry-sdk + server.py 초기화 + 테스트) 로컬 게이트 통과 확인. "push ok" 뒤 `git push origin main` 실행 → 원격 main 갱신.
  </what-built>
  <how-to-verify>
    **0. push 승인** — "push ok" 라고 답하면 Advisor 가 `git push origin main` 을 실행한다. (Railway 프로젝트가 아직 없으므로 이 push 자체는 GitHub 만 바꾼다. 프로젝트를 만들면 그 시점의 main 으로 첫 빌드가 돈다.)

    **A. 아직 안 했으면 먼저(무료 · 카드 불필요 · 15분 — 배포 02 §1-0 A):**
    - [ ] UptimeRobot 가입(uptimerobot.com, 이메일) — 모니터 추가는 URL 이 나온 뒤(Task 3)
    - [ ] Sentry 가입 → Create Project → Platform `Python` → Framework `FastAPI` → **DSN 복사해 비밀번호 관리자에 보관**. Railway Variables 에는 Day 4(07-03) 에 넣는다
    - [ ] Railway 가입(railway.com, **GitHub 로그인**) → 가입 흐름 또는 `Account Settings → Integrations → GitHub` 에서 **GitHub 앱 설치 → repository access 에 `ShinWon-Chul/millie-rec` 허용**

    **B. push 직후(카드 필요 · 10분 + 빌드 대기 — 배포 02 §1-0 B = §3-1 그대로):**
    1. Railway **Hobby 플랜 결제**(카드) — 이 시점부터 월 $5 구독 시작
    2. 계정 **Settings → Usage → Usage Limit = $15/월**(하드 캡) + 알림 이메일 켬
    3. **New Project → Deploy from GitHub repo → `millie-rec`** 선택(A 에서 허용했으면 바로 보임; 안 보이면 Configure GitHub App 에서 접근 추가) → **Dockerfile 자동 감지** 확인(빌더가 Dockerfile 로 표시)
    4. 서비스 → **Settings**
       - Region: `Singapore`(한국에서 지연 최소) 또는 기본값
       - Networking → **Generate Domain** → `https://<name>.up.railway.app` 기록
       - Deploy → Health Check Path `/health`(railway.json 이 있으므로 자동 — 확인만)
       - **App Sleeping: OFF**(기본) — 확인만
    5. 서비스 → **Volumes → New Volume** → Mount path `/data`, 크기 1GB
    6. 서비스 → **Variables** → `DATA_DIR=/data`(스켈레톤은 이것만. `SENTRY_DSN` 은 아직 넣지 않는다)
    7. Deployments 탭에서 첫 빌드 로그(uv 설치 → sync → 이미지 → 헬스체크 통과) 를 지켜본다. 실패하면 로그 마지막 20줄을 붙여 준다 — 배포 02 §7 표로 원인을 매핑한다(`ModuleNotFoundError: millie_rec` → 두 번째 uv sync / `unable to open database file` → 볼륨·DATA_DIR / 헬스체크 실패 반복 → PORT / `COPY artifacts/serving` 실패 → .gitkeep)

    **하지 말 것:** 커스텀 도메인 구입 · Railway CLI 설치 · 프로젝트 전에 Volume/Variables 미리 만들기(프로젝트 없으면 못 만들고, 만들면 결제 시작)
  </how-to-verify>
  <resume-signal>먼저 "push ok". 끝나면 "BASE=https://<name>.up.railway.app, region=<Singapore|…>" 한 줄(빌드 실패면 로그 마지막 20줄)</resume-signal>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && git fetch -q origin && test "$(git rev-list --count origin/main..HEAD)" -eq 0 && git log --oneline -3 origin/main | grep -q "07-01" && echo PUSHED</automated>
  </verify>
  <acceptance_criteria>
    - 사용자 "push ok" 후 `git push origin main` 출력 기록 · `git rev-list --count origin/main..HEAD` == 0
    - 사용자 회신에 `BASE=https://….up.railway.app` 와 리전
    - Railway 첫 빌드 성공(사용자 확인) — 실패 시 §7 매핑 결과와 조치(07-01 재개 수정 브리프) 를 SUMMARY 에
    - Task 1 시작 시각 기록(90분 규칙 D-13 판정 기준)
  </acceptance_criteria>
  <done>원격 main = 로컬 HEAD · Railway 서비스 1개 · 볼륨 /data · DATA_DIR=/data · 도메인 1개 · BASE 확보.</done>
</task>

<task type="auto">
  <name>Task 2: 배포 URL 검증(배포 02 §3-2 curl 묶음) + 볼륨 표식 행 1개 + 기준값 기록</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §3-2 검증
    - .planning/STATE.md "## Current Position" · "### Day 게이트" 표 2행(편집 전 재읽기 — 다른 세션이 편집한다)
  </read_first>
  <action>
    `BASE` 를 Task 1 회신값으로 두고 순서대로 실행, 출력을 SUMMARY 에 verbatim:
    ```bash
    BASE=https://<name>.up.railway.app
    curl -fsS $BASE/health | jq '{status, api_version, model_version, artifacts_loaded_at, db_ok, db_row_count, nearline_last_run, uptime_s}'
    curl -s -o /dev/null -w "%{http_code}\n" $BASE/                    # 200 (demo index.html)
    curl -s -o /dev/null -w "%{http_code}\n" "$BASE/?source=mock"      # 200
    curl -s -o /dev/null -w "%{http_code}\n" "$BASE/?source=api"       # 200 (화면 완주는 07-03 — 06-CONTEXT D-09)
    curl -s $BASE/docs | head -c 200                                    # Swagger UI HTML
    curl -fsS "$BASE/api/recommend?seeds=1,2,3,4,5&k=5" | jq '{fallback_level, model_version, rows: [.rows[].row_id], latency_ms}'
    curl -fsS $BASE/api/showcase | jq '.eval_table.split_mode, (.eval_table.rows | length)'
    # 볼륨 표식 행 — Task 3 의 Restart 뒤 남아 있어야 한다
    curl -fsS -X POST $BASE/api/preferences -H 'content-type: application/json' \
      -d '{"user_key":"probe-volume-20260906","seeds":[1,2,3,4,5]}' | jq -c '{user_key, snapshot_id}'
    N_BEFORE=$(curl -fsS $BASE/health | jq '.db_row_count.users'); echo "users_before_restart=$N_BEFORE"
    ```
    기대: `status "ok"` · `db_ok true` · `model_version` 비-null(D-09 — 아티팩트가 이미지에 있다) · `/`·`?source=mock`·`?source=api` 전부 200 · `/docs` 가 `<!DOCTYPE html>` 로 시작 · `/api/recommend` 200(seeds cold-start 는 1행 `trending`, level 은 0 이 아닐 수 있음 — 결정 'seeds cold-start 는 1행 유지'(개발일지 2026-09-06 파일 항목 D78)) · `/api/showcase` `split_mode "holdout"`·rows 4 · `N_BEFORE ≥ 1`.
    **`latency_ms` 는 참고값** — SUMMARY 에 적되 PDF·draft 에 쓰지 않는다(`../.claude/rules/serving.md` 숫자 규칙, D-12).
    `db_ok false` 면 배포 02 §7 "볼륨 권한(비root) 또는 마운트 경로 불일치" — Dockerfile `USER` 0 은 07-01 에서 확인했으므로 Variables `DATA_DIR=/data` 와 Volume mount path `/data` 일치를 사용자에게 다시 확인(Task 3 체크리스트 앞에 끼운다).
    **STATE.md(재읽기 후 최소 편집):** "## Current Position" 아래에 `Deploy: BASE=<url> (region <…>, volume /data 1GB, 스켈레톤 2026-09-06)` 1줄 추가. Day 게이트 표 2행의 "배포 URL `/health` 200·볼륨 영구 확인 ⬜" 는 Task 3 통과 뒤 ✅ 로(여기서는 `/health` 200 ✅·볼륨 ⬜ 로 분리 표기).
  </action>
  <verify>
    <automated>test -n "$BASE" && curl -fsS "$BASE/health" | jq -e '.status == "ok" and .db_ok == true and .model_version != null and (.db_row_count.users >= 1)' >/dev/null && test "$(curl -s -o /dev/null -w '%{http_code}' "$BASE/")" = 200 && test "$(curl -s -o /dev/null -w '%{http_code}' "$BASE/?source=api")" = 200 && curl -fsS "$BASE/api/recommend?seeds=1,2,3,4,5&k=5" | jq -e '.rows | length >= 1' >/dev/null && curl -s "$BASE/docs" | head -c 200 | grep -qi "html" && grep -q "up.railway.app" "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec/.planning/STATE.md" && echo SKELETON_VERIFIED</automated>
  </verify>
  <acceptance_criteria>
    - `curl -fsS $BASE/health | jq -e '.status=="ok" and .db_ok and .model_version != null'` 종료 0
    - `/`·`/?source=mock`·`/?source=api` 모두 `200` · `/docs` HTML
    - `/api/recommend?seeds=…` 200 · `/api/showcase` `split_mode "holdout"`·rows 4
    - 표식 행 POST 200 · `N_BEFORE ≥ 1` 이 SUMMARY 와 STATE.md 에 기록
    - `grep -c "up.railway.app" .planning/STATE.md` ≥ 1
  </acceptance_criteria>
  <done>§3-2 curl 묶음 전부 기대값 · 표식 행 1개 · N_BEFORE 기록 · STATE.md BASE 1줄.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: 사용자: Restart(볼륨 검증) · UptimeRobot 모니터 + 상태 페이지 · Sentry DSN 보관 확인 · 빈 커밋 push 승인 → Advisor: 영구성 확인 + 재배포 다운타임 실측 → STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §3-2 "볼륨 검증"·"재배포 다운타임 측정" · §3-3 · §3-4 완료 기준
    - Task 2 의 `N_BEFORE`
  </read_first>
  <action>
    **자동 부분(사용자 확인 전):** Task 2 결과 요약(BASE·N_BEFORE) 을 붙여 아래 체크리스트를 제시한다. 사용자가 1(Restart) 을 마치고 "restart done" 을 주면 즉시:
    ```bash
    sleep 5; curl -fsS $BASE/health | jq '{db_ok, db_row_count, uptime_s}'
    N_AFTER=$(curl -fsS $BASE/health | jq '.db_row_count.users'); echo "users_after_restart=$N_AFTER  (before=$N_BEFORE)"
    ```
    `uptime_s` 가 작아졌고(실제 재시작) `db_ok true` · `N_AFTER ≥ N_BEFORE` 이면 **볼륨 영구성 확인**(D-10, DEPLOY-01). `N_AFTER < N_BEFORE` 또는 `db_ok false` → 볼륨이 `/data` 에 안 붙었거나 DATA_DIR 불일치 — 체크리스트 5·6 재확인 요청 후 재시도.

    **재개 후(사용자 "push ok" 포함 회신):** 재배포 다운타임 실측(D-14, 배포 02 §3-2) — 폴링을 **먼저** 백그라운드로 띄우고 그 다음 push:
    ```bash
    LOG="$SCRATCH/downtime.log"   # 스크래치패드(repo 밖)
    ( while true; do printf '%s %s\n' "$(date +%T)" "$(curl -s -o /dev/null -m 4 -w '%{http_code}' $BASE/health)"; sleep 5; done ) > "$LOG" 2>&1 &  POLL=$!
    git commit --allow-empty -m "chore(07-02): redeploy downtime probe" && git push origin main
    # Railway 빌드(캐시 시 수 분) → 배포 전환. 새 배포 Active 확인 후 60초 더 폴링하고 종료
    # kill $POLL
    awk '$2 != 200 {n++} END {print "non-200 samples:", n+0, "→ downtime ≈", (n+0)*5, "s (5s 간격, 상한)"}' "$LOG"
    grep -v ' 200$' "$LOG" | head -3; grep -v ' 200$' "$LOG" | tail -1
    ```
    다운 구간 = 첫 non-200 시각 ~ 마지막 non-200 시각(+5초 해상도). **Railway 볼륨 서비스는 정지→시작이므로 0초가 아니어야 정상**이다(아키텍처 01 §2-1 "수십 초"). 0 이면 폴링이 빌드 중에 끝났거나 새 배포가 아직 Active 가 아니다 — Deployments 탭 Active 시각을 기준으로 폴링 범위를 확인. 결과를 SUMMARY 에 `downtime_s=<n> (samples non-200 <k>, 5s 간격, 빌드 시간 <m>분)` 형식으로 기록.
    **STATE.md(재읽기 후):** Day 게이트 2행 "배포 URL `/health` 200·볼륨 영구 확인" → ✅(09-06) + `재배포 다운타임 ≈ <n>s` · Current Position Deploy 줄에 `status=<UptimeRobot 상태 페이지 URL>` 추가. `../PROGRESS.md`·개발일지·`report/draft.md`·아키텍처 01 §2-1 문구 교체는 **07-04** 에서 일괄.
  </action>
  <what-built>
    Advisor 가 마친 것: §3-2 curl 검증 전부 통과(Task 2) · 볼륨 표식 행 `probe-volume-20260906` 1개 · `users_before_restart=N_BEFORE`. 이제 대시보드 작업 3개와 push 승인 1개가 필요하다.
  </what-built>
  <how-to-verify>
    1. **볼륨 검증(배포 02 §3-2):** Railway → 서비스 → 우상단 ⋮ → **Restart**. 재시작이 끝나면(Deployments 탭 Active) "restart done" 이라고 답한다 → Advisor 가 `/health.db_row_count.users` 가 `N_BEFORE` 이상인지 확인한다.
    2. **UptimeRobot 모니터(배포 02 §3-3):** UptimeRobot → **Add New Monitor** → Monitor Type `HTTP(s)` → Friendly Name `millie-rec /health` → URL `<BASE>/health` → Monitoring Interval **5 minutes** → Alert Contacts 이메일 체크 → Create. 그 다음 **Status Pages → Add Status Page** → 이 모니터 1개 선택 → 공개 URL(`https://stats.uptimerobot.com/<id>`) 을 기록해 회신한다.
    3. **Sentry DSN 보관 확인:** Sentry 프로젝트(Python/FastAPI) 의 **Settings → Client Keys (DSN)** 에 DSN 이 있고 비밀번호 관리자에 저장돼 있는지만 확인. **Railway Variables 에는 넣지 않는다** — Day 4 본배포(07-03) 에서 코드 push 와 한 번에(변수 변경도 재배포를 유발한다, 배포 02 §3-3).
    4. **빈 커밋 push 승인(다운타임 실측용):** Advisor 가 `git commit --allow-empty -m "chore(07-02): redeploy downtime probe"` 후 `git push origin main` 을 실행해 재배포를 1회 유발하고 `/health` 를 5초 간격으로 폴링해 다운 구간(초) 을 기록한다. 이 push 는 코드 변경 0 이다. 승인하면 "push ok".
    5. (선택, 재배포 소음 줄이기) Railway → 서비스 → Settings → Build → **Watch Paths** 에 `!.planning/**`·`!report/**`·`!**/*.md` 를 추가하면 문서만 바뀐 push 는 재배포되지 않는다. 안 해도 동작에는 영향 없음 — 07-04 문서 커밋 push 시 다운타임이 한 번 더 생기는 것만 감수.
  </how-to-verify>
  <resume-signal>순서대로: "restart done" → (Advisor 확인 후) "status=https://stats.uptimerobot.com/<id>, sentry dsn saved, push ok"</resume-signal>
  <verify>
    <automated>test -n "$BASE" && curl -fsS "$BASE/health" | jq -e --argjson n "${N_BEFORE:-1}" '.db_ok == true and (.db_row_count.users >= $n)' >/dev/null && cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && git log --oneline -1 origin/main | grep -q "downtime probe" && grep -q "다운타임" .planning/STATE.md && grep -q "stats.uptimerobot.com" .planning/STATE.md && echo VOLUME_AND_DOWNTIME_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - Restart 후 `db_ok true` · `uptime_s` 감소(실제 재시작) · `N_AFTER ≥ N_BEFORE`(SUMMARY 에 두 값)
    - `git log --oneline -1 origin/main` = `chore(07-02): redeploy downtime probe` · 폴링 로그에서 `downtime_s` 1개(0 이 아닌 값이 기대 — 0 이면 폴링 범위 재확인 기록)
    - UptimeRobot 모니터 1개(URL `<BASE>/health`, 5분) — 사용자 확인 + 상태 페이지 URL `curl -s -o /dev/null -w '%{http_code}'` == 200
    - STATE.md 에 `다운타임`·`stats.uptimerobot.com` 문자열 · Day 게이트 2행 ✅
    - 배포 02 §3-4 완료 기준 6항 중 "PROGRESS.md 기록" 외 5항 ✅(PROGRESS 는 07-04)
  </acceptance_criteria>
  <done>볼륨 영구성 확인 · 다운타임 실측 1회 · UptimeRobot 감시 활성 + 상태 페이지 · Sentry DSN 보관 · STATE.md 갱신. 스켈레톤 배포 완료(DEPLOY-01 · DEPLOY-03 UptimeRobot 절반).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 인터넷 → `$BASE/api/*` | 인증 없는 공개 API(과제 범위 '안 함: 인증'). 가명 `user_key` 만 |
| Railway 빌드 → GitHub repo | 공개 repo main 을 그대로 빌드 — repo 에 시크릿·저작물이 없어야 한다 |
| 대시보드(사용자) → Variables·Volume | 시크릿·데이터 경로는 사람이 넣는다 |
| Sentry/UptimeRobot 계정 | 외부 SaaS — DSN·API 키는 비밀번호 관리자 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | 공개 repo 에 과제 PDF·캡처·`.assets` | mitigate | 07-01 Task 2 표 6·7(`git ls-files` grep 0 · `report/figures/` 무시) 통과 후에만 push(D-07·D-08) |
| T-07-02-02 | Information Disclosure | `DATA_DIR`·`SENTRY_DSN` 이 repo/로그에 | mitigate | Variables 에만(§1-4). `SENTRY_DSN` 은 이 플랜에서 넣지 않음. SUMMARY 에 DSN·토큰 문자열을 적지 않는다 |
| T-07-02-03 | Denial of Service | 공개 URL 남용으로 과금 폭증 | mitigate | Usage Limit **$15/월** 하드 캡(체크리스트 B-2) + `numReplicas 1` + UptimeRobot 알림. 초과 시 서비스가 멈추는 쪽을 택한다(PDF 는 완결, §9) |
| T-07-02-04 | Tampering | 익명 POST 로 SQLite 오염(표식 행 포함) | accept | 데모 데이터·가명 키. `DELETE /api/users/{key}/personalization` 으로 삭제 가능. 백업은 §5 주 1회(07-04 이후 운영) |
| T-07-02-05 | Elevation of Privilege | 컨테이너 root 실행(`USER` 없음) | accept | Railway 볼륨 root 소유 제약(D-11 ②). 단일 컨테이너·외부 프로세스 없음. 문서화(아키텍처 01 §5) |
| T-07-02-06 | Repudiation | 다운타임·영구성 숫자의 근거 | mitigate | 폴링 로그(스크래치) 요약·N_BEFORE/N_AFTER·Deployments 시각을 SUMMARY 에 verbatim |
</threat_model>

<verification>
- Task 1: `git rev-list --count origin/main..HEAD` == 0 · BASE 확보 · 90분 규칙 판정(시작~`/health` 200 소요 기록)
- Task 2: §3-2 curl 8개 기대값 · `N_BEFORE ≥ 1`
- Task 3: `N_AFTER ≥ N_BEFORE` · `downtime_s` 1개 · UptimeRobot 상태 페이지 200 · STATE.md 갱신
</verification>

<success_criteria>
- **DEPLOY-01** — `/health` 200·정적 데모 200·Restart 후 SQLite 유지(ROADMAP Success Criteria 1 전반, 다운타임 실측값 기록 포함)
- **DEPLOY-03** 후반 — UptimeRobot 이 `/health` 를 5분 간격으로 감시(전반 저작권 게이트는 07-01)
- 결정 '스켈레톤을 지금 한다'(07-CONTEXT D-01)·'볼륨은 /data 하나'(D-10)·'다운타임 실측 기록'(D-14)·'UptimeRobot 유지'(D-06) 이행
- 배포 02 §3-4 스켈레톤 완료 기준 6항 중 5항(PROGRESS 기록은 07-04)
</success_criteria>

<output>
완료 후 `.planning/phases/07-deploy/07-02-SUMMARY.md`: BASE·리전·볼륨 · Task 1 시작~`/health` 200 소요(90분 규칙) · push 출력 · §3-2 curl 출력 verbatim(`latency_ms` 는 "참고값" 표기) · `N_BEFORE/N_AFTER` · `downtime_s`(+빌드 시간·Active 시각) · UptimeRobot 모니터·상태 페이지 URL · Sentry DSN 보관 확인(문자열 미기재) · 빌드 실패가 있었다면 §7 매핑과 조치 · "PROGRESS·개발일지·draft 기록은 07-04" 1줄 · 07-03 착수 조건(Phase 6 완료 = `06-07-SUMMARY.md` 존재) 안내.
</output>
