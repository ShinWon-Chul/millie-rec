---
phase: 07-deploy
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - report/figures/p5_01_onboarding.png   # 캡처 6장 — .gitignore `report/figures/` 로 무시된다(D-07). 커밋 대상 아님
  - report/figures/p5_02_home.png
  - report/figures/p5_03_detail.png
  - report/figures/p5_04_reader_done.png
  - report/figures/p5_05_refresh.png
  - report/figures/p5_06_home_after.png
  - report/figures/p5_qr.png              # 배포 URL QR — 위와 같이 repo 밖
  - report/draft.md                       # §3-3 배포 실측 1문장 + §5-3 `[Phase 7]` 마커 → URL·QR·그림 6장
  - .planning/STATE.md                    # Day 게이트 2·4 · Current Position · Phase 7 완료
  - ../PROGRESS.md                        # 결정 로그 2행(배포 URL·다운타임 / DEPLOY-04 폐기) + 미결 5(계정) 해소
  - ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md   # ### D81 6요소 1항목(마지막이 D80)
  - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md  # §2-1 그림 주석 "수십 초 다운" → 실측 <n>초
# autonomous 이유: Task 2 = Codex 3분류 승인 · Task 4 = 문서·커밋 승인 체크포인트
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
lane: 조립(Advisor 직접 — 게이트·캡처·문서·Codex. 코드 0. Worker 위임 없음: 편집 대상이 전부 Advisor 전용 문서다)
tdd_gate_evidence: pytest-output          # 코드 무변경 — 증거는 전역 pytest 요약 줄 + make smoke + curl 출력
push_policy: "이 플랜은 push 를 기본으로 하지 않는다. 문서 커밋 1개(사용자 승인 후, Task 4). push 하면 Railway 재배포가 1회 더 일어나 심사 기간 다운타임이 생긴다(07-02 Task 3 선택 5의 Watch Paths 를 설정했다면 `.planning/**`·`report/**`·`**/*.md` 는 제외되어 재배포 없음) — push 여부는 사용자 결정. 승인 시 선행 조건은 Task 1 게이트 전부(uv run pytest --no-header failed 0 · make smoke PASS · $BASE/health 200)(07-CONTEXT D-12)"
must_not_touch:
  - src/**                              # 코드 0 — Day 5 는 PDF 숫자를 깨는 버그 수정만(simplicity.md)
  - tests/**
  - demo/**                             # Phase 6 산출물 그대로
  - artifacts/serving/**                # 🧊 make millie* 절대 실행 안 함
  - Dockerfile · railway.json · .dockerignore · Makefile · pyproject.toml
  - results/**                          # p95 79.5ms·비교표 4행이 PDF 숫자 정본 — 재측정·재생성 금지(Railway 에서 bench 안 돈다)
  - .planning/phases/07-deploy/07-01-PLAN.md · 07-02-PLAN.md · 07-03-PLAN.md · 07-CONTEXT.md   # 앞 플랜 문서는 SUMMARY 로만 보완

must_haves:
  truths:
    - "저작권 게이트가 배포 후에도 통과한다: git ls-files | grep -icE 'pdf|png|assets' == 0 이고 report/figures/ 의 캡처 7장이 git check-ignore 로 무시된다(DEPLOY-03 전반, 07-CONTEXT D-07·D-08)"
    - "UptimeRobot 이 $BASE/health 를 5분 간격으로 감시 중이고(모니터 Up · 마지막 체크 5분 이내 — 사용자 대시보드 확인) 공개 상태 페이지가 200 이다(DEPLOY-03 후반, D-06)"
    - "$BASE/health 가 200·db_ok true·api_version v2 로 살아 있고 2분 폴링 5회가 전부 200 이다(심사 기간 가동)"
    - "코드 무변경 상태에서 전역 게이트가 통과한다: uv run pytest --no-header failed 0 · make smoke PASS · git status 에 src/·tests/·demo/ 변경 0(D-12)"
    - "Codex 필수 리뷰 2회차(배포 전체 변경분 — ../.claude/rules/codex-review.md '브랜치 전체 vs main' 행) 가 실행되고 필수 fix / 토론 / 무시 3분류 ≤10줄로 보고되며 사용자 승인분만 반영된다"
    - "배포 URL 에서 캡처 6장(온보딩 → 메인 → 상세 → 뷰어 완독 → 재설정 → 메인 변화, ?capture=1, device_scale_factor=2)과 QR 1장이 report/figures/ 에 생기고 report/draft.md §5-3 의 `[Phase 7]` 마커가 실제 URL·그림 파일명으로 교체된다"
    - "기록 4곳이 남는다: ../PROGRESS.md 결정 로그 2행(배포 URL·다운타임 실측 / DEPLOY-04 폐기) · 개발일지 ### D81(6요소) · .planning/STATE.md Day 게이트 2·4 ✅ · report/draft.md §3-3 배포 실측 1문장"
    - "운영 인계가 07-04-SUMMARY.md 에 남는다: 심사 기간 점검 항목(배포 02 §5)·장애 대응 요약(§7)·롤백 1줄(§6)·철거 시점(§8)"
  artifacts:
    - path: "report/figures/p5_qr.png"
      provides: "배포 URL QR — Notion 조판 시 5장에 삽입(repo 밖, D-07)"
    - path: "report/draft.md"
      provides: "§5-3 `[Phase 7]` 마커 해소(배포 URL·QR·그림 6장) + §3-3 배포 실측 1문장(다운타임·볼륨 영구성)"
      contains: "up.railway.app"
    - path: "../PROGRESS.md"
      provides: "결정 로그 2행 + 미결 5(계정) 해소 표기"
      contains: "up.railway.app"
    - path: "../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md"
      provides: "### D81 6요소 1항목(문제·동기·선택지·근거·장애·결과, 12줄 이내)"
      contains: "### D81."
    - path: ".planning/STATE.md"
      provides: "Day 게이트 2·4 ✅ · Current Position 'Phase 7 배포 완료' · Phase 8 안내"
      contains: "Phase 7"
  key_links:
    - from: "report/figures/p5_qr.png"
      to: "$BASE"
      via: "qrcode 임시 설치(uv run --with) — 프로젝트 의존성 추가 아님(Makefile millie-collect 의 playwright 선례)"
      pattern: "up.railway.app"
    - from: "report/draft.md §5-3"
      to: "배포 URL · report/figures/p5_*.png"
      via: "`[Phase 7]` 마커 교체 — 그림은 repo 밖이라 파일명만 적고 조판 시 삽입"
      pattern: "\\[Phase 7\\]"
    - from: "../.assets/설계서/… 01_시스템_아키텍처_기술스택_배포.md §2-1"
      to: "07-02 의 downtime_s 실측"
      via: "'수십 초 다운' → '실측 <n>초(2026-09-06)' 1곳 교체(배포 02 §3-2 지시)"
      pattern: "다운"
---

<objective>
Phase 7 '배포'(.planning/ROADMAP.md "### Phase 7: 배포") 의 **마감 플랜**. 배포는 07-03 에서 끝났고 여기서는 (1) 배포 후에도 저작권·가동 게이트가 서 있는지 기계로 확인하고 (2) Codex 필수 리뷰를 배포 전체 변경분으로 한 번 더 닫고 (3) PDF 5장이 요구하는 **캡처 6장 + QR** 을 배포 URL 에서 뽑아 `report/draft.md` 의 `[Phase 7]` 마커를 해소하고 (4) 결정·다운타임 실측·**DEPLOY-04 폐기**를 네 곳(PROGRESS·개발일지 D81·STATE·설계서 §2-1)에 기록한 뒤 운영 인계를 남긴다. 코드는 한 줄도 바뀌지 않는다.

Purpose: Phase 7 의 산출물은 URL 자체가 아니라 **PDF 5장에 들어가는 URL·QR·그림 6장과, 그 배포가 살아 있다는 증거**다. 결정 'Grafana 폐기'(.planning/phases/07-deploy/07-CONTEXT.md D-05) 로 요구사항 DEPLOY-04 가 사라진 사실도 여기서만 문서에 남는다 — 남기지 않으면 Phase 8 'PDF 제출물' 에서 관측 문단이 다시 흔들린다. 결정 '다운타임 실측 기록'(D-14) 의 숫자는 PDF 3장 "컨테이너 1개" 주장의 운영 현실이다.
Output: 그림 7장(repo 밖) · draft 2곳 · 기록 4곳 · 커밋 1(문서, 사용자 승인 후) · push 0 또는 1(사용자 결정).

**이 플랜을 막는 것:** 07-03 완료(배포 URL 이 본배포 상태이고 사용자 "approved"). `$BASE` 가 죽어 있으면 Task 3 캡처가 불가 — 배포 02 §6 롤백(직전 성공 배포 Redeploy) 후 재시도하고, 90분을 넘기면 결정 '90분 규칙·대체 경로'(07-CONTEXT D-13, 배포 02 §9) 로 전환해 **로컬 `make serve` 화면으로 같은 6장을 캡처**하고 draft 의 URL 자리를 "로컬 데모, 면접 시 시연" 으로 바꾼다. 그 경우에도 이 플랜의 나머지(게이트·기록·인계)는 그대로 수행한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-deploy/07-CONTEXT.md
@.planning/phases/07-deploy/07-01-SUMMARY.md
@.planning/phases/07-deploy/07-02-SUMMARY.md
@.planning/phases/07-deploy/07-03-SUMMARY.md
@report/draft.md
@../PROGRESS.md
@../.assets/개발일지/README.md
@../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md
@../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md
@../.assets/설계서/화면 구성 및 디자인/02_화면_구성_및_명세.md
@../.claude/rules/local-run.md
@../.claude/rules/serving.md
@../.claude/rules/codex-review.md
@../.claude/rules/simplicity.md
@../.claude/rules/references.md

<interfaces>
<!-- 이 플랜이 편집할 곳의 현재 형태. 전부 재읽기 후 최소 편집(다른 세션이 같은 파일을 편집한다 — ../CLAUDE.md §6). -->

From report/draft.md L243 (§5-3 데모와 로드맵 — **해소 대상은 이 1곳뿐**. L3 의 `` `[Phase N]` = 채울 자리 `` 는 조판 범례라 **그대로 둔다** — 파일에 `[Phase` 는 2개, 그중 `[Phase 7]` 만 0 이 되어야 한다):
```
`[Phase 7]` 배포 URL·QR·스크린샷 6장(온보딩 7단계 → 메인 → 상세 → 뷰어 완독 → 재설정 → 메인 변화).
```

From report/draft.md §3-3 실서비스 ↔ 5일 데모 대응 (표 마지막 행 + 표 아래 실측 문단 1개가 이미 있다 — 배포 실측 문장을 그 뒤에 1줄 추가):
```
| Kubernetes | 컨테이너 1개 | 이미지·헬스체크·재배포 동일 |

- (Day 1 실측, Phase 3 '밀리 카탈로그 빌드') 데모 카탈로그 = … 이 스냅샷은 Day 3 freeze 대상이라 이후 재빌드하지 않는다.
```

From report/draft.md §3-2 기술 스택 표 배포·관측 행(현재 — 문구가 이미 맞다, 손대지 않는다):
```
| 배포·관측 | Docker 1컨테이너 · Sentry(예외) · 가동 모니터(5분 간격 헬스체크) | Prometheus → Grafana · 오토스케일링 | … 지표 수집기는 설계만 … |
```

From ../PROGRESS.md "## 결정 로그" 표 형식(마지막 행 뒤에 append):
```
| 날짜 | 결정 | 이유 | 영향 |
```
그리고 "## 미결" 5번: `**계정(사용자, Day 2 전까지 25분)** — Railway Hobby(카드 필수, Usage Limit $15 설정) · UptimeRobot · Sentry. Grafana Cloud는 Day 4. 절차: 배포 문서 02 §1·§3.` → 취소선 + 해소 표기 대상.

From ../.assets/개발일지/README.md 의사결정 6요소(고정 순서·항목당 12줄 이내):
`### D<n>. <제목>  [축 N · <문서> §X / <문서> §Y]  (확정 — <주체> <날짜>)` 뒤에
`- **문제:** / - **동기:** / - **선택지:** / - **근거:** / - **장애:** / - **결과:**` 6줄. 결과에는 **반영 위치 / 잃은 것 / 되돌리는 조건**.
마지막 항목은 `### D80. Phase 7 '배포' 범위 확정 …` (같은 파일 L91) — **새 번호는 D81**.

From ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md L74 (§2-1 그림 주석 — 실측 교체 1곳):
```
                                 ▲ git push main → Railway 자동 빌드·배포 (볼륨 서비스는 정지→시작, 수십 초 다운)
```
같은 문서 L79·L297 은 "다운타임은 02 §3-2에서 실측해 기록" 이라 문장 자체가 참이다 — **L74 한 곳만** 실측 숫자로 바꾼다.

From demo/js/app.js L236-238 (캡처 모드 — 화면 구성 02 §7):
```javascript
const cap = api.config.capture;
if (cap === "1") document.body.classList.add("is-capture");    // 폰만(상단바·인스펙터 숨김)
if (cap === "2") document.body.classList.add("is-capture-2");  // 폰 + 인스펙터
```
6장은 전부 `?capture=1`(폰만) · `device_scale_factor=2`. 07-03 Task 1-c 의 `walk.py`(스크래치패드) 가 완주 셀렉터를 이미 갖고 있다 — 그 스크립트를 복제해 `shoot.py` 로 만든다(repo 밖).

From ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §5 운영 표(인계 대상):
UptimeRobot 알림 즉시 → Deployments 로그 → §7 · Railway Usage 주 1회 · 백업 주 1회 · Day 5 이후 코드 금지(`FREEZE_BOOK_STATS=1`).
**주의:** §5 백업 행의 `GET /api/admin/export` 는 **미구현**(05-CONTEXT Deferred 유지) 이고 §5 의 `METRICS_TOKEN` 도 이 프로젝트엔 없다(D-05 폐기) — 인계 요약에 "백업은 Railway 볼륨에만 존재, 내보내기 API 없음" 으로 사실대로 적는다.
</interfaces>

<project_rules_inline>
**로컬 기동(`../.claude/rules/local-run.md`):** "모든 브리프의 완료 기준에 로컬 스모크를 넣는다: `uv run pytest --no-header` + `make smoke`." · "배포는 로컬 통과 후에만." → 이 플랜은 코드를 안 바꾸지만 **push 가능성이 있으므로** Task 1 에서 두 게이트를 다시 돌린다.

**서빙 숫자 규칙(`../.claude/rules/serving.md`):** "서버 `latency_ms`는 PDF 숫자가 아니다. p95는 `bench.py`(로컬 uvicorn 1 worker) → `results/latency.json`만. 관제 대시보드·Grafana 캡처는 '관측 존재'의 증거로만 쓰고 수치를 본문에 인용하지 않는다." → **캡처에 보이는 숫자를 draft 본문으로 옮기지 않는다.** `results/` 는 must_not_touch.

**간결함(`../.claude/rules/simplicity.md`):** "**최소 변경**: 언급된 것만, 기존 시그니처·흐름 변경 금지, 요청 없는 리팩토링 금지." · "Day 5: 코드 변경은 PDF 숫자를 깨는 버그 수정만." · "의존성 추가는 Advisor 승인 + `PROGRESS.md` 기록" → **`qrcode` 는 프로젝트 의존성으로 추가하지 않는다.** `uv run --with "qrcode[pil]"` 임시 설치로만 쓴다(`Makefile` `millie-collect` 의 `uv run --with playwright` 선례 — "dev 전용·런타임 미포함").

**Codex(`../.claude/rules/codex-review.md`):** "`Dockerfile` · `railway.json` · 배포 직전(Day 4) → **필수** — `/codex:review --base main --scope branch --background`" · "브랜치 전체 vs main → `/codex:review --base main --scope branch --background`" · 표준 흐름: "Advisor 가 지적을 **필수 fix / 토론 / 무시** 3분류로 ≤10줄 보고. 무시 사유 1줄씩 → **사용자가 승인한 항목만** 반영 → 사용자가 '커밋' 승인 → `git commit`. push(= Railway 자동 배포)는 별도 승인." · "필수에 해당하면 안내가 아니라 실행 후 3분류 보고까지가 완료 조건."

**참조 표기(`../.claude/rules/references.md`):** 번호 단독 금지. `DEPLOY-04` 는 `요구사항 'Sentry·Grafana Cloud scrape 연결'(.planning/REQUIREMENTS.md DEPLOY-04)` 처럼, `D80` 은 `결정 'Phase 7 배포 범위 확정'(개발일지 2026-09-06 파일 항목 D80)` 처럼. PROGRESS·개발일지·draft·SUMMARY 전부 적용.

**문서 소유(`../CLAUDE.md` §6 세션 규칙):** "다른 세션이 이 파일·설계서·rules 를 동시에 편집한다. **Edit 전 반드시 다시 읽는다.**" → `../PROGRESS.md`·개발일지·설계서 01 은 Edit 직전 재읽기 필수. 문서 이동·개명은 하지 않는다.
</project_rules_inline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 최종 게이트 — 저작권(DEPLOY-03)·가동 지속·전역 pytest·smoke·코드 무변경 확인</name>
  <files></files>
  <read_first>
    - .planning/phases/07-deploy/07-03-SUMMARY.md(`BASE` · 본배포 sha · `LOCAL_MV` · `N_AFTER` · 사용자 approved 시각)
    - .planning/phases/07-deploy/07-02-SUMMARY.md(`downtime_s` · UptimeRobot 상태 페이지 URL)
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §3-4 스켈레톤 완료 기준 · §4-4 검증 체크리스트 · §5 운영
  </read_first>
  <action>
    `BASE`·`STATUS_PAGE` 를 앞 SUMMARY 에서 읽어 셸 변수로 두고 아래를 순서대로 실행한다. 출력 요약 줄을 SUMMARY 에 verbatim. 하나라도 실패하면 Task 2 로 가지 않고 원인·기대·실제 3줄로 보고한다.

    | # | 명령 | 기대 |
    |---|---|---|
    | 1 | `git ls-files \| grep -icE "pdf\|png\|assets"` | `0`(DEPLOY-03 전반 — 배포 후에도 유지, D-08 완화 없음) |
    | 2 | `git check-ignore -q report/figures/p5_qr.png && echo FIGURES_IGNORED` | `FIGURES_IGNORED`(D-07 — Task 3 이 만들 파일이 커밋되지 않음을 **미리** 확인) |
    | 3 | `git status --short -- src/ tests/ demo/ artifacts/ results/ Dockerfile railway.json Makefile pyproject.toml \| wc -l` | `0`(코드·아티팩트·숫자 무변경) |
    | 4 | `git diff --stat origin/main -- src/ tests/ demo/` | 빈 출력(07-03 push 이후 로컬 미반영 코드 없음) |
    | 5 | `curl -fsS "$BASE/health" \| jq -e '.db_ok and .api_version == "v2" and .model_version != null'` | 종료 0 |
    | 6 | `for i in 1 2 3 4 5; do curl -s -o /dev/null -m 5 -w "%{http_code} " "$BASE/health"; sleep 25; done; echo` | `200 200 200 200 200`(약 2분 가동 지속) |
    | 7 | `curl -s -o /dev/null -w '%{http_code}\n' "$STATUS_PAGE"` | `200`(UptimeRobot 공개 상태 페이지 — 대시보드 "Up · 마지막 체크 5분 이내" 육안 확인은 Task 4) |
    | 8 | `curl -s -o /dev/null -w '%{http_code}\n' "$BASE/metrics"` | `404`(결정 'Grafana 폐기'(.planning/phases/07-deploy/07-CONTEXT.md D-05) 재확인) |
    | 9 | `uv run ruff format --check . && uv run ruff check .` | 클린 |
    | 10 | `uv run pytest --no-header` | `failed` 없음(요약 줄 verbatim — 기준선을 하드코딩하지 않는다) |
    | 11 | `make smoke` | `smoke: PASS` |
    | 12 | `curl -fsS "$BASE/health" \| jq '.db_row_count'` | `users` 가 07-03 SUMMARY 값 이상(볼륨 유지 재확인) |

    6 에서 비-200 이 섞이면 재배포 중이거나 헬스체크 실패다 — Railway Deployments 탭 상태를 사용자에게 확인 요청하고, 실패 배포면 배포 02 §6 롤백(직전 성공 배포 Redeploy) 을 제안한다. 10·11 은 코드를 안 바꿨으므로 실패하면 다른 세션의 미커밋 변경이 섞인 것 — 3 을 다시 본다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test "$(git ls-files | grep -icE 'pdf|png|assets')" -eq 0 && git check-ignore -q report/figures/p5_qr.png && test "$(git status --short -- src/ tests/ demo/ artifacts/ results/ Dockerfile railway.json Makefile pyproject.toml | wc -l | tr -d ' ')" -eq 0 && test -n "$BASE" && curl -fsS "$BASE/health" | jq -e '.db_ok and .api_version == "v2" and .model_version != null' >/dev/null && test "$(curl -s -o /dev/null -w '%{http_code}' "$BASE/metrics")" = 404 && uv run pytest --no-header && make smoke && echo FINAL_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - 표 1·2·3: `0` · `FIGURES_IGNORED` · `0`(DEPLOY-03 + D-07 + 코드 무변경)
    - 표 5·6: `jq -e` 종료 0 · 5회 폴링 전부 `200`(SUMMARY 에 그대로)
    - 표 7·8: 상태 페이지 `200` · `/metrics` `404`
    - 표 9~11: ruff 클린 · pytest 요약 줄에 `failed` 없음 · `smoke: PASS`
    - 표 12: `db_row_count.users` ≥ 07-03 값(두 값 모두 기록)
  </acceptance_criteria>
  <done>게이트 12행 통과·요약 기록. 코드·아티팩트·`results/` 무변경 증명.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Codex 필수 리뷰 2회차(배포 전체 변경분 = Phase 6 데모 + Phase 7) → 3분류 ≤10줄 → 사용자 승인분만 반영</name>
  <files></files>
  <read_first>
    - ../.claude/rules/codex-review.md "언제" 표(`Dockerfile`·`railway.json`·배포 직전 = 필수) · "표준 흐름" 1~6 · "상시 안내" 표
    - .planning/phases/07-deploy/07-01-SUMMARY.md(1회차 3분류·사용자 승인 번호 — 같은 지적을 두 번 올리지 않는다)
    - .planning/phases/07-deploy/07-03-SUMMARY.md(1-d 리뷰 결과 — "배포 파일 무변경" 이었는지)
  </read_first>
  <action>
    **자동 부분(사용자 확인 전):**
    (1) 리뷰 기준점을 확정한다: `BASE_SHA=$(git log --format=%h --grep='feat(phase1-5)' -1)`(= `20d5fa3`, Phase 1~5 일괄 커밋). 이 sha 이후가 **배포된 전체 변경분**(Phase 6 데모 재구성 + Phase 7 Sentry·문서)이다. 07-01·07-03 의 리뷰는 각각 작업 트리·07-01 이후 브랜치였으므로 **범위가 다르다** — 중복 실행이 아니다(`../.claude/rules/codex-review.md` "브랜치 전체 vs main" 행).
    (2) `/codex:review --base $BASE_SHA --scope branch --background` 실행. 프롬프트에 초점을 명시한다: "심사 기간 공개되는 단일 컨테이너 배포본이다. 초점 ① 인증 없는 공개 API 의 입력 검증·SQLite 오염 내성 ② `app/server.py` Sentry 초기화(DSN 없으면 완전 비활성·PII off) ③ `Dockerfile`/`railway.json` 배포 안전성(root 실행·replica 1·헬스체크) ④ `demo/` 정적 자산이 같은 origin 에서 서빙될 때의 문제. `/metrics`·prometheus 는 폐기됐으니 지적 대상 아님. 개인정보는 가명 `user_key` 만."
    (3) 지적을 **필수 fix / 토론 / 무시** 3분류 ≤10줄로 정리. 무시 사유 1줄씩. 07-01 에서 이미 무시로 분류된 항목이 다시 나오면 "1회차 무시 유지(사유 재사용)" 한 줄로 묶는다. 소유 레인을 함께 적는다(`demo/` = Phase 6 · `serving/` = Phase 5 · 배포 파일 = 이 페이즈).
    (4) `<how-to-verify>` 를 제시하고 멈춘다.

    **재개 후:** 사용자가 승인한 항목만 반영한다. **이 플랜은 `src/**`·`demo/**` 를 못 고친다(must_not_touch)** — 승인된 필수 fix 가 코드면 ① 심사 기간 서비스에 실제 위험인가를 먼저 판단해 ② 위험하면 소유 페이즈 수정 브리프 1개를 만들어 사용자에게 제시하고 이 플랜을 잠시 멈춘 뒤(수정 → 로컬 게이트 → push 는 별도 승인) ③ 위험이 아니면 "Phase 8 'PDF 제출물' 이후 인계" 로 `../PROGRESS.md` 미결에 1줄만 남긴다(Task 4 에서 함께 기록). 배포 파일(`Dockerfile`·`railway.json`) 의 5줄 이하 fix 만 Advisor 가 직접 고치고, 그 경우 Task 1 표 9~11 을 재실행한 뒤 push 승인을 따로 받는다(재배포 발생).
  </action>
  <what-built>
    Advisor 가 자동으로 마친 것: Task 1 게이트 12행(저작권 0 · 가동 5/5 · pytest failed 0 · smoke PASS · `/metrics` 404) · Codex 리뷰 실행(범위 = `20d5fa3` 이후 배포 전체) · 3분류 표 + 각 항목의 소유 레인.
  </what-built>
  <how-to-verify>
    1. Codex 3분류 표(≤10줄)를 읽고 **반영할 항목을 번호로** 고른다. 판단 기준 두 가지만 보면 된다 — "심사 기간(약 2주) 공개 URL 에서 실제로 문제가 되는가", "고치는 비용이 재배포 1회 값어치인가". 아니면 인계로 넘긴다.
    2. 기본 무시 후보(사유 재사용): Dockerfile 3조건 변경 제안(결정 'Dockerfile 3조건 불변'(.planning/phases/07-deploy/07-CONTEXT.md D-11) — architect 검토에서 이미 잡힌 결함의 수정분이다) · 인증 도입 제안(과제 범위 '안 함') · `/metrics` 관련(폐기) · 테스트 커버리지 요구(simplicity.md "커버리지 목표 없음").
    3. **UptimeRobot 대시보드 확인(DEPLOY-03 후반, 이 자리에서 함께):** UptimeRobot → 모니터 `millie-rec /health` 가 **Up** 이고 **Monitoring Interval 5 minutes**, 마지막 체크가 5분 이내인지. 가동률 %(재배포 포함)도 한 줄로 알려 준다 — 07-04-SUMMARY 운영 인계에 적는다.
    4. 답 형식: `"approved: fix 2 / 나머지 인계 / uptimerobot up 99.x% 5min"`. 반영할 것이 없으면 `"approved: none / uptimerobot …"`.
  </how-to-verify>
  <resume-signal>"approved: fix <번호들 또는 none> / uptimerobot <up|down> <가동률> <간격>" — 코드 필수 fix 가 있으면 수정 브리프 승인 여부도 한 줄</resume-signal>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && test "$(git status --short -- src/ demo/ tests/ | wc -l | tr -d ' ')" -eq 0 && uv run pytest --no-header && make smoke && curl -fsS "$BASE/health" | jq -e '.db_ok' >/dev/null && echo CODEX_ROUND2_CLOSED</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 Codex 3분류 표(필수 fix / 토론 / 무시, ≤10줄) + 각 항목 소유 레인 + 무시 사유 1줄씩
    - 리뷰 범위 기준 sha(`20d5fa3`) 와 1회차(07-01)·1-d(07-03) 와의 차이 1줄 명시
    - 사용자 승인 번호와 처리(직접 수정 / 수정 브리프 / 인계) 가 항목별로 기록
    - 반영이 있었다면 `uv run pytest --no-header` failed 0 · `make smoke` PASS 재확인, 없었다면 `git status --short -- src/ demo/ tests/` 빈 출력
    - UptimeRobot 상태(Up 여부·가동률·간격) 사용자 회신값 기록
  </acceptance_criteria>
  <done>Codex 필수 리뷰 마감(3분류 + 승인 처리) · UptimeRobot 감시 동작 사용자 확인 → DEPLOY-03 완결.</done>
</task>

<task type="auto">
  <name>Task 3: 배포 URL 캡처 6장(?capture=1, 2x) + QR 1장 → report/figures/(repo 밖) → report/draft.md §5-3 마커 해소 · §3-3 배포 실측 1문장</name>
  <files>report/figures/p5_01_onboarding.png, report/figures/p5_02_home.png, report/figures/p5_03_detail.png, report/figures/p5_04_reader_done.png, report/figures/p5_05_refresh.png, report/figures/p5_06_home_after.png, report/figures/p5_qr.png, report/draft.md</files>
  <read_first>
    - .planning/phases/07-deploy/07-03-SUMMARY.md(`walk.py` 스크래치패드 경로 — 완주 셀렉터·온보딩 7단계 클릭 순서를 그대로 복제한다)
    - .planning/phases/06-demo-rebuild/06-06-PLAN.md 수동 체크리스트(각 화면의 `data-act` 이름·기대 요소)
    - ../.assets/설계서/화면 구성 및 디자인/02_화면_구성_및_명세.md §7 PDF 캡처 계획(2x · `?capture=1` 폰만 / `?capture=2` 폰+인스펙터 · "D7·Grafana 수치는 본문 인용 금지")
    - report/draft.md L243 `[Phase 7]` 마커 · §3-3 표 아래 실측 문단(편집 전 재읽기)
    - .planning/phases/07-deploy/07-02-SUMMARY.md `downtime_s`
  </read_first>
  <action>
    **3-a 캡처 스크립트(스크래치패드, repo 밖):** 07-03 의 `walk.py` 를 복제해 `shoot.py` 로 만든다. `?source=api&capture=1` 로 배포 URL 을 열고 `device_scale_factor=2`·`viewport 420x900`(폰 프레임) 으로 6장을 찍는다. 파일명·화면은 draft §5-3 의 6장 순서 그대로:

    | 파일 | 화면(화면 구성 02 §2) | 찍는 시점 |
    |---|---|---|
    | `p5_01_onboarding.png` | 취향 설정 화면(`#/onboarding`) | 7단계 중 책 5권 선택 단계(진행바가 보이는 화면) |
    | `p5_02_home.png` | 메인 화면(`#/home`) | 온보딩 완료 직후 — 앵커 행 "『○○』을 좋아하셨다면" 이 보이게 |
    | `p5_03_detail.png` | 책 상세 화면(`#/book/:id`) | 앵커 행 첫 카드 클릭 |
    | `p5_04_reader_done.png` | 뷰어 시뮬레이션 화면(`#/reader/:id`) | **완독 처리 직후**(완독 상태 UI) |
    | `p5_05_refresh.png` | 취향 재설정 화면(`#/refresh`) | 새 5권 선택 화면 |
    | `p5_06_home_after.png` | 메인 화면(`#/home`) | 재설정 후 — 01·02 와 카드가 달라진 상태 |

    각 캡처 전 `page.wait_for_selector(...)` + `wait_for_timeout(600)` 로 렌더를 기다리고, 콘솔 에러를 계속 수집해 마지막에 `console_errors` 를 출력한다(0 이어야 한다 — 07-03 게이트와 동일 기준). 저장 경로는 `report/figures/`(절대경로). 스크립트는 스크래치패드에 두고 경로를 SUMMARY 에 적는다 — `demo/`·`src/`·`scripts/` 에 넣지 않는다.
    ```bash
    uv run --with playwright python "$SCRATCH/shoot.py" "$BASE" "$(pwd)/report/figures"
    ```
    **90분 규칙 대체 경로(D-13, 배포 02 §9):** `$BASE` 가 죽었거나 캡처가 막히면 `make serve` 후 `http://localhost:8000` 을 같은 스크립트에 넣어 6장을 만들고, SUMMARY·draft 에 "로컬 캡처" 를 명시한다.

    **3-b QR(의존성 추가 없음):**
    ```bash
    uv run --with "qrcode[pil]" python -c "import sys, qrcode; qrcode.make(sys.argv[1]).save(sys.argv[2])" "$BASE" report/figures/p5_qr.png
    ```
    `uv run --with` 는 일회성 환경이라 `pyproject.toml`·`uv.lock` 이 바뀌지 않는다(`Makefile` `millie-collect` 의 playwright 선례). 실행 후 `git status --short -- pyproject.toml uv.lock` 이 빈 출력인지 확인한다.

    **3-c repo 밖 확인(D-07·D-08):** `git status --short -- report/` 에 `p5_*.png` 가 **나타나지 않아야** 한다(`report/figures/` 무시). `git ls-files | grep -icE "pdf|png|assets"` 가 여전히 `0`.

    **3-d draft.md 편집 2곳(재읽기 후 최소 변경):**
    - **§5-3 L243** 의 `` `[Phase 7]` 배포 URL·QR·스크린샷 6장(…) `` 한 줄을 실제 값으로 교체한다. 예(값은 실측으로):
      `데모: <BASE> (QR 이미지 report/figures/p5_qr.png — 조판 시 삽입). 화면 6장은 배포본에서 그대로 찍었다: 취향 설정 7단계 → 메인(앵커 행) → 책 상세 → 뷰어 완독 → 취향 재설정 → 메인 변화(report/figures/p5_01_onboarding.png ~ p5_06_home_after.png).`
      그림 파일은 repo 에 없다(밀리 표지 공개 금지 — 결정 '그림은 repo 밖'(.planning/phases/07-deploy/07-CONTEXT.md D-07)) → **draft 에는 파일명만** 적고 Notion 조판에서 로컬 파일을 붙인다. 마크다운 `![](...)` 이미지 링크를 넣지 않는다(`make pdf` HTML 미리보기가 깨진다).
    - **§3-3** 표 아래 기존 실측 문단 **뒤에** 배포 실측 1줄 추가:
      `- (Day 2·Day 4 실측, Phase 7 '배포') 같은 이미지가 로컬 docker 와 Railway 에서 똑같이 뜬다. 재배포는 정지→시작이라 <downtime_s>초 동안 응답이 끊겼고(2026-09-06 실측), 그 사이에도 볼륨 `/data` 의 SQLite 는 유지되어 재시작 후 사용자·스냅샷 행이 그대로였다. 가동은 5분 간격 헬스체크로 감시한다.`
      숫자는 07-02 SUMMARY 의 `downtime_s` 를 그대로 복사한다(손으로 쓰지 않는다). §3-2 기술 스택 표의 배포·관측 행은 **손대지 않는다**(문구가 이미 맞다).
    - draft 의 다른 절·§4-3 관측 행·5장 나머지 문장은 건드리지 않는다. `grep -c '\[Phase 7\]' report/draft.md` 가 0 이 되고 `grep -c '\[Phase N\]' report/draft.md` 는 **1 로 유지**되는지(L3 조판 범례) 확인한다.
  </action>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && for f in p5_01_onboarding p5_02_home p5_03_detail p5_04_reader_done p5_05_refresh p5_06_home_after p5_qr; do test -s "report/figures/$f.png" || { echo "MISSING $f"; exit 1; }; done && test "$(ls report/figures/p5_*.png | wc -l | tr -d ' ')" -eq 7 && test "$(git status --short -- report/figures | wc -l | tr -d ' ')" -eq 0 && test "$(git ls-files | grep -icE 'pdf|png|assets')" -eq 0 && test "$(grep -c '\[Phase 7\]' report/draft.md)" -eq 0 && test "$(grep -c '\[Phase N\]' report/draft.md)" -eq 1 && grep -q "up.railway.app" report/draft.md && test -z "$(git status --short -- pyproject.toml uv.lock)" && echo FIGURES_AND_DRAFT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `report/figures/p5_*.png` 7개 존재·크기 >0 · `shoot.py` 출력 `console_errors == []`(SUMMARY 에 JSON)
    - `git status --short -- report/figures` 빈 출력 · `git ls-files | grep -icE "pdf|png|assets"` == 0(D-07·D-08 유지)
    - `git status --short -- pyproject.toml uv.lock` 빈 출력(qrcode 임시 설치 — 의존성 추가 0)
    - `grep -c '\[Phase 7\]' report/draft.md` == 0 이고 `grep -c '\[Phase N\]' report/draft.md` == 1(L3 조판 범례 유지) · `grep -c "up.railway.app" report/draft.md` ≥ 1
    - §3-3 새 문장의 다운타임 숫자 == 07-02 SUMMARY 의 `downtime_s`(두 값 나란히 SUMMARY 에)
    - `git diff --stat report/draft.md` 가 2곳 근처만(다른 절 변경 0)
  </acceptance_criteria>
  <done>PDF 5장 재료 완성(URL·QR·그림 6장, 전부 repo 밖) · draft 마커 0 · §3-3 배포 실측 1줄.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 기록 4곳(PROGRESS 결정 로그·개발일지 D81·STATE·설계서 §2-1) + 운영 인계 → 문서 커밋 승인(push 는 별도 결정)</name>
  <files>../PROGRESS.md, ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md, .planning/STATE.md, ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md</files>
  <read_first>
    - ../.assets/개발일지/README.md "작성 규칙" 6요소 · "항목 템플릿" · `### D80` 항목(같은 파일 L91 — 형식·길이 참고, 새 번호는 **D81**)
    - ../PROGRESS.md "## 결정 로그" 표 마지막 행 · "## 미결" 5번(계정) — **Edit 직전 재읽기**(다른 세션이 편집한다)
    - .planning/STATE.md frontmatter(`progress`·`stopped_at`) · "## Current Position" · "### Day 게이트" 표 2·4행
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md L74(§2-1 그림 주석 "수십 초 다운")
    - ../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/02_배포_절차_및_설정.md §5 운영 · §6 롤백 · §7 장애 대응표 · §8 철거
  </read_first>
  <action>
    **자동 부분(사용자 확인 전) — 네 곳을 재읽기 후 최소 편집한다.**

    **4-a `../PROGRESS.md`** — "## 결정 로그" 표 끝에 2행 append(형식 `| 날짜 | 결정 | 이유 | 영향 |`):
    - 1행: `| 09-06 | **Phase 7 '배포' 완료 — 배포 URL <BASE>(Railway Hobby, region <…>, volume /data 1GB)** · 재배포 다운타임 실측 <n>초 · UptimeRobot 5분 감시 + 상태 페이지 <URL> | 결정 '스켈레톤을 지금 한다'·'본배포는 재배포다'·'다운타임 실측 기록'(.planning/phases/07-deploy/07-CONTEXT.md D-01·D-02·D-14) · 배포 02 §3-2·§4-4 | `report/draft.md` §3-3·§5-3 · `.planning/STATE.md` Day 게이트 2·4 · 아키텍처 01 §2-1 |`
    - 2행: `| 09-06 | **요구사항 'Sentry·Grafana Cloud scrape 연결'(.planning/REQUIREMENTS.md DEPLOY-04) 폐기 확정** — `/metrics`·prometheus-client·Grafana 를 만들지 않았고 배포본에서 `/metrics` 404 로 확인 | 결정 'Grafana 폐기'(.planning/phases/07-deploy/07-CONTEXT.md D-05) = 결정 'Should 꼬리…'(개발일지 2026-09-06 파일 항목 D76) 유지. PDF 는 "설계만 — 관측" 1줄(draft §3-2·§4-3, 이미 반영) | `.planning/ROADMAP.md` Phase 7 Should 꼬리 · `.planning/REQUIREMENTS.md` DEPLOY-04 |`
    - "## 미결" 5번(계정) 을 `~~계정~~ → **해소(09-06)**: Railway Hobby 결제·Usage Limit $15 · UptimeRobot 모니터·상태 페이지 · Sentry DSN 주입 완료. Grafana Cloud 는 폐기(DEPLOY-04).` 로 바꾼다(취소선 유지 관례).
    - Task 2 에서 "인계" 로 분류된 Codex 지적이 있으면 미결에 1줄 추가한다.
    - **`.planning/REQUIREMENTS.md` 는 이 플랜에서 편집하지 않는다**(GSD 소유 파일 — 상태 반영은 `/gsd-verify-work 7` 또는 Advisor 별도 판단). 대신 PROGRESS 2행이 근거로 남는다.

    **4-b 개발일지 `### D81`** — `../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md` 의 `### D80` 항목 **뒤, "## 고민 포인트" 앞**에 6요소 1항목을 추가한다(항목당 12줄 이내, 각 요소 1~2줄·결과만 3줄). 제목:
    `### D81. Phase 7 '배포' 실행 — 스켈레톤·본배포 2회 push 로 URL 확정, 다운타임 <n>초 실측, DEPLOY-04 폐기 확정  [축 ③·실서비스 · 아키텍처 01 §5·§2-1 / 배포 02 §3·§4·§5 / `millie-rec/.planning/phases/07-deploy/07-01~04-PLAN.md`]  (확정 — Advisor 실행 + 사용자 승인 09-06)`
    내용 지침(사실만, 숫자는 SUMMARY 에서 복사):
    - **문제:** 배포는 사용자 시간(결제·대시보드 35분)이 드는 유일한 항목인데 빌드·볼륨 권한·PORT 결함은 배포 시점에만 드러난다.
    - **동기:** PDF 5장의 URL·QR·스크린샷 6장이 이 페이즈에만 달려 있고, Day 4 에 처음 시도하면 90분 규칙을 넘긴다.
    - **선택지:** 2회 배포(스켈레톤 → 본배포, 채택) · Day 4 한 번에 · 로컬 시연만(§9 대체 경로, 보류).
    - **근거:** 실측을 적는다 — 로컬 docker 스모크 통과 후 push, 첫 빌드 결과, `/health` `db_ok`·`model_version`, Restart 전후 `db_row_count.users`, 다운타임 `<n>`초, 4 variant 전환 시 카드 변화.
    - **장애:** 실제로 겪은 것만(빌드 실패·헬스체크·볼륨 권한·Codex 지적 등). 없었으면 "없음" + 왜 없었는지 한 구절(예: Dockerfile 3조건이 architect 검토에서 이미 고쳐져 있었다).
    - **결과:** 반영 위치(`report/draft.md` §3-3·§5-3 · `PROGRESS.md` · `STATE.md` · 아키텍처 01 §2-1) / 잃은 것(월 $5~8 · Grafana 관측) / 되돌리는 조건(심사 결과 통보 +7일 철거 — 배포 02 §8, 또는 배포 장애 90분 초과 시 §9 로컬 시연).
    추가 전 `grep -h '^### D' ../.assets/개발일지/*.md | tail -3` 로 마지막 번호가 아직 D80 인지 확인한다(다른 세션이 D81 을 먼저 쓸 수 있다 — 그러면 다음 번호로).

    **4-c `.planning/STATE.md`** — 재읽기 후: Day 게이트 표 2행의 배포 항목 ✅(이미 07-02 에서 처리됐으면 유지) · 4행 "배포 URL에서 쇼케이스 → 관제 대시보드 완주, variant 전환 시 책이 바뀐다" ✅(07-03) 확인 · "## Current Position" 을 `Phase: 7 (배포) ✅ 완료 — 배포 URL <BASE> · 다음은 Phase 8 'PDF 제출물'` 로 · frontmatter `stopped_at` 을 Phase 7 완료 문장으로 · `progress.completed_phases`·`completed_plans`·`percent` 를 실제 값으로 갱신. **Day 표의 다른 행·다른 절은 건드리지 않는다.**

    **4-d 설계서 아키텍처 01 §2-1(L74)** — `수십 초 다운` 을 `<n>초 다운(2026-09-06 실측, 배포 02 §3-2)` 으로 **1곳만** 교체(배포 02 §3-2 의 지시 "아키텍처 문서 §2-1 '수십 초' 문구를 실측값으로 교체"). L79·L297 은 "실측해 기록" 이라 이미 참 — 손대지 않는다. 그림 주석의 정렬(선행 공백)을 유지한다.

    **4-e 운영 인계(07-04-SUMMARY 에 기록, 별도 파일 없음)** — 배포 02 §5·§6·§7·§8 을 이 프로젝트의 현재 사실로 요약한다:
    - 심사 기간 점검(§5): UptimeRobot 알림 → Railway Deployments 로그 → §7 표 · Railway Usage 주 1회($15 하드 캡) · Day 5 이후 코드 변경 금지 · `FREEZE_BOOK_STATS=1` 은 필요 시 Variables 추가.
    - **사실 정정:** §5 백업 행의 `GET /api/admin/export` 는 **미구현**이고 `METRICS_TOKEN` 도 없다(결정 'Grafana 폐기'(.planning/phases/07-deploy/07-CONTEXT.md D-05)) → 백업 수단은 Railway 볼륨뿐. 데이터 유실 시 데모 상태가 초기화될 뿐 PDF 숫자(`results/`)는 영향 없음.
    - 롤백(§6) 1줄: Railway → Deployments → 직전 성공 배포 → **Redeploy**(1클릭). 코드 롤백은 `git revert <sha> && git push`.
    - 장애 대응 요약(§7) 상위 4줄: `ModuleNotFoundError: millie_rec` → 두 번째 `uv sync` · `unable to open database file` → 볼륨/`DATA_DIR` · 헬스체크 반복 실패 → `${PORT}` · 표지 안 뜸 → 밀리 CDN 핫링크(플레이스홀더가 정상, 다운로드 금지).
    - 철거(§8): 심사 결과 통보 +7일 → Railway 서비스 Delete(볼륨 포함, 과금 중단) → Sentry·UptimeRobot 정리 → PROGRESS 종료 기록. 캘린더 알림은 사용자 몫.

    **재개 후(사용자 커밋 승인):** `git add .planning/ report/draft.md && git commit -m "docs(07-04): Phase 7 배포 마감 — URL·QR·캡처 6장·다운타임 실측·DEPLOY-04 폐기 기록"`. `../PROGRESS.md`·`../.assets/**` 는 `millie-rec/` **밖**이라 이 repo 의 커밋 대상이 아니다. push 는 사용자가 별도로 승인할 때만(아래 4번 항목).
  </action>
  <what-built>
    Advisor 가 자동으로 마친 것: Task 1 게이트 12행 · Codex 3분류 + 승인 처리(Task 2) · 캡처 6장 + QR + draft 2곳(Task 3) · 기록 4곳 초안(PROGRESS 2행 + 미결 5 해소 · 개발일지 D81 · STATE · 아키텍처 01 §2-1 다운타임 실측).
  </what-built>
  <how-to-verify>
    1. `git diff report/draft.md` — 바뀐 곳이 §5-3 한 줄과 §3-3 한 줄뿐인지. 5장 문장에 실제 URL 이 들어갔고 그림은 **파일명으로만** 언급되는지(repo 에 안 들어간다 — 결정 '그림은 repo 밖'(.planning/phases/07-deploy/07-CONTEXT.md D-07)).
    2. `report/figures/` 의 7장을 열어 본다 — 6장이 draft §5-3 이 약속한 순서(취향 설정 → 메인 → 상세 → 뷰어 완독 → 재설정 → 메인 변화)이고 폰 프레임·2x 로 또렷한지, QR 을 폰 카메라로 찍으면 배포 URL 로 가는지. **여기가 PDF 에 실제로 들어갈 그림이다** — 마음에 안 드는 컷이 있으면 어느 컷인지 말해 주면 다시 찍는다.
    3. 개발일지 `### D81` 을 읽는다 — 6요소가 다 있고 **선택지에 버린 대안이 있는지**(없으면 미완성 — 개발일지 README 규칙), 숫자가 실측인지.
    4. **push 여부 결정:** 이 커밋은 문서만이다. push 하면 Railway 가 한 번 더 재배포한다(다운타임 약 <n>초, 07-02 실측). 선택지 셋 중 하나로 답한다 — ① `commit only`(권장: 심사 기간 다운타임 0, GitHub 는 나중에 한 번에) ② `push ok`(원격 문서 최신화, 재배포 1회 감수) ③ `push ok, watch paths 설정함`(07-02 Task 3 선택 5 를 적용했으면 재배포 없음).
    5. 문제가 없으면 `"커밋 ok / <위 선택지>"`.
  </how-to-verify>
  <resume-signal>"커밋 ok / commit only" 또는 "커밋 ok / push ok" (다시 찍을 컷이 있으면 파일명과 이유를 함께)</resume-signal>
  <verify>
    <automated>cd "/Users/shinwonchul/Documents/신원철/밀리의서재/millie-rec" && grep -q "up.railway.app" ../PROGRESS.md && grep -q "DEPLOY-04" ../PROGRESS.md && grep -q "### D81\." "../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md" && grep -q "Phase 7" .planning/STATE.md && test "$(grep -c '수십 초 다운' "../.assets/설계서/시스템 아키텍처, 기술 스택 및 배포(프론트, 백엔드 서빙)/01_시스템_아키텍처_기술스택_배포.md")" -eq 0 && test "$(git ls-files | grep -icE 'pdf|png|assets')" -eq 0 && git log --oneline -1 | grep -q "07-04" && test "$(git status --short -- .planning report/draft.md | wc -l | tr -d ' ')" -eq 0 && echo PHASE7_CLOSED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "up.railway.app" ../PROGRESS.md` ≥ 1 · 결정 로그 2행(배포 완료 / DEPLOY-04 폐기) · 미결 5 취소선 + 해소 문구
    - `grep -c "### D81\." ../.assets/개발일지/2026-09-06_Day2_Phase4_파이프라인과_freeze.md` == 1 · 6요소 6줄 전부 존재 · 항목 12줄 이내 · 선택지 2개 이상
    - `.planning/STATE.md` Current Position 이 Phase 7 완료 · Day 게이트 2·4 ✅ · frontmatter `progress` 갱신
    - `grep -c '수십 초 다운' <아키텍처 01>` == 0 이고 실측 숫자가 그 자리에(다른 줄 변경 0 — `git diff --stat` 로 확인, 단 이 파일은 repo 밖이라 `diff` 대신 편집 전후 줄 비교)
    - `git log --oneline -1` 에 `07-04` 문서 커밋 · `git status --short -- .planning report/draft.md` 빈 출력 · `git ls-files | grep -icE "pdf|png|assets"` == 0
    - push 결정(①/②/③)이 SUMMARY 에 기록 — `push ok` 였다면 `git rev-list --count origin/main..HEAD` == 0, 아니면 ≥ 1(로컬 보관, 의도된 상태)
    - 07-04-SUMMARY 에 운영 인계 5항목(점검·백업 사실 정정·롤백·장애 4줄·철거)
  </acceptance_criteria>
  <done>기록 4곳 완료 · 운영 인계 기록 · 문서 커밋 1(사용자 승인) · Phase 7 '배포' 종료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 `report/figures/` → PDF 조판(Notion) | 밀리 표지 이미지가 들어간 캡처가 repo·공개 채널로 새면 저작권 문제(`../.claude/rules/data.md`) |
| 작업 트리 → Codex(외부 모델) | 소스 diff 전송(2회차 = `20d5fa3` 이후 전체) |
| Playwright(Advisor) → 배포 DB | 캡처 완주가 실제 SQLite 에 사용자·이벤트 행을 남긴다 |
| 문서 커밋 → GitHub public → Railway | push 시 재배포 1회 + 문서 내용 공개 |
| 외부 SaaS 계정(Railway·Sentry·UptimeRobot) | 심사 기간 유지·철거 책임이 사용자에게 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Information Disclosure | 캡처 7장이 repo 로 유입 | mitigate | `.gitignore` `report/figures/`(D-07) + Task 1 표 2 사전 확인 + Task 3 `git status --short -- report/figures` 빈 출력 + Task 4 `git ls-files` grep 0 — 3중 |
| T-07-04-02 | Information Disclosure | draft 에 배포 URL 외 시크릿(DSN·토큰) 이 섞임 | mitigate | draft·PROGRESS·SUMMARY 에 URL·상태 페이지만 적는다. DSN 문자열은 어디에도 기록하지 않는다(07-03 T-07-03-02 계속) |
| T-07-04-03 | Tampering | 캡처 완주가 심사자에게 보일 데모 DB 를 오염 | accept | 가명 `probe-*`·데모 데이터. 대시보드 수치는 "관측 존재" 증거로만 쓰고 PDF 본문에 인용하지 않는다(`../.claude/rules/serving.md`) |
| T-07-04-04 | Denial of Service | 문서 push 가 심사 중 재배포·다운타임 유발 | mitigate | push 를 기본으로 하지 않는다(`push_policy`). 사용자 선택 ①/②/③ + Watch Paths 안내. 다운타임은 07-02 실측값으로 사전 고지 |
| T-07-04-05 | Tampering | `qrcode` 임시 설치가 `pyproject.toml`·`uv.lock` 을 오염 | mitigate | `uv run --with` 일회성 환경(Makefile `millie-collect` 선례) + Task 3 에서 `git status --short -- pyproject.toml uv.lock` 빈 출력 확인 |
| T-07-04-06 | Repudiation | "게이트 통과·감시 동작" 주장의 근거 부재 | mitigate | 게이트 12행 출력·5회 폴링 결과·UptimeRobot 가동률·Codex 3분류를 SUMMARY 에 verbatim |
| T-07-04-07 | Information Disclosure | 철거 누락으로 데모 DB·계정이 무기한 공개 | mitigate | 배포 02 §8 철거 절차를 07-04-SUMMARY 운영 인계에 명시(심사 통보 +7일, 캘린더 알림은 사용자 몫) |
</threat_model>

<verification>
- Task 1: 게이트 12행 전부 기대값(`git ls-files` grep 0 · `/health` 5/5 200 · `/metrics` 404 · pytest failed 0 · `make smoke` PASS)
- Task 2: Codex 3분류 ≤10줄 + 범위 sha 명시 + 사용자 승인 처리 · UptimeRobot Up·5분 간격 사용자 확인
- Task 3: `report/figures/p5_*.png` 7장(>0 바이트, repo 밖) · `console_errors []` · draft `[Phase 7]` 0개(L3 `[Phase N]` 범례는 유지) · `pyproject.toml`·`uv.lock` 무변경
- Task 4: PROGRESS 2행 + 미결 5 해소 · 개발일지 `### D81` 6요소 · STATE Phase 7 완료 · 아키텍처 01 §2-1 `수십 초 다운` 0건 · 문서 커밋 1 · push 결정 기록
</verification>

<success_criteria>
- **DEPLOY-03** 완결 = ROADMAP Success Criteria 3(`git ls-files | grep -iE "pdf|png|assets"` 빈 결과 + UptimeRobot 5분 간격 `/health` 감시) — 배포 **후** 상태로 재확인
- **DEPLOY-01·DEPLOY-02** 의 기록 측 마감 — 배포 URL·다운타임 실측·완주 결과가 `../PROGRESS.md`·개발일지 D81·`.planning/STATE.md`·`report/draft.md` 네 곳에 남는다
- 요구사항 'Sentry·Grafana Cloud scrape 연결'(.planning/REQUIREMENTS.md DEPLOY-04) **폐기 사실이 문서에 남는다** — 결정 'Grafana 폐기'(.planning/phases/07-deploy/07-CONTEXT.md D-05)·결정 'Phase 7 배포 범위 확정'(개발일지 2026-09-06 파일 항목 D80)
- PDF 5장 재료 완성: 배포 URL · QR · 캡처 6장(전부 repo 밖) · `report/draft.md` 의 `[Phase 7]` 마커 0개(L3 조판 범례는 유지) → Phase 8 'PDF 제출물' 착수 가능
- Codex 필수 리뷰(`../.claude/rules/codex-review.md` "브랜치 전체 vs main" 행) 마감 · 운영 인계(배포 02 §5·§6·§7·§8) 기록
</success_criteria>

<output>
완료 후 `.planning/phases/07-deploy/07-04-SUMMARY.md`: 게이트 12행 결과표(폴링 5회·`db_row_count` 전후 포함) · Codex 2회차 3분류 표 + 범위 sha + 사용자 승인·처리 · UptimeRobot 상태(Up·가동률·간격) · `shoot.py` 경로 + `console_errors` JSON + 캡처 7장 목록(파일명·크기) + QR 검증 · draft 변경 2곳 diff 발췌 · PROGRESS 2행·개발일지 D81 제목·STATE 갱신 줄·아키텍처 01 §2-1 전후 1줄 · **운영 인계 5항목**(심사 기간 점검 · 백업 사실 정정 `GET /api/admin/export` 미구현 · 롤백 1줄 · 장애 대응 4줄 · 철거 시점) · 커밋 sha + push 결정(①/②/③) · Phase 7 종료 선언과 Phase 8 'PDF 제출물' 착수 조건.
</output>
